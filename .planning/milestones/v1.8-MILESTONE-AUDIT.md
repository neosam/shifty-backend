---
milestone: v1.8
milestone_name: Freiwilligen-Auswahl & Urlaubsanspruch-Korrektur (HR-UX)
audited: 2026-06-29
status: passed
scores:
  requirements: 2/2
  phases: 2/2
  integration: 100%
  flows: 2/2
gaps: {}   # No critical blockers
tech_debt:
  - phase: meta / .planning
    items:
      - "REQUIREMENTS.md is frozen at v1.7 — the v1.8 requirements VOL-SEL-01 and VAC-OFFSET-01 are defined only in ROADMAP.md and never added to the REQUIREMENTS.md traceability table. Documentation drift; both requirements are fully traced via ROADMAP → SUMMARY/VERIFICATION and verified."
  - phase: 28-urlaubsanspruch-korrektur-offset
    items:
      - "The four 28-*-SUMMARY.md files carry no `requirements-completed: [VAC-OFFSET-01]` frontmatter (Phase 27's SUMMARY does list VOL-SEL-01). Cosmetic frontmatter gap — VAC-OFFSET-01 is fully covered by 28-VERIFICATION.md (6/6 SC + live smoke)."
      - "DAO `find_by_id` on VacationEntitlementOffsetDao is implemented but currently unconsumed — a documented forward hook for a future DELETE /{id} route, not a v1.8 wiring gap."
---

# Milestone v1.8 — Freiwilligen-Auswahl & Urlaubsanspruch-Korrektur (HR-UX) — Audit Report

**Audited:** 2026-06-29
**Status:** ✅ PASSED
**Scope:** Phases 27 + 28 (Phases 18–26 belong to earlier milestones v1.5/v1.6/v1.7; Phase 999.1 is backlog, off-theme.)

## Milestone Goal

HR-UX rund um Abwesenheiten/Urlaub: Freiwillige sind in den Abwesenheits-Selektoren
auswählbar, und HR kann den berechneten Jahres-Urlaubsanspruch per Korrektur-Offset
anpassen.

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY frontmatter | ROADMAP traceability | Integration | Final |
|--------|-------|-----------------|---------------------|----------------------|-------------|-------|
| VOL-SEL-01 | 27 | passed (6/6 SC, live HR smoke) | listed `[VOL-SEL-01]` | defined in ROADMAP (not in REQUIREMENTS.md table) | WIRED | **satisfied** |
| VAC-OFFSET-01 | 28 | passed (6/6 SC, live HR smoke + dev-proxy bug fixed) | not listed (cosmetic gap) | defined in ROADMAP (not in REQUIREMENTS.md table) | WIRED | **satisfied** |

**Coverage: 2/2 requirements satisfied.** No unsatisfied, no partial-blocking, no orphaned requirements.

> Note: REQUIREMENTS.md was never advanced past v1.7, so neither v1.8 requirement appears
> in its traceability table. This is documentation drift, **not** an orphaned-requirement
> blocker (orphan = in traceability but never verified; here it is the inverse — verified
> but absent from the table). Both requirements are independently confirmed by their phase
> VERIFICATION.md files and the cross-phase integration check.

## Phase Verifications

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 27 — Freiwillige in Abwesenheitsliste auswählbar (FE) | passed | 6/6 SC | Live HR browser smoke: grouped Employees/Volunteers optgroups in AbsenceModal + AbsenceFilterBar; inactive excluded; no empty optgroup; i18n de/en/cs. `behavior_unverified: 0`. |
| 28 — Urlaubsanspruch-Korrektur via Offset (BE+FE) | passed | 6/6 SC | Live HR browser smoke: read-path (entitled 16 = round(15)+1), HR inline editor, write-path persisted (offset 3 → effective 18), API-level hiding for non-HR. Off-by-one proration fix + snapshot bump 11→12. One dev-proxy bug (HTTP 405 on `/vacation-entitlement-offset`) found & fixed during smoke. `behavior_unverified: 0`. |

Both phases also delivered two extra bugfixes found during the live smokes (committed):
`fix(28)` Dioxus.toml dev-proxy registration, and a fix for AbsenceModal not closing
after a clean (no-warnings) Create/Update.

## Cross-Phase Integration — 100% (no blockers, no warnings)

Independent gsd-integration-checker pass confirmed every expected connection wired end-to-end:

- **Flow A (VOL-SEL-01):** pure `grouped_selectable()` → shared `grouped_person_options()`
  RSX helper → consumed by BOTH AbsenceModal selector and AbsenceFilterBar HR filter
  (single helper, no copy-paste); i18n in all three locales. COMPLETE.
- **Flow B (VAC-OFFSET-01):** FE editor (double-gated `is_hr && computed.is_some()`) →
  `SaveOffset` → loader → api POST → Dioxus dev-proxy (`/vacation-entitlement-offset`
  present) → REST route (mounted + ApiDoc nested, HR gate) → Basic offset service →
  DAO/table; read path: offset added **after** `.round()` in `vacation_balance.rs`,
  flows into `remaining_days`; API-level hiding sets breakdown fields `Some` only for HR.
  Service tiering correct (Business-Logic `VacationBalanceService` → Basic offset service,
  no cycle). Migration additive (partial unique index `WHERE deleted IS NULL`). Snapshot
  `CURRENT_SNAPSHOT_SCHEMA_VERSION == 12`, guard tests re-pinned. COMPLETE.

Phases 27 and 28 share only `shifty-dioxus/src/page/absences.rs`, touched in
non-conflicting regions (P27: selector helpers; P28: `VacationContractCell`).

## Automated Gates (from phase VERIFICATIONs)

- Backend `cargo test --workspace` — 0 failures (incl. new offset/balance/off-by-one/snapshot-guard tests).
- Backend `cargo clippy --workspace -- -D warnings` — clean (hard CI gate).
- Frontend WASM build (`cargo build --target wasm32-unknown-unknown`) — 0 errors.
- Frontend `cargo test` — 678 passed, 0 failed; no new clippy warnings vs baseline.
- sqlx migration applied additively (never reset); `.sqlx/` offline cache regenerated.

## Tech Debt (non-blocking)

1. **REQUIREMENTS.md drift** — advance it to v1.8 (or document that v1.8 requirements live
   in ROADMAP) and add VOL-SEL-01 / VAC-OFFSET-01 to a traceability table. Best fixed during
   `/gsd-complete-milestone v1.8`.
2. **Phase 28 SUMMARY frontmatter** — add `requirements-completed: [VAC-OFFSET-01]` to one of
   the 28-*-SUMMARY.md files for parity with Phase 27 (cosmetic).
3. **`find_by_id` forward hook** — unconsumed by design; either wire a future `DELETE /{id}`
   or drop it when the future need is dismissed.

## Nyquist

`workflow.nyquist_validation` toggle is inactive in `.planning/config.json` → Nyquist
compliance discovery skipped (no active validate-phase step hook for this milestone).

---

*Audit by /gsd-audit-milestone orchestrator, 2026-06-29. Phase VERIFICATION.md files
(both `passed`, `behavior_unverified: 0`, live HR browser smokes) and an independent
100% cross-phase integration check are authoritative.*
