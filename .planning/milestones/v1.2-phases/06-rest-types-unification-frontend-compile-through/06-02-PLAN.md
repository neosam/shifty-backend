---
phase: 6
plan: 2
slug: slot-capacity-state-mirror
wave: 2
depends_on: [1]
files_modified:
  - shifty-dioxus/src/state/shiftplan.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/component/week_view.rs
  - shifty-dioxus/src/component/day_aggregate_view.rs
requirements: [RT-03, FC-01]
must_haves: [M-04, M-05]
autonomous: true

must_haves:
  truths:
    - "`state::shiftplan::Slot` hat `max_paid_employees: Option<u8>` und `current_paid_count: u8` als zusätzliche Felder."
    - "`From<&SlotTO> for Slot` mappt das neue Feld `max_paid_employees`; `current_paid_count` defaulted auf `0`."
    - "`loader.rs::load_shift_plan` und `load_day_aggregate` reichen `current_paid_count` aus `ShiftplanSlotTO` und `max_paid_employees` aus `SlotTO` durch."
    - "Test-Fixtures (`make_slot` in week_view.rs und day_aggregate_view.rs, sowie `let slot = Slot {...}` in week_view.rs:1599) konstruieren Slot mit Default-Werten für die neuen Felder."
    - "Weekday::from_num_from_monday liefert für invalide Inputs einen defensiven Fallback (Weekday::Monday) statt zu panicen (FC-01-Subziel: Weekday-Panic-Defense)."
    - "Cluster C (`ShiftplanDayTO.unavailable`) wird durch loader.rs als no-op behandelt — Feld wird ignoriert; cargo check zeigt KEINE `no field`/`unresolved variant`-Fehler für `ShiftplanDayTO.unavailable` in loader.rs."
    - "Cluster G (`BillingPeriodTO.snapshot_schema_version`) erfordert keinen Code-Change im Frontend — Feld existiert auf dem Backend-Type, wird aber von keinem Frontend-Konsumenten gelesen; cargo check zeigt KEINE `no field`-Fehler für `BillingPeriodTO.snapshot_schema_version`."
  artifacts:
    - path: "shifty-dioxus/src/state/shiftplan.rs"
      provides: "Slot-State-Typ mit zwei neuen Feldern + Mirror in From<&SlotTO>; defensiver Weekday-Fallback"
      contains: "max_paid_employees"
    - path: "shifty-dioxus/src/loader.rs"
      provides: "load_shift_plan + load_day_aggregate reichen die neuen Slot-Felder durch; ShiftplanDayTO.unavailable wird per UI-SPEC Regel 2 ignoriert (kein State-Mirror in v1.2 — kein ShiftplanDay-State-Typ existiert)"
      contains: "current_paid_count"
  key_links:
    - from: "shifty-dioxus/src/loader.rs"
      to: "shifty-dioxus/src/state/shiftplan.rs"
      via: "Slot-Konstruktion mit current_paid_count + max_paid_employees aus ShiftplanSlotTO bzw. SlotTO"
      pattern: "current_paid_count:\\s*slot\\.current_paid_count"
    - from: "shifty-dioxus/src/loader.rs"
      to: "shifty-backend/rest-types/src/lib.rs (ShiftplanDayTO.unavailable)"
      via: "Feld wird durch days.iter().flat_map(|day| day.slots.iter()) ignoriert — kein Lese-Pfad in v1.2"
      pattern: "flat_map\\(\\|day\\| day\\.slots\\.iter"

# FC-01-Scope-Hinweis (Revision iter 1):
# Plan 06-02 deckt von FC-01 NUR das Weekday-Panic-Subziel ab (state/shiftplan.rs:59).
# Match-Arm-Exhaustivität für WarningTO (5 Varianten), UnavailabilityMarkerTO (3 Varianten) und
# InvitationStatus an Render-Sites wandert nach Plan 06-04 (data-driven aus dem WASM-Build-Output;
# Pre-Survey-Sites sind in 06-04 must_haves.artifacts dokumentiert).
---

<objective>
Cluster E (RESEARCH §2): Slot-Capacity-Felder in den Frontend-State mirroren.
`SlotTO.max_paid_employees: Option<u8>` und `ShiftplanSlotTO.current_paid_count: u8`
sind nach Wave 1 in `rest_types` verfügbar; Frontend muss sie aus Backend-Antworten
in den State-Mirror übernehmen — OHNE sie zu rendern (UI-SPEC Regel 2; FUI-01/02 in v1.3).

Cluster C (`ShiftplanDayTO.unavailable`) und Cluster G (`BillingPeriodTO.snapshot_schema_version`)
werden in diesem Plan als **no-op** behandelt: beide Felder existieren nach Wave 1 auf den
Backend-Types, werden aber von keinem Frontend-Konsumenten gelesen. Konkret prüfbar in
loader.rs: `flat_map(|day| day.slots.iter())` ignoriert `day.unavailable` strukturell;
für `BillingPeriodTO.snapshot_schema_version` gibt es laut RESEARCH keinen aktiven Lese-Pfad
im Frontend. Acceptance Criteria sichern beide Befunde via cargo-check-Negativtests ab.

Zusätzlich: defensive Fallback in `Weekday::from_num_from_monday` (UI-SPEC Regel 3) — das
**FC-01-Subziel dieses Plans**. Voll-Exhaustivität für `WarningTO`/`UnavailabilityMarkerTO`/
`InvitationStatus` ist Plan 06-04-Scope (data-driven über WASM-Build-Output).

Purpose: RT-03 strukturell für die 2 SlotTO/ShiftplanSlotTO-Felder + no-op-Verifikation für
Cluster C+G; FC-01-Subziel für Weekday-Panic-Defense.
Output: State-Mirror-Felder, durchreichende Mappings in loader.rs, fixture-Updates,
defensiver Weekday-Fallback, no-op-Verifikation für ShiftplanDayTO.unavailable und
BillingPeriodTO.snapshot_schema_version.

Visuelles Delta = 0 (UI-SPEC Regel 4): kein neues Render, kein neues UI.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-RESEARCH.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-UI-SPEC.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-VALIDATION.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-01-SUMMARY.md
@CLAUDE.md
@shifty-backend/CLAUDE.md
@shifty-backend/CLAUDE.local.md

<interfaces>
<!-- Backend-rest-types post-Wave-0 (verifizierte Stellen): -->

```rust
// rest-types/src/lib.rs ~Zeilen 306-321: SlotTO
pub struct SlotTO {
    pub id: Uuid,
    pub day_of_week: DayOfWeekTO,
    pub from: time::Time,
    pub to: time::Time,
    pub min_resources: u8,
    // ... ggf. weitere Felder ...
    pub max_paid_employees: Option<u8>,
}

// rest-types/src/lib.rs ~Zeilen 977-987: ShiftplanSlotTO
pub struct ShiftplanSlotTO {
    pub slot: SlotTO,
    pub bookings: Arc<[BookingWithSalesPersonTO]>,
    pub current_paid_count: u8,
}

// rest-types/src/lib.rs ~Zeile 997: ShiftplanDayTO
pub struct ShiftplanDayTO {
    // ... bestehende Felder ...
    pub unavailable: Option<UnavailabilityMarkerTO>,  // Cluster C — wird in v1.2 ignoriert
}

// rest-types/src/lib.rs ~Zeile 1312: BillingPeriodTO
pub struct BillingPeriodTO {
    // ... bestehende Felder ...
    pub snapshot_schema_version: u32,  // Cluster G — diagnostic; wird in v1.2 nicht gelesen
}
```

<!-- Frontend Slot-Definition (state/shiftplan.rs:164-198, Ist-Zustand): -->
```rust
#[derive(Clone, Debug, PartialEq, Eq)]
pub struct Slot {
    pub id: Uuid,
    pub day_of_week: Weekday,
    pub from: time::Time,
    pub to: time::Time,
    pub bookings: Rc<[Booking]>,
    pub min_resources: u8,
}

impl From<&SlotTO> for Slot {
    fn from(slot: &SlotTO) -> Self {
        Self {
            id: slot.id,
            day_of_week: slot.day_of_week.into(),
            from: slot.from,
            to: slot.to,
            bookings: [].into(),
            min_resources: slot.min_resources,
        }
    }
}
```

<!-- Slot-Literal-Konstruktionen (alle MÜSSEN aktualisiert werden): -->
<!-- - state/shiftplan.rs:189 (in From<&SlotTO> Impl) -->
<!-- - loader.rs:132 (in update-Map mit `..slot`) — KEIN Update nötig (`..slot` propagiert die neuen Felder) -->
<!-- - loader.rs:162 (load_shift_plan, |slot| Slot { ... }) -->
<!-- - loader.rs:213 (load_day_aggregate, |slot| Slot { ... }) -->
<!-- - component/week_view.rs:1356 (test-Fixture make_slot) -->
<!-- - component/week_view.rs:1599 (test-Fixture inline `let slot = Slot { ... }`) -->
<!-- - component/day_aggregate_view.rs:212 (test-Fixture make_slot) -->

<!-- Weekday-Panic-Site (state/shiftplan.rs:50-61): -->
```rust
pub fn from_num_from_monday(num: u8) -> Self {
    match num {
        0 => Weekday::Monday,
        1 => Weekday::Tuesday,
        2 => Weekday::Wednesday,
        3 => Weekday::Thursday,
        4 => Weekday::Friday,
        5 => Weekday::Saturday,
        6 => Weekday::Sunday,
        _ => panic!("Invalid weekday number: {}", num),
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Slot-State-Struct + From-Impl + Weekday-Panic-Defense in state/shiftplan.rs</name>
  <files>shifty-dioxus/src/state/shiftplan.rs</files>
  <read_first>
    - shifty-dioxus/src/state/shiftplan.rs (vollständig, ~270 Zeilen — Slot-Definition Zeilen 164-198, Weekday-Panic Zeile 59)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md (Slot-Pattern Zeilen 150-233)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-UI-SPEC.md (Regel 2 + Regel 3)
  </read_first>
  <action>
**Änderung A — Slot-Struct erweitern (Zeilen 164-172).**

Zwei neue Felder am Ende von `pub struct Slot`, mit Inline-Kommentar zur v1.3-Owner-Information:

```rust
#[derive(Clone, Debug, PartialEq, Eq)]
pub struct Slot {
    pub id: Uuid,
    pub day_of_week: Weekday,
    pub from: time::Time,
    pub to: time::Time,
    pub bookings: Rc<[Booking]>,
    pub min_resources: u8,
    /// Optional cap on paid employees per slot/week (None = no limit).
    /// Phase 6: state-mirror only; Phase v1.3 FUI-02 renders.
    pub max_paid_employees: Option<u8>,
    /// Live count of paid bookings for the view-week.
    /// Phase 6: state-mirror only; Phase v1.3 FUI-01 renders.
    pub current_paid_count: u8,
}
```

**Änderung B — `From<&SlotTO> for Slot` erweitern (Zeilen 187-198).**

`SlotTO` hat `max_paid_employees`. `current_paid_count` lebt NICHT auf `SlotTO`, sondern auf `ShiftplanSlotTO`. Default hier auf `0`; loader.rs überschreibt es im konkreten Konstruktionspfad.

```rust
impl From<&SlotTO> for Slot {
    fn from(slot: &SlotTO) -> Self {
        Self {
            id: slot.id,
            day_of_week: slot.day_of_week.into(),
            from: slot.from,
            to: slot.to,
            bookings: [].into(),
            min_resources: slot.min_resources,
            max_paid_employees: slot.max_paid_employees,
            // Default — loader.rs sets the real value when constructing
            // Slot from a ShiftplanSlotTO (load_shift_plan, load_day_aggregate).
            current_paid_count: 0,
        }
    }
}
```

**Änderung C — Weekday-Panic defensiv machen (Zeile 59, UI-SPEC Regel 3, FC-01-Subziel).**

```rust
pub fn from_num_from_monday(num: u8) -> Self {
    match num {
        0 => Weekday::Monday,
        1 => Weekday::Tuesday,
        2 => Weekday::Wednesday,
        3 => Weekday::Thursday,
        4 => Weekday::Friday,
        5 => Weekday::Saturday,
        6 => Weekday::Sunday,
        // Defensive fallback (Phase 6, UI-SPEC Regel 3): invalid input maps
        // to Monday rather than panicking the WASM runtime. Real callers pass
        // values 0..=6 by construction.
        _ => Weekday::Monday,
    }
}
```
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && PANIC_COUNT=$(grep -c 'panic!("Invalid weekday' src/state/shiftplan.rs) && CHECK_OUT=$(cargo check 2>&1 | grep -E 'src/state/shiftplan\.rs' | head -10) && echo "panic_count=$PANIC_COUNT" && echo "check_errors=$CHECK_OUT" && test "$PANIC_COUNT" -eq 0 && echo "PANIC_DEFENSE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max_paid_employees:\s*Option<u8>' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/state/shiftplan.rs` ist `>= 1`
    - `grep -c 'current_paid_count:\s*u8' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/state/shiftplan.rs` ist `>= 1`
    - `grep -c 'panic!("Invalid weekday' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/state/shiftplan.rs` ist `0` (Panic-Branch ENTFERNT — FC-01-Subziel-Gate)
    - `grep -cE '_\s*=>\s*Weekday::Monday' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/state/shiftplan.rs` ist `>= 1` (defensiver Fallback vorhanden)
    - `grep -v '^#\|^//\|^\s*//\|^\s*///' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/state/shiftplan.rs | grep -c 'max_paid_employees: slot.max_paid_employees'` ist `1` (in From-Impl gemappt, nicht im Kommentar)
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'error.*src/state/shiftplan\.rs'` ist `0` (state/shiftplan.rs Compile-Errors sind weg)
  </acceptance_criteria>
  <done>
    Slot-Struct hat zwei neue Felder; From<&SlotTO> mappt max_paid_employees aus SlotTO und defaultet current_paid_count auf 0; Weekday-Panic ist durch defensive Fallback ersetzt UND der Panic-Count im File ist 0. State-Compile-Errors für state/shiftplan.rs verschwinden, downstream-Compile-Errors (in loader.rs/component/) bleiben offen für Task 1+2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: loader.rs durchreicht max_paid_employees + current_paid_count + no-op für ShiftplanDayTO.unavailable</name>
  <files>shifty-dioxus/src/loader.rs</files>
  <read_first>
    - shifty-dioxus/src/loader.rs (vollständig — load_shift_plan ab Zeile ~150, load_day_aggregate ab Zeile ~195)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md (Loader-Pattern Zeilen 288-338)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-RESEARCH.md (§1b ShiftplanDayTO.unavailable; §6 Landmine 5 BillingPeriodTO.snapshot_schema_version)
  </read_first>
  <action>
**Änderung A — `load_shift_plan`-Slot-Map (Zeile ~162).**

VORHER (Slot-Konstruktion innerhalb `load_shift_plan`):
```rust
.map(|slot| Slot {
    id: slot.slot.id,
    day_of_week: slot.slot.day_of_week.into(),
    from: slot.slot.from,
    to: slot.slot.to,
    min_resources: slot.slot.min_resources,
    bookings: slot.bookings.iter().map(|booking| Booking { /* unverändert */ }).collect(),
})
```

NACHHER (zwei neue Felder durchreichen — `slot.slot` ist `SlotTO`, `slot` selbst ist `&ShiftplanSlotTO`):
```rust
.map(|slot| Slot {
    id: slot.slot.id,
    day_of_week: slot.slot.day_of_week.into(),
    from: slot.slot.from,
    to: slot.slot.to,
    min_resources: slot.slot.min_resources,
    max_paid_employees: slot.slot.max_paid_employees,
    current_paid_count: slot.current_paid_count,
    bookings: slot.bookings.iter().map(|booking| Booking { /* unverändert */ }).collect(),
})
```

**Änderung B — `load_day_aggregate`-Slot-Map (Zeile ~213).**

Identisches Muster. Die innere Slot-Konstruktion in `load_day_aggregate` muss DIE GLEICHEN zwei neuen Felder durchreichen:

NACHHER:
```rust
.map(|slot| Slot {
    id: slot.slot.id,
    day_of_week: slot.slot.day_of_week.into(),
    from: slot.slot.from,
    to: slot.slot.to,
    min_resources: slot.slot.min_resources,
    max_paid_employees: slot.slot.max_paid_employees,
    current_paid_count: slot.current_paid_count,
    bookings: slot.bookings.iter().map(|booking| Booking { /* unverändert */ }).collect(),
})
```

**Änderung C — Update-Map mit `..slot` (Zeile ~132).**

KEINE Änderung nötig. Die existierende Konstruktion `Slot { bookings: ..., ..slot }` propagiert die neuen Felder automatisch über das Struct-Update-Syntax (`..slot`). Verifiziere dass das Pattern intakt bleibt:

```rust
.map(|slot: Slot| Slot {
    bookings: bookings.iter().filter(...).cloned().collect(),
    ..slot  // <-- propagiert ALLE anderen Felder, inkl. der neuen zwei
})
```

Falls der existierende Code dort statt `..slot` ein explizites Feld-Listing nutzt: ALLE Felder (auch die zwei neuen) explizit setzen. Aktueller Stand laut Grep auf Zeile 132 ist `..slot` — keine Änderung.

**Änderung D — Cluster C (`ShiftplanDayTO.unavailable`) NO-OP-Verifikation.**

KEIN Code-Change. Nur Verifikation. Die `load_shift_plan`-Funktion iteriert via `days.iter().flat_map(|day| day.slots.iter())` über die `slots` jedes `ShiftplanDayTO`s. Das Feld `day.unavailable` wird strukturell nicht gelesen — kein State-Mirror-Typ `ShiftplanDay` existiert in v1.2. Per UI-SPEC Regel 2 ist das zulässig: das Feld existiert auf dem Backend-TO, wird aber im Frontend ignoriert. Wenn der Compiler einen `error[E0609]: no field 'unavailable'`-Fehler wirft, wäre das ein Hinweis, dass irgendwo doch versucht wird, das Feld zu lesen — dann den Lese-Pfad finden und die Stelle entfernen oder ignorieren.

Strukturcheck im Task-Body:
```bash
grep -n 'flat_map(|day| day\.slots\.iter()' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/loader.rs
```
Erwartung: mindestens ein Treffer (das Pattern, das `day.unavailable` strukturell überspringt).

**Änderung E — Cluster G (`BillingPeriodTO.snapshot_schema_version`) NO-OP-Verifikation.**

KEIN Code-Change. Nur Verifikation. Das Feld lebt auf dem Backend-`BillingPeriodTO`. Frontend `loader.rs` und `state/*.rs` lesen es nicht. Wenn der Compiler einen `error[E0609]: no field 'snapshot_schema_version'`-Fehler wirft, wäre das ein Hinweis, dass eine Frontend-Konstruktion versucht, das Feld zu setzen — dann hier (oder in dem entdeckten File) das Feld einfach ergänzen mit Default `0_u32`.

Strukturcheck im Task-Body:
```bash
grep -rn 'snapshot_schema_version' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/ 2>/dev/null
```
Erwartung: 0 Treffer (kein Frontend-Konsument).

**Änderung F — Final-Verify für loader.rs UND no-op-Cluster.**

Der `<verify>`-Block prüft sowohl die zwei neuen Slot-Felder durchgereicht als auch dass keine `no field`/`unresolved variant`-Fehler für `ShiftplanDayTO.unavailable` oder `BillingPeriodTO.snapshot_schema_version` aus loader.rs entstehen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && CHECK_LOG=$(cargo check 2>&1) && LOADER_ERR=$(echo "$CHECK_LOG" | grep -cE 'error.*src/loader\.rs') && UNAVAIL_ERR=$(echo "$CHECK_LOG" | grep -cE 'no field `unavailable`|unresolved variant.*UnavailabilityMarker') && SNAP_ERR=$(echo "$CHECK_LOG" | grep -cE 'no field `snapshot_schema_version`') && echo "loader_errors=$LOADER_ERR unavail_errors=$UNAVAIL_ERR snapshot_errors=$SNAP_ERR" && test "$UNAVAIL_ERR" -eq 0 && test "$SNAP_ERR" -eq 0 && echo "NO_OP_CLUSTERS_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'current_paid_count:\s*slot\.current_paid_count' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/loader.rs` ist `>= 2` (load_shift_plan + load_day_aggregate)
    - `grep -c 'max_paid_employees:\s*slot\.slot\.max_paid_employees' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/loader.rs` ist `>= 2`
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'error.*src/loader\.rs'` ist `0` (loader.rs Compile-Errors für Slot-Konstruktion sind weg)
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'no field \`unavailable\`|unresolved variant.*UnavailabilityMarker'` ist `0` (Cluster C: kein Pattern-Match-Fehler auf `ShiftplanDayTO.unavailable`)
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'no field \`snapshot_schema_version\`'` ist `0` (Cluster G: kein Pattern-Match-Fehler auf `BillingPeriodTO.snapshot_schema_version`)
    - `grep -rn 'snapshot_schema_version' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/ 2>/dev/null | wc -l` ist `0` (kein Frontend-Konsument für Cluster G)
  </acceptance_criteria>
  <done>
    Beide Slot-Konstruktionssites in loader.rs reichen die zwei neuen Felder durch. cargo check zeigt keine Slot-Mismatch-Fehler mehr in loader.rs. Cluster C + Cluster G no-op-Verifikation grün (keine `no field`/`unresolved variant`-Fehler).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Test-Fixtures in week_view.rs + day_aggregate_view.rs aktualisieren</name>
  <files>shifty-dioxus/src/component/week_view.rs, shifty-dioxus/src/component/day_aggregate_view.rs</files>
  <read_first>
    - shifty-dioxus/src/component/week_view.rs (Zeilen 1341-1370 für make_slot; Zeilen 1599-1610 für inline-Slot)
    - shifty-dioxus/src/component/day_aggregate_view.rs (Zeilen 211-225 für make_slot)
  </read_first>
  <action>
Drei Test-Fixture-Stellen mit zwei neuen Default-Feldern erweitern. Defaults: `max_paid_employees: None`, `current_paid_count: 0`.

**Stelle 1 — `shifty-dioxus/src/component/week_view.rs:1356-1364` (`make_slot`):**

VORHER (innerhalb `fn make_slot(num_bookings: usize, min_resources: u8) -> Slot`):
```rust
Slot {
    id: Uuid::from_u128(42),
    day_of_week: Weekday::Monday,
    from: time::Time::from_hms(9, 0, 0).unwrap(),
    to: time::Time::from_hms(10, 0, 0).unwrap(),
    bookings: Rc::from(bookings),
    min_resources,
}
```

NACHHER:
```rust
Slot {
    id: Uuid::from_u128(42),
    day_of_week: Weekday::Monday,
    from: time::Time::from_hms(9, 0, 0).unwrap(),
    to: time::Time::from_hms(10, 0, 0).unwrap(),
    bookings: Rc::from(bookings),
    min_resources,
    max_paid_employees: None,
    current_paid_count: 0,
}
```

**Stelle 2 — `shifty-dioxus/src/component/week_view.rs:1599-1605` (inline `let slot = Slot { ... }`):**

Read first die exakten Zeilen mit `sed -n '1595,1615p'`. Erweitere die Slot-Konstruktion identisch um `max_paid_employees: None, current_paid_count: 0`. Falls `min_resources` o.ä. dort einen anderen Wert hat, das beibehalten — nur die zwei neuen Felder ergänzen.

**Stelle 3 — `shifty-dioxus/src/component/day_aggregate_view.rs:212-220` (`make_slot`):**

VORHER:
```rust
fn make_slot(day: Weekday) -> Slot {
    Slot {
        id: uuid::Uuid::nil(),
        day_of_week: day,
        from: time::Time::from_hms(8, 0, 0).unwrap(),
        to: time::Time::from_hms(16, 0, 0).unwrap(),
        bookings: Rc::new([]),
        min_resources: 1,
    }
}
```

NACHHER:
```rust
fn make_slot(day: Weekday) -> Slot {
    Slot {
        id: uuid::Uuid::nil(),
        day_of_week: day,
        from: time::Time::from_hms(8, 0, 0).unwrap(),
        to: time::Time::from_hms(16, 0, 0).unwrap(),
        bookings: Rc::new([]),
        min_resources: 1,
        max_paid_employees: None,
        current_paid_count: 0,
    }
}
```

**Suche nach weiteren Slot-Literal-Konstruktionen** (Sicherheitsnetz):
```bash
grep -n '^\s*Slot\s*{' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/**/*.rs
```
Falls weitere `Slot { ... }`-Sites auftauchen (außer den 3 erwähnten + den loader.rs-Sites + state/shiftplan.rs-Site), an denen explizite Felder gesetzt werden, dort ebenfalls `max_paid_employees: None, current_paid_count: 0` ergänzen.

**KEINE Tailwind-Klassen, KEINE rsx!-Änderungen** in diesen Komponenten — nur Test-Fixture-Konstruktoren. Visuelles Delta = 0 (UI-SPEC Regel 4).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'error.*missing field.*max_paid_employees|error.*missing field.*current_paid_count'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'max_paid_employees: None' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/component/week_view.rs` ist `>= 2` (make_slot + inline Slot)
    - `grep -c 'current_paid_count: 0' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/component/week_view.rs` ist `>= 2`
    - `grep -c 'max_paid_employees: None' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/component/day_aggregate_view.rs` ist `>= 1`
    - `grep -c 'current_paid_count: 0' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/component/day_aggregate_view.rs` ist `>= 1`
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'missing field.*max_paid_employees|missing field.*current_paid_count'` ist `0`
  </acceptance_criteria>
  <done>
    Alle Slot-Literal-Sites in den zwei Komponenten konstruieren mit den zwei neuen Default-Feldern. Compile-Errors zu `missing field` für Slot sind weg.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 2 ist State-Mirror ohne neue Render-Pfade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | I | Slot-State carrying paid-employee counts | accept | `max_paid_employees`/`current_paid_count` werden in Phase 6 NICHT gerendert (UI-SPEC Regel 2). Auch wenn ein Bug Daten leakt, gibt es keinen Render-Pfad in v1.2. Phase 7 verifiziert keine Visual-Regression. |
| T-06-02-02 | T | Weekday-Defense | mitigate | Defensive Fallback statt Panic — die Aufrufer (alle internen Konvertierungen) übergeben 0..=6 by construction; der `_`-Arm ist unerreichbar in well-formed Daten. Falls ein malformiertes Backend-Payload je das `num=99`-Argument erzeugt, gibt's stille Korrektur statt WASM-Crash. |
| T-06-02-03 | T | ShiftplanDayTO.unavailable / BillingPeriodTO.snapshot_schema_version no-op | accept | Beide Felder werden in v1.2 nicht im Frontend gelesen (per Strukturcheck verifiziert). Wire-kompatibel — Backend serialisiert sie, Frontend ignoriert sie. v1.3 / spätere Phasen können Lese-Pfade hinzufügen. |
</threat_model>

<verification>
- `cargo check` zeigt keine `missing field`-Fehler für Slot mehr
- `cargo check` zeigt keine `no field`/`unresolved variant`-Fehler für `ShiftplanDayTO.unavailable` oder `BillingPeriodTO.snapshot_schema_version`
- `state/shiftplan.rs` enthält 0 `panic!("Invalid weekday`-Branches
- Strukturchecks (siehe acceptance_criteria pro Task)
- Keine Tailwind-/RSX-Änderungen → Visual-Delta = 0
</verification>

<success_criteria>
- Drei Tasks grün; alle Slot-Literal-Konstruktionssites haben die zwei neuen Felder
- Weekday-Panic durch defensive Fallback ersetzt (Phase-6-FC-01-Subziel; voll-FC-01-Match-Arm-Exhaustivität ist Plan 06-04-Scope)
- Cluster C + Cluster G no-op-Verifikation grün
- Cargo-Check für state/shiftplan.rs, loader.rs, week_view.rs, day_aggregate_view.rs zeigt keine Errors aus diesem Cluster
</success_criteria>

<output>
After completion, create `.planning/phases/06-rest-types-unification-frontend-compile-through/06-02-SUMMARY.md`
</output>
</content>
</invoke>