---
phase: 6
plan: 3
slug: invitation-redeemed-at-cutover
wave: 2
depends_on: [1]
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/service/user_management.rs
  - shifty-dioxus/src/state/user_management.rs
requirements: [RT-03]
must_haves: [M-04, M-05]
autonomous: true

must_haves:
  truths:
    - "Der Frontend-Code referenziert `rest_types::InvitationStatus`, `rest_types::InvitationResponse`, `rest_types::GenerateInvitationRequest` ohne Compile-Fehler."
    - "`InvitationResponse.redeemed_at` ist im Frontend-Code als `Option<String>` (RFC3339-String) konsumierbar — alle Lese-Sites kompilieren gegen den neuen Typ."
    - "Die lokale `state::user_management::ShiftplanAssignment`-Struct bleibt erhalten (wire-kompatibel mit Backend `ShiftplanAssignmentTO`); kein Refactor nötig."
  artifacts:
    - path: "shifty-dioxus/src/api.rs"
      provides: "Imports `rest_types::{InvitationResponse, GenerateInvitationRequest}` aufgelöst (Backend-rest-types)"
      contains: "GenerateInvitationRequest, InvitationResponse"
    - path: "shifty-dioxus/src/service/user_management.rs"
      provides: "Konsumiert `rest_types::InvitationResponse` ohne Field-Zugriff-Compile-Fehler"
      contains: "InvitationResponse"
  key_links:
    - from: "shifty-dioxus/src/service/user_management.rs"
      to: "shifty-backend/rest-types/src/lib.rs"
      via: "Type-Reference InvitationResponse — Backend-Definition aus Wave 0"
      pattern: "use rest_types::InvitationResponse"
---

<objective>
Cluster F (RESEARCH §2): Invitation-Surface-Anpassungen im Frontend nach dem
Cargo-Swap. Die Types `InvitationStatus`, `InvitationResponse`, `GenerateInvitationRequest`
sind nach Wave 0 in Backend-`rest-types` und nach Wave 1 importierbar — ABER mit
einer Field-Type-Differenz: `redeemed_at` ist im neuen `rest_types::InvitationResponse`
als `Option<String>` deklariert (D-Phase6-01), nicht mehr als `Option<time::OffsetDateTime>`.

Lese-Sites im Frontend, die `redeemed_at` als `OffsetDateTime` behandelten, müssen
auf String-Verarbeitung umgestellt werden — minimal-invasiv (UI-SPEC Regel 4: kein
neues Visual). Wenn der String nur weitergereicht/geloggt wird: trivialer Update.
Wenn er geparst wird: lokales Parsing einbauen (gleiches Pattern wie der gelöschte
Frontend-Fork in `optional_timestamp::deserialize` hatte).

Zusätzlich: `state::user_management::ShiftplanAssignment` (lokale Struct) bleibt
erhalten — sie ist wire-kompatibel mit Backend-`ShiftplanAssignmentTO` (identische
Felder, identische Serde-Defaults). Kein Refactor.

Purpose: RT-03 strukturell für Invitation-Familie + ShiftplanAssignment-Kompatibilität.
Output: Frontend-Code referenziert post-Wave-0 Backend-`rest-types` ohne Compile-Errors für diese Cluster.

Visuelles Delta = 0.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-RESEARCH.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-UI-SPEC.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-VALIDATION.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-00-SUMMARY.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-01-SUMMARY.md
@CLAUDE.md
@shifty-backend/CLAUDE.md
@shifty-backend/CLAUDE.local.md

<interfaces>
<!-- Post-Wave-0 Backend rest_types::InvitationResponse (D-Phase6-01): -->

```rust
pub struct InvitationResponse {
    pub id: Uuid,
    pub username: String,
    pub token: Uuid,
    pub invitation_link: String,
    /// RFC3339 string (Wire-Kompat mit Backend, das time::serde::rfc3339::option produziert)
    pub redeemed_at: Option<String>,
    pub status: InvitationStatus,
}

pub enum InvitationStatus {
    Valid,
    Expired,
    Redeemed,
    SessionRevoked,  // serde-rename "sessionrevoked"
}
```

<!-- shifty-dioxus/src/api.rs Import-Block (Zeile 3-11, Ist-Zustand): -->
```rust
use rest_types::{
    BillingPeriodTO, BlockTO, BookingConflictTO, BookingLogTO, BookingTO,
    CreateBillingPeriodRequestTO, CreateTextTemplateRequestTO, CustomExtraHoursTO, DayOfWeekTO,
    EmployeeReportTO, EmployeeWorkDetailsTO, ExtraHoursCategoryTO, ExtraHoursTO,
    GenerateInvitationRequest, InvitationResponse, RoleTO, SalesPersonTO, SalesPersonUnavailableTO,
    ShiftplanTO, ShortEmployeeReportTO, SlotTO, SpecialDayTO, TextTemplateTO,
    UpdateTextTemplateRequestTO, UserRole, UserTO, VacationPayloadTO, WeekMessageTO,
    WeeklySummaryTO,
};
```

<!-- Funktionen, die InvitationResponse konsumieren (api.rs:1115-1148): -->
<!-- - generate_invitation: Result<InvitationResponse, reqwest::Error> -->
<!-- - list_user_invitations: Result<Rc<[InvitationResponse]>, reqwest::Error> -->

<!-- service/user_management.rs:7,53 (Ist-Zustand): -->
```rust
use rest_types::InvitationResponse;
// ...
pub user_invitations: Rc<[InvitationResponse]>,
```

<!-- state/user_management.rs:7-12 (lokale ShiftplanAssignment, Ist-Zustand): -->
```rust
#[derive(Debug, Clone, Eq, PartialEq, Serialize, Deserialize)]
pub struct ShiftplanAssignment {
    pub shiftplan_id: Uuid,
    #[serde(default = "default_permission_level")]
    pub permission_level: String,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Inventur — wer liest InvitationResponse-Felder?</name>
  <files>shifty-dioxus/src/api.rs, shifty-dioxus/src/service/user_management.rs</files>
  <read_first>
    - shifty-dioxus/src/api.rs (Imports Zeilen 3-11, Funktionen Zeilen 1100-1160)
    - shifty-dioxus/src/service/user_management.rs (vollständig)
  </read_first>
  <action>
**Schritt 1: Read-Sites für `redeemed_at` finden.**

```bash
grep -rn 'redeemed_at\|\.status\s*=\s*InvitationStatus\|\.invitation_link\|\.token\s*[)},]' \
  /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/ \
  | grep -v '/rest-types/' | head -30
```

ERWARTUNG (basierend auf RESEARCH-Codescan):
- `api.rs::generate_invitation` und `list_user_invitations` reichen `InvitationResponse` nur als Ganzes durch (kein Field-Read).
- `service/user_management.rs` legt `InvitationResponse` in `user_invitations: Rc<[InvitationResponse]>` ab (no field access).
- Falls eine Page (`page/user_management.rs` oder ähnlich) das Feld liest und es als `OffsetDateTime` formatiert, hier die einzigen Compile-Errors für `redeemed_at`-Mismatches.

**Schritt 2: Field-Mismatch-Patches falls nötig.**

Falls Codestellen `invitation.redeemed_at` als `OffsetDateTime` lesen (z.B. via `.format(...)`), eine der folgenden Strategien anwenden:

(a) **Pass-through-Strategie** (bevorzugt, wenn der String nur angezeigt wird):
```rust
// VORHER: invitation.redeemed_at.map(|dt| dt.format(...).unwrap())
// NACHHER:
invitation.redeemed_at.clone()  // bereits ein RFC3339-String
```

(b) **Parse-Strategie** (nur wenn die Datums-Logik wirklich auf einem `OffsetDateTime` operieren muss):
```rust
use time::format_description::well_known::Rfc3339;
use time::OffsetDateTime;

let parsed: Option<OffsetDateTime> = invitation
    .redeemed_at
    .as_deref()
    .and_then(|s| OffsetDateTime::parse(s, &Rfc3339).ok());
```

WICHTIG: Diese Patches gelten NUR wenn der Compiler tatsächliche Read-Sites findet. Wenn `redeemed_at` nirgendwo gelesen wird (was die wahrscheinliche Realität nach RESEARCH-Codescan ist), reicht der Type-Reference; kein Code-Edit nötig.

**Schritt 3: ShiftplanAssignment-Verifikation (Cluster F, no-op).**

Lokale `state::user_management::ShiftplanAssignment` (Zeilen 7-12) bleibt UNVERÄNDERT. Die Backend-`ShiftplanAssignmentTO` ist wire-kompatibel (gleiche Felder + gleiche `default_permission_level`-Default-Funktion). KEINE Type-Substitution. KEIN `pub use rest_types::ShiftplanAssignmentTO`. Lassen wie ist.

**Schritt 4: Quick-Compile-Check.**

```bash
cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | \
  grep -cE 'src/(api|service/user_management|state/user_management|page/user_management)\.rs'
```

Erwartung: 0 Compile-Errors aus diesen Files für Invitation-Surface. Falls > 0, in den nächsten Tasks adressieren.

**Schritt 5: Strukturcheck — `redeemed_at: OffsetDateTime` darf NICHT mehr stehen.**

```bash
grep -rn 'redeemed_at:\s*Option<.*OffsetDateTime\|redeemed_at:\s*Option<time::OffsetDateTime' \
  /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/ 2>/dev/null
```

Erwartung: kein Treffer (Frontend referenziert nicht mehr den alten Typ).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -E 'src/(api|service/user_management|state/user_management|page/user_management)\.rs.*error' | head -10 ; echo '----' ; grep -rn 'redeemed_at:\s*Option<.*OffsetDateTime' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-dioxus && cargo check 2>&1 | grep -cE 'error.*src/api\.rs.*Invitation|error.*src/service/user_management\.rs.*Invitation|error.*src/state/user_management\.rs.*Invitation'` ist `0`
    - `grep -rn 'redeemed_at:\s*Option<.*OffsetDateTime' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/ | grep -v '^Binary' | wc -l` ist `0`
    - `grep -c 'GenerateInvitationRequest, InvitationResponse' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/api.rs` ist `>= 1` (Import bleibt; Resolve klappt jetzt gegen Backend-rest-types)
    - `grep -c 'pub struct ShiftplanAssignment\b' /home/neosam/programming/rust/projects/shifty/shifty-dioxus/src/state/user_management.rs` ist `1` (lokale Struct erhalten)
    - Falls Step 1 echte Read-Sites findet: alle erwähnten Files in `<files>` aufgenommen und gepatcht
  </acceptance_criteria>
  <done>
    Inventur abgeschlossen: kein Frontend-Code liest `redeemed_at` als `OffsetDateTime` mehr (oder die Read-Sites sind auf String/Parse umgestellt). ShiftplanAssignment-Lokal-Struct bleibt unverändert. cargo check zeigt keine Invitation-/ShiftplanAssignment-bezogenen Errors aus den Cluster-Files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 2 Cluster F ist Type-Reference-Konsolidierung; keine neue Auth-/Authz-/Network-Pfade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T | InvitationResponse.redeemed_at Wire-Format | accept | `Option<String>` (RFC3339) ist wire-identisch mit dem alten `Option<OffsetDateTime>` (das via `time::serde::rfc3339::option` ebenfalls als RFC3339-String serialisiert wird). Backend liefert `null` oder `"2026-05-07T10:00:00Z"`. Frontend kann beide Formen als String entgegennehmen. |
| T-06-03-02 | I | ShiftplanAssignment-Lokal-Struct vs Backend-TO | accept | Beide haben identische Felder + identische `default_permission_level`-Funktion. Kein Drift-Risiko, weil das Wire-Schema-Match per Test (Phase 7) verifizierbar wäre, falls je ein Smoke-Test gegen einen Backend-Endpoint läuft. |
</threat_model>

<verification>
- Strukturchecks (acceptance_criteria)
- cargo check zeigt keine Invitation-/ShiftplanAssignment-Errors
</verification>

<success_criteria>
- Frontend-Code kompiliert gegen Backend-rest_types::Invitation*-Familie
- `redeemed_at`-Type-Mismatch ist überall behoben (oder gar nicht aktiv genutzt)
- ShiftplanAssignment-State-Struct bleibt erhalten
</success_criteria>

<output>
After completion, create `.planning/phases/06-rest-types-unification-frontend-compile-through/06-03-SUMMARY.md`
</output>
