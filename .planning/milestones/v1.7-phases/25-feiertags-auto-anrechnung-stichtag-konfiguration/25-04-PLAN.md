---
phase: 25-feiertags-auto-anrechnung-stichtag-konfiguration
plan: 04
type: tdd
wave: 3
depends_on: [25-02]
files_modified:
  - service_impl/src/test/reporting_holiday_auto_credit.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [HOL-01, HOL-02, HOL-03, HCFG-01, HCFG-03]
must_haves:
  truths:
    - "An employee whose contract works the holiday weekday gets exactly EmployeeWorkDetails::holiday_hours() credited automatically from special_day (HOL-01, D-25-02)"
    - "The derived credit produces the identical holiday_hours, expected_hours and balance as an equivalent manual ExtraHours(Holiday) (HOL-02)"
    - "A holiday dated before the cutoff yields 0 credit; the same holiday on/after the cutoff yields the full credit (HCFG-01, D-25-05)"
    - "When a manual ExtraHours(Holiday) covers the same day, the holiday is credited once, not twice (HCFG-03, D-25-03)"
    - "booking_information year-view hours (paid_hours/committed_voluntary_hours/volunteer_hours) are unchanged when a holiday falls on a contracted day (HOL-03, D-25-08)"
  artifacts:
    - path: "service_impl/src/test/reporting_holiday_auto_credit.rs"
      provides: "HOL-01/02/03, HCFG-01, HCFG-03 tests with extended ReportingMocks"
      contains: "test_holiday_auto_credit"
    - path: "service_impl/src/test/mod.rs"
      provides: "module registration for reporting_holiday_auto_credit"
      contains: "reporting_holiday_auto_credit"
  key_links:
    - from: "service_impl/src/test/reporting_holiday_auto_credit.rs"
      to: "service_impl/src/reporting.rs"
      via: "tests drive ReportingServiceImpl with MockSpecialDayService + MockToggleService"
      pattern: "ReportingServiceImpl"
    - from: "service_impl/src/test/mod.rs"
      to: "service_impl/src/test/reporting_holiday_auto_credit.rs"
      via: "pub mod registration"
      pattern: "reporting_holiday_auto_credit"
---

<objective>
Prove the Phase-25 holiday derive-on-read behaves correctly with focused unit tests: the basic
derive amount (HOL-01), equivalence to a manual ExtraHours(Holiday) in holiday_hours +
expected_hours + balance (HOL-02), the cutoff gate boundary (HCFG-01), the manual-wins conflict
rule (HCFG-03), and a regression guard that the booking_information year-view is untouched (HOL-03).
This is the behavioral acceptance layer for the reporting implementation landed in 25-02.

Purpose: HOL-01/02/03, HCFG-01, HCFG-03 verification. Backs D-25-02, D-25-03, D-25-05, D-25-08.

Output: new test module reporting_holiday_auto_credit.rs (registered in test/mod.rs) with five tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-RESEARCH.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-PATTERNS.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-VALIDATION.md

# Implementation under test
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-02-SUMMARY.md

# Test instructions: always have tests for the changes
@CLAUDE.md
</context>

<artifacts_produced>
- New file service_impl/src/test/reporting_holiday_auto_credit.rs with ReportingMocks extended for SpecialDayService + ToggleService, and tests:
  - test_holiday_auto_credit_basic (HOL-01)
  - test_holiday_auto_credit_equivalence (HOL-02)
  - test_holiday_before_cutoff_skipped (HCFG-01)
  - test_holiday_manual_wins (HCFG-03)
  - test_holiday_auto_credit_no_year_view_impact (HOL-03)
- service_impl/src/test/mod.rs gains the `pub mod reporting_holiday_auto_credit;` registration
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOL-01/02 + HCFG-01/03 derive-on-read tests</name>
  <files>service_impl/src/test/reporting_holiday_auto_credit.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - 25-PATTERNS.md section "service_impl/src/test/reporting_holiday_auto_credit.rs" (imports, ReportingMocks struct extension with MockSpecialDayService + MockToggleService, TestDeps assoc types, test stubs) and "service_impl/src/test/mod.rs"
    - 25-RESEARCH.md section 8 (HOL-02 equivalence test shape, HCFG-01 boundary shape, HCFG-03 no-double-count shape) and section 4 (conflict match by employee+date)
    - service_impl/src/test/reporting_additive_merge.rs (complete harness to copy: ReportingMocks, TestDeps, building ReportingServiceImpl, calling get_report_for_employee_range)
    - service_impl/src/test/reporting_phase2_fixtures.rs (fixture_sales_person, fixture_sales_person_id, fixture_work_details_8h_mon_fri)
    - service/src/special_days.rs (MockSpecialDayService, SpecialDay/get_by_week), service/src/toggle.rs (MockToggleService, get_toggle/get_toggle_value, Toggle.value)
  </read_first>
  <behavior>
    - test_holiday_auto_credit_basic (HOL-01): Mon-Fri 40h contract → holiday_hours()=8. Mock toggle value = a cutoff date before the holiday; MockSpecialDayService returns one Holiday on a Monday in range; no manual ExtraHours. Expect report.holiday_hours == 8.0.
    - test_holiday_auto_credit_equivalence (HOL-02): Run A = derived (special_day Holiday, no manual). Run B = manual ExtraHours(Holiday, 8.0), no special_day. Assert both produce identical holiday_hours (8.0), expected_hours (40-8=32) and balance.
    - test_holiday_before_cutoff_skipped (HCFG-01): Holiday on 2024-03-18. cutoff "2024-03-25" → holiday_hours == 0.0. cutoff "2024-03-18" (== boundary) → holiday_hours == 8.0.
    - test_holiday_manual_wins (HCFG-03): special_day Holiday on Mon + manual ExtraHours(Holiday, 8.0) same date, cutoff before date → holiday_hours == 8.0 (not 16.0).
  </behavior>
  <action>
    Create reporting_holiday_auto_credit.rs by copying the reporting_additive_merge.rs harness and
    extending ReportingMocks + TestDeps with MockSpecialDayService and MockToggleService (mocks already
    exist — both traits carry #[automock]). Set up the toggle mock to return the cutoff via whichever
    read the implementation uses (get_toggle returning a Toggle with value, or get_toggle_value) — match
    25-02's actual call. Set up MockSpecialDayService::get_by_week to return the configured Holiday for
    the relevant week and empty for others. Write the four tests per the behavior block, driving
    get_report_for_employee_range and asserting holiday_hours / expected_hours / balance. Register the
    module in service_impl/src/test/mod.rs (`#[cfg(test)] pub mod reporting_holiday_auto_credit;`) next
    to the other reporting_* test modules. Use date! / datetime! macros and ShiftyDate as the existing
    tests do; derive concrete holiday dates with from_iso_week_date semantics consistent with 25-02.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop -c bash -c "SQLX_OFFLINE=true cargo test -p service_impl holiday_auto_credit"</automated>
  </verify>
  <acceptance_criteria>
    Four tests pass: basic derive == 8.0 (HOL-01); derived == manual in holiday_hours/expected_hours/balance (HOL-02);
    before-cutoff 0.0 and on/after-cutoff 8.0 (HCFG-01); manual-present yields single count 8.0 (HCFG-03). Module registered.
  </acceptance_criteria>
  <done>reporting_holiday_auto_credit module exists, registered, and its four tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HOL-03 year-view regression guard</name>
  <files>service_impl/src/test/reporting_holiday_auto_credit.rs</files>
  <read_first>
    - 25-RESEARCH.md section 8 "HOL-03 regression test shape" + "HOL-03 implementation note" (booking_information has no special_day/toggle deps; guard pins get_weekly_summary output)
    - 25-CONTEXT.md D-25-08 (year-view values identical with the automatic active)
    - service/src/booking_information.rs (WeeklySummary, get_weekly_summary) and service_impl/src/booking_information.rs:203-273 (paid_hours/committed_voluntary_hours/volunteer_hours)
    - an existing booking_information test in service_impl/src/test/ for the mock harness shape
  </read_first>
  <behavior>
    - test_holiday_auto_credit_no_year_view_impact (HOL-03): build the year-view via booking_information for an employee whose contracted weekday is also a holiday; assert paid_hours, committed_voluntary_hours and volunteer_hours equal the values produced with no holiday in play (the automatic must not reduce committed/paid/volunteer). The guard rests on booking_information having NO special_day/toggle dependency.
  </behavior>
  <action>
    Add test_holiday_auto_credit_no_year_view_impact to reporting_holiday_auto_credit.rs. Construct a
    BookingInformationServiceImpl (or reuse the existing booking_information test harness) with mocks,
    request get_weekly_summary for an employee+week where a holiday falls on a contracted day, and
    assert paid_hours / committed_voluntary_hours / volunteer_hours match the pre-Phase-25 expected
    values (pin them explicitly). The point is that the booking_information path never consults
    special_day or the toggle, so the holiday automatic leaves the year-view untouched. If the cleanest
    harness lives in an existing booking_information test module, the executor may instead add this test
    there and adjust files_modified accordingly; default placement is this file to keep ownership simple.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop -c bash -c "SQLX_OFFLINE=true cargo test -p service_impl no_year_view_impact && SQLX_OFFLINE=true cargo clippy --workspace -- -D warnings"</automated>
  </verify>
  <acceptance_criteria>
    test_holiday_auto_credit_no_year_view_impact passes: paid_hours/committed_voluntary_hours/volunteer_hours are
    unchanged by a holiday on a contracted day (HOL-03). Workspace passes clippy -D warnings.
  </acceptance_criteria>
  <done>Year-view regression guard green; full clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test code only) | This plan adds unit tests; it introduces no runtime surface or external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-09 | Tampering | test correctness | low | accept | Tests assert exact numeric outcomes (holiday_hours/expected/balance) so a regression cannot pass silently |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `cargo test -p service_impl holiday_auto_credit` passes all five tests.
- `cargo clippy --workspace -- -D warnings` clean (hard gate).
- `cargo test --workspace` green end-to-end (full phase verification).
</verification>

<success_criteria>
- HOL-01 basic derive, HOL-02 equivalence, HCFG-01 cutoff boundary, HCFG-03 manual-wins, and HOL-03 year-view guard all pass.
- No regression in the existing reporting/billing suites; clippy clean.
</success_criteria>

<output>
Create `.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-04-SUMMARY.md` when done.
</output>
