---
phase: 53-freiwilligen-abwesenheiten-jahresansicht
plan: 02
type: tdd
wave: 2
depends_on:
  - 53-01
files_modified:
  - service_impl/src/booking_information.rs
  - service_impl/src/test/booking_information_vaa.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements:
  - VAA-01
  - VAA-02
  - VAA-03
must_haves:
  truths:
    - "[D-53-02] hours = Sigma find_working_hours_for_calendar_week(all_work_details, year, week).filter(sales_person_id == sp_id && (cap_planned_hours_to_expected || expected_hours == 0.0)).map(committed_voluntary).sum — identische Cap-Gate-Formel zu committed_voluntary_hours (booking_information.rs:509-517) OHNE absent-Filter"
    - "[D-53-03] Sichtbarkeit = exakt absent_volunteer_ids (VFA-01 whole-week-out, D-26-01); kein partielles Anteiligkeits-Kriterium"
    - "[D-53-05] Fill-Site 1 = BookingInformationServiceImpl::get_weekly_summary im Per-Woche-Loop; NICHT assemble_weeks (D-52-09 MUST-preserve)"
    - "[D-53-06] Fill-Site 2 = BookingInformationServiceImpl::get_summery_for_week; muss all_absences + absent_volunteer_ids + all_sales_persons zusaetzlich laden (Single-Week hat sie bisher nicht)"
    - "[VAA-01] Freiwilliger mit Vacation/SickLeave/UnpaidLeave-Period in Woche N erscheint im DTO-Feld sales_person_absences mit Name + Stunden fertig geformt (Fat Backend, Thin Client, D-51-02)"
    - "[VAA-02] Freiwilliger mit committed_voluntary=5.0 in cap-gated wh liefert hours=5.0 im DTO-Feld (Test vaa03_volunteer_with_period_appears_with_correct_hours)"
    - "[VAA-03 #1] Backend-Test vaa03_volunteer_with_period_appears_with_correct_hours gruen"
    - "[VAA-03 #2] Backend-Test vaa03_volunteer_without_period_absent_not_in_list gruen"
    - "[VAA-03 #3] Backend-Test vaa03_paid_employee_unchanged_regression_lock gruen — bezahlter Mitarbeiter erscheint weiter in working_hours_per_sales_person, NICHT in sales_person_absences (Regression-Lock)"
    - "[Pitfall 1] sales_person_service.get_all() genau EINmal je Fill-Site; all_sales_persons wird gehalten, volunteer_ids davon abgeleitet"
    - "[Pitfall 2] wh.sales_person_id == sp_id-Filter ist in der VAA-02-Formel Pflicht — sonst Sigma ueber alle Personen"
    - "[Pitfall 6] absent_volunteer_ids in get_summery_for_week wird via volunteer_ids.contains(...) gefiltert — bezahlte Mitarbeiter mit Absence duerfen NICHT ins Freiwilligen-Feld leaken"
  artifacts:
    - service_impl/src/booking_information.rs (get_weekly_summary + get_summery_for_week Fill-Sites erweitert)
    - service_impl/src/test/booking_information_vaa.rs (neue Test-Datei mit 3 VAA-03-Tests)
    - service_impl/src/test/mod.rs (Registrierung pub mod booking_information_vaa)
  key_links:
    - "get_weekly_summary Assembly-Loop -> sales_person_absences DTO-Feld (Anzeige-Bruecke zur Jahresansicht)"
    - "get_summery_for_week Single-Week-Fill-Site -> sales_person_absences DTO-Feld (Anzeige-Bruecke zur Wochensicht-Konsistenz, D-53-06)"
    - "absent_volunteer_ids (VFA-01) ist der Sichtbarkeits-Schluessel — kein neuer Filter erlaubt (D-53-03)"
---

<objective>
Zwei Fill-Sites im `BookingInformationServiceImpl` befuellen das in Plan 01
angelegte `sales_person_absences`-Feld auf `WeeklySummary`:
1. **`get_weekly_summary`** (Jahres-Assembly-Loop, Zeile 267+) — nutzt bereits
   geladene `absent_volunteer_ids`, `all_work_details`, `sales_person_service.get_all()`.
2. **`get_summery_for_week`** (Single-Week, Zeile 643+) — muss `all_absences`,
   `absent_volunteer_ids` und `all_sales_persons` erst laden, damit die
   Semantik zwischen Wochensicht und Jahressicht byte-identisch ist (D-53-06,
   Pitfall 4 aus RESEARCH.md).

Purpose: VAA-01/02/03 gemaess D-53-02/03/05/06. Fat Backend, Thin Client
(D-51-02) — keine Business-Logic im FE. Praezedenzformel identisch zu
`committed_voluntary_hours` (Zeilen 509-517), aber ohne den
`!absent_volunteer_ids.contains(...)`-Filter, weil hier gerade die
Abwesenden gemessen werden.

Output: Beide Fill-Sites liefern gefuelltes DTO-Feld; drei Backend-Tests
belegen VAA-03 (Freiwilliger mit Period sichtbar, ohne Period nicht,
Bezahlter unveraendert).

**TDD-Zyklus:**
- **RED:** Task 1 erstellt `booking_information_vaa.rs` mit den 3 Assertions
  gegen leeres `sales_person_absences` (aus Plan 01) — alle 3 Tests scheitern.
- **GREEN:** Task 2 implementiert `get_weekly_summary` Fill-Site + Task 3
  `get_summery_for_week` Fill-Site; Tests werden gruen.
- **REFACTOR:** Task 4 (optional) extrahiert `absent_volunteer_ids_for_week`-
  Helper, wenn die Duplikation zwischen Fill-Sites unschoen wird — Tests
  bleiben gruen.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-01-PLAN.md
@service_impl/src/booking_information.rs
@service_impl/src/test/booking_information_vfa.rs
@service_impl/src/test/mod.rs
</context>

## Artifacts this phase produces

- **Fill-Site 1 (`get_weekly_summary`)**: `all_sales_persons` wird load-once
  gehalten (vorher weggeworfen); `volunteer_ids` davon abgeleitet; neuer
  `sales_person_absences`-Build im Per-Woche-Loop gefuellt und ins
  `WeeklySummary`-Literal (Zeile 620) uebernommen.
- **Fill-Site 2 (`get_summery_for_week`)**: neu geladen `all_absences` +
  `all_sales_persons`; `absent_volunteer_ids: HashSet<Uuid>` inline gebaut
  (identisches Muster wie in `get_weekly_summary`); Fill in beiden
  `WeeklySummary`-Literalen der Methode (Zeilen 901, 960).
- **Neue Test-Datei**: `service_impl/src/test/booking_information_vaa.rs` mit
  `TestDeps`-Block (aus `booking_information_vfa.rs` uebernommen), Fixtures
  und drei `#[tokio::test]`-Funktionen.
- **Neue Test-Funktionen**:
  - `vaa03_volunteer_with_period_appears_with_correct_hours` (VAA-01+02).
  - `vaa03_volunteer_without_period_absent_not_in_list` (VAA-03 #2).
  - `vaa03_paid_employee_unchanged_regression_lock` (VAA-03 #3).
- **Registrierung**: `service_impl/src/test/mod.rs` bekommt
  `#[cfg(test)] pub mod booking_information_vaa;` direkt nach der bestehenden
  `booking_information_vfa`-Zeile.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Test-Datei booking_information_vaa.rs mit 3 scheiternden Assertions</name>
  <files>
    service_impl/src/test/booking_information_vaa.rs,
    service_impl/src/test/mod.rs
  </files>
  <read_first>
    - service_impl/src/test/booking_information_vfa.rs (komplette Datei, ca. 320 Zeilen) — Template fuer TestDeps, Mock-Setup, Fixture-Muster
    - service_impl/src/test/mod.rs (die relevante Zeile `pub mod booking_information_vfa;` — direkt daneben landet die neue Registrierung)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"6. service_impl/src/test/booking_information_vaa.rs — neue Test-Datei" (Zeilen 411-522) — Fixture-Konstanten, Mock-Setup-Pattern, 3 Test-Funktions-Skelette
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md §"Test-Skelett fuer VAA-03 (Backend)" (Zeilen 646-711) — exakte Assertion-Signaturen
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G2 — Angezeigter Stunden-Wert" (D-53-02) — Referenzwert `committed_voluntary=5.0` -> `entry.hours=5.0`
  </read_first>
  <behavior>
    Diese Test-Datei ist die RED-Baseline. Nach Task 1 muessen ALLE drei Tests
    scheitern (`assert_left != assert_right`), weil Plan 01 das Feld
    `sales_person_absences` mit leerem Default gefuellt hat. Erst Task 2 (Fill-
    Site 1) macht Test 1 und Test 2 gruen; Task 3 (Fill-Site 2) beruehrt
    diese Tests nicht (die Tests gehen ueber `get_weekly_summary`, nicht
    `get_summery_for_week`).

    Test-Erwartungen:
    - **`vaa03_volunteer_with_period_appears_with_correct_hours`**: Nach
      Setup (Freiwilliger 1 hat AbsencePeriod fuer Woche 20 2026,
      committed_voluntary=5.0 in cap-gated wh) MUSS
      `week.sales_person_absences.iter().any(|a| a.sales_person_id == volunteer_id_absent())`
      wahr sein UND der Eintrag hours ≈ 5.0 (±0.001) haben.
    - **`vaa03_volunteer_without_period_absent_not_in_list`**: Nach Setup
      (Freiwilliger 2 hat KEINE AbsencePeriod) MUSS
      `week.sales_person_absences.iter().any(|a| a.sales_person_id == volunteer_id_present())`
      falsch sein.
    - **`vaa03_paid_employee_unchanged_regression_lock`**: Nach Setup
      (Bezahlter hat AbsencePeriod fuer Woche 20 + wh) MUSS gleichzeitig
      `!week.sales_person_absences.iter().any(|a| a.sales_person_id == paid_id())`
      UND `week.working_hours_per_sales_person.iter().any(|wh| wh.sales_person_id == paid_id())`
      wahr sein — Freiwilligen-Feld leakt keinen Bezahlten, bezahlten-Feld
      bleibt korrekt befuellt.
  </behavior>
  <action>
    Lege `service_impl/src/test/booking_information_vaa.rs` **neu** an. Starte
    von einer 1:1-Kopie des `booking_information_vfa.rs`-Templates (PATTERNS.md
    §6 gibt die Sektions-Referenzen: TestDeps Zeile 45-66, Fixture-Muster
    Zeilen 88-153, Mock-Setup-Pattern Zeilen 193-299, Service-Konstruktion
    Zeilen 304-316 dort). Ersetze dann drei Bereiche:

    (a) **Fixture-Konstanten**: `YEAR = 2026`, `WEEK_UNDER_TEST = 20`, drei
    UUID-Helper `volunteer_id_absent()`, `volunteer_id_present()`, `paid_id()`
    (jeweils `Uuid::from_u128(<konstante>)` mit einfach lesbaren Zahlen — die
    hex-Konstanten aus PATTERNS.md §6 Zeile 452-460 sind Anschauungswerte, in
    Rust muessen die Konstanten valide u128-Literale sein: nutze dezimale
    Konstanten wie `Uuid::from_u128(0x00000000_0000_0000_0000_0000_00530001)`
    fuer den ersten, `..._00530002` fuer den zweiten, `..._00530003` fuer den
    dritten — die Ziffern in der letzten Doppelgruppe kodieren „53" +
    Sequenz).

    (b) **Mock-Setup**: `sales_person_service.expect_get_all()` liefert Vec mit
    3 Personen: `volunteer_id_absent()` (is_paid=false), `volunteer_id_present()`
    (is_paid=false), `paid_id()` (is_paid=true). `absence_service.expect_find_all()`
    liefert nur EINE AbsencePeriod, `sales_person_id: volunteer_id_absent()`,
    Kategorie `Vacation`, from/to_date beide innerhalb ISO-W20/2026 (Mo
    2026-05-11, So 2026-05-17). Fuer `vaa03_paid_employee_unchanged_regression_lock`
    kommt eine zweite AbsencePeriod fuer `paid_id()` dazu (der Test setzt sein
    eigenes Mock-Set auf). `employee_work_details_service.expect_all()`
    liefert cap-gated wh mit `committed_voluntary: 5.0` fuer alle drei
    Personen; Zeitraum umschliesst Woche 20. Uebrige Mocks (toggle_service,
    special_day_service, reporting_service, shiftplan_report_service, slot_service,
    shiftplan_service, transaction_dao) leer/passthrough analog vfa.rs.

    (c) **Drei `#[tokio::test]`-Funktionen** mit exakt den in <behavior>
    definierten Assertions. Signatur: `async fn <name>() { ... let summaries =
    service.get_weekly_summary(YEAR, Authentication::Full, None).await.unwrap(); let
    week = summaries.iter().find(|s| s.week == WEEK_UNDER_TEST).unwrap(); ... }`.

    Zusaetzlich in `service_impl/src/test/mod.rs`: direkt hinter der Zeile
    `pub mod booking_information_vfa;` (oder aequivalent) den neuen Eintrag
    `#[cfg(test)] pub mod booking_information_vaa;` einfuegen. Attribut-Praefix
    (`#[cfg(test)]`) exakt so setzen, wie der vfa-Nachbar es setzt — 1:1
    kopieren.

    Kein Fill-Site-Code in dieser Task — die Tests scheitern absichtlich.
  </action>
  <verify>
    <automated>cargo test -p service_impl booking_information_vaa 2>&amp;1 | grep -E "FAILED|test result" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei Tests `vaa03_volunteer_with_period_appears_with_correct_hours`, `vaa03_volunteer_without_period_absent_not_in_list`, `vaa03_paid_employee_unchanged_regression_lock` existieren und werden gefunden.
    - Der Test-Runner zeigt drei `FAILED`-Zeilen (RED-Baseline erreicht).
    - `cargo build -p service_impl --tests` gruen (die Test-Datei compiliert, insbesondere alle Mock-Setups und die Imports auf `service::booking_information::SalesPersonAbsence` sind korrekt).
    - `grep -n "pub mod booking_information_vaa" service_impl/src/test/mod.rs` liefert genau eine Zeile.
  </acceptance_criteria>
  <done>
    3 scheiternde Tests + Registrierung. RED-Zustand erreicht. Task 2 wird sie zu GREEN treiben.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — Fill-Site 1 in get_weekly_summary</name>
  <files>service_impl/src/booking_information.rs</files>
  <read_first>
    - service_impl/src/booking_information.rs (Zeilen 267-641 — komplette `get_weekly_summary`-Methode. Insbesondere Zeile 290-297 volunteer_ids-Load, Zeile 308-311 absence_service.find_all, Zeile 422-442 absent_volunteer_ids-HashSet-Bau, Zeile 509-517 committed_voluntary_hours-Formel (D-53-02-Praezedenz), Zeile 620-636 WeeklySummary-Literal)
    - service_impl/src/booking_information.rs (Zeile 2 — Import `use crate::reporting::find_working_hours_for_calendar_week;` bzw. entsprechender Pfad; kein neuer Import noetig)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"2. service_impl/src/booking_information.rs — Fill-Site get_weekly_summary" (Zeilen 68-169) — Schritt A `all_sales_persons`-Refactor, Schritt B VAA-02-Formel, Schritt C WeeklySummary-Literal-Erweiterung
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md §"Praezedenzformel-Verifikation (D-53-02)" (Zeilen 207-263) — Formel Zelle-fuer-Zelle abgeleitet, inklusive Pitfall 1 und Pitfall 2 Hinweise
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G5 — Fill-Site" (D-53-05) — bindet explizit an `get_weekly_summary`, NICHT an `assemble_weeks` (D-52-09 Preservation)
  </read_first>
  <behavior>
    Nach diesem Task muessen zwei der drei Tests aus Task 1 gruen sein:
    - `vaa03_volunteer_with_period_appears_with_correct_hours` GREEN.
    - `vaa03_paid_employee_unchanged_regression_lock` GREEN (weil Fill-Site 1
      im Jahresansicht-Pfad laeuft, den beide Tests aufrufen).
    - `vaa03_volunteer_without_period_absent_not_in_list` GREEN.
    Alle drei laufen ueber `service.get_weekly_summary(...)`. Task 3 kuemmert
    sich anschliessend um die Single-Week-Fill-Site.
  </behavior>
  <action>
    Drei Aenderungen in `get_weekly_summary` (Zeile 267-641):

    (a) **`all_sales_persons` load-once** (Pitfall 1): Ersetze den bestehenden
    Block Zeile 290-297, der `sales_person_service.get_all()` aufruft und
    direkt `.iter().filter(...).map(...).collect()` fuer `volunteer_ids`
    kettet. Ziel: Ergebnis in einer Zwischenvariable `all_sales_persons`
    halten (Arc-typisiert wie vom Service geliefert), dann davon
    `volunteer_ids: Arc<[Uuid]>` per `iter().filter(|sp| !sp.is_paid.unwrap_or(false)).map(|sp| sp.id).collect()`
    ableiten (PATTERNS.md §2 Schritt A). Kein zweiter Service-Call, keine
    Signaturaenderung an `get_weekly_summary` — nur eine lokale Variable
    zusaetzlich, die im Loop wiederverwendet wird.

    (b) **VAA-02-Fill-Block im Per-Woche-Loop** — direkt nach der bestehenden
    `committed_voluntary_hours`-Zeile 517, aber VOR dem
    `WeeklySummary`-Literal (Zeile 620): Baue
    `sales_person_absences: Arc<[service::booking_information::SalesPersonAbsence]>`
    per `absent_volunteer_ids.iter().filter_map(|&sp_id| { ... }).collect()`.
    Im Closure:
    1. `name` aus `all_sales_persons.iter().find(|sp| sp.id == sp_id).map(|sp| sp.name.clone())?` (early-return via `?`, Person-Not-Found ueberspringen — kann in valide Daten nicht auftreten, aber semantisch robust).
    2. `hours = find_working_hours_for_calendar_week(&all_work_details, year, week).filter(|wh| wh.sales_person_id == sp_id && (wh.cap_planned_hours_to_expected || wh.expected_hours == 0.0)).map(|wh| wh.committed_voluntary).sum()` — **Pitfall 2**: der `wh.sales_person_id == sp_id`-Filter ist Pflicht, sonst summiert die Formel ueber alle Personen (der Iterator ist personenuebergreifend). Praezedenzformel ist Zeile 509-517 (`committed_voluntary_hours`) OHNE den `!absent_volunteer_ids.contains(...)`-Filter, weil hier gerade die Abwesenden gesucht sind.
    3. `Some(service::booking_information::SalesPersonAbsence { sales_person_id: sp_id, name, hours })`.

    (c) **`WeeklySummary`-Literal erweitern** (Zeile 620-636): Direkt nach
    `working_hours_per_sales_person: working_hours_per_sales_person.into(),`
    (Zeile 627) das neue Feld einfuegen: `sales_person_absences,` (die
    lokale Variable aus Schritt b). Damit ersetzt der Fill-Wert den in Plan 01
    gesetzten leeren Default.

    Kein Touch an `assemble_weeks` (D-52-09 MUST-preserve). Keine
    Signaturaenderung an `find_working_hours_for_calendar_week`. Keine neue
    SQL-Query.

    **WICHTIG — Fenced-code-Diszplin**: Setze **keine** Kommentare in den
    neuen Block, die die Zeile `"{name}: {hours} h"` woertlich enthalten
    (das ist Plan 03s Rendering-Grep-Gate) — nutze stattdessen
    beschreibende Prosa wie „Anzeige-Wert je Freiwilligem".
  </action>
  <verify>
    <automated>cargo test -p service_impl booking_information_vaa</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei `vaa03_*`-Tests gruen (test result: ok. 3 passed).
    - `grep -n "sales_person_absences" service_impl/src/booking_information.rs` liefert mindestens zwei Zeilen (Fill-Block + Literal), lokalisiert in `get_weekly_summary`.
    - `grep -n "sales_person_service.get_all" service_impl/src/booking_information.rs` liefert innerhalb `get_weekly_summary` genau EINEN Treffer (Pitfall 1: kein zweiter Call).
    - `grep -c "wh.sales_person_id ==" service_impl/src/booking_information.rs` >= 1 im neuen Fill-Block (Pitfall 2 dokumentiert).
    - `cargo test -p service_impl booking_information_chain_c` gruen (Regression-Sanity: Phase-52-Assembly bleibt unberuehrt).
    - `cargo test -p service_impl booking_information_vfa` gruen (VFA-01 Regression).
  </acceptance_criteria>
  <done>
    Fill-Site 1 aktiv; VAA-01/02/03 fuer `get_weekly_summary` erfuellt. Task 3 zieht die Single-Week-Methode nach.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: GREEN — Fill-Site 2 in get_summery_for_week (Single-Week)</name>
  <files>service_impl/src/booking_information.rs</files>
  <read_first>
    - service_impl/src/booking_information.rs (Zeilen 643-970 — komplette `get_summery_for_week`-Methode. Insbesondere volunteer_ids-Load ~Zeile 666-673, sowie die BEIDEN `WeeklySummary`-Literale in dieser Methode auf Zeilen 901 und 960 — beide muessen das neue Feld setzen)
    - service_impl/src/booking_information.rs (Zeile ~81 — `period_overlaps_week`-Helper, bereits vorhanden, kein Re-Import noetig)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"3. service_impl/src/booking_information.rs — Fill-Site get_summery_for_week" (Zeilen 172-263) — Schritt A-E, insbesondere Schritt C mit dem HashSet-Bau
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md §"get_summery_for_week — absent_volunteer_ids aufbauen (D-53-06)" (Zeilen 304-355) — komplette Rezept-Sequenz mit Pitfall 4 und Pitfall 6
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G6" (D-53-06) — verbindliche Motivation
  </read_first>
  <behavior>
    Die drei Tests aus Task 1 laufen ueber `get_weekly_summary`, nicht ueber
    `get_summery_for_week`. Dieser Task hat daher KEINEN Test-Deltahebel auf
    die vaa03_*-Tests; sie bleiben gruen. Statt neuer Assertion-Tests
    genuegt hier ein starkes verhaltensbezogenes Acceptance-Kriterium: der
    Fill-Block ist strukturell identisch zu Fill-Site 1, `absent_volunteer_ids`
    wird per `volunteer_ids.contains(...)`-Filter gebaut (Pitfall 6), und
    `cargo test --workspace` bleibt gruen. Optional: eine zusaetzliche
    Assertion-Erweiterung in `booking_information_vaa.rs`, die
    `service.get_summery_for_week(year, week, ...)` aufruft und dieselbe
    Sichtbarkeit prueft — **nicht Pflicht** in dieser Wave, weil VAA-03 nur
    die Jahresansicht spezifiziert; als optionale Ausbaustufe empfohlen.
  </behavior>
  <action>
    In `get_summery_for_week` (Zeile 643-970) drei bzw. vier Erweiterungen:

    (a) **`all_sales_persons` load-once** — Zwischen dem bestehenden
    `sales_person_service.get_all()`-Aufruf und der davon abgeleiteten
    `volunteer_ids`-Sammlung dasselbe Muster wie in Task 2 anwenden (PATTERNS.md
    §3 Schritt A, RESEARCH.md §304+ Punkt „all_sales_persons laden"). Kein
    zweiter Service-Call.

    (b) **`all_absences` laden** — direkt nach dem `all_sales_persons`-Block
    (PATTERNS.md §3 Schritt B). Nutzt `self.absence_service.find_all(Authentication::Full, tx.clone().into()).await?`
    — identisch zu Zeile 308-311 in `get_weekly_summary`.

    (c) **`absent_volunteer_ids` HashSet inline bauen** — genau wie Zeile
    422-442 in `get_weekly_summary`: `if let (Ok(week_monday), Ok(week_sunday)) =
    (time::Date::from_iso_week_date(year as i32, week, time::Weekday::Monday),
    time::Date::from_iso_week_date(year as i32, week, time::Weekday::Sunday))
    { all_absences.iter().filter(|period| volunteer_ids.contains(&period.sales_person_id)
    && period_overlaps_week(period.from_date, period.to_date, week_monday, week_sunday))
    .map(|period| period.sales_person_id).collect() } else { HashSet::new() }`. Der
    `volunteer_ids.contains(...)`-Filter ist Pflicht (Pitfall 6): Ohne ihn
    tauchen bezahlte Mitarbeiter mit Absence im Freiwilligen-Feld auf.

    (d) **`sales_person_absences` bauen** — identische Formel wie in Task 2
    Schritt B (Pitfall 2: `wh.sales_person_id == sp_id`-Filter). Achte darauf,
    dass `all_work_details` in `get_summery_for_week` bereits verfuegbar ist
    oder ebenfalls geladen werden muss — RESEARCH.md sagt in §304+ Punkt „laden
    analog", pruefe die Methode. Wenn `employee_work_details_service.all()` noch
    nicht geladen ist, laedt Task 3d es einmalig vor dem Fill-Block.

    (e) **Beide `WeeklySummary`-Literale erweitern** — Zeile 901 und Zeile 960
    bekommen jeweils das neue Feld `sales_person_absences: sales_person_absences.clone(),`
    (oder `.into()` falls Typ noch nicht `Arc<[...]>` ist — waehle das Muster,
    das mit dem Fill-Block-Typ konsistent ist). Zwei Fill-Werte: Wenn die
    Methode zwei getrennte Assembly-Pfade hat, jeder muss den korrekt gebauten
    Vec sehen. Wenn ein Pfad (z.B. Fehler-Fall) einen leeren Default liefern
    soll, ist `Vec::new().into()` explizit erlaubt — dokumentiere in einer
    Zeilen-Kommentar-Prosa, warum genau dort leer.

    Kein neuer Import noetig; alle Bausteine sind bereits im Modul verfuegbar.
    Keine `cargo sqlx prepare`-Aenderung.
  </action>
  <verify>
    <automated>cargo test -p service_impl booking_information_vaa &amp;&amp; cargo test -p service_impl booking_information_vfa &amp;&amp; cargo test -p service_impl booking_information_chain_c</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei `vaa03_*`-Tests weiterhin gruen (kein Regressionsschaden durch Fill-Site 2).
    - `grep -c "sales_person_absences" service_impl/src/booking_information.rs` liefert insgesamt mindestens FUENF Zeilen (2 Fill-Bloecke + 3 Literal-Uses — je 1 in Fill-Site 1, je 2 in Fill-Site 2).
    - `grep -n "volunteer_ids.contains" service_impl/src/booking_information.rs` liefert mindestens ZWEI Treffer (einer je Fill-Site — Pitfall 6 dokumentiert).
    - `cargo test --workspace` gruen (Regression-Sanity).
  </acceptance_criteria>
  <done>
    Beide Fill-Sites befuellen das DTO-Feld konsistent; Wochensicht und Jahresansicht sind semantisch aequivalent (D-53-06 erfuellt). Kein FE-Touch — Plan 03 uebernimmt.
  </done>
</task>

<task type="execute">
  <name>Task 4: Wave-2-Gate — Clippy + Full-Workspace-Test</name>
  <files></files>
  <read_first>
    - CLAUDE.md §"Development Guidelines" — Clippy-Gate `cargo clippy --workspace -- -D warnings` (Pflicht).
    - Memory `feedback_clippy_gate` — `nix build` failt bei jedem Warning.
    - Memory `reference_sqlx_prepare_after_new_query` — hier NICHT anwendbar, da RESEARCH.md §Runtime-State-Inventory bestaetigt: keine neue SQL-Query.
  </read_first>
  <action>
    Sequenziell:
    1. `cargo build --workspace` — sanity check.
    2. `cargo test --workspace` — insbesondere die Regressions-Tests
       `booking_information_chain_c` und `booking_information_vfa` sind
       kritisch (Phase 52 assembly + VFA-01 whole-week-out muessen unangetastet
       gruen sein).
    3. `cargo clippy --workspace -- -D warnings` — hartes Gate.

    Kein `cargo sqlx prepare` in Phase 53 (keine neue Query, RESEARCH.md
    §"Runtime State Inventory" bestaetigt). Kein FE-Gate in dieser Wave —
    das ist Plan 03s Verantwortung.
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei Kommandos exit-code 0.
    - Kein Test schlaegt fehl, insbesondere nicht `booking_information_chain_c` (Phase-52-Assembly-Regression) und nicht `booking_information_vfa` (VFA-01-Regression).
    - `cargo clippy` liefert keine Warnung.
  </acceptance_criteria>
  <done>
    Wave 2 abgeschlossen. Backend liefert `sales_person_absences` in beiden Fill-Sites gefuellt. Plan 03 kann den FE-Union-Merge auf das neue DTO-Feld verdrahten.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Endpoint `GET /booking-information/weekly-resource-report/{year}` bleibt `SHIFTPLANNER_PRIVILEGE`-gated (bestehender Check in `get_weekly_summary` unveraendert). Kein neuer Endpoint. |
| Endpoint `GET /booking-information/for-week/{year}/{week}` (get_summery_for_week) | Bereits SHIFTPLANNER-gated (bestehende Permission-Check-Kette, unveraendert). Das neue DTO-Feld wird durch denselben Gate geschuetzt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-02-01 | Information Disclosure | `SalesPersonAbsence.name` Feld im Response (Freiwilligen-Namen an Client) | informational | mitigate | Bereits durch `is_shiftplanner`-Check am Anfang beider Fill-Site-Methoden geschuetzt. Freiwilligen-Namen sind zusaetzlich bereits im selben Endpoint via `working_hours_per_sales_person.sales_person_name` (bezahlte Zeilen — nur weil deren Namen sichtbar sind, ist der Gate identisch) UND via `/sales-persons`-Endpoint (auch shiftplanner-gated) verfuegbar. Kein neuer Datenpfad. RESEARCH.md §"Hinweis: Freiwilligen-Namen im DTO" bestaetigt: kein neues Berechtigungsrisiko. |
| T-53-02-02 | Information Disclosure | Absence-Kategorie leak (Vacation vs SickLeave vs UnpaidLeave) via committed_voluntary-Wert | low | accept | Der Stundenwert ist kategorie-agnostisch (D-53-02: identisch fuer Vacation/SickLeave/UnpaidLeave); es gibt keinen inferierbaren Rueckschluss auf die spezifische Absence-Kategorie ausschliesslich aus `hours`. Der Endpoint gated bereits SHIFTPLANNER, die die vollen Absence-Details ohnehin sehen duerfen. Kein weiterer Handlungsbedarf. |
| T-53-02-03 | Tampering | Konsumenten aelterer JSON-Client-Version deserialisieren das Response ohne `sales_person_absences` | informational | mitigate | Wire-Kompat via `#[serde(default)]` auf `WeeklySummaryTO.sales_person_absences` (Plan 01 Task 2). Legacy-Client bekommt leeres Feld; kein Absturz. Test `committed_voluntary_hours_defaults_to_zero_when_absent` in rest-types belegt das Serde-Muster. |
| T-53-02-04 | Elevation of Privilege | Bezahlter Mitarbeiter mit Absence taucht faelschlich im Freiwilligen-Feld auf (Pitfall 6) | low | mitigate | Beide Fill-Sites filtern `absent_volunteer_ids` per `volunteer_ids.contains(&period.sales_person_id)` — Regressionstest `vaa03_paid_employee_unchanged_regression_lock` (Task 1) blockiert diese Klasse Bug. Grep-Gate in Acceptance-Criteria Task 3 zaehlt `volunteer_ids.contains`-Treffer als hartes Kriterium. |
| T-53-02-SC | Tampering | Cargo-Deps | informational | accept | Keine neuen Cargo-Deps in Phase 53 (RESEARCH.md §"Package Legitimacy Audit"). |

**ASVS-Kategorien Not Applicable in Plan 02**:
- V2 Authentication — kein neuer Auth-Code; bestehender `is_shiftplanner`-Check schuetzt implizit.
- V3 Session Management — unbetroffen.
- V4 Access Control — kein neuer Endpoint, kein neuer Gate-Punkt; das neue Feld liegt hinter dem bestehenden Gate.
- V5 Input Validation — Backend hat keine neuen Inputs; nur Output-Feld auf DTO.
- V7 Cryptography — nicht anwendbar.
- V8 Data Protection — nicht anwendbar; keine persistierten Daten in Phase 53.
</threat_model>

<verification>
- Task 1 RED: alle drei `vaa03_*`-Tests scheitern beim ersten Lauf; nach Task 2 sind sie gruen (drei GREEN-Uebergaenge belegen VAA-01/02/03).
- Task 3 baut Fill-Site 2 strukturell identisch; Regressionstests Chain-C und VFA-01 bleiben gruen (harte Grep-Kriterien in Acceptance).
- `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` in Task 4 als hartes Wave-Gate.
- Kein `cargo sqlx prepare` und kein `.sqlx`-Commit noetig (keine neue Query — RESEARCH.md §"Runtime State Inventory" verifiziert).
- Kein Docs-Trigger ausgeloest (kein `migrations/`, kein `permission.rs`, kein Snapshot-Bump, kein `assemble_weeks`-Touch, kein `reporting.rs`-Touch).
</verification>

<success_criteria>
1. Drei neue Tests `vaa03_*` in `service_impl/src/test/booking_information_vaa.rs` existieren und laufen gruen.
2. `get_weekly_summary` befuellt `sales_person_absences` fuer alle abwesenden Freiwilligen mit `hours = Sigma cap-gated committed_voluntary` (D-53-02).
3. `get_summery_for_week` befuellt dasselbe Feld analog; Fill-Bloecke sind grep-nachweisbar in beiden Methoden.
4. `working_hours_per_sales_person` bleibt fuer Bezahlte unveraendert befuellt (Regression-Lock VAA-03 #3 durch Test verifiziert).
5. `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` gruen.
6. Kein Touch an `assemble_weeks` in `reporting.rs` (D-52-09 MUST-preserve, D-53-05).
</success_criteria>

<output>
Create `.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-02-SUMMARY.md` when done
</output>
