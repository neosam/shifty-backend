---
phase: 53-freiwilligen-abwesenheiten-jahresansicht
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service/src/booking_information.rs
  - rest-types/src/lib.rs
autonomous: true
requirements:
  - VAA-01
must_haves:
  truths:
    - "[D-53-01] SalesPersonAbsence struct existiert in service::booking_information mit Feldern sales_person_id: Uuid, name: Arc<str>, hours: f32 und derive Clone+Debug+PartialEq"
    - "[D-53-01] SalesPersonAbsenceTO struct existiert in rest-types::lib mit denselben Feldern und derive Clone+Debug+PartialEq+Serialize+Deserialize+ToSchema"
    - "[D-53-01] WeeklySummary hat neues Feld sales_person_absences: Arc<[SalesPersonAbsence]> zusaetzlich zu working_hours_per_sales_person"
    - "[D-53-01] WeeklySummaryTO hat neues Feld sales_person_absences: Arc<[SalesPersonAbsenceTO]> mit #[serde(default)] (Pitfall-3-Guard)"
    - "[D-53-01] From<&WeeklySummary> for WeeklySummaryTO mappt sales_person_absences per iter().map(SalesPersonAbsenceTO::from).collect()"
    - "[Regression-Lock] working_hours_per_sales_person Struct + Feld bleiben unveraendert bezahlten-only Vertrag (VAA-03 #3 Vorbereitung)"
    - "cargo build -p service && cargo build -p rest-types laufen warnungsfrei nach Feld-Erweiterung"
    - "cargo test -p rest-types committed_voluntary_hours_defaults_to_zero_when_absent bleibt gruen (Legacy-JSON-Wire-Compat)"
  artifacts:
    - service/src/booking_information.rs (erweitert um SalesPersonAbsence + Feld)
    - rest-types/src/lib.rs (erweitert um SalesPersonAbsenceTO + Feld + From-Impl)
  key_links:
    - "SalesPersonAbsence <-> SalesPersonAbsenceTO via From-Impl (Assembly-Fill-Sites in Plan 02 landen im neuen Service-Feld, DTO-Serialisierung erfolgt automatisch)"
    - "WeeklySummary.sales_person_absences ist die einzige Bruecke, ueber die Freiwilligen-Absencen aus dem Backend zum FE fliessen (Fat Backend, Thin Client, D-51-02)"
---

<objective>
Rein additive Erweiterung der Datenkontrakte um `SalesPersonAbsence`
(Service-Layer) und `SalesPersonAbsenceTO` (DTO-Layer) plus je ein neues
Feld `sales_person_absences` auf `WeeklySummary` und `WeeklySummaryTO`,
inklusive `From`-Impl-Mapping. Kein Business-Logic-Code, keine Fill-Site-
Aenderung — dieses Plan 01 schafft die Traeger, die Plan 02 (Backend-
Assembly) und Plan 03 (FE-Union-Merge) befuellen bzw. konsumieren.

Purpose: Vorbereitung Fat-Backend-Thin-Client-Umbau der Jahresansicht
(VAA-01 gemaess D-53-01). Ohne diese Traeger koennen Plan 02 und Plan 03
nicht compilieren.

Output: Zwei neue Structs + zwei neue Felder + ein erweitertes From-Impl.
Beide Backend-Crates (`service`, `rest-types`) bauen weiterhin
warnungsfrei; Legacy-Wire-Kompat via `#[serde(default)]` erhalten.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md
@service/src/booking_information.rs
@rest-types/src/lib.rs
</context>

## Artifacts this phase produces

- **Struct (service)**: `service::booking_information::SalesPersonAbsence` mit
  Feldern `sales_person_id: Uuid`, `name: Arc<str>`, `hours: f32`,
  `#[derive(Clone, Debug, PartialEq)]`.
- **Struct (DTO)**: `rest_types::SalesPersonAbsenceTO` mit denselben
  Feldern, `#[derive(Clone, Debug, PartialEq, Serialize, Deserialize, ToSchema)]`.
- **Feld (service)**: `WeeklySummary::sales_person_absences: Arc<[SalesPersonAbsence]>`.
- **Feld (DTO)**: `WeeklySummaryTO::sales_person_absences: Arc<[SalesPersonAbsenceTO]>`
  mit `#[serde(default)]` (Legacy-JSON-Kompatibilitaet, Pitfall 3 aus RESEARCH.md).
- **From-Impl (service -> DTO)**: `From<&SalesPersonAbsence> for SalesPersonAbsenceTO`
  hinter `#[cfg(feature = "service-impl")]` (Muster analog zu `From<&WorkingHoursPerSalesPerson>`).
- **From-Impl-Erweiterung**: `From<&WeeklySummary> for WeeklySummaryTO` (Zeile ~1013+)
  mappt das neue Feld per `iter().map(SalesPersonAbsenceTO::from).collect::<Vec<_>>().into()`.

<tasks>

<task type="execute">
  <name>Task 1: SalesPersonAbsence Struct + WeeklySummary-Feld im Service-Layer</name>
  <files>service/src/booking_information.rs</files>
  <read_first>
    - service/src/booking_information.rs (Zeilen 1-56 — Imports, WorkingHoursPerSalesPerson Zeile 24-35, WeeklySummary Zeile 37-56 mit working_hours_per_sales_person Zeile 55)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"1. service/src/booking_information.rs — neues Struct + neues Feld" (Zeilen 26-66) — direktes Analog zu WorkingHoursPerSalesPerson
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G1 — Backend-Traeger" (D-53-01) — verbindliche Struct-Signatur
  </read_first>
  <action>
    Fuege im Service-Crate `service/src/booking_information.rs` **direkt hinter** dem
    bestehenden `WorkingHoursPerSalesPerson`-Struct (Zeile 24-35) einen neuen
    `SalesPersonAbsence`-Struct ein — Felder in exakt der Reihenfolge aus D-53-01:
    `sales_person_id: Uuid`, `name: Arc<str>`, `hours: f32`. Derive-Attribute
    `Clone`, `Debug`, `PartialEq` — identisch zum Nachbar-Struct
    `WorkingHoursPerSalesPerson`. `Uuid` und `Arc` sind bereits in Zeile 3+8
    importiert; keine neuen Imports noetig.

    Direkt darunter im bestehenden `WeeklySummary`-Struct (Zeile 37-56)
    ergaenze das neue Feld `pub sales_person_absences: Arc<[SalesPersonAbsence]>`
    **direkt nach** `working_hours_per_sales_person` (Zeile 55) — analog zum
    Muster in PATTERNS.md §1. Reihenfolge: erst `working_hours_per_sales_person`,
    dann `sales_person_absences` — so bleibt das bezahlten-Vertrag
    (`working_hours_per_sales_person`) semantisch am selben Platz und der
    Freiwilligen-Traeger direkt daneben.

    Alle Anwender, die `WeeklySummary { ... }` als Struct-Literal konstruieren
    (`service_impl/src/booking_information.rs` Zeilen 620, 901, 960 und
    `shifty-dioxus/src/loader.rs` Zeile 518+), muessen im selben Commit compilebar
    bleiben — Setze das neue Feld in genau diesen Literalen auf einen
    leeren Default: im Backend `Arc::from(Vec::<SalesPersonAbsence>::new())`
    bzw. `[].into()`, im Frontend `Vec::new()`. Plan 02 ueberschreibt die
    Backend-Werte im Assembly-Loop mit der echten Berechnung, Plan 03 loest den
    FE-Loader-Default sauber via DTO-Mapping ab. Nach diesem Task **muss beides
    kompilieren**: `cargo build -p service` UND `cargo build --workspace`, weil
    das Feld sonst die Struct-Literal-Sites bricht.

    Kein Business-Logic-Code, keine From-Impls in dieser Datei — reine Struct-Additive.
  </action>
  <verify>
    <automated>cargo build -p service &amp;&amp; cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub struct SalesPersonAbsence" service/src/booking_information.rs` liefert genau eine Zeile.
    - `grep -n "pub sales_person_absences: Arc<\[SalesPersonAbsence\]>" service/src/booking_information.rs` liefert genau eine Zeile.
    - `cargo build --workspace` gruen (kein Feld-fehlt-Literal-Fehler in `service_impl/src/booking_information.rs` oder `shifty-dioxus/src/loader.rs`).
    - `service::booking_information::WorkingHoursPerSalesPerson` Struct bleibt zeichengleich zum Original (Regression-Lock VAA-03 #3): `grep -A 11 "pub struct WorkingHoursPerSalesPerson" service/src/booking_information.rs` unveraendert.
  </acceptance_criteria>
  <done>
    SalesPersonAbsence-Struct + WeeklySummary-Feld existieren; Workspace baut warnungsfrei; alle bestehenden Struct-Literal-Sites verwenden leere Defaults, die Plan 02/03 ersetzen werden.
  </done>
</task>

<task type="execute">
  <name>Task 2: SalesPersonAbsenceTO Struct + WeeklySummaryTO-Feld + From-Impl im DTO-Layer</name>
  <files>rest-types/src/lib.rs</files>
  <read_first>
    - rest-types/src/lib.rs (Zeilen 955-1036: WorkingHoursPerSalesPersonTO + From-Impl Zeilen 955-987, WeeklySummaryTO + From-Impl Zeilen 989-1036, insbesondere `#[serde(default)]` auf committed_voluntary_hours Zeile 997-998)
    - rest-types/src/lib.rs (Zeilen 2536-2565: bestehender Test `committed_voluntary_hours_defaults_to_zero_when_absent` — Muster fuer Legacy-JSON-Wire-Kompat, muss nach Erweiterung weiterhin gruen sein)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"4. rest-types/src/lib.rs" (Zeilen 265-338) — vollstaendige Struct-Signatur + From-Impl-Muster
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md §"OpenAPI / utoipa Impact" (Zeilen 460-482) — bestaetigt: `WeeklySummaryTO` selbst hat kein `ToSchema`, kein ApiDoc-Eintrag noetig; `SalesPersonAbsenceTO` bekommt trotzdem `ToSchema` (D-53-01)
  </read_first>
  <action>
    In `rest-types/src/lib.rs` genau drei Erweiterungen — **kein anderer Code**:

    (a) **Neuer Struct** — direkt hinter `WorkingHoursPerSalesPersonTO` (Zeile
    955-966), vor `WeeklySummaryTO` (Zeile 989): `SalesPersonAbsenceTO` mit
    Feldern `sales_person_id: Uuid`, `name: Arc<str>`, `hours: f32` und
    Derive-Attributen `Clone`, `Debug`, `PartialEq`, `Serialize`, `Deserialize`,
    `ToSchema`. `Uuid`, `Arc`, `Serialize`, `Deserialize`, `ToSchema` sind alle
    bereits am Datei-Anfang importiert.

    (b) **From-Impl** — direkt unter dem neuen Struct: `impl
    From<&service::booking_information::SalesPersonAbsence> for
    SalesPersonAbsenceTO`, hinter dem selben Feature-Gate, das die anderen
    From-Impls in dieser Datei nutzen (PATTERNS.md §4 nennt es
    `#[cfg(feature = "service-impl")]`; pruefe die bestehende
    `From<&WorkingHoursPerSalesPerson> for WorkingHoursPerSalesPersonTO`-
    Impl Zeile 968-987 und uebernimm exakt dieselben Gate-Attribute wie dort,
    damit das Feature-Setup byte-identisch bleibt). Body kopiert `sales_person_id`
    direkt, `name` per `.clone()` (Arc-Clone), `hours` direkt.

    (c) **Feld auf `WeeklySummaryTO`** — direkt hinter dem bestehenden Feld
    `pub working_hours_per_sales_person: Arc<[WorkingHoursPerSalesPersonTO]>`
    (Zeile 1006) neues Feld
    `pub sales_person_absences: Arc<[SalesPersonAbsenceTO]>` **mit
    `#[serde(default)]`** auf der Zeile davor. Der `#[serde(default)]`-Guard
    ist Pflicht: Er verhindert Deser-Fehler bei Legacy-JSON-Responses ohne
    das neue Feld (Pitfall 3 aus RESEARCH.md); Praezedenz ist
    `committed_voluntary_hours` Zeile 997-998 in derselben Datei.

    (d) **From-Impl-Erweiterung** — im `From<&WeeklySummary> for
    WeeklySummaryTO` (Zeilen 1013+) direkt hinter dem
    `working_hours_per_sales_person`-Mapping-Block (Zeilen 1026-1033) das
    neue Feld mappen — `iter().map(SalesPersonAbsenceTO::from).collect::<Vec<_>>().into()`
    exakt analog zum Nachbar-Block (PATTERNS.md §4, Ziel-Delta am Ende der
    Section). Die From-Referenz `SalesPersonAbsenceTO::from` funktioniert nur,
    wenn Task 2a die `From`-Impl unter demselben Feature-Gate stehen hat
    wie das umschliessende `From<&WeeklySummary>` — pruefe das mit einem
    Compile-Lauf.

    Der Legacy-Wire-Kompat-Test `committed_voluntary_hours_defaults_to_zero_when_absent`
    (Zeilen 2536-2565) darf NICHT touched werden — bleibt als
    Regression-Guard gruen und bestaetigt implizit, dass das neue Feld
    per `#[serde(default)]` ebenfalls Legacy-tolerant ist. Optional: einen
    zusaetzlichen Test `sales_person_absences_defaults_to_empty_when_absent`
    im selben Muster erlaubt; **nicht Pflicht**, das Serde-Verhalten ist
    bereits durch das Attribute + den bestehenden Praezedenz-Test gedeckt.
  </action>
  <verify>
    <automated>cargo build -p rest-types &amp;&amp; cargo test -p rest-types committed_voluntary_hours</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub struct SalesPersonAbsenceTO" rest-types/src/lib.rs` liefert genau eine Zeile.
    - `grep -B 1 "pub sales_person_absences: Arc<\[SalesPersonAbsenceTO\]>" rest-types/src/lib.rs` zeigt `#[serde(default)]` in der direkt vorangegangenen Zeile (Pitfall-3-Guard).
    - `grep -n "impl From<&service::booking_information::SalesPersonAbsence> for SalesPersonAbsenceTO" rest-types/src/lib.rs` liefert genau eine Zeile.
    - `grep -n "SalesPersonAbsenceTO::from" rest-types/src/lib.rs` liefert mindestens eine Zeile im `From<&WeeklySummary>`-Block.
    - `cargo test -p rest-types committed_voluntary_hours_defaults_to_zero_when_absent` gruen (Legacy-Wire-Kompat unveraendert).
    - `cargo build --workspace` gruen (Feature-Gate-Attribute an `From<&SalesPersonAbsence> for SalesPersonAbsenceTO` matched das umschliessende `From<&WeeklySummary>` — kein "cannot find function `SalesPersonAbsenceTO::from`"-Fehler).
  </acceptance_criteria>
  <done>
    SalesPersonAbsenceTO + WeeklySummaryTO.sales_person_absences + From-Impl-Kette existieren; Legacy-Wire-Kompat-Test bleibt gruen; ToSchema am neuen DTO-Struct vorhanden; kein ApiDoc-Eintrag noetig (WeeklySummaryTO hat selbst kein ToSchema — RESEARCH.md verifiziert).
  </done>
</task>

<task type="execute">
  <name>Task 3: Wave-1-Gate — Clippy + Full-Workspace-Test</name>
  <files></files>
  <read_first>
    - CLAUDE.md §"Development Guidelines" — Clippy-Gate ist Pflicht (`cargo clippy --workspace -- -D warnings`).
    - shifty-backend/CLAUDE.md §"Testing" — `cargo test --workspace` als Full-Suite-Kriterium.
    - Memory `feedback_clippy_gate` — `cargo test` allein reicht NICHT; `nix build` failt bei jedem Clippy-Warning.
  </read_first>
  <action>
    Fuehre nacheinander drei Kommandos aus:
    1. `cargo build --workspace` — sanity check nach den beiden Struct/DTO-Erweiterungen.
    2. `cargo test --workspace` — bestaetigt keine Regression an bestehenden Tests
       (insbesondere `booking_information_vfa`, `booking_information_chain_c`,
       `committed_voluntary_hours_defaults_to_zero_when_absent`).
    3. `cargo clippy --workspace -- -D warnings` — hartes Clippy-Gate; jedes
       Warning bricht den Task ab (Pflicht laut CLAUDE.md und Memory
       `feedback_clippy_gate`).

    Kein FE-WASM-Gate in dieser Wave — Plan 03 erledigt das nach dem FE-Umbau.
    Kein `cargo sqlx prepare` — Phase 53 hat keine neue SQL-Query (RESEARCH.md
    §"Runtime State Inventory" verifiziert).
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei Kommandos exit-code 0.
    - `cargo clippy` liefert keine Warnung (`-D warnings` ist scharfgestellt).
    - Kein Test schlaegt fehl, insbesondere nicht der Legacy-Wire-Kompat-Test in rest-types.
  </acceptance_criteria>
  <done>
    Wave 1 abgeschlossen. Datenkontrakte stehen; Plan 02 kann Backend-Fill-Sites befuellen, Plan 03 kann FE-Union-Merge auf das neue DTO-Feld verdrahten.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Keine neue Boundary; das neue DTO-Feld wird ueber denselben Endpoint (`GET /booking-information/weekly-resource-report/{year}`) geliefert, der bereits `SHIFTPLANNER_PRIVILEGE`-gated ist. |
| Backend-intern | Kein neuer Datenfluss; nur additive Feld-Erweiterung ohne Business-Logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-01-01 | Information Disclosure | `SalesPersonAbsenceTO.name` im JSON-Response | informational | mitigate | Kein neuer Datenpfad — Freiwilligen-Namen sind bereits im selben Endpoint via `working_hours_per_sales_person` (bezahlt) und via `/sales-persons` verfuegbar; `is_shiftplanner`-Gate im umschliessenden Endpoint (unveraendert) schuetzt sie identisch. Kein Aenderungsbedarf in Plan 01 (reine Struct-Additive). Verifikation im Plan-02-Threat-Model, da die Assembly-Fill-Site den `is_shiftplanner`-Check verantwortet. |
| T-53-01-SC | Tampering | npm/pip/cargo installs | informational | accept | Keine neuen Cargo-Deps in Phase 53 (RESEARCH.md §"Package Legitimacy Audit" bestaetigt). Kein Legitimacy-Gate noetig. |

**ASVS-Kategorien Not Applicable in Plan 01** (reine Datenkontrakt-Erweiterung):
- V2 Authentication — kein neuer Auth-Code.
- V3 Session Management — kein neuer Session-Pfad.
- V4 Access Control — keine neue Endpoint, kein neuer Gate-Punkt.
- V5 Input Validation — keine neuen Inputs (nur Output-Feld auf Struct).
- V7 Cryptography — nicht anwendbar.
- V8 Data Protection — nicht anwendbar; keine persistierten Daten in Phase 53 (kein Migration, kein Snapshot-Bump).
</threat_model>

<verification>
- `cargo build --workspace` gruen nach beiden Tasks (kein Struct-Literal-Feld-fehlt-Fehler).
- `cargo test --workspace` gruen — insbesondere `committed_voluntary_hours_defaults_to_zero_when_absent` (Regression-Guard fuer Legacy-Wire-Kompat).
- `cargo clippy --workspace -- -D warnings` gruen.
- grep-Verifikationen aus den Task-Acceptance-Criteria (Struct-Existenz + `#[serde(default)]` + From-Impl-Namen).
- Kein neuer `cargo sqlx prepare`-Lauf noetig (keine neue SQL-Query — RESEARCH.md §"Runtime State Inventory").
- Kein Docs-Freshness-Trigger ausgeloest in Wave 1 (kein `migrations/`, kein `permission.rs`, kein Snapshot-Bump, keine Trait-Signatur-Aenderung).
</verification>

<success_criteria>
1. `service::booking_information::SalesPersonAbsence` existiert mit Feldern `sales_person_id`, `name`, `hours` und Derives `Clone+Debug+PartialEq`.
2. `rest_types::SalesPersonAbsenceTO` existiert mit denselben Feldern und Derives `Clone+Debug+PartialEq+Serialize+Deserialize+ToSchema`.
3. `WeeklySummary.sales_person_absences: Arc<[SalesPersonAbsence]>` und `WeeklySummaryTO.sales_person_absences: Arc<[SalesPersonAbsenceTO]>` (mit `#[serde(default)]`) existieren.
4. `From<&SalesPersonAbsence> for SalesPersonAbsenceTO` + Erweiterung des `From<&WeeklySummary> for WeeklySummaryTO`-Mappings existieren.
5. `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` beide gruen.
6. `working_hours_per_sales_person`-Struct + Feld sind unveraendert (Regression-Lock VAA-03 #3 Vorbereitung).
</success_criteria>

<output>
Create `.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-01-SUMMARY.md` when done
</output>
