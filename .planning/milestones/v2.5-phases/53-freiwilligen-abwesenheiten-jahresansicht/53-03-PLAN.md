---
phase: 53-freiwilligen-abwesenheiten-jahresansicht
plan: 03
type: tdd
wave: 3
depends_on:
  - 53-01
  - 53-02
files_modified:
  - shifty-dioxus/src/state/weekly_overview.rs
autonomous: true
requirements:
  - VAA-01
  - VAA-04
must_haves:
  truths:
    - "[D-53-04] FE-Mapper baut Union-Vec aus (a) bestehendem Bezahlten-Loop ueber working_hours_per_sales_person + (b) Iteration ueber neues DTO-Feld sales_person_absences"
    - "[D-53-04] Union-Vec wird nach name case-insensitive sortiert via sort_by(|x, y| x.name.to_lowercase().cmp(&y.name.to_lowercase())); Vec::sort_by ist stable (Namens-Duplikate ok)"
    - "[D-53-04] FE-Filter >= 0.1 gilt auch fuer Freiwillige (Randfall Zusage=0 raus)"
    - "[D-53-04] Keine visuelle Unterscheidung Freiwilliger/Bezahlt — kein Icon, kein Suffix, keine Farbe (VAA-04-Lock)"
    - "[VAA-04-Lock] Rendering-Zeile shifty-dioxus/src/page/weekly_overview.rs Zeile 126 (format!(\"{}: {} {hours_short}\", absence.name, format_hours(absence.absence_hours, 2))) bleibt woertlich unveraendert; grep-Guard bestaetigt Existenz"
    - "[Regression-Lock] Bezahlten-Filter effective_absence = absence_hours - holiday_hours + unavailable_hours >= 0.1 bleibt inhaltlich unveraendert (VAA-03 #3 FE-Seite)"
    - "[Pitfall 5] make_to()-Test-Fixture in state/weekly_overview.rs bekommt das neue sales_person_absences-Feld (Vec::new().into()) — sonst Compile-Fehler"
    - "cargo test -p shifty-dioxus state::weekly_overview gruen; Union-Merge-Assertion (Freiwilliger + Bezahlter in Sort-Order) belegt VAA-04-Anzeige"
    - "cargo build --target wasm32-unknown-unknown im shifty-dioxus-Workspace gruen (WASM-Gate)"
  artifacts:
    - shifty-dioxus/src/state/weekly_overview.rs (Union-Refactor des From<&WeeklySummaryTO>-Impl + make_to-Fixture erweitert + neuer Union-Merge-Test)
  key_links:
    - "state::WeeklySummary::from(&WeeklySummaryTO) ist der einzige FE-Bruecken-Punkt zwischen DTO-Feld sales_person_absences und der Anzeige-Zeile in page/weekly_overview.rs:126"
    - "page/weekly_overview.rs:121-131 rendert die Union-Liste unveraendert (VAA-04-Lock); kein Touch in diesem Plan"
---

<objective>
Der FE-Mapper `impl From<&WeeklySummaryTO> for state::WeeklySummary`
(shifty-dioxus/src/state/weekly_overview.rs Zeilen 30-65) baut die
Anzeige-Liste `sales_person_absences: Vec<SalesPersonAbsence>` aus **zwei**
Quellen zusammen und sortiert sie case-insensitive nach Name (D-53-04).
Bestehender Bezahlten-Loop ueber `working_hours_per_sales_person` bleibt
**inhaltlich unveraendert** (Regression-Lock VAA-03 #3). Neu: Iteration
ueber das in Plan 02 befuellte `WeeklySummaryTO.sales_person_absences`-Feld
mit demselben `>= 0.1`-Filter.

Purpose: VAA-01 (Freiwillige erscheinen sichtbar) + VAA-04 (visuelle
Konsistenz, keine neue Farbe/Icon, Rendering-Zeile in
`page/weekly_overview.rs:126` bleibt woertlich unveraendert).

Output: Ein-Datei-Aenderung im FE (`state/weekly_overview.rs`), erweiterte
Test-Fixture `make_to()` (Pitfall 5), neuer Test der die Sort-Order und
Union-Vollstaendigkeit beweist, sowie WASM-Build-Gate. Kein Touch an
`page/weekly_overview.rs`, `loader.rs`, `weekly_overview_chart.rs`
(FE-Init-Sites nutzen bereits Default-Vec und brechen nicht, weil das
`state::WeeklySummary.sales_person_absences`-Feld schon vor Phase 53
existiert — RESEARCH.md §"FE-Type-Init-Sites vollstaendig" verifiziert).

**TDD-Zyklus:**
- **RED:** Task 1 erweitert `make_to()`-Fixture (compile-fix) und schreibt
  einen neuen Union-Merge-Test der gegen den heutigen bezahlten-only Mapper
  scheitert (Freiwilliger aus DTO-Feld fehlt).
- **GREEN:** Task 2 baut den Union-Refactor, Test wird gruen.
- **REFACTOR:** Task 3 verifiziert Rendering-Lock + WASM-Gate.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-01-PLAN.md
@.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-02-PLAN.md
@shifty-dioxus/src/state/weekly_overview.rs
@shifty-dioxus/src/page/weekly_overview.rs
</context>

## Artifacts this phase produces

- **Umgebauter `From<&WeeklySummaryTO> for state::WeeklySummary`**: neuer
  Union-Merge in `shifty-dioxus/src/state/weekly_overview.rs` (Zeilen 30-65
  heute). Bezahlten-Loop bleibt inhaltlich unveraendert, Freiwilligen-Loop
  kommt dazu, anschliessend `sort_by` case-insensitive nach Name.
- **Erweiterte `make_to()`-Fixture**: Zeile ~83-101 bekommt neues Feld
  `sales_person_absences: Vec::new().into()` (Pitfall 5 — sonst Compile-Fehler
  nach DTO-Erweiterung aus Plan 01).
- **Neuer Union-Merge-Test**: `union_merge_freiwilliger_und_bezahlter_sortiert_nach_name`
  (oder aequivalenter, aussagekraeftiger Name) im bestehenden `mod tests`-Block.
  Prueft: Freiwilliger `"Bob"` (aus DTO-Feld) + Bezahlter `"Anna"` (aus
  `working_hours_per_sales_person`) landen beide in `sales_person_absences`
  in Sort-Order Anna vor Bob.
- **Zusatz-Test fuer Regression-Lock VAA-03 #3**: `bezahlter_bleibt_via_working_hours_pfad_sichtbar`
  (o.a.), stellt sicher, dass die bezahlten-Zeile mit `effective_absence >= 0.1`
  weiterhin via `working_hours_per_sales_person`-Loop landet, auch wenn das
  neue DTO-Feld sie nicht enthaelt.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — make_to()-Fixture erweitern + Union-Merge-Test schreiben</name>
  <files>shifty-dioxus/src/state/weekly_overview.rs</files>
  <read_first>
    - shifty-dioxus/src/state/weekly_overview.rs (komplette Datei, 131 Zeilen — Struct-Definitionen Zeilen 5-28, From-Impl Zeilen 30-65, Tests Zeilen 76-130 inklusive `make_to()`-Fixture Zeilen 83-101 und zwei bestehende Tests Zeilen 107-129)
    - shifty-dioxus/src/page/weekly_overview.rs (Zeilen 121-131 — die Rendering-Zeile format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2)) auf Zeile 126 ist der VAA-04-Lock)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"5. shifty-dioxus/src/state/weekly_overview.rs" (Zeilen 342-410) — Ziel-Delta des Union-Merge inklusive make_to-Fixture-Note
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md §"FE-Union-Merge in state/weekly_overview.rs" (Zeilen 358-425) — Bezahlt-Loop UNVERAENDERT + Freiwilligen-extend + Sort-by
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G4 — Visuelle Praesentation" (D-53-04) — Sortier-Regel + kein visueller Unterschied
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G5 — Fill-Site" (D-53-05) — FE-Mapper-Folgeschritt-Semantik
  </read_first>
  <behavior>
    Nach Task 1 muss `cargo build -p shifty-dioxus` gruen bleiben (weil die
    Fixture-Erweiterung compile-verhindernd sein wuerde, wenn Plan 01 das
    neue Feld auf `WeeklySummaryTO` schon eingefuehrt hat und die Fixture es
    nicht setzt — das ist Pitfall 5). Der neue Union-Merge-Test scheitert
    jedoch, weil der aktuelle Mapper (Zeilen 47-62) nur ueber
    `working_hours_per_sales_person` iteriert und den Freiwilligen aus dem
    DTO-Feld ignoriert.

    Konkret: Der Test baut ein `WeeklySummaryTO` mit
    - `working_hours_per_sales_person` = 1 Bezahlter „Anna" mit `absence_hours = 8.0`,
      `holiday_hours = 0.0`, `unavailable_hours = 0.0` (`effective_absence = 8.0 >= 0.1`).
    - `sales_person_absences` (neues DTO-Feld) = 1 Freiwilliger „Bob" mit `hours = 5.0`.
    Erwartung: `state::WeeklySummary::from(&to).sales_person_absences`
    enthaelt beide, sortiert Anna vor Bob.

    Vor dem Union-Refactor: Bob fehlt → Test FAIL.
  </behavior>
  <action>
    Zwei Aenderungen in `shifty-dioxus/src/state/weekly_overview.rs`:

    (a) **`make_to()`-Fixture erweitern** (Zeilen 83-101): Direkt nach der
    Zeile `working_hours_per_sales_person: Vec::new().into(),` (Zeile 99) das
    neue Feld einfuegen: `sales_person_absences: Vec::new().into(),`. Ohne
    diesen Fix bricht die Fixture nach der DTO-Erweiterung aus Plan 01 den
    Build (Struct-Literal muss alle Felder nennen — Pitfall 5).

    (b) **Neuen Union-Merge-Test schreiben** — im bestehenden `mod tests`-
    Block (nach dem letzten bestehenden Test auf Zeile 129):

    Test-Name: `sales_person_absences_union_merges_paid_and_volunteers_sorted_by_name`
    (deskriptiv, matched das Verhalten).

    Aufbau:
    1. Baue eine erweiterte `WeeklySummaryTO`-Fixture (kann `make_to()`
       kopieren oder einen dedizierten Helper `make_to_with_paid_and_volunteer()`
       einfuehren; letzteres empfohlen fuer Lesbarkeit).
    2. Setze `working_hours_per_sales_person` = ein `WorkingHoursPerSalesPersonTO`
       mit `sales_person_name = "Anna".into()`, `absence_hours = 8.0`,
       `holiday_hours = 0.0`, `unavailable_hours = 0.0`, restliche Felder
       Default oder Dummy-Wert.
    3. Setze `sales_person_absences` = ein `SalesPersonAbsenceTO` mit `name
       = "Bob".into()`, `hours = 5.0`, `sales_person_id = Uuid::nil()` (nur
       im FE-State nicht getragen, aber TO braucht das Feld).
    4. Rufe `state::WeeklySummary::from(&to)`, extrahiere `sales_person_absences`.
    5. Assert: `.len() == 2`, `[0].name.as_ref() == "Anna"`, `[1].name.as_ref() == "Bob"`
       (case-insensitive sort mit 'A' < 'B'), `[0].absence_hours ≈ 8.0`,
       `[1].absence_hours ≈ 5.0`.

    Zusatztest (optional, empfohlen): `bezahlter_bleibt_via_working_hours_pfad_sichtbar`
    — nur Bezahlter „Anna" (`sales_person_absences` DTO-Feld leer),
    Erwartung: `.len() == 1`, `[0].name.as_ref() == "Anna"`. Belegt
    Regression-Lock VAA-03 #3 aus FE-Sicht.

    Kein From-Impl-Touch in dieser Task — der Test scheitert erwartungsgemaess.

    **WICHTIG — Fenced-Code-Diszplin fuer den finalen Grep-Guard**: In
    Kommentaren, `println!` oder `assert!`-Messages **niemals** die Zeichenkette
    `format!("{}: {} {hours_short}"` woertlich verwenden — der grep-Guard aus
    Task 3 sucht genau nach diesem Muster in
    `page/weekly_overview.rs`, und ein Vorkommen in der State-Datei wuerde
    ihn nicht direkt brechen (grep zielt auf die page-Datei), aber es ist
    trotzdem hygienischer, das Rendering-Format nur an genau EINEM Ort im
    Repo zu haben. Beschreibe stattdessen prosaisch: „Sort-Order Anna vor
    Bob nach case-insensitive Name-Sort".
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo test state::weekly_overview 2>&amp;1 | grep -E "FAILED|test result" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd shifty-dioxus &amp;&amp; cargo build` gruen (make_to-Fixture-Erweiterung fixt den Compile-Break, Pitfall 5 abgefangen).
    - Der neue Union-Merge-Test `sales_person_absences_union_merges_paid_and_volunteers_sorted_by_name` schlaegt fehl (RED-Baseline: Bob aus DTO-Feld wird noch nicht in die Liste uebernommen).
    - Bestehende Tests `committed_voluntary_hours_maps_from_to` und `available_hours_maps_from_overall_available_hours` bleiben gruen (keine Regression durch Fixture-Erweiterung).
  </acceptance_criteria>
  <done>
    RED-Baseline etabliert: Union-Test scheitert, Fixture-Compile-Fix ist drin, bestehende Tests gruen. Task 2 macht den Union-Test gruen.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — Union-Refactor im From<&WeeklySummaryTO>-Mapper</name>
  <files>shifty-dioxus/src/state/weekly_overview.rs</files>
  <read_first>
    - shifty-dioxus/src/state/weekly_overview.rs (Zeilen 30-65 — bestehender Bezahlt-Only Mapper, insbesondere Zeilen 47-62 mit dem `sales_person_absences: summary.working_hours_per_sales_person.iter().filter_map(...).collect()`-Block)
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-PATTERNS.md §"5" Ziel-Delta (Zeilen 374-410) — vollstaendiger Union-Merge-Code
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-RESEARCH.md §"FE-Union-Merge" (Zeilen 384-419) — inklusive Randfall Zusage=0
    - .planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-CONTEXT.md §"G4" (D-53-04) — Kein visueller Unterschied
  </read_first>
  <behavior>
    Nach diesem Task muss der Union-Merge-Test aus Task 1 gruen sein. Die
    Regression-Locks bleiben erhalten: `committed_voluntary_hours_maps_from_to`
    und `available_hours_maps_from_overall_available_hours` gruen; falls Task 1
    einen `bezahlter_bleibt_via_working_hours_pfad_sichtbar`-Test schrieb,
    ebenfalls gruen.
  </behavior>
  <action>
    Baue den `From<&WeeklySummaryTO> for WeeklySummary`-Impl (Zeilen 30-65)
    um. Statt des heutigen Struct-Literals mit `sales_person_absences:
    summary.working_hours_per_sales_person.iter().filter_map(...).collect()`
    (Zeilen 47-62) eine Block-Expression, die einen `Vec<SalesPersonAbsence>`
    baut, sortiert, und dann in das Struct-Feld ubergibt (PATTERNS.md §5
    Ziel-Delta, RESEARCH.md §"FE-Union-Merge" Zeilen 384-419). Konkret:

    1. **Bezahlten-Loop UNVERAENDERT** — der bestehende `filter_map` ueber
       `summary.working_hours_per_sales_person` mit dem gleichnamigen Filter
       `effective_absence = absence_hours - holiday_hours + unavailable_hours`
       und Schwelle `>= 0.1` wird in eine `let mut v: Vec<SalesPersonAbsence>
       = ...collect();`-Bindung uebertragen — inhaltlich identisch, nur die
       aeussere Klammer wird zu einem Block. Dies ist der Regression-Lock
       fuer VAA-03 #3.
    2. **Freiwilligen-`extend`** — direkt anschliessend:
       `v.extend(summary.sales_person_absences.iter().filter(|a| a.hours >= 0.1)
       .map(|a| SalesPersonAbsence { name: a.name.clone(), absence_hours: a.hours }));`.
       Der `>= 0.1`-Filter faengt den Randfall Zusage=0 (D-53-04) ab.
    3. **Sortierung** — `v.sort_by(|x, y| x.name.to_lowercase().cmp(&y.name.to_lowercase()));`.
       Case-insensitive; Vec::sort_by ist stable (Namens-Duplikate haben
       stabile Reihenfolge — Sortier-Stabilitaet aus RESEARCH.md §"Open
       Questions" Punkt 1 dokumentiert).
    4. **Rueckgabe** — `v` in das Struct-Feld `sales_person_absences` (der
       Block-Ausdruck ergibt `v`).

    Kein `SalesPersonAbsence`-Struct-Aenderung im FE-State (der behaelt seine
    zwei Felder `name` + `absence_hours` — der neue DTO-`sales_person_id`
    wird bewusst NICHT in den FE-State uebernommen; er lebt nur im TO fuer
    zukuenftige Nutzungen). Kein Redesign in `page/weekly_overview.rs`.
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo test state::weekly_overview</automated>
  </verify>
  <acceptance_criteria>
    - Alle Tests in `mod tests` gruen: neuer Union-Merge-Test + bestehende `committed_voluntary_hours_maps_from_to` + `available_hours_maps_from_overall_available_hours` + optionaler `bezahlter_bleibt_via_working_hours_pfad_sichtbar`.
    - `grep -n "sort_by" shifty-dioxus/src/state/weekly_overview.rs` liefert mindestens eine Zeile mit `to_lowercase().cmp` (case-insensitive Sort dokumentiert).
    - `grep -n "summary.sales_person_absences" shifty-dioxus/src/state/weekly_overview.rs` liefert mindestens eine Zeile (Freiwilligen-Iteration verdrahtet).
    - Der bestehende `filter_map`-Block ueber `working_hours_per_sales_person` mit `effective_absence = absence_hours - holiday_hours + unavailable_hours` ist inhaltlich unveraendert vorhanden (Regression-Lock VAA-03 #3): `grep -A 2 "effective_absence" shifty-dioxus/src/state/weekly_overview.rs` zeigt die 3 Term-Formel unveraendert.
  </acceptance_criteria>
  <done>
    Union-Merge aktiv, Sort case-insensitive, Bezahlten-Regression-Lock erhalten. Rendering-Zeile in `page/weekly_overview.rs` wird von Task 3 als Lock verifiziert.
  </done>
</task>

<task type="execute">
  <name>Task 3: Wave-3-Gate — Rendering-Lock + WASM-Build + Full-Workspace-Test + Clippy</name>
  <files></files>
  <read_first>
    - shifty-dioxus/src/page/weekly_overview.rs (Zeilen 120-131 — die Rendering-Zeile ist auf Zeile 126: `format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2))`)
    - CLAUDE.md §"WASM-Build-Gate" — `cargo build --target wasm32-unknown-unknown` aus `shifty-dioxus/` (Pflicht-Gate).
    - shifty-dioxus/CLAUDE.md §"Code Quality" — `cargo clippy` in shifty-dioxus (Memory `reference_dioxus_clippy_not_gated` sagt: shifty-dioxus ist eigener Workspace und **kein Teil** des Backend-Clippy-Gates; das FE-Clippy-Gate ist seit v2.2/Phase 45 erstmals grün-gestellt und wird als hartes Gate weiter erwartet).
    - Memory `feedback_clippy_gate` + `reference_dioxus_clippy_not_gated` — beide relevant: Backend-Clippy separat vom FE-Clippy.
    - Memory `project_frontend_dx_version_pin` — dx CLI ist gepinnt; kein Impact hier, nur Info.
  </read_first>
  <action>
    Vier Gates sequentiell — jedes einzelne muss gruen sein:

    (a) **VAA-04 Rendering-Lock (grep-Gate)**: Verifiziere, dass die
    Rendering-Zeile in `shifty-dioxus/src/page/weekly_overview.rs` **woertlich
    unveraendert** vorliegt. Kommando:
    `grep -n 'format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2))' shifty-dioxus/src/page/weekly_overview.rs`
    MUSS genau eine Zeile liefern (heute Zeile 126). Sollte der grep leer
    zurueckkommen, ist der VAA-04-Lock verletzt — Task schlaegt fehl. Kein
    Touch an dieser Datei in Wave 3 (das ist der Sinn des Locks).

    (b) **WASM-Build-Gate**:
    `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown`.
    Muss warnungsfrei durchlaufen (Memory `feedback_dioxus_proxy_for_new_backend_endpoints`
    ist hier NICHT relevant — kein neuer Backend-Endpoint in Phase 53).

    (c) **FE-Test + FE-Clippy** — `cd shifty-dioxus && cargo test`; danach
    `cd shifty-dioxus && cargo clippy -p shifty-dioxus -- -D warnings`. Der
    FE-Clippy-Gate ist scharfgestellt seit Phase 45 (v2.2); jede Warning
    bricht ab. Memory `reference_dioxus_clippy_not_gated` warnt vor
    Toolchain-Split — Clippy MUSS aus der Backend-Shell laufen (nix develop
    aus shifty-backend-Root), nicht aus dem dioxus-Nix-Shell (dort ist
    Clippy E0514-kaputt).

    (d) **Backend-Regression** — `cargo build --workspace && cargo test --workspace &&
    cargo clippy --workspace -- -D warnings` (aus shifty-backend-Root).
    Bestaetigt: Backend bleibt von der FE-Aenderung unberuehrt, alle
    Wave-1/2-Tests weiterhin gruen.

    Keine Docs-Aenderung in dieser Wave (F07-Update ist optional; RESEARCH.md
    §"Docs-Freshness-Trigger-Check" hat keinen harten Trigger ausgeloest — falls
    User es wuenscht, kann ein Nebensatz in `docs/features/F07-reporting-balance.md`
    + `_de.md` als Follow-up ausserhalb dieser Phase erfasst werden).
  </action>
  <verify>
    <automated>grep -n 'format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2))' shifty-dioxus/src/page/weekly_overview.rs &amp;&amp; cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown &amp;&amp; cargo test &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Rendering-Zeile in `page/weekly_overview.rs` woertlich unveraendert (grep-Gate liefert genau eine Zeile mit `format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2))`).
    - `cargo build --target wasm32-unknown-unknown` in shifty-dioxus warnungsfrei.
    - `cd shifty-dioxus && cargo test` gruen (inklusive der neuen Union-Merge-Tests und der bestehenden Tests).
    - `cd shifty-dioxus && cargo clippy -p shifty-dioxus -- -D warnings` gruen (FE-Clippy-Gate seit v2.2 scharf).
    - `cargo build --workspace && cargo test --workspace && cargo clippy --workspace -- -D warnings` aus shifty-backend-Root gruen (Backend-Regression).
  </acceptance_criteria>
  <done>
    Phase 53 komplett integriert: DTO+Struct existieren (Plan 01), Backend liefert das Feld gefuellt (Plan 02), FE zeigt die Union-Liste sortiert an (Plan 03). VAA-01/02/03/04 durch Tests + grep-Locks belegt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> Client | FE deserialisiert `WeeklySummaryTO` per serde. Kein neuer Auth-Pfad — Endpoint bleibt shiftplanner-gated. `#[serde(default)]` auf `sales_person_absences` (Plan 01) schuetzt Legacy-Wire-Kompat. |
| Client-intern | Rendering-Layer konsumiert die neue Union-Liste. Kein XSS-Risiko: `format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2))` ist Dioxus RSX, das automatisch escaped (kein `dangerous_inner_html`, kein Direct-DOM-Insert). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-03-01 | Information Disclosure | Freiwilligen-Namen im Client-Rendering | informational | mitigate | Endpoint ist shiftplanner-gated (Backend, Plan 02); Client kann nur zeigen was Backend liefert. Keine neue Client-seitige Persistenz oder Uebertragung an Dritte. |
| T-53-03-02 | Tampering | Alte JSON-Responses ohne `sales_person_absences`-Feld | informational | mitigate | `#[serde(default)]` in `WeeklySummaryTO` (Plan 01 Task 2) macht das Feld optional beim Deser; FE-Mapper iteriert dann ueber leeres `Arc<[]>`, keine Absturz. |
| T-53-03-03 | Spoofing/XSS | Injizierter HTML in Freiwilligen-Name via `format!` | informational | accept | Dioxus RSX escaped automatisch alle Interpolationen; kein `dangerous_inner_html` im FE-Rendering. Der Rendering-Zeile ist unveraendert (VAA-04-Lock), kein neuer Angriffsflaechen-Ort. |
| T-53-03-SC | Tampering | npm/pip/cargo installs | informational | accept | Keine neuen Deps in Phase 53. |

**ASVS-Kategorien Not Applicable in Plan 03**:
- V2 Authentication — kein Auth-Code im FE-Mapper.
- V3 Session Management — kein Session-Impact.
- V4 Access Control — keine neue Rolle, kein neuer Gate-Punkt; das serverseitige `is_shiftplanner`-Gate ist einziger Schutz und lebt in Plan 02.
- V5 Input Validation — kein neuer User-Input; das FE liest nur DTO-Response.
- V7 Cryptography — nicht anwendbar.
- V8 Data Protection — nicht anwendbar; kein Client-Storage der Freiwilligen-Absencen ueber Session-Ende hinaus.
</threat_model>

<verification>
- Task 1 RED: Union-Merge-Test scheitert; `make_to()`-Fixture-Compile-Fix ist drin (Pitfall 5).
- Task 2 GREEN: Union-Merge-Refactor macht den Test gruen; bestehende Regression-Tests fuer Bezahlten-Pfad bleiben gruen.
- Task 3 Gate: grep-Lock auf Rendering-Zeile (VAA-04), WASM-Build gruen, FE-Test gruen, FE-Clippy gruen, Backend-Regression aus shifty-backend-Root gruen.
- Rendering-Zeile in `page/weekly_overview.rs:126` bleibt **woertlich unveraendert** — `format!("{}: {} {hours_short}", absence.name, format_hours(absence.absence_hours, 2))`. Kein neuer Farb-, Icon- oder Suffix-Code (VAA-04, D-53-04).
- Bestehender bezahlten-Loop `effective_absence = absence_hours - holiday_hours + unavailable_hours >= 0.1` bleibt inhaltlich unveraendert (Regression-Lock VAA-03 #3 FE-Seite).
- Kein Docs-Trigger ausgeloest; F07-Nebensatz-Update ist optional und wird als post-phase Follow-up behandelt.
</verification>

<success_criteria>
1. `state::WeeklySummary::from(&WeeklySummaryTO)` liefert Union aus bezahlten Absencen (via `working_hours_per_sales_person`) + Freiwilligen-Absencen (via neuem DTO-Feld `sales_person_absences`), case-insensitive sortiert nach Name.
2. Der `>= 0.1`-Filter gilt fuer beide Quellen (Randfall Zusage=0 raus).
3. Rendering-Zeile in `page/weekly_overview.rs:126` woertlich unveraendert (VAA-04-Lock, grep-verifiziert).
4. FE-WASM-Build + FE-Tests + FE-Clippy gruen; Backend-Regression aus shifty-backend-Root gruen.
5. Bestehender bezahlten-Filter (`effective_absence = absence_hours - holiday_hours + unavailable_hours`) inhaltlich unveraendert (Regression-Lock VAA-03 #3 FE-Seite).
6. Manual-Only VAA-04-Verifikation (laut 53-VALIDATION.md) — Browser-Sichtprobe auf `/weekly_overview/` mit Test-Daten (Freiwilliger mit Vacation-Period + Bezahlter mit `absence_hours`) — bleibt fuer den User-UAT-Schritt reserviert, nicht Teil des automatisierten Gates.
</success_criteria>

<output>
Create `.planning/phases/53-freiwilligen-abwesenheiten-jahresansicht/53-03-SUMMARY.md` when done
</output>
