---
phase: 52-weekly-overview-performance-refactor
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - service_impl/src/test/mod.rs
  - service_impl/src/test/booking_information_weekly_summary_year_batch.rs
autonomous: true
requirements: [WOP-03, WOP-05]
must_haves:
  truths:
    - "D-52-11 — 8 Fixture-Achsen decken Baseline / Feiertag / ShortDay / Freiwilliger-Vacation / CVC-06-Cap / Gate-off / Gate-on / Kombi ab"
    - "D-52-12 — Byte-Identität via `f32::to_bits()` pro Feld auf `WeeklySummary` + inneren `WorkingHoursPerSalesPerson`; NaN wird explizit vor `to_bits()` ausgeschlossen"
    - "D-52-13 — Kein `proptest`-Dep, kein Seeded-Random; deterministische Fixtures"
    - "D-52-14 — Fallback-Ansatz: hart-kodierter Vec-Vergleich pro Fixture (kein neuer Cargo-Dep, kein `insta`)"
    - "D-52-16 — Latenz-Baseline vor Umbau via `curl -w %{time_total}`, 5 Runs, Median dokumentiert"
    - "WOP-03 — Golden-Snapshot gegen die AKTUELLE (unveränderte) `get_weekly_summary`-Impl aufgenommen und grün — dient als hartes Regressions-Gate für Waves 2-4"
    - "WOP-05 — Fixture-Test läuft in `cargo test --package service_impl` + gesamter Workspace grün"
  artifacts:
    - service_impl/src/test/booking_information_weekly_summary_year_batch.rs
    - .planning/phases/52-weekly-overview-performance-refactor/52-01-latency-baseline.txt
  key_links:
    - "Fixture-Test → Wave 2/3/4 Regressions-Gate (byte-identisch invariant)"
    - "Latenz-Baseline → Wave 4 Vergleich für WOP-04"
---

<objective>
Wave-0-Gate: Aufsetzen des Golden-Snapshot-Fixture-Tests und Aufnahme der Latenz-Baseline VOR jedem Semantik-Refactor. Der Test läuft gegen die AKTUELLE unveränderte `get_weekly_summary`-Impl und fixiert die erwartete `Arc<[WeeklySummary]>` als hart-kodierte Rust-Literale (kein `insta`, kein neuer Dep). Alle folgenden Waves (2-4) müssen diesen Test byte-identisch grün lassen.

Purpose: Ohne dieses Gate ist ein byte-identischer Refactor (WOP-03) unbeweisbar — der Snapshot ist die einzige harte Sicherung gegen Semantik-Drift beim Umbau der Wochen-Iteration auf Jahres-Aggregation.

Output: Fixture-Test-Datei mit 8 Szenarien, Modul-Registrierung, Latenz-Baseline-Textfile im Phase-Ordner.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-weekly-overview-performance-refactor/52-CONTEXT.md
@.planning/phases/52-weekly-overview-performance-refactor/52-RESEARCH.md
@service_impl/src/booking_information.rs
@service_impl/src/test/booking_information_chain_c.rs
@service_impl/src/test/mod.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Fixture-Testdatei mit 8 Golden-Snapshots gegen aktuelle Impl</name>
  <files>service_impl/src/test/booking_information_weekly_summary_year_batch.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/booking_information.rs:259-491 (`get_weekly_summary`-Body — Vollständig lesen, Semantik verstehen)
    - service_impl/src/test/booking_information_chain_c.rs (Fixture-Vorbild — Chain-C-ShortDay-Slot-Clipping-Tests, dieselbe Mock-Struktur, dieselben Trait-Mocks)
    - service_impl/src/test/booking_information.rs (Basis-Mock-Setup für `BookingInformationServiceImpl`)
    - service_impl/src/reporting.rs:884-1160 (`get_week`-Body — Semantik-Referenz für Feld-Erwartungen)
    - service_impl/src/test/mod.rs (Modul-Registrierungs-Muster)
    - rest-types/src/booking_information.rs (`WeeklySummary`, `WorkingHoursPerSalesPerson` — Feldnamen für `to_bits()`-Vergleich)
    - service_impl/src/shortday_gate.rs (Toggle-Read-Kontrakt für Fixture 6/7)
  </read_first>
  <behavior>
    Test-Fixtures decken die WOP-03-Achsen ab, jede als eigene `#[tokio::test]` mit sprechendem Namen:
    - `fixture_1_baseline` — Basis-Setup, 1 bezahlter MA, keine Special-Days, keine Absencen, Cap aus.
    - `fixture_2_holiday_week_n` — `SpecialDayType::Holiday` an einem Wochentag in einer bestimmten Woche; expected/holiday/available reduziert.
    - `fixture_3_shortday_week_n` — `SpecialDayType::ShortDay` mit `until` an einem Wochentag; Slot wird geclippt.
    - `fixture_4_volunteer_vacation_period` — Freiwilliger mit `AbsencePeriod` (`Vacation`), die Woche N (Mo-So) überlappt; VFA-01 whole-week-out greift.
    - `fixture_5_cvc06_cap_active` — `EmployeeWorkDetails` mit `cap_planned_hours_to_expected=true`, `expected_hours>0`, `raw_shiftplan_hours > expected_hours_for_cap`.
    - `fixture_6_gate_off_legacy` — `shortday_gate::read_active_from` → `None` (Toggle liefert `Unauthorized`/kein Wert); ShortDay-Slot per Legacy-Filter ganz verworfen.
    - `fixture_7_gate_on_active_from_before_week` — `shortday_gate::read_active_from` → `Some(vor Woche N)`; ShortDay-Slot geclippt (nicht verworfen).
    - `fixture_8_combined_holiday_shortday_volunteer_cap_gate` — Alle Achsen zusammen; deckt Off-by-one im Spillover-Übergang Woche 52→53→next_year (R6) ab.

    Jeder Test:
    1. Baut per `mockall`-Mocks eine `BookingInformationServiceImpl` (bestehendes Test-Vorbild aus `booking_information_chain_c.rs`).
    2. Ruft `service.get_weekly_summary(2026, Authentication::Full, None).await.unwrap()` gegen die AKTUELLE unveränderte Impl.
    3. Vergleicht das Ergebnis Feld für Feld gegen einen hart-kodierten `expected: Vec<WeeklySummary>`.
    4. Der Vergleich läuft über einen Helper `assert_weekly_summary_bit_exact(&actual, &expected)`, der pro `f32`-Feld erst `assert!(!f.is_nan())` und dann `assert_eq!(actual.to_bits(), expected.to_bits())` prüft. Für `Arc<[WorkingHoursPerSalesPerson]>` iteriert der Helper Element-für-Element mit demselben Bit-Vergleich.

    Kein `proptest`, kein `insta`, keine Serialisierung — nur Rust-Literals und `to_bits()`-Compare. Fallback aus D-52-14: hart-kodierter Vec-Vergleich (Snapshot-Ansatz mit `.snap`-Files hätte hand-gerollte Deserialisierung benötigt; hart-kodierte Rust-Literale sind lesbarer und einfacher zu debuggen).
  </behavior>
  <action>
    Erzeuge `service_impl/src/test/booking_information_weekly_summary_year_batch.rs` mit den 8 Fixtures oben. Register in `service_impl/src/test/mod.rs` als `mod booking_information_weekly_summary_year_batch;` (Reihenfolge alphabetisch neben `booking_information_chain_c` einfügen).

    Nutze die bestehenden Mock-Builder aus `booking_information_chain_c.rs` als Vorlage — dieselben Trait-Mocks (`MockReportingService`, `MockSpecialDayService`, `MockShiftplanReportService`, `MockSlotService`, `MockEmployeeWorkDetailsService`, `MockAbsenceService`, `MockSalesPersonService`, `MockToggleService`, `MockPermissionService`, `MockTransactionDao`). Kopiere die minimale Setup-Boilerplate nicht — extrahiere sie in einen privaten `fn build_service_with(...)` innerhalb der neuen Testdatei, damit die 8 Fixtures nur die Deltas (`.expect_*`-Rückgabewerte) setzen.

    Die erwarteten `WeeklySummary`-Werte pro Fixture werden EINMALIG erzeugt: Lauf den Test-Runner mit einer `println!("{:?}", weekly_report)`-Zeile (temporär), kopiere die Ausgabe als Rust-Literal in den Test, entferne das `println!`. Danach ist der Test hermetisch — jede Semantik-Änderung in Waves 2-4 führt zu einem Diff.

    Chain-C-Filter-Behavior für Fixture 6/7: Der `shortday_gate::read_active_from`-Call wird über `MockToggleService.expect_get()` gemockt (siehe D-51-06/07-Muster in Chain-C-Tests) — nicht direkt die Gate-Fn.

    Für Fixture 8 (Spillover): Wähle `year=2020` (weeks_in_year(2020)=53, damit Loop-Iteration `54..=56` in `year+1` fallthroughed) — oder `year=2025` (weeks_in_year(2025)=52, `53..=55` → 2026 W1-W3). **Wichtig:** Fixture MUSS mindestens eine Spillover-Woche produzieren, um R6-Off-by-one abzudecken.

    Permission-Mocks: `SHIFTPLANNER_PRIVILEGE` = Ok in Fixtures mit `working_hours_per_sales_person`-Erwartung; `SALES_PRIVILEGE` in Baseline.

    Test-Naming: `#[tokio::test]` mit `snake_case` Fixture-Namen. Nutze `#[serial]` NICHT — Mocks sind lokal.
  </action>
  <verify>
    <automated>cargo test --package service_impl --test booking_information_weekly_summary_year_batch fixture_ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 8 Fixtures im Test-Modul; alle 8 grün gegen aktuelle `get_weekly_summary`-Impl.
    - `cargo test --package service_impl booking_information_weekly_summary_year_batch` grün.
    - `cargo test --workspace` grün (keine Regression an bestehenden Tests durch die Modul-Registrierung).
    - Kein `#[ignore]`, kein `#[cfg(feature = ...)]` — alle 8 laufen im Standard-Testlauf.
    - Kein neuer Cargo-Dep in `Cargo.toml`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Latenz-Baseline aufnehmen (Pre-Refactor-Messung, Dev-DB)</name>
  <files>.planning/phases/52-weekly-overview-performance-refactor/52-01-latency-baseline.txt</files>
  <read_first>
    - .planning/phases/52-weekly-overview-performance-refactor/52-CONTEXT.md (D-52-16 Messmethode)
    - CLAUDE.md, shifty-backend/CLAUDE.md, shifty-backend/CLAUDE.local.md (nix develop + sqlx-Reset-Regeln — Memory: `sqlx database reset` DESTRUCTIVE)
  </read_first>
  <action>
    Miss die aktuelle End-to-End-Latenz von `GET /booking-information/weekly-resource-report/2026` BEVOR irgendein Refactor beginnt. Der Wert wird in `52-01-latency-baseline.txt` archiviert und in Wave 4 gegen die Post-Refactor-Messung verglichen.

    Schritte:
    1. Backend in Dev-Modus starten (bestehende Dev-DB `dev.sqlite` unverändert lassen — NICHT `sqlx database reset` (destruktiv, Memory `feedback_destructive_db_ops.md`)). Falls Backend nicht läuft: `cd shifty-backend && nix develop --command cargo run` in Hintergrund, warte bis `Server started on 3000`.
    2. Mock-Auth aktiv (Dev-Default über `mock_auth`-Feature). Falls nicht: dokumentiere im txt-File und überspringe Auth-Header.
    3. 5× messen via:
       ```
       for i in 1 2 3 4 5; do curl -s -o /dev/null -w "%{time_total}\n" http://localhost:3000/booking-information/weekly-resource-report/2026; done | sort -n
       ```
    4. Median (3. Wert nach `sort -n`) plus alle 5 Runs ins Textfile schreiben. Format:
       ```
       # Phase 52 Wave 0 — Latency Baseline (vor Refactor)
       # Endpoint: GET /booking-information/weekly-resource-report/2026
       # Auth: mock_auth (Dev-Default)
       # Runs (sorted):
       # 1: 0.XXX s
       # 2: 0.XXX s
       # 3: 0.XXX s   ← median
       # 4: 0.XXX s
       # 5: 0.XXX s
       Median: X.XXX s
       Backend commit: <git rev-parse HEAD>
       Measured: <date -Iseconds>
       Dev-DB path: shifty-backend/dev.sqlite
       ```
    5. Backend stoppen.

    **Wenn Backend startet aber Endpoint 401/403 zurückgibt:** Dev-Setup dokumentieren (welcher User im mock_auth-Kontext hat SHIFTPLANNER oder SALES); ggf. impersoniert per Header.
    **Wenn Dev-DB leer / <10 Bookings:** Baseline dokumentieren und Warnung ins txt schreiben, dass die Zielwert-Verifizierung in Wave 4 evtl. nicht aussagekräftig ist. Kein Reset erzwingen (destruktiv).
  </action>
  <verify>
    <automated>test -f .planning/phases/52-weekly-overview-performance-refactor/52-01-latency-baseline.txt && grep -c "^Median:" .planning/phases/52-weekly-overview-performance-refactor/52-01-latency-baseline.txt</automated>
  </verify>
  <done>
    - Datei `52-01-latency-baseline.txt` existiert, enthält 5 Runs + Median + Commit-Hash + Timestamp.
    - Baseline-Wert in Sekunden (Median) dokumentiert — dient in Wave 4 als Vergleichs-Referenz.
    - Backend gestoppt, keine hängenden Prozesse.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reine interne Test-/Mess-Task, keine neuen Trust Boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Tampering | Golden-Snapshot-Fixture-Werte | low | accept | Hart-kodierte Rust-Literale werden im PR-Review gesichtet; jede spätere Änderung erzwingt Erklärung |
| T-52-02 | Information Disclosure | Latenz-Baseline-Textfile | low | accept | Enthält nur Timing-Werte + Commit-Hash, keine PII |
</threat_model>

<verification>
- `cargo test --package service_impl booking_information_weekly_summary_year_batch` grün — alle 8 Fixtures.
- `cargo test --workspace` grün.
- `cargo clippy --workspace -- -D warnings` grün.
- `52-01-latency-baseline.txt` existiert mit 5 gemessenen Runs + Median.
</verification>

<success_criteria>
- 8 Fixtures im neuen Test-Modul erfassen alle WOP-03-Achsen (Baseline, Holiday, ShortDay, Volunteer-Vacation, CVC-06 Cap, Gate off, Gate on, Kombi mit Spillover).
- Golden-Snapshots hart-kodiert (kein neuer Dep, kein Fixture-Framework).
- Bit-exakte f32-Vergleiche via `to_bits()`.
- Latenz-Baseline aufgenommen und archiviert.
</success_criteria>

<output>
Create `.planning/phases/52-weekly-overview-performance-refactor/52-01-SUMMARY.md` when done. Include:
- Anzahl Fixtures und Feld-Coverage-Matrix.
- Latenz-Baseline-Median.
- Bestätigung: „Alle 8 Fixtures grün gegen aktuelle Impl — sind ab jetzt hartes Regressions-Gate für Waves 2-4."
- Ggf. Auffälligkeiten (z.B. NaN in einem Feld, leere Dev-DB, mock_auth-Setup-Notes).
</output>

## Artifacts this phase produces

- `service_impl/src/test/booking_information_weekly_summary_year_batch.rs` (neu)
- Modul-Registrierung in `service_impl/src/test/mod.rs`
- `.planning/phases/52-weekly-overview-performance-refactor/52-01-latency-baseline.txt`
