---
phase: 52-weekly-overview-performance-refactor
plan: 02
type: tdd
wave: 2
depends_on: ["52-01"]
files_modified:
  - service_impl/src/reporting.rs
autonomous: true
requirements: [WOP-02, WOP-05]
must_haves:
  truths:
    - "D-52-08 — `assemble_weeks`-Helper in `service_impl/src/reporting.rs`, Slice-Referenzen als Input; ABER: `async fn` mit `tx: Option<Self::Transaction>` und `context: Authentication<Self::Context>`, weil `derive_hours_for_range`, `build_derived_holiday_map`, `sales_person_service.get` async DAO-Calls sind (RESEARCH Q2 überschreibt CONTEXT.md-Vereinfachung `fn`)"
    - "D-52-09 — MUST-preserve: Balance-Formel (`dynamic_hours = shiftplan+extra_available-expected`), CVC-06 Cap-Gating pro `(sales_person_id, calendar_week)`, ExtraHours-Kategorien-Aufsplittung, `is_paid`-Filter (`reporting.rs:1140-1142`)"
    - "D-52-09 — MUST-NOT-move: Chain-C-Toggle-Read bleibt in `booking_information.get_weekly_summary`, DARF NICHT in `assemble_weeks` wandern (R8)"
    - "D-52-10 — `get_week` bleibt Public-Trait-Methode (unveränderte Signatur); Helper ist `pub(crate)` (nicht `pub`)"
    - "WOP-02 — `get_week` delegiert intern auf `assemble_weeks(&[(year, week)], ...)` mit 1-Element-Vec; byte-identisches Verhalten zum Vor-Refactor-Zustand"
    - "WOP-05 — Alle bestehenden Tests grün, insbesondere die 8 Wave-1-Fixtures und die vollständige Reporting-Test-Suite"
  artifacts:
    - "`service_impl::reporting::assemble_weeks` (pub(crate) async fn)"
  key_links:
    - "`assemble_weeks` → wird in Wave 3 sowohl von `get_week` als auch neuem `get_year` konsumiert (single source of truth)"
    - "1-Element-Vec-Delegation von `get_week` → garantiert dass alle bestehenden `get_week`-Konsumenten (`booking_information`, `rest/src/report.rs:148`) byte-identisch bleiben"
---

<objective>
Reiner Refactor: Der bestehende `get_week`-Body (Zeilen ~892-1156 in `service_impl/src/reporting.rs`) wird in einen internen `pub(crate) async fn assemble_weeks(weeks, work_details, shiftplan_reports, extra_hours, context, tx)`-Helper extrahiert. `get_week` behält seine Public-Trait-Signatur und wird auf einen 1-Element-Vec-Aufruf des Helpers reduziert. Kein Verhaltens-Change; die 8 Wave-1-Fixtures und alle bestehenden Reporting-Tests bleiben grün.

Purpose: Der Helper ist die single source of truth für die Wochen-Aggregations-Semantik. Wave 3 fügt danach eine `get_year`-Trait-Methode hinzu, die denselben Helper mit einem 55-Element-Vec ruft — dadurch ist Byte-Identität zwischen Wochen-Iteration und Jahres-Batch STRUKTURELL garantiert (nicht semantisch nachgewiesen werden muss).

Output: `assemble_weeks` als neuer interner Helper; `get_week` als dünner Wrapper.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-weekly-overview-performance-refactor/52-CONTEXT.md
@.planning/phases/52-weekly-overview-performance-refactor/52-RESEARCH.md
@.planning/phases/52-weekly-overview-performance-refactor/52-01-SUMMARY.md
@service_impl/src/reporting.rs
@service/src/reporting.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extrahiere `assemble_weeks`-Helper aus `get_week`, delegiere von `get_week`</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:84-93 (`find_working_hours_for_calendar_week` — pure Funktion, bleibt unverändert)
    - service_impl/src/reporting.rs:124-136 (`apply_weekly_cap` — pure Funktion, bleibt unverändert)
    - service_impl/src/reporting.rs:884-1160 (Vollständiger `get_week`-Body — die Zeilen 892-1156 wandern in `assemble_weeks`)
    - service_impl/src/reporting.rs:1030-1031 (Cap-Aktivierung per-Woche — MUSS im Helper pro `week`-Iteration frisch berechnet werden, nie über Jahr aggregiert)
    - service_impl/src/reporting.rs:1084-1093 (`build_derived_holiday_map`-Call — async, pro Person pro Woche)
    - service_impl/src/reporting.rs:1113-1116 (`apply_weekly_cap`-Anwendung)
    - service_impl/src/reporting.rs:1136-1142 (`sales_person_service.get` + `is_paid`-Filter — MUSS erhalten bleiben, siehe R9)
    - service_impl/src/reporting.rs:1233-1289 (`weight_for_week` — pure Funktion, bleibt unverändert)
    - service/src/reporting.rs:397-403 (Trait-Signatur `get_week` — DARF SICH NICHT ÄNDERN, R4)
    - rest/src/report.rs:140-160 (externer `get_week`-Konsument — bleibt kompatibel, wenn Signatur nicht geändert)
  </read_first>
  <behavior>
    Nach diesem Task:
    - `pub(crate) async fn assemble_weeks(&self, weeks: &[(u32, u8)], work_details: &[EmployeeWorkDetails], shiftplan_reports: &[ShiftplanReportDay], extra_hours: &[ExtraHours], context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<Vec<(u8, Arc<[ShortEmployeeReport]>)>, ServiceError>` existiert in `impl ReportingServiceImpl` (nicht im Trait — es ist `pub(crate)`, kein Trait-Method).
    - Der Helper iteriert über `weeks: &[(u32, u8)]`, aggregiert pro `(year, week)` und pro `sales_person_id` denselben Ablauf wie der alte `get_week`-Body: `find_working_hours_for_calendar_week` → `apply_weekly_cap` mit `raw_shiftplan_hours` aus `shiftplan_reports`-Filter → ExtraHours-Split nach `Availability`/Kategorie → `derive_hours_for_range` (async, pro Person) → `build_derived_holiday_map` (async, pro Person) → `sales_person_service.get(sales_person_id, ...)` mit `is_paid`-Filter (async, pro Person) → `ShortEmployeeReport::push`.
    - Return-Value ist ein `Vec<(u8, Arc<[ShortEmployeeReport]>)>` geordnet nach der Eingabe-Reihenfolge in `weeks`. Für Wave-2 wird das noch nicht ausgenutzt — `get_week` extrahiert immer nur das erste Element.
    - `get_week` wird auf folgende Delegation reduziert:
      ```
      async fn get_week(&self, year, week, context, tx) -> Result<Arc<[ShortEmployeeReport]>, ServiceError> {
          let tx = self.transaction_dao.use_transaction(tx).await?;
          let work_details = self.employee_work_details_service.all_for_week(week, year, context.clone(), tx.clone()).await?;
          let shiftplan_report = self.shiftplan_report_service.extract_shiftplan_report_for_week(year, week, Authentication::Full, tx.clone()).await?;
          let extra_hours = self.extra_hours_service.find_by_week(year, week, Authentication::Full, tx.clone()).await?;
          let mut assembled = self.assemble_weeks(&[(year, week)], &work_details, &shiftplan_report, &extra_hours, context, tx.clone()).await?;
          self.transaction_dao.commit(tx).await?;
          Ok(assembled.pop().map(|(_, reports)| reports).unwrap_or_else(|| Arc::from(Vec::<ShortEmployeeReport>::new())))
      }
      ```
    - Die Trait-Signatur von `get_week` in `service/src/reporting.rs:397-403` bleibt UNVERÄNDERT.
  </behavior>
  <action>
    Extrahiere die Zeilen aus dem heutigen `get_week`-Body ab dem `let mut result = Vec::new();`-Punkt (aktuell ~Zeile 915) bis zum letzten `result.push(ShortEmployeeReport {...});`-Block (aktuell ~Zeile 1156) in eine neue `pub(crate) async fn assemble_weeks` in derselben `impl`-Block. Passe an:

    1. **Signatur:** `pub(crate) async fn assemble_weeks(&self, weeks: &[(u32, u8)], work_details: &[EmployeeWorkDetails], shiftplan_reports: &[ShiftplanReportDay], extra_hours: &[ExtraHours], context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<Vec<(u8, Arc<[ShortEmployeeReport]>)>, ServiceError>`

    2. **Iterations-Struktur:** Der bestehende Body iteriert pro Person pro Woche mit lokalem `working_hours`, `shiftplan_report`, `extra_hours` als HashMaps. Der Helper hüllt das in eine äußere Schleife über `weeks: &[(u32, u8)]`:
       ```
       let mut assembled: Vec<(u8, Arc<[ShortEmployeeReport]>)> = Vec::with_capacity(weeks.len());
       for &(year, week) in weeks {
           // pro Woche: HashMap-Buckets aus den drei Slice-Referenzen aufbauen (RESEARCH Q2 Struktur C).
           // Bestehende collect_to_hash_map_by-Aufrufe bleiben, aber operieren jetzt auf einem
           // In-Memory-Filter der Slice-Referenzen.
           let working_hours = work_details.iter().cloned().collect_to_hash_map_by(|wh| wh.sales_person_id);
           let shiftplan_report_bucket = shiftplan_reports
               .iter()
               .filter(|r| r.year == year && r.calendar_week == week)
               .collect_to_hash_map_by(|r| r.sales_person_id);
           let extra_hours_bucket = extra_hours
               .iter()
               .filter(|eh| { let sw = eh.to_date().as_shifty_week(); sw.year == year && sw.week == week })
               .collect_to_hash_map_by(|eh| eh.sales_person_id);
           let mut week_result: Vec<ShortEmployeeReport> = Vec::new();
           for (sales_person_id, working_hours) in &working_hours {
               // ... bestehender Per-Person-Aggregations-Body 1:1 kopieren ...
           }
           assembled.push((week, week_result.into()));
       }
       Ok(assembled)
       ```

    3. **Semantik-Invarianten strikt erhalten** (D-52-09):
       - Der `find_working_hours_for_calendar_week`-Aufruf innerhalb der Per-Person-Aggregation bleibt bestehen (er filtert auf `(year, week)`).
       - `apply_weekly_cap` wird pro Person pro Woche EINMALIG mit `raw_shiftplan_hours` (per-Person-Summe aus Bucket) und `expected_hours_for_cap` (aus `weight_for_week` + `abense_hours_for_balance` + `absence_derived_balance_total`) gerufen.
       - `derive_hours_for_range`, `build_derived_holiday_map`, `sales_person_service.get` bleiben async-Calls pro Person pro Woche (RESEARCH Q2 explizit dokumentiert — nicht zu Load-once in diesem Task; das ist Follow-Up, siehe R3).
       - Der `is_paid`-Filter (`if !sales_person.is_paid.unwrap_or(false) { continue; }`, `reporting.rs:1140-1142`) bleibt an genau derselben Stelle in der Per-Person-Schleife (R9).
       - Kein Toggle-Read (`shortday_gate::read_active_from`) im Helper (R8, D-52-09).

    4. **`get_week` umschreiben**:
       - Neue Impl gemäß `behavior`-Block oben.
       - Die drei Bulk-Loads (`all_for_week`, `extract_shiftplan_report_for_week`, `find_by_week`) BLEIBEN im `get_week`-Body (sind für den Week-Fall unverändert billig).
       - `assemble_weeks(&[(year, week)], ...)` liefert ein 1-Element-Vec; `.pop()` extrahiert das `(week, reports)`-Tupel und wir geben nur `reports` zurück.
       - `self.transaction_dao.commit(tx).await?` bleibt am Ende.

    5. **`info!("Extra hours: {:?}", &extra_hours);` (Zeile 910)**: Log-Statement bleibt im `get_week`-Wrapper (nicht im Helper — Helper wird auch von `get_year` aufgerufen, dann würde 55× geloggt). Wenn das Log entfernt werden soll: separater Task, nicht hier.

    6. **Kein Verhaltens-Change**: `cargo test --workspace` muss grün bleiben, insbesondere die 8 Wave-1-Fixtures aus Plan 01.
  </action>
  <verify>
    <automated>cargo test --package service_impl booking_information_weekly_summary_year_batch fixture_ && cargo test --package service_impl reporting</automated>
  </verify>
  <done>
    - `pub(crate) async fn assemble_weeks(...)` existiert in `service_impl/src/reporting.rs`.
    - `get_week` delegiert auf `assemble_weeks(&[(year, week)], ...)`, unveränderte Trait-Signatur.
    - `cargo test --package service_impl` grün — alle Reporting-Tests + alle 8 Wave-1-Fixtures.
    - `cargo test --workspace` grün.
    - `cargo clippy --workspace -- -D warnings` grün.
    - Grep-Guard: `grep -c 'shortday_gate' service_impl/src/reporting.rs` liefert 0 (Toggle-Read wanderte NICHT ins Helper, R8/D-52-09).
    - Trait-Signatur `get_week` in `service/src/reporting.rs:397-403` unverändert (`git diff service/src/reporting.rs` zeigt keine Änderung an dieser Datei).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Interner Refactor — keine neuen Boundaries. Auth-Kontext (`context: Authentication<Self::Context>`) wird 1:1 durchgereicht.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-03 | Tampering | `is_paid`-Filter in `assemble_weeks` | medium | mitigate | R9: `continue`-Statement bei Zeile 1140-1142 wandert 1:1 mit, Fixture-Test 4 (Volunteer-Vacation) verifiziert dass Freiwillige nicht in `WorkingHoursPerSalesPerson` leaken |
| T-52-04 | Elevation of Privilege | Toggle-Read wandert versehentlich in `assemble_weeks` | high | mitigate | D-52-09/R8: Grep-Guard im `<done>` gate; Chain-C-Tests würden bei fälschlicher Migration failen |
</threat_model>

<verification>
- `cargo test --package service_impl` grün (Reporting-Tests + Wave-1-Fixtures).
- `cargo test --workspace` grün (Chain-C-Tests, `booking_information`-Tests, `rest`-Layer-Tests).
- `cargo clippy --workspace -- -D warnings` grün.
- `git diff service/src/reporting.rs` zeigt keine Änderung (Trait-Signatur intakt).
- `grep -v '^#\|//' service_impl/src/reporting.rs | grep -c 'shortday_gate'` liefert 0.
</verification>

<success_criteria>
- Reiner Refactor: Wave-1-Fixtures bleiben bit-exakt grün → Byte-Identität verifiziert.
- `assemble_weeks` ist `pub(crate)`, wird von `get_week` intern verwendet.
- Keine Trait-Signatur-Änderung an `ReportingService::get_week`.
- Kein Toggle-Read im Helper.
</success_criteria>

<output>
Create `.planning/phases/52-weekly-overview-performance-refactor/52-02-SUMMARY.md` when done. Include:
- Diff-Größe (Zeilen extrahiert, Zeilen im `get_week`-Wrapper).
- Bestätigung: „Wave-1-Fixtures bit-exakt grün — Semantik-Erhalt strukturell nachgewiesen."
- Grep-Ergebnis: `shortday_gate` in `reporting.rs` = 0.
- Trait-Signatur `get_week` unverändert (`git diff service/src/reporting.rs` = leer).
</output>

## Artifacts this phase produces

- `service_impl::reporting::assemble_weeks` (neu, `pub(crate) async fn`)
- `service_impl::reporting::ReportingServiceImpl::get_week` (umgeschrieben — Trait-Signatur unverändert, Body = Wrapper mit 3 Bulk-Loads + 1-Element-Vec-Delegation)
