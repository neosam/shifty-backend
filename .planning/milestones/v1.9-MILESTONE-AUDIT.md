---
milestone: v1.9
audited: 2026-06-29
status: passed
scores:
  requirements: 7/7
  phases: 4/4
  integration: 4/4
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 30-stale-daten-race-guard-fe
    items:
      - "Optional, non-blocking manual browser smoke (deferred UAT, user-accepted): rapid NextWeek/PreviousWeek clicking must never flash another week's summary data. Not pixel-automatable; structural correctness fully verified (pure predicate tests + source-verified synchronous-before-dispatch ordering across all dispatch paths + all 4 summary loaders guarded incl. the code-review-found working_hours_mini)."
  - phase: 32-admin-impersonation-frontend-audit-schicht-fe-be
    items:
      - "Optional, non-blocking manual browser smoke (deferred UAT): live impersonation roundtrip (admin starts from Users tab → amber banner on every page → survives hard reload → a write is audited with the real admin identity → Stop re-inits stores). Structural correctness fully verified (17/17 must-haves, 3 backend integration tests SC3/SC5/P10, banner non-closable asserted by SSR test)."
---

# Milestone v1.9 — Audit Report

**Milestone:** v1.9 Schichtplan-/Urlaubs-UX-Korrekturen & Admin-Impersonation
**Audited:** 2026-06-29 (autonomous run)
**Status:** ✅ passed
**Phases:** 29–32 (4/4 complete, all VERIFICATION=passed)
**Requirements:** 7/7 satisfied

## Definition of Done

> Drei betrieblich aufgefallene UX-/Korrektheits-Lücken im Schichtplan-/Urlaubs-Bereich
> schließen und Admins eine vollwertige (lesend **und** schreibend) Impersonation anderer
> Benutzer geben — mit korrektem Audit der echten Admin-Identität und ohne Privilege-Eskalation.

All three UX/correctness gaps closed (VAC-01, SHP-01, SHP-02); full read+write admin
impersonation delivered with real-admin write auditing and no privilege escalation (IMP-01..04).

## Requirements Coverage (3-Source Cross-Reference)

| REQ | Phase | VERIFICATION | SUMMARY (req-completed) | Traceability | Final |
|-----|-------|--------------|--------------------------|--------------|-------|
| VAC-01 | 29 | passed (3/3) | listed | ✅ done | **satisfied** |
| SHP-02 | 30 | passed (5/5 structural + 1 deferred UAT) | listed | ✅ done | **satisfied** |
| SHP-01 | 31 | passed (7/7) | listed | ✅ done | **satisfied** |
| IMP-01 | 32 | passed (17/17) | listed | ✅ done | **satisfied** |
| IMP-02 | 32 | passed | listed | ✅ done | **satisfied** |
| IMP-03 | 32 | passed | listed | ✅ done | **satisfied** |
| IMP-04 | 32 | passed | listed | ✅ done | **satisfied** |

No unsatisfied, partial, or orphaned requirements.

## Phase Verifications

| Phase | Plans | Verification | Notes |
|-------|-------|--------------|-------|
| 29 Urlaubs-Balken-Konsistenz | 1/1 | passed | pure `compute_vacation_bar` + 6 tests; review 2 INFO fixed |
| 30 Stale-Daten-Race Guard | 1/1 | passed (structural) | `week_guard` shared across 4 loaders; review WR-01 (4th loader `working_hours_mini`) fixed; 1 optional smoke deferred |
| 31 Abwesenheit→Nicht-Verfügbar | 1/1 | passed | `absence_marker` + 8 tests; category-set == BookingOnAbsenceDay (zero drift); reuses Phase-30 guard; review 3 INFO (2 fixed) |
| 32 Admin-Impersonation + Audit | 3/3 | passed | RealUser + central audit middleware + full FE; 3 backend integration tests; review 3 WR fixed (audit-quality); 1 optional smoke deferred |

## Cross-Phase Integration (gsd-integration-checker)

**4/4 checks PASS — 0 orphaned, 0 missing, 0 broken flows.**

1. **Phase 30 ↔ 31 wiring — WIRED.** Phase 31's `reload_absence_days` reuses Phase 30's
   `is_current_selection`/`SELECTED_WEEK` (shared global, no duplication). `set_selected_week`
   synchronous before all dispatches (mount/NextWeek/PreviousWeek); `UpdateSalesPerson`
   correctly omits it (week unchanged). Phase-30's 4 loaders unaffected by the 5th (absence) loader.
2. **discourage_weekdays union-merge — WIRED.** unavailable_days ∪ absence-derived weekdays
   feed the same `WeekView` prop; idempotent, empty default before load.
3. **Phase 29/30/31 coexistence — WIRED.** absences.rs (29) disjoint from shiftplan.rs (30+31);
   guard infra + 5th loader + render-guard coexist with no structural conflict.
4. **Phase 32 E2E — WIRED.** Users-tab Start → POST → effective Context=target + RealUser=admin
   → banner on every page → survives reload (LoadStatus on mount) → mutating write audited by the
   central middleware (layered after context_extractor) → Stop → full reload re-inits stores.
   FE↔BE paths/methods/`ImpersonateTO` shape all line up.

## E2E Flows (4/4 complete)

- Vacation bar matches the remaining-days number (incl. overdraw color signal). ✅
- Rapid week-switching never shows a non-selected week's summary cards. ✅
- Own/selected-person absence days render as discourage in the shiftplan grid. ✅
- Admin impersonation roundtrip (start→banner→reload-persist→audited write→stop→teardown). ✅

## Constraints Honored

- **No new dependencies** (only a `tower-cookies` dev-dependency added to shifty_bin for tests).
- **No `Authentication<Context>` signature change** (impersonation handled at REST layer).
- **No snapshot schema bump** (no `BillingPeriodValueType` computation touched) — matches the v1.9 expectation of no bump (baseline stays 12).
- **No DB migration** (impersonation reuses the existing `session.impersonate_user_id` column).
- **Clippy hard-gate green** (`cargo clippy --workspace -- -D warnings` EXIT 0, independently re-verified) + `cargo test --workspace` green; FE `cargo test` 705/0 + `cargo build --target wasm32-unknown-unknown` OK.

## Tech Debt / Deferred (non-blocking)

- 2 optional manual browser smokes (P30 rapid-week, P32 impersonation roundtrip) — not
  pixel/timing-automatable; structural correctness fully verified; user-accepted as deferred UAT.
- Pre-existing carry-over deferred items from v1.4–v1.8 (see STATE.md "Deferred Items") — not
  v1.9-specific.

## Verdict

All 7 requirements satisfied, all 4 phases verified, cross-phase integration and E2E flows
clean, all quality gates green, all milestone constraints honored. **Milestone v1.9 is
audit-passed.** The only open items are 2 optional non-automatable browser smokes accepted as
deferred UAT.

*Note: all v1.9 code is uncommitted in the working tree (jj manual-commit policy) — the user
commits with jj.*
