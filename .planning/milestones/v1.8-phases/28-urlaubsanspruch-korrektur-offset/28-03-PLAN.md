---
phase: 28-urlaubsanspruch-korrektur-offset
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - service/src/employee_work_details.rs
  - service_impl/src/billing_period_report.rs
  - service_impl/src/test/billing_period_snapshot_locking.rs
autonomous: true
requirements: [VAC-OFFSET-01]
user_setup: []

must_haves:
  truths:
    - "D-28-04: `EmployeeWorkDetails::vacation_days_for_year` year-START branch subtracts `(ordinal()-1)/days_in_year` instead of `ordinal()/days_in_year` (a 1.1. start, ordinal()==1, now subtracts 0); the year-END branch (`1.0 - ordinal/days_in_year`) is ALREADY correct and is left UNCHANGED."
    - "D-28-04: For a full-year contract (1.1.–31.12.) `vacation_days_for_year(year) == vacation_days` (no proration); a mid-year start subtracts the correct fraction of days strictly before the start; a 31.12. end subtracts 0."
    - "D-28-05: Because `vacation_days_for_year` feeds the persisted `BillingPeriodValueType::VacationEntitlement` (reporting.rs:853 ← :803 — NOT VacationDays), the off-by-one fix changes a persisted value_type, so `CURRENT_SNAPSHOT_SCHEMA_VERSION` is bumped 11→12 per the CLAUDE.md rule."
    - "D-28-05: A v12 entry is added to the version-history doc-comment naming VacationEntitlement (and noting VacationDays is unaffected), and the snapshot-version guard test pins `CURRENT_SNAPSHOT_SCHEMA_VERSION == 12` with the Phase 28 rationale."
  artifacts:
    - "service/src/employee_work_details.rs (fixed year-start proration + #[cfg(test)] regression module)"
    - "service_impl/src/billing_period_report.rs (CURRENT_SNAPSHOT_SCHEMA_VERSION = 12 + v12 history doc-comment)"
    - "service_impl/src/test/billing_period_snapshot_locking.rs (assert == 12 + rationale)"
  key_links:
    - "The offset mechanism (Plans 28-01/28-02) is snapshot-SAFE — this bump is driven SOLELY by the off-by-one fix to vacation_days_for_year."
    - "Only the constant + the guard-test literal change; CURRENT_SNAPSHOT_SCHEMA_VERSION is the single source of truth (no other hardcoded 12)."
---

<objective>
Fix the year-start off-by-one in vacation-entitlement proration and carry the mandatory billing-snapshot schema-version bump it triggers.

Purpose: D-28-04 corrects a ~1/365 over-subtraction that occasionally tipped `.round()` from 18 to 17 for full-year contracts. Because `vacation_days_for_year` also feeds the persisted `BillingPeriodValueType::VacationEntitlement` (RESEARCH §2 correction — NOT `VacationDays`), CLAUDE.md mandates a `CURRENT_SNAPSHOT_SCHEMA_VERSION` bump 11→12 (D-28-05) so snapshot validators can distinguish schema drift from real data bugs.

Output: A one-line proration fix + regression tests, the constant bumped 11→12 with a documented v12 history entry, and the snapshot-version guard test updated. This plan is independent of the offset table and can run in Wave 1.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-urlaubsanspruch-korrektur-offset/28-CONTEXT.md
@.planning/phases/28-urlaubsanspruch-korrektur-offset/28-RESEARCH.md
@CLAUDE.md
</context>

<vcs_jj_only>
VCS = jj (Jujutsu), co-located with git. Single working tree (use_worktrees=false).
- Commit EACH task as exactly ONE jj change: run the task's full <verify> gates FIRST, THEN `jj commit -m "<type>(28): <subject>"`.
- NEVER run `git commit`, `git add`, `git stash`, `git merge`, `git reset`, or `gsd-tools query commit`.
- NEVER create or use worktrees.
- Commit subjects use conventional types feat/fix/test/docs/chore, scope always `(28)`.
</vcs_jj_only>

<artifacts_this_phase_produces>
NEW / CHANGED symbols introduced by THIS plan:
- `service::employee_work_details::EmployeeWorkDetails::vacation_days_for_year` — corrected year-start proration.
- A `#[cfg(test)]` regression module pinning full-year / mid-year-start / year-end behavior.
- `service_impl::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION` = **12** (was 11) + v12 history doc-comment.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Off-by-one proration fix + regression tests</name>
  <files>service/src/employee_work_details.rs</files>
  <read_first>
    - service/src/employee_work_details.rs:158-191 (vacation_days_for_year — BUG at :173 year-start; year-end at :180-189 already correct)
    - 28-RESEARCH.md § "Off-by-one fix (D-28-04)" + "Pitfall 4" (fix START only, leave END)
    - service/src/test or existing #[cfg(test)] modules in service/src/employee_work_details.rs for the test-harness pattern (how EmployeeWorkDetails fixtures with from_date/to_date are built)
  </read_first>
  <behavior>
    - Full-year contract (from 1.1., to 31.12. of `year`): vacation_days_for_year(year) == vacation_days (no subtraction).
    - Mid-year start (e.g. from 1.7.): subtracts vacation_days * (ordinal_of_start - 1)/days_in_year (days strictly before the start).
    - Year-end on 31.12.: end branch subtracts 0 (already-correct behavior pinned to prevent a future "symmetry" regression).
    - Year fully outside [from_year, to_year]: returns 0.0 (unchanged).
  </behavior>
  <action>
    In `service/src/employee_work_details.rs`, fix ONLY the year-START branch (around :173): change the proration ratio from `from_date.to_date().ordinal() / days_in_year` to `(from_date.to_date().ordinal() - 1) / days_in_year` (the days STRICTLY before the start; a 1.1. start has 0 prior days → 0 subtraction). Keep the `f32` math and the surrounding `if from_year == year { if let Ok(from_date) = ... }` structure. Do NOT touch the year-END branch (:180-189) — RESEARCH/Pitfall 4 confirm it is already symmetric and correct; changing it would introduce a new bug.

    Add (or extend) a `#[cfg(test)] mod` in this file with regression tests covering the four behaviors above. Build EmployeeWorkDetails fixtures with explicit from/to dates so the proration is deterministic. Name a test so `cargo test -p service vacation_days_for_year` selects it.
  </action>
  <verify>
    <automated>cargo test -p service vacation_days_for_year 2>&1 | tail -15</automated>
    <automated>cargo clippy --workspace -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Full-year contract returns exactly `vacation_days` (no proration) — the previously-buggy ~0.05/day over-subtraction is gone (D-28-04).
    - Mid-year start subtracts the correct prior-days fraction; 31.12. end subtracts 0; out-of-range returns 0.0.
    - Only the year-start branch changed; clippy clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot schema-version bump 11→12 + guard test</name>
  <files>service_impl/src/billing_period_report.rs, service_impl/src/test/billing_period_snapshot_locking.rs</files>
  <read_first>
    - service_impl/src/billing_period_report.rs:60-108 (version-history doc-comment ladder v5..v11 + the `CURRENT_SNAPSHOT_SCHEMA_VERSION` constant), :256-272 (VacationDays vs VacationEntitlement value_type mappings)
    - service_impl/src/test/billing_period_snapshot_locking.rs:1-69 (the pinned-version test at :25-38 + the surface-locked test at :45-69 — the latter needs NO change since VacationEntitlement is already an arm)
    - CLAUDE.md § "Billing Period Snapshot Schema Versioning" (bump rule)
    - 28-RESEARCH.md § "Snapshot bump (D-28-05)" + "Pitfall 1" (it is VacationEntitlement, NOT VacationDays)
  </read_first>
  <behavior>
    - test_snapshot_schema_version_pinned asserts CURRENT_SNAPSHOT_SCHEMA_VERSION == 12 and fails the build if the constant is not bumped.
    - test_billing_period_value_type_surface_locked still compiles unchanged (no new value_type added).
  </behavior>
  <action>
    In `service_impl/src/billing_period_report.rs`: change `CURRENT_SNAPSHOT_SCHEMA_VERSION` from `11` to `12`. Add a `- v12:` entry to the version-history doc-comment ladder above the constant explaining the Phase 28 off-by-one fix: state that `EmployeeWorkDetails::vacation_days_for_year` no longer over-subtracts ~1/365 at a 1.1. start, that this changes the persisted `BillingPeriodValueType::VacationEntitlement` (reporting.rs:853 ← :803), that validators MUST treat v11 snapshots as older schema and skip vacation-entitlement re-validation, and explicitly NOTE that `VacationDays` (taken vacation) is UNAFFECTED — only `VacationEntitlement` (contract aliquot) changes (Pitfall 1).

    In `service_impl/src/test/billing_period_snapshot_locking.rs`: update `test_snapshot_schema_version_pinned` to assert `== 12` and rewrite the assertion message rationale to the Phase 28 VAC-OFFSET-01 off-by-one fix (cite VacationEntitlement). Update the module head doc-comment bullet accordingly. Leave `test_billing_period_value_type_surface_locked` unchanged (no enum variant added).
  </action>
  <verify>
    <automated>cargo test -p service_impl test_snapshot_schema_version_pinned 2>&1 | tail -10</automated>
    <automated>cargo test --workspace 2>&1 | tail -15</automated>
    <automated>cargo clippy --workspace -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `CURRENT_SNAPSHOT_SCHEMA_VERSION == 12`; guard test pins 12 with a Phase 28 rationale citing VacationEntitlement.
    - v12 history doc-comment present and notes VacationDays is unaffected.
    - Full workspace test suite green (no other snapshot/reporting test regressed by the proration change); clippy clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| computation → persisted snapshot | A changed value_type computation must be versioned so validators can distinguish drift from data bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-07 | Repudiation | billing snapshot validation | medium | mitigate | Bump CURRENT_SNAPSHOT_SCHEMA_VERSION 11→12 + guard test so v11 snapshots are correctly treated as older schema (D-28-05) |
| T-28-08 | Tampering | proration math | low | mitigate | Regression tests pin full-year/mid-year/year-end behavior, preventing a future "symmetry" regression on the year-end branch |
| T-28-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies introduced |
</threat_model>

<verification>
- `cargo test -p service vacation_days_for_year` + `cargo test -p service_impl test_snapshot_schema_version_pinned` green.
- `cargo test --workspace` green (no reporting/snapshot regression).
- `cargo clippy --workspace -- -D warnings` clean.
</verification>

<success_criteria>
- Year-start off-by-one fixed; full-year contract no longer prorated (D-28-04).
- Snapshot version bumped 11→12, documented, and guarded (D-28-05).
- Year-end branch untouched; VacationDays untouched.
</success_criteria>

<output>
Create `.planning/phases/28-urlaubsanspruch-korrektur-offset/28-03-SUMMARY.md` when done.
</output>
