---
phase: 28-urlaubsanspruch-korrektur-offset
plan: 04
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - shifty-dioxus/src/state/vacation_balance.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/service/vacation_balance.rs
  - shifty-dioxus/src/page/absences.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [VAC-OFFSET-01]
user_setup: []

must_haves:
  truths:
    - "D-28-07: In the HR person-detail view (`VacationEntitlementSelfBody` on the `forced_self` path), the 'Vertragsanspruch' StatBox shows an always-visible signed inline number field with a 'berechnet {computed} + Offset [x]' line; the box/hero shows the EFFECTIVE value; editing saves HR-gated on blur/Enter via POST /vacation-entitlement-offset (ROADMAP SC4)."
    - "D-28-03: In the user self-view the same StatBox shows ONLY the effective value — no 'berechnet/Offset' line and no input field (computed_entitled_days/offset_days arrive as None for self-only callers, so there is nothing to render) (ROADMAP SC5)."
    - "D-28-08: New labels ('berechnet', 'Offset', and any helper text) are added to de (Locale::De — not Locale::En), en, and cs, and added to the i18n key-coverage test array so all three locales are guarded (ROADMAP SC6)."
    - "D-28-09: The offset edit always targets the year currently selected in the overview (the year already threaded into VacationEntitlementSelfBody) and the sales_person_id from the displayed balance."
  artifacts:
    - "shifty-dioxus/src/state/vacation_balance.rs (VacationBalance += offset_days, computed_entitled_days + From mapping + test)"
    - "shifty-dioxus/src/api.rs (save_vacation_entitlement_offset POST call)"
    - "shifty-dioxus/src/service/vacation_balance.rs (SaveOffset action → post + reload)"
    - "shifty-dioxus/src/page/absences.rs (is_hr passthrough + inline offset editor at VacationStatContract)"
    - "shifty-dioxus/src/i18n/{mod,en,de,cs}.rs (new offset labels + coverage-test entries)"
  key_links:
    - "Depends on Plan 28-02: VacationBalanceTO must already carry offset_days: Option<i32> + computed_entitled_days: Option<f32>."
    - "is_hr reaches VacationEntitlementSelfBody from VacationEntitlementCard.props.is_hr; the editor renders only when is_hr AND computed_entitled_days is Some."
    - "After a successful save, the service reloads the balance (LoadTeam/LoadSelf) so the effective value recomputes from the backend."
---

<objective>
Render the HR inline offset editor in the person-detail StatBox and keep the user self-view showing only the effective number — backed by the new TO fields and the HR-gated save endpoint from Plan 28-02.

Purpose: Deliver the frontend half of VAC-OFFSET-01 (D-28-07 inline signed editor with "berechnet {n} + Offset [x]", D-28-03 user-side hiding, D-28-08 i18n de/en/cs, D-28-09 year-scoped edit). The backend already hides the breakdown for self-only callers, so the FE simply renders what it receives.

Output: FE state/api/service plumbing for the offset plus the inline editor UI and translations, verified by the WASM build gate and cargo tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-urlaubsanspruch-korrektur-offset/28-CONTEXT.md
@.planning/phases/28-urlaubsanspruch-korrektur-offset/28-RESEARCH.md
@.planning/phases/28-urlaubsanspruch-korrektur-offset/28-02-SUMMARY.md
@shifty-dioxus/CLAUDE.md
</context>

<vcs_jj_only>
VCS = jj (Jujutsu), co-located with git. Single working tree (use_worktrees=false).
- Commit EACH task as exactly ONE jj change: run the task's full <verify> gates FIRST, THEN `jj commit -m "<type>(28): <subject>"`.
- NEVER run `git commit`, `git add`, `git stash`, `git merge`, `git reset`, or `gsd-tools query commit`.
- NEVER create or use worktrees.
- Commit subjects use conventional types feat/fix/test/docs/chore, scope always `(28)`.
- Frontend gates run from the BACKEND nix devShell (lld / wasm linker); frontend clippy is NOT CI-gated but should still be attempted from the backend shell.
</vcs_jj_only>

<artifacts_this_phase_produces>
NEW symbols introduced by THIS plan:
- `state::vacation_balance::VacationBalance.offset_days: Option<i32>`, `.computed_entitled_days: Option<f32>`.
- `api::save_vacation_entitlement_offset(config, sales_person_id, year, offset_days)`.
- `service::vacation_balance::VacationBalanceAction::SaveOffset(sales_person_id, year, offset_days)`.
- i18n Keys for the "berechnet" + "Offset" labels (de/en/cs).
- Inline offset editor sub-UI inside `VacationEntitlementSelfBody`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FE state + api + service plumbing for the offset</name>
  <files>shifty-dioxus/src/state/vacation_balance.rs, shifty-dioxus/src/api.rs, shifty-dioxus/src/service/vacation_balance.rs</files>
  <read_first>
    - shifty-dioxus/src/state/vacation_balance.rs (whole file — struct + From<&VacationBalanceTO> + existing test)
    - shifty-dioxus/src/api.rs:633-686 (update_absence_period POST/PUT pattern + get_vacation_balance)
    - shifty-dioxus/src/service/vacation_balance.rs (whole file — VacationBalanceAction + coroutine + stores)
    - rest-types VacationBalanceTO + VacationEntitlementOffsetTO (from Plan 28-02)
  </read_first>
  <behavior>
    - VacationBalance (FE state) carries offset_days + computed_entitled_days mapped 1:1 from the TO; the existing round-trip test still passes and a new assertion covers the two Option fields (Some and None).
    - SaveOffset action POSTs the offset, then re-dispatches a reload so the stores reflect the new effective value.
  </behavior>
  <action>
    In `shifty-dioxus/src/state/vacation_balance.rs`: add `pub offset_days: Option<i32>` and `pub computed_entitled_days: Option<f32>` to `VacationBalance`, map them in `From<&VacationBalanceTO>`, and update the existing `vacation_balance_from_to_preserves_all_fields` test (and add a case asserting None round-trips and Some round-trips) so the two new fields are covered.

    In `shifty-dioxus/src/api.rs`: add `save_vacation_entitlement_offset(config, sales_person_id: Uuid, year: u32, offset_days: i32) -> Result<(), ...>` that POSTs `VacationEntitlementOffsetTO { sales_person_id, year, offset_days }` to `{backend}/vacation-entitlement-offset` (mirror the `reqwest::Client::new().post(url).json(&body).send()` + `error_for_status_ref()` pattern from update_absence_period).

    In `shifty-dioxus/src/service/vacation_balance.rs`: add `SaveOffset(Uuid, u32, i32)` to `VacationBalanceAction`; in the coroutine match, on SaveOffset call the api save (via loader if a loader wrapper is the convention — otherwise call api directly with CONFIG), and on success re-dispatch the appropriate reload (LoadTeam(year) for the HR overview path, plus LoadSelf if the self store is populated) so the effective value re-computes from the backend; on error write ERROR_STORE.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test vacation_balance 2>&1 | tail -15</automated>
    <automated>cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - FE state carries + round-trips the two new Option fields; tests green.
    - `save_vacation_entitlement_offset` POSTs to the new endpoint; SaveOffset action wired to reload after save.
    - WASM build succeeds (run from the backend devShell for the wasm linker).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Inline HR offset editor in the StatBox + i18n de/en/cs</name>
  <files>shifty-dioxus/src/page/absences.rs, shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/page/absences.rs:404-556 (VacationEntitlementCard, forced_self/show_self logic, VacationEntitlementSelfBody, the VacationStatContract StatBox at :533-536, StatBox component)
    - shifty-dioxus/src/i18n/mod.rs:465-475 (Key enum Vacation* entries) and :808-843 (the key-coverage test array)
    - shifty-dioxus/src/i18n/de.rs:791, en.rs:724, cs.rs:777 (VacationStatContract translation rows — add the new labels alongside)
    - shifty-dioxus/src/service/vacation_balance.rs (VacationBalanceAction::SaveOffset to dispatch from the editor — use the existing coroutine handle pattern in absences.rs)
  </read_first>
  <action>
    Thread `is_hr` into `VacationEntitlementSelfBody`: add `is_hr: bool` to `VacationEntitlementSelfBodyProps` and pass `is_hr: props.is_hr` from `VacationEntitlementCard` (the SelfBody is only shown to HR on the `forced_self` path, so `props.is_hr` correctly distinguishes HR-detail from employee-self).

    Replace the plain `VacationStatContract` StatBox in `VacationEntitlementSelfBody` with a contract-anspruch cell that:
      - When `props.is_hr && balance.computed_entitled_days.is_some()`: shows the EFFECTIVE entitled value as the box number, plus a "berechnet {computed} + Offset [x]" line where `{computed}` is `computed_entitled_days` (formatted) and `[x]` is a signed inline number `<input>` seeded from `offset_days.unwrap_or(0)`. On blur/Enter, parse the signed integer and dispatch `VacationBalanceAction::SaveOffset(balance.sales_person_id, props.year, parsed_offset)` (year-scoped per D-28-09; HR-gating enforced server-side). Use STATIC Tailwind classes (no `format!` into class strings — Pitfall 5 in this file).
      - Otherwise (employee self-view, or fields None): render exactly the current effective-only StatBox — no "berechnet/Offset" line, no input (D-28-03 / ROADMAP SC5).

    Add i18n Keys (e.g. `VacationOffsetComputedLabel` for "berechnet {n}" and `VacationOffsetLabel` for "Offset") to `i18n/mod.rs` and provide translations: de "berechnet" / "Offset" (`Locale::De` — never `Locale::En`), en "calculated" / "Offset", cs "vypočteno" / "Offset" (cs wording is Claude's discretion). Add the new Keys to the i18n key-coverage test array (mod.rs ~:822-832) so all three locales are guarded.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test i18n 2>&1 | tail -15</automated>
    <automated>cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -8</automated>
    <human-check>HR detail of a person with computed 17, offset +1 shows "berechnet 17 + Offset [1]" and box 18; the same person in user self-view shows only 18 (no field, no berechnet/Offset line). Editing the field and blurring persists and the number updates after reload.</human-check>
  </verify>
  <acceptance_criteria>
    - HR person-detail StatBox shows effective value + signed inline offset field + "berechnet {computed} + Offset [x]" (D-28-07, ROADMAP SC4).
    - User self-view shows only the effective value — no field, no berechnet/Offset line (D-28-03, ROADMAP SC5).
    - New labels present in de (Locale::De), en, cs and covered by the i18n coverage test (D-28-08, ROADMAP SC6).
    - Edit dispatches SaveOffset for the selected year + person; WASM build + i18n tests green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser FE → REST | The save call is a client request; HR enforcement is server-side (the FE editor visibility is convenience, not a security control) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-09 | Elevation of Privilege | FE offset editor / save call | medium | mitigate | FE only renders the editor for HR, but real enforcement is the server-side HR_PRIVILEGE gate (Plan 28-01/02); FE hiding is not the control |
| T-28-10 | Information Disclosure | self-view rendering | medium | mitigate | Backend sends offset/computed as None to self-only callers (D-28-03); FE has nothing to leak |
| T-28-SC | Tampering | npm/pip/cargo installs | high | accept | No new dependencies introduced (reuses dioxus/reqwest) |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test` green (state round-trip + i18n coverage).
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` succeeds from the backend devShell.
- Browser roundtrip (human-check): HR sees berechnet/Offset + effective; user sees effective only; save persists.
</verification>

<success_criteria>
- HR inline signed offset editor with "berechnet {n} + Offset [x]" (D-28-07).
- User self-view effective-only (D-28-03).
- i18n de/en/cs labels added + guarded (D-28-08).
- Year-scoped edit (D-28-09).
</success_criteria>

<output>
Create `.planning/phases/28-urlaubsanspruch-korrektur-offset/28-04-SUMMARY.md` when done.
</output>
