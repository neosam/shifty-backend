---
phase: 27-freiwillige-abwesenheitsliste-selector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/page/absences.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: false
requirements: [VOL-SEL-01]

must_haves:
  truths:
    - "SC1 + D-27-01: Active volunteers (`!inactive && !is_paid`) appear in a labeled \"Freiwillige\" optgroup rendered BELOW the \"Angestellte\" optgroup in the AbsenceModal person dropdown, and an absence can be created for such a person (create-path roundtrip)."
    - "SC2 + D-27-06: The AbsenceFilterBar (HR-only) person filter applies the SAME grouping, and the existing \"Alle\"-option is preserved and placed BEFORE the two groups (D-27-06)."
    - "SC3 + D-27-02: Inactive persons (`inactive`) appear in NEITHER group in BOTH selectors; the shared `is_selectable_employee` stays `is_paid && !inactive` and `selectable_balances` stays unchanged, so the HR `VacationPerPersonList` remains paid-only (D-27-02)."
    - "SC4 + D-27-04: Both group labels resolve non-empty (never \"??\") in en/de/cs via the two new keys `AbsenceGroupEmployees` / `AbsenceGroupVolunteers` (D-27-04)."
    - "SC5 + D-27-03: A group with zero active members renders NO optgroup — no empty \"Freiwillige\" optgroup when there are no volunteers, no empty \"Angestellte\" optgroup when there are no employees (D-27-03)."
    - "D-27-05: The category dropdown in the modal is UNCHANGED — volunteers get the same categories as employees (Vacation + UnpaidLeave; SickLeave stays globally hidden via `SICK_LEAVE_ENABLED = false`)."
  artifacts:
    - "shifty-dioxus/src/page/absences.rs — pure `grouped_selectable(&[SalesPerson]) -> Vec<(PersonGroup, Vec<&SalesPerson>)>`, `PersonGroup` enum, RSX helper `grouped_person_options(...)`, plus pure-function tests"
    - "shifty-dioxus/src/i18n/mod.rs — two new `Key` variants `AbsenceGroupEmployees` / `AbsenceGroupVolunteers` + coverage-test extension"
    - "shifty-dioxus/src/i18n/en.rs, de.rs, cs.rs — translations for both new keys in all three locales"
  key_links:
    - "Both call-sites (AbsenceModal person dropdown `absences.rs:1217`, AbsenceFilterBar person loop `absences.rs:1428`) call the SAME RSX helper `grouped_person_options` — no copy-paste (D-27-01)."
    - "`grouped_person_options` derives its `<option>`s from the pure `grouped_selectable`; `SelectInput` (`component/form/inputs.rs:83`) is UNCHANGED — it renders `{ children }`, so native `optgroup` passes through as children."
    - "FilterBar passes `selected_id = Uuid::parse_str(&person_value).ok()` so the \"all\" value (parse-fail → None) keeps the standalone \"Alle\"-option selected while the groups carry no selection."
---

<objective>
Make **Freiwillige** (sales persons with `is_paid == false`, active) selectable on the absences page — in the AbsenceModal person dropdown AND the AbsenceFilterBar HR person filter — visually separated from Angestellte via native `optgroup` grouping ("Angestellte" first, then "Freiwillige"). One shared helper feeds both call-sites; `SelectInput` is untouched.

This is a **frontend-only** phase (`shifty-dioxus/`). The backend already supports volunteer absences (Phase 26 VFA + Phase 17 `EmployeeWorkDetails`); the full `sales_persons` list is already loaded. Phase-level prerequisite (ROADMAP "Depends on Phase 26") is already satisfied — no in-planning `depends_on`.

Purpose: VOL-SEL-01 — the business needs to record and filter absences for volunteers, not just paid employees.
Output: a pure, unit-tested grouping function + a thin RSX helper rendering two non-empty optgroups, both call-sites rewired, two new i18n keys (de/en/cs), and a browser smoke verifying the backend roundtrip.

Scope guards (do NOT do):
- Do NOT loosen `is_selectable_employee` (stays `is_paid && !inactive`) and do NOT change `selectable_balances` — the HR `VacationPerPersonList` stays paid-only (D-27-02). Volunteers in the vacation overview are a Phase 28 topic.
- Do NOT change `SelectInput`, the category dropdown (D-27-05), or any backend code.
- Do NOT touch the existing `is_selectable_employee` tests (`:2476-2504`) or `selectable_balances` tests (`:3557-3610`) — they MUST stay green unchanged.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-freiwillige-abwesenheitsliste-selector/27-CONTEXT.md
@.planning/phases/27-freiwillige-abwesenheitsliste-selector/SEED.md
@CLAUDE.md
@shifty-dioxus/CLAUDE.md

# Source files in scope
@shifty-dioxus/src/page/absences.rs
@shifty-dioxus/src/component/form/inputs.rs
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/state/shiftplan.rs
</context>

<vcs_jj_only>
## VCS: jj (Jujutsu) ONLY — co-located git, NO git commands

This repo is managed with **jj**, co-located with git. GSD auto-commit is DISABLED.

- Commit EACH task as exactly ONE jj change with a Conventional-Commit message:
  `jj commit -m "<type>(27): <subject>"` — or `jj describe -m "..."` then `jj new`.
- jj has NO staging area and NO hooks — `jj commit` snapshots the whole working copy. Do NOT stage.
- **NEVER** run `git commit`, `git add`, `git stash`, `git merge`, `git reset`, or any `git` write command.
- **NEVER** run `gsd-tools query commit`.
- No worktree isolation (`workflow.use_worktrees=false`) — work directly on the main working copy.
- Run the verification gates (below) BEFORE committing each task; only commit when they pass.
- Suggested per-task messages:
  - Task 1: `feat(27): add AbsenceGroupEmployees/Volunteers i18n keys (de/en/cs)`
  - Task 2: `feat(27): group absence person selectors by employees/volunteers (VOL-SEL-01)`
  - Task 3 (after human smoke OK): handled by the orchestrator / user — do not auto-commit a checkpoint.
</vcs_jj_only>

<tasks>

<task type="auto">
  <name>Task 1: Add the two grouping i18n keys in all three locales</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs:438-448 — the `Key` enum block where `AbsenceCategory*` / `AbsenceFilterPerson*` variants live.
    - shifty-dioxus/src/i18n/mod.rs:800-839 — the absence-domain locale-coverage test (`for key in [...]` asserting non-empty, non-"??" in En/De/Cs).
    - shifty-dioxus/src/i18n/en.rs:676-682, de.rs:747-753, cs.rs:729-735 — `add_text(Locale::_, Key::Absence…, "…")` registration pattern.
  </read_first>
  <action>
    Implements D-27-04. Add two new `Key` enum variants to `i18n/mod.rs` — `AbsenceGroupEmployees` and `AbsenceGroupVolunteers` — placed next to the existing `AbsenceFilterPerson*` variants so they stay grouped with the absence-domain keys. Register a translation for each new key in all three locale files via the existing `add_text` pattern: en.rs → "Employees" / "Volunteers"; de.rs (use `Locale::De`, not `Locale::En`) → "Angestellte" / "Freiwillige"; cs.rs → "Zaměstnanci" / "Dobrovolníci" (cs wording is Claude's discretion per D-27-04 — use the common professional translation). Then extend the absence-domain coverage test's `for key in [...]` list (mod.rs:806-829) with both new keys so the test asserts every locale resolves them non-empty and not "??". Do not add or modify any other keys.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --lib i18n -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `Key::AbsenceGroupEmployees` and `Key::AbsenceGroupVolunteers` exist in the enum.
    - All three locale files register both keys; de.rs uses `Locale::De`.
    - The absence-domain coverage test includes both new keys and passes (no "??", no empty).
  </acceptance_criteria>
  <done>Both new keys exist in the `Key` enum, are translated in en/de/cs (de.rs uses `Locale::De`), and the absence-domain coverage test (extended with both keys) passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure grouping logic + RSX helper, rewire both selectors</name>
  <files>shifty-dioxus/src/page/absences.rs</files>
  <read_first>
    - shifty-dioxus/src/page/absences.rs:110-141 — `is_selectable_employee` (KEEP `is_paid && !inactive`) and `selectable_balances` (KEEP unchanged) — the style template for the new pure function.
    - shifty-dioxus/src/page/absences.rs:1207-1258 — AbsenceModal person dropdown (`:1217` flat filter loop) + category dropdown (`:1229`, UNCHANGED per D-27-05) + `is_hr` gate (`:1211`).
    - shifty-dioxus/src/page/absences.rs:1347-1438 — AbsenceFilterBar: `person_value: String` (`:1361`), "Alle"-option (`:1425`), person loop (`:1428`).
    - shifty-dioxus/src/page/absences.rs:2400-2504 — test module header (`mod tests { use super::*; ... }`) + `sales_person(is_paid, inactive)` helper + the 4 `is_selectable_employee` tests (do NOT modify these 4).
    - shifty-dioxus/src/page/absences.rs:3557-3611 — `selectable_balances` tests (do NOT modify; they prove paid-only stays paid-only, D-27-02).
    - shifty-dioxus/src/component/form/inputs.rs:82-102 — `SelectInput` renders `{ props.children }`; optgroup passes through unchanged.
    - shifty-dioxus/src/state/shiftplan.rs:124-131 — `SalesPerson { id, name, is_paid, inactive, ... }`.
    - shifty-dioxus/src/i18n/mod.rs:636 — `pub type I18nType = I18n<Key, Locale>` (the `i18n` value type held by `I18N.read().clone()`).
  </read_first>
  <behavior>
    Pure function `grouped_selectable(&[SalesPerson]) -> Vec<(PersonGroup, Vec<&SalesPerson>)>`:
    - Test partition: input {active-paid A, active-unpaid B, inactive-paid C, inactive-unpaid D} → exactly two groups; Employees group members == [A], Volunteers group members == [B]; C and D appear in neither (D-27-06).
    - Test order: when both groups non-empty, result[0].0 == PersonGroup::Employees and result[1].0 == PersonGroup::Volunteers (Angestellte before Freiwillige, D-27-01).
    - Test empty-volunteers: input only active-paid persons → result has exactly one group, PersonGroup::Employees; no Volunteers entry (D-27-03).
    - Test empty-employees: input only active-unpaid persons → result has exactly one group, PersonGroup::Volunteers; that single group is index 0 (D-27-03).
    - Test order-preserved-within-group: two active-paid persons in input order [A, B] → Employees members == [A, B] (load order kept).
  </behavior>
  <action>
    Implements D-27-01, D-27-02, D-27-03, D-27-05, D-27-06.

    (a) Add a small public enum `PersonGroup` with variants `Employees` and `Volunteers` (derive `Debug, Clone, Copy, PartialEq, Eq`).

    (b) Add the pure function `grouped_selectable` next to `is_selectable_employee` (around `:117`), in the same documented pure-function style. It filters `!inactive && is_paid` into the employees vec and `!inactive && !is_paid` into the volunteers vec (its OWN predicate — it does NOT call and does NOT modify `is_selectable_employee`, per D-27-02), preserving input order within each group, then returns only the NON-empty groups as an ordered Vec with Employees before Volunteers (D-27-01, D-27-03). Inactive persons land in neither group (D-27-06).

    (c) Add a private RSX helper returning `Element`, e.g. `grouped_person_options(sales_persons: &[SalesPerson], selected_id: Option<Uuid>, i18n: &I18nType) -> Element`. It iterates the result of `grouped_selectable` and, per group, renders one native `optgroup` whose `label` is the group's i18n string (map `PersonGroup::Employees` → `Key::AbsenceGroupEmployees`, `PersonGroup::Volunteers` → `Key::AbsenceGroupVolunteers`), with one `option` per member: `value` = the person id, `selected` = `Some(sp.id) == selected_id`, text = the person name. Because `grouped_selectable` already omits empty groups, no empty `optgroup` is ever rendered. `optgroup` is a standard HTML element supported by Dioxus RSX.

    (d) Rewire the AbsenceModal person dropdown (`:1217`): replace the flat `for sp in props.sales_persons.iter().filter(|sp| is_selectable_employee(sp)) { option { ... } }` with a single call embedding `{ grouped_person_options(&props.sales_persons, Some(*employee_id.read()), &i18n) }` inside the existing `SelectInput`. The `is_hr` disabled gate (`:1211`), `on_change`, and the category dropdown (`:1229`) stay exactly as-is (D-27-05).

    (e) Rewire the AbsenceFilterBar person loop (`:1428`): KEEP the standalone "Alle"-option (`:1425`) exactly and BEFORE the groups; replace only the `for sp in ...` loop with `{ grouped_person_options(&props.sales_persons, Uuid::parse_str(&person_value).ok(), &i18n) }`. When `person_value == "all"` the parse fails → `None` → no group option is marked selected while the "Alle"-option carries the selection (D-27-06).

    (f) Add the pure-function tests described in `<behavior>` to the existing `#[cfg(test)] mod tests` (`:2401`), near the `is_selectable_employee` tests. Build persons with distinct ids/names (the existing `sales_person(is_paid, inactive)` helper yields nil ids — use the inline `SalesPerson { id, name, is_paid, inactive, ..Default::default() }` form like the `selectable_balances` tests, or add a small local `named_sp(...)` helper). Do NOT modify the existing `is_selectable_employee` or `selectable_balances` tests.

    Follow TDD: write the failing pure-function tests first (RED), then implement `PersonGroup` + `grouped_selectable` to pass (GREEN), then wire the RSX helper and the two call-sites.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --lib grouped 2>&1 | tail -30</automated>
    <automated>cd shifty-dioxus && cargo test --lib selectable 2>&1 | tail -20</automated>
    <automated>grep -nF 'sales_person.is_paid && !sales_person.inactive' shifty-dioxus/src/page/absences.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grouped_selectable` and `PersonGroup` exist; the five `<behavior>` tests pass.
    - `is_selectable_employee` body is still `is_paid && !inactive` (grep matches exactly once) and the 4 existing `is_selectable_employee` tests + 5 `selectable_balances` tests pass unchanged.
    - Both call-sites call `grouped_person_options`; the AbsenceFilterBar "Alle"-option is preserved before the groups.
    - The category dropdown and `SelectInput` are unchanged.
  </acceptance_criteria>
  <done>`grouped_selectable` + `PersonGroup` + `grouped_person_options` exist; the five new pure-function tests pass; both selectors render the two optgroups via the shared helper; `is_selectable_employee`/`selectable_balances` and their existing tests are unchanged and green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser smoke — volunteer grouped in Modal + Filter (backend roundtrip)</name>
  <action>Run the WASM build, FE test, and clippy gates from the backend nix shell, then perform the browser smoke described in how-to-verify. This is a human-verify checkpoint — do not auto-commit; wait for the resume-signal.</action>
  <what-built>
    Volunteers are now selectable in both absence person selectors, grouped under a "Freiwillige" optgroup below "Angestellte", in the AbsenceModal and the HR AbsenceFilterBar. All automated gates (WASM build, FE tests, clippy -D warnings) have passed before this checkpoint.
  </what-built>
  <how-to-verify>
    Run all gates from the BACKEND nix shell (`nix develop`) because FE clippy + lld live there (toolchain split):
    1. WASM build gate (from `shifty-dioxus/`): `cargo build --target wasm32-unknown-unknown` — succeeds.
    2. FE tests: `cd shifty-dioxus && cargo test` — all green (new grouping tests + unchanged existing tests).
    3. Clippy hard gate (from `shifty-dioxus/`, backend shell): `cargo clippy --workspace -- -D warnings` — zero warnings.
    4. Browser smoke (backend roundtrip — create-path ≠ edit-path): start backend (port 3000) + frontend (port 8080). Ensure at least one ACTIVE sales person with `is_paid == false` exists. On `/absences`:
       a. Open the AbsenceModal "Neu" dialog → the person dropdown shows a "Freiwillige" group (below "Angestellte") containing that volunteer; select them, pick a category (Vacation/UnpaidLeave), pick a range, and SAVE — the absence is created (no error banner; it appears in the list).
       b. As HR, open the AbsenceFilterBar person filter → the same "Alle" option is first, then the grouped "Angestellte"/"Freiwillige" groups; selecting the volunteer filters the list.
       c. Confirm inactive persons appear in NEITHER group, and that a group with no members renders no empty optgroup.
       d. Switch locale to de and cs → group labels read "Angestellte"/"Freiwillige" and "Zaměstnanci"/"Dobrovolníci".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>All three automated gates pass and the human confirms volunteers are grouped/selectable in both selectors with a working create-path and localized labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (WASM) → backend REST | Person id + category + range submitted when creating an absence. Pre-existing boundary; this phase adds no new endpoint and no new field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | AbsenceFilterBar person filter | low | mitigate | The `is_hr` gate (`absences.rs:1411`) is preserved unchanged; the grouping helper only re-orders existing options, it does not add an unauthorized control surface. |
| T-27-02 | Tampering | New volunteer-selectable absence create-path | low | accept | A volunteer id was always part of the loaded `sales_persons`; the backend (Phase 26 VFA) already authorizes/validates absence creation for unpaid persons. No new client-trusted field is introduced. |
| T-27-03 | Information Disclosure | i18n group labels | low | accept | Labels are static UI strings in en/de/cs; no data is exposed. |
| T-27-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — no new dependencies added; legitimacy gate not applicable. |
</threat_model>

<verification>
Run from the BACKEND nix shell (`nix develop`) — FE clippy and lld for the WASM target live there (known toolchain split; do NOT run clippy from the dioxus shell):

1. `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` — WASM build gate passes.
2. `cd shifty-dioxus && cargo test` — all FE tests green (new `grouped_selectable` tests + the 4 unchanged `is_selectable_employee` tests + the 5 unchanged `selectable_balances` tests + i18n coverage incl. the 2 new keys).
3. `cd shifty-dioxus && cargo clippy --workspace -- -D warnings` — zero warnings (hard gate).
4. `grep -nF 'sales_person.is_paid && !sales_person.inactive' shifty-dioxus/src/page/absences.rs` — `is_selectable_employee` body still intact (D-27-02).
5. Browser smoke (Task 3 checkpoint) — volunteer visible/grouped in Modal + Filter; absence creatable; labels localized de/en/cs.
</verification>

<success_criteria>
- Active volunteers appear under a labeled "Freiwillige" optgroup BELOW "Angestellte" in BOTH the AbsenceModal person dropdown and the AbsenceFilterBar person filter (SC1, SC2; D-27-01).
- An absence can be created for an active volunteer via the create-path (SC1; backend roundtrip).
- Inactive persons appear in neither group in either selector (SC3; D-27-06).
- `is_selectable_employee` (`is_paid && !inactive`) and `selectable_balances` are unchanged; HR `VacationPerPersonList` stays paid-only; their existing tests remain green (D-27-02).
- The AbsenceFilterBar "Alle"-option is preserved and placed before the groups (SC2; D-27-06).
- Both group labels resolve in en/de/cs via the new keys (SC4; D-27-04).
- No empty optgroup is rendered for an empty group (SC5; D-27-03).
- Category dropdown unchanged — volunteers get the same categories as employees (D-27-05).
- WASM build, `cargo test`, and `cargo clippy -- -D warnings` all pass.

## Artifacts this phase produces
- `PersonGroup` enum (`{ Employees, Volunteers }`) — `shifty-dioxus/src/page/absences.rs`.
- `grouped_selectable(&[SalesPerson]) -> Vec<(PersonGroup, Vec<&SalesPerson>)>` — pure, unit-tested grouping (active-only, employees-first, empty groups omitted).
- `grouped_person_options(&[SalesPerson], Option<Uuid>, &I18nType) -> Element` — RSX helper rendering the two optgroups; called by BOTH selectors.
- `Key::AbsenceGroupEmployees`, `Key::AbsenceGroupVolunteers` — two i18n keys + en/de/cs translations.
</success_criteria>

<output>
Create `.planning/phases/27-freiwillige-abwesenheitsliste-selector/27-01-SUMMARY.md` when done.
</output>
