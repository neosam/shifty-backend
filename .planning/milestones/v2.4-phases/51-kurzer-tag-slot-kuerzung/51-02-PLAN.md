---
phase: 51
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260704000000_seed-shortday-slot-clipping-toggle.sql
  - service_impl/src/lib.rs
  - service_impl/src/shortday_gate.rs
autonomous: true
requirements: [SHC-06]
must_haves:
  truths:
    - "D-51-07: Toggle `shortday_slot_clipping_active_from` existiert im Katalog, seed via additive Migration (analog `holiday_auto_credit`)"
    - "D-51-07 gate: helper `should_clip(booking_date, active_from) -> bool` gibt `false` zurück wenn `active_from == None` (Legacy-Verhalten, Rollout-Default)"
    - "D-51-07 gate: helper gibt `true` zurück gdw `booking_date >= active_from` (Grenzfall inklusiv am Stichtag)"
    - "D-51-07 gate: helper gibt `false` zurück bei `booking_date == active_from - 1` (Vortag vor Stichtag = ungeclippt)"
    - "Snapshot-Version bleibt 12 — additive Toggle-Zeile, keine `BillingPeriodValueType`-Berührung"
  artifacts:
    - "`migrations/sqlite/20260704000000_seed-shortday-slot-clipping-toggle.sql` — additive `INSERT OR IGNORE` in `toggle`-Tabelle"
    - "`service_impl/src/shortday_gate.rs` — pure fn `should_clip` + `parse_active_from` + `resolve_active_from`"
    - "`service_impl/src/shortday_gate.rs` — `#[cfg(test)] mod tests` mit Grenzfall-Tests"
  key_links:
    - "Alle vier Ketten (P03/P04/P05/P06) konsumieren `shortday_gate::should_clip(booking_date, active_from)` vor `Slot::clip_to`"
---

<objective>
Additive Toggle-Seed-Migration `shortday_slot_clipping_active_from` (D-51-07, analog
HCFG-02 aus v1.7) und ein wiederverwendbares Rust-Helper-Modul `shortday_gate` mit
purer Stichtag-Gate-Funktion. Kein Schema-Change, kein Snapshot-Bump.

Purpose: SHC-06 Backend-Backbone — eine einzige Stelle, die die Stichtag-Semantik
(kein Wert → aus, sonst `booking_date >= active_from` → clip) implementiert. Wave 2
konsumiert diesen Helper in allen vier Aggregat-Ketten.

Output:
- Neue Migration `20260704000000_seed-shortday-slot-clipping-toggle.sql`.
- Neues Modul `service_impl/src/shortday_gate.rs` mit purer `should_clip`-Fn plus
  ISO-8601-Parser plus Unit-Tests.
- Registrierung des Moduls in `service_impl/src/lib.rs`.
</objective>

<execution_context>
Auto-commit via git (jj-import).
Migrations laufen automatisch beim Server-Start / `sqlx migrate run` — keine
`sqlx setup`/`reset`-Aufrufe hier (DB nicht jj-getrackt, siehe
`feedback_destructive_db_ops`).
</execution_context>

<context>
- `migrations/sqlite/20260628000001_seed-holiday-auto-credit-toggle.sql` — Referenz-Migration für HCFG-02. **Verbatim-Struktur übernehmen**, nur Name/Description/`update_process` austauschen.
- `migrations/sqlite/20260628000000_toggle-value-column.sql` — belegt, dass `toggle`-Tabelle bereits ein `value TEXT`-Feld hat (aus HCFG-02-Phase). Hier NICHT neu anlegen.
- `service_impl/src/reporting.rs:164-180` — HCFG-02-Konsummuster: `toggle_service.get_toggle_value("holiday_auto_credit", context, None)` → `Option<String>` → `time::Date::parse(..., Iso8601::DEFAULT)`. Denselben Parser hier für unser Modul referenzieren; Konsumenten in Wave 2 rufen den Toggle selbst auf und übergeben `Option<time::Date>` an unseren Helper.
- `service_impl/src/lib.rs` — Root-Modul-Deklarationen. Neues `pub mod shortday_gate;` einfügen (alphabetisch sortiert).
- CONTEXT.md D-51-07 (Semantik), CLAUDE.md § Snapshot-Versioning (klar: keine value_type-Berührung → kein Bump).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration file — seed `shortday_slot_clipping_active_from`</name>
  <files>migrations/sqlite/20260704000000_seed-shortday-slot-clipping-toggle.sql</files>
  <action>
Datei neu anlegen mit genau folgendem Inhalt (Struktur exakt wie `20260628000001_seed-holiday-auto-credit-toggle.sql`, nur die drei String-Werte ausgetauscht):

```sql
INSERT OR IGNORE INTO toggle (name, enabled, description, update_process)
VALUES (
    'shortday_slot_clipping_active_from',
    0,
    'When a cutoff date is set in `value` (ISO YYYY-MM-DD), slots at short-day dates >= that date are clipped at the ShortDay cutoff time in rendering and hours calculation. Leave value NULL to disable (legacy behavior).',
    'phase-51-migration'
);
```

- `enabled = 0` gemäß HCFG-02-Präzedenz: der Legacy-Zustand ist "aus" bei
  fehlendem `value`.
- **Kein Schema-Change**, nur additive Zeile.
- Filename-Timestamp `20260704000000` liegt korrekt nach der HCFG-02-Migration
  (`20260628000001`).
- `INSERT OR IGNORE`, damit ein Re-Run der Migration idempotent bleibt.

Nach Anlage: **kein** `sqlx migrate run` in diesem Task ausführen (destructive-Risk-Regel; Migration läuft beim nächsten `cargo run`/Server-Start). `cargo sqlx prepare --workspace` ist ebenfalls NICHT nötig, weil keine `query!`/`query_as!`-Makro betroffen ist — die Migration ist ein pures INSERT.
  </action>
  <verify>
    <automated>test -f /home/neosam/programming/rust/projects/shifty/shifty-backend/migrations/sqlite/20260704000000_seed-shortday-slot-clipping-toggle.sql &amp;&amp; grep -q 'shortday_slot_clipping_active_from' /home/neosam/programming/rust/projects/shifty/shifty-backend/migrations/sqlite/20260704000000_seed-shortday-slot-clipping-toggle.sql &amp;&amp; grep -q 'phase-51-migration' /home/neosam/programming/rust/projects/shifty/shifty-backend/migrations/sqlite/20260704000000_seed-shortday-slot-clipping-toggle.sql</automated>
  </verify>
  <done>
Migrationsdatei existiert und enthält Toggle-Namen + `update_process`-Marker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `shortday_gate` helper module</name>
  <files>service_impl/src/shortday_gate.rs, service_impl/src/lib.rs</files>
  <behavior>
    - `parse_active_from(raw: Option<&str>) -> Option<time::Date>`:
      - `None` → `None`.
      - `Some("")` → `None`.
      - `Some(bad)` → `None` (defensive; Konsumenten sollen Legacy fallen, nicht crashen — analog `reporting.rs:173-179`).
      - `Some("2026-08-01")` → `Some(Date::from_calendar_date(2026, Month::August, 1))`.
    - `should_clip(booking_date: time::Date, active_from: Option<time::Date>) -> bool`:
      - `active_from = None` → `false` (Legacy / Rollout-Default).
      - `booking_date < active_from` → `false`.
      - `booking_date == active_from` → `true` (Grenzfall inklusiv, D-51-07 SHC-06-Test).
      - `booking_date > active_from` → `true`.
    - `resolve_active_from_for_week(year: u32, week: u8, day_of_week: DayOfWeek, active_from: Option<time::Date>) -> bool` (Convenience für Konsumenten, die keine konkrete `time::Date` haben, sondern nur ISO-Woche + DayOfWeek):
      - Konstruiert `booking_date = time::Date::from_iso_week_date(year as i32, week, day_of_week.into())`; bei Parse-Fehler `false` (defensive skip).
      - Delegiert an `should_clip`.
  </behavior>
  <action>
Neue Datei `service_impl/src/shortday_gate.rs` anlegen. Modul-Deklaration:
- `use shifty_utils::DayOfWeek;`
- `use time::{Date, format_description::well_known::Iso8601};`
- Konstante `pub const TOGGLE_NAME: &str = "shortday_slot_clipping_active_from";` — Konsumenten in Wave 2 nutzen diese statt Magic-String.

Drei `pub fn` implementieren wie in `<behavior>`:
- `parse_active_from` nutzt `time::Date::parse(s, &Iso8601::DEFAULT).ok()`. Leerer String und `None` liefern `None`.
- `should_clip` — pure Fn, 4 Zweige, keine `.unwrap()`.
- `resolve_active_from_for_week` — nutzt `time::Date::from_iso_week_date(year as i32, week, day_of_week.into()).ok()`; `None` → `false`.

Datei-Ende: `#[cfg(test)] mod tests { use super::*; ... }` mit sieben Tests:
1. `parse_none_returns_none` — `parse_active_from(None)` == `None`.
2. `parse_empty_returns_none` — `parse_active_from(Some(""))` == `None`.
3. `parse_malformed_returns_none` — `parse_active_from(Some("not-a-date"))` == `None`.
4. `parse_iso_valid` — `parse_active_from(Some("2026-08-01"))` liefert das erwartete Date.
5. `should_clip_none_active_from_returns_false` — `should_clip(any_date, None)` == `false`.
6. `should_clip_before_stichtag_returns_false` — `booking_date = 2026-07-31`, `active_from = 2026-08-01` → `false`. (Grenzfall Vortag).
7. `should_clip_at_or_after_stichtag_returns_true` — `booking_date = 2026-08-01`, `active_from = 2026-08-01` → `true`; `booking_date = 2026-08-02`, `active_from = 2026-08-01` → `true`. (Grenzfall inklusiv am Stichtag).

**Modul in `service_impl/src/lib.rs` registrieren:** eine neue Zeile `pub mod shortday_gate;` einfügen (alphabetisch zwischen anderen `pub mod`-Zeilen; z. B. nach dem letzten `pub mod s*`-Eintrag).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib shortday_gate::tests</automated>
  </verify>
  <done>
Alle sieben Tests grün. `service_impl/src/lib.rs` referenziert `pub mod shortday_gate;`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Workspace gates</name>
  <files>(gate only)</files>
  <action>
Vor Commit die Standard-Gates fahren. `cargo sqlx prepare` NICHT nötig (keine neue
`query!` — nur ein additives `INSERT` in einer Migration). Snapshot-Version bleibt
12 (grep-check als Sanity).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings &amp;&amp; grep -q 'pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12;' service_impl/src/billing_period_report.rs</automated>
  </verify>
  <done>
Alle Tests grün, Clippy grün, `CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt `12`.
  </done>
</task>

</tasks>

<verification>
- Migration ist idempotent (`INSERT OR IGNORE`) und hat `update_process = 'phase-51-migration'` als Herkunfts-Marker (parallel zu HCFG-02-Muster).
- `shortday_gate::should_clip` ist pure (kein `Result`, kein async, keine DAO-Deps) — testbar ohne Fixtures.
- Der Toggle-Wert wird von den Konsumenten in Wave 2 selbst gelesen (via `ToggleService::get_toggle_value(shortday_gate::TOGGLE_NAME, ..)`); dieses Plan legt nur den Helper an, keine Konsum-Site.
</verification>

<success_criteria>
1. `cargo test -p service_impl --lib shortday_gate::tests` zeigt "7 passed".
2. Migrationsdatei existiert mit korrektem Toggle-Namen + `update_process`.
3. `cargo test --workspace` grün, `cargo clippy --workspace -- -D warnings` grün.
4. `service_impl/src/lib.rs` enthält genau eine neue Zeile `pub mod shortday_gate;`.
5. Snapshot-Version `12` unverändert.
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-02-SUMMARY.md` mit:
Migration-Filename + Timestamp, Helper-API (`TOGGLE_NAME`, `parse_active_from`,
`should_clip`, `resolve_active_from_for_week`), Signaturen und Test-Namen.
</output>
