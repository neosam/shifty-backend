---
phase: 51
plan: 04
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - service_impl/src/block.rs
  - service_impl/src/test/block.rs
autonomous: true
requirements: [SHC-02, SHC-05, SHC-06]
must_haves:
  truths:
    - "D-51-06 Chain A': `get_blocks_for_sales_person_week` clippt Slots via `Slot::clip_to(cutoff)` VOR dem Merge-Loop"
    - "D-51-06 Chain A': `get_unsufficiently_booked_blocks` clippt Slots vor `day_map.entry(...)`"
    - "D-51-05: iCal-Export (`get_blocks_for_next_weeks_as_ical`) fällt automatisch aus Chain A'-Fix — konsumiert `get_blocks_for_sales_person_week` bzw. `get_unsufficiently_booked_blocks`"
    - "D-51-07: Stichtag-Gate greift pro Wochentag vor Clip; false → roher Slot"
    - "D-04 Zeile 4: Slot 14:00–15:00 mit Cutoff 14:30 landet mit `to=14:30` in `Block.slots`; `Block.to` respektiert Cutoff"
    - "D-04 Zeile 3: Slot 15:00–16:00 mit Cutoff 14:30 wird nicht in `Block` aufgenommen (booking_slot_pair übersprungen)"
    - "SHC-05: Bookings auf teil-geclippten Slots bleiben in `Block.bookings`; nur der Slot-`to` verschiebt sich"
  artifacts:
    - "`service_impl/src/block.rs:87-96` — per-Slot-Clip vor `booking_slot_pairs.push`"
    - "`service_impl/src/block.rs:237-269` — per-Slot-Clip vor Filter+Merge in `get_unsufficiently_booked_blocks`"
    - "`service_impl/src/test/block.rs` — neue Tests: block-merge mit geclipptem Slot, iCal-Aggregat, Stichtag-Gate-Grenzfall"
  key_links:
    - "Merge-Reihenfolge kritisch (Research §Risks 4): erst pro-Slot-Clip, dann `slot.from == to`-Consecutive-Detection. Wird durch Clip vor Merge automatisch korrekt (geclippter Slot A endet auf Cutoff, Slot B beginnt hinter Cutoff → keine False-Merge)"
    - "Konsumiert `Slot::clip_to` aus P01, `shortday_gate` aus P02, `SpecialDayService::get_by_week` (existiert bereits)"
---

<objective>
Chain A' (BlockService — iCal + insufficient-booked + MyBlocks) — pro-Slot-Clip vor
den bestehenden Merge-Loops einbauen. Gilt für `get_blocks_for_sales_person_week`
(`service_impl/src/block.rs:47-180`) und `get_unsufficiently_booked_blocks`
(`service_impl/src/block.rs:227-346`). Beide reichen sich indirekt an
`get_blocks_for_next_weeks_as_ical` (`:182-225`) und `get_blocks_for_current_user`
(`:349-395`) durch, die keine eigene Änderung brauchen.

Purpose: SHC-02 (iCal + insufficient-Report sehen geclippte Zeiten), SHC-05
(existierende Bookings überleben), SHC-06 (Stichtag-Gate).

Output: BlockService konsumiert `Slot::clip_to` und `shortday_gate::should_clip`
in beiden Slot-Aggregat-Loops. Test-Suite deckt Merge-Grenzfall +
Stichtag-Grenzfall ab.
</objective>

<execution_context>
Auto-commit via git (jj-import).
`BlockService` ist Business-Logic-Tier (konsumiert `SlotService`, `BookingService`,
`SalesPersonService`, `ShiftplanViewService`, `IcalService`, `ConfigService`,
`ClockService`). Wir fügen `SpecialDayService` und `ToggleService` als neue
Basic-Deps hinzu — bleibt zyklen-frei.
</execution_context>

<context>
- `service_impl/src/block.rs:1-40` — Deps + `gen_service_impl!`. Neue Basic-Deps `SpecialDayService`, `ToggleService` hier andocken.
- `service_impl/src/block.rs:47-180` (`get_blocks_for_sales_person_week`):
  - `:87-96` — `booking_slot_pairs` werden gebaut. **Hier per-Slot-Clip einbauen**, nach dem `slot_service.get_slot(...)`-Call.
  - `:114-176` — Merge-Loop. Konsumiert `slot.from`, `slot.to`. Automatisch korrekt sobald der geclippte Slot ins Paar geht.
- `service_impl/src/block.rs:227-346` (`get_unsufficiently_booked_blocks`):
  - `:237-240` — `all_slots` via `slot_service.get_slots_for_week_all_plans(...)`.
  - `:247-250` — `day_map` gebaut. **Hier per-Slot-Clip einbauen**, VOR dem `day_map.entry(...)`-Push.
  - `:275-341` — Merge-Loop. Automatisch korrekt sobald geclippte Slots im `day_map`.
- `service_impl/src/block.rs:182-225` (`get_blocks_for_next_weeks_as_ical`): iCal delegiert an die zwei geclippten Methoden — automatisch mit-korrekt (D-51-05). Zeile 197-213 zeigt: `sales_person_id != nil` → `get_blocks_for_sales_person_week`; sonst `get_unsufficiently_booked_blocks`. Beide sind gefixt.
- `service_impl/src/block.rs:349-395` (`get_blocks_for_current_user`): loopt Wochen und delegiert an `get_blocks_for_sales_person_week` — auto-korrekt.
- `service_impl/src/test/block.rs` — bestehende Tests; wir hängen Tests an.
- CONTEXT.md D-51-06 (Chain A'), D-51-05 (iCal via Block), D-51-07 (Gate).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chain A' — `get_blocks_for_sales_person_week` clippt vor Merge</name>
  <files>service_impl/src/block.rs</files>
  <behavior>
    - Vor dem Sammeln der `booking_slot_pairs` (`:87-96`) einen SpecialDay-Lookup pro Woche einbauen (Muster: `reporting.rs:189-192`): `special_day_service.get_by_week(year, week, context.clone()).await?`.
    - Toggle-Lookup einmal pro Method-Call (nicht pro Slot): `toggle_service.get_toggle_value(shortday_gate::TOGGLE_NAME, context.clone(), None).await` → `active_from: Option<time::Date>`. Bei `Unauthorized` → `None` (Legacy-Fallback, wie in `reporting.rs:170-171`).
    - Im booking-slot-Loop (`:87-96`): nach `let Ok(slot) = self.slot_service.get_slot(...)`, aber vor `booking_slot_pairs.push((booking.clone(), slot))`, den Clip anwenden:
      - Cutoff für `slot.day_of_week` aus `special_days` finden: `special_days.iter().find_map(|sd| { if sd.day_of_week == slot.day_of_week && sd.day_type == SpecialDayType::ShortDay { sd.time_of_day } else { None } })`. (Zeile 42-51 in `shiftplan.rs` als Vorlage.)
      - Gate: `let should_clip = time::Date::from_iso_week_date(year as i32, week, slot.day_of_week.into()).ok().map(|d| shortday_gate::should_clip(d, active_from)).unwrap_or(false);`.
      - Wenn `should_clip == true` und `cutoff.is_some()`: `match slot.clip_to(cutoff.unwrap()) { None => continue, Some(clipped) => booking_slot_pairs.push((booking.clone(), clipped)) }`.
      - Sonst: `booking_slot_pairs.push((booking.clone(), slot))` (Legacy, ungeclippt).
    - Merge-Loop (`:114-176`) bleibt unverändert — `slot.from == to`-Detection ist stabil, weil geclippter Slot A auf `cutoff` endet und Slot B mit `from >= cutoff` bereits per `None` weggefallen wäre. Falls Slot B auf einer Grenze `from == cutoff` wäre, wäre A + B ein legaler Merge — semantisch okay (Block endet an B.to, aber B ist per Clip auch bei cutoff geendet oder komplett raus).
  </behavior>
  <action>
Schritt 1 — Deps erweitern:
- `gen_service_impl!`-Block (`service_impl/src/block.rs:28-39`): zwei neue Zeilen einfügen:
  ```
  SpecialDayService: service::special_days::SpecialDayService<Context = Self::Context> = special_day_service,
  ToggleService: service::toggle::ToggleService<Context = Self::Context, Transaction = Self::Transaction> = toggle_service,
  ```
- `use service::special_days::{SpecialDay, SpecialDayService, SpecialDayType};` (SpecialDayType schon nicht importiert — hier ergänzen).
- `use service::toggle::ToggleService;`.

Schritt 2 — `get_blocks_for_sales_person_week` (`:47-180`):
- Nach `sales_person = ... Arc::new(...)` (`:64-68`), aber vor dem `all_bookings`-Fetch, das Toggle+SpecialDay-Prefetch einbauen:
  ```
  let special_days = self.special_day_service.get_by_week(year, week, context.clone()).await?;
  let toggle_value = match self.toggle_service.get_toggle_value(crate::shortday_gate::TOGGLE_NAME, context.clone(), None).await {
      Ok(v) => v, Err(ServiceError::Unauthorized) => None, Err(e) => return Err(e),
  };
  let active_from = crate::shortday_gate::parse_active_from(toggle_value.as_deref());
  ```
- Im booking-Loop (`:87-96`): den `booking_slot_pairs.push((booking.clone(), slot))` durch den Clip-Zweig ersetzen (Semantik wie in `<behavior>` beschrieben). Cutoff-Lookup als Helper-Closure lokal, um Duplizierung zu vermeiden:
  ```
  let cutoff_for = |dow: shifty_utils::DayOfWeek| -> Option<time::Time> {
      special_days.iter().find_map(|sd| {
          if sd.day_of_week == dow && sd.day_type == SpecialDayType::ShortDay { sd.time_of_day } else { None }
      })
  };
  ```
- `debug!`/`dbg!`-Aufrufe im Original (`:62-63, 69, 82`) unangetastet lassen (v1.3-Debug-Logging).

Schritt 3 — DI-Konstruktion in `shifty_bin/src/main.rs`: `BlockServiceImpl` neu mit `special_day_service` und `toggle_service` konstruieren. Beide existieren bereits im DI-Graph (v1.7 HCFG-02 für ToggleService, ältere Phase für SpecialDayService).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl -p shifty_bin</automated>
  </verify>
  <done>
`get_blocks_for_sales_person_week` clippt pro Slot mit Gate. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain A' — `get_unsufficiently_booked_blocks` clippt vor Filter</name>
  <files>service_impl/src/block.rs</files>
  <behavior>
    - Vor `day_map.entry(...).push(slot)` (`:247-250`): pro Slot Cutoff + Gate anwenden. Wenn geclippt → `None` → Slot fällt aus dem Insufficient-Report. Wenn `Some(clipped)` → geclippten Slot in `day_map` einfügen (nicht das Original).
    - `min_resources`-Aggregation im Merge-Loop (`:275-341`) bleibt korrekt — `min_resources` ist ein Slot-Feld, unbeeinflusst vom Clip.
    - `slot.id`-basierte Booking-Filter (`:262-268`, `:293-296`) bleiben korrekt (id ist über Clip stabil).
  </behavior>
  <action>
- Nach dem `all_bookings`-Fetch (`:241-244`), aber vor `day_map`-Konstruktion (`:247-250`), das Toggle+SpecialDay-Prefetch einbauen (identisches Muster wie in Task 1; Duplizierung im Rahmen ok, weil zwei parallele Methods).
- Alternative-Refactor (optional, nur wenn es sauberer wird): einen privaten Helper-Fn `async fn clip_gate_for_week(&self, year, week, context) -> Result<(Arc<[SpecialDay]>, Option<time::Date>), ServiceError>` extrahieren, in beiden Methods aufrufen. Nicht Pflicht — der Executor entscheidet.
- Im `for slot in all_slots.iter()`-Loop (`:248-250`): pro Slot Cutoff nachschlagen + Gate prüfen; wenn clippen erforderlich, `slot.clip_to(cutoff)?` (bei `None` `continue`), sonst Original-Klon. Push in `day_map.entry(clipped_or_original.day_of_week).or_default().push(clipped_or_original)`.
- Achte darauf, dass `day_map` bisher `Vec<&Slot>` hält (`:247`). Der Clip erzeugt einen owned `Slot` — Typ `day_map` auf `Vec<Slot>` umstellen (Zeile 247). Nachfolgende `slots.clone()` (`:256`) etc. dann anpassen (owned statt Ref). Das ist nur ein type-Umbau, keine semantische Änderung. **Wenn der Refactor größer als 20 Zeilen wird, den Executor bitten, den Compiler-Fehler-Trail als Anleitung zu nehmen — jeder Fehler zeigt genau den nächsten Fix.**
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl</automated>
  </verify>
  <done>
Zweite Method clippt vor Filter+Merge. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests</name>
  <files>service_impl/src/test/block.rs</files>
  <behavior>
    - **Test A** `test_get_blocks_clips_overlap_slot_at_shortday_cutoff`: Slot Mo 10:00–14:00 + Slot Mo 14:00–16:00; ShortDay Mo Cutoff 15:00; `active_from = Some(vergangenes_Datum)` → gate active. Erwartet: Block Mo `from=10:00, to=15:00`, `slots = [10:00–14:00, 14:00–15:00]`. (Merge greift, weil beide Slots consecutive und der zweite wird auf 15:00 geclippt.)
    - **Test B** `test_get_blocks_drops_post_cutoff_slot`: Slot Mo 16:00–18:00, ShortDay Cutoff 15:00, Gate aktiv, Booking existiert auf 16:00-Slot → `booking_slot_pairs` überspringt das Paar → kein Block für Mo. **Wichtig für SHC-05:** die Booking-DB-Row wird NICHT gelöscht — nur die Aggregat-Sicht filtert. Der Test prüft daher, dass `get_blocks_for_sales_person_week` keinen Block enthält, aber ein direkter `booking_service.get_for_week`-Call weiterhin die Booking liefert. (Simulation im Test: der Mock von `booking_service` gibt sie zurück; das Fehlen im Block-Result bestätigt View-Layer-Filterung.)
    - **Test C** `test_get_blocks_ungated_before_stichtag`: Setup wie Test A, aber `active_from = Some(2099-01-01)` → Gate aus → Slot 14:00–16:00 bleibt ungeclippt → Block Mo `to=16:00`.
    - **Test D** `test_get_blocks_none_active_from_no_clip`: `toggle_service` returned `Ok(None)` → Legacy-Verhalten (keine Kürzung, ShortDay wird ignoriert im BE-Aggregat).
    - **Test E** `test_get_unsufficiently_booked_blocks_respects_cutoff`: unbookter Slot 16:00–18:00 mit ShortDay-Cutoff 15:00 + Gate aktiv → nicht in `insufficient_blocks`. Ohne ShortDay wäre der Slot drin (weil unbooked, `min_resources > 0`).
  </behavior>
  <action>
- `service_impl/src/test/block.rs`: neuen `#[tokio::test]`-Block für jeden der fünf Cases (A–E). Struktur folgt bestehenden Tests in dieser Datei.
- Mock-Erweiterung: existierende `build_dependencies()`-Fixture (falls vorhanden) um `special_day_service: MockSpecialDayService` und `toggle_service: MockToggleService` erweitern. Alle bestehenden Tests brauchen Default-Mock-Returns:
  - `special_day_service.expect_get_by_week().returning(|_, _, _| Ok(Arc::new([])))` (keine ShortDays, Legacy).
  - `toggle_service.expect_get_toggle_value().returning(|_, _, _| Ok(None))` (Legacy off).
- Neue Tests konfigurieren die Mocks entsprechend Case-Beschreibung.
- Fixtures: `year = 2026, week = 31` (Monday = 2026-07-27). `active_from = Some(Date::from_calendar_date(2026, Month::July, 1))` für "Gate aktiv"-Cases, `Some(Date::from_calendar_date(2099, Month::January, 1))` für "Gate aus"-Cases.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib test::block</automated>
  </verify>
  <done>
Fünf neue Tests grün. Alle bestehenden Tests weiter grün (mit den Default-Mock-Ergänzungen).
  </done>
</task>

<task type="auto">
  <name>Task 4: Workspace gates</name>
  <files>(gate only)</files>
  <action>
Nach den drei vorherigen Tasks Workspace-Gates fahren. Keine SQL-Änderung → kein
`sqlx prepare` nötig.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
Alle Workspace-Tests grün, Clippy grün. Snapshot-Version bleibt 12.
  </done>
</task>

</tasks>

<verification>
- Chain A' clippt in beiden Slot-Aggregat-Loops.
- `get_blocks_for_next_weeks_as_ical` (`:182-225`) und `get_blocks_for_current_user` (`:349-395`) delegieren an gefixte Methoden → SHC-02 fällt für iCal automatisch (D-51-05).
- `service/src/my_block.rs` ist dead code (Research §Risks 5) — nicht angefasst.
</verification>

<success_criteria>
1. `cargo test --workspace` grün.
2. `cargo clippy --workspace -- -D warnings` grün.
3. Merge-Reihenfolge visuell verifizierbar: Test A produziert Block mit `to=15:00` (nicht `to=16:00`) — beweist, dass Clip VOR Merge greift.
4. Snapshot-Version bleibt 12.
5. Kein neues `sqlx`-Prepare (keine neue Query).
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-04-SUMMARY.md` mit: Deps-Diff (SpecialDayService+ToggleService), Merge-Reihenfolge-Test-Belege, DI-Konstruktions-Änderung, ggf. `day_map`-Typ-Umbau in `get_unsufficiently_booked_blocks`.
</output>
