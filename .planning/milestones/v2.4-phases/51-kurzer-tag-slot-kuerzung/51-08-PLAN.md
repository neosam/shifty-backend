---
phase: 51
plan: 08
type: execute
wave: 4
depends_on: [51-02]
files_modified:
  - shifty-dioxus/src/page/settings.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [SHC-06]
must_haves:
  truths:
    - "SHC-06 FE-Anteil: Admin-Editor für `shortday_slot_clipping_active_from` in Settings-Seite, admin-gated"
    - "D-51-07 FE-Konsequenz: leeres Feld / Clear-Button → Legacy off; gesetzter ISO-8601-Date-Wert → gate aktiv ab diesem Datum"
    - "D-51-07 Muster HCFG-02: Editor lehnt sich exakt an die HCFG-02 Card-2-Implementierung an (`shifty-dioxus/src/page/settings.rs:905-966`); i18n-Keys analog `SettingsHolidayAutoCredit*`"
    - "Toggle-Endpoint `/toggle` bereits im Dioxus.toml-Proxy (`shifty-dioxus/Dioxus.toml:110`) — kein Proxy-Add nötig"
    - "Nur 3 neue i18n-Keys (Label, Description, UnsetHint) — Save/Clear-Keys werden von HCFG-02 re-used (Text ist generisch)"
    - "D-25-06 WASM-Datepicker-Caveat: strukturelle Test-Assertion (via pure fn) als Fallback; Programmatisches Setzen von `<input type=date>` triggert Dioxus-Signale nicht (Auto-Memory `reference_dioxus_browser_test_date_inputs`)"
  artifacts:
    - "`shifty-dioxus/src/page/settings.rs` — neue Card `Card 2b` (oder als Card 3 nach der bestehenden HCFG-02 Card 2) mit Label/Description/Datepicker/Save/Clear/UnsetHint"
    - "`shifty-dioxus/src/i18n/mod.rs` — drei neue `Key`-Enum-Varianten: `SettingsShortdayClippingLabel`, `SettingsShortdayClippingDescription`, `SettingsShortdayClippingUnsetHint`"
    - "`shifty-dioxus/src/i18n/de.rs, en.rs, cs.rs` — drei Übersetzungen pro Locale (9 add_text-Aufrufe total)"
    - "Optional: pure fn zur Validierung eines eingegebenen Datumsstrings (ISO-8601), unit-getestet — Fallback falls Browser-Test nicht cooperiert"
  key_links:
    - "Depends on P02: Toggle `shortday_slot_clipping_active_from` muss existieren, damit der Editor beim ersten Save auf einen existierenden Toggle-Row schreibt (`INSERT OR IGNORE` in P02 hat die Zeile bereits mit `value=NULL` angelegt; Editor macht `UPDATE toggle SET value = ? WHERE name = ?`)"
    - "HCFG-02 als Blueprint: Card 2 (Zeile 905-966 in `settings.rs`) hat alle Bausteine — kopieren, umbenennen, Toggle-Name austauschen"
---

<objective>
Admin-Editor für den Stichtag `shortday_slot_clipping_active_from` in der
Settings-Seite. Muster identisch zu HCFG-02 (Feiertags-Automatik-Stichtag) aus v1.7.
Drei neue i18n-Keys, Card in Settings-Seite, kein neuer API-Endpoint (Toggle-API
existiert bereits).

Purpose: SHC-06 FE-Anteil — Admin kann den Rollout-Stichtag setzen/löschen.

Output: Settings-Seite hat neue Card, drei i18n-Keys in allen drei Locales, kein
neuer Backend-Code (der Toggle wird über den existierenden `/toggle`-Endpoint
gespeichert).
</objective>

<execution_context>
Auto-commit via git (jj-import).
- FE-Workspace: `shifty-dioxus/` mit `dx serve --hot-reload` (port 8080) und
  Backend-Proxy auf 3000.
- WASM-Build-Gate: `cargo build --target wasm32-unknown-unknown` aus `shifty-dioxus/`.
- Dioxus-CLI-Pin `dx = 0.6.x` (Auto-Memory `project_frontend_dx_version_pin`); ändern
  wir nicht.
</execution_context>

<context>
- `shifty-dioxus/src/page/settings.rs:905-966` — HCFG-02 Card 2 mit Label + Description + Datepicker + Save + Clear + UnsetHint. **Blueprint.**
- `shifty-dioxus/src/page/settings.rs:870-903` — Card 1 (Paid-Limit-Toggle-Button-Zeile). Layout-Grundmuster.
- `shifty-dioxus/src/i18n/de.rs:1130-1155` — HCFG-02 i18n de. Muster für neue Keys.
- `shifty-dioxus/src/i18n/en.rs:1050-1073` — HCFG-02 i18n en. Analog.
- `shifty-dioxus/src/i18n/cs.rs` — HCFG-02 i18n cs (Line-Bereich analog analog; grep nach `SettingsHolidayAutoCreditLabel`).
- `shifty-dioxus/src/i18n/mod.rs:659-667` — HCFG-02 Key-Enum-Einträge. Muster.
- `shifty-dioxus/Dioxus.toml:110` — `/toggle`-Proxy schon da (aus v1.7 HCFG-02).
- CONTEXT.md D-51-07 (Semantik), REQUIREMENTS.md SHC-06 (Grenzfall inklusiv am Stichtag), Auto-Memory `reference_dioxus_browser_test_date_inputs` (Datepicker-Test-Fallback).
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n-Keys (mod.rs + drei Locales)</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <action>
Schritt 1 — `shifty-dioxus/src/i18n/mod.rs`: nach dem letzten `SettingsHolidayAutoCredit*`-Eintrag (Zeile ~667) drei neue Key-Enum-Varianten anlegen:
```
SettingsShortdayClippingLabel,
SettingsShortdayClippingDescription,
SettingsShortdayClippingUnsetHint,
```

Ebenfalls sicherstellen, dass sie im Test-Vektor am Ende der `mod.rs` (Zeile ~1591-1595 nutzt HCFG-02 als Präzedenz) mit registriert werden — analog. Grep nach `SettingsHolidayAutoCreditLabel` in `mod.rs` und die neuen Keys an den gleichen Stellen anhängen.

Schritt 2 — `de.rs` (Zeile 1130-1155 als Vorbild):
```rust
i18n.add_text(
    Locale::De,
    Key::SettingsShortdayClippingLabel,
    "Kurzer-Tag-Slot-Kürzung aktiv ab",
);
i18n.add_text(
    Locale::De,
    Key::SettingsShortdayClippingDescription,
    "Ab diesem Datum werden Slots an Kurzen Tagen am Cutoff gekürzt (Rendering und Ist-Stunden). Leer lassen = keine Kürzung.",
);
i18n.add_text(
    Locale::De,
    Key::SettingsShortdayClippingUnsetHint,
    "Nicht gesetzt — Kürzung inaktiv.",
);
```

Schritt 3 — `en.rs` (Zeile 1050-1073 als Vorbild):
```rust
i18n.add_text(
    Locale::En,
    Key::SettingsShortdayClippingLabel,
    "Short-day slot clipping active from",
);
i18n.add_text(
    Locale::En,
    Key::SettingsShortdayClippingDescription,
    "From this date, slots at short-day dates are clipped at the cutoff time (rendering and hours). Leave empty to disable.",
);
i18n.add_text(
    Locale::En,
    Key::SettingsShortdayClippingUnsetHint,
    "Not set — clipping disabled.",
);
```

Schritt 4 — `cs.rs`: Übersetzungen (Freetext — Executor darf sinngemäß übersetzen, Vorschlag:
- Label: "Zkracování slotů v krátkých dnech aktivní od"
- Description: "Od tohoto data se sloty v krátkých dnech zkracují na čas cutoff (zobrazení i hodiny). Prázdné = zkracování vypnuto."
- UnsetHint: "Nenastaveno — zkracování vypnuto."
).

**Save/Clear-Keys wiederverwenden:** `SettingsHolidayAutoCreditSave` und `SettingsHolidayAutoCreditClear` sind semantisch generisch ("Datum speichern" / "Löschen (deaktivieren)") und werden in der neuen Card re-used. Kein neuer Key nötig. (Alternative: eigene Keys anlegen wenn Executor findet, dass Reuse verwirrt — dann analog 6 statt 3 Keys pro Locale.)
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo check</automated>
  </verify>
  <done>
Drei neue Keys in allen drei Locales; `cargo check` grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings-Seite — neue Card</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <action>
- Kopiere die HCFG-02 Card 2 (Zeile 905-966 in `settings.rs`) und dupliziere sie **direkt darunter** als neue Card:
  - Label-Text: `Key::SettingsShortdayClippingLabel`.
  - Description-Text: `Key::SettingsShortdayClippingDescription`.
  - UnsetHint-Text: `Key::SettingsShortdayClippingUnsetHint`.
  - Save/Clear-Buttons: re-use `Key::SettingsHolidayAutoCreditSave` und `Key::SettingsHolidayAutoCreditClear` (semantisch generisch).
- Neue Signals in `settings.rs`-`Component`:
  - `sc_date_str: Signal<String>` — analog `date_str` für HCFG-02 (Zeile ~sicherstellen der genaue Name — grep `date_str` in `settings.rs`).
  - `sc_is_saving: Signal<bool>`.
  - `sc_save_result: Signal<Option<bool>>`.
  - `sc_loaded_empty: Signal<bool>`.
- API-Calls: den existierenden Toggle-`get_toggle_value` und `set_toggle_value` (oder wie es im FE heißt — grep in `shifty-dioxus/src/api.rs` nach `holiday_auto_credit`) analog für `"shortday_slot_clipping_active_from"` aufrufen. Der Executor sucht die genaue FE-API-Wrapper-Fn und passt den Toggle-Namen an.
- Admin-Gate: HCFG-02 Card ist bereits admin-gated (im Umgebungs-Signal `is_admin`/`is_toggle_admin` — grep an der Card-Aufhängung). Neue Card in den **selben** Admin-Gate-Zweig legen. Keine Sichtbarkeit ohne Admin-Privileg.
- Layout-Konsistenz: gleiche Tailwind-Klassen wie Card 2 (`bg-surface border border-border rounded-md p-4 flex flex-col gap-3 mt-4`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <done>
Neue Card in Settings-Seite; WASM-Build grün. Admin-gated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure fn tests (D-25-06 Fallback)</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <behavior>
    - Da `<input type=date>` in Dioxus schwer per Browser-Test zu triggern ist (Auto-Memory `reference_dioxus_browser_test_date_inputs`), verlassen wir uns auf pure Fn-Tests.
    - Neue Helper-Fn `fn is_valid_shortday_date_input(s: &str) -> bool` (pure): true für leerstring (Legacy) oder gültiges `YYYY-MM-DD`; false sonst.
    - **Test 1** `test_empty_shortday_input_is_valid`: `""` → true.
    - **Test 2** `test_valid_iso_date_is_accepted`: `"2026-08-01"` → true.
    - **Test 3** `test_malformed_date_is_rejected`: `"08.01.2026"`, `"tomorrow"`, `"2026-13-01"` → false.
    - **Test 4** `test_grenzfall_active_from_equals_booking_date` (dokumentativ; nutzt `shortday_gate::should_clip` aus dem `rest-types`-Kontext — oder eigene Kopie): booking_date == active_from → true (clip); booking_date == active_from - 1 → false. **Diese Verhaltenslogik lebt im Backend (P02); FE-Test hier ist NICHT redundant, sondern belegt, dass der FE-Editor genau denselben Grenzfall reproduziert wenn er das Datum speichert.**
  </behavior>
  <action>
- Neue Pure Fn im Settings-Modul oder in `shifty-dioxus/src/util.rs` (Executor wählt).
- Vier `#[cfg(test)] mod tests`-Cases anlegen.
- Test 4: nicht `shortday_gate` importieren (das lebt im Backend-`service_impl`) — stattdessen eine kleine `fn is_within_shortday_gate(booking_date, active_from) -> bool` FE-lokal duplizieren (zwei Zeilen); der Zweck ist dokumentativer Nature: der Test zeigt, dass FE und BE identische Semantik erwarten. **Alternative:** Diesen Test in ein `#[test]`-Modul im BE-Land packen (redundant zu P02) — Executor entscheidet nach Test-File-Layout.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo test</automated>
  </verify>
  <done>
Vier Tests grün. `is_valid_shortday_date_input` in Verwendung im Editor als optionaler Input-Guard (Save-Button disabled wenn `!is_valid_shortday_date_input(&date_str)`).
  </done>
</task>

<task type="auto">
  <name>Task 4: FE-Gates</name>
  <files>(gate only)</files>
  <action>
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` — WASM-Build grün.
- `cd shifty-dioxus && cargo test` — FE-Tests grün.
- `cd shifty-dioxus && cargo clippy -- -D warnings` — für unsere neuen Zeilen. Falls pre-existing Lints ausschlagen: nur unsere Zeilen fixen (siehe Auto-Memory `reference_dioxus_clippy_not_gated`).
- Backend nicht angefasst → Backend-Gates nicht Pflicht in diesem Plan, aber ein `cargo test --workspace` schadet nicht.
- **Kein Dioxus.toml-Proxy-Add nötig** (`/toggle` schon in Zeile 110).
- **Auto-Memory-Check `feedback_dioxus_proxy_for_new_backend_endpoints`:** manuell prüfen, dass wir keinen neuen BE-Endpoint eingezogen haben. Wir haben nicht — der Toggle-API ist bereits aus v1.7 vorhanden.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown &amp;&amp; cargo test &amp;&amp; cargo clippy -- -D warnings 2&gt;&amp;1 | (grep -E 'shortday|shifty-dioxus/src/page/settings.rs' &amp;&amp; false) || true</automated>
  </verify>
  <done>
WASM-Build + FE-Tests grün. Keine Clippy-Warnings in unseren neuen Zeilen.
  </done>
</task>

</tasks>

<verification>
- FE hat neue Card, admin-gated, mit ISO-8601-Datepicker + Save + Clear.
- Nur drei neue i18n-Keys pro Locale (Save/Clear von HCFG-02 re-used).
- Kein neuer BE-Endpoint, kein neuer Dioxus.toml-Eintrag.
- Pure fn `is_valid_shortday_date_input` als Fallback für WASM-Datepicker-Test-Caveat.
</verification>

<success_criteria>
1. `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` grün.
2. `cd shifty-dioxus && cargo test` grün.
3. Drei neue Key-Enum-Varianten in `i18n/mod.rs`; jeweils drei `add_text`-Aufrufe pro Locale.
4. Settings-Card struktur-identisch zu HCFG-02 Card 2.
5. Kein `Dioxus.toml`-Proxy-Change.
6. Snapshot-Version bleibt 12.
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-08-SUMMARY.md` mit: Card-Position in `settings.rs`, i18n-Keys + Übersetzungen, pure-fn-Test-Namen, Screenshot-Path falls angelegt.
</output>
