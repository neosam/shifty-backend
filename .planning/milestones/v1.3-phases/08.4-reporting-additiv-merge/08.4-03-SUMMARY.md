---
phase: 08.4-reporting-additiv-merge
plan: 03
subsystem: api
tags: [rust, reporting, absence-period, additive-merge, year-bounds, week-summary]

# Dependency graph
requires:
  - phase: 08.4-reporting-additiv-merge (Plan 02)
    provides: Additiver Merge in get_report_for_employee_range (Referenz-Implementierung)
  - phase: 08.2-cutover
    provides: soft_delete_bulk (per-row deleted IS NULL Invariante)
  - phase: 08.3
    provides: derive_hours_for_range Halbtag-Awareness
provides:
  - Additiver absence_period-Merge in get_reports_for_all_employees (Gap 1 CR-01 + IN-03)
  - Additiver absence_period-Merge in get_week (Gap 1 CR-01)
  - 4 neue Tests: alle_employees_additive_merge, get_week_additive_merge, same_day_overlap (WR-02), year_bounds (IN-03)
affects: [08.4-04, 08.5-read-projection]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additiver Display-Field-Merge: extra_hours.sum() + absence_derived_*_hours in allen drei report-produzierenden Methoden"
    - "Year-Bounds-Scoping: [first_day_in_year(year)..until_week-Sonntag] verhindert Woche-0/Overflow-Leak"

key-files:
  created:
    - .planning/phases/08.4-reporting-additiv-merge/08.4-03-SUMMARY.md
  modified:
    - service_impl/src/reporting.rs
    - service_impl/src/test/reporting_additive_merge.rs

key-decisions:
  - "get_reports_for_all_employees Year-Bounds: [first_day_in_year(year)..ShiftyWeek::new(year,until_week).as_date(Sunday)] schliessen Woche-0 (Vorjahr) und Jahresend-Overflow exakt aus (IN-03)."
  - "fixture_work_details_8h_mon_fri fuer Test A benoetigt: leere employee_work_details wuerden expected_hours <= 0.0 ausloesen -> vacation_hours=0.0 im extra_hours-Branch (Bug in Test-Setup, nicht im Produktionscode)."

requirements-completed: []

# Metrics
duration: ~25min
completed: 2026-06-09
---

# Phase 8.4 Plan 03: Schwester-Methoden-Additiv-Merge Summary

**`get_reports_for_all_employees` und `get_week` summieren Vacation/SickLeave/UnpaidLeave jetzt additiv aus lebenden `extra_hours` PLUS `derive_hours_for_range`; Year-Bounds-Scoping (IN-03) und Gleichtags-Overlap-Test (WR-02) abgedeckt; 8 Tests gruen.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-06-09T10:15Z
- **Completed:** 2026-06-09
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

### Task 1 — Additiver Merge in get_reports_for_all_employees (`48941c2e`)

- `reporting.rs`: Nach dem `weekly_hours`-Fold und vor `short_employee_report.push(...)` eingefuegt: `derive_hours_for_range`-Call mit Year-Bounds `[ShiftyDate::first_day_in_year(year).to_date() .. ShiftyWeek::new(year, until_week).as_date(Sunday).to_date()]`.
- Drei Akkumulatoren (`absence_derived_vacation_hours`, `absence_derived_sick_leave_hours`, `absence_derived_unpaid_leave_hours`) per `match resolved.category`.
- `short_employee_report.push(...)`: `vacation_hours`, `sick_leave_hours`, `unpaid_leave_hours` auf additive Form geaendert; alle anderen Felder (balance, expected, dynamic, holiday, unavailable, volunteer, custom_absence) unveraendert (Balance-Symmetrie kommt in Plan 08.4-04).
- `context.clone()` beibehalten (T-0843-01 mitigiert — kein Authentication::Full-Aufweichen).

### Task 2 — Additiver Merge in get_week (`321a1220`)

- `reporting.rs`: Nach `balance_hours`-Berechnung und vor `result.push(...)`: `derive_hours_for_range`-Call mit Wochen-Range `[ShiftyWeek::new(year, week).as_date(Monday).to_date() .. ShiftyWeek::new(year, week).as_date(Sunday).to_date()]`.
- Gleiche Drei-Akkumulator-Pattern wie in Task 1.
- `result.push(...)`: `vacation_hours`, `sick_leave_hours`, `unpaid_leave_hours` additiv; alle anderen Felder unveraendert.
- `context.clone()` beibehalten (T-0843-02 mitigiert).

### Task 3 — 4 neue Tests (`dda7ccf7`)

- **Test A (`test_all_employees_additive_merge`)**: 4h extra_hours + 8h absence_period = 12h vacation — `get_reports_for_all_employees` addiert beide Quellen.
- **Test B (`test_get_week_additive_merge`)**: 3h extra_hours + 8h absence_period = 11h sick_leave — `get_week` addiert beide Quellen.
- **Test C (`test_same_day_overlap_additive_no_dedup`)** (WR-02): 8h extra_hours + 8h absence_period am selben Tag (2024-06-03) = 16h additiv, kein Dedup (M-02).
- **Test D (`test_all_employees_year_bounds_no_leak`)** (IN-03): Mock assertiert `from == 2024-01-01` und `to <= 2024-12-31` — Woche-0/Overflow-Leak verhindert.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Arc-Typ-Annotation fehlte in Tests B und C**
- **Found during:** Task 3 (erster Build).
- **Issue:** `let extras = Arc::from(vec![...])` konnte `T` nicht inferieren ohne explizite Annotation.
- **Fix:** `let extras: Arc<[ExtraHours]> = Arc::from(vec![...])` — in beiden Tests.
- **Files modified:** `service_impl/src/test/reporting_additive_merge.rs`.
- **Commit:** `dda7ccf7` (Teil von Task 3).

**2. [Rule 1 - Bug] Test A assertions falsch: vacation_hours == 8.0 statt 12.0**
- **Found during:** Task 3 (erster Test-Lauf).
- **Issue:** Test A setzte `employee_work_details_service.expect_all()` auf leeren Slice. Bei leerem `employee_work_details` trifft `expected_hours <= 0.0` zu und der Code-Zweig setzt `vacation_hours: 0.0` — die extra_hours-Vacation-4h wurden gar nicht aggregiert. Nur die `absence_derived_vacation_hours` (8h) flossen in die Summe ein.
- **Fix:** `fixture_work_details_8h_mon_fri()` als Rueckgabe des `expect_all()`-Mocks gesetzt — damit ist `expected_hours > 0` fuer KW23/2024 und der `else`-Zweig liest vacation_hours korrekt aus `extra_hours_array`.
- **Files modified:** `service_impl/src/test/reporting_additive_merge.rs`.
- **Commit:** `dda7ccf7` (Teil von Task 3).

## Build-/Test-Status

- `cargo build -p service_impl`: **gruen, keine Warnings**.
- `cargo test -p service_impl reporting_additive`: **8 passed, 0 failed**.
- `cargo test --workspace`: **vollstaendig gruen** — alle Crates `test result: ok`, 0 Failures (service_impl 427 passed, shifty_bin 79 passed).

## Acceptance Criteria (verifiziert)

- `grep -c derive_hours_for_range service_impl/src/reporting.rs` == **3** (range-Pfad + all_employees + get_week) ✓
- `grep -n "ShiftyDate::first_day_in_year(year).to_date()" service_impl/src/reporting.rs` findet get_reports_for_all_employees Z. 385 ✓
- `grep -n "ShiftyWeek::new(year, week).as_date(DayOfWeek::Monday)" service_impl/src/reporting.rs` findet get_week Z. 796 ✓
- `grep -c "#[tokio::test]" reporting_additive_merge.rs` == **8** (4 bestehend + 4 neu) ✓
- `grep -c "get_reports_for_all_employees" reporting_additive_merge.rs` == **8** (>= 2) ✓
- `grep -c "get_week" reporting_additive_merge.rs` == **5** (>= 1) ✓
- `grep -n "16.0" reporting_additive_merge.rs` findet WR-02-Overlap-Assertion Z. 531 ✓
- `context.clone()` in beiden neuen derive_hours_for_range-Calls (T-0843-01/02 mitigiert) ✓

## Known Stubs

Keine.

## Threat Surface

Keine neue Threat-Surface ueber den Plan-`<threat_model>` hinaus:
- T-0843-01 (Elevation of Privilege): `context.clone()` beibehalten — kein Authentication::Full.
- T-0843-02 (Information Disclosure): `context.clone()` in get_week — keine Cross-Employee-Leakage.
- T-0843-03 (Tampering / IN-03): Year-Bounds-Scoping implementiert und durch Test D verifiziert.

## Task Commits

1. **Task 1: Additiver Merge in get_reports_for_all_employees** — `48941c2e` (feat).
2. **Task 2: Additiver Merge in get_week** — `321a1220` (feat).
3. **Task 3: 4 neue Tests** — `dda7ccf7` (test).

## Self-Check: PASSED

- `service_impl/src/reporting.rs` (2 neue derive_hours_for_range-Calls) — FOUND
- `service_impl/src/test/reporting_additive_merge.rs` (4 neue Tests) — FOUND
- Commit `48941c2e` (Task 1) — FOUND
- Commit `321a1220` (Task 2) — FOUND
- Commit `dda7ccf7` (Task 3) — FOUND

---
*Phase: 08.4-reporting-additiv-merge*
*Completed: 2026-06-09*
