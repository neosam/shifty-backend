---
phase: 08.4-reporting-additiv-merge
plan: 04
type: execute
wave: 2
depends_on: [08.4-03]
gap_closure: true
files_modified:
  - service_impl/src/reporting.rs
  - service_impl/src/billing_period_report.rs
  - service_impl/src/test/billing_period_snapshot_locking.rs
  - service_impl/src/test/reporting_additive_merge.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "absence_period-derived Vacation/SickLeave/UnpaidLeave-Stunden reduzieren expected_hours und bewegen balance_hours SYMMETRISCH zu extra_hours-Absence — in get_report_for_employee_range, get_reports_for_all_employees UND get_week."
    - "Eine reine absence_period-Buchung bewegt die Balance identisch zum aequivalenten extra_hours-Eintrag (Balance-Parity)."
    - "CURRENT_SNAPSHOT_SCHEMA_VERSION ist 6 (Bump 5→6, weil die Computation der persistierten value_types Balance + ExpectedHours geaendert wird)."
    - "Der Snapshot-Pin-Test erwartet 6."
  artifacts:
    - path: "service_impl/src/reporting.rs"
      provides: "Symmetrische Einspeisung der absence-derived Stunden in den expected/balance-Pfad aller drei Methoden"
      contains: "absence_derived"
    - path: "service_impl/src/billing_period_report.rs"
      provides: "CURRENT_SNAPSHOT_SCHEMA_VERSION = 6"
      contains: "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6"
    - path: "service_impl/src/test/billing_period_snapshot_locking.rs"
      provides: "Pin auf 6"
      contains: "CURRENT_SNAPSHOT_SCHEMA_VERSION, 6"
    - path: "service_impl/src/test/reporting_additive_merge.rs"
      provides: "Balance-Parity-Test"
      contains: "balance_hours"
  key_links:
    - from: "reporting.rs (alle 3 Methoden)"
      to: "expected_hours / balance_hours"
      via: "absence_derived_*_hours fliessen in den absense_hours-Reducer"
      pattern: "absence_derived"
    - from: "billing_period_report.rs Writer"
      to: "snapshot value_types Balance + ExpectedHours"
      via: "get_report_for_employee_range liefert geaenderte balance/expected → Bump 5→6"
      pattern: "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6"
---

<objective>
Gap 2 (VERIFICATION truth 2 / REVIEW WR-01): Die via `derive_hours_for_range` abgeleiteten
Absence-Stunden speisen bisher NUR die Display-Felder (vacation_hours/sick_leave_hours/
unpaid_leave_hours). Die Balance-Mathematik (`expected_hours`/`balance_hours`/`dynamic_hours`/
`vacation_days`/`absence_days`) wird NICHT korrigiert — waehrend extra_hours vom
`ReportType::AbsenceHours` `expected_hours` SCHON reduzieren (reporting.rs:982, 758, 368).
Diese Asymmetrie bedeutet: eine reine absence_period-Buchung zeigt vacation_hours, bewegt aber
die Balance nicht — oekonomisch identische Abwesenheiten ergeben unterschiedliche Balances
post-Cutover.

Dieser Plan speist die abgeleiteten Absence-Stunden SYMMETRISCH in den expected/balance-Pfad
aller drei Report-Methoden ein UND zieht den dadurch erzwungenen Snapshot-Schema-Bump 5→6.

Purpose: Balance-Neutralitaet bei Abwesenheit muss quellen-unabhaengig gelten (M-02/M-03:
per-row Koexistenz korrekt in JEDEM Zwischenzustand).

Output: reporting.rs speist absence-derived Stunden in den absense_hours/expected-Reducer aller
drei Methoden; CURRENT_SNAPSHOT_SCHEMA_VERSION 5→6; Pin-Test 6; Balance-Parity-Test; Full-Suite-Gate.

Optional gefaltet (Cleanup, low-risk): WR-03/IN-01/IN-02 — siehe Task 5. ENTSCHEIDUNG dieses Plans:
WR-03 (toter FeatureFlagService-Dep) + IN-01/IN-02 (stale Kommentare) sind als EINZELNER optionaler
Cleanup-Task (Task 5) INKLUDIERT — sie sind low-risk, klar abgegrenzt und schliessen die in der
VERIFICATION/REVIEW gemeldeten Anti-Patterns. Falls Task 5 den Build gefaehrdet, kann er ohne
Einfluss auf die beiden Gaps zurueckgestellt werden (die Gaps sind Task 1-4).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located git). GSD-Auto-Commit ist AUS (`commit_docs: false`).
NIEMALS `git commit` / `git add` ausfuehren. Der Snapshot-Bump (billing_period_report.rs) + die
Reporting-Computation-Aenderung gehoeren laut CLAUDE.md § "Billing Period Snapshot Schema
Versioning" in DENSELBEN Commit — falls committet wird, jj-nativ und gemeinsam. Keine git-Fallbacks.
</vcs_jj_only>

<context>
@.planning/phases/08.4-reporting-additiv-merge/08.4-CONTEXT.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-VERIFICATION.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-REVIEW.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-03-SUMMARY.md

<snapshot_versioning_decision>
BLOCKING-Entscheidung (mit Code-Evidenz, NICHT angenommen):

Der Billing-Period-Snapshot-Writer `build_and_persist_billing_period_report()` ruft
`get_report_for_employee_range` (billing_period_report.rs:85-118) und persistiert daraus u.a.
diese value_types (billing_period_report.rs):
- `BillingPeriodValueType::Balance` ← report.balance_hours (Z.139-144)
- `BillingPeriodValueType::ExpectedHours` ← report.expected_hours (Z.148-153)
- `BillingPeriodValueType::VacationDays` ← report.vacation_days (Z.205)
- (sowie VacationHours/SickLeave/UnpaidLeave — die wurden bereits in Plan 02 fuer 4→5 erfasst)

Gap 2 AENDERT die Computation von `balance_hours`, `expected_hours` UND `vacation_days`/
`absence_days` in `get_report_for_employee_range` (indem absence-derived Stunden den
expected/balance-Pfad reduzieren). Damit aendert sich die Computation persistierter value_types
(Balance, ExpectedHours, VacationDays). Ein Validator, der die Live-Computation re-runned und gegen
einen v5-Snapshot diffed, wuerde Drift sehen, die in Wahrheit der Modelaenderung entstammt.

→ ZWINGEND: CURRENT_SNAPSHOT_SCHEMA_VERSION 5→6 + Pin-Test 5→6, im selben Commit wie die
  reporting.rs-Computation-Aenderung. Das ist KEIN stilles Auslassen — es ist die in CLAUDE.md
  geforderte Pflicht-Bump-Bedingung "Change the computation that produces an existing value_type".
</snapshot_versioning_decision>

<interfaces>
Symmetrie-Mechanik (EXAKT spiegeln, was extra_hours-Absence bereits tut):

`AbsenceCategory::{Vacation, SickLeave, UnpaidLeave}` entsprechen ExtraHoursCategory-Werten, deren
`as_report_type()` == `ReportType::AbsenceHours` ist (service/src/extra_hours.rs:55-58). Genau
diese AbsenceHours reduzieren expected_hours. Also: ALLE drei derived-Kategorien (V+S+U) zaehlen
in die absence-Reduktion — symmetrisch zu extra_hours.

Pro Methode der Reduktions-Punkt:
- get_reports_for_all_employees: `expected_hours = weekly_hours.planned_hours - weekly_hours.absense_hours` (reporting.rs:368); `dynamic_hours = weekly_hours.dynamic_hours - weekly_hours.absense_hours` (Z.369); `balance_hours = overall_hours - expected_hours + carryover` (Z.371). `absense_hours` kommt nur aus extra_hours AbsenceHours (Z.285-289).
- get_week: `expected_hours = planned_hours - abense_hours` (reporting.rs:758); `dynamic_hours = dynamic_hours - abense_hours` (Z.762); `balance_hours = overall_hours - expected_hours` (Z.764). `abense_hours` nur aus extra_hours Availability::Unavailable (Z.676-683).
- get_report_for_employee_range: expected/balance kommen aus `by_week` (hours_per_week), wo `absence_hours` (reporting.rs:915-923) nur aus extra_hours AbsenceHours stammt und in `expected_hours`/`balance` einfliesst (Z.982/985). by_week sieht die derived-Stunden NICHT.

WICHTIG Cap-Interaktion (get_report_for_employee_range / get_week): Die absence-Reduktion ist
in beiden bereits in `expected_hours` (vor apply_weekly_cap bei get_week) bzw. in hours_per_week
verankert. Spiegele die EXISTIERENDE Reihenfolge — addiere die derived absence in denselben
`absense_hours`-Summanden, NICHT an einer neuen Stelle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symmetrische absence-Reduktion in get_reports_for_all_employees + get_week</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:116-393 (get_reports_for_all_employees; absense_hours Z.285-289; expected_hours Z.368; dynamic_hours Z.369; balance_hours Z.371; der in Plan 03 eingefuegte absence_derived_*-Block)
    - service_impl/src/reporting.rs:629-786 (get_week; abense_hours Z.676-683; expected_hours Z.758; dynamic_hours Z.762; balance_hours Z.764; der in Plan 03 eingefuegte absence_derived_*-Block)
    - service/src/extra_hours.rs:52-58 (as_report_type — Vacation/SickLeave/UnpaidLeave == AbsenceHours; Beleg fuer Symmetrie)
  </read_first>
  <action>
Plan 03 hat in beiden Methoden bereits die Akkumulatoren `absence_derived_vacation_hours`,
`absence_derived_sick_leave_hours`, `absence_derived_unpaid_leave_hours` eingefuehrt. Bilde in
beiden Methoden die Summe der drei (alle sind AbsenceHours → reduzieren expected symmetrisch):

```rust
let absence_derived_total =
    absence_derived_vacation_hours + absence_derived_sick_leave_hours + absence_derived_unpaid_leave_hours;
```

**get_reports_for_all_employees** — aendere die Balance-Berechnung (reporting.rs:368-371) so, dass
`absence_derived_total` zusaetzlich zu `weekly_hours.absense_hours` reduziert:
```rust
let expected_hours = weekly_hours.planned_hours - weekly_hours.absense_hours - absence_derived_total;
let dynamic_hours = weekly_hours.dynamic_hours - weekly_hours.absense_hours - absence_derived_total;
let overall_hours = weekly_hours.shiftplan_hours + weekly_hours.extra_working_hours;
let balance_hours = overall_hours - expected_hours + previous_year_carryover;
```
(overall_hours + carryover bleiben unveraendert; nur expected/dynamic verlieren die derived absence,
balance folgt automatisch.) Platziere die `absence_derived_total`-Bindung VOR Z.368, also nach dem
in Plan 03 eingefuegten derived-Akkumulator-Block.

**get_week** — aendere die Reduktion (reporting.rs:758) so, dass die derived absence VOR
apply_weekly_cap mit reduziert wird (gleiche Stelle wie abense_hours):
```rust
let expected_hours = planned_hours - abense_hours - absence_derived_total;
```
Die nachfolgenden Zeilen (apply_weekly_cap mit expected_hours, dynamic_hours = dynamic_hours - abense_hours,
balance_hours = overall_hours - expected_hours) konsumieren das neue expected_hours automatisch.
Zusaetzlich `dynamic_hours` symmetrisch reduzieren (reporting.rs:762):
```rust
let dynamic_hours = dynamic_hours - abense_hours - absence_derived_total;
```
Platziere die `absence_derived_total`-Bindung in get_week nach dem Plan-03-Akkumulator-Block,
VOR Z.758.

Keine neuen Imports noetig.
  </action>
  <acceptance_criteria>
    - `grep -c "absence_derived_total" service_impl/src/reporting.rs` >= 4 (2 Bindungen + Verwendungen in beiden Methoden).
    - `grep -n "planned_hours - weekly_hours.absense_hours - absence_derived_total" service_impl/src/reporting.rs` findet die all_employees-Reduktion.
    - `grep -n "planned_hours - abense_hours - absence_derived_total" service_impl/src/reporting.rs` findet die get_week-Reduktion.
    - `cargo build -p service_impl` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "planned_hours - abense_hours - absence_derived_total" service_impl/src/reporting.rs && cargo build -p service_impl</automated>
  </verify>
  <done>get_reports_for_all_employees + get_week reduzieren expected_hours/dynamic_hours/balance_hours symmetrisch um die absence-derived Stunden; compiliert.</done>
</task>

<task type="auto">
  <name>Task 2: Symmetrische absence-Reduktion in get_report_for_employee_range (via hours_per_week)</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:423-627 (get_report_for_employee_range; derived-Block Z.477-496; by_week-Call Z.518-524; planned_hours/dynamic_hours/balance aus by_week Z.539-540, 577-580)
    - service_impl/src/reporting.rs:847-1025 (hours_per_week; absence_hours Z.915-923; expected_hours Z.952-956; weeks.push mit expected_hours Z.982 / balance Z.985 / dynamic_hours Z.983)
    - service/src/absence.rs:155-157 (ResolvedAbsence) — BTreeMap<Date, ResolvedAbsence> ist das derive-Ergebnis
  </read_first>
  <action>
In `get_report_for_employee_range` liegt die Balance-Mathematik in `hours_per_week` (by_week), das
die derived-Stunden bisher nicht sieht. Reiche die bereits in Plan 03 (range-Pfad) berechnete
`derived`-Map (BTreeMap<Date, ResolvedAbsence>, reporting.rs:477-486) in `hours_per_week` durch und
reduziere dort die wochenweise `absence_hours` um die derived AbsenceHours dieser Woche.

Schritt A — Signatur von `hours_per_week` (reporting.rs:847) um einen Parameter erweitern:
```rust
fn hours_per_week(
    shiftplan_hours_list: &Arc<[ShiftplanReportDay]>,
    extra_hours_list: &Arc<[ExtraHours]>,
    working_hours: &[EmployeeWorkDetails],
    derived_absence: &std::collections::BTreeMap<time::Date, service::absence::ResolvedAbsence>,
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> Result<Arc<[GroupedReportHours]>, ServiceError> {
```

Schritt B — in der Wochen-Schleife (nach `let absence_hours = ...` Z.915-923) die derived absence
dieser Woche summieren (alle drei Kategorien sind AbsenceHours → reduzieren expected symmetrisch),
ABER nur wenn `working_hours_for_week > 0.0` (gleiche Bedingung wie der bestehende absence_hours,
Z.915 — bei 0 Vertragsstunden keine Reduktion):
```rust
let derived_absence_hours = if working_hours_for_week <= 0.0 {
    0.0f32
} else {
    derived_absence
        .iter()
        .filter(|(d, _)| d.as_shifty_week() == week)
        .map(|(_, r)| r.hours)
        .sum::<f32>()
};
let absence_hours = absence_hours + derived_absence_hours;
```
(Das `let absence_hours = absence_hours + ...`-Shadowing addiert die derived auf die extra_hours-
Absence; alle nachfolgenden Nutzungen von `absence_hours` in `weeks.push` Z.982/983/985 — expected_hours,
dynamic_hours, balance — konsumieren die Summe automatisch.) `ShiftyDate`/Date hat `.as_shifty_week()`
(genutzt Z.862). Falls `d` ein `time::Date` ist und kein `.as_shifty_week()` hat, wandle via
`ShiftyDate::from(*d)` bzw. nutze die im File bereits verwendete Konvertierung (siehe Z.862
`eh.to_date().as_shifty_week()` als Vorlage — pruefe die genaue ShiftyDate-Konstruktion aus time::Date).

Schritt C — den `derived`-Wert in den Call durchreichen (reporting.rs:518):
```rust
let by_week = hours_per_week(
    &shiftplan_report,
    &extra_hours,
    &working_hours,
    &derived,
    from_date,
    to_date,
)?;
```

Schritt D — die bestehenden Unit-Tests von `hours_per_week` (reporting.rs Module
`test_dynamic_vacation_days` ab Z.1027 + `test_weekly_planned_hours_cap`) rufen die freie Funktion
`hours_per_week` mit 5 Argumenten auf. Es gibt GENAU 13 solche Test-Call-Sites (zusaetzlich zum
1 Produktions-Call in get_report_for_employee_range, der `&derived` durchreicht). Finde sie mit:
`grep -nE "[^.]hours_per_week\\(" service_impl/src/reporting.rs | grep -v "fn hours_per_week"`
(das `[^.]` schliesst die GroupedReportHours-Methode `.hours_per_week()` aus — die NICHT anfassen).
Aktualisiere ALLE 13 Test-Call-Sites auf 6 Argumente, indem du eine leere Map an der neuen
4. Position (zwischen `&[work_details]`/`&[wd]` und `from`) einschiebst:
`&std::collections::BTreeMap::new()`. Beispiel vorher/nachher:
```rust
// vorher: hours_per_week(&shiftplan, &extra_hours, &[work_details], from, to)
// nachher: hours_per_week(&shiftplan, &extra_hours, &[work_details], &std::collections::BTreeMap::new(), from, to)
```

Hinweis Display-vs-Balance-Konsistenz: Die in Plan 03 gesetzten Display-Felder
(vacation_hours = extra_hours + absence_derived_*) bleiben unveraendert; dieser Task fuegt NUR die
Balance-Reduktion hinzu. vacation_days/absence_days (aus by_week, Z.527-537) profitieren NICHT
automatisch von der derived-Reduktion, da sie aus week.vacation_hours/etc. (nur extra_hours)
berechnet werden — das ist akzeptabel und konsistent mit dem Scope (WR-01 nennt explizit
expected/balance; vacation_days-Symmetrie waere ein separates Item). Dokumentiere das im SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -n "derived_absence:" service_impl/src/reporting.rs` findet den neuen hours_per_week-Parameter.
    - `grep -c "derived_absence_hours" service_impl/src/reporting.rs` >= 1.
    - `grep -c "BTreeMap::new()" service_impl/src/reporting.rs` == 13 (alle aktualisierten Test-Call-Sites; production-Call nutzt `&derived`, nicht `BTreeMap::new()`).
    - `cargo build -p service_impl` exit 0.
    - `cargo test -p service_impl test_dynamic_vacation_days` exit 0 (bestehende hours_per_week-Tests gruen nach Signatur-Update).
    - `cargo test -p service_impl test_weekly_planned_hours_cap` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p service_impl test_dynamic_vacation_days && cargo test -p service_impl test_weekly_planned_hours_cap</automated>
  </verify>
  <done>hours_per_week akzeptiert die derived-Map und reduziert expected/balance/dynamic pro Woche symmetrisch; alle bestehenden hours_per_week-Tests kompilieren + gruen.</done>
</task>

<task type="auto">
  <name>Task 3: Snapshot-Schema-Version-Bump 5→6 + Pin-Test-Update</name>
  <files>service_impl/src/billing_period_report.rs, service_impl/src/test/billing_period_snapshot_locking.rs</files>
  <read_first>
    - service_impl/src/billing_period_report.rs:31-56 (Version-Const + History-Doc-Kommentar)
    - service_impl/src/billing_period_report.rs:139-153,205 (persistierte value_types Balance/ExpectedHours/VacationDays die get_report_for_employee_range konsumieren — Gap-2-betroffen)
    - service_impl/src/test/billing_period_snapshot_locking.rs:1-40 (Pin-Test test_snapshot_schema_version_pinned, aktuell assert_eq! == 5)
  </read_first>
  <action>
In billing_period_report.rs:56 die Konstante bumpen:
```rust
pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6;
```
Ergaenze den History-Doc-Block (vor Z.56) um einen v6-Eintrag:
```rust
/// - v6: Phase 8.4 (Gap 2 / WR-01) — absence_period-derived Vacation/SickLeave/
///   UnpaidLeave reduzieren jetzt SYMMETRISCH zu extra_hours-Absence die
///   `expected_hours`/`balance_hours` (und damit die persistierten value_types
///   Balance + ExpectedHours). v5-Snapshots haben die absence_period-Seite nur
///   in den Display-Stunden, nicht in der Balance gezaehlt — ein Validator kann
///   v5-Balance/ExpectedHours nicht gegen die neue Computation re-validieren.
```

In billing_period_snapshot_locking.rs den Pin auf 6 setzen:
```rust
assert_eq!(
    CURRENT_SNAPSHOT_SCHEMA_VERSION, 6,
    "CURRENT_SNAPSHOT_SCHEMA_VERSION muss 6 sein nach Phase-8.4-Gap2-Bump \
     (absence-derived Stunden bewegen jetzt balance/expected symmetrisch)"
);
```
Aktualisiere auch den Doc-Kommentar im Locking-Test (Z.7: "erwartet 5" → "erwartet 6").
  </action>
  <acceptance_criteria>
    - `grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6" service_impl/src/billing_period_report.rs` == 1.
    - `grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 5" service_impl/src/billing_period_report.rs` == 0.
    - `grep -n "CURRENT_SNAPSHOT_SCHEMA_VERSION, 6" service_impl/src/test/billing_period_snapshot_locking.rs` findet den Pin.
    - `cargo test -p service_impl snapshot_schema_version_pinned` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6" service_impl/src/billing_period_report.rs && cargo test -p service_impl snapshot_schema_version_pinned</automated>
  </verify>
  <done>Version ist 6, History-Doc um v6 ergaenzt, Pin-Test erwartet 6 und ist gruen.</done>
</task>

<task type="auto">
  <name>Task 4: Balance-Parity-Test (reine absence_period bewegt Balance wie extra_hours)</name>
  <files>service_impl/src/test/reporting_additive_merge.rs</files>
  <read_first>
    - service_impl/src/test/reporting_additive_merge.rs (Mock-Pattern, setup_common_mocks, run_report, make_extra_hours; die in Plan 03 hinzugefuegten Tests)
    - service_impl/src/test/reporting_phase2_fixtures.rs:44-... (fixture_work_details_8h_mon_fri — 8h/Tag Mo-Fr KW22-25/2024, deckt KW23 = 2024-06-03..09)
    - service_impl/src/reporting.rs:423-627 (get_report_for_employee_range — balance_hours/expected_hours Felder)
  </read_first>
  <action>
Fuege einen `#[tokio::test] async fn test_balance_parity_absence_period_vs_extra_hours()` in
reporting_additive_merge.rs hinzu. Er belegt: ein reiner absence_period-Vacation-Eintrag bewegt
balance_hours/expected_hours IDENTISCH zum aequivalenten extra_hours-Vacation-Eintrag.

Da das Balance-Movement Vertragsstunden braucht, muessen beide Laeufe `fixture_work_details_8h_mon_fri()`
nutzen (nicht das leere setup_common_mocks-WorkDetails). Baue daher ein eigenes Mock-Setup (nicht
setup_common_mocks) mit:
- permission/verify_user_is_sales_person/get → Ok wie gehabt
- employee_work_details_service.expect_find_by_sales_person_id().returning(|_,_,_| Ok(Arc::from(vec![fixture_work_details_8h_mon_fri()])))
- shiftplan_report_service.expect_extract_shiftplan_report().returning(|_,_,_,_,_| Ok(Arc::from(vec![]))) (kein shiftplan → overall_hours aus extra_work = 0)
- carryover None, transaction passthrough

Lauf 1 (extra_hours-Vacation): derive_hours_for_range → leere Map; find_by_sales_person_id_and_year_range
→ [Vacation 8h am 2024-06-03 (Mo, KW23)]. run_report → report_extra.
Lauf 2 (absence_period-Vacation): derive_hours_for_range → {2024-06-03: Vacation 8h};
find_by_sales_person_id_and_year_range → []. run_report → report_absence.

Assertions:
```rust
assert!((report_extra.expected_hours - report_absence.expected_hours).abs() < 0.01,
    "expected_hours muss quellen-unabhaengig identisch sein: extra={} absence={}",
    report_extra.expected_hours, report_absence.expected_hours);
assert!((report_extra.balance_hours - report_absence.balance_hours).abs() < 0.01,
    "balance_hours muss quellen-unabhaengig identisch sein: extra={} absence={}",
    report_extra.balance_hours, report_absence.balance_hours);
// Beide muessen die Vacation auch tatsaechlich in der Balance verbucht haben (Sanity):
assert!(report_absence.expected_hours < 40.0,
    "absence_period-Vacation MUSS expected_hours unter die vollen 40h druecken (8h reduziert), got {}",
    report_absence.expected_hours);
```
Importiere `fixture_work_details_8h_mon_fri` aus `crate::test::reporting_phase2_fixtures`.
Faktorisiere das gemeinsame Mock-Setup in eine lokale Helper-Closure/-fn falls hilfreich (optional).
  </action>
  <acceptance_criteria>
    - `grep -c "test_balance_parity" service_impl/src/test/reporting_additive_merge.rs` == 1.
    - `grep -c "balance_hours" service_impl/src/test/reporting_additive_merge.rs` >= 1.
    - `cargo test -p service_impl test_balance_parity_absence_period_vs_extra_hours` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p service_impl test_balance_parity_absence_period_vs_extra_hours</automated>
  </verify>
  <done>Parity-Test gruen: reine absence_period-Buchung bewegt balance_hours + expected_hours identisch zum extra_hours-Aequivalent, und reduziert expected_hours nachweislich.</done>
</task>

<task type="auto">
  <name>Task 5 (optional Cleanup): WR-03 toter FeatureFlagService-Dep + IN-01/IN-02 stale Kommentare</name>
  <files>service_impl/src/extra_hours.rs, shifty_bin/src/main.rs, service_impl/src/billing_period_report.rs</files>
  <read_first>
    - service_impl/src/extra_hours.rs:14,30 (FeatureFlagService-Import + gen_service_impl!-Dep-Zeile)
    - shifty_bin/src/main.rs:288-295,853-875 (ExtraHoursServiceDependencies type-alias + Konstruktor-Wiring + Konstruktions-Reihenfolge-Kommentar)
    - service_impl/src/billing_period_report.rs:183-185 (stale Flag-Kommentar IN-01)
    - service_impl/src/test/ (etwaige extra_hours-Unit-Tests die den FeatureFlagService-Mock verdrahten)
  </read_first>
  <action>
NUR ausfuehren, wenn er den Build nicht gefaehrdet. Andernfalls zuruckstellen + im SUMMARY als
deferred markieren.

WR-03: Entferne den toten `FeatureFlagService`-Dep aus `ExtraHoursServiceImpl`:
- extra_hours.rs:30 — die `FeatureFlagService: ... = feature_flag_service,`-Zeile aus dem
  `gen_service_impl!`-Block loeschen.
- extra_hours.rs:14 — den ungenutzten `FeatureFlagService`-Import loeschen.
- main.rs:292 — `type FeatureFlagService = FeatureFlagService;` aus `ExtraHoursServiceDependencies`
  entfernen.
- main.rs:871 — `feature_flag_service: feature_flag_service.clone(),` aus dem
  ExtraHoursServiceImpl-Konstruktor entfernen.
- main.rs:853-858 — den Konstruktions-Reihenfolge-Kommentar ("FeatureFlagService MUST be
  constructed BEFORE ExtraHoursService ... flag-gate") entfernen/umschreiben, da das Gate weg ist.
- Etwaige extra_hours-Unit-Tests, die den FeatureFlagService-Mock verdrahten, entsprechend
  entschlacken (grep `feature_flag` im extra_hours-Test-Umfeld).

IN-01: billing_period_report.rs:183-185 — den stale Flag-Kommentar ("Bei Flag=off ... bei Flag=on
...") ersetzen durch: "Ab Phase 8.4 additiv: lebende extra_hours(UnpaidLeave) PLUS
derive_hours_for_range-abgeleitete UnpaidLeave-Stunden, summiert. Kein Flag-Branch mehr."

IN-02: main.rs:288-295 — stale Flag-Gate-Kommentare zum POST /extra-hours entfernen/anpassen.

Nach den Aenderungen: `cargo check --workspace` MUSS exit 0 liefern (gen_service_impl! materialisiert
den Dep als Feld — Entfernung muss konsistent ueber Trait-Deps + Konstruktor erfolgen).
  </action>
  <acceptance_criteria>
    - `grep -c "FeatureFlagService" service_impl/src/extra_hours.rs` == 0.
    - `grep -c "feature_flag_service" shifty_bin/src/main.rs` zeigt keine ExtraHoursServiceImpl-Wiring-Referenz mehr (verbleibende Treffer nur fuer andere, legitime Konsumenten von FeatureFlagService).
    - `grep -c "Bei Flag=off" service_impl/src/billing_period_report.rs` == 0.
    - `cargo check --workspace` exit 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo check --workspace</automated>
  </verify>
  <done>Toter FeatureFlagService-Dep aus ExtraHoursServiceImpl entfernt, DI-Wiring + Reihenfolge-Kommentar bereinigt, stale Flag-Kommentare aktualisiert; Workspace compiliert. (ODER: im SUMMARY als deferred dokumentiert, falls Build-Risiko.)</done>
</task>

<task type="auto">
  <name>Task 6: Full-Suite-Gate</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - .planning/phases/08.4-reporting-additiv-merge/08.4-VERIFICATION.md (SC-4: cargo test --workspace muss gruen sein, Billing-Period-Snapshot-Regression sauber)
  </read_first>
  <action>
Fuehre die volle Test-Suite als Abschluss-Gate aus. Falls Tools (cargo) nicht im PATH sind, ueber
`nix develop` ausfuehren (CLAUDE.local.md). KEINE destruktiven DB-Operationen (kein `sqlx database
reset`) ohne User-Confirmation — die Suite laeuft gegen in-memory SQLite und braucht keinen Reset.
Behebe etwaige durch Gap-2 verursachte Regressionen (insb. billing_period-Snapshot-Tests, die jetzt
v6-Computation gegen v6-Pin laufen lassen). Keine neue Logik — nur Gruen-Ziehen der bestehenden Suite.
  </action>
  <acceptance_criteria>
    - `cargo test --workspace` exit 0 (0 compile-errors, 0 failures).
    - `cargo test -p service_impl reporting_additive` exit 0.
    - `cargo test -p service_impl snapshot_schema_version_pinned` exit 0.
    - `grep -c derive_hours_for_range service_impl/src/reporting.rs` == 3 (Invariante aus Plan 03 weiterhin gehalten).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>cargo test --workspace gruen; alle additiven + Snapshot- + Parity-Tests bestehen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST-Caller → ReportingService | HR/self-gepruefte Reporting-Reads |
| ReportingService → AbsenceService.derive_hours_for_range | interner Call mit Authentication-Kontext |
| ReportingService.get_report_for_employee_range → BillingPeriodReport-Snapshot | persistierte balance/expected-Werte (write-once) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0844-01 | Tampering | Snapshot value_types Balance/ExpectedHours | mitigate | Gap-2 aendert die Computation persistierter value_types → CURRENT_SNAPSHOT_SCHEMA_VERSION 5→6 (Task 3) verhindert, dass ein Validator Modelldrift mit echtem Daten-Bug verwechselt. Pin-Test erzwingt 6. |
| T-0844-02 | Elevation of Privilege | hours_per_week derived-Durchreichung | accept | hours_per_week ist eine reine Funktion ohne eigene Auth; die derived-Map wurde bereits in get_report_for_employee_range mit dem HR/self-geprueften `context` erzeugt (reporting.rs:483). Kein neuer Auth-Pfad. |
| T-0844-03 | Information Disclosure | Authentication-Konsistenz | mitigate | Keine derive_hours_for_range-Aufrufe in diesem Plan aufgeweicht; die in Plan 03 etablierte `context.clone()`-Nutzung bleibt. RBAC-Check unveraendert. |

Kein `high`-Threat. Snapshot-Versionierung ist der einzige sicherheits-/korrektheitsrelevante Pflicht-Punkt — durch Task 3 abgedeckt.
</threat_model>

<verification>
- `cargo test --workspace` exit 0.
- `grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6"` == 1; Pin-Test erwartet 6.
- Balance-Parity-Test gruen (absence_period == extra_hours Balance-Movement).
- expected_hours/balance_hours in allen drei Methoden um die absence-derived Stunden reduziert.
- `grep -c derive_hours_for_range service_impl/src/reporting.rs` == 3 (Plan-03-Invariante gehalten).
</verification>

<success_criteria>
- absence_period-derived Stunden bewegen balance/expected symmetrisch zu extra_hours-Absence in get_report_for_employee_range, get_reports_for_all_employees, get_week.
- Balance-Parity-Test belegt Quellen-Unabhaengigkeit.
- CURRENT_SNAPSHOT_SCHEMA_VERSION == 6, Pin-Test == 6, beide gruen.
- cargo test --workspace gruen.
- WR-03/IN-01/IN-02 bereinigt ODER explizit deferred dokumentiert.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.4-reporting-additiv-merge/08.4-04-SUMMARY.md` erstellen.
Dokumentiere darin: (a) den Snapshot-Bump-Entscheid 5→6 mit value_type-Evidenz, (b) ob Task 5
(WR-03/IN-01/IN-02) inkludiert oder deferred wurde, (c) den vacation_days/absence_days-Scope-Hinweis
aus Task 2 (Display-Days nicht symmetrisch — bewusst, da WR-01 nur expected/balance nennt).
</output>
