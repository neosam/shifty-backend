# Phase 8.4: Reporting-Additiv-Merge + Deprecation-Rückbau — Pattern Map

**Erstellt:** 2026-06-09
**Dateien analysiert:** 9 (3 modifiziert, 6 Tests)
**Analoga gefunden:** 9 / 9

---

## Datei-Klassifizierung

| Neue/Modifizierte Datei | Rolle | Data Flow | Nächstes Analog | Match-Qualität |
|-------------------------|-------|-----------|-----------------|----------------|
| `service_impl/src/reporting.rs` (Z. 472–682) | service (business-logic) | transform / request-response | `service_impl/src/reporting.rs` selbst (vorhandener `use_absence_range_source`-Block) | exact |
| `service_impl/src/extra_hours.rs` (Z. 197–225) | service (basic) | request-response | `service_impl/src/extra_hours.rs` (dasselbe Gate) | exact |
| `service_impl/src/billing_period_report.rs` (Z. 50) | service (business-logic) | config/const | `billing_period_report.rs` (v3→v4-Bump) | exact |
| `service_impl/src/test/reporting_additive_merge.rs` (NEU) | test | unit (mockall) | `service_impl/src/test/reporting_flag_on_integration.rs` | role-match |
| `service_impl/src/test/reporting_flag_on_integration.rs` (Umbau) | test | unit (mockall) | sich selbst | exact |
| `service_impl/src/test/reporting_flag_off_bit_identity.rs` (LÖSCHEN) | test | unit (mockall) | — | entfällt |
| `service_impl/src/test/billing_period_snapshot_locking.rs` (Z. 27–35) | test | unit (const-pin) | sich selbst (v4-Version) | exact |
| `service_impl/src/test/extra_hours.rs` (Löschung Test 2) | test | unit (mockall) | sich selbst (Tests 1 + 3) | exact |
| `shifty_bin/src/integration_test/cutover.rs` (Löschung) | test | integration | sich selbst | exact |

---

## Pattern-Zuweisungen

---

### `service_impl/src/reporting.rs` — Additiver Merge (Modifikation)

**Analog:** dieselbe Datei, bestehender `use_absence_range_source`-Block

**Zu entfernender Block — Import-Abhängigkeit prüfen** (Z. 472–482):
```rust
// WIRD GELÖSCHT. Vor dem Entfernen prüfen (Fallstrick 1 aus RESEARCH.md):
//   grep -n "feature_flag_service" service_impl/src/reporting.rs
// Wenn 0 weitere Treffer → Dep aus gen_service_impl! + main.rs entfernen.
let use_absence_range_source = self
    .feature_flag_service
    .is_enabled(
        "absence_range_source_active",
        Authentication::Full,
        tx.clone(),
    )
    .await?;
```

**Zu entfernender Block — ExtraHours-Filter** (Z. 489–505):
```rust
// WIRD GELÖSCHT (kein Filter mehr, lebende extra_hours fließen ungefiltert).
let extra_hours: Arc<[ExtraHours]> = if use_absence_range_source {
    extra_hours
        .iter()
        .filter(|eh| {
            !matches!(
                eh.category,
                ExtraHoursCategory::Vacation
                    | ExtraHoursCategory::SickLeave
                    | ExtraHoursCategory::UnpaidLeave
            )
        })
        .cloned()
        .collect::<Vec<_>>()
        .into()
} else {
    extra_hours
};
```

**Zielzustand — Unbedingter `derive_hours_for_range`-Call** (ersetzt Z. 513–541):
```rust
// Additiver Merge (Phase 8.4 / D-01): immer beide Quellen.
// Konvertierte extra_hours sind via soft_delete_bulk (Phase 8.2) soft-deleted
// und fallen per `WHERE deleted IS NULL` im DAO-Load bereits heraus (D-02).
let derived = self
    .absence_service
    .derive_hours_for_range(
        from_date.to_date(),
        to_date.to_date(),
        *sales_person_id,
        context.clone(),
        tx.clone(),
    )
    .await?;
let mut absence_derived_vacation_hours = 0.0_f32;
let mut absence_derived_sick_leave_hours = 0.0_f32;
let mut absence_derived_unpaid_leave_hours = 0.0_f32;
for resolved in derived.values() {
    match resolved.category {
        AbsenceCategory::Vacation => absence_derived_vacation_hours += resolved.hours,
        AbsenceCategory::SickLeave => absence_derived_sick_leave_hours += resolved.hours,
        AbsenceCategory::UnpaidLeave => absence_derived_unpaid_leave_hours += resolved.hours,
    }
}
```

**Zielzustand — EmployeeReport-Konstruktion** (ersetzt Z. 642–674):
```rust
// Additiv: extra_hours (lebend, deleted IS NULL) + absence_period-abgeleitete Stunden.
vacation_hours: extra_hours
    .iter()
    .filter(|eh| eh.category == ExtraHoursCategory::Vacation)
    .map(|eh| eh.amount)
    .sum::<f32>()
    + absence_derived_vacation_hours,
sick_leave_hours: extra_hours
    .iter()
    .filter(|eh| eh.category == ExtraHoursCategory::SickLeave)
    .map(|eh| eh.amount)
    .sum::<f32>()
    + absence_derived_sick_leave_hours,
unpaid_leave_hours: extra_hours
    .iter()
    .filter(|eh| eh.category == ExtraHoursCategory::UnpaidLeave)
    .map(|eh| eh.amount)
    .sum::<f32>()
    + absence_derived_unpaid_leave_hours,
```

---

### `service_impl/src/extra_hours.rs` — Schreibsperre-Rückbau (Modifikation)

**Analog:** dieselbe Datei, Z. 197–225

**Zu entfernender Block vollständig** (Z. 197–225):
```rust
// WIRD GELÖSCHT (D-03: Vacation/SickLeave/UnpaidLeave sind ab 8.4 wieder
// ohne Einschränkung anlegbar — M-01 Koexistenz-Modell).
if matches!(
    extra_hours.category,
    ExtraHoursCategory::Vacation
        | ExtraHoursCategory::SickLeave
        | ExtraHoursCategory::UnpaidLeave
) {
    let flag_active = self
        .feature_flag_service
        .is_enabled(
            "absence_range_source_active",
            Authentication::Full,
            Some(tx.clone()),
        )
        .await?;
    if flag_active {
        return Err(ServiceError::ExtraHoursCategoryDeprecated(Box::new(
            extra_hours.category.clone(),
        )));
    }
}
// Kommentar-Block Z. 197–205 wird ebenfalls gelöscht.
```

**Hinweis:** `FeatureFlagService`-Dep in `ExtraHoursServiceImpl` bleibt (wird noch
von `soft_delete_bulk`-Permission und anderen Pfaden genutzt — Entfernung erst in 8.6).

---

### `service_impl/src/billing_period_report.rs` — Snapshot-Schema-Version-Bump

**Analog:** vorhandener Kommentarblock + Const (Z. 31–50); Muster aus Phase-8.3-Bump
identisch.

**Aktueller Wert** (Z. 50):
```rust
pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 4;
```

**Zielwert + Kommentar-Extension** (History-Eintrag im Kommentar Z. 39–49 erweitern):
```rust
/// - v5: Phase 8.4 — Additiver Merge: Vacation/SickLeave/UnpaidLeave werden
///   nun aus BEIDEN Quellen (lebende `extra_hours` + `absence_period` via
///   `derive_hours_for_range`) summiert statt quellen-exklusiv per Flag.
///   Ältere Snapshots (v4) haben die `extra_hours`-Seite nicht mitgezählt
///   (Flag war on → nur absence_period) oder umgekehrt (Flag off → nur
///   extra_hours). Ein Validator kann v4-Snapshots nicht sicher re-validieren.
pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 5;
```

---

### `service_impl/src/test/reporting_additive_merge.rs` — NEUE Testdatei

**Primäranalog:** `service_impl/src/test/reporting_flag_on_integration.rs` (vollständiges
Mockall-Setup für `ReportingServiceImpl`).

**Sekundäranalog:** `service_impl/src/test/reporting_flag_off_bit_identity.rs` (Fixture-
Helfer `make_extra_hours`, gleiches `ReportingMocks`-Bundle).

**Imports-Pattern** (aus `reporting_flag_on_integration.rs`, Z. 10–31):
```rust
use std::collections::BTreeMap;
use std::sync::Arc;

use time::macros::{date, datetime};
use uuid::Uuid;

use service::absence::{AbsenceCategory, MockAbsenceService, ResolvedAbsence};
use service::carryover::MockCarryoverService;
use service::clock::MockClockService;
use service::employee_work_details::MockEmployeeWorkDetailsService;
use service::extra_hours::{ExtraHours, ExtraHoursCategory, MockExtraHoursService};
use service::feature_flag::MockFeatureFlagService;
use service::permission::Authentication;
use service::reporting::ReportingService;
use service::sales_person::MockSalesPersonService;
use service::shiftplan_report::MockShiftplanReportService;
use service::uuid_service::MockUuidService;
use service::MockPermissionService;
use shifty_utils::ShiftyDate;

use crate::reporting::{ReportingServiceDeps, ReportingServiceImpl};
use crate::test::reporting_phase2_fixtures::{fixture_sales_person, fixture_sales_person_id};
```

**`ReportingMocks`-Struct + `TestDeps`-Impl** (aus `reporting_flag_on_integration.rs`, Z. 33–96 —
unverändertes Copy-Pattern; nach Rückbau des `FeatureFlagService`-Deps: falls Compiler
unused-field warnt, `feature_flag_service`-Felder entfernen):
```rust
struct ReportingMocks {
    extra_hours_service: MockExtraHoursService,
    shiftplan_report_service: MockShiftplanReportService,
    employee_work_details_service: MockEmployeeWorkDetailsService,
    sales_person_service: MockSalesPersonService,
    carryover_service: MockCarryoverService,
    permission_service: MockPermissionService,
    clock_service: MockClockService,
    uuid_service: MockUuidService,
    feature_flag_service: MockFeatureFlagService, // ggf. nach Dep-Entfernung weglassen
    absence_service: MockAbsenceService,
    transaction_dao: dao::MockTransactionDao,
}

struct TestDeps;
impl ReportingServiceDeps for TestDeps {
    type Context = ();
    type Transaction = dao::MockTransaction;
    type ExtraHoursService = MockExtraHoursService;
    type ShiftplanReportService = MockShiftplanReportService;
    type EmployeeWorkDetailsService = MockEmployeeWorkDetailsService;
    type SalesPersonService = MockSalesPersonService;
    type CarryoverService = MockCarryoverService;
    type PermissionService = MockPermissionService;
    type ClockService = MockClockService;
    type UuidService = MockUuidService;
    type FeatureFlagService = MockFeatureFlagService; // ggf. nach Dep-Entfernung weglassen
    type AbsenceService = MockAbsenceService;
    type TransactionDao = dao::MockTransactionDao;
}

impl ReportingMocks {
    fn new() -> Self { /* alle Felder ::new() */ }

    fn build(self) -> ReportingServiceImpl<TestDeps> {
        ReportingServiceImpl {
            extra_hours_service: Arc::new(self.extra_hours_service),
            shiftplan_report_service: Arc::new(self.shiftplan_report_service),
            employee_work_details_service: Arc::new(self.employee_work_details_service),
            sales_person_service: Arc::new(self.sales_person_service),
            carryover_service: Arc::new(self.carryover_service),
            permission_service: Arc::new(self.permission_service),
            clock_service: Arc::new(self.clock_service),
            uuid_service: Arc::new(self.uuid_service),
            feature_flag_service: Arc::new(self.feature_flag_service), // ggf. weglassen
            absence_service: Arc::new(self.absence_service),
            transaction_dao: Arc::new(self.transaction_dao),
        }
    }
}
```

**`make_extra_hours`-Fixture** (identisch in beiden alten Reporting-Tests, Z. 98–110):
```rust
fn make_extra_hours(category: ExtraHoursCategory, amount: f32, day: time::Date) -> ExtraHours {
    ExtraHours {
        id: Uuid::new_v4(),
        sales_person_id: fixture_sales_person_id(),
        amount,
        category,
        description: Arc::from(""),
        date_time: time::PrimitiveDateTime::new(day, time::Time::from_hms(9, 0, 0).unwrap()),
        created: Some(datetime!(2024 - 06 - 01 09:00:00)),
        deleted: None,
        version: Uuid::nil(),
    }
}
```

**Standard-Mock-Boilerplate** (aus `reporting_flag_on_integration.rs`, Z. 117–205 — Pattern
für alle drei additiven Tests):
```rust
// permission + sales_person (immer gleich)
mocks.permission_service
    .expect_check_permission()
    .returning(|_, _| Ok(()));
mocks.sales_person_service
    .expect_verify_user_is_sales_person()
    .returning(|_, _, _| Ok(()));
mocks.sales_person_service
    .expect_get()
    .returning(|_, _, _| Ok(fixture_sales_person()));

// employee_work_details + shiftplan_report: leer (isoliert Stunden-Aggregation)
mocks.employee_work_details_service
    .expect_find_by_sales_person_id()
    .returning(|_, _, _| Ok(Arc::from(vec![])));
mocks.shiftplan_report_service
    .expect_extract_shiftplan_report()
    .returning(|_, _, _, _, _| Ok(Arc::from(vec![])));

// carryover: None
mocks.carryover_service
    .expect_get_carryover()
    .returning(|_, _, _, _| Ok(None));

// transaction: passthrough
mocks.transaction_dao
    .expect_use_transaction()
    .returning(|_| Ok(dao::MockTransaction));
```

**Kritische Mock-Erwartungen für die neuen additiven Tests:**
- Kein `feature_flag_service.expect_is_enabled()` mehr für `absence_range_source_active`
  (8.4 liest den Flag nicht mehr im Reporting-Pfad).
- `absence_service.expect_derive_hours_for_range()` wird immer erwartet (`times(1)`),
  auch wenn extra_hours leer sind.
- `extra_hours_service.expect_find_by_sales_person_id_and_year_range()` wird immer
  erwartet (die lebenden Rows, kann leerer Slice sein).

**Drei additive Testszenarien** (zu implementieren — Struktur aus `test_flag_on_uses_absence_source`):

*Szenario 1: Nur extra_hours (keine absence_period)*
```rust
// derive_hours_for_range → leere BTreeMap
// extra_hours → [Vacation 8h, SickLeave 4h, UnpaidLeave 2h]
// Erwartung: vacation_hours = 8.0, sick_leave_hours = 4.0, unpaid_leave_hours = 2.0
```

*Szenario 2: Nur absence_period (keine extra_hours)*
```rust
// derive_hours_for_range → {2024-06-03: Vacation 8h, 2024-06-04: SickLeave 8h}
// extra_hours → leerer Slice
// Erwartung: vacation_hours = 8.0, sick_leave_hours = 8.0, unpaid_leave_hours = 0.0
```

*Szenario 3: Beide Quellen, distinkte Tage → additive Summe (Kern-SC)*
```rust
// derive_hours_for_range → {2024-06-03: Vacation 8h}
// extra_hours → [Vacation 4h am 2024-06-05 (anderer Tag, lebt noch)]
// Erwartung: vacation_hours = 12.0  (4h + 8h)
// ExtraWork aus extra_hours bleibt korrekt summiert
```

*Szenario 4: Konvertierte Rows zählen nicht doppelt (per-row Invariante)*
```rust
// extra_hours → leerer Slice (soft-deleted Rows fallen per deleted IS NULL im DAO heraus
//               → der DAO-Mock liefert einfach [] für den konvertierten Tag)
// derive_hours_for_range → {2024-06-03: Vacation 8h}
// Erwartung: vacation_hours = 8.0 (nur einmal, keine Doppelzählung)
// Hinweis: Dieser Test macht die strukturelle Invariante explizit — der Mock
//          liefert bereits den korrekten DAO-Zustand (deleted rows gar nicht erst
//          im Slice). Kein neuer DAO-Code nötig.
```

---

### `service_impl/src/test/reporting_flag_on_integration.rs` — Umbau auf additives Modell

**Analog:** sich selbst (vollständige Datei ist der Umbau-Ausgangspunkt).

**Was sich ändert:**
1. Datei-Header-Kommentar: Flag=on-Beschreibung ersetzen durch additive Beschreibung.
2. `feature_flag_service.expect_is_enabled()` mit `absence_range_source_active` entfernen.
3. `absence_service.expect_derive_hours_for_range()` bleibt, wird jetzt ohne Flag-Guard
   erwartet (kein `.times(0)` mehr, einfach `.returning(...)`).
4. `extra_hours`-Slice: Die 999h-Vacation/Sick/Unpaid-Einträge aus dem alten Test
   entweder entfernen oder auf realistische Werte ändern, da sie jetzt addiert werden.
5. Assertions anpassen: `vacation_hours = derive-Wert + extra_hours-Wert`.

**Muster bleibt sonst identisch:** `ReportingMocks`-Struct, `TestDeps`-Impl, `build()`
unverändert.

---

### `service_impl/src/test/reporting_flag_off_bit_identity.rs` — LÖSCHEN

Keine Anpassung — Datei vollständig löschen. Registrierung in
`service_impl/src/test/mod.rs` entfernen:
```rust
// Diese Zeile löschen:
#[cfg(test)]
pub mod reporting_flag_off_bit_identity;
```

---

### `service_impl/src/test/billing_period_snapshot_locking.rs` — Version-Pin Update

**Analog:** sich selbst (Z. 26–35 ist das exakte Pattern).

**Aktueller Zustand** (Z. 26–35):
```rust
#[test]
fn test_snapshot_schema_version_pinned() {
    assert_eq!(
        CURRENT_SNAPSHOT_SCHEMA_VERSION, 4,
        "CURRENT_SNAPSHOT_SCHEMA_VERSION muss 4 sein nach Phase-8.3-Bump \
         (Trigger: AbsencePeriod.day_fraction = Half halbiert die effektive \
         Soll-Stundenzahl pro Tag in derive_hours_for_range; betrifft die \
         Vacation/SickLeave/UnpaidLeave-derived value_types hours + days). \
         Siehe CLAUDE.md § Snapshot Schema Versioning."
    );
}
```

**Zielzustand** (gleiches Pattern, Wert + Nachricht tauschen):
```rust
#[test]
fn test_snapshot_schema_version_pinned() {
    assert_eq!(
        CURRENT_SNAPSHOT_SCHEMA_VERSION, 5,
        "CURRENT_SNAPSHOT_SCHEMA_VERSION muss 5 sein nach Phase-8.4-Bump \
         (Trigger: Additiver Merge — Vacation/SickLeave/UnpaidLeave werden \
         aus BEIDEN Quellen summiert: lebende extra_hours + derive_hours_for_range. \
         Ältere Snapshots können nicht sicher re-validiert werden). \
         Siehe CLAUDE.md § Snapshot Schema Versioning."
    );
}
```

**Datei-Header-Kommentar** (Z. 7 aktualisieren):
```rust
//! - `test_snapshot_schema_version_pinned`: erwartet 5 (Phase-8.4-Bump —
//!   additiver Merge beider Quellen für Vacation/SickLeave/UnpaidLeave ändert
//!   die Input-Menge der Aggregations-Computation).
```

---

### `service_impl/src/test/extra_hours.rs` — Test 2 löschen, Tests 1 + 3 anpassen

**Analog:** Tests 1 und 3 bleiben als strukturelles Vorbild.

**Test 1 umbenennen + vereinfachen** (Z. 618–638):
```rust
// Alter Name: create_vacation_succeeds_when_flag_off
// Neuer Name: create_vacation_always_succeeds
// Änderung: feature_flag_service.expect_is_enabled() komplett entfernen
//           (nach Gate-Rückbau wird is_enabled für diese Kategorien nie mehr aufgerufen)
// deps.feature_flag_service.expect_is_enabled().times(0); // ← hinzufügen um explizit zu machen
```

**Test 2 vollständig löschen** (Z. 640–662):
```rust
// create_vacation_returns_403_error_variant_when_flag_on → LÖSCHEN
// ExtraHoursCategoryDeprecated wird nach Rückbau nie mehr geworfen.
```

**Test 3 umbenennen** (Z. 664–682):
```rust
// Alter Name: create_extra_work_succeeds_when_flag_on
// Neuer Name: create_extra_work_does_not_trigger_flag_check
// Inhalt: times(0) auf is_enabled bleibt korrekt — ExtraWork triggert nach
//         wie vor keinen Flag-Check (jetzt weil das Gate vollständig weg ist).
```

---

### `shifty_bin/src/integration_test/cutover.rs` — Zwei Abschnitte löschen/anpassen

**Analog:** umgebender Code bleibt; die Lösch-Grenzen sind exakt identifiziert.

**Block 1 löschen** (Z. 668–728 — `AFTER: Vacation create rejected`-Abschnitt):
```rust
// LÖSCHEN: Kommentar "AFTER: Vacation create rejected with ExtraHoursCategoryDeprecated"
// und den zugehörigen let after = ...; assert!(matches!(...ExtraHoursCategoryDeprecated...));
// Block. ExtraWork-Test (Z. 701–728) ebenfalls prüfen — bleibt korrekt (ExtraWork
// war nie geblockt), aber Assertion muss weiterhin Ok erwarten.
```

**Block 2 löschen** (Z. 735–789 — `test_403_body_format_for_deprecated_category`):
```rust
// LÖSCHEN: gesamten #[tokio::test] test_403_body_format_for_deprecated_category
// (Z. 735–789). Der Test flippt den Flag manuell und erwartet ExtraHoursCategoryDeprecated —
// nach Gate-Rückbau kommt dieser Fehler nie mehr.
```

---

## Gemeinsame Muster (cross-cutting)

### Mockall Transaction-Boilerplate
**Quelle:** `service_impl/src/test/reporting_flag_on_integration.rs`, Z. 200–203 + `extra_hours.rs`, Z. 158–165
**Anwenden auf:** alle neuen/umgeschriebenen Unit-Tests
```rust
mocks.transaction_dao
    .expect_use_transaction()
    .returning(|_| Ok(dao::MockTransaction));
// commit() wird für read-only Pfade (get_report_for_employee_range) nicht erwartet;
// für write-Pfade (extra_hours create): .expect_commit().returning(|_| Ok(()))
```

### `mod.rs`-Registrierung für neue Testdatei
**Quelle:** `service_impl/src/test/mod.rs`, bestehendes Muster (Z. 1–63)
**Anwenden auf:** neue Datei `reporting_additive_merge.rs`
```rust
#[cfg(test)]
pub mod reporting_additive_merge;
// Gleichzeitig: reporting_flag_off_bit_identity entfernen
```

### Snapshot-Bump-im-selben-Commit-Regel
**Quelle:** CLAUDE.md § "Billing Period Snapshot Schema Versioning" + RESEARCH.md § 3
**Anwenden auf:** `billing_period_report.rs` + `billing_period_snapshot_locking.rs`
Beide Dateien müssen im selben logischen Commit geändert werden (Fallstrick 3 aus RESEARCH.md).

---

## Kein Analog gefunden

Keine Dateien ohne Analog — alle Muster sind im bestehenden Codebase vertreten.

---

## Metadaten

**Analog-Suchbereich:** `service_impl/src/`, `service_impl/src/test/`, `shifty_bin/src/integration_test/`
**Dateien gescannt:** 12
**Pattern-Extraktion:** 2026-06-09
