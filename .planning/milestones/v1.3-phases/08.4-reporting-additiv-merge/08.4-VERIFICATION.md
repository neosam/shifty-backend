---
phase: 08.4-reporting-additiv-merge
verified: 2026-06-09T14:30:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 8/9 must-haves verified
  gaps_closed:
    - "Balance-Parity fuer DYNAMIC Vertraege in get_reports_for_all_employees (CR-01 geschlossen durch per-Woche-gegatete absence_derived_balance_hours im WeeklyHours-Fold)"
    - "Balance-Parity fuer DYNAMIC Vertraege in get_week (WR-01 geschlossen durch planned_hours<=0.0-Guard auf both abense_hours_for_balance + absence_derived_balance_total)"
    - "is_dynamic=true Fixture (fixture_work_details_dynamic_mon_fri) angelegt"
    - "Drei dynamische Parity-Tests (test_balance_parity_dynamic_employee_range/all_employees/get_week) gruen"
  gaps_remaining: []
  regressions: []
---

# Phase 08.4: Reporting-Additiv-Merge Verification Report (Re-Verifikation nach Gap-Closure 08.4-05)

**Phase-Ziel:** Reporting-Additiv-Merge + Deprecation-Rückbau — das Reporting projiziert
`absence_period`-Stunden additiv neben `extra_hours` (dauerhafte Koexistenz statt Big-Bang-Cutover).
Display-Felder additiv; balance/expected werden symmetrisch und QUELLEN-UNABHÄNGIG bewegt für JEDEN
Vertragstyp (statisch UND dynamisch) über alle drei Report-Methoden (`get_report_for_employee_range`,
`get_reports_for_all_employees`, `get_week`). Snapshot-Schema-Versionierung korrekt gepflegt.

**Verified:** 2026-06-09T14:30:00Z
**Status:** passed
**Re-verification:** Ja — nach Gap-Closure Plan 08.4-05 (Balance-Parity dynamic-contract)

---

## Re-Verifikation Zusammenfassung

Plan 08.4-05 hat das einzige verbleibende Gap der Vorverifikation (Truth 9: Balance-Parity für
`is_dynamic=true`) vollständig geschlossen. Beide Defekte (Defekt A: Jahreslumpen-Abzug in
`get_reports_for_all_employees`; Defekt B: negatives `expected_hours` in `get_week`) sind durch
per-Woche-gegatete Logik behoben, die exakt die Referenz-Implementierung in `hours_per_week`
(reporting.rs Z.988-997) spiegelt. Die drei neuen dynamischen Parity-Tests beweisen den pre-Fix
RED → post-Fix GREEN-Übergang. Alle 12 Tests in `reporting_additive_merge`, der Snapshot-Pin
(`= 6`) und die Full-Suite (`431 service_impl + 79 shifty_bin = 510 Tests`) sind grün.

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidenz |
|----|-------|--------|---------|
| 1  | Additive Koexistenz: Vacation/SickLeave/UnpaidLeave-extra_hours bleiben anlegbar (Schreibsperre entfernt) | ✓ VERIFIED | `ExtraHoursCategoryDeprecated`-Gate entfernt; `create_vacation_always_succeeds` grün; `absence_range_source_active` 0 Treffer in reporting.rs |
| 2  | `get_report_for_employee_range` summiert beide Quellen additiv (`derive_hours_for_range` + `extra_hours`) | ✓ VERIFIED | reporting.rs:509-528 unbedingter `derive_hours_for_range`-Aufruf; additive Akkumulatoren; 4 additive Tests + 3 Parity-Tests grün |
| 3  | `get_reports_for_all_employees` summiert Display-Felder additiv aus beiden Quellen (12h = 4h + 8h) | ✓ VERIFIED | reporting.rs:414-423 ungegater Jahres-Display-Lumpen; `test_all_employees_additive_merge` grün (12h); Display-Merge Z.439-443 unveraendert |
| 4  | `get_week` summiert Display-Felder additiv aus beiden Quellen (11h = 3h + 8h) | ✓ VERIFIED | reporting.rs:828-837 ungegater Display-Merge; `test_get_week_additive_merge` grün (11h); Display-Felder Z.868-872 unveraendert |
| 5  | Gleichtags-Koexistenz: extra_hours Vacation + absence_period Vacation am selben Tag summieren additiv zu 16h (kein Dedup) | ✓ VERIFIED | `test_same_day_overlap_additive_no_dedup` assertiert 16.0 (8h+8h am 2024-06-03); grün |
| 6  | Konvertierte (soft-deleted) extra_hours zählen nicht doppelt | ✓ VERIFIED | `test_additive_converted_rows_not_double_counted`: leere extra_hours + 8h derived → vacation_hours == 8.0; grün |
| 7  | `CURRENT_SNAPSHOT_SCHEMA_VERSION` ist 6 (kein Bump 6→7); `snapshot_schema_version_pinned` grün | ✓ VERIFIED | billing_period_report.rs:62 `pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6`; billing_period_snapshot_locking.rs:27 pinnt 6; Test grün |
| 8  | Balance-Parity für non-dynamic Verträge: reine absence_period-Buchung bewegt balance_hours identisch zu extra_hours-Buchung | ✓ VERIFIED | `test_balance_parity_absence_period_vs_extra_hours` (is_dynamic=false): beide Läufe (expected_extra − expected_absence).abs() < 0.01 UND (balance_extra − balance_absence).abs() < 0.01; grün |
| 9  | Balance-Parity für DYNAMIC Verträge: reine absence_period-Buchung bewegt balance_hours identisch zu extra_hours (balance ~0) in ALLEN drei Methoden | ✓ VERIFIED | `test_balance_parity_dynamic_employee_range` / `_all_employees` / `_get_week`: alle 3 grün; balance_a.abs() < 0.01 UND balance_b.abs() < 0.01 in jedem Test; per-Woche-Gating korrekt implementiert |

**Score:** 9/9 Truths verified

### Deferred Items

Keine.

---

## Required Artifacts

| Artifact | Erwartet | Status | Details |
|----------|----------|--------|---------|
| `service_impl/src/reporting.rs` | Additiver Merge in allen 3 Methoden + Balance-Symmetrie für alle Vertragstypen | ✓ VERIFIED | 3× `derive_hours_for_range`; `absence_derived_balance_hours` (WeeklyHours-Feld, 7 Treffer); `absence_derived_balance_total` (get_week, 3 Treffer); Display-Merge ungegated; per-Woche-Guard in beiden defekten Methoden |
| `service_impl/src/billing_period_report.rs` | `CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6` | ✓ VERIFIED | Z.62 exakt; v6-History-Kommentar vollständig |
| `service_impl/src/extra_hours.rs` | Schreibsperre entfernt, Permission-Check erhalten | ✓ VERIFIED | Gate-Block entfernt; kommentarbasierte Hinweise noch vorhanden (kein Stub) |
| `service_impl/src/test/reporting_additive_merge.rs` | 12 Tests (4 SC-1 + 5 Schwester/Overlap/Bounds + 1 non-dyn Parity + 3 dyn Parity) | ✓ VERIFIED | 12 `#[tokio::test]`-Funktionen; alle 12 grün; `fixture_work_details_dynamic_mon_fri` in 6 Treffern |
| `service_impl/src/test/reporting_phase2_fixtures.rs` | `fixture_work_details_dynamic_mon_fri` mit `is_dynamic: true` | ✓ VERIFIED | Z.78-83: Spread-Syntax mit `is_dynamic: true`; eigene UUID `0x...0011` |
| `service_impl/src/test/billing_period_snapshot_locking.rs` | Pin auf 6 | ✓ VERIFIED | `assert_eq!(CURRENT_SNAPSHOT_SCHEMA_VERSION, 6, ...)` Z.27; grün |
| `service_impl/src/test/mod.rs` | `reporting_additive_merge` registriert; alte Flag-Dateien deregistriert | ✓ VERIFIED | `pub mod reporting_additive_merge;` vorhanden; `reporting_flag_off_bit_identity` 0 Treffer |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| reporting.rs `get_report_for_employee_range` | `absence_service.derive_hours_for_range` | unbedingter Call (über `hours_per_week`-Pfad) | ✓ WIRED | Referenz-Implementierung; war bereits pre-Fix korrekt |
| reporting.rs `get_reports_for_all_employees` | `absence_service.derive_hours_for_range` | Call Z.209 VOR dem per-Woche-Fold | ✓ WIRED | Range: `[first_day_in_year(year)..until_week-Sonntag]`; korrekt |
| reporting.rs `get_reports_for_all_employees` Balance-Reduktion | `absence_derived_balance_hours` → `expected_hours` / `dynamic_hours` (Z.429-430) | per-Woche-gegateter Fold; Guard Z.271: `if expected_hours <= 0.0 { 0.0 }` | ✓ WIRED | Symmetrisch zu `absense_hours` (Z.298); identische Gate-Bedingung; Defekt A geschlossen |
| reporting.rs `get_week` | `absence_service.derive_hours_for_range` | Call Z.818 (Montag..Sonntag der Woche) | ✓ WIRED | Korrekte Platzierung vor `apply_weekly_cap` |
| reporting.rs `get_week` Balance-Reduktion | `absence_derived_balance_total` → `expected_hours` / `dynamic_hours` (Z.851/855) | Guard Z.846: `if planned_hours <= 0.0 { 0.0 }` | ✓ WIRED | Zusätzlich: `abense_hours_for_balance` Z.845 ebenfalls gegated (dokumentierte Deviation, korrekt); Defekt B geschlossen |
| reporting.rs Display-Merge `get_reports_for_all_employees` | `absence_derived_vacation/sick/unpaid_leave_hours` → `ShortEmployeeReport` (Z.439-443) | ungegater Jahreslumpen Z.414-423 | ✓ WIRED | Display bleibt additiv ungegated; Truth 3 gehalten |
| reporting.rs Display-Merge `get_week` | `absence_derived_vacation/sick/unpaid_leave_hours` → `ShortEmployeeReport` (Z.868-872) | ungegater Merge Z.828-837 | ✓ WIRED | Display bleibt additiv ungegated; Truth 4 gehalten |
| billing_period_report.rs Writer | `CURRENT_SNAPSHOT_SCHEMA_VERSION` → `snapshot_schema_version` Spalte | Z.320 | ✓ WIRED | Const korrekt in Writer konsumiert; kein Bump 6→7 (dokumentierter Entscheid) |

---

## Data-Flow Trace (Level 4)

| Artifact | Data Variable | Quelle | Echte Daten | Status |
|----------|--------------|--------|-------------|--------|
| reporting.rs `get_reports_for_all_employees` | `vacation_hours` (Display) | `weekly_hours.vacation_hours` + ungegater `absence_derived_vacation_hours`-Lumpen | Ja (beide Quellen additiv) | ✓ FLOWING |
| reporting.rs `get_reports_for_all_employees` | `expected_hours` / `balance_hours` | `weekly_hours.planned_hours − absense_hours − absence_derived_balance_hours` (per-Woche gegated) | Ja, korrekt für dynamic + non-dynamic | ✓ FLOWING |
| reporting.rs `get_week` | `vacation_hours` (Display) | `vacation_hours + absence_derived_vacation_hours` (ungegated) | Ja (beide Quellen additiv) | ✓ FLOWING |
| reporting.rs `get_week` | `expected_hours` / `balance_hours` | `planned_hours − abense_hours_for_balance − absence_derived_balance_total` (beide gegated bei `planned_hours <= 0.0`) | Ja, korrekt für dynamic + non-dynamic | ✓ FLOWING |

---

## Behavioral Spot-Checks

| Verhalten | Kommando | Ergebnis | Status |
|-----------|----------|----------|--------|
| 3 dynamische Balance-Parity-Tests | `nix develop -c cargo test -p service_impl test_balance_parity_dynamic` | 3 passed, 0 failed | ✓ PASS |
| 12 additive Reporting-Szenarien | `nix develop -c cargo test -p service_impl reporting_additive` | 12 passed, 0 failed | ✓ PASS |
| Snapshot-Version-Pin = 6 | `nix develop -c cargo test -p service_impl snapshot_schema_version_pinned` | 1 passed | ✓ PASS |
| Full-Suite-Gate | `nix develop -c cargo test --workspace` | service_impl 431, shifty_bin 79, 0 failures, 0 compile-errors | ✓ PASS |

---

## Requirements Coverage

Keine Requirement-IDs (Closure-/Modell-Revisions-Phase). Alle fünf Plans `requirements: []`.
ROADMAP Phase 8.4 trägt keine FUI-Requirements. Keine orphaned Requirements in REQUIREMENTS.md.

---

## Anti-Patterns Found

| Datei | Zeile | Muster | Schweregrad | Auswirkung |
|-------|-------|--------|-------------|------------|
| `service_impl/src/test/extra_hours.rs` | 13, 612 | Kommentar-Referenzen auf `ExtraHoursCategoryDeprecated` (nur Kommentar, kein Assert) | ℹ️ Info | Kein funktionaler Stub; reine Dokumentation des entfernten Gates. Keine Auswirkung auf Testsuite-Korrektheit. |

Keine Blocker- oder Warning-Anti-Patterns gefunden. Die `ExtraHoursCategoryDeprecated`-Treffer in
`extra_hours.rs` sind ausschließlich Kommentare (Modulkopf Z.13 + Abschnitts-Kommentar Z.612),
keine Asserts und keine Produktions-Logik.

---

## Warnings aus Code-Review (offen, nicht blockierend)

Folgende Punkte aus dem Re-Review (08.4-REVIEW.md) sind nachgewiesen **nicht blockierend** und
sind in nachfolgenden Phasen zu adressieren — sie berühren NICHT das Phase-Ziel:

- **WR-A** (`get_week`: `apply_weekly_cap` cappt gegen reduziertes statt unreduziertesExpected) —
  pre-existing, `cap_planned_hours_to_expected: false` in allen Fixtures; kein Test bricht.
- **WR-B** (Display-Asymmetrie in `get_reports_for_all_employees` für dynamic-extra_hours) —
  pre-existing; Display-Ziel (Truth 3 & 4) für den additiven Merge erfüllt.
- **WR-C** (`vacation_days`/`absence_days` ignorieren absence_period-derived Stunden) —
  pre-existing; offen seit Vor-Review; kein Scope von Phase 8.4.
- **IN-A** (Dreifache Duplikation der derived-Aggregation) — Refactoring-Backlog.
- **IN-B** (dynamic-Parity-Tests assertieren Display-Spalten nicht) — Coverage-Verbesserung, nicht
  Korrektheitsproblem.

---

## Human Verification Required

Keine — alle must-haves sind durch automatische Tests vollständig verifizierbar.

---

## Gaps Summary

Keine offenen Gaps. Alle 9 Must-Haves sind verified.

Die drei in der Vorverifikation fehlenden Punkte (Truth 9 / dynamische Balance-Parity) sind durch
Plan 08.4-05 korrekt geschlossen:

1. **`get_reports_for_all_employees` (Defekt A):** `WeeklyHours`-Struct um
   `absence_derived_balance_hours: f32`-Feld erweitert; per-Woche-Guard `if expected_hours <= 0.0 {
   0.0 }` (Z.271) symmetrisch zu `absense_hours` (Z.298); Fold akkumuliert das Feld (Z.399); Balance-
   Reduktion konsumiert `weekly_hours.absence_derived_balance_hours` statt Jahreslumpen (Z.429-430).

2. **`get_week` (Defekt B):** `abense_hours_for_balance = if planned_hours <= 0.0 { 0.0 } else {
   abense_hours }` (Z.845) und `absence_derived_balance_total = if planned_hours <= 0.0 { 0.0 } else
   { sum }` (Z.846-850); `expected_hours = planned_hours − abense_hours_for_balance −
   absence_derived_balance_total` (Z.851); symmetrisch zur Referenz `hours_per_week`.

3. **Fixture + Tests:** `fixture_work_details_dynamic_mon_fri()` in
   `reporting_phase2_fixtures.rs`; drei Tests `test_balance_parity_dynamic_*` in
   `reporting_additive_merge.rs`; alle drei post-Fix grün (two pre-Fix RED: all_employees, get_week).

Die Display-Felder (`vacation_hours`/`sick_leave_hours`/`unpaid_leave_hours`) blieben in beiden
Methoden bewusst **ungegated additiv** — der Jahreslumpen-Merge in `get_reports_for_all_employees`
(Z.414-423) und der Wochen-Merge in `get_week` (Z.828-837) wurden nicht angetastet. Truth 3 & 4
bleiben grün (12h bzw. 11h). Snapshot-Version bleibt korrekt bei 6 (dokumentierter Entscheid:
kein weiterer Bump 6→7, da v6 noch in dieser Phase eingeführt wurde und kein released Snapshot mit
defekter dynamischer Balance existiert).

---

_Verified: 2026-06-09T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
