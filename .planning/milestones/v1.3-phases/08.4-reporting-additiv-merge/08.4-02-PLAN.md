---
phase: 08.4-reporting-additiv-merge
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - service_impl/src/reporting.rs
  - service_impl/src/billing_period_report.rs
  - service_impl/src/extra_hours.rs
  - shifty_bin/src/main.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "reporting.rs summiert Vacation/SickLeave/UnpaidLeave additiv aus lebenden extra_hours (deleted IS NULL) PLUS derive_hours_for_range-Ergebnissen; kein globaler Flag-Branch mehr."
    - "Konvertierte (soft-deleted) extra_hours zaehlen nicht doppelt — sie fallen strukturell via WHERE deleted IS NULL aus dem DAO-Load."
    - "Neue Vacation/SickLeave/UnpaidLeave-extra_hours sind wieder anlegbar (ExtraHoursCategoryDeprecated-Gate entfernt)."
    - "CURRENT_SNAPSHOT_SCHEMA_VERSION ist 5; Bump + Reporting-Aenderung im selben jj-Commit."
    - "cargo test --workspace ist gruen."
  artifacts:
    - path: "service_impl/src/reporting.rs"
      provides: "Additiver Merge ohne Flag-Branch (SC-1)"
      contains: "absence_derived_vacation_hours"
    - path: "service_impl/src/billing_period_report.rs"
      provides: "CURRENT_SNAPSHOT_SCHEMA_VERSION = 5 (SC-3)"
      contains: "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 5"
    - path: "service_impl/src/extra_hours.rs"
      provides: "Schreibsperre entfernt (SC-2)"
  key_links:
    - from: "service_impl/src/reporting.rs"
      to: "absence_service.derive_hours_for_range"
      via: "unconditional call (kein if use_absence_range_source)"
      pattern: "derive_hours_for_range"
    - from: "service_impl/src/billing_period_report.rs"
      to: "snapshot_schema_version Spalte"
      via: "Writer build_and_persist_billing_period_report"
      pattern: "CURRENT_SNAPSHOT_SCHEMA_VERSION"
---

<objective>
Wave 2 — Produktionscode-Aenderung. Stellt das Reporting von globaler Flag-gesteuerter
Quellen-Umschaltung auf dauerhafte additive Koexistenz um (Modell A, D-01/D-02), baut die
extra_hours-Schreibsperre zurueck (D-03) und zieht den Snapshot-Schema-Version-Bump 4 -> 5
(D-04). Die in Plan 01 angelegten additiven Tests + der Locking-Test werden hierdurch GREEN.

Purpose: Kern der Phase. Drei chirurgische Edits an drei Service-Dateien; der Reporting-Edit
und der Snapshot-Bump muessen im SELBEN jj-Commit landen (CLAUDE.md § Snapshot Versioning —
die Input-Menge der Computation aendert sich).
Output: reporting.rs additiv, extra_hours.rs ohne Gate, Snapshot-Version 5, gruene Suite.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-reporting-additiv-merge/08.4-CONTEXT.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-RESEARCH.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-PATTERNS.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-VALIDATION.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-01-SUMMARY.md

<interfaces>
<!-- Live-Code-Stand verifiziert 2026-06-09; Zeilen stimmen mit RESEARCH ueberein. -->

reporting.rs gen_service_impl! (Z. 60-75) listet FeatureFlagService als Dep (Z. 71):
  FeatureFlagService: FeatureFlagService<Context = Self::Context, Transaction = Self::Transaction> = feature_flag_service,
Einziger is_enabled-Call im Modul: Z. 476 (der zu loeschende use_absence_range_source-Block).
Import Z. 12: feature_flag::FeatureFlagService.

reporting.rs zu ersetzender Block (Z. 472-541): Flag-Read + ExtraHours-Filter +
conditional derive_hours_for_range. EmployeeReport-Konstruktion mit Flag-Branch: Z. 642-674.

billing_period_report.rs Z. 50: pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 4;
Writer konsumiert die Const bei Z. 314 (snapshot_schema_version: CURRENT_SNAPSHOT_SCHEMA_VERSION).

extra_hours.rs Z. 197-225: ExtraHoursCategoryDeprecated-Gate (matches!-Block + is_enabled +
return Err). FeatureFlagService-Dep in ExtraHoursServiceImpl BLEIBT (andere Pfade nutzen ihn).

Ziel-Code (verbatim): 08.4-PATTERNS.md § "reporting.rs — Additiver Merge" (Z. 70-118) und
§ "extra_hours.rs — Schreibsperre-Rueckbau" (Z. 126-151).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additiver Reporting-Merge + Snapshot-Bump 4->5 (EIN jj-Commit)</name>
  <files>service_impl/src/reporting.rs, service_impl/src/billing_period_report.rs, shifty_bin/src/main.rs</files>
  <read_first>
    - service_impl/src/reporting.rs (Z. 60-75 gen_service_impl!, Z. 12 Import, Z. 472-682 Flag-Block + EmployeeReport)
    - service_impl/src/billing_period_report.rs (Z. 39-50 History-Kommentar + Const, Z. 314 Writer)
    - shifty_bin/src/main.rs (ReportingServiceImpl-DI-Konstruktion — nur falls FeatureFlagService-Dep entfernt wird)
    - .planning/phases/08.4-reporting-additiv-merge/08.4-PATTERNS.md § "reporting.rs — Additiver Merge", § "billing_period_report.rs — Snapshot-Schema-Version-Bump"
    - .planning/phases/08.4-reporting-additiv-merge/08.4-RESEARCH.md § 1, § 3, Fallstrick 1
    - shifty-backend/CLAUDE.md § "Billing Period Snapshot Schema Versioning"
  </read_first>
  <behavior>
    - reporting_additive_merge::test_additive_only_extra_hours -> vacation 8, sick 4, unpaid 2
    - reporting_additive_merge::test_additive_only_absence_period -> vacation 8, sick 8, unpaid 0
    - reporting_additive_merge::test_additive_both_sources_distinct_days -> vacation 12
    - reporting_additive_merge::test_additive_converted_rows_not_double_counted -> vacation 8
    - billing_period_snapshot_locking::test_snapshot_schema_version_pinned -> erwartet 5
  </behavior>
  <action>
    Beide Aenderungen MUESSEN in einem jj-Commit landen (Snapshot-Versionierungs-Regel).

    SCHRITT A — service_impl/src/reporting.rs additiver Merge (D-01):

    1. Flag-Read-Block (Z. 472-482, let use_absence_range_source = self.feature_flag_service
       .is_enabled("absence_range_source_active", ...)) VOLLSTAENDIG loeschen.

    2. ExtraHours-Filter-Block (Z. 489-505, let extra_hours: Arc<[ExtraHours]> = if
       use_absence_range_source { ... filter Vacation/SickLeave/UnpaidLeave ... } else {
       extra_hours };) VOLLSTAENDIG loeschen. Die lebenden extra_hours fliessen ab jetzt
       ungefiltert (deleted-IS-NULL-Filterung passiert bereits im DAO — D-02).

    3. Den conditional derive_hours_for_range-Block (Z. 513-541) ersetzen durch den
       UNCONDITIONAL-Call (verbatim aus PATTERNS.md Z. 72-94):

         let derived = self
             .absence_service
             .derive_hours_for_range(
                 from_date.to_date(),
                 to_date.to_date(),
                 *sales_person_id,
                 context.clone(),
                 tx.clone(),
             )
             .await?;
         let mut absence_derived_vacation_hours = 0.0_f32;
         let mut absence_derived_sick_leave_hours = 0.0_f32;
         let mut absence_derived_unpaid_leave_hours = 0.0_f32;
         for resolved in derived.values() {
             match resolved.category {
                 AbsenceCategory::Vacation => absence_derived_vacation_hours += resolved.hours,
                 AbsenceCategory::SickLeave => absence_derived_sick_leave_hours += resolved.hours,
                 AbsenceCategory::UnpaidLeave => absence_derived_unpaid_leave_hours += resolved.hours,
             }
         }

    4. EmployeeReport-Konstruktion (Z. 642-674): die drei Felder vacation_hours /
       sick_leave_hours / unpaid_leave_hours auf additive Form bringen (verbatim PATTERNS.md
       Z. 100-117):

         vacation_hours: extra_hours
             .iter()
             .filter(|eh| eh.category == ExtraHoursCategory::Vacation)
             .map(|eh| eh.amount)
             .sum::<f32>()
             + absence_derived_vacation_hours,
         sick_leave_hours: extra_hours
             .iter()
             .filter(|eh| eh.category == ExtraHoursCategory::SickLeave)
             .map(|eh| eh.amount)
             .sum::<f32>()
             + absence_derived_sick_leave_hours,
         unpaid_leave_hours: extra_hours
             .iter()
             .filter(|eh| eh.category == ExtraHoursCategory::UnpaidLeave)
             .map(|eh| eh.amount)
             .sum::<f32>()
             + absence_derived_unpaid_leave_hours,

       (Pruefe das exakte Feld-Mapping eh.amount vs eh.hours gegen den bestehenden Code —
       uebernimm den im Live-Code verwendeten Feldnamen fuer den ExtraHours-Betrag.)

    5. FeatureFlagService-Dep-Entfernung (Fallstrick 1 / Offene Frage 1, A1):
       Fuehre `grep -n "feature_flag_service" service_impl/src/reporting.rs` aus.
       - Wenn 0 echte Verwendungen verbleiben (ausser Dep-Deklaration Z. 71 + Import Z. 12):
         entferne die Dep-Zeile aus gen_service_impl! (Z. 71), den Import (Z. 12) UND passe
         die ReportingServiceImpl-DI-Konstruktion in shifty_bin/src/main.rs an
         (feature_flag_service-Argument streichen). Passe auch das ReportingMocks/TestDeps-
         Bundle in reporting_additive_merge.rs UND reporting_flag_on_integration.rs an
         (feature_flag_service-Feld + Type entfernen) — sonst Compile-Fehler.
       - Wenn >0 echte Verwendungen verbleiben: Dep + Import BEHALTEN; main.rs nicht aendern;
         die Test-Mocks behalten das times(0)-is_enabled wie in Plan 01 gesetzt.
       Dokumentiere im SUMMARY, welcher Zweig genommen wurde.

    SCHRITT B — service_impl/src/billing_period_report.rs Snapshot-Bump (D-04):

    6. Z. 50 pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 4; aendern zu = 5;.
       Den History-Kommentar (Z. 39-49) um den v5-Eintrag erweitern (verbatim PATTERNS.md
       Z. 170-176):
         /// - v5: Phase 8.4 — Additiver Merge: Vacation/SickLeave/UnpaidLeave werden
         ///   nun aus BEIDEN Quellen (lebende `extra_hours` + `absence_period` via
         ///   `derive_hours_for_range`) summiert statt quellen-exklusiv per Flag.
         ///   Aeltere Snapshots (v4) haben die `extra_hours`-Seite nicht mitgezaehlt
         ///   (Flag war on -> nur absence_period) oder umgekehrt (Flag off -> nur
         ///   extra_hours). Ein Validator kann v4-Snapshots nicht sicher re-validieren.

    7. cargo build + `cargo test -p service_impl reporting_additive` + `cargo test -p
       service_impl billing_period_snapshot_locking` ausfuehren — alle gruen.

    8. EINEN jj-Commit erstellen, der Reporting-Aenderung + Snapshot-Bump zusammen enthaelt:
         jj commit -m "feat(08.4): additive reporting merge + snapshot schema bump 4->5"
       NUR jj verwenden — NIEMALS git commit / git add / gsd-sdk query commit.
       (use_worktrees=false — Executor arbeitet direkt auf dem Haupt-Tree.)
  </action>
  <acceptance_criteria>
    - grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 5" service_impl/src/billing_period_report.rs returns 1.
    - grep -c "use_absence_range_source" service_impl/src/reporting.rs returns 0.
    - grep -c "absence_derived_vacation_hours" service_impl/src/reporting.rs returns >=2 (Init + Addition).
    - grep -v '^[[:space:]]*//' service_impl/src/reporting.rs piped to grep -c "absence_range_source_active" returns 0 (kein Flag-Read im Code).
    - cargo test -p service_impl reporting_additive exits 0.
    - cargo test -p service_impl billing_period_snapshot_locking exits 0.
    - cargo build --workspace exits 0 (keine unused-import-Warning auf FeatureFlagService, falls Dep entfernt).
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p service_impl reporting_additive billing_period_snapshot_locking 2>&1 | tail -5 ; test "$(grep -c 'CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 5' service_impl/src/billing_period_report.rs)" = "1" && test "$(grep -c use_absence_range_source service_impl/src/reporting.rs)" = "0" && echo MERGE_OK</automated>
  </verify>
  <done>reporting.rs summiert additiv ohne Flag-Branch; FeatureFlagService-Dep-Status dokumentiert (entfernt oder behalten je nach grep); CURRENT_SNAPSHOT_SCHEMA_VERSION = 5; Locking-Test + additive Tests gruen; EIN jj-Commit fuer Reporting+Bump.</done>
</task>

<task type="auto">
  <name>Task 2: Schreibsperre-Rueckbau in extra_hours.rs + Full-Suite-Gate</name>
  <files>service_impl/src/extra_hours.rs</files>
  <read_first>
    - service_impl/src/extra_hours.rs (Z. 197-225 — ExtraHoursCategoryDeprecated-Gate; pruefe, dass der normale Permission-Check DAVOR/DANACH erhalten bleibt)
    - .planning/phases/08.4-reporting-additiv-merge/08.4-PATTERNS.md § "extra_hours.rs — Schreibsperre-Rueckbau"
    - .planning/phases/08.4-reporting-additiv-merge/08.4-RESEARCH.md § 2, Assumption A2
  </read_first>
  <action>
    1. service_impl/src/extra_hours.rs: Den gesamten Gate-Block (Z. 197-225) loeschen — den
       erklaerenden Kommentar-Block (Z. 197-205) UND den
       if matches!(extra_hours.category, Vacation | SickLeave | UnpaidLeave) { let flag_active
       = ... is_enabled("absence_range_source_active", ...); if flag_active { return
       Err(ServiceError::ExtraHoursCategoryDeprecated(...)); } }-Block (Z. 206-225).

       WICHTIG (Security, D-03): NUR das Deprecation/Flag-Gate entfernen. Der normale
       Permission-Gate auf extra_hours create (permission_service.check_permission(...) bzw.
       der RBAC-Privileg-Check oberhalb) MUSS erhalten bleiben — er ist eine SEPARATE Pruefung.
       Verifiziere nach dem Edit per grep, dass der Permission-Check noch im create-Pfad steht.

       Der FeatureFlagService-Dep in ExtraHoursServiceImpl BLEIBT (andere Pfade nutzen ihn —
       RESEARCH § 2). Den ExtraHoursCategoryDeprecated-ServiceError-Variant + rest/src/lib.rs-
       Arm NICHT entfernen (toter Code bis 8.6 — A2).

    2. cargo build --workspace + `cargo test -p service_impl extra_hours` ausfuehren — gruen
       (Plan-01-Tests create_vacation_always_succeeds / create_extra_work_does_not_trigger_
       flag_check passen jetzt).

    3. PHASE-GATE: `cargo test --workspace` ausfuehren — vollstaendig gruen (inkl.
       shifty_bin-Integration-Tests, die in Plan 01 bereinigt wurden).

    4. SMOKE: `cargo run` mit Timeout (z.B. 30s) starten, pruefen dass der Server ohne Panic
       hochkommt, dann beenden.

    5. jj-Commit:
         jj commit -m "feat(08.4): remove extra_hours vacation/sick/unpaid write-gate (D-03)"
       NUR jj — NIEMALS git/gsd-sdk query commit.
  </action>
  <acceptance_criteria>
    - grep -c "ExtraHoursCategoryDeprecated" service_impl/src/extra_hours.rs returns 0 (Gate weg).
    - grep -c "absence_range_source_active" service_impl/src/extra_hours.rs returns 0 (Flag-Read im create-Pfad weg).
    - grep -cE "check_permission|verify_user_is_sales_person|Privilege" service_impl/src/extra_hours.rs returns >=1 (normaler Permission-Gate erhalten).
    - cargo test -p service_impl extra_hours exits 0.
    - cargo test --workspace exits 0 (Phase-Gate).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c ExtraHoursCategoryDeprecated service_impl/src/extra_hours.rs)" = "0" && test "$(grep -c absence_range_source_active service_impl/src/extra_hours.rs)" = "0" && cargo test --workspace 2>&1 | tail -8 && echo GATE_DONE</automated>
  </verify>
  <done>Gate-Block aus extra_hours.rs entfernt, normaler Permission-Check erhalten; cargo test --workspace gruen; cargo run Smoke ok; jj-Commit gesetzt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> REST extra_hours create | Untrusted Input (neue Vacation/SickLeave/UnpaidLeave-Eintraege) kreuzt hier nach Gate-Rueckbau wieder ungehindert (D-03). |
| Service -> Reporting-Aggregation | Aggregation ueber Sales-Person-gescopte Daten; darf keine fremden Stunden mischen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.4-02 | Elevation of Privilege | extra_hours.rs create (Gate-Rueckbau) | accept | NUR das Deprecation/Flag-Gate wird entfernt. Der normale RBAC-Permission-Check (permission_service.check_permission / verify_user_is_sales_person) im create-Pfad bleibt erhalten und gated weiterhin Schreibzugriff — Task 2 acceptance_criteria verifiziert dessen Verbleib per grep. Kein neuer Authorization-Bypass: das entfernte Gate war ein Deprecations-Lock, kein Privileg-Check. INFORMATIONAL. |
| T-08.4-03 | Information Disclosure | reporting.rs additiver Merge | accept | Der additive Merge addiert ausschliesslich innerhalb der bereits per sales_person_id gescopten Quellen (extra_hours via find_by_sales_person_id_and_year_range, absence via derive_hours_for_range(.., sales_person_id, ..)). Beide Calls tragen die sales_person_id-Scoping-Garantie unveraendert; der Merge fuehrt keine Cross-Sales-Person-Aggregation ein. Reporting-Tests (test_additive_*) verifizieren korrekte Per-Person-Summen. LOW. |
| T-08.4-04 | Tampering | Snapshot-Schema-Versionierung (4 -> 5) | mitigate | Bump ist die Mitigation: ohne ihn koennte ein Validator Modelldrift nicht von Daten-Bug unterscheiden. Locking-Test pinnt 5; Bump + Reporting-Aenderung in EINEM jj-Commit (CLAUDE.md-Regel) verhindert Versionsdrift zwischen Code und Snapshot. |

Keine high-Threats: die Surface ist eng (interne Reporting-Computation + Entfernen eines Deprecation-Locks). Der RBAC-Permission-Check bleibt intakt; Sales-Person-Scoping bleibt erhalten. Gate passiert (ASVS L1, kein high).
</threat_model>

<verification>
- Task 1: reporting_additive_merge + billing_period_snapshot_locking gruen; kein use_absence_range_source mehr; Snapshot-Const = 5.
- Task 2: extra_hours-Tests gruen; kein ExtraHoursCategoryDeprecated/absence_range_source_active in extra_hours.rs create-Pfad; Permission-Check erhalten.
- Phase-Gate: cargo test --workspace vollstaendig gruen; cargo run Smoke ohne Panic.
</verification>

<success_criteria>
- SC-1: reporting.rs additiv (extra_hours deleted IS NULL + derive_hours_for_range), kein Flag-Filter, keine Doppelzaehlung konvertierter Rows. Vier additive Tests gruen.
- SC-2: extra_hours-Schreibsperre entfernt; Vacation/SickLeave/UnpaidLeave wieder anlegbar.
- SC-3: CURRENT_SNAPSHOT_SCHEMA_VERSION = 5; Locking-Test gruen.
- SC-4: cargo test --workspace gruen; Billing-Period-Snapshot-Regression sauber (alte Snapshots als older schema markiert via Versionsvergleich).
</success_criteria>

<output>
Nach Abschluss: .planning/phases/08.4-reporting-additiv-merge/08.4-02-SUMMARY.md erstellen.
</output>
