---
phase: 08.4-reporting-additiv-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service_impl/src/test/reporting_flag_off_bit_identity.rs
  - service_impl/src/test/reporting_flag_on_integration.rs
  - service_impl/src/test/reporting_additive_merge.rs
  - service_impl/src/test/billing_period_snapshot_locking.rs
  - service_impl/src/test/extra_hours.rs
  - service_impl/src/test/mod.rs
  - shifty_bin/src/integration_test/cutover.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Die Test-Suite kompiliert nach der Code-Aenderung (Plan 02) ohne broken-compile-Fenster, weil kein Test mehr das alte Flag/Gate-Modell asserted."
    - "Eine neue Test-Datei reporting_additive_merge.rs existiert mit vier additiven Szenarien (nur extra_hours / nur absence_period / beide / soft-deleted zaehlen nicht doppelt)."
    - "Der Snapshot-Locking-Test erwartet Version 5 statt 4."
    - "Kein Test asserted mehr ExtraHoursCategoryDeprecated oder den times(0)-Trap auf derive_hours_for_range."
  artifacts:
    - path: "service_impl/src/test/reporting_additive_merge.rs"
      provides: "Vier additive Reporting-Szenarien (SC-1)"
      min_lines: 120
      contains: "derive_hours_for_range"
    - path: "service_impl/src/test/billing_period_snapshot_locking.rs"
      provides: "Snapshot-Version-Pin auf 5 (SC-3)"
      contains: "CURRENT_SNAPSHOT_SCHEMA_VERSION, 5"
    - path: "service_impl/src/test/mod.rs"
      provides: "Registrierung reporting_additive_merge, Deregistrierung reporting_flag_off_bit_identity"
      contains: "pub mod reporting_additive_merge"
  key_links:
    - from: "service_impl/src/test/mod.rs"
      to: "service_impl/src/test/reporting_additive_merge.rs"
      via: "pub mod reporting_additive_merge;"
      pattern: "pub mod reporting_additive_merge"
    - from: "service_impl/src/test/reporting_additive_merge.rs"
      to: "ReportingServiceImpl"
      via: "ReportingMocks build()"
      pattern: "ReportingServiceImpl"
---

<objective>
Wave 0 — destruktive Test-Bereinigung. Diese Phase entfernt das globale Flag/Gate-Modell aus
Reporting und extra_hours (Plan 02). Mehrere bestehende Tests asserten exakt dieses Modell und
brechen in dem Moment, in dem der Produktionscode geaendert wird (insbesondere
reporting_flag_off_bit_identity.rs mit derive_hours_for_range().times(0) und die
ExtraHoursCategoryDeprecated-Gate-Tests).

Dieser Plan loescht/ersetzt die brechenden Tests und legt die neue additive Test-Datei an,
BEVOR der Produktionscode geaendert wird — damit nie ein broken-compile-Fenster entsteht.

Purpose: Sequencing-Constraint aus RESEARCH § "Offene Fragen" / VALIDATION § "Wave 0". Tests
zuerst, Code danach (Plan 02). Die neuen additiven Tests sind RED bis Plan 02 landet — das ist
erwartet und gewollt (Test-first fuer den additiven Merge).
Output: Bereinigte Test-Suite + neue Datei reporting_additive_merge.rs (4 Szenarien),
Snapshot-Locking-Test auf 5, mod.rs aktualisiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.4-reporting-additiv-merge/08.4-CONTEXT.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-RESEARCH.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-PATTERNS.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-VALIDATION.md

<interfaces>
<!-- Aus Live-Code extrahiert. Executor braucht hierfuer KEINE Codebase-Exploration. -->

ResolvedAbsence (service::absence):
  pub struct ResolvedAbsence { pub category: AbsenceCategory, pub hours: f32 }
  pub enum AbsenceCategory { Vacation, SickLeave, UnpaidLeave }

derive_hours_for_range (MockAbsenceService — wird gemockt):
  async fn derive_hours_for_range(
      &self, from: Date, to: Date, sales_person_id: Uuid,
      context: Authentication<Context>, tx: Option<Transaction>,
  ) -> Result<BTreeMap<time::Date, ResolvedAbsence>, ServiceError>

ExtraHours (service::extra_hours):
  pub struct ExtraHours {
      pub id: Uuid, pub sales_person_id: Uuid, pub amount: f32,
      pub category: ExtraHoursCategory, pub description: Arc<str>,
      pub date_time: time::PrimitiveDateTime, pub created: Option<PrimitiveDateTime>,
      pub deleted: Option<PrimitiveDateTime>, pub version: Uuid,
  }
  pub enum ExtraHoursCategory { Vacation, SickLeave, UnpaidLeave, ExtraWork, Holiday, /* ... */ }

ReportingMocks-Bundle + TestDeps-Impl + make_extra_hours-Fixture: vollstaendig dokumentiert
in 08.4-PATTERNS.md § "reporting_additive_merge.rs — NEUE Testdatei" (Imports Z. 189-213,
Struct/TestDeps Z. 218-268, Fixture Z. 273-285, Boilerplate Z. 290-319). Copy-Pattern aus
service_impl/src/test/reporting_flag_on_integration.rs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brechende Flag/Gate-Tests loeschen und mod.rs bereinigen</name>
  <files>service_impl/src/test/reporting_flag_off_bit_identity.rs, service_impl/src/test/mod.rs, service_impl/src/test/extra_hours.rs, shifty_bin/src/integration_test/cutover.rs</files>
  <read_first>
    - service_impl/src/test/reporting_flag_off_bit_identity.rs (wird geloescht — vorher lesen, um times(0)-Trap zu bestaetigen)
    - service_impl/src/test/extra_hours.rs (Z. 614-682 — drei betroffene Tests)
    - shifty_bin/src/integration_test/cutover.rs (Z. 660-789 — zwei betroffene Abschnitte)
    - service_impl/src/test/mod.rs (Z. 26, 28 — Registrierungen)
    - .planning/phases/08.4-reporting-additiv-merge/08.4-PATTERNS.md § "reporting_flag_off_bit_identity.rs — LOESCHEN", § "extra_hours.rs", § "cutover.rs"
    - .planning/phases/08.4-reporting-additiv-merge/08.4-RESEARCH.md § "Bestehende Tests — Analyse und Handlungsbedarf"
  </read_first>
  <action>
    Fuehre die folgenden destruktiven Bereinigungen aus (alle aus RESEARCH/PATTERNS verifiziert):

    1. Datei service_impl/src/test/reporting_flag_off_bit_identity.rs VOLLSTAENDIG loeschen.
       Grund: asserted derive_hours_for_range().times(0) — bricht sofort nach Plan 02
       (additiver Merge ruft derive_hours_for_range immer auf).

    2. service_impl/src/test/mod.rs: Zeile `pub mod reporting_flag_off_bit_identity;`
       (Z. 26) entfernen. NICHT entfernen: `reporting_flag_on_integration` (Z. 28, wird in
       Task 2 umgebaut) und `reporting_phase2_fixtures` (Z. 30, liefert fixture_sales_person).

    3. service_impl/src/test/extra_hours.rs:
       - Test `create_vacation_returns_403_error_variant_when_flag_on` (~Z. 640-662)
         VOLLSTAENDIG loeschen. Grund: ExtraHoursCategoryDeprecated wird nach Plan 02 nie
         mehr geworfen.
       - Test `create_vacation_succeeds_when_flag_off` (~Z. 618-638) umbenennen zu
         `create_vacation_always_succeeds`. Den
         `feature_flag_service.expect_is_enabled()`-Setup fuer `absence_range_source_active`
         entfernen und durch `deps.feature_flag_service.expect_is_enabled().times(0);`
         ersetzen (explizit machen, dass der Flag fuer Vacation nicht mehr gelesen wird). Die
         create(Vacation)-Assertion bleibt Ok.
       - Test `create_extra_work_succeeds_when_flag_on` (~Z. 664-682) umbenennen zu
         `create_extra_work_does_not_trigger_flag_check`. Inhalt bleibt: times(0) auf
         is_enabled ist weiter korrekt (ExtraWork triggert nie den Flag-Check).

    4. shifty_bin/src/integration_test/cutover.rs:
       - Block "AFTER: Vacation create rejected with ExtraHoursCategoryDeprecated"
         (~Z. 668-728): den
         `assert!(matches!(... ExtraHoursCategoryDeprecated ...))`-Abschnitt loeschen. Falls
         der umgebende Test danach noch Vacation-Create aufruft, die Assertion auf Ok aendern;
         falls der Test dadurch leer/sinnlos wird, den ganzen Test entfernen.
       - `#[tokio::test] test_403_body_format_for_deprecated_category` (~Z. 735-789)
         VOLLSTAENDIG loeschen. Grund: flippt den Flag manuell und erwartet
         ExtraHoursCategoryDeprecated — kommt nach Gate-Rueckbau nie mehr.

    Hinweis: Der ExtraHoursCategoryDeprecated-ServiceError-Variant und der
    rest/src/lib.rs-Handler-Arm bleiben in 8.4 als toter Code stehen (RESEARCH Assumption A2 —
    Entfernung erst in 8.6). NICHT anfassen.

    Diese Datei kompiliert nach diesem Task noch NICHT vollstaendig grun, weil Task 2 die
    neue Datei und den mod.rs-Eintrag ergaenzt und Plan 02 erst den Produktionscode aendert.
    Das ist erwartet — die acceptance_criteria pruefen reine grep-Zustaende, keinen Build.
  </action>
  <acceptance_criteria>
    - `test ! -f service_impl/src/test/reporting_flag_off_bit_identity.rs` exits 0 (Datei weg).
    - `grep -c reporting_flag_off_bit_identity service_impl/src/test/mod.rs` returns 0.
    - `grep -c create_vacation_returns_403_error_variant_when_flag_on service_impl/src/test/extra_hours.rs` returns 0.
    - `grep -c create_vacation_always_succeeds service_impl/src/test/extra_hours.rs` returns >=1.
    - `grep -c test_403_body_format_for_deprecated_category shifty_bin/src/integration_test/cutover.rs` returns 0.
    - `grep -c "matches.*ExtraHoursCategoryDeprecated" shifty_bin/src/integration_test/cutover.rs` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f service_impl/src/test/reporting_flag_off_bit_identity.rs && [ "$(grep -c reporting_flag_off_bit_identity service_impl/src/test/mod.rs)" = "0" ] && [ "$(grep -c create_vacation_returns_403_error_variant_when_flag_on service_impl/src/test/extra_hours.rs)" = "0" ] && [ "$(grep -c test_403_body_format_for_deprecated_category shifty_bin/src/integration_test/cutover.rs)" = "0" ] && echo CLEANUP_OK</automated>
  </verify>
  <done>reporting_flag_off_bit_identity.rs geloescht + aus mod.rs entfernt; extra_hours.rs Test 2 geloescht, Tests 1+3 umbenannt; cutover.rs zwei Deprecated-Abschnitte entfernt. Keine grep-Treffer mehr auf die alten Flag/Gate-Asserts.</done>
</task>

<task type="auto">
  <name>Task 2: Snapshot-Locking-Test auf 5 + neue additive Test-Datei anlegen + registrieren</name>
  <files>service_impl/src/test/billing_period_snapshot_locking.rs, service_impl/src/test/reporting_additive_merge.rs, service_impl/src/test/reporting_flag_on_integration.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/billing_period_snapshot_locking.rs (Z. 7, 26-35 — Pin auf 4)
    - service_impl/src/test/reporting_flag_on_integration.rs (vollstaendig — Copy-Vorlage fuer ReportingMocks/TestDeps/make_extra_hours)
    - service_impl/src/test/reporting_phase2_fixtures.rs (fixture_sales_person, fixture_sales_person_id)
    - service_impl/src/test/mod.rs (Registrierungs-Pattern)
    - .planning/phases/08.4-reporting-additiv-merge/08.4-PATTERNS.md § "reporting_additive_merge.rs — NEUE Testdatei" (Imports/Struct/Fixture/4 Szenarien), § "billing_period_snapshot_locking.rs — Version-Pin Update"
  </read_first>
  <action>
    1. billing_period_snapshot_locking.rs: In `test_snapshot_schema_version_pinned`
       (~Z. 26-35) `assert_eq!(CURRENT_SNAPSHOT_SCHEMA_VERSION, 4, ...)` aendern zu
       `assert_eq!(CURRENT_SNAPSHOT_SCHEMA_VERSION, 5, ...)`. Die Nachricht ersetzen durch
       (verbatim aus PATTERNS.md Z. 419-426):
         "CURRENT_SNAPSHOT_SCHEMA_VERSION muss 5 sein nach Phase-8.4-Bump \
          (Trigger: Additiver Merge — Vacation/SickLeave/UnpaidLeave werden \
          aus BEIDEN Quellen summiert: lebende extra_hours + derive_hours_for_range. \
          Aeltere Snapshots koennen nicht sicher re-validiert werden). \
          Siehe CLAUDE.md § Snapshot Schema Versioning."
       Den Datei-Header-Kommentar (~Z. 7) auf die Phase-8.4-Begruendung aktualisieren
       (PATTERNS.md Z. 432-434).
       Hinweis: Dieser Test ist nach diesem Task RED, bis Plan 02 die Const auf 5 bumpt —
       das ist gewollt (Test-first).

    2. Neue Datei service_impl/src/test/reporting_additive_merge.rs anlegen. Verwende das
       vollstaendige ReportingMocks/TestDeps/build()/make_extra_hours-Pattern aus PATTERNS.md
       § "reporting_additive_merge.rs — NEUE Testdatei" (Imports Z. 189-213, Struct/TestDeps
       Z. 218-268, Fixture Z. 273-285, Boilerplate Z. 290-319). Kopiere die Mock-Struktur 1:1
       aus reporting_flag_on_integration.rs.

       Kritische Mock-Erwartungen fuer JEDES der vier Szenarien:
       - KEIN feature_flag_service.expect_is_enabled() fuer absence_range_source_active mehr.
         Stattdessen explizit `mocks.feature_flag_service.expect_is_enabled().times(0);`
         (Plan 02 entfernt den Flag-Read im Reporting-Pfad).
       - mocks.absence_service.expect_derive_hours_for_range() wird IMMER erwartet
         (times(1), auch wenn extra_hours leer) — KEIN times(0)-Trap.
       - mocks.extra_hours_service.expect_find_by_sales_person_id_and_year_range() wird immer
         erwartet (kann leeren Slice liefern).
       - Standard-Boilerplate (permission/sales_person/employee_work_details/shiftplan_report/
         carryover/transaction) aus PATTERNS.md Z. 290-319.

       Implementiere genau diese vier #[tokio::test]-Funktionen
       (Werte verbatim aus PATTERNS.md Z. 329-362):

       a) test_additive_only_extra_hours:
          derive_hours_for_range -> leere BTreeMap.
          extra_hours -> [Vacation 8h, SickLeave 4h, UnpaidLeave 2h].
          assert: vacation_hours == 8.0, sick_leave_hours == 4.0, unpaid_leave_hours == 2.0.

       b) test_additive_only_absence_period:
          derive_hours_for_range -> {2024-06-03: Vacation 8h, 2024-06-04: SickLeave 8h}.
          extra_hours -> leerer Slice.
          assert: vacation_hours == 8.0, sick_leave_hours == 8.0, unpaid_leave_hours == 0.0.

       c) test_additive_both_sources_distinct_days (Kern-SC-1):
          derive_hours_for_range -> {2024-06-03: Vacation 8h}.
          extra_hours -> [Vacation 4h am 2024-06-05].
          assert: vacation_hours == 12.0.

       d) test_additive_converted_rows_not_double_counted:
          extra_hours -> leerer Slice (soft-deleted Rows fallen per deleted IS NULL im DAO
            heraus -> Mock liefert []).
          derive_hours_for_range -> {2024-06-03: Vacation 8h}.
          assert: vacation_hours == 8.0 (nur einmal).
          Kommentar im Test: macht die strukturelle per-row-Invariante (D-02) explizit; kein
          neuer DAO-Code noetig.

       Verwende fuer den Reporting-Aufruf dieselbe Methode wie reporting_flag_on_integration.rs
       (get_report_for_employee bzw. die dort genutzte Range-Methode) — uebernimm exakt deren
       Call-Signatur und from/to-Range.

    3. service_impl/src/test/mod.rs: `pub mod reporting_additive_merge;` ergaenzen (gleiches
       cfg(test)-Pattern wie die uebrigen Reporting-Test-Module).
  </action>
  <acceptance_criteria>
    - `grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION, 5" service_impl/src/test/billing_period_snapshot_locking.rs` returns >=1.
    - `grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION, 4" service_impl/src/test/billing_period_snapshot_locking.rs` returns 0.
    - `test -f service_impl/src/test/reporting_additive_merge.rs` exits 0.
    - `grep -c "test_additive_only_extra_hours\|test_additive_only_absence_period\|test_additive_both_sources_distinct_days\|test_additive_converted_rows_not_double_counted" service_impl/src/test/reporting_additive_merge.rs` returns 4.
    - `grep -c "expect_derive_hours_for_range" service_impl/src/test/reporting_additive_merge.rs` returns >=4.
    - `grep -c "pub mod reporting_additive_merge" service_impl/src/test/mod.rs` returns 1.
    - `grep -v '^#' service_impl/src/test/reporting_additive_merge.rs | grep -c "expect_is_enabled().times(0)\|times(0)"` returns >=1 (kein Flag-Read erwartet).
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -c 'CURRENT_SNAPSHOT_SCHEMA_VERSION, 5' service_impl/src/test/billing_period_snapshot_locking.rs)" -ge 1 ] && test -f service_impl/src/test/reporting_additive_merge.rs && [ "$(grep -c 'pub mod reporting_additive_merge' service_impl/src/test/mod.rs)" = "1" ] && [ "$(grep -cE 'test_additive_only_extra_hours|test_additive_only_absence_period|test_additive_both_sources_distinct_days|test_additive_converted_rows_not_double_counted' service_impl/src/test/reporting_additive_merge.rs)" = "4" ] && echo SCAFFOLD_OK</automated>
  </verify>
  <done>Snapshot-Locking-Test pinnt 5; reporting_additive_merge.rs existiert mit 4 additiven Szenarien (jeweils mit unconditional derive_hours_for_range-Mock und times(0)-is_enabled); in mod.rs registriert. Tests sind RED bis Plan 02 (gewollt).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (keine neue) | Reine Test-Bereinigung. Es wird kein neuer Trust-Boundary eingefuehrt; keine externen Inputs, keine REST-Surface beruehrt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.4-01 | Tampering | Test-Coverage-Regression durch Loeschen von Tests | accept | Loeschungen sind durch die neue additive Test-Datei (4 Szenarien) und den Locking-Test ueberdeckt; Coverage der relevanten Verhaltensweisen (SC-1, SC-3) bleibt erhalten. Kein Produktionscode in diesem Plan. |

Keine high/medium-Threats: dieser Plan veraendert ausschliesslich Test-Dateien (`service_impl/src/test/`, `shifty_bin/src/integration_test/`). Keine Authentifizierung, keine Aggregation ueber Sales-Person-Grenzen, keine REST-Handler beruehrt. Gate passiert (ASVS L1, kein high).
</threat_model>

<verification>
- Task 1 acceptance_criteria gruen (alle alten Flag/Gate-Asserts entfernt).
- Task 2 acceptance_criteria gruen (Locking-Pin auf 5, neue Datei mit 4 Szenarien registriert).
- HINWEIS: `cargo test` ist nach diesem Plan absichtlich NICHT vollstaendig gruen — die neuen
  additiven Tests + der Locking-Test sind RED bis Plan 02 den Produktionscode aendert. Der
  Build muss aber nach Plan 01+02 zusammen ohne broken-compile-Fenster sein. Plan 02 fuehrt
  `cargo test --workspace` als Phase-Gate aus.
</verification>

<success_criteria>
- reporting_flag_off_bit_identity.rs geloescht + dereregistriert.
- reporting_additive_merge.rs mit vier additiven Szenarien angelegt + registriert (SC-1 Test-Seite).
- billing_period_snapshot_locking.rs pinnt Version 5 (SC-3 Test-Seite).
- Keine ExtraHoursCategoryDeprecated-Asserts und kein derive_hours_for_range().times(0)-Trap mehr in der Suite.
</success_criteria>

<output>
Nach Abschluss: .planning/phases/08.4-reporting-additiv-merge/08.4-01-SUMMARY.md erstellen.
</output>
