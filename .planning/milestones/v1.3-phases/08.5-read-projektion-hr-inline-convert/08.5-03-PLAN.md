---
phase: 08.5-read-projektion-hr-inline-convert
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - rest-types/src/lib.rs
  - rest/src/extra_hours.rs
  - rest/src/lib.rs
  - shifty_bin/src/main.rs
  - rest/tests/openapi_surface.rs
  - rest/tests/convert_to_absence.rs
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "POST /extra-hours/{id}/convert-to-absence wandelt eine stundenbasierte Row in ein absence_period um (200 + AbsencePeriodTO)"
    - "Ohne hr-Privileg liefert der Endpoint 403"
    - "Endpoint + ConvertExtraHoursRequestTO erscheinen im OpenAPI-Surface-Test"
  artifacts:
    - path: "rest/src/extra_hours.rs"
      provides: "convert_extra_hours_to_absence Handler + #[utoipa::path] + Route"
      contains: "convert_extra_hours_to_absence"
    - path: "rest-types/src/lib.rs"
      provides: "ConvertExtraHoursRequestTO + ExtraHoursMarkerTO + AbsenceListWithProjectionTO"
      contains: "ConvertExtraHoursRequestTO"
    - path: "rest/tests/openapi_surface.rs"
      provides: "Surface-Assertion für neuen Endpoint + 3 Schemas"
      contains: "convert-to-absence"
  key_links:
    - from: "rest/src/extra_hours.rs"
      to: "absence_conversion_service().convert_extra_hours_to_absence"
      via: "Handler dispatch nach ISO-8601-Parse am Edge"
      pattern: "convert_extra_hours_to_absence"
    - from: "shifty_bin/src/main.rs"
      to: "AbsenceConversionServiceImpl"
      via: "DI-Wiring BL-Tier nach Basic-Services"
      pattern: "AbsenceConversionServiceImpl"
---

<objective>
Verdrahte den Convert-Endpoint `POST /extra-hours/{id}/convert-to-absence` (D-13): neuer Handler in `rest/src/extra_hours.rs`, drei neue DTOs in rest-types (`ConvertExtraHoursRequestTO`, plus die für Plan 04 benötigten `ExtraHoursMarkerTO` + `AbsenceListWithProjectionTO`), `RestStateDef`-Erweiterung um `AbsenceConversionService`, DI-Wiring des `AbsenceConversionServiceImpl` (BL-Tier nach Basic-Services + MigrationSourceDaoImpl) in `shifty_bin/src/main.rs`, sowie OpenAPI-`#[utoipa::path]` + `ToSchema` + Surface-Test-Erweiterung (D-13, D-06). Privileg `hr` wird im Service erzwungen (Plan 02); dieser Plan reicht echtes Context durch.

Purpose: SC2 + SC6 — HR konvertiert per Endpoint außerhalb des cutover-Namespace; OpenAPI dokumentiert + surface-gepinnt.
Output: 3 DTOs, Handler+Route, DI-Wiring, Integration-Test (200 + 403), Surface-Test-Einträge.

Dieser Plan legt ALLE drei rest-types-DTOs an, damit Plan 04 (Read-Projektion) sie nur konsumiert (kein rest-types-Konflikt).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-02-SUMMARY.md

<interfaces>
From service/src/absence_conversion.rs (Plan 02):
```rust
async fn convert_extra_hours_to_absence(&self, extra_hours_id: Uuid, from_date: time::Date, to_date: time::Date,
    day_fraction: Option<service::absence::DayFraction>, context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>) -> Result<service::absence::AbsencePeriod, ServiceError>;
```

Existing handler analog (rest/src/extra_hours.rs update_extra_hours, PATTERNS.md Z. 235-294):
- `#[instrument(skip(rest_state))]` + `#[utoipa::path(...)]`
- `pub async fn update_extra_hours<RestState: RestStateDef>(rest_state: State<RestState>, Extension(context): Extension<Context>, Path(id): Path<Uuid>, Json(to): Json<ExtraHoursTO>) -> Response`
- body wrapped in `error_handler((async { ... }).await)`

Existing route fn (rest/src/extra_hours.rs:20-28):
```rust
pub fn generate_route<RestState: RestStateDef>() -> Router<RestState> {
    Router::new()
        .route("/", post(create_extra_hours::<RestState>))
        .route("/{id}", put(update_extra_hours::<RestState>))
        .route("/{id}", delete(delete_extra_hours::<RestState>))
        .route("/by-sales-person/{id}", get(get_extra_hours_for_sales_person::<RestState>))
}
```

Existing ExtraHoursApiDoc (rest/src/extra_hours.rs:194-207):
```rust
#[openapi(paths(get_extra_hours_for_sales_person, create_extra_hours, update_extra_hours, delete_extra_hours),
    components(schemas(ExtraHoursTO)), tags((name = "Extra Hours", ...)))]
pub struct ExtraHoursApiDoc;
```

DTO analogs (rest-types/src/lib.rs): AbsencePeriodTO (~1625), ExtraHoursTO (~752), DayFractionTO (~1599-1623), AbsenceCategoryTO, ExtraHoursCategoryTO. Date-Felder: `#[schema(value_type = String, format = "date")]`.

error_handler maps: DateOrderWrong->422, ValidationError->422, Forbidden->403, EntityNotFound->404 (alle bereits vorhanden, keine neuen Arms).

RestStateDef extension (PATTERNS.md Z. 664-675): in rest/src/lib.rs `type AbsenceConversionService: service::absence_conversion::AbsenceConversionService<Context = Context> + Send + Sync + 'static; fn absence_conversion_service(&self) -> Arc<Self::AbsenceConversionService>;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drei DTOs in rest-types/src/lib.rs</name>
  <read_first>
    - rest-types/src/lib.rs:1599-1681 (AbsencePeriodTO + DayFractionTO Pattern, ToSchema, #[schema(value_type=String, format="date")] — PATTERNS.md Z. 300-326)
    - rest-types/src/lib.rs (ExtraHoursTO ~752 + ExtraHoursCategoryTO + AbsenceCategoryTO Definitionen)
  </read_first>
  <action>
    Füge in `rest-types/src/lib.rs` drei neue TOs hinzu (alle `#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]`):

    ```rust
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct ConvertExtraHoursRequestTO {
        #[schema(value_type = String, format = "date")]
        pub start: time::Date,
        #[schema(value_type = String, format = "date")]
        pub end: time::Date,
        #[serde(default)]
        pub day_fraction: Option<DayFractionTO>,
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct ExtraHoursMarkerTO {
        pub extra_hours_id: Uuid,
        pub sales_person_id: Uuid,
        #[schema(value_type = String, format = "date")]
        pub when: time::Date,
        pub amount: f32,
        pub category: ExtraHoursCategoryTO,
        #[serde(default)]
        pub description: Arc<str>,
        #[serde(default)]
        pub person_name: Arc<str>,
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct AbsenceListWithProjectionTO {
        pub absence_periods: Vec<AbsencePeriodTO>,
        pub hourly_markers: Vec<ExtraHoursMarkerTO>,
    }
    ```
    KEINE `#[cfg(feature = "service-impl")]`-Conversions auf diese drei TOs — sie werden im REST-Handler direkt aus Service-Ergebnissen gemappt (das hält den WASM-Build des Frontends sauber, da kein service-Crate hineingezogen wird). Stelle sicher, dass `ExtraHoursCategoryTO`, `AbsenceCategoryTO`, `DayFractionTO`, `Arc`, `Uuid` in lib.rs im Scope sind (sind sie, da AbsencePeriodTO/ExtraHoursTO sie nutzen).
  </action>
  <verify>
    <automated>cargo check -p rest-types 2>&1 | tail -5; grep -q "struct ConvertExtraHoursRequestTO" rest-types/src/lib.rs && grep -q "struct ExtraHoursMarkerTO" rest-types/src/lib.rs && grep -q "struct AbsenceListWithProjectionTO" rest-types/src/lib.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Drei Structs vorhanden mit korrekten Feldern (start/end/day_fraction; extra_hours_id/sales_person_id/when/amount/category/description/person_name; absence_periods/hourly_markers)
    - Alle drei mit `ToSchema`-Derive
    - Date-Felder mit `#[schema(value_type = String, format = "date")]`
    - `cargo check -p rest-types` exit 0
    - WASM-Sanity: `cd shifty-dioxus && nix develop --command cargo check --target wasm32-unknown-unknown -p rest-types` exit 0 (kein service-Crate-Pull-in)
  </acceptance_criteria>
  <done>Drei DTOs in rest-types, ToSchema-fähig, WASM-kompatibel.</done>
</task>

<task type="auto">
  <name>Task 2: Convert-Handler + Route + ApiDoc + RestStateDef + DI-Wiring</name>
  <read_first>
    - rest/src/extra_hours.rs:20-28 (generate_route), :121-207 (update_extra_hours Handler + ExtraHoursApiDoc — PATTERNS.md Z. 231-294)
    - rest/src/lib.rs:275-414 (RestStateDef Trait — bestehende Service-Type-Einträge als Pattern)
    - shifty_bin/src/main.rs:228-720 (DI-Konstruktionsreihenfolge: Basic-Services -> BL-Tier; VacationBalanceService als BL-Referenz; RestStateImpl::new)
    - service/src/absence_conversion.rs (Trait), service/src/absence.rs (DayFraction)
  </read_first>
  <action>
    **(a) Handler** in `rest/src/extra_hours.rs`. Importiere `ConvertExtraHoursRequestTO`, `AbsencePeriodTO` aus rest_types und `service::absence_conversion::AbsenceConversionService`. Neuer Handler analog `update_extra_hours`:
    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post,
        path = "/{id}/convert-to-absence",
        tags = ["Extra Hours"],
        params(("id", description = "Extra hours logical id")),
        request_body = ConvertExtraHoursRequestTO,
        responses(
            (status = 200, description = "Converted absence period", body = AbsencePeriodTO),
            (status = 403, description = "Forbidden — requires hr privilege"),
            (status = 404, description = "Extra hours not found or already soft-deleted"),
            (status = 422, description = "Validation error (DateOrderWrong or OverlappingPeriod)"),
        ),
    )]
    pub async fn convert_extra_hours_to_absence<RestState: RestStateDef>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Path(extra_hours_id): Path<Uuid>,
        Json(body): Json<ConvertExtraHoursRequestTO>,
    ) -> Response {
        error_handler((async {
            let day_fraction = body.day_fraction.as_ref().map(|f| f.into()); // DayFractionTO -> service::absence::DayFraction
            let result = rest_state
                .absence_conversion_service()
                .convert_extra_hours_to_absence(extra_hours_id, body.start, body.end, day_fraction, context.into(), None)
                .await?;
            let to = AbsencePeriodTO::from(&result);
            Ok(Response::builder().status(200).header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&to).unwrap())).unwrap())
        }).await)
    }
    ```
    Hinweis: `body.start`/`body.end` sind bereits `time::Date` (serde parst ISO-8601 am Edge; bei Parse-Fehler liefert axum automatisch 422/400 für den Json-Extractor). Prüfe die genaue `From<&DayFractionTO> for service::absence::DayFraction`-Richtung — falls die Conversion nur als `(&f).into()` verfügbar ist, passe entsprechend an.

    **(b) Route** in `generate_route`: ergänze `.route("/{id}/convert-to-absence", post(convert_extra_hours_to_absence::<RestState>))`.

    **(c) ApiDoc**: erweitere `ExtraHoursApiDoc` um `convert_extra_hours_to_absence` in `paths(...)` und `ConvertExtraHoursRequestTO` in `components(schemas(...))`.

    **(d) RestStateDef** in `rest/src/lib.rs`: ergänze bei den anderen Service-Einträgen:
    ```rust
    type AbsenceConversionService: service::absence_conversion::AbsenceConversionService<Context = Context> + Send + Sync + 'static;
    fn absence_conversion_service(&self) -> Arc<Self::AbsenceConversionService>;
    ```

    **(e) DI-Wiring** in `shifty_bin/src/main.rs` (Service-Tier-Konvention: erst Basic, dann BL):
    - Konstruiere `MigrationSourceDaoImpl::new(pool.clone())` bei den anderen DAO-Impls (Basic-Tier).
    - Definiere einen `AbsenceConversionServiceDependencies`-Struct + `impl service_impl::absence_conversion::AbsenceConversionServiceDeps` (analog zu wie VacationBalanceService verdrahtet ist) mit den 6 Deps (extra_hours_dao, absence_dao, migration_source_dao, extra_hours_service, permission_service, transaction_dao).
    - Konstruiere `AbsenceConversionServiceImpl` NACH allen Basic-Services + dem MigrationSourceDaoImpl (BL-Tier-Position, kein OnceLock/Forward-Decl).
    - Füge `absence_conversion_service: Arc<...>` zu `RestStateImpl` hinzu, wire es in `RestStateImpl::new()`, und implementiere `type AbsenceConversionService` + `fn absence_conversion_service()` im `impl RestStateDef for RestStateImpl`.
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | tail -10; grep -q "convert-to-absence" rest/src/extra_hours.rs && grep -q "absence_conversion_service" rest/src/lib.rs && grep -q "AbsenceConversionServiceImpl" shifty_bin/src/main.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `rest/src/extra_hours.rs` enthält Handler `convert_extra_hours_to_absence` mit `#[utoipa::path]` (post, path `/{id}/convert-to-absence`) + Route registriert + im ApiDoc gelistet (path + schema)
    - `rest/src/lib.rs` RestStateDef enthält `type AbsenceConversionService` + `fn absence_conversion_service`
    - `shifty_bin/src/main.rs` konstruiert `MigrationSourceDaoImpl` (Basic) + `AbsenceConversionServiceImpl` (BL, nach Basic) ohne OnceLock-Tricks
    - `cargo check --workspace` exit 0
  </acceptance_criteria>
  <done>Endpoint live + DI verdrahtet + OpenAPI-dokumentiert; Workspace compiliert.</done>
</task>

<task type="auto">
  <name>Task 3: Integration-Test (200 + 403) + Surface-Test-Erweiterung</name>
  <read_first>
    - rest/tests/openapi_surface.rs:48-208 (EXPECTED_PATHS + EXPECTED_SCHEMAS — PATTERNS.md Z. 359-383)
    - rest/tests/ (vorhandene Integration-Test-Datei als Setup-Pattern für RestState-Test-Harness; falls keine existiert, Setup analog service_impl Integration-Tests + axum-Router)
    - rest/src/extra_hours.rs (Handler aus Task 2)
  </read_first>
  <action>
    **(a) Surface-Test** `rest/tests/openapi_surface.rs`: füge `"/extra-hours/{id}/convert-to-absence"` zu `EXPECTED_PATHS` hinzu und `"ConvertExtraHoursRequestTO"`, `"ExtraHoursMarkerTO"`, `"AbsenceListWithProjectionTO"` zu `EXPECTED_SCHEMAS` (alle drei, da Plan 04 die letzten beiden registriert; falls Plan 04 noch nicht ausgeführt, schlägt der Schema-Assert für die Projektion-TOs fehl — daher: füge nur `ConvertExtraHoursRequestTO` jetzt hinzu, und die beiden Projektion-Schemas kommen in Plan 04 mit der absence.rs-ApiDoc-Registrierung). Konkret HIER nur: Path `/extra-hours/{id}/convert-to-absence` + Schema `ConvertExtraHoursRequestTO`.

    **(b) Integration-Test** `rest/tests/convert_to_absence.rs`: baue ein RestState-Test-Harness (in-memory SQLite + run migrations + alle Service-Impls inkl. AbsenceConversionServiceImpl). Tests:
    - `convert_to_absence_happy_path`: seed Vacation-extra_hours-Row für eine SalesPerson; als hr-berechtigter Context POST `/extra-hours/{id}/convert-to-absence` mit `{start, end}` -> Status 200, Response-Body ist AbsencePeriodTO mit category Vacation.
    - `convert_requires_hr_privilege`: gleicher Request mit einem Context OHNE hr-Privileg -> Status 403.
    Falls das Aufsetzen eines vollständigen RestState-Harness im rest-Crate unverhältnismäßig ist und kein bestehendes Pattern existiert, verschiebe die 403/200-Assertion auf Service-Level (bereits in Plan 02 abgedeckt) und halte hier MINDESTENS den happy-path-200-Test über den echten Router. Kein `#[ignore]`.
  </action>
  <verify>
    <automated>cargo test -p rest convert_to_absence 2>&1 | tail -8; cargo test -p rest openapi_surface 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `rest/tests/openapi_surface.rs` EXPECTED_PATHS enthält `/extra-hours/{id}/convert-to-absence`; EXPECTED_SCHEMAS enthält `ConvertExtraHoursRequestTO`
    - `rest/tests/convert_to_absence.rs` enthält happy-path-200-Test + (sofern Harness es erlaubt) 403-Test, kein `#[ignore]`
    - `cargo test -p rest convert_to_absence` + `cargo test -p rest openapi_surface` exit 0
  </acceptance_criteria>
  <done>Endpoint integration-getestet + surface-gepinnt; 200 + 403 verifiziert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> REST | Untrusted POST-Body `{start, end, day_fraction}` + Path-Uuid; Auth-Context aus Extension |
| REST -> Service | Handler reicht echtes Context durch; Service erzwingt hr-Gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-03a | Elevation of Privilege | convert_extra_hours_to_absence Endpoint | mitigate | Handler reicht `context.into()` durch; Service-hr-Gate (Plan 02); Integration-Test `convert_requires_hr_privilege` asserted 403 |
| T-8.5-03b | Tampering | ISO-8601 start/end im Body | mitigate | serde/axum Json-Extractor parst time::Date am Edge; Parse-Fehler -> 400/422 ohne Service-Aufruf |
| T-8.5-03c | Tampering | Overlap/DateOrder | mitigate | Service liefert DateOrderWrong/ValidationError -> error_handler mappt auf 422 (vorhandene Arms) |
| T-8.5-03d | Information Disclosure | 404 vs 403 Leakage | accept | EntityNotFound (404) vs Forbidden (403) sind bestehende error_handler-Semantik; kein zusätzliches Leak über bestehende Endpoints hinaus |
</threat_model>

<verification>
- `cargo check --workspace` exit 0
- `cargo test -p rest convert_to_absence` + `cargo test -p rest openapi_surface` grün
- OpenAPI 3-run-deterministisch (Surface-Assertion ist set-stabil)
- `cargo test --workspace` grün (Wave-Merge-Gate)
</verification>

<success_criteria>
- Convert-Endpoint live, hr-erzwungen (403 ohne Privileg), 200 mit AbsencePeriodTO
- Drei DTOs in rest-types, OpenAPI dokumentiert + surface-gepinnt
- DI-Wiring konvention-konform (Basic vor BL)
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-03-SUMMARY.md`
</output>
