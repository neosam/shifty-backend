---
phase: 08.5-read-projektion-hr-inline-convert
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - rest/src/absence.rs
  - rest/tests/openapi_surface.rs
  - rest/tests/absence_projection.rs
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "GET /absence-period liefert AbsenceListWithProjectionTO mit hourly_markers für lebende Vacation/Sick/Unpaid extra_hours"
    - "GET /absence-period/by-sales-person/{id} liefert dieselbe Wrapper-Shape, gescoped auf die Person"
    - "Marker zeigt Roh-Eintrag am when-Datum (kein Range-Raten); nur Kategorien Vacation/SickLeave/UnpaidLeave"
    - "Projektion folgt hr ∨ self (bestehende /absences-Permission)"
  artifacts:
    - path: "rest/src/absence.rs"
      provides: "Beide GET-Handler augmentiert + ApiDoc schemas"
      contains: "AbsenceListWithProjectionTO"
  key_links:
    - from: "rest/src/absence.rs"
      to: "extra_hours_service.find_by_sales_person_id_and_year_range"
      via: "Marker-Load nach absence find, gefiltert auf 3 Kategorien"
      pattern: "find_by_sales_person_id_and_year_range"
---

<objective>
Erweitere die beiden Absence-Read-Endpoints (`GET /absence-period` + `GET /absence-period/by-sales-person/{id}`) um die Read-Projektion lebender stundenbasierter `extra_hours` (Vacation/SickLeave/UnpaidLeave) als Roh-Marker am `when`-Datum — KEIN Range-Raten (D-07). Beide Endpoints geben jetzt `AbsenceListWithProjectionTO { absence_periods, hourly_markers }` zurück (Wrapper-TO aus Plan 03). Sichtbarkeit folgt der bestehenden `/absences`-Permission `hr ∨ self` (D-06).

Purpose: SC1 — Absence-Liste blendet lebende Stunden-Urlaube read-only ein, ehrlich am Eintrags-Datum, getrennt typisiert von nativen Ranges.
Output: Beide Handler augmentiert + ApiDoc-Schemas registriert + Integration-Test. Surface-Test erhält die zwei Projektion-Schemas.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-03-SUMMARY.md

<interfaces>
From rest-types/src/lib.rs (Plan 03): `AbsenceListWithProjectionTO`, `ExtraHoursMarkerTO`.

Existing handlers (rest/src/absence.rs:89-106 get_all_absence_periods, :219-238 get_absence_periods_for_sales_person — PATTERNS.md Z. 330-355). Both currently:
```rust
let svc = rest_state.absence_service();
let entities = svc.find_all(context.into(), None).await?;   // or find_by_sales_person(sp_id, ...)
let tos: Vec<AbsencePeriodTO> = entities.iter().map(AbsencePeriodTO::from).collect();
```

RestState accessors available: `rest_state.absence_service()`, `rest_state.extra_hours_service()`, `rest_state.sales_person_service()`.

ExtraHoursService load (service/src/extra_hours.rs:200): `find_by_sales_person_id_and_year_range(&self, sales_person_id: Uuid, from_date: ShiftyDate, to_date: ShiftyDate, context, tx) -> Result<Arc<[ExtraHours]>, ServiceError>` (KEINE Jahr-Beschränkung — vs. find_by_sales_person_id_and_year mit until_week; nutze die _range-Variante, Pitfall 7).
`ShiftyDate` aus `shifty_utils::ShiftyDate`, `ShiftyDate::from(time::Date)`.

ExtraHours fields: `id` (= logical_id), `sales_person_id`, `amount: f32`, `category: ExtraHoursCategory`, `description: Arc<str>`, `date_time: time::PrimitiveDateTime` (-> `.date()` für `when`).

SalesPersonService: `get_all(context, tx)` (HR-View), `get(sp_id, context, tx)`. SalesPerson trägt `name`/Anzeige-Feld für `person_name`.

ExtraHoursCategory variants: ExtraWork, Vacation, SickLeave, Holiday, Unavailable, UnpaidLeave, VolunteerWork, CustomExtraHours. Filter: nur {Vacation, SickLeave, UnpaidLeave}.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: get_all_absence_periods augmentieren (HR-View, alle Personen)</name>
  <read_first>
    - rest/src/absence.rs:79-106 (get_all_absence_periods Handler + #[utoipa::path] — PATTERNS.md Z. 334-352)
    - rest/src/absence.rs:240-260 (AbsenceApiDoc components(schemas(...)))
    - service/src/extra_hours.rs:200-207 (find_by_sales_person_id_and_year_range)
    - service/src/sales_person.rs:55-70 (get_all / get)
  </read_first>
  <action>
    Ändere im `#[utoipa::path]` von `get_all_absence_periods` das Response-`body` von `[AbsencePeriodTO]` zu `AbsenceListWithProjectionTO`.

    Im Handler-Body nach dem `let entities = svc.find_all(context.into(), None).await?;`:
    1. Map Ranges: `let absence_periods: Vec<AbsencePeriodTO> = entities.iter().map(AbsencePeriodTO::from).collect();`
    2. Bestimme das Zwei-Jahres-Fenster (Pitfall 7 / RESEARCH Open Question 2): `current_year - 1` bis `current_year + 1`. Leite `from_bound = ShiftyDate::from(time::Date::from_calendar_date(current_year - 1, time::Month::January, 1)?)` und `to_bound = ShiftyDate::from(time::Date::from_calendar_date(current_year + 1, time::Month::December, 31)?)`. `current_year` aus `time::OffsetDateTime::now_utc().year()`.
    3. Lade alle Personen für die HR-View: `let persons = rest_state.sales_person_service().get_all(context.clone().into(), None).await?;` (Permission-Scope bleibt: find_all/get_all enforced bereits hr∨self bzw. hr; reuse die bestehende Scoping — kein neuer Gate).
    4. Für jede Person `find_by_sales_person_id_and_year_range(person.id, from_bound, to_bound, context.clone().into(), None)`, filtere auf Kategorie ∈ {Vacation, SickLeave, UnpaidLeave}, map zu `ExtraHoursMarkerTO { extra_hours_id: eh.id, sales_person_id: eh.sales_person_id, when: eh.date_time.date(), amount: eh.amount, category: (&eh.category).into(), description: eh.description.clone(), person_name: person.name.clone() }`. Sammle in `hourly_markers: Vec<ExtraHoursMarkerTO>`.
    5. Baue `let result = AbsenceListWithProjectionTO { absence_periods, hourly_markers };` und serialisiere als Response-Body (200, application/json) — ersetze den alten `&tos`-Body.

    WICHTIG: KEIN Range-Raten — jeder Marker trägt `when = date_time.date()`, KEINE Ableitung einer Range. KEIN Lesen von `absence_range_source_active`-Flag.

    Erweitere `AbsenceApiDoc` `components(schemas(...))` um `ExtraHoursMarkerTO` und `AbsenceListWithProjectionTO`.
  </action>
  <verify>
    <automated>cargo check -p rest 2>&1 | tail -6; grep -q "AbsenceListWithProjectionTO" rest/src/absence.rs && grep -q "find_by_sales_person_id_and_year_range" rest/src/absence.rs && ! grep -q "absence_range_source_active" rest/src/absence.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `get_all_absence_periods` gibt `AbsenceListWithProjectionTO` zurück (utoipa body + Handler-Body)
    - hourly_markers nur aus Kategorien {Vacation, SickLeave, UnpaidLeave}; jeder Marker trägt `when = date_time.date()` (kein abgeleiteter Range)
    - `ExtraHoursMarkerTO` + `AbsenceListWithProjectionTO` im AbsenceApiDoc registriert
    - grep `absence_range_source_active` in rest/src/absence.rs liefert 0 Treffer
    - `cargo check -p rest` exit 0
  </acceptance_criteria>
  <done>HR-View liefert Ranges + Stunden-Marker, ehrlich am Datum, gefiltert.</done>
</task>

<task type="auto">
  <name>Task 2: get_absence_periods_for_sales_person augmentieren (Self/HR, eine Person)</name>
  <read_first>
    - rest/src/absence.rs:207-238 (get_absence_periods_for_sales_person Handler — PATTERNS.md Z. 330-355)
    - rest/src/absence.rs (Task 1 Änderungen für konsistentes Marker-Mapping)
  </read_first>
  <action>
    Analog zu Task 1, aber gescoped auf die eine `sales_person_id` aus dem Path:
    - Response-`body` von `[AbsencePeriodTO]` zu `AbsenceListWithProjectionTO`.
    - Nach `find_by_sales_person(sales_person_id, context.into(), None)` (Permission `hr ∨ self` bleibt unverändert — D-06): map Ranges wie gehabt.
    - Lade die eine Person: `let person = rest_state.sales_person_service().get(sales_person_id, context.clone().into(), None).await?;` für `person_name`.
    - Lade Marker NUR für diese `sales_person_id` via `find_by_sales_person_id_and_year_range(sales_person_id, from_bound, to_bound, context.clone().into(), None)`, gleiche Kategorie-Filterung + Marker-Mapping wie Task 1 (Helper-Funktion extrahieren, z.B. `fn map_marker(eh: &ExtraHours, person_name: Arc<str>) -> ExtraHoursMarkerTO`, um Duplikation zu vermeiden).
    - Baue `AbsenceListWithProjectionTO`-Response.

    Die `context.clone().into()`-Weiterreichung stellt sicher, dass die Self-Variante NUR die eigenen Stunden lädt (extra_hours_service enforced sein eigenes Scoping; falls find_by_sales_person_id_and_year_range hr∨self prüft, ist Self-Scope automatisch). Falls der extra_hours-Service hier einen strengeren Gate hat als /absences, dokumentiere im SUMMARY und reiche das Context unverändert durch — KEIN Bypass via Authentication::Full für Self-Reads.
  </action>
  <verify>
    <automated>cargo check -p rest 2>&1 | tail -6; grep -q "AbsenceListWithProjectionTO" rest/src/absence.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `get_absence_periods_for_sales_person` gibt `AbsenceListWithProjectionTO` zurück, gescoped auf die Path-sales_person_id
    - Marker-Mapping identisch zu Task 1 (gemeinsamer Helper)
    - `cargo check -p rest` exit 0
  </acceptance_criteria>
  <done>Self/HR-Per-Person-View liefert konsistente Wrapper-Shape.</done>
</task>

<task type="auto">
  <name>Task 3: Integration-Test Projektion + Surface-Schemas</name>
  <read_first>
    - rest/tests/openapi_surface.rs (EXPECTED_SCHEMAS — Plan 03 fügte ConvertExtraHoursRequestTO hinzu)
    - rest/tests/convert_to_absence.rs (Plan 03 Test-Harness als Setup-Referenz)
    - rest/src/absence.rs (Handler aus Task 1+2)
  </read_first>
  <action>
    **(a) Surface-Test** `rest/tests/openapi_surface.rs`: ergänze `EXPECTED_SCHEMAS` um `"ExtraHoursMarkerTO"` und `"AbsenceListWithProjectionTO"` (ConvertExtraHoursRequestTO ist bereits durch Plan 03 drin).

    **(b) Integration-Test** `rest/tests/absence_projection.rs`: nutze das RestState-Test-Harness (analog Plan 03). Seed: eine SalesPerson + eine lebende Vacation-extra_hours-Row (im Zwei-Jahres-Fenster) + optional ein natives absence_period. Tests:
    - `absence_projection_includes_living_hourly_markers`: GET `/absence-period` (HR-Context) -> Body deserialisiert als `AbsenceListWithProjectionTO`; `hourly_markers` enthält genau einen Marker mit `category == Vacation`, `when == <seed-date>`, `amount == <seed-amount>`; `absence_periods` enthält das native Range (falls geseedet).
    - `absence_projection_excludes_non_absence_categories`: seede zusätzlich eine ExtraWork-Row -> diese erscheint NICHT in hourly_markers.
    - `absence_projection_excludes_soft_deleted`: eine soft-deletete Vacation-Row erscheint NICHT (find_by_sales_person_id_and_year_range filtert deleted IS NULL).
    Kein `#[ignore]`. Falls das volle Harness nicht praktikabel ist, decke mindestens `absence_projection_includes_living_hourly_markers` über den echten Router ab.
  </action>
  <verify>
    <automated>cargo test -p rest absence_projection 2>&1 | tail -10; cargo test -p rest openapi_surface 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - EXPECTED_SCHEMAS enthält `ExtraHoursMarkerTO` + `AbsenceListWithProjectionTO`
    - `rest/tests/absence_projection.rs` enthält ≥ 1 grünen Test, der den Marker für eine lebende Vacation-Row in der Projektion verifiziert + die Kategorie-/soft-delete-Filterung abdeckt, kein `#[ignore]`
    - `cargo test -p rest absence_projection` + `cargo test -p rest openapi_surface` exit 0
  </acceptance_criteria>
  <done>Read-Projektion integration-getestet (inkl. Filterung) + Surface-gepinnt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> REST | Auth-Context bestimmt Sicht (hr ∨ self); Path-sales_person_id (per-person) |
| REST -> Service | Projektion reicht echtes Context an absence_service + extra_hours_service + sales_person_service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-04a | Information Disclosure | Read-Projektion fremder Stunden | mitigate | Projektion reicht Context unverändert durch (hr∨self); Self-Scope erbt von /absences + extra_hours-Service-Scoping; KEIN Authentication::Full-Bypass für Reads |
| T-8.5-04b | Information Disclosure | Falsche Kategorie geleakt | mitigate | Filter auf {Vacation,SickLeave,UnpaidLeave}; Integration-Test `absence_projection_excludes_non_absence_categories` |
| T-8.5-04c | Tampering | Soft-deletete Rows angezeigt | mitigate | find_by_sales_person_id_and_year_range filtert `deleted IS NULL`; Test `absence_projection_excludes_soft_deleted` |
| T-8.5-04d | Information Disclosure | Range-Raten erzeugt falsche Daten | mitigate | Marker trägt nur Roh-`when`-Datum (D-07); kein abgeleiteter Range — driftfrei |
</threat_model>

<verification>
- `cargo check -p rest` + `cargo test -p rest absence_projection` + `cargo test -p rest openapi_surface` grün
- `cargo test --workspace` grün (Wave-Merge-Gate)
- grep: kein `absence_range_source_active` in rest/src/absence.rs
</verification>

<success_criteria>
- Beide Absence-Read-Endpoints liefern AbsenceListWithProjectionTO
- Marker ehrlich am when-Datum, gefiltert auf 3 Kategorien, soft-delete-respektierend
- hr ∨ self Scoping erhalten, kein Read-Bypass
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-04-SUMMARY.md`
</output>
