---
phase: 08.5-read-projektion-hr-inline-convert
plan: 05
type: execute
wave: 5
depends_on: [03, 04]
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/state/absence_period.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/service/absence.rs
  - shifty-dioxus/src/component/absence_convert_modal.rs
  - shifty-dioxus/src/component/mod.rs
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "Frontend lädt AbsenceListWithProjectionTO und trennt Ranges (ABSENCE_STORE) von Stunden-Markern (neuer Store)"
    - "api::convert_extra_hours_to_absence ruft POST /extra-hours/{id}/convert-to-absence"
    - "AbsenceConvertModal ist eine eigenständige Komponente mit generalisierten Props (kein EntryRef)"
  artifacts:
    - path: "shifty-dioxus/src/state/absence_period.rs"
      provides: "ExtraHoursMarker state-Typ + From<&ExtraHoursMarkerTO>"
      contains: "struct ExtraHoursMarker"
    - path: "shifty-dioxus/src/component/absence_convert_modal.rs"
      provides: "pub fn AbsenceConvertModal (generalisiert, P-7-Submit-Defense)"
      contains: "pub fn AbsenceConvertModal"
    - path: "shifty-dioxus/src/api.rs"
      provides: "convert_extra_hours_to_absence + angepasste list_absence_periods"
      contains: "fn convert_extra_hours_to_absence"
  key_links:
    - from: "shifty-dioxus/src/loader.rs"
      to: "AbsenceListWithProjectionTO"
      via: "list_absence_periods liefert Wrapper, loader splittet"
      pattern: "hourly_markers"
    - from: "shifty-dioxus/src/api.rs"
      to: "POST /extra-hours/{id}/convert-to-absence"
      via: "convert_extra_hours_to_absence"
      pattern: "convert-to-absence"
---

<objective>
Adaptiere die Frontend-Daten-Schicht an die neue Response-Shape (Pitfall 3 — Breaking Change) und lege die Convert-Infrastruktur an: (1) `list_absence_periods`/`list_absence_periods_by_sales_person` liefern `AbsenceListWithProjectionTO`; (2) neuer State-Typ `ExtraHoursMarker` + `From<&ExtraHoursMarkerTO>`; (3) loader splittet Wrapper in Ranges + Marker und joinet `person_name`/`background_color`; (4) neuer `ABSENCE_HOURLY_STORE` + erweiterte `AbsenceAction`-Branches; (5) neue `api::convert_extra_hours_to_absence`-Funktion; (6) `ManualConvertModal` aus `cutover_admin.rs` nach `component/absence_convert_modal.rs` extrahiert + auf generalisierte Props umgestellt (D-09, A5, Pitfall 6) — die Cutover-Page bleibt vorerst funktionsfähig (8.6 löscht sie).

Purpose: SC2/SC3-Vorbereitung — die Frontend-Daten-Schicht trägt Read-Projektion + Convert, bevor Plan 06 die Page rendert.
Output: api.rs, state, loader, service-store-Wiring, eigenständiges Convert-Modal. WASM-Build grün.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-04-SUMMARY.md

<interfaces>
From rest-types (Plan 03): `AbsenceListWithProjectionTO { absence_periods: Vec<AbsencePeriodTO>, hourly_markers: Vec<ExtraHoursMarkerTO> }`, `ExtraHoursMarkerTO { extra_hours_id, sales_person_id, when: time::Date, amount: f32, category: ExtraHoursCategoryTO, description: Arc<str>, person_name: Arc<str> }`, `ConvertExtraHoursRequestTO { start, end, day_fraction: Option<DayFractionTO> }`.

Existing api.rs (src/api.rs:490-514): `list_absence_periods` + `list_absence_periods_by_sales_person` return `Rc<[AbsencePeriodTO]>`. `update_extra_hour` (456-472) does PUT /extra-hours/{id} with 409 handling. `create_absence_period` (PATTERNS.md Z. 436-452) shows POST + 422 ShiftyError::Validation handling.

Existing state (src/state/absence_period.rs:73-105): `AbsencePeriod` struct (id, sales_person_id, category, from_date, to_date, description, version, day_fraction, person_name, background_color) + `From<&AbsencePeriodTO>`.

Existing loader (src/loader.rs:884-916): `load_absence_periods_all(config, sales_persons)` joinet person_name/background_color; `load_absence_periods_by_sales_person(config, sp_id)` skippt join (Self-view).

Existing service (src/service/absence.rs:33-115): `ABSENCE_STORE: GlobalSignal<Rc<[AbsencePeriod]>>`, `ABSENCE_REFRESH`, `enum AbsenceAction { LoadAll(Rc<[SalesPerson]>), LoadForSalesPerson(Uuid), Create, Update, Delete, Refresh }`, `absence_service(rx)`-Coroutine. `bump_absence_refresh()`.

ManualConvertModal source: shifty-dioxus/src/page/cutover_admin.rs:923-1090 (PATTERNS.md Z. 503-571) — Props `(entry: EntryRef, category, on_submit: EventHandler<(Uuid, Date, Date, DayFractionTO)>, on_cancel)`, P-7-Submit-Defense (parse + s<=e + inline error), DayFraction-Select, Backdrop `onclick: on_cancel` + inner `stop_propagation`.

ExtraHoursCategory frontend state enum + From<&ExtraHoursCategoryTO> — prüfe ob in src/state/ vorhanden; falls nicht, nutze rest_types::ExtraHoursCategoryTO direkt im ExtraHoursMarker oder mappe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.rs — Response-Shape-Migration + convert-Funktion</name>
  <read_first>
    - shifty-dioxus/src/api.rs:456-514 (update_extra_hour + list_absence_periods + by_sales_person), :536-559 (create_absence_period 422-Pattern — PATTERNS.md Z. 436-457)
    - shifty-dioxus/src/api.rs (top imports — rest_types Import-Liste)
  </read_first>
  <action>
    1. Ändere `list_absence_periods` Return-Typ von `Result<Rc<[AbsencePeriodTO]>, reqwest::Error>` zu `Result<AbsenceListWithProjectionTO, reqwest::Error>` (deserialisiere `response.json::<AbsenceListWithProjectionTO>()`).
    2. Ändere `list_absence_periods_by_sales_person` analog zu `Result<AbsenceListWithProjectionTO, reqwest::Error>`.
    3. Füge `AbsenceListWithProjectionTO`, `ExtraHoursMarkerTO`, `ConvertExtraHoursRequestTO` zu den `rest_types`-Imports am Datei-Anfang hinzu.
    4. Neue Funktion (Pattern aus create_absence_period mit 422-Handling):
    ```rust
    pub async fn convert_extra_hours_to_absence(
        config: Config,
        extra_hours_id: Uuid,
        body: ConvertExtraHoursRequestTO,
    ) -> Result<AbsencePeriodTO, ShiftyError> {
        let url = format!("{}/extra-hours/{}/convert-to-absence", config.backend, extra_hours_id);
        let client = reqwest::Client::new();
        let response = client.post(url).json(&body).send().await?;
        if response.status() == reqwest::StatusCode::UNPROCESSABLE_ENTITY {
            let text = response.text().await.unwrap_or_default();
            return Err(ShiftyError::Validation(text));
        }
        response.error_for_status_ref()?;
        let result: AbsencePeriodTO = response.json().await?;
        Ok(result)
    }
    ```
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "fn convert_extra_hours_to_absence" src/api.rs && grep -q "AbsenceListWithProjectionTO" src/api.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `list_absence_periods` + `_by_sales_person` geben `AbsenceListWithProjectionTO` zurück
    - `convert_extra_hours_to_absence` postet auf `/extra-hours/{id}/convert-to-absence` mit 422-ShiftyError::Validation-Handling
    - rest_types-Imports erweitert
  </acceptance_criteria>
  <done>API-Client trägt Wrapper-Shape + Convert.</done>
</task>

<task type="auto">
  <name>Task 2: ExtraHoursMarker state + loader-Split + service store/actions</name>
  <read_first>
    - shifty-dioxus/src/state/absence_period.rs:73-105 (AbsencePeriod + From — PATTERNS Z. 758)
    - shifty-dioxus/src/loader.rs:884-916 (load_absence_periods_all + by_sales_person)
    - shifty-dioxus/src/service/absence.rs:33-115 (ABSENCE_STORE, AbsenceAction, coroutine)
  </read_first>
  <action>
    1. **State** in `src/state/absence_period.rs`: neuer Typ
    ```rust
    #[derive(Clone, Debug, PartialEq)]
    pub struct ExtraHoursMarker {
        pub extra_hours_id: Uuid,
        pub sales_person_id: Uuid,
        pub when: time::Date,
        pub amount: f32,
        pub category: rest_types::ExtraHoursCategoryTO, // oder gemappter state-Enum, falls vorhanden
        pub description: Arc<str>,
        pub person_name: Arc<str>,
    }
    impl From<&rest_types::ExtraHoursMarkerTO> for ExtraHoursMarker {
        fn from(t: &rest_types::ExtraHoursMarkerTO) -> Self {
            Self { extra_hours_id: t.extra_hours_id, sales_person_id: t.sales_person_id, when: t.when,
                amount: t.amount, category: t.category.clone(), description: t.description.clone(),
                person_name: t.person_name.clone() }
        }
    }
    ```
    Stelle `PartialEq` sicher (für Dioxus-Props später) — `ExtraHoursCategoryTO` muss PartialEq sein; falls nicht, mappe auf einen state-Enum mit PartialEq.

    2. **Loader** in `src/loader.rs`: ändere `load_absence_periods_all` Return-Typ zu `Result<(Rc<[AbsencePeriod]>, Rc<[ExtraHoursMarker]>), ShiftyError>`. Der API-Call liefert jetzt `AbsenceListWithProjectionTO`: map `wrapper.absence_periods` zu `AbsencePeriod` (mit person_name/background_color-Join wie bisher), und `wrapper.hourly_markers` zu `ExtraHoursMarker`. Analog `load_absence_periods_by_sales_person` -> `Result<(Rc<[AbsencePeriod]>, Rc<[ExtraHoursMarker]>), ShiftyError>` (Self-view: kein person_name-Join nötig, Marker tragen person_name bereits vom Backend).

    3. **Service** in `src/service/absence.rs`: neuer Store `pub static ABSENCE_HOURLY_STORE: GlobalSignal<Rc<[ExtraHoursMarker]>> = Signal::global(|| Rc::new([]));`. In den Branches `AbsenceAction::LoadAll` + `LoadForSalesPerson`: schreibe das Tupel-Ergebnis in beide Stores (`*ABSENCE_STORE.write() = ranges; *ABSENCE_HOURLY_STORE.write() = markers;`). Füge eine neue Action-Variante hinzu:
    ```rust
    /// HR-only convert of a living hourly extra_hours row into an absence_period.
    ConvertExtraHours { extra_hours_id: Uuid, start: time::Date, end: time::Date, day_fraction: rest_types::DayFractionTO },
    ```
    Branch: baue `ConvertExtraHoursRequestTO { start, end, day_fraction: Some(day_fraction) }`, rufe `api::convert_extra_hours_to_absence(config, extra_hours_id, body)`. Bei Ok: `bump_absence_refresh()` (D-15: Liste neu laden — der Marker verschwindet, native Range erscheint). Bei Err: `*ERROR_STORE.write() = ErrorStore { error: Some(err) }` (oder ABSENCE_MODAL_EVENT für Validation — wähle konsistent mit dem bestehenden Fehler-Surfacing).
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "struct ExtraHoursMarker" src/state/absence_period.rs && grep -q "ABSENCE_HOURLY_STORE" src/service/absence.rs && grep -q "ConvertExtraHours" src/service/absence.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ExtraHoursMarker` + `From<&ExtraHoursMarkerTO>` in state, PartialEq-fähig
    - loader-Funktionen geben `(Rc<[AbsencePeriod]>, Rc<[ExtraHoursMarker]>)` zurück
    - `ABSENCE_HOURLY_STORE` existiert; LoadAll/LoadForSalesPerson füllen beide Stores
    - `AbsenceAction::ConvertExtraHours`-Variante + Branch (ruft api::convert_extra_hours_to_absence + bump_absence_refresh bei Ok)
  </acceptance_criteria>
  <done>Daten-Schicht trennt Ranges/Marker; Convert-Action verdrahtet.</done>
</task>

<task type="auto">
  <name>Task 3: AbsenceConvertModal extrahieren (generalisierte Props)</name>
  <read_first>
    - shifty-dioxus/src/page/cutover_admin.rs:923-1090 (ManualConvertModal vollständig — PATTERNS.md Z. 503-571)
    - shifty-dioxus/src/component/mod.rs (Modul-Registrierung)
    - shifty-dioxus/src/i18n/ (vorhandene CutoverManualConvert*-Keys, die das Modal nutzt)
  </read_first>
  <action>
    Erstelle `src/component/absence_convert_modal.rs` mit der generalisierten Komponente (Pitfall 6 / A5 — KEIN EntryRef/DriftEntryRowData):
    ```rust
    #[component]
    pub fn AbsenceConvertModal(
        extra_hours_id: Uuid,
        initial_date: time::Date,    // pre-fill beide Von/Bis-Inputs
        amount: f32,                 // read-only Anzeige
        category: rest_types::AbsenceCategoryTO, // read-only Anzeige
        on_submit: EventHandler<(Uuid, time::Date, time::Date, rest_types::DayFractionTO)>,
        on_cancel: EventHandler<()>,
    ) -> Element
    ```
    Kopiere die Modal-Logik aus `ManualConvertModal` (cutover_admin.rs:923-1090) 1:1, ersetze nur die Prop-Quelle: `entry_id = extra_hours_id`, `initial_date` als Parameter (formatiere via `time::macros::format_description!("[year]-[month]-[day]")` für die `use_signal`-Initialwerte der String-Datumsfelder), `amount`/`category` als read-only spans. BEHALTE: P-7-Submit-Defense (parse start/end, `s <= e`-Check, inline `text-bad`-Error bei Parse-Fail oder start>end; nur (Ok,Ok mit s<=e) dispatcht `on_submit`), DayFractionTO-Select (Full/Half), Custom-Backdrop (`bg-modal-veil` + outer `onclick: on_cancel`, inner `stop_propagation`). Die i18n-Keys, die das Modal nutzt (`CutoverManualConvert*`), bleiben vorerst wiederverwendet — Plan 06/07 entscheidet, ob neue Absence-spezifische Keys nötig sind; falls die Strings ("Von", "Bis", "Umwandeln") generisch passen, reuse sie.

    Registriere `pub mod absence_convert_modal;` in `src/component/mod.rs`. Die bestehende `ManualConvertModal` in `cutover_admin.rs` bleibt UNVERÄNDERT (Cutover-Page muss bis 8.6 funktionsfähig bleiben) — nur kopieren, nicht ausschneiden.

    Füge unten einen `#[cfg(test)] mod tests` mit mindestens einem dioxus-ssr Snapshot-Test an (analog bestehender Modal-SSR-Tests in cutover_admin.rs): rendere `AbsenceConvertModal` mit Beispiel-Props, asserte dass zwei `type="date"`-Inputs + der read-only amount-Span gerendert werden. Test-Name: `absence_convert_modal_renders_date_inputs`.
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "pub fn AbsenceConvertModal" src/component/absence_convert_modal.rs && grep -q "absence_convert_modal" src/component/mod.rs && ! grep -q "EntryRef\|DriftEntryRowData" src/component/absence_convert_modal.rs && cargo test absence_convert_modal 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `src/component/absence_convert_modal.rs` enthält `pub fn AbsenceConvertModal` mit den 6 generalisierten Props
    - Datei enthält KEIN `EntryRef`/`DriftEntryRowData`
    - P-7-Submit-Defense + DayFraction-Select + Backdrop-Pattern vorhanden
    - `src/component/mod.rs` registriert das Modul
    - `cargo test absence_convert_modal` exit 0 (≥ 1 SSR-Snapshot-Test grün)
    - cutover_admin.rs ManualConvertModal unverändert (git diff zeigt keine Änderung dort)
  </acceptance_criteria>
  <done>Eigenständiges, generalisiertes Convert-Modal mit Tests; Cutover-Page intakt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Backend | api.rs sendet POST-Body; Backend erzwingt hr + Validierung |
| User-Input -> Modal | Von/Bis-Datumsfelder im AbsenceConvertModal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-05a | Tampering | Modal Datums-Input | mitigate | P-7-Submit-Defense: parse + s<=e + inline error; nur valide (Ok,Ok,s<=e) dispatcht |
| T-8.5-05b | Elevation of Privilege | Convert vom Client erzwingbar | mitigate | Authz ausschließlich Backend (Plan 02/03 hr-Gate); Frontend-Sichtbarkeit ist nur UX, kein Security-Boundary |
| T-8.5-05c | Spoofing | 422-Validation-Leak | accept | ShiftyError::Validation trägt Backend-Message; gleiche Semantik wie bestehende create_absence_period-422-Pfad |
</threat_model>

<verification>
- `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` exit 0 (WASM-Gate)
- `cargo test absence_convert_modal` grün
- grep: kein EntryRef im neuen Modal; cutover_admin.rs unverändert
</verification>

<success_criteria>
- Daten-Schicht adoptiert Wrapper-Shape, trennt Ranges/Marker
- Convert-API + Action verdrahtet (D-15 reload)
- Eigenständiges generalisiertes AbsenceConvertModal mit P-7-Defense
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-05-SUMMARY.md`
</output>
