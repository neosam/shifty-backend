---
phase: 08.5-read-projektion-hr-inline-convert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dao/src/migration_source.rs
  - dao/src/lib.rs
  - dao_impl_sqlite/src/migration_source.rs
  - dao_impl_sqlite/src/lib.rs
  - migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "Backlink extra_hours_id -> absence_period_id kann ohne cutover_run_id geschrieben werden"
    - "Backlink kann per extra_hours_id wieder gelesen werden"
    - "MigrationSourceDao lebt in eigenem Modul, keine Cutover-Kopplung"
  artifacts:
    - path: "dao/src/migration_source.rs"
      provides: "MigrationSourceDao trait + MigrationSourceRow (ohne cutover_run_id)"
      contains: "trait MigrationSourceDao"
    - path: "dao_impl_sqlite/src/migration_source.rs"
      provides: "SQLite-Impl MigrationSourceDaoImpl"
      contains: "MigrationSourceDaoImpl"
    - path: "migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql"
      provides: "DROP+RECREATE absence_period_migration_source ohne cutover_run_id"
      contains: "CREATE TABLE absence_period_migration_source"
  key_links:
    - from: "dao_impl_sqlite/src/migration_source.rs"
      to: "absence_period_migration_source"
      via: "INSERT ... ON CONFLICT(extra_hours_id) DO NOTHING"
      pattern: "INSERT INTO absence_period_migration_source"
---

<objective>
Schaffe die überlebende Heimat für den Convert-Backlink (`extra_hours -> absence_period`), losgelöst von der Cutover-Maschinerie. Ein neues kleines `MigrationSourceDao`-Trait + SQLite-Impl ersetzt den Zugriff über `CutoverDao::upsert_migration_source`, und eine DB-Migration entfernt die `cutover_run_id`-Spalte aus `absence_period_migration_source` (DROP+RECREATE, da Prod/INT keine Rows haben — CONTEXT.md Fakt 2026-06-09).

Purpose: D-04 — Backlink bleibt erhalten, vom Cutover-Ballast befreit. Phase 8.6 kann dann `CutoverDao` vollständig subtraktiv löschen, ohne diese Heimat zu berühren.
Output: `dao/src/migration_source.rs`, `dao_impl_sqlite/src/migration_source.rs`, neue Migration; in beiden `lib.rs` registriert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md

<interfaces>
<!-- Existing reference shape (dao/src/cutover.rs:54-60) — the new MigrationSourceRow drops cutover_run_id -->
```rust
// dao/src/cutover.rs:54-60 (REFERENCE — do NOT reuse cutover_run_id)
pub struct MigrationSourceRow {
    pub extra_hours_id: Uuid,
    pub absence_period_id: Uuid,
    pub cutover_run_id: Uuid,   // <-- DROPPED in new home
    pub migrated_at: time::PrimitiveDateTime,
}
```

Existing CutoverDao upsert semantics (dao/src/cutover.rs:85-90):
`upsert_migration_source(&self, row: &MigrationSourceRow, tx: Self::Transaction) -> Result<(), DaoError>`
SQL: `INSERT ... ON CONFLICT(extra_hours_id) DO NOTHING`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB-Migration — absence_period_migration_source ohne cutover_run_id</name>
  <read_first>
    - migrations/sqlite/20260517120000_add-day-fraction-to-absence-period.sql (analog: Migrations-File-Format, PATTERNS.md Z. 387-414)
    - .planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md (D-04 + Modell-Fakt: Prod/INT haben 0 Rows)
    - Bestehendes Schema von absence_period_migration_source (grep in migrations/sqlite/ nach "CREATE TABLE absence_period_migration_source")
  </read_first>
  <action>
    Lege `migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql` an (Timestamp NACH der letzten Migration 20260517120000). Da Prod/INT 0 Rows haben (CONTEXT.md Fakt 2026-06-09), ist DROP+RECREATE schmerzlos und der einzige SQLite-Weg, eine Spalte zu entfernen. Inhalt exakt:

    ```sql
    -- Phase 8.5 (D-04): Entferne cutover_run_id aus absence_period_migration_source.
    -- Der Backlink ueberlebt 8.6 (CutoverDao wird subtraktiv geloescht), aber loesgeloest
    -- von Cutover-Run-Semantik. Prod/INT haben 0 Rows (CONTEXT.md Fakt 2026-06-09) -> DROP+RECREATE safe.
    DROP TABLE IF EXISTS absence_period_migration_source;
    CREATE TABLE absence_period_migration_source (
        extra_hours_id    BLOB(16) NOT NULL PRIMARY KEY,
        absence_period_id BLOB(16) NOT NULL,
        migrated_at       TEXT NOT NULL
    );
    ```

    Führe die Migration lokal additiv aus (NICHT `sqlx database reset` — das ist DESTRUCTIVE und braucht User-Confirmation). Verwende: `nix develop --command sqlx migrate run --source migrations/sqlite` (sqlx ist nur via `nix develop` verfügbar, NICHT `nix-shell`). Falls SQLx Compile-time-Query-Checks im nächsten Task `cargo check` brechen, muss die DB diese Migration enthalten — daher zuerst migrieren, dann der DAO-Impl-Task.
  </action>
  <verify>
    <automated>test -f migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql && grep -q "CREATE TABLE absence_period_migration_source" migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql && ! grep -q "cutover_run_id" migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Datei `migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql` existiert
    - Enthält `CREATE TABLE absence_period_migration_source` mit Spalten `extra_hours_id`, `absence_period_id`, `migrated_at`
    - Enthält KEINE Spalte `cutover_run_id`
    - Lokale DB hat die Migration eingespielt (`sqlx migrate info` zeigt sie als applied)
  </acceptance_criteria>
  <done>Migration angelegt + lokal applied; Tabelle hat kein cutover_run_id mehr.</done>
</task>

<task type="auto">
  <name>Task 2: MigrationSourceDao Trait (dao-Crate)</name>
  <read_first>
    - dao/src/cutover.rs:54-90 (MigrationSourceRow + upsert_migration_source Referenz-Signatur)
    - dao/src/absence.rs:1-60 (analog: Imports, #[automock], trait-Pattern, MockTransaction — PATTERNS.md Z. 32-61)
    - dao/src/lib.rs (Modul-Registrierung: `pub mod ...;`)
  </read_first>
  <action>
    Erstelle `dao/src/migration_source.rs`. Imports analog `dao/src/absence.rs`:
    ```rust
    use async_trait::async_trait;
    use mockall::automock;
    use uuid::Uuid;
    use crate::{DaoError, MockTransaction, Transaction};
    ```
    Deklariere `MigrationSourceRow` NEU (OHNE cutover_run_id):
    ```rust
    #[derive(Clone, Debug, PartialEq)]
    pub struct MigrationSourceRow {
        pub extra_hours_id: Uuid,
        pub absence_period_id: Uuid,
        pub migrated_at: time::PrimitiveDateTime,
    }
    ```
    Deklariere das Trait mit #[automock]:
    ```rust
    #[automock(type Transaction = crate::MockTransaction;)]
    #[async_trait]
    pub trait MigrationSourceDao {
        type Transaction: crate::Transaction;

        /// UPSERT (INSERT ... ON CONFLICT(extra_hours_id) DO NOTHING) in
        /// absence_period_migration_source. Semantik identisch zur ehemaligen
        /// CutoverDao::upsert_migration_source, aber ohne cutover_run_id.
        async fn upsert_migration_source(
            &self,
            row: &MigrationSourceRow,
            tx: Self::Transaction,
        ) -> Result<(), DaoError>;

        /// Lookup: gibt den Backlink-Eintrag zu einer extra_hours_id zurueck.
        async fn find_by_extra_hours_id(
            &self,
            extra_hours_id: Uuid,
            tx: Self::Transaction,
        ) -> Result<Option<MigrationSourceRow>, DaoError>;
    }
    ```
    Registriere in `dao/src/lib.rs`: füge `pub mod migration_source;` bei den anderen `pub mod`-Deklarationen ein (alphabetische Nähe zu `absence`/`extra_hours`).
  </action>
  <verify>
    <automated>cargo check -p dao 2>&1 | tail -5; grep -q "trait MigrationSourceDao" dao/src/migration_source.rs && grep -q "pub mod migration_source" dao/src/lib.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dao/src/migration_source.rs` enthält `pub trait MigrationSourceDao` mit `upsert_migration_source` + `find_by_extra_hours_id`
    - `MigrationSourceRow` enthält KEIN `cutover_run_id`-Feld (grep `cutover_run_id` in der Datei liefert 0 Treffer)
    - `dao/src/lib.rs` enthält `pub mod migration_source;`
    - `cargo check -p dao` exit 0 (automock generiert `MockMigrationSourceDao`)
  </acceptance_criteria>
  <done>Trait + Row im dao-Crate, automock-Mock verfügbar, dao compiliert.</done>
</task>

<task type="auto">
  <name>Task 3: MigrationSourceDaoImpl + Integration-Test (dao_impl_sqlite-Crate)</name>
  <read_first>
    - dao_impl_sqlite/src/absence.rs:1-100 (analog: Pool-Wrapper, DbRow-Struct, TryFrom, Uuid-Byte-Serde, query/query_as — PATTERNS.md Z. 65-99)
    - dao_impl_sqlite/src/lib.rs (Modul-Registrierung + ggf. Re-Export)
    - dao/src/migration_source.rs (das Trait aus Task 2 — Signaturen)
  </read_first>
  <action>
    Erstelle `dao_impl_sqlite/src/migration_source.rs`. Pool-Wrapper-Pattern (PATTERNS.md Shared Pattern "DAO Pool Constructor"):
    ```rust
    pub struct MigrationSourceDaoImpl {
        pool: Arc<sqlx::Pool<sqlx::Sqlite>>,
    }
    impl MigrationSourceDaoImpl {
        pub fn new(pool: Arc<sqlx::Pool<sqlx::Sqlite>>) -> Self { Self { pool } }
    }
    ```
    Impl `MigrationSourceDao for MigrationSourceDaoImpl`:
    - `upsert_migration_source`: `INSERT INTO absence_period_migration_source (extra_hours_id, absence_period_id, migrated_at) VALUES (?, ?, ?) ON CONFLICT(extra_hours_id) DO NOTHING`. Uuids als Bytes schreiben (`row.extra_hours_id.as_bytes().to_vec()` / `.as_bytes().as_slice()` — exakt wie in `dao_impl_sqlite/src/absence.rs`). `migrated_at` als ISO8601-TEXT (analog absence-Impl Datums-Serialisierung).
    - `find_by_extra_hours_id`: `SELECT extra_hours_id, absence_period_id, migrated_at FROM absence_period_migration_source WHERE extra_hours_id = ?`. Map über DbRow-Struct + `TryFrom<&Db> for MigrationSourceRow` (Uuid::from_slice für die beiden BLOB-Spalten, PrimitiveDateTime-Parse für migrated_at).

    Registriere in `dao_impl_sqlite/src/lib.rs`: `pub mod migration_source;` (analog zu den anderen DAO-Modulen).

    Füge unten in `migration_source.rs` einen `#[cfg(test)] mod tests` an mit einem `#[tokio::test]`-Integration-Test gegen in-memory SQLite (analog Setup-Pattern aus `dao_impl_sqlite/src/absence.rs`-Tests oder `service_impl/src/test/reporting_additive_merge.rs`): lege eine in-memory-Pool an + run migrations, dann `upsert_migration_source(row)` schreiben, mit `find_by_extra_hours_id` zurücklesen, asserten dass `absence_period_id` + `migrated_at` übereinstimmen, und dass KEIN cutover_run_id existiert (implizit: Row-Struct hat das Feld nicht). Test-Name: `migration_source_roundtrip_without_cutover_run_id`.
  </action>
  <verify>
    <automated>cargo test -p dao_impl_sqlite migration_source 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `dao_impl_sqlite/src/migration_source.rs` enthält `MigrationSourceDaoImpl` + `impl MigrationSourceDao`
    - SQL-Strings referenzieren NICHT `cutover_run_id` (grep liefert 0 Treffer in der Datei)
    - `dao_impl_sqlite/src/lib.rs` enthält `pub mod migration_source;`
    - `cargo test -p dao_impl_sqlite migration_source` exit 0 (mindestens 1 Test grün: roundtrip read/write)
  </acceptance_criteria>
  <done>SQLite-Impl + grüner Roundtrip-Integration-Test ohne cutover_run_id-Kopplung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> DAO | AbsenceConversionService (Plan 02) ruft MigrationSourceDao; DAO macht keine eigene Authz |
| App -> DB | SQLite-Schema-Migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-01a | Tampering | MigrationSourceDaoImpl SQL | mitigate | Parametrisierte sqlx-Queries (kein String-Concat); Uuid als Bytes — keine Injection-Surface |
| T-8.5-01b | Information Disclosure | Backlink-Tabelle | accept | Tabelle enthält nur Uuid-Paare + Timestamp, keine PII; Read nur intern aus Service |
| T-8.5-01c | Denial of Service | DROP+RECREATE-Migration | accept | Prod/INT haben 0 Rows (CONTEXT.md Fakt) — kein Datenverlust möglich; Migration ist idempotent (`DROP TABLE IF EXISTS`) |
</threat_model>

<verification>
- `cargo check -p dao` + `cargo test -p dao_impl_sqlite migration_source` grün
- `cargo check --workspace` darf nicht durch fehlende Migration brechen (rust-analyzer-Stale-Diagnostics ignorieren, `cargo check` ist Ground-Truth)
- grep: `cutover_run_id` taucht in keiner der drei neuen/migrierten Dateien auf
</verification>

<success_criteria>
- MigrationSourceDao-Trait + Impl + Migration vorhanden, Backlink ohne cutover_run_id read/write-bar
- Roundtrip-Integration-Test grün
- Keine Kopplung an CutoverDao (das alte `CutoverDao::upsert_migration_source` bleibt vorerst unangetastet — wird erst in 8.6 gelöscht)
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-01-SUMMARY.md`
</output>
