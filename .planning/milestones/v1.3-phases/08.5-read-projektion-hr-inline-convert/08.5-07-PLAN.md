---
phase: 08.5-read-projektion-hr-inline-convert
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - shifty-dioxus/src/component/add_extra_hours_form.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/i18n/mod.rs
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "Working-Hours-Dialog macht nur noch Stunden-Eintrag — kein VacationDays-Branch, kein add_vacation-Call, keine Von/Bis-Felder"
    - "Bei Vacation/SickLeave/UnpaidLeave erscheint ein nicht-blockierender Inline-Hinweis mit Link zu /absences"
    - "Hinweis verschwindet bei anderer Kategorie; Dialog blockiert nie"
  artifacts:
    - path: "shifty-dioxus/src/component/add_extra_hours_form.rs"
      provides: "Schlanker Stunden-Dialog + Inline-Hinweis"
      contains: "ExtraHoursAbsenceHint"
  key_links:
    - from: "shifty-dioxus/src/component/add_extra_hours_form.rs"
      to: "/absences"
      via: "nicht-blockierender Hinweis-Link bei 3 Kategorien"
      pattern: "/absences"
---

<objective>
Baue den Working-Hours-Dialog (`add_extra_hours_form.rs`) um (D-10, D-11, D-12): entferne den `VacationDays`-Branch + den `api::add_vacation`-Range-Call + die `from`/`to`-Signals + das `vacation_days_str`-Binding + die `VacationDays`-`<select>`-Option (Pitfall 1). Der Dialog macht nur noch Stunden-Einträge. Sobald `Vacation`, `SickLeave` ODER `UnpaidLeave` (alle drei, D-11) gewählt ist, erscheint direkt unter dem Kategorie-Select ein dezenter, NICHT-blockierender Inline-Hinweis mit Empfehlung + Link zu `/absences` (D-10). Plus i18n + finaler Full-Suite-Gate (cargo test --workspace) + WASM-Build-Gate.

Purpose: SC4 + SC5 — Dialog wieder einfach, Soft-Migration statt Block (Modell A); v1.3-Gates grün. FUI-A-08 erfüllt.
Output: schlanker Dialog + 2 i18n-Keys × 3 Locales + per-locale-matcher + grüner Workspace + grüner WASM-Build.

Hinweis: Der dadurch tote Backend-`add_vacation`-Endpoint wird NICHT hier, sondern in Phase 8.6 aufgeräumt (D-12).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-06-SUMMARY.md

<interfaces>
Existing add_extra_hours_form.rs (verbatim, src/component/add_extra_hours_form.rs):
- Z. 37-38 (REMOVE): `let mut from = use_signal(|| js::current_datetime().date().format(&date_format).unwrap());` + analog `to`.
- Z. 54 (REMOVE): `let vacation_days_str = i18n.t(Key::CategoryVacationDays);`
- Z. 105-122 (REMOVE der gesamte VacationDays-if-Zweig; behalte nur den else-Body als unbedingten `add_extra_hour`-Call):
  ```rust
  if category == WorkingHoursCategory::VacationDays {
      // ... add_vacation(...) ...  <-- ENTFERNEN
  } else {
      result_handler(api::add_extra_hour(config.to_owned(), sales_person_id, amount, (&category).into(), description, when).await);  <-- BEHALTEN, else-Wrapper raus
  }
  ```
- `category: use_signal(WorkingHoursCategory)` — `*category.read()`. WorkingHoursCategory-Varianten inkl. `Vacation`, `SickLeave`, `UnpaidLeave`, `VacationDays`, `ExtraWork(..)`, etc. Das RSX hat einen `<select>` mit Optionen je Kategorie (inkl. `vacation_days_str`).

Reactive Inline-Hinweis (RESEARCH Pattern 7 / PATTERNS.md Z. 609-624) unter dem Kategorie-`<select>`:
```rust
if matches!(*category.read(), WorkingHoursCategory::Vacation | WorkingHoursCategory::SickLeave | WorkingHoursCategory::UnpaidLeave) {
    rsx! {
        div { class: "text-small text-ink-muted mt-1",
            "{i18n.t(Key::ExtraHoursAbsenceHint)}"
            a { href: "/absences", class: "text-link ml-1", "{i18n.t(Key::ExtraHoursAbsenceHintLink)}" }
        }
    }
}
```
Prüfe die exakte WorkingHoursCategory-Variant-Namen (Vacation vs VacationHours) im state-Enum bevor du das `matches!` schreibst.

i18n: neue Keys `ExtraHoursAbsenceHint`, `ExtraHoursAbsenceHintLink` in Key-enum + de/en/cs. Per-locale-matcher-Pattern (Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n-Keys für den Dialog-Hinweis (De/En/Cs)</name>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (Key-enum + matcher-Tests)
    - shifty-dioxus/src/i18n/de.rs / en.rs / cs.rs (Locale-spezifische Einträge)
  </read_first>
  <action>
    Füge zwei Key-Varianten hinzu:
    - `ExtraHoursAbsenceHint` — Empfehlungs-Satz (De z.B.: „Für ganze Urlaubs-/Abwesenheits-Zeiträume nutze bitte die Abwesenheits-Maske.", En + Cs analog)
    - `ExtraHoursAbsenceHintLink` — Link-Text (De: „Zu Abwesenheiten", En: „Go to absences", Cs: …)
    In de.rs (`Locale::De`), en.rs (`Locale::En`), cs.rs (`Locale::Cs`). Erweitere die per-locale-reference-matcher-Tests um beide neuen Keys (Pitfall 2 Guard).
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "ExtraHoursAbsenceHint" src/i18n/mod.rs && grep -q "ExtraHoursAbsenceHintLink" src/i18n/de.rs && cargo test i18n 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Beide Keys im Key-Enum + alle drei Locales
    - de.rs nutzt `Locale::De`
    - matcher-Tests um beide Keys erweitert
    - `cargo test i18n` exit 0
  </acceptance_criteria>
  <done>Hinweis-i18n vollständig + Locale-korrekt.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog-Umbau — VacationDays/add_vacation raus + Inline-Hinweis</name>
  <read_first>
    - shifty-dioxus/src/component/add_extra_hours_form.rs (vollständig, v.a. Z. 30-145 + das RSX mit dem Kategorie-`<select>`)
    - .planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md (Pattern 7 + Pitfall 1)
    - shifty-dioxus/src/state/ (WorkingHoursCategory-Enum-Varianten)
  </read_first>
  <action>
    1. Entferne die `from`/`to`-Signals (Z. 37-38) — sofern keine andere Stelle sie liest (grep prüfen; Compile-Warning `unused` zeigt Restbestände).
    2. Entferne `let vacation_days_str = i18n.t(Key::CategoryVacationDays);` (Z. 54).
    3. Im `Submit`-Branch (Z. 105-122): entferne den `if category == WorkingHoursCategory::VacationDays { ... add_vacation ... } else { ... }`-Block und ersetze ihn durch den unbedingten `add_extra_hour`-Call (der bisherige else-Body):
       ```rust
       result_handler(
           api::add_extra_hour(config.to_owned(), sales_person_id, amount, (&category).into(), description, when).await,
       );
       ```
       Entferne auch das jetzt unbenutzte `let format = format_description!(...)`/`from`/`to`-Parsing im Branch.
    4. Entferne die `VacationDays`-`<option>` aus dem Kategorie-`<select>`-RSX (Pitfall 1 — sonst kann der User eine tote Variante wählen). Entferne `vacation_days_str` aus dem RSX.
    5. Falls `from`/`to`/`VacationDays` im `to_owned![...]`-Block der Coroutine auftauchen, dort ebenfalls entfernen.
    6. Füge den NICHT-blockierenden Inline-Hinweis direkt UNTER dem Kategorie-`<select>` im RSX ein, reaktiv auf `*category.read()` (verwende die exakten Variant-Namen aus dem state-Enum — Vacation/SickLeave/UnpaidLeave, D-11 alle drei):
       ```rust
       if matches!(*category.read(), WorkingHoursCategory::Vacation | WorkingHoursCategory::SickLeave | WorkingHoursCategory::UnpaidLeave) {
           div { class: "text-small text-ink-muted mt-1",
               "{i18n.t(Key::ExtraHoursAbsenceHint)}"
               a { href: "/absences", class: "text-link ml-1", "{i18n.t(Key::ExtraHoursAbsenceHintLink)}" }
           }
       }
       ```
       NICHT-blockierend: kein `disabled` auf Submit, keine Validierung — nur ein Hinweis (Modell A, D-10).
  </action>
  <verify>
    <automated>cd shifty-dioxus && ! grep -q "VacationDays\|add_vacation\|vacation_days_str" src/component/add_extra_hours_form.rs && grep -q "ExtraHoursAbsenceHint" src/component/add_extra_hours_form.rs && grep -q "/absences" src/component/add_extra_hours_form.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep `VacationDays`, `add_vacation`, `vacation_days_str` in add_extra_hours_form.rs liefert 0 Treffer
    - `from`/`to`-Signals entfernt (kein `unused`-Warning im WASM-Build)
    - Inline-Hinweis-Block + `/absences`-Link vorhanden, reaktiv auf 3 Kategorien
    - kein `disabled`/Block durch den Hinweis (Submit bleibt immer möglich)
  </acceptance_criteria>
  <done>Dialog schlank (nur Stunden) + nicht-blockierender Soft-Migration-Hinweis.</done>
</task>

<task type="auto">
  <name>Task 3: SSR-Test für den Hinweis + Full-Suite + WASM-Gate</name>
  <read_first>
    - shifty-dioxus/src/component/add_extra_hours_form.rs (Test-Modul falls vorhanden; pin_de_locale-Helper aus Plan 08-05)
    - .planning/phases/08.5-read-projektion-hr-inline-convert/08.5-VALIDATION.md (Sampling/Gate-Definitionen)
  </read_first>
  <behavior>
    - extra_hours_form_absence_hint_shown_for_vacation: category=Vacation -> gerendertes HTML enthält den Hinweis-Text + href="/absences"
    - extra_hours_form_no_hint_for_extra_work: category=ExtraWork -> KEIN Hinweis-Text im HTML
  </behavior>
  <action>
    1. Füge ein SSR-Test-Modul (oder erweitere das bestehende) in `add_extra_hours_form.rs` mit den zwei Tests aus <behavior>. Nutze `pin_de_locale()` + VirtualDom-Render-Pattern. Da der Hinweis reaktiv auf das `category`-Signal ist, das per Default `ExtraWork` ist, prüft Test 1 ggf. über eine Hilfs-Render-Variante mit vorgesetztem category-Signal — falls das SSR-seitig nicht direkt setzbar ist, dokumentiere die Limitation (analog 8.2-02 SSR-Test-Limit) und teste mindestens den ExtraWork-Default (kein Hinweis) + die Existenz der i18n-Keys; den sichtbaren Wechsel deckt die Manual-UAT (VALIDATION.md) ab.
    2. **Full-Suite-Gate:** `cargo test --workspace` MUSS grün sein (alle Backend-Crates + Frontend-Binary-Tests). Behebe etwaige Regressionen (z.B. SSR-Snapshot-Tests, die durch die Response-Shape-Änderung in Plan 05/06 brechen).
    3. **WASM-Build-Gate:** `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` MUSS exit 0 liefern.
    4. Setze in `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-VALIDATION.md` das Frontmatter `nyquist_compliant: true` + `wave_0_complete: true` (alle Wave-0-Test-Files sind jetzt angelegt + grün).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test --workspace 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ≥ 1 SSR-Test für den Hinweis grün (mind. der ExtraWork-no-hint-Fall)
    - `cargo test --workspace` exit 0 (gesamter Workspace grün)
    - `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` exit 0
    - VALIDATION.md frontmatter `nyquist_compliant: true` + `wave_0_complete: true`
  </acceptance_criteria>
  <done>Dialog SSR-getestet; Workspace + WASM grün; Phase UAT-bereit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Dialog | Stunden-Eingabe (date/amount/description) |
| Dialog -> Backend | api::add_extra_hour (bestehender, gesicherter Pfad) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-07a | Tampering | Dialog-Submit nach Entfernung des VacationDays-Branch | mitigate | Nur noch `add_extra_hour`-Pfad; tote VacationDays-Option aus `<select>` entfernt (Pitfall 1) — kein leeres Formular dispatchbar |
| T-8.5-07b | Information Disclosure | Hinweis-Link | accept | statischer Link zu `/absences` (kein User-Input); keine Daten exponiert |
| T-8.5-07c | Injection (XSS) | i18n-Hinweis-Strings | accept | statische i18n-Werte aus Translations, kein User-Input — kein Injection-Vektor |
</threat_model>

<verification>
- `cargo test --workspace` exit 0 (Backend + Frontend)
- `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` exit 0
- grep: kein `VacationDays`/`add_vacation`/`vacation_days_str` in add_extra_hours_form.rs
- VALIDATION.md nyquist_compliant + wave_0_complete = true
</verification>

<success_criteria>
- Working-Hours-Dialog nur noch Stunden-Eintrag + nicht-blockierender Soft-Migration-Hinweis für 3 Kategorien
- i18n De/En/Cs vollständig, Locale-korrekt
- cargo test --workspace + WASM-Build grün → Phase 8.5 UAT-bereit (FUI-A-08 erfüllt)
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-07-SUMMARY.md`
</output>
