---
phase: 08.5-read-projektion-hr-inline-convert
verified: 2026-06-09T21:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 08.5: read-projektion-hr-inline-convert — Verifikationsbericht

**Phasenziel:** Requirement FUI-A-08 (revidiert — Soft-Migration statt Deprecation)
**Verifiziert:** 2026-06-09T21:00:00Z
**Status:** passed
**Re-Verifikation:** Nein — Erstverifikation

---

## Ziel-Erreichung

### Beobachtbare Wahrheiten (Success Criteria)

| #  | Wahrheit | Status | Evidenz |
|----|----------|--------|---------|
| SC1 | `GET /absence-period*` liefern `AbsenceListWithProjectionTO` mit lebenden Vacation/Sick/Unpaid-Markern; Frontend rendert „stundenbasiert"-Label + Edit-Deep-Link | ✓ VERIFIED | `rest/src/absence.rs`: beide Handler (`get_all_absence_periods`, `get_absence_periods_for_sales_person`) geben `AbsenceListWithProjectionTO` zurück; `is_absence_category()`-Filter auf 3 Kategorien; `when = date_time.date()` (kein Range-Raten, D-07). Absences-Page: `HourlyMarkerRow` mit `AbsenceHourlyBadge` + Edit-Button. Integration-Tests `absence_projection_includes_living_hourly_markers`, `absence_projection_excludes_non_absence_categories`, `absence_projection_excludes_soft_deleted` grün. |
| SC2 | `POST /extra-hours/{id}/convert-to-absence` außerhalb cutover-Namespace; Tx atomar; Privileg `hr`; Heuristik nicht beteiligt | ✓ VERIFIED | `rest/src/extra_hours.rs`: Route `/{id}/convert-to-absence` registriert; Handler ruft `absence_conversion_service().convert_extra_hours_to_absence(...)`. `service_impl/src/absence_conversion.rs`: hr-Gate VOR Tx-Open; 3 Writes (absence_dao.create + migration_source_dao.upsert + extra_hours_service.soft_delete_bulk) in einer Tx; kein Cutover-Artefakt. CR-01-Fix verifiziert: `Arc::from([entity.id])` (physische id, nicht logical_id). Integration-Tests `convert_to_absence_happy_path` (200) + `convert_requires_hr_privilege` (403) grün. |
| SC3 | Inline-Aktion „In Zeitraum umwandeln" (HR-only) öffnet Range-Modal; Liste lädt nach Convert neu (Marker weg, Range da) | ✓ VERIFIED | `shifty-dioxus/src/page/absences.rs`: `HourlyMarkerRow` rendert `AbsenceConvertToRangeAction`-Button nur unter `if props.is_hr`; `AbsenceConvertModal` konditional gerendert; `on_submit` dispatcht `AbsenceAction::ConvertExtraHours`; `service/absence.rs` ruft `api::convert_extra_hours_to_absence` + `bump_absence_refresh()` bei Ok. SSR-Tests `convert_action_visible_to_hr_only` + `hourly_marker_row_renders_badge` + `hourly_marker_row_renders_edit_action` grün. |
| SC4 | Dialog-Umbau `add_extra_hours_form.rs`: Von/Bis-Range-Felder + `VacationDays`-Branch + `add_vacation`-Call entfernt; Vacation/SickLeave/UnpaidLeave zeigt nicht-blockierenden Hinweis + Link zu `/absences` | ✓ VERIFIED | `add_extra_hours_form.rs`: Submit-Pfad ruft `api::add_extra_hour` direkt (kein `if VacationDays`-Zweig); keine `from`/`to`/`vacation_days_str`-Signale; `VacationDays`-Option aus `<select>` entfernt (nur in `#[cfg(test)]`-Regressionstest referenziert). `matches!(Vacation|SickLeave|UnpaidLeave)` reaktiver Hinweis + `/absences`-Link vorhanden (Z. 193–207). Kein `disabled` auf Submit — nicht-blockierend. |
| SC5 | i18n De/En/Cs für neue Labels/Warnungen vollständig; `cargo build --target wasm32-unknown-unknown` grün; `cargo test --workspace` grün | ✓ VERIFIED | Neue Keys (`AbsenceHourlyBadge`, `AbsenceEditHoursAction`, `AbsenceConvertToRangeAction`, `AbsenceHourlyAmountLabel`, `ExtraHoursAbsenceHint`, `ExtraHoursAbsenceHintLink`) in `i18n/mod.rs` + allen 3 Locales (de.rs mit `Locale::De`, en.rs mit `Locale::En`, cs.rs mit `Locale::Cs`). Per-locale-Matcher-Tests in `i18n/mod.rs` erweitert. WASM-Build-Gate: vom Orchestrator als grün bestätigt. `cargo test --workspace`: 437 service_impl + 86 shifty_bin + 581 shifty-dioxus — alle grün. |
| SC6 | OpenAPI `#[utoipa::path]` + `ToSchema` + Surface-Test für den Convert-Endpoint | ✓ VERIFIED | `rest/src/extra_hours.rs`: `#[utoipa::path(post, path = "/{id}/convert-to-absence", ...)]` vollständig annotiert (200/403/404/422); `ExtraHoursApiDoc` registriert `convert_extra_hours_to_absence` in `paths` + `ConvertExtraHoursRequestTO` in `schemas`. `rest/tests/openapi_surface.rs`: `EXPECTED_PATHS` enthält `/extra-hours/{id}/convert-to-absence`; `EXPECTED_SCHEMAS` enthält `ConvertExtraHoursRequestTO`, `ExtraHoursMarkerTO`, `AbsenceListWithProjectionTO`. Surface-Test grün. |

**Score:** 6/6 Wahrheiten verifiziert

---

## Pflichtartefakte

| Artefakt | Erwartet | Status | Details |
|----------|----------|--------|---------|
| `dao/src/migration_source.rs` | `MigrationSourceDao`-Trait + `MigrationSourceRow` (ohne `cutover_run_id`) | ✓ VERIFIED | Trait mit `upsert_migration_source` + `find_by_extra_hours_id`; Row ohne `cutover_run_id`-Feld |
| `dao_impl_sqlite/src/migration_source.rs` | `MigrationSourceDaoImpl` + Roundtrip-Test | ✓ VERIFIED | Pool-Wrapper, INSERT ON CONFLICT DO NOTHING, 3 Integrationstests grün inkl. `migration_source_roundtrip_without_cutover_run_id` |
| `migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql` | DROP+RECREATE ohne `cutover_run_id` | ✓ VERIFIED | Datei vorhanden; enthält `CREATE TABLE absence_period_migration_source` mit 3 Spalten (kein `cutover_run_id`) |
| `service/src/absence_conversion.rs` | `AbsenceConversionService`-Trait | ✓ VERIFIED | `#[automock]`-Trait mit `convert_extra_hours_to_absence`-Signatur; DayFraction als Option; keine ManualRange-Wrapper |
| `service_impl/src/absence_conversion.rs` | `AbsenceConversionServiceImpl` (BL-Tier, 3-write-Tx) | ✓ VERIFIED | `gen_service_impl!` mit 6 Deps; hr-Gate VOR Tx; 3 Writes; `entity.id` für soft-delete (CR-01-Fix); kein Cutover-Artefakt |
| `service_impl/src/test/absence_conversion.rs` | 4 Mockall-Unit-Tests + 1 Integration-Test + CR-01-Regressionstest | ✓ VERIFIED | happy_path, date_order_wrong, overlap_rejected, hr_privilege_required; `convert_extra_hours_happy_path_integration`; `convert_soft_deletes_by_physical_id_not_logical_id` mit `withf`-Assertion |
| `rest-types/src/lib.rs` | 3 DTOs: `ConvertExtraHoursRequestTO`, `ExtraHoursMarkerTO`, `AbsenceListWithProjectionTO` | ✓ VERIFIED | Alle 3 mit `ToSchema`; Date-Felder mit `#[schema(value_type = String, format = "date")]`; kein `cfg(feature = "service-impl")` |
| `rest/src/extra_hours.rs` | Convert-Handler + Route + `#[utoipa::path]` + ApiDoc-Eintrag | ✓ VERIFIED | Handler `convert_extra_hours_to_absence`; Route `/{id}/convert-to-absence`; vollständige utoipa-Annotation; im `ExtraHoursApiDoc` gelistet |
| `rest/src/absence.rs` | Beide GET-Handler augmentiert + `AbsenceListWithProjectionTO` + ApiDoc-Schemas | ✓ VERIFIED | Beide Handler liefern Wrapper; `ExtraHoursMarkerTO` + `AbsenceListWithProjectionTO` im `AbsenceApiDoc` |
| `rest/src/lib.rs` | `RestStateDef`-Extension um `AbsenceConversionService` | ✓ VERIFIED | `type AbsenceConversionService` + `fn absence_conversion_service()` eingetragen |
| `shifty_bin/src/main.rs` | DI-Wiring: `MigrationSourceDaoImpl` (Basic) + `AbsenceConversionServiceImpl` (BL nach Basic) | ✓ VERIFIED | `type MigrationSourceDao = ...MigrationSourceDaoImpl` (Z.44); `AbsenceConversionServiceImpl` nach Basic-Services konstruiert (Z.1095) |
| `rest/tests/openapi_surface.rs` | Surface-Assertions für Endpoint + 3 Schemas | ✓ VERIFIED | Path + alle 3 Schemas in `EXPECTED_PATHS`/`EXPECTED_SCHEMAS` |
| `shifty_bin/src/integration_test/convert_to_absence.rs` | happy-path-200 + 403-Test | ✓ VERIFIED | `convert_to_absence_happy_path` + `convert_requires_hr_privilege` vorhanden und grün |
| `shifty_bin/src/integration_test/absence_projection.rs` | Projektion-Integration-Tests (inkl. Filterung) | ✓ VERIFIED | 4 Tests: `absence_projection_includes_living_hourly_markers`, `absence_projection_excludes_non_absence_categories`, `absence_projection_excludes_soft_deleted`, `absence_projection_coexists_with_absence_period` |
| `shifty-dioxus/src/api.rs` | `list_absence_periods` → `AbsenceListWithProjectionTO`; `convert_extra_hours_to_absence` | ✓ VERIFIED | Beide list-Funktionen geben Wrapper zurück; `convert_extra_hours_to_absence` postet auf `/extra-hours/{id}/convert-to-absence` mit 422-Handling |
| `shifty-dioxus/src/state/absence_period.rs` | `ExtraHoursMarker` + `From<&ExtraHoursMarkerTO>` | ✓ VERIFIED | Struct mit allen Feldern; `ExtraHoursCategoryTO` hat `PartialEq` (Copy+Clone+PartialEq+Eq+Hash); From-Impl vorhanden |
| `shifty-dioxus/src/loader.rs` | Beide Loader geben `(Rc<[AbsencePeriod]>, Rc<[ExtraHoursMarker]>)` zurück | ✓ VERIFIED | `load_absence_periods_all` + `load_absence_periods_by_sales_person` mit Tuple-Return; Marker-Mapping vorhanden |
| `shifty-dioxus/src/service/absence.rs` | `ABSENCE_HOURLY_STORE` + `AbsenceAction::ConvertExtraHours` + Branch | ✓ VERIFIED | GlobalSignal vorhanden; Action-Variante + Branch mit `api::convert_extra_hours_to_absence` + `bump_absence_refresh()` |
| `shifty-dioxus/src/component/absence_convert_modal.rs` | Generalisiertes Modal (kein EntryRef/DriftEntryRowData); P-7-Defense; SSR-Test | ✓ VERIFIED | 6 Props: `extra_hours_id`, `initial_date`, `amount`, `category`, `on_submit`, `on_cancel`; P-7-Submit-Defense + DayFraction-Select + Backdrop; SSR-Test `absence_convert_modal_renders_date_inputs` vorhanden |
| `shifty-dioxus/src/page/absences.rs` | `HourlyMarkerRow` + `ABSENCE_HOURLY_STORE`-Read + `AbsenceConvertModal`-Verdrahtung + `ConvertExtraHours`-Dispatch + SSR-Tests | ✓ VERIFIED | `HourlyMarkerRow` existiert; STATIC Tailwind-Klassen (kein format!); hr-Only-Convert-Button; ABSENCE_HOURLY_STORE gelesen; ≥3 SSR-Tests grün |
| `shifty-dioxus/src/component/add_extra_hours_form.rs` | Kein VacationDays-Branch/add_vacation/vacation_days_str; Inline-Hinweis für 3 Kategorien | ✓ VERIFIED | Produktionscode ohne VacationDays-Branch; nur `add_extra_hour` im Submit; `matches!(Vacation|SickLeave|UnpaidLeave)` + `/absences`-Link vorhanden; nicht-blockierend |
| `shifty-dioxus/src/i18n/de.rs`, `en.rs`, `cs.rs` | Neue Keys in allen 3 Locales; de.rs nutzt `Locale::De` | ✓ VERIFIED | 6 neue Keys (AbsenceHourly*, ExtraHoursAbsenceHint*) in allen 3 Locales; de.rs durchgängig `Locale::De`; per-locale-Matcher-Tests erweitert |

---

## Key-Link-Verifikation

| Von | Zu | Via | Status | Details |
|-----|----|-----|--------|---------|
| `service_impl/src/absence_conversion.rs` | `MigrationSourceDao.upsert_migration_source` | Write 2 nach absence_dao.create | ✓ WIRED | Aufruf Z.151-160; `entity.id` für soft-delete (CR-01-Fix) |
| `service_impl/src/absence_conversion.rs` | `extra_hours_service.soft_delete_bulk` | Authentication::Full + `entity.id` | ✓ WIRED | Z.173-179; physische id korrekt |
| `rest/src/extra_hours.rs` | `absence_conversion_service().convert_extra_hours_to_absence` | Handler → Service | ✓ WIRED | Z.229-238 |
| `shifty_bin/src/main.rs` | `AbsenceConversionServiceImpl` | DI-Wiring BL nach Basic | ✓ WIRED | Z.1095; `MigrationSourceDaoImpl` bei Basic-DAOs (Z.44) |
| `shifty-dioxus/src/loader.rs` | `AbsenceListWithProjectionTO.hourly_markers` | Wrapper split in Loader | ✓ WIRED | Z.910-925; person_name-Join-Fallback vorhanden |
| `shifty-dioxus/src/api.rs` | `POST /extra-hours/{id}/convert-to-absence` | `convert_extra_hours_to_absence` | ✓ WIRED | Z.525; 422-ShiftyError::Validation-Handling |
| `shifty-dioxus/src/page/absences.rs` | `AbsenceAction::ConvertExtraHours` | `AbsenceConvertModal.on_submit` | ✓ WIRED | Z.1822; `absence_service.send(...)` |
| `shifty-dioxus/src/page/absences.rs` | `ABSENCE_HOURLY_STORE` | Page liest Marker-Store | ✓ WIRED | Z.1725 |

---

## Daten-Fluss-Überprüfung (Level 4)

| Artefakt | Datenvariable | Quelle | Echte Daten | Status |
|----------|---------------|--------|-------------|--------|
| `rest/src/absence.rs` `get_all_absence_periods` | `hourly_markers` | `extra_hours_service.find_by_sales_person_id_and_year_range` | Ja — DB-Query über ExtraHoursDao | ✓ FLIESSEND |
| `shifty-dioxus/src/page/absences.rs` | `hourly_markers` | `ABSENCE_HOURLY_STORE` → `loader.load_absence_periods_all` → `api::list_absence_periods` | Ja — Backend-DB-Query | ✓ FLIESSEND |
| `service_impl/src/absence_conversion.rs` | `absence_entity` | 3 DAO-Writes in gemeinsamer Tx | Ja — echte SQLite-Schreiboperationen | ✓ FLIESSEND |

---

## Verhaltens-Stichproben (Behavioral Spot-Checks)

| Verhalten | Prüfmethode | Ergebnis | Status |
|-----------|-------------|----------|--------|
| `cargo test --workspace` grün | Orchestrator-Gate-Bestätigung | 437 service_impl + 86 shifty_bin + 581 shifty-dioxus, 0 failed | ✓ PASS |
| WASM-Build `cargo build --target wasm32-unknown-unknown` | Orchestrator-Gate-Bestätigung | exit 0 | ✓ PASS |
| CR-01-Regressionstest `convert_soft_deletes_by_physical_id_not_logical_id` | `withf`-Assertion auf `entity.id` | schlägt auf Bug-Stand fehl, grün mit Fix | ✓ PASS |
| Surface-Test `openapi_paths_contain_expected_routes` + `openapi_schemas_contain_expected_dtos` | `cargo test -p rest openapi_surface` | `/extra-hours/{id}/convert-to-absence` + 3 Schemas im Set | ✓ PASS |
| i18n-Tests `i18n_de_match_german_reference` | `cargo test i18n` in shifty-dioxus | neue Keys in Matcher erweitert + grün | ✓ PASS |

---

## Anforderungsabdeckung

| Anforderung | Plan | Beschreibung | Status | Evidenz |
|-------------|------|--------------|--------|---------|
| FUI-A-08 (revidiert) | 08.5-01 bis 08.5-07 | Soft-Migration statt Deprecation: Read-Projektion + Convert-Endpoint + HR-Inline-Aktion + Dialog-Umbau | ✓ ERFÜLLT | Alle 6 SC verifiziert; kein Blocking; no Heuristik; hr-gated |

**Hinweis zur REQUIREMENTS.md-Traceability:** FUI-A-08 ist dort noch als `Phase 11` zugewiesen. Diese Phase 08.5 setzt es früher um (revidierter Plan). Die Traceability-Tabelle in REQUIREMENTS.md ist veraltet, aber funktional irrelevant — die Implementation ist vollständig.

---

## Anti-Pattern-Scan

| Datei | Zeile | Muster | Schwere | Auswirkung |
|-------|-------|--------|---------|-----------|
| `shifty-dioxus/src/page/absences.rs` WR-01 | ~1821 | `convert_target.set(None)` synchron vor async-Antwort — Modal schließt vor Fehler-Feedback | ⚠️ WARNING | 422-Validation-Fehler beim Convert erreicht User nicht sichtbar (nur ERROR_STORE); Modal verschwindet |
| `service_impl/src/absence_conversion.rs` WR-02 | ~137 | `description: Arc::from("")` — Quell-Description wird verworfen | ⚠️ WARNING | Fachlicher Kontext der extra_hours-Row geht verloren |
| `service_impl/src/absence_conversion.rs` WR-03 | ~138 | `created: migrated_at` — Convert-Datum statt Quell-Anlagedatum | ⚠️ WARNING | Audit-Semantik unklar, aber bewusste Entscheidung (kein Blocker) |
| `rest/src/absence.rs` WR-04 | ~160-178 | Read-Projektion filtert nicht auf `absence_period_migration_source`-Backlink | ⚠️ WARNING | Zweite Verteidigungslinie fehlt; durch CR-01-Fix primär entschärft |
| `rest/src/absence.rs` IN-01 | ~162-178 | N+1-Marker-Loads im HR-GET (Schleife über Personen) | ℹ️ INFO | Performance-Hinweis für große Mitarbeiterlisten |
| `dao_impl_sqlite/src/migration_source.rs` IN-02 | ~36-43 | `_pool`-Feld ungenutzt (Unterstrich unterdrückt Warning) | ℹ️ INFO | Konsistent mit CutoverDaoImpl; kein Bug |
| `shifty-dioxus/src/component/absence_convert_modal.rs` IN-03 | ~177-179 | Hartkodierter englischer Fallback "Invalid date format" | ℹ️ INFO | Nur bei manipuliertem Input; seltener Pfad |

**Blockierende Anti-Pattern:** Keine. Alle 5 WARNINGs aus dem Code-Review sind non-blocking advisory (kein CR mehr offen; CR-01 wurde gefixt + Regressionstest ergänzt).

---

## Menschliche Verifikation erforderlich

Keine offenen Punkte für menschliche Verifikation — alle kritischen Pfade sind programmatisch verifizierbar und wurden entweder durch die Orchestrator-Gates oder durch direkten Quellcode-Abgleich bestätigt.

**Hinweis zur manuellen UAT:** Sichtbarkeit und UX-Qualität der HR-Inline-Convert-Funktion (Modal öffnet sich, Marker verschwindet nach Convert, Range erscheint) kann erst beim manuellen Testen mit laufendem Stack vollständig beurteilt werden. Die Logik ist jedoch durch SSR-Tests + Integration-Tests vollständig abgedeckt.

---

## Lücken-Zusammenfassung

Keine Lücken. Alle 6 Success Criteria sind verifiziert.

Die 5 verbleibenden WARNINGs aus 08.5-REVIEW.md (WR-01 bis WR-05, wobei WR-05 durch den CR-01-Fix adressiert wurde) sind advisory und nicht phasenziel-blockierend. Sie sind für Phase 8.6 oder folgende Iterationen vorgemerkt.

---

_Verifiziert: 2026-06-09T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
