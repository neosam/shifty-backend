---
phase: 08.5-read-projektion-hr-inline-convert
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - shifty-dioxus/src/page/absences.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/i18n/mod.rs
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "Stundenbasierte Marker erscheinen chronologisch inline in der Absence-Liste mit sichtbarem 'stundenbasiert'-Badge"
    - "Marker sind read-only; 'Stunden bearbeiten' (self+hr) öffnet extra_hours-Edit; 'In Zeitraum umwandeln' nur HR"
    - "Convert dispatcht AbsenceAction::ConvertExtraHours; Liste lädt nach Convert neu (Marker weg, Range da)"
  artifacts:
    - path: "shifty-dioxus/src/page/absences.rs"
      provides: "HourlyMarkerRow inline + AbsenceConvertModal-Verdrahtung + SSR-Tests"
      contains: "HourlyMarkerRow"
  key_links:
    - from: "shifty-dioxus/src/page/absences.rs"
      to: "AbsenceAction::ConvertExtraHours"
      via: "AbsenceConvertModal on_submit dispatch"
      pattern: "ConvertExtraHours"
    - from: "shifty-dioxus/src/page/absences.rs"
      to: "ABSENCE_HOURLY_STORE"
      via: "Page liest Marker-Store + reicht an AbsenceList"
      pattern: "ABSENCE_HOURLY_STORE"
---

<objective>
Rendere die Read-Projektion inline in der Absence-Liste (D-07): neuer `HourlyMarkerRow` (chronologisch gemischt mit Range-Rows, sichtbares „stundenbasiert"-Badge, read-only), zwei Affordances — „Stunden bearbeiten" (self+hr, öffnet ein extra_hours-Edit-Modal direkt auf der Absence-Seite, D-08) und „In Zeitraum umwandeln" (HR-only, öffnet das `AbsenceConvertModal` aus Plan 05, D-09). Convert dispatcht `AbsenceAction::ConvertExtraHours`; nach 200 lädt die Liste neu (D-15). Plus die i18n-Keys für die neuen Labels in De/En/Cs.

Purpose: SC1 + SC3 — sichtbare, ehrliche Stunden-Marker + HR-Inline-Convert + Inline-Stunden-Edit.
Output: absences.rs erweitert (HourlyMarkerRow + Modals + Store-Read + Merge) + i18n-Keys + SSR-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-05-SUMMARY.md

<interfaces>
From Plan 05: `state::absence_period::ExtraHoursMarker { extra_hours_id, sales_person_id, when: time::Date, amount: f32, category, description, person_name }`. `service::absence::ABSENCE_HOURLY_STORE: GlobalSignal<Rc<[ExtraHoursMarker]>>`. `AbsenceAction::ConvertExtraHours { extra_hours_id, start, end, day_fraction }`. `component::absence_convert_modal::AbsenceConvertModal { extra_hours_id, initial_date, amount, category, on_submit: EventHandler<(Uuid, Date, Date, DayFractionTO)>, on_cancel }`.

Existing absences.rs (PATTERNS.md Z. 461-499): `AbsenceListProps { rows: Rc<[AbsencePeriod]>, is_hr, today, filter_active, on_row_click }` + `AbsenceList`; `AbsenceListRow` (button, 5-col grid `md:grid-cols-[1.5fr_170px_140px_90px_70px]`). `CategoryBadge` (Z. 104-129, STATIC Tailwind match arms — Pitfall 5). Page reads `ABSENCE_STORE` (line ~1560); store-driven via `AbsenceAction::LoadAll/LoadForSalesPerson` (use_effect line ~1535-1548). `is_hr` computed line ~1504. `ABSENCE_REFRESH` refresh-token. AbsenceList rendered in AbsencesPage RSX (line ~1650 `is_hr: is_hr`).

Existing extra_hours edit: `api::update_extra_hour(config, ExtraHoursTO)` (PUT /extra-hours/{id}, 409-aware). For the inline "Stunden bearbeiten" modal, FIRST check `shifty-dioxus/src/component/` for an existing extra_hours edit modal/form (e.g. extra_hours_modal.rs, add_extra_hours_form.rs); reuse if present. Otherwise build a minimal inline edit modal (date/amount/description) that calls api::update_extra_hour and bumps ABSENCE_REFRESH on success.

Static Tailwind badge pattern (PATTERNS.md Z. 708-719) — STATIC match arms, NEVER format!() for class names:
```rust
let (text_class, bg_class) = match marker.category { Vacation => ("text-good","bg-good-soft"), SickLeave => ("text-warn","bg-warn-soft"), UnpaidLeave => ("text-ink-muted","bg-surface-2") };
```
T-8-XSS-01: all user strings via `"{x}"` interpolation (auto-escape), never dangerous_inner_html.

i18n key pattern: keys added to Key-enum (i18n/mod.rs) + de.rs/en.rs/cs.rs. Per-locale-reference-matcher tests guard the Locale::En-in-de bug (Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n-Keys für Marker-Labels + Aktionen (De/En/Cs)</name>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (Key-enum + present-in-all-locales-Test + per-locale-matcher-Pattern aus 08-04/08-05)
    - shifty-dioxus/src/i18n/de.rs (Locale::De-Einträge — Pitfall 2: NICHT Locale::En verwenden)
    - shifty-dioxus/src/i18n/en.rs + cs.rs
  </read_first>
  <action>
    Füge neue Key-Varianten zum `Key`-Enum in `i18n/mod.rs` hinzu:
    - `AbsenceHourlyBadge` — Badge-Text (De: „stundenbasiert", En: „hours-based", Cs: passende Übersetzung)
    - `AbsenceEditHoursAction` — (De: „Stunden bearbeiten", En: „Edit hours", Cs: …)
    - `AbsenceConvertToRangeAction` — (De: „In Zeitraum umwandeln", En: „Convert to range", Cs: …)
    - `AbsenceHourlyAmountLabel` — Amount-Suffix (De: „Std.", En: „hrs", Cs: …)

    Trage jeden Key in `de.rs` (mit `Locale::De`!), `en.rs` (`Locale::En`), `cs.rs` (`Locale::Cs`) ein. Erweitere die per-locale-reference-matcher-Tests (`i18n_*_match_german_reference` etc.) um mindestens 3 der neuen Keys als Stichprobe (Guard gegen Locale::En-statt-De-Bug, Pitfall 2).
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "AbsenceHourlyBadge" src/i18n/mod.rs && grep -q "AbsenceConvertToRangeAction" src/i18n/de.rs && cargo test i18n 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Neue Keys im Key-Enum + in allen drei Locales (de/en/cs)
    - de.rs-Einträge nutzen `Locale::De` (kein Locale::En)
    - per-locale-matcher-Test um ≥ 3 neue Keys erweitert
    - `cargo test i18n` exit 0 (present-in-all-locales + matcher grün)
  </acceptance_criteria>
  <done>i18n vollständig + Locale-korrekt für die neuen UI-Strings.</done>
</task>

<task type="auto">
  <name>Task 2: HourlyMarkerRow + chronologischer Merge + AbsenceList-Props</name>
  <read_first>
    - shifty-dioxus/src/page/absences.rs:1369-1490 (AbsenceList + AbsenceListRow + CategoryBadge — PATTERNS.md Z. 461-499)
    - shifty-dioxus/src/page/absences.rs:1494-1560 (AbsencesPage mount, is_hr, store-reads, use_effect)
    - .planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md (D-07: chronologisch inline, kein Range-Raten; D-08; D-09)
  </read_first>
  <action>
    1. Erweitere `AbsenceListProps` um `pub hourly_markers: Rc<[ExtraHoursMarker]>` und zwei EventHandler: `pub on_edit_hours: EventHandler<ExtraHoursMarker>` + `pub on_convert: EventHandler<ExtraHoursMarker>`.
    2. In `AbsenceList`: baue eine chronologisch gemischte Render-Liste. Definiere `enum AbsenceListItem { Range(AbsencePeriod), Marker(ExtraHoursMarker) }`, sammle beide Quellen, sortiere nach Datum (Range -> `from_date`, Marker -> `when`), iteriere und rendere `AbsenceListRow` bzw. `HourlyMarkerRow`. Die Empty-State-Logik greift nur wenn BEIDE Quellen leer sind (`props.rows.is_empty() && props.hourly_markers.is_empty()`).
    3. Neuer `HourlyMarkerRow`-Component (inline in absences.rs, Single-File-Pattern):
       Props: `marker: ExtraHoursMarker, is_hr: bool, on_edit: EventHandler<ExtraHoursMarker>, on_convert: EventHandler<ExtraHoursMarker>`.
       Render im gleichen `md:grid-cols-[1.5fr_170px_140px_90px_70px]`-Layout wie AbsenceListRow, aber:
       - person_name (oder „—")
       - statt Range: `"{marker.when}"` + `"{marker.amount} {i18n.t(Key::AbsenceHourlyAmountLabel)}"`
       - Kategorie-Badge mit STATIC Tailwind match arms (Pitfall 5: KEIN `format!()` für Tailwind-Klassen) + ein sichtbares „stundenbasiert"-Badge (`i18n.t(Key::AbsenceHourlyBadge)`)
       - read-only: KEIN onclick-zum-Edit auf der ganzen Row; stattdessen zwei explizite Buttons im letzten Cell:
         - „Stunden bearbeiten" (`Key::AbsenceEditHoursAction`) — immer sichtbar (self+hr, D-08), `onclick: move |_| on_edit.call(marker.clone())`
         - „In Zeitraum umwandeln" (`Key::AbsenceConvertToRangeAction`) — nur in `if props.is_hr { ... }`, `onclick: move |_| on_convert.call(marker.clone())` (D-09, HR-only)
       - description via `"{marker.description}"` (auto-escape, T-8-XSS-01).
    4. In der `AbsenceList`-Render-Schleife: rendere `HourlyMarkerRow { marker, is_hr: props.is_hr, on_edit: props.on_edit_hours, on_convert: props.on_convert }` für Marker-Items, `AbsenceListRow { ... }` für Range-Items.
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "fn HourlyMarkerRow" src/page/absences.rs && grep -q "hourly_markers" src/page/absences.rs && grep -q "AbsenceHourlyBadge" src/page/absences.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AbsenceListProps` trägt `hourly_markers` + `on_edit_hours` + `on_convert`
    - `HourlyMarkerRow`-Component existiert, rendert when/amount + „stundenbasiert"-Badge + 2 Buttons
    - „In Zeitraum umwandeln"-Button nur unter `if props.is_hr`
    - Kategorie-Badge nutzt STATIC match-arm-Klassen (kein `format!()` für `text-`/`bg-`-Klassen)
    - chronologischer Merge nach Datum (AbsenceListItem-Enum)
  </acceptance_criteria>
  <done>Marker inline gerendert, read-only, HR-gated Convert-Affordance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Page-Verdrahtung (Store-Read, Modals, Convert-Dispatch, Inline-Edit) + SSR-Tests</name>
  <read_first>
    - shifty-dioxus/src/page/absences.rs:1494-1660 (AbsencesPage: store-reads, modal-state, use_effect, AbsenceList-Render-Site)
    - shifty-dioxus/src/component/absence_convert_modal.rs (Plan 05 — AbsenceConvertModal Props)
    - shifty-dioxus/src/component/ (existierendes extra_hours-Edit-Modal für D-08, falls vorhanden)
    - shifty-dioxus/src/page/absences.rs (bestehende SSR-Test-Module + pin_de_locale-Helper aus Plan 08-05)
  </read_first>
  <behavior>
    - convert_action_visible_to_hr_only: HourlyMarkerRow mit is_hr=true rendert „In Zeitraum umwandeln"-Button; mit is_hr=false NICHT
    - hourly_marker_row_renders_badge: HourlyMarkerRow rendert den „stundenbasiert"-Badge-Text + amount
    - hourly_marker_row_renders_edit_action: „Stunden bearbeiten"-Button ist auch bei is_hr=false sichtbar (D-08 self+hr)
  </behavior>
  <action>
    1. In `AbsencesPage`: lese `let hourly_markers = ABSENCE_HOURLY_STORE.read().clone();` (neben `ABSENCE_STORE`).
    2. Page-lokaler State für die Convert-Modal-Sichtbarkeit: `let mut convert_target = use_signal(|| None::<ExtraHoursMarker>);` und analog `let mut edit_hours_target = use_signal(|| None::<ExtraHoursMarker>);`.
    3. Reiche an `AbsenceList`: `hourly_markers: hourly_markers.clone()`, `on_edit_hours: move |m| edit_hours_target.set(Some(m))`, `on_convert: move |m| convert_target.set(Some(m))`.
    4. Rendere konditional das `AbsenceConvertModal`, wenn `convert_target` Some ist:
       ```rust
       if let Some(m) = convert_target.read().clone() {
           AbsenceConvertModal {
               extra_hours_id: m.extra_hours_id,
               initial_date: m.when,
               amount: m.amount,
               category: /* ExtraHoursCategory -> AbsenceCategoryTO mapping für die 3 Kategorien */,
               on_submit: move |(id, start, end, day_fraction)| {
                   absence_service.send(AbsenceAction::ConvertExtraHours { extra_hours_id: id, start, end, day_fraction });
                   convert_target.set(None);
               },
               on_cancel: move |_| convert_target.set(None),
           }
       }
       ```
       (D-15: Reload passiert über `bump_absence_refresh()` im ConvertExtraHours-Branch aus Plan 05 → use_effect lädt neu.)
    5. Inline-Stunden-Edit (D-08): wenn `edit_hours_target` Some ist, öffne das wiederverwendete extra_hours-Edit-Modal (falls vorhanden) oder ein minimales inline Modal (Datum/Stunden/Beschreibung), das bei Submit `api::update_extra_hour` ruft und bei Ok `bump_absence_refresh()` + `edit_hours_target.set(None)`. Reuse des bestehenden extra_hours-Update-Pfads + dessen Permission (kein neuer API-Pfad). Falls bereits ein passendes Modal in component/ existiert, dieses verwenden.
    6. SSR-Tests im bestehenden Test-Modul von absences.rs (nutze `pin_de_locale()`-Helper aus Plan 08-05 + VirtualDom-Render): die drei Tests aus <behavior>. Render `HourlyMarkerRow` direkt mit Beispiel-Props (einmal is_hr=true, einmal is_hr=false), asserte auf das Vorhandensein/Fehlen der Button-Labels + des Badge-Texts im gerenderten HTML.
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "ABSENCE_HOURLY_STORE" src/page/absences.rs && grep -q "AbsenceConvertModal" src/page/absences.rs && grep -q "AbsenceAction::ConvertExtraHours" src/page/absences.rs && cargo test -p shifty-dioxus hourly_marker 2>&1 | tail -10; cargo test -p shifty-dioxus convert_action 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Page liest `ABSENCE_HOURLY_STORE`, reicht Marker + beide EventHandler an `AbsenceList`
    - `AbsenceConvertModal` konditional gerendert; on_submit dispatcht `AbsenceAction::ConvertExtraHours`
    - Inline-Stunden-Edit ruft `api::update_extra_hour` + bumpt ABSENCE_REFRESH (kein neuer API-Pfad)
    - ≥ 3 SSR-Tests grün: badge, hr-only-convert-button, edit-button-für-alle
    - `cargo test -p shifty-dioxus hourly_marker` + `... convert_action` exit 0
  </acceptance_criteria>
  <done>Page vollständig verdrahtet; Convert + Inline-Edit + Reload funktionieren; SSR-getestet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend-UI -> Backend | Convert + Edit gehen über api.rs an authz-erzwingende Endpoints |
| Render -> DOM | User-Strings (description, person_name) interpoliert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-06a | Information Disclosure | „In Zeitraum umwandeln"-Button für Non-HR | mitigate | Button nur unter `if props.is_hr`; Backend-hr-Gate ist der echte Guard; SSR-Test `convert_action_visible_to_hr_only` |
| T-8.5-06b | Injection (XSS) | marker.description / person_name | mitigate | RSX `"{x}"`-Interpolation (Dioxus auto-escape); kein dangerous_inner_html (T-8-XSS-01) |
| T-8.5-06c | Tampering | Inline-Stunden-Edit | mitigate | Reuse PUT /extra-hours/{id} mit dessen bestehender Permission; kein neuer ungesicherter Pfad |
</threat_model>

<verification>
- `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` exit 0 (WASM-Gate)
- `cargo test -p shifty-dioxus` (SSR-Tests + i18n) grün
- grep: kein `format!("text-` / `format!("bg-` für Tailwind in absences.rs
</verification>

<success_criteria>
- Stunden-Marker chronologisch inline, „stundenbasiert"-gelabelt, read-only
- HR-Convert öffnet AbsenceConvertModal → ConvertExtraHours → Reload (Marker weg, Range da)
- Inline-Stunden-Edit über bestehenden extra_hours-Update-Pfad
- SSR-Tests + i18n grün
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-06-SUMMARY.md`
</output>
