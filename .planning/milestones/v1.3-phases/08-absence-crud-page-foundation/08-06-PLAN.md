---
phase: 08-absence-crud-page-foundation
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified: []
autonomous: false
requirements: [FUI-A-01, FUI-A-02, FUI-A-03, FUI-A-04]
requirements_addressed: [FUI-A-01, FUI-A-02, FUI-A-03, FUI-A-04]
tags: [shifty, uat-smoke, end-to-end, closure]
must_haves:
  truths:
    - "HR-User UAT-Smoke: Login → Menü-Eintrag sichtbar → Liste zeigt alle Mitarbeiter → Anlage neu → Edit existing → Delete via Confirm → Resturlaubs-Card zeigt Team-Aggregat — FUI-A-01..04"
    - "Employee-User UAT-Smoke: Login → Menü-Eintrag sichtbar → Liste zeigt nur eigene Einträge → Anlage neu → Edit eigene → Delete eigene → Resturlaubs-Card zeigt Self-Aggregat"
    - "Backend cargo check --workspace + cargo test --workspace grün als Final-Regression-Gate"
    - "Frontend WASM-Build grün als Final-Compile-Gate"
    - "Phase-8-Goal-Success-Criteria 1-5 (ROADMAP.md) sind alle als verified markierbar"
  artifacts: []
  key_links: []
---

<objective>
Wave-6-Closure für Phase 8: End-to-End-UAT-Smoke gegen Integrationsumgebung. Verifiziere Goal-Success-Kriterien aus ROADMAP.md § "Phase 8". Final-Regression-Gates ausführen.

Purpose: Bestätigung, dass die Phase-Implementation USER-FACING funktioniert — nicht nur compiletechnisch grün ist. UI-Closure für v1.3 Phase 8.

Output:
- UAT-Smoke-Protokoll im SUMMARY (HR + Employee Login je einmal Anlage + Edit + Delete + Resturlaubs-Anzeige)
- Final-Regression-Confirmation (Backend Full-Suite + WASM-Build)
- Plan-Closure-Verifikation aller Phase-8-Goal-Success-Criteria
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

## Project Constraints

- **VCS:** jj-only. KEINE neuen Commits in Plan 06 außer SUMMARY.md falls gewünscht.
- **NixOS:** `nix develop` für `cargo run` (Backend), `dx serve` (Frontend).
- **UAT-Setup:** Backend (Port 3000) UND Frontend (Port 8080) parallel laufen lassen. Frontend proxy-routet `/absence-period` und `/vacation-balance` an Backend (Plan 04 Dx-Proxy-Eintrag).

## D-Mapping

- D-09: HR/Employee-Sicht aus Auth-Context — Test mit zwei verschiedenen Test-Usern.
- D-10: Menü-Eintrag für alle eingeloggten User — beide Test-User sehen ihn.
- D-11: 422-Banner — UAT erzwingt Self-Overlap.
- D-12: Forward-Warnings — UAT erzwingt Booking-Überlappung.
- D-08: 409-Banner — Optional, falls zwei Browser-Tabs nutzbar.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-absence-crud-page-foundation/08-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md
@.planning/phases/08-absence-crud-page-foundation/08-04-PLAN.md
@.planning/phases/08-absence-crud-page-foundation/08-05-PLAN.md
@CLAUDE.md
@CLAUDE.local.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Final-Regression-Gates (Backend Full-Suite + WASM-Build)</name>

  <read_first>
    - .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md § "Sampling Rate"
    - .planning/STATE.md § "Constraints In Force"
  </read_first>

  <files>(none)</files>

  <action>
    Im Repo-Root ausführen:

    1. **Backend Full-Suite:**
       ```bash
       nix develop --command cargo check --workspace 2>&1 | tail -20
       nix develop --command cargo test --workspace 2>&1 | tail -20
       ```
       Erwartung: beide grün. Test-Anzahl ≥ 466 (v1.2-Baseline) + ≥6 neue VacationBalance-Tests aus Plan 02.

    2. **Frontend WASM-Build:**
       ```bash
       cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown 2>&1 | tail -20
       ```
       Erwartung: grün, Exit-Code 0.

    3. **Frontend-Workspace-Tests:**
       ```bash
       cd shifty-dioxus && nix develop --command cargo test 2>&1 | tail -20
       ```
       Erwartung: grün, inkl. i18n-Tests aus Plan 04.

    4. **Status-File-Schritt:** Falls eines der Gates rot ist → eskaliere als Bug, fix in einem Followup-Plan, NICHT durch UAT-Skip ignorieren.
  </action>

  <verify>
    <automated>nix develop --command cargo test --workspace 2>&1 | tail -10 &amp;&amp; nix develop --command cargo build --target wasm32-unknown-unknown --manifest-path shifty-dioxus/Cargo.toml 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `nix develop --command cargo check --workspace` Exit-Code 0
    - `nix develop --command cargo test --workspace` Exit-Code 0; Output enthält "test result: ok"
    - `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` Exit-Code 0
    - `cd shifty-dioxus && nix develop --command cargo test` Exit-Code 0
  </acceptance_criteria>

  <done>Alle automatischen Gates grün; UAT (Task 2) ist freigeschaltet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT-Smoke — HR-User-Flow</name>

  <action>Human checkpoint — siehe &lt;how-to-verify&gt; für die exakten Schritte. Agent pausiert bis User "approved" oder Abweichung beschreibt.</action>

  <what-built>
    Phase 8 hat geliefert: Top-Level-Route `/absences`, Backend-Resturlaubs-Endpoint, AbsenceModal mit CRUD, VacationEntitlementCard + VacationPerPersonList, HR/Employee-Sicht-Switch aus Auth-Context, i18n in De/En/Cs.

    Verifiziert wird der HR-User-Flow gegen die Integrationsumgebung.
  </what-built>

  <how-to-verify>
    **Setup:**

    1. Backend starten (Repo-Root, neue Shell):
       ```bash
       nix develop --command cargo run
       ```
       Warten bis "listening on 0.0.0.0:3000".

    2. Tailwind-Watch starten (in shifty-dioxus, neue Shell):
       ```bash
       cd shifty-dioxus
       npx tailwindcss -i ./input.css -o ./assets/tailwind.css --watch
       ```

    3. Frontend Dev-Server starten (in shifty-dioxus, neue Shell):
       ```bash
       cd shifty-dioxus
       nix develop --command dx serve --hot-reload
       ```
       Warten bis Dev-Server URL ausgegeben (typisch http://localhost:8080).

    **HR-User-Flow:**

    1. Login als HR-User (Mock-Auth / OIDC-User mit `hr`-Privilege).
    2. Top-Bar zeigt Menü-Eintrag "Abwesenheiten" (De) / "Absences" (En) / "Nepřítomnosti" (Cs) — abhängig von Locale-Wahl.
    3. Click "Abwesenheiten" → Page rendert auf `/absences`.
    4. **VacationEntitlementCard** zeigt **Team-Variante** mit:
       - Header "Urlaubsanspruch Team · {N} Personen"
       - Stats-Boxen Vertrag / Übertrag / Genommen / Beantragt / Verbleibend
       - **VacationPerPersonList**-Section mit Avatar + Name + Resturlaub pro Mitarbeiter, sortiert nach `remaining` aufsteigend
    5. **AbsenceFilterBar** zeigt drei Filter: Person-Dropdown (HR-only) + Kategorie-Pills + Status-Dropdown.
    6. **AbsenceList** zeigt alle Mitarbeiter-Abwesenheiten in 5-Spalten-Layout (Person+Description / Range+Days / CategoryBadge / StatusPill / Warnings).
    7. Click **"Neue Abwesenheit"**-Btn → Modal öffnet (Center, Width 520).
    8. Modal-Form:
       - Mitarbeiter-Dropdown: editierbar (HR-Variante).
       - Kategorie: Vacation/SickLeave/UnpaidLeave wählbar.
       - Von / Bis: zwei native Date-Inputs.
       - Beschreibung: Textarea.
    9. **Cross-Field-Validation** (D-05):
       - Setze Bis vor Von → Field-Error "Enddatum liegt vor Startdatum" am Bis-Feld; Save-Btn DISABLED.
       - Korrigiere → Save-Btn ENABLED.
    10. Wähle einen Mitarbeiter, Kategorie=Vacation, Von=heute+1, Bis=heute+5, Description="UAT-Test".
    11. Click "Anlegen" → 201 → Modal schließt (sofern keine Forward-Warnings; falls die Vacation-Range einen existing Booking überlappt, **WarningList** rendert + Submit-Btn-Beschriftung wechselt zu "Verstanden" → click "Verstanden" → Modal-Close).
    12. **AbsenceList** refreshed automatisch via `ABSENCE_REFRESH`-Bump → neuer Eintrag sichtbar.
    13. **VacationEntitlementCard** refreshed → Team-Stats neu (Genommen/Beantragt aktualisiert für den ausgewählten Mitarbeiter).
    14. Click auf den eben angelegten Eintrag in der Liste → Modal öffnet im **Edit-Mode**.
    15. Ändere Description, click "Speichern" → 200 → Modal schließt → Liste reflektiert Änderung.
    16. Click den Eintrag erneut → Modal Edit → Click **Delete-Btn** (Danger, links unten).
    17. **DeleteConfirmDialog** öffnet (Center, Width 360, NICHT `window.confirm`):
        - Title "Abwesenheit löschen?"
        - Body "Soft-Delete — der Eintrag bleibt für Audit-Logs erhalten..."
        - Buttons: Cancel (Ghost) + Löschen (Danger)
    18. Click "Löschen" → 204 → beide Modals schließen → Liste reflektiert (Eintrag verschwunden).

    **Self-Overlap-Test (D-11):**

    19. Click "Neue Abwesenheit" → wähle gleichen Mitarbeiter, Kategorie=Vacation, Range überlappt mit existierendem Eintrag (z. B. soeben gelöschter Range-Window — falls bereits andere Vacation existiert) → Save → Backend liefert 422 → **SelfOverlapBanner** rendert inline unter Range-Feldern (linker `border-bad`, `bg-bad-soft`, Header "Selbst-Überlappung"). Modal bleibt OFFEN, Form-State erhalten.
    20. Korrigiere Range → Save → 201 → Modal schließt.

    **Sign-Off-Frage:**

    War der HR-User-Flow vollständig erfolgreich (Schritte 1-20)? Falls Schritt 19 nicht reproduzierbar war (kein passender existing Eintrag), markiere ihn als skipped.
  </how-to-verify>

  <resume-signal>Type "approved" wenn alle Schritte 1-20 (oder 1-18 falls 19 skipped) erfolgreich, sonst beschreibe genau welcher Schritt fehlschlug.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT-Smoke — Employee-User-Flow</name>

  <action>Human checkpoint — siehe &lt;how-to-verify&gt; für die exakten Schritte. Agent pausiert bis User "approved" oder Abweichung beschreibt.</action>

  <what-built>Verifiziert wird der Employee-User-Flow — Sicht-Auswahl ohne HR-Privilege, eingeschränkte Filter-Bar, Self-Resturlaubs-Anzeige.</what-built>

  <how-to-verify>
    **Setup-Voraussetzung:** Backend + Frontend laufen aus Task 2 weiter (oder restart, falls Task 2 closed wurde).

    **Employee-User-Flow:**

    1. Logout aus HR-User; Login als Employee-User (Mock-Auth / OIDC-User OHNE `hr`-Privilege; aber MIT `sales_person_id` zugewiesen).
    2. Top-Bar zeigt Menü-Eintrag "Abwesenheiten" (D-10: für alle eingeloggten User).
    3. Click "Abwesenheiten" → Page rendert auf `/absences`.
    4. **VacationEntitlementCard** zeigt **Self-Variante**:
       - 2-Spalten-Hero-Layout
       - Hero-Spalte mit `text-display font-mono text-good` Hero-Zahl (`{remaining}/{entitled}`)
       - Sublabel "Tage verbleibend"
       - Card-Title "Dein Urlaubskonto"
       - Stats Vertrag / Übertrag / Genommen / Beantragt / Verbleibend
       - **KEINE** VacationPerPersonList-Section (HR-only)
    5. **AbsenceFilterBar** zeigt **KEIN** Person-Dropdown (Employee-Variante); nur Kategorie + Status + "Vergangene anzeigen".
    6. **AbsenceList** zeigt nur Einträge des eingeloggten Employee.
    7. Click "Neue Abwesenheit" → Modal öffnet.
    8. Modal-Form:
       - Mitarbeiter-Dropdown: **DISABLED**, vorgefüllt mit dem eingeloggten Employee (D-09 `lock_person = true`).
       - Kategorie / Range / Description: editierbar.
    9. Anlage erfolgt analog HR-Flow Schritt 10-12 → Modal schließt → Liste refreshed → VacationCard refreshed.
    10. Edit eines eigenen Eintrags analog → 200 → Modal schließt.
    11. Delete eines eigenen Eintrags via Confirm-Dialog → 204 → Liste refreshed.
    12. **Forbidden-Test (Defense-in-Depth):** versuche im Browser-DevTools direkt `GET /absence-period/by-sales-person/{andere-uuid}` (z. B. einer fremden Person) — Backend liefert 403 (nicht 200). UI hat fremde IDs nicht exposed, aber dieser Smoke-Test bestätigt das Backend-Gate.

    **i18n-Stichprobe (D-13 + FUI-A-09):**

    13. Switche Locale auf En → Page-Title rendert "Absences", Kategorie-Pills rendern "Vacation"/"Sick leave"/"Unpaid leave".
    14. Switche Locale auf Cs → Page-Title rendert "Nepřítomnosti", Kategorie-Pills rendern "Dovolená"/"Nemoc"/"Neplacené".
    15. Switche zurück auf De → "Abwesenheiten", "Urlaub"/"Krankheit"/"Unbezahlt".

    **Sign-Off-Frage:**

    War der Employee-User-Flow vollständig erfolgreich (Schritte 1-15)?

    **Goal-Success-Criteria-Mapping (zur Phase-8-Closure):**
    - [ ] SC-1 (FUI-A-01): Route `/absences` via Menü erreichbar; HR-Privileg schaltet Filter-Modus → bestätigt Schritte 2 (HR) + 2 (Employee) + 5 (Filter-Bar-Variante).
    - [ ] SC-2 (FUI-A-02 + FUI-A-03): CRUD AbsencePeriodTO mit Range-Picker, Kategorie-Dropdown, Description; 422 inline gerendert → bestätigt Schritte 8-11 (HR) + 19-20 (Self-Overlap).
    - [ ] SC-3 (FUI-A-04): Forward-Warnings als nicht-blockierende Liste → bestätigt Schritt 11 (HR-Flow Forward-Warnings).
    - [ ] SC-4 (D-03/D-04): Backend-Resturlaubs-Endpoint mit hr ∨ self + utoipa + ToSchema; Frontend-Komponenten konsumieren ihn → bestätigt Schritte 4 (HR Team) + 4 (Employee Self).
    - [ ] SC-5: WASM-Build grün, Backend Full-Suite grün, UAT erfolgreich → bestätigt Plan-06-Task-1 + Tasks 2-3.
  </how-to-verify>

  <resume-signal>Type "approved" wenn alle Schritte 1-15 erfolgreich UND alle 5 Goal-Success-Criteria als bestätigt markierbar sind, sonst beschreibe genau welcher Schritt fehlschlug.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-Browser → Frontend | Manuelles Login als HR + Employee — UAT verifiziert UI-Behavior |
| Browser DevTools → Backend (Task 3 Schritt 12) | Manueller curl/fetch-Versuch auf andere sales_person_id — defense-in-depth-Verifikation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-01 | Elevation of Privilege | HR-Filter-Bypass | mitigate (UAT) | Task 3 Schritt 12 verifiziert manuell, dass Backend bei direktem Zugriff auf fremde sales_person_id 403 liefert. |
| T-8-AUTH-02 | Elevation of Privilege | HR-Team-Endpoint | mitigate (UAT) | Task 3 Schritt 4 (Self-Variante zeigt KEINE PerPersonList) plus Task 2 Schritt 4 (HR sieht PerPersonList) zusammen verifizieren, dass `/vacation-balance/team/*` HR-only ist. |
| T-8-IDOR-01 | Information Disclosure | Modal Edit-Bypass | mitigate (UAT) | Task 3 Schritt 12 verifiziert defense-in-depth (Backend-Gate gegen direkten REST-Zugriff). Frontend ist nur UI-Display. |
| T-8-XSS-01 | Tampering | Description-Field | mitigate (UAT-Optional) | Tester kann optional in Task 2/3 eine Description mit `<script>alert(1)</script>` anlegen → erwartet escaped als Text gerendert (kein Alert popup). |
| T-8-INJECTION-01 | Injection | i18n-Substitution | accept | i18n-Substitutions sind statisch typed (kein User-Input fließt durch); UAT verifiziert per Schritte 13-15, dass Locale-Switch deterministisch funktioniert. |
| T-8-PERSISTENCE | Tampering | Soft-Delete | accept | Task 2 Schritt 17-18 + Task 3 Schritt 11: Delete soft-deleted nur (Body-Text confirmt das). Re-Read würde aus Audit-Logs noch sichtbar sein, aber UI zeigt Eintrag nicht mehr. |
</threat_model>

<verification>
**Layer 1 — Automated Gates (Task 1):**
- Backend `cargo check --workspace` + `cargo test --workspace` grün
- Frontend `cargo build --target wasm32-unknown-unknown` + `cargo test` grün

**Layer 2 — Manual UAT (Tasks 2 + 3):**
- HR-Flow: Schritte 1-20 erfolgreich (oder 1-18 falls Self-Overlap-Reproduktion nicht möglich)
- Employee-Flow: Schritte 1-15 erfolgreich
- Defense-in-Depth: Backend liefert 403 bei direktem Zugriff auf fremde sales_person_id

**Layer 3 — Goal-Success-Criteria-Closure:**
- Alle 5 SC aus ROADMAP.md § "Phase 8" als bestätigt markierbar
</verification>

<success_criteria>
- Backend Full-Suite grün
- WASM-Build grün
- HR-User-UAT erfolgreich (Anlage + Edit + Delete + Resturlaubs-Anzeige + Self-Overlap-Inline-Banner)
- Employee-User-UAT erfolgreich (Anlage + Edit + Delete + Resturlaubs-Self-Anzeige + lock_person + i18n-Locale-Switch)
- Defense-in-Depth-Backend-403-Check erfolgreich (Task 3 Schritt 12)
- Alle 5 Phase-8-Goal-Success-Criteria erfüllt
- Phase 8 ist closure-ready für `/gsd-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/08-absence-crud-page-foundation/08-06-SUMMARY.md`.

Im SUMMARY:
- HR-Flow Schritte 1-20 mit Pass/Fail-Markierung
- Employee-Flow Schritte 1-15 mit Pass/Fail-Markierung
- Goal-Success-Criteria-Verification (SC-1..5 ja/nein/teilweise)
- Final-Test-Counts (Backend + Frontend)

User commited manuell, falls SUMMARY committed werden soll: `jj describe -m "test(08): UAT smoke — phase 8 closure"`.
</output>
</content>
</invoke>