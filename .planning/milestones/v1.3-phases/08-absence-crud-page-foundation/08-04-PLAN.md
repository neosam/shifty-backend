---
phase: 08-absence-crud-page-foundation
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - shifty-dioxus/src/state/mod.rs
  - shifty-dioxus/src/state/absence_period.rs
  - shifty-dioxus/src/state/vacation_balance.rs
  - shifty-dioxus/src/error.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/service/mod.rs
  - shifty-dioxus/src/service/absence.rs
  - shifty-dioxus/src/service/vacation_balance.rs
  - shifty-dioxus/src/app.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/Dioxus.toml
autonomous: true
requirements: [FUI-A-01, FUI-A-02, FUI-A-03, FUI-A-04]
requirements_addressed: [FUI-A-01, FUI-A-02, FUI-A-03, FUI-A-04]
tags: [shifty-dioxus, absence, frontend, api, state, service-coroutine, i18n, dx-proxy, error-variant]
must_haves:
  truths:
    - "API-Layer in src/api.rs liefert 7 neue Funktionen (list_absence_periods, list_absence_periods_by_sales_person, get_absence_period, create_absence_period, update_absence_period, delete_absence_period, get_vacation_balance + get_team_vacation_balance) — D-03"
    - "404 wird für `Uuid::nil()` bei POST verhindert: api::create_absence_period setzt id=Uuid::nil() im Body — Backend würfelt ID neu (Pitfall 9 / Anti-Pattern)"
    - "ShiftyError::Validation(String) Variant existiert (für 422 Self-Overlap inline) — D-11, RESEARCH.md OQ3"
    - "ShiftyError::Conflict bleibt unverändert für 409 Version-Konflikt — D-08"
    - "GlobalSignals ABSENCE_STORE, ABSENCE_REFRESH, VACATION_BALANCE_STORE, VACATION_TEAM_STORE existieren mit Coroutine-Services absence_service + vacation_balance_service — Pattern aus service/employee.rs"
    - "Coroutines sind in app.rs registriert via use_coroutine — sonst no-op"
    - "i18n-Keys für Absence-Domain existieren (mind. 50 Keys aus UI-SPEC Copywriting Tabelle) und sind in en.rs/de.rs/cs.rs befüllt — D-13, FUI-A-09"
    - "Locale::De wird konsequent in de.rs verwendet, Locale::Cs in cs.rs (kein Locale::En-Bug, Pitfall 2)"
    - "Dioxus.toml hat Proxy-Einträge für /absence-period UND /vacation-balance — Pitfall 10"
    - "WASM-Build (cargo build --target wasm32-unknown-unknown) bleibt grün ohne service-impl-Pull-In (Pitfall 1)"
    - "i18n-Test prüft, dass alle drei Locales für die neuen Keys getmappt sind und keinen \"??\"-Fallback liefern (D-13, Pitfall 2)"
    - "Plan 04 hat 6 Tasks (Iteration-1 Revision: i18n-Mega-Task wurde in 3a/3b/3c/3d gesplittet zur Pitfall-2-Risiko-Mitigation; jeder Sub-Task ist Locale-spezifisch und ~5 Min Implementation)"
  artifacts:
    - path: shifty-dioxus/src/state/absence_period.rs
      provides: "AbsencePeriod state-type + AbsenceCategory enum + From<&AbsencePeriodTO>"
      min_lines: 40
      contains: "pub struct AbsencePeriod"
    - path: shifty-dioxus/src/state/vacation_balance.rs
      provides: "VacationBalance state-type + From<&VacationBalanceTO>"
      min_lines: 25
      contains: "pub struct VacationBalance"
    - path: shifty-dioxus/src/service/absence.rs
      provides: "AbsenceAction enum + ABSENCE_STORE + ABSENCE_REFRESH + absence_service coroutine"
      min_lines: 80
      contains: "pub enum AbsenceAction"
    - path: shifty-dioxus/src/service/vacation_balance.rs
      provides: "VacationBalanceAction enum + VACATION_*_STORE + vacation_balance_service coroutine"
      min_lines: 50
      contains: "pub enum VacationBalanceAction"
    - path: shifty-dioxus/src/api.rs
      provides: "7 neue async fn (Absence-CRUD + Vacation-Balance)"
      contains: "pub async fn create_absence_period"
    - path: shifty-dioxus/src/error.rs
      provides: "ShiftyError::Validation(String) Variant"
      contains: "Validation"
    - path: shifty-dioxus/src/i18n/mod.rs
      provides: "≥50 neue Key-Variants unter // Absence management + i18n-Test"
      contains: "AbsencePageTitle"
    - path: shifty-dioxus/Dioxus.toml
      provides: "[[web.proxy]] für /absence-period und /vacation-balance"
      contains: "vacation-balance"
  key_links:
    - from: shifty-dioxus/src/app.rs
      to: shifty-dioxus/src/service/absence.rs::absence_service
      via: "use_coroutine(service::absence::absence_service)"
      pattern: "use_coroutine\\(service::absence::absence_service\\)"
    - from: shifty-dioxus/src/app.rs
      to: shifty-dioxus/src/service/vacation_balance.rs::vacation_balance_service
      via: "use_coroutine(service::vacation_balance::vacation_balance_service)"
      pattern: "use_coroutine\\(service::vacation_balance::vacation_balance_service\\)"
    - from: shifty-dioxus/src/api.rs::create_absence_period
      to: backend /absence-period
      via: "reqwest POST"
      pattern: "/absence-period"
    - from: shifty-dioxus/src/api.rs::get_vacation_balance
      to: backend /vacation-balance/{sp}/{year}
      via: "reqwest GET"
      pattern: "/vacation-balance/"
---

<objective>
Wave-4-Frontend-Foundation: Lege alle Infrastruktur-Layer für die Absence-Page an, OHNE die Page selbst (das ist Plan 05). Konkret:

- API-Layer (src/api.rs) für `/absence-period` (CRUD) + `/vacation-balance` (read).
- Error-Variant `ShiftyError::Validation(String)` für 422 Self-Overlap (D-11).
- State-Types (AbsencePeriod + VacationBalance) mit From-TOs.
- Loader-Funktionen mit Cross-Resolve (HR-Liste mit SalesPerson-Namen).
- Service-Coroutines (absence_service, vacation_balance_service) mit Action-Enums + GlobalSignal-Stores.
- Coroutine-Registrierung in app.rs.
- Vollständiges i18n-Set (~50 Keys, drei Locales befüllt) + i18n-Test.
- Dx-Proxy für beide Backend-Resources.

Purpose: Plan 05 baut darauf die Page + Modal + Routing + TopBar. Indem die Layers hier vorab gerollt werden, hält Plan 05 den Fokus auf UI-Komposition statt API-/State-Plumbing.

Output: Alle Foundation-Files. WASM-Build grün, Backend nicht-aufgerufen aber kompilierbar.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

## Project Constraints (verbindlich)

- **VCS:** jj-only. Kein `git commit`/`git add`. Empfohlene jj-Commits (logisch): `feat(08): absence frontend api+error+state`, `feat(08): absence + vacation_balance coroutine services`, `feat(08): absence i18n keys for de/en/cs`, `chore(08): proxy entries for absence-period + vacation-balance`.
- **NixOS:** `nix develop` für WASM-Toolchain (Pitfall 9).
- **rest-types Cross-Crate:** Frontend zieht rest-types mit `default-features = false` (Pitfall 1). Plan 04 darf den Eintrag in `shifty-dioxus/Cargo.toml` Z. 28-30 NICHT verändern.
- **i18n-Drei-Locales-Pflicht:** `Locale::De` in de.rs, `Locale::Cs` in cs.rs, `Locale::En` in en.rs. Pre-Commit grep gegen falschem Locale-Tag (Pitfall 2).
- **WASM-Build-Gate:** `cargo build --target wasm32-unknown-unknown --manifest-path shifty-dioxus/Cargo.toml` MUSS am Ende grün sein.
- **Tailwind-Klassen:** Domain-Farb-Match-Arme statisch (per Plan 05); Plan 04 berührt keine Tailwind-Klassen direkt — i18n-Strings sind reine Text-Strings.

## D-Mapping

- D-03: Resturlaubs-Endpoint-API-Funktion + Loader hier; Page-Render in Plan 05.
- D-08: Conflict-Variant existiert bereits in error.rs (`Conflict(String)`); Plan 04 fügt nur `Validation(String)` hinzu (D-11).
- D-09/D-10: Auth-Privilege-Check ist im Service NICHT involviert (passiert in der Page); Service liest die `auth.has_privilege("hr")` über Caller-Path.
- D-11: 422 inline → `Validation(String)` Variant in ShiftyError; api.rs setzt diesen Variant bei `StatusCode::UNPROCESSABLE_ENTITY`.
- D-12: Forward-Warnings rendern in Plan 05 (Page); api.rs liefert `AbsencePeriodCreateResultTO` durch.
- D-13: i18n inline mit Implementation; alle drei Locales gleichzeitig befüllt.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-absence-crud-page-foundation/08-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md
@.planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md
@.planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md
@CLAUDE.md
@CLAUDE.local.md
@shifty-dioxus/CLAUDE.md

<interfaces>
<!-- Aus Plan 01/02: -->
<!-- rest_types::AbsencePeriodTO, rest_types::AbsenceCategoryTO, rest_types::AbsencePeriodCreateResultTO, rest_types::WarningTO -->
<!-- rest_types::VacationBalanceTO -->

From rest-types/src/lib.rs Z. 1565+:
```rust
pub struct AbsencePeriodTO {
    pub id: Uuid,
    pub sales_person_id: Uuid,
    pub category: AbsenceCategoryTO,
    pub from_date: time::Date,
    pub to_date: time::Date,
    pub description: Arc<str>,
    pub created: Option<time::PrimitiveDateTime>,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,  // serde-rename "$version"
}

pub enum AbsenceCategoryTO { Vacation, SickLeave, UnpaidLeave }

pub struct AbsencePeriodCreateResultTO {
    pub absence: AbsencePeriodTO,
    pub warnings: Vec<WarningTO>,
}

pub enum WarningTO {
    AbsenceOverlapsBooking { /* ... */ },
    AbsenceOverlapsManualUnavailable { /* ... */ },
    PaidEmployeeLimitExceeded,
    // weitere Varianten — siehe rest-types/src/lib.rs
}

pub struct VacationBalanceTO {
    pub sales_person_id: Uuid,
    pub year: u32,
    pub entitled_days: f32,
    pub carryover_days: i32,
    pub used_days: f32,
    pub planned_days: f32,
    pub remaining_days: f32,
}
```

From shifty-dioxus/src/api.rs:392-468 (existing extra_hours-Pattern — exakte Vorlage für 409-Handling):
```rust
pub async fn update_extra_hour(config: Config, extra_hours: ExtraHoursTO) -> Result<ExtraHoursTO, ShiftyError> {
    let url = format!("{}/extra-hours/{}", config.backend, extra_hours.id);
    let client = reqwest::Client::new();
    let response = client.put(url).json(&extra_hours).send().await?;
    if response.status() == reqwest::StatusCode::CONFLICT {
        return Err(ShiftyError::Conflict(String::new()));
    }
    response.error_for_status_ref()?;
    let updated: ExtraHoursTO = response.json().await?;
    Ok(updated)
}
```

From shifty-dioxus/src/error.rs (existing):
```rust
#[derive(Error, Debug)]
pub enum ShiftyError {
    #[error("reqwest error: {0}")]
    Reqwest(#[from] reqwest::Error),
    #[error("Time ComponentRange error: {0}")]
    TimeComponentRange(#[from] time::error::ComponentRange),
    #[error("{0}")]
    Conflict(String),
}
```

From shifty-dioxus/src/service/employee.rs (Coroutine-Service-Pattern Z. 28-86, 191-276):
```rust
pub static EMPLOYEE_STORE: GlobalSignal<EmployeeStore> = Signal::global(|| EmployeeStore { /* default */ });
pub static EMPLOYEES_LIST_REFRESH: GlobalSignal<u64> = Signal::global(|| 0);

pub(crate) fn bump_employees_list_refresh() {
    *EMPLOYEES_LIST_REFRESH.write() += 1;
}

pub enum EmployeeAction { /* ... */ }

pub async fn employee_service(mut rx: UnboundedReceiver<EmployeeAction>) {
    use futures_util::StreamExt;
    while let Some(action) = rx.next().await {
        // match action { ... }
    }
}
```

From shifty-dioxus/src/loader.rs Z. 76-102 (Cross-Resolve-Pattern):
```rust
pub async fn load_bookings(
    config: Config, sales_persons: Rc<[SalesPerson]>, week: u8, year: u32,
) -> Result<Rc<[Booking]>, ShiftyError> {
    let booking_tos = api::get_bookings_for_week(config, week, year).await?;
    let bookings: Rc<[Booking]> = booking_tos.iter()
        .map(|to| to.into())
        .map(|booking: Booking| { /* join with sales_persons */ })
        .collect();
    Ok(bookings)
}
```

From shifty-dioxus/Dioxus.toml Z. 45-92:
```toml
[[web.proxy]]
backend = "http://localhost:3000/extra-hours"
[[web.proxy]]
backend = "http://localhost:3000/working-hours"
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: API-Layer + Error-Variant + State-Types + Loader</name>

  <read_first>
    - shifty-dioxus/src/api.rs Z. 1-50 (Imports + Config-Helper); Z. 392-468 (extra_hours-CRUD-Block — exakte Vorlage für POST/PUT/DELETE/GET-Pattern inkl. 409-Handling)
    - shifty-dioxus/src/error.rs (gesamte Datei, 44 LOC — existing Variants + error_handler-Function)
    - shifty-dioxus/src/state/employee.rs (struct-with-From-Pattern, ca. Z. 170+ für die TO→state-Konversion)
    - shifty-dioxus/src/state/mod.rs (existing pub mod-Liste)
    - shifty-dioxus/src/loader.rs Z. 1-130 (gesamter Loader-Pattern-Block, insb. `load_bookings`-Cross-Resolve Z. 76-102)
    - shifty-dioxus/Cargo.toml Z. 28-30 (rest-types Dep — NICHT verändern)
    - shifty-dioxus/CLAUDE.md (Konventionen)
    - rest-types/src/lib.rs Z. 1565..2040 (AbsencePeriodTO + Cat + Wrapper + Warning + VacationBalanceTO aus Plan 01)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Code Example 2" (api-funktions-Bodies); § "Pitfall 9" (Uuid::nil bei POST); § "Pattern 3" (409-Handling)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "shifty-dioxus/src/api.rs" + § "shifty-dioxus/src/loader.rs" + § "shifty-dioxus/src/error.rs" + § "shifty-dioxus/src/state/absence_period.rs"
  </read_first>

  <files>
    shifty-dioxus/src/error.rs (modify),
    shifty-dioxus/src/state/absence_period.rs (NEW),
    shifty-dioxus/src/state/vacation_balance.rs (NEW),
    shifty-dioxus/src/state/mod.rs (modify),
    shifty-dioxus/src/api.rs (modify),
    shifty-dioxus/src/loader.rs (modify)
  </files>

  <action>
    **A. `shifty-dioxus/src/error.rs` (modify):** Füge `ShiftyError::Validation(String)` Variant hinzu:
    ```rust
    /// HTTP 422 Validation error — e.g. self-overlap of absence periods (D-11).
    #[error("{0}")]
    Validation(String),
    ```
    Dazu im `error_handler` (oder analoger error-Match-Funktion) einen `ShiftyError::Validation(msg) => eprintln!("Validation: {}", msg),`-Arm ergänzen, falls die existierende Funktion match-exhaustive ist.

    **B. `shifty-dioxus/src/state/absence_period.rs` (NEW):**
    ```rust
    use rest_types::{AbsencePeriodTO, AbsenceCategoryTO};
    use std::sync::Arc;
    use uuid::Uuid;

    #[derive(Clone, Debug, PartialEq, Eq)]
    pub enum AbsenceCategory { Vacation, SickLeave, UnpaidLeave }

    impl From<&AbsenceCategoryTO> for AbsenceCategory {
        fn from(c: &AbsenceCategoryTO) -> Self {
            match c {
                AbsenceCategoryTO::Vacation => Self::Vacation,
                AbsenceCategoryTO::SickLeave => Self::SickLeave,
                AbsenceCategoryTO::UnpaidLeave => Self::UnpaidLeave,
            }
        }
    }
    impl From<&AbsenceCategory> for AbsenceCategoryTO {
        fn from(c: &AbsenceCategory) -> Self {
            match c {
                AbsenceCategory::Vacation => Self::Vacation,
                AbsenceCategory::SickLeave => Self::SickLeave,
                AbsenceCategory::UnpaidLeave => Self::UnpaidLeave,
            }
        }
    }

    #[derive(Clone, Debug, PartialEq, Eq)]
    pub struct AbsencePeriod {
        pub id: Uuid,
        pub sales_person_id: Uuid,
        pub category: AbsenceCategory,
        pub from_date: time::Date,
        pub to_date: time::Date,
        pub description: Arc<str>,
        pub version: Uuid,
        // Side-join Felder (Loader füllt):
        pub person_name: Arc<str>,
        pub background_color: Arc<str>,
    }

    impl From<&AbsencePeriodTO> for AbsencePeriod {
        fn from(t: &AbsencePeriodTO) -> Self {
            Self {
                id: t.id,
                sales_person_id: t.sales_person_id,
                category: (&t.category).into(),
                from_date: t.from_date,
                to_date: t.to_date,
                description: t.description.clone(),
                version: t.version,
                person_name: "".into(),
                background_color: "".into(),
            }
        }
    }
    ```

    **C. `shifty-dioxus/src/state/vacation_balance.rs` (NEW):**
    ```rust
    use rest_types::VacationBalanceTO;
    use uuid::Uuid;

    #[derive(Clone, Debug, PartialEq)]
    pub struct VacationBalance {
        pub sales_person_id: Uuid,
        pub year: u32,
        pub entitled_days: f32,
        pub carryover_days: i32,
        pub used_days: f32,
        pub planned_days: f32,
        pub remaining_days: f32,
    }

    impl From<&VacationBalanceTO> for VacationBalance {
        fn from(t: &VacationBalanceTO) -> Self {
            Self {
                sales_person_id: t.sales_person_id,
                year: t.year,
                entitled_days: t.entitled_days,
                carryover_days: t.carryover_days,
                used_days: t.used_days,
                planned_days: t.planned_days,
                remaining_days: t.remaining_days,
            }
        }
    }
    ```

    **D. `shifty-dioxus/src/state/mod.rs` (modify):** Füge `pub mod absence_period;` und `pub mod vacation_balance;` ein (Position alphabetisch).

    **E. `shifty-dioxus/src/api.rs` (modify):** Füge nach dem extra_hours-Block (ca. Z. 470) folgende async fn an:

    1. `pub async fn list_absence_periods(config: Config) -> Result<Rc<[AbsencePeriodTO]>, reqwest::Error>` — analog `get_extra_hours_for_year`. URL: `{}/absence-period`.

    2. `pub async fn list_absence_periods_by_sales_person(config: Config, sales_person_id: Uuid) -> Result<Rc<[AbsencePeriodTO]>, reqwest::Error>`. URL: `{}/absence-period/by-sales-person/{}`. (Backend-Pfad existiert seit v1.0 Phase 1.)

    3. `pub async fn get_absence_period(config: Config, id: Uuid) -> Result<AbsencePeriodTO, reqwest::Error>`. URL: `{}/absence-period/{}`.

    4. `pub async fn create_absence_period(config: Config, mut body: AbsencePeriodTO) -> Result<AbsencePeriodCreateResultTO, ShiftyError>` — POST. **W-7 DEFENSIVE Uuid::nil im Function-Body (NICHT nur Caller-Responsibility):** Setze als ersten Schritt im Function-Body `body.id = Uuid::nil(); body.version = Uuid::nil();` — defensive, weil Backend sonst 422 IdSetOnCreate liefert (Pitfall 9). Doc-Comment dokumentiert das Verhalten: "POST body — function defensively sets id and version to Uuid::nil() before send to prevent accidental ID/version-passthrough from caller. Backend rejects non-nil IDs at create time (422 IdSetOnCreate)." Handhabe 422 → `ShiftyError::Validation(text)`. Body-text via `response.text().await.unwrap_or_default()`.

    5. `pub async fn update_absence_period(config: Config, id: Uuid, body: AbsencePeriodTO) -> Result<AbsencePeriodCreateResultTO, ShiftyError>` — PUT auf `{}/absence-period/{}`. 409 → `ShiftyError::Conflict(String::new())`; 422 → `ShiftyError::Validation(text)`. Pattern aus extra_hours::update_extra_hour:452-468 + Validation-Branch.

    6. `pub async fn delete_absence_period(config: Config, id: Uuid) -> Result<(), reqwest::Error>` — DELETE auf `{}/absence-period/{}`.

    7. `pub async fn get_vacation_balance(config: Config, sales_person_id: Uuid, year: u32) -> Result<VacationBalanceTO, reqwest::Error>` — GET auf `{}/vacation-balance/{}/{}`.

    8. `pub async fn get_team_vacation_balance(config: Config, year: u32) -> Result<Rc<[VacationBalanceTO]>, reqwest::Error>` — GET auf `{}/vacation-balance/team/{}`.

    Imports am Datei-Anfang erweitern: `use rest_types::{AbsencePeriodTO, AbsencePeriodCreateResultTO, VacationBalanceTO};`.

    **F. `shifty-dioxus/src/loader.rs` (modify):** Füge nach `load_bookings` (ca. Z. 102) folgende async fn an:

    1. `pub async fn load_absence_periods_all(config: Config, sales_persons: Rc<[SalesPerson]>) -> Result<Rc<[AbsencePeriod]>, ShiftyError>` — analog `load_bookings`-Pattern: `api::list_absence_periods()` → map TO→state → join mit sales_persons für `person_name` + `background_color`.

    2. `pub async fn load_absence_periods_by_sales_person(config: Config, sales_person_id: Uuid) -> Result<Rc<[AbsencePeriod]>, ShiftyError>` — `api::list_absence_periods_by_sales_person()` → map TO→state ohne Cross-Join (Self-Variante).

    3. `pub async fn load_vacation_balance(config: Config, sales_person_id: Uuid, year: u32) -> Result<VacationBalance, ShiftyError>` — `api::get_vacation_balance()` → state-Mapping (`(&to).into()`).

    4. `pub async fn load_team_vacation(config: Config, year: u32) -> Result<Rc<[VacationBalance]>, ShiftyError>` — `api::get_team_vacation_balance()` → state-Mapping per `Vec<VacationBalance>` → `Rc::from(...)`.

    Imports am Datei-Anfang erweitern: `use crate::state::absence_period::AbsencePeriod;` `use crate::state::vacation_balance::VacationBalance;`.

    KEINE Änderung an Cargo.toml. KEIN Page-Render hier.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo check 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Validation(String)" shifty-dioxus/src/error.rs` ergibt mindestens 1
    - `grep -c "pub struct AbsencePeriod " shifty-dioxus/src/state/absence_period.rs` ergibt 1
    - `grep -c "pub enum AbsenceCategory " shifty-dioxus/src/state/absence_period.rs` ergibt 1
    - `grep -c "impl From<&AbsencePeriodTO>" shifty-dioxus/src/state/absence_period.rs` ergibt 1
    - `grep -c "pub struct VacationBalance " shifty-dioxus/src/state/vacation_balance.rs` ergibt 1
    - `grep -c "impl From<&VacationBalanceTO>" shifty-dioxus/src/state/vacation_balance.rs` ergibt 1
    - `grep -E "pub mod absence_period|pub mod vacation_balance" shifty-dioxus/src/state/mod.rs | wc -l` ergibt 2
    - `grep -c "pub async fn create_absence_period" shifty-dioxus/src/api.rs` ergibt 1
    - **W-7 defensive Uuid::nil:** `grep -A 3 'pub async fn create_absence_period' shifty-dioxus/src/api.rs | grep -c 'Uuid::nil'` ergibt mindestens 1 (defensive id+version reset im Function-Body)
    - `grep -c "pub async fn update_absence_period" shifty-dioxus/src/api.rs` ergibt 1
    - `grep -c "pub async fn delete_absence_period" shifty-dioxus/src/api.rs` ergibt 1
    - `grep -c "pub async fn list_absence_periods" shifty-dioxus/src/api.rs` ergibt mindestens 2 (list + list_by_sales_person)
    - `grep -c "pub async fn get_vacation_balance\|pub async fn get_team_vacation_balance" shifty-dioxus/src/api.rs` ergibt 2
    - `grep -c "ShiftyError::Validation" shifty-dioxus/src/api.rs` ergibt mindestens 2 (in create + update)
    - `grep -c "ShiftyError::Conflict" shifty-dioxus/src/api.rs` ergibt mindestens 1 (in update)
    - `grep -E "pub async fn load_absence_periods_all|pub async fn load_absence_periods_by_sales_person|pub async fn load_vacation_balance|pub async fn load_team_vacation" shifty-dioxus/src/loader.rs | wc -l` ergibt mindestens 4
    - `cd shifty-dioxus && nix develop --command cargo check` Exit-Code 0
  </acceptance_criteria>

  <done>API-Layer + Error-Variant + State-Types + Loader sind kompiliert; cargo check im shifty-dioxus-Subordner grün. Plan-04 Task 2 baut darauf die Coroutine-Services.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Service-Coroutines (absence + vacation_balance) + app.rs-Registrierung</name>

  <read_first>
    - shifty-dioxus/src/service/employee.rs (vollständig — 361 LOC; insb. Z. 28-86 für GlobalSignal+Action-Enum, Z. 191-276 für Coroutine-Body mit 409-Branch)
    - shifty-dioxus/src/service/mod.rs (existing pub mod-Liste, Z. 1-19)
    - shifty-dioxus/src/app.rs (gesamte Datei, 65 LOC — Coroutine-Registrierung Z. 13-26)
    - shifty-dioxus/src/api.rs (Output von Task 1)
    - shifty-dioxus/src/loader.rs (Output von Task 1)
    - shifty-dioxus/src/error.rs (Output von Task 1)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pattern 1" (Page-Service-State-Coroutine); § "Pitfall 4" (AUTH loading_done)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "shifty-dioxus/src/service/absence.rs" + § "shifty-dioxus/src/service/vacation_balance.rs" + § "shifty-dioxus/src/app.rs"
  </read_first>

  <files>
    shifty-dioxus/src/service/absence.rs (NEW),
    shifty-dioxus/src/service/vacation_balance.rs (NEW),
    shifty-dioxus/src/service/mod.rs (modify),
    shifty-dioxus/src/app.rs (modify)
  </files>

  <action>
    **A. `shifty-dioxus/src/service/absence.rs` (NEW):**

    Imports kopieren von service/employee.rs Header (`dioxus::prelude::*`, `std::rc::Rc`, `uuid::Uuid`, `rest_types::*`, `crate::*`, `futures_util::StreamExt`).

    1. **GlobalSignals:**
       ```rust
       pub static ABSENCE_STORE: GlobalSignal<Rc<[AbsencePeriod]>> = Signal::global(|| Rc::new([]));
       pub static ABSENCE_REFRESH: GlobalSignal<u64> = Signal::global(|| 0);

       pub(crate) fn bump_absence_refresh() {
           *ABSENCE_REFRESH.write() += 1;
       }
       ```

    2. **Action-Enum:**
       ```rust
       #[derive(Debug)]
       pub enum AbsenceAction {
           LoadAll,                                              // HR-Variant
           LoadForSalesPerson(Uuid),                              // Employee-Variant
           Create(AbsencePeriodTO, EventHandler<Result<AbsencePeriodCreateResultTO, ShiftyError>>),
           Update(AbsencePeriodTO, EventHandler<Result<AbsencePeriodCreateResultTO, ShiftyError>>),
           Delete(Uuid),
           Refresh,
       }
       ```
       Begründung des `EventHandler<Result<...>>`-Side-Channels: D-08 + D-11 + D-12 verlangen, dass 409/422/Warnings Modal-lokal gehandhabt werden, nicht global in ERROR_STORE. Der EventHandler liefert das Resultat zurück an die Page, die dann den Modal-State (Banner, WarningList, Close) updated. Pattern aus extra_hours_modal.rs:189-192 (`svc.send(...); on_saved.call(())`). Falls `EventHandler<Result<...>>` als Action-Enum-Variant nicht trivial Debug-able ist, nutze einen separaten `GlobalSignal<Option<AbsenceModalEvent>>` als Side-Channel — Plan-Phase erlaubt beides (PATTERNS.md Z. 522-525).

    3. **Coroutine:**
       ```rust
       pub async fn absence_service(mut rx: UnboundedReceiver<AbsenceAction>) {
           use futures_util::StreamExt;
           while let Some(action) = rx.next().await {
               tracing::info!("AbsenceAction: {:?}", &action);
               let config = CONFIG.read().clone();
               let sales_persons = SALES_PERSONS.read().clone();  // oder existing GlobalSignal name
               let result: Result<(), ShiftyError> = match action {
                   AbsenceAction::LoadAll => {
                       match loader::load_absence_periods_all(config, sales_persons).await {
                           Ok(list) => { *ABSENCE_STORE.write() = list; Ok(()) }
                           Err(e) => Err(e),
                       }
                   }
                   AbsenceAction::LoadForSalesPerson(sp_id) => {
                       match loader::load_absence_periods_by_sales_person(config, sp_id).await {
                           Ok(list) => { *ABSENCE_STORE.write() = list; Ok(()) }
                           Err(e) => Err(e),
                       }
                   }
                   AbsenceAction::Create(body, on_done) => {
                       let r = api::create_absence_period(config.clone(), body).await;
                       on_done.call(r.clone());
                       if r.is_ok() { bump_absence_refresh(); }
                       r.map(|_| ())
                   }
                   AbsenceAction::Update(body, on_done) => {
                       let id = body.id;
                       let r = api::update_absence_period(config.clone(), id, body).await;
                       on_done.call(r.clone());
                       if r.is_ok() { bump_absence_refresh(); }
                       r.map(|_| ())
                   }
                   AbsenceAction::Delete(id) => {
                       match api::delete_absence_period(config, id).await {
                           Ok(_) => { bump_absence_refresh(); Ok(()) }
                           Err(e) => Err(e.into()),
                       }
                   }
                   AbsenceAction::Refresh => Ok(()),  // bump nur, kein API-Call
               };
               if let Err(err) = result {
                   match err {
                       ShiftyError::Conflict(_) | ShiftyError::Validation(_) => {
                           // Modal-lokal — handled per EventHandler-Side-Channel.
                       }
                       other => {
                           *ERROR_STORE.write() = ErrorStore { error: Some(other) };
                       }
                   }
               }
           }
       }
       ```
       Falls `Result::clone()` für `AbsencePeriodCreateResultTO` nicht trivial ist (TO ist Clone), passt das automatisch. Wenn Pattern adaptiert werden muss → analog `service/employee.rs:191-276` strukturieren.

    4. **Pflicht:** AUTH `loading_done`-Check ist NICHT hier — der findet in der Page statt (Plan 05). Coroutine darf vor Auth-Resolve laufen, weil sie nur auf Send wartet.

    **B. `shifty-dioxus/src/service/vacation_balance.rs` (NEW):**

    1. **GlobalSignals:**
       ```rust
       pub static VACATION_BALANCE_STORE: GlobalSignal<Option<VacationBalance>> = Signal::global(|| None);
       pub static VACATION_TEAM_STORE: GlobalSignal<Rc<[VacationBalance]>> = Signal::global(|| Rc::new([]));
       ```

    2. **Action-Enum:**
       ```rust
       #[derive(Debug)]
       pub enum VacationBalanceAction {
           LoadSelf(Uuid, u32),
           LoadTeam(u32),
       }
       ```

    3. **Coroutine:**
       ```rust
       pub async fn vacation_balance_service(mut rx: UnboundedReceiver<VacationBalanceAction>) {
           use futures_util::StreamExt;
           while let Some(action) = rx.next().await {
               let config = CONFIG.read().clone();
               match action {
                   VacationBalanceAction::LoadSelf(sp_id, year) => {
                       match loader::load_vacation_balance(config, sp_id, year).await {
                           Ok(b) => { *VACATION_BALANCE_STORE.write() = Some(b); }
                           Err(e) => { *ERROR_STORE.write() = ErrorStore { error: Some(e) }; }
                       }
                   }
                   VacationBalanceAction::LoadTeam(year) => {
                       match loader::load_team_vacation(config, year).await {
                           Ok(list) => { *VACATION_TEAM_STORE.write() = list; }
                           Err(e) => { *ERROR_STORE.write() = ErrorStore { error: Some(e) }; }
                       }
                   }
               }
           }
       }
       ```

    **C. `shifty-dioxus/src/service/mod.rs` (modify):** Füge `pub mod absence;` und `pub mod vacation_balance;` ein (alphabetisch).

    **D. `shifty-dioxus/src/app.rs` (modify):** Im `App()`-Component, im `use_coroutine`-Block (Z. 13-26), füge ein:
    ```rust
    use_coroutine(service::absence::absence_service);
    use_coroutine(service::vacation_balance::vacation_balance_service);
    ```
    Position: nach den existierenden `use_coroutine`-Calls (z. B. nach `service::billing_period::billing_period_service`).
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo check 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ABSENCE_STORE" shifty-dioxus/src/service/absence.rs` ergibt mindestens 1
    - `grep -c "ABSENCE_REFRESH" shifty-dioxus/src/service/absence.rs` ergibt mindestens 1
    - `grep -c "pub enum AbsenceAction" shifty-dioxus/src/service/absence.rs` ergibt 1
    - `grep -c "pub async fn absence_service" shifty-dioxus/src/service/absence.rs` ergibt 1
    - `grep -E "LoadAll|LoadForSalesPerson|Create|Update|Delete|Refresh" shifty-dioxus/src/service/absence.rs | grep -vc '^//'` ergibt mindestens 6
    - `grep -c "VACATION_BALANCE_STORE\|VACATION_TEAM_STORE" shifty-dioxus/src/service/vacation_balance.rs` ergibt mindestens 2
    - `grep -c "pub enum VacationBalanceAction" shifty-dioxus/src/service/vacation_balance.rs` ergibt 1
    - `grep -c "pub async fn vacation_balance_service" shifty-dioxus/src/service/vacation_balance.rs` ergibt 1
    - `grep -E "pub mod absence|pub mod vacation_balance" shifty-dioxus/src/service/mod.rs | wc -l` ergibt mindestens 2
    - `grep -c "use_coroutine(service::absence::absence_service)" shifty-dioxus/src/app.rs` ergibt 1
    - `grep -c "use_coroutine(service::vacation_balance::vacation_balance_service)" shifty-dioxus/src/app.rs` ergibt 1
    - `cd shifty-dioxus && nix develop --command cargo check` Exit-Code 0
  </acceptance_criteria>

  <done>Coroutine-Services existieren mit GlobalSignal-Stores, sind in app.rs registriert, cargo check grün. Plan 05 kann `use_coroutine_handle::<AbsenceAction>()` und `use_coroutine_handle::<VacationBalanceAction>()` aus der Page heraus konsumieren.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3a: Key-Enum-Variants + i18n-Test + Dx-Proxy</name>

  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (gesamte Datei, ~552 LOC; insbesondere Z. 55-150 für Key-Enum-Block-Pattern, Z. 411-415 für `generate(locale)` switch, Z. 425-457 für i18n-Test-Pattern)
    - .planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md § "Copywriting Contract" (vollständige i18n-Tabelle, alle 60 Keys)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "shifty-dioxus/src/i18n/mod.rs"
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pitfall 2" (Locale::En-statt-Locale::De-Bug)
    - shifty-dioxus/Dioxus.toml (gesamte Datei, ~93 LOC; Proxy-Block Z. 45-92)
    - shifty-dioxus/CLAUDE.md (i18n-Konvention)
  </read_first>

  <files>
    shifty-dioxus/src/i18n/mod.rs (modify),
    shifty-dioxus/Dioxus.toml (modify)
  </files>

  <action>
    **A. `shifty-dioxus/src/i18n/mod.rs` — Key-Variants:**

    Im `pub enum Key { ... }`-Block (~Z. 55+) füge einen neuen Comment-Block hinzu (Position: vor dem letzten existierenden Block, z. B. vor `// Working-hours mini overview`). Alle 60 Keys aus UI-SPEC § "Copywriting Contract":

    ```rust
        // Absence management (Phase 8)
        // Page-Level
        AbsencePageTitle,
        AbsencePageSubtitle,
        AbsenceMenuLabel,
        // Primary CTA
        AbsenceNewBtn,
        AbsenceModalCreateBtn,
        AbsenceModalSaveBtn,
        AbsenceModalCancelBtn,
        AbsenceModalDeleteBtn,
        // Empty State
        AbsenceEmptyFilterHeading,
        AbsenceEmptyFilterBody,
        AbsenceEmptySelfHeading,
        AbsenceEmptySelfBody,
        // Form Labels & Hints
        AbsenceFieldEmployee,
        AbsenceFieldCategory,
        AbsenceFieldFrom,
        AbsenceFieldTo,
        AbsenceFieldDescription,
        AbsenceFieldDescriptionHint,
        AbsenceModalCreateSubtitle,
        AbsenceModalEditSubtitle,
        AbsencePreviewHeader,
        AbsencePreviewFooter,
        // Categories
        AbsenceCategoryVacation,
        AbsenceCategorySickLeave,
        AbsenceCategoryUnpaidLeave,
        AbsenceFilterCategoryLabel,
        AbsenceFilterCategoryAll,
        AbsenceFilterPersonLabel,
        AbsenceFilterPersonAll,
        AbsenceFilterStatusLabel,
        AbsenceFilterStatusAll,
        // Status
        AbsenceStatusActive,
        AbsenceStatusPlanned,
        AbsenceStatusFinished,
        // Liste-Spaltenheader
        AbsenceColEmployee,
        AbsenceColRange,
        AbsenceColCategory,
        AbsenceColStatus,
        AbsenceColWarnings,
        AbsenceDayUnit,
        AbsenceDaysUnit,
        // VacationEntitlementCard
        VacationEntitlementHero,
        VacationDaysRemaining,
        VacationCardSelfTitle,
        VacationCardSelfSubtitle,
        VacationCardTeamTitle,
        VacationCardTeamSubtitle,
        VacationStatContract,
        VacationStatCarryover,
        VacationStatUsed,
        VacationStatPending,
        VacationStatRemaining,
        VacationPerPersonHeader,
        VacationPerPersonShowAll,
        VacationPerPersonShowLess,
        // Statistik-Cards
        AbsenceStatSickLeaveDays,
        AbsenceStatUnpaidDays,
        AbsenceStatActive,
        // Errors & Warnings
        AbsenceErrorRangeInverted,
        AbsenceErrorSelfOverlapHeader,
        AbsenceErrorSelfOverlapBody,
        AbsenceErrorVersionConflictHeader,
        AbsenceErrorVersionConflictBody,
        AbsenceErrorVersionConflictReload,
        AbsenceErrorNetwork,
        AbsenceWarningHeaderSingular,
        AbsenceWarningHeaderPlural,
        AbsenceWarningAcknowledgeBtn,
        AbsenceWarningOverlapsBooking,
        AbsenceWarningOverlapsManual,
        // Destructive Confirmation
        AbsenceDeleteConfirmTitle,
        AbsenceDeleteConfirmBody,
        AbsenceDeleteConfirmBtn,
        AbsenceDeleteCancelBtn,
        // Filter
        AbsenceFilterShowPast,
        AbsenceFilterCounter,
    ```

    Insgesamt **60 Keys**.

    **i18n-Test ergänzen** (analog Z. 425-457 in existing mod.rs) — ZWINGEND (B-2 / Pitfall 2 Schutz):
    ```rust
    #[test]
    fn i18n_absence_keys_present_in_all_locales() {
        for locale in [Locale::En, Locale::De, Locale::Cs] {
            let i18n = generate(locale);
            for key in [
                Key::AbsencePageTitle,
                Key::AbsencePageSubtitle,
                Key::AbsenceMenuLabel,
                Key::AbsenceNewBtn,
                Key::AbsenceCategoryVacation,
                Key::AbsenceCategorySickLeave,
                Key::AbsenceCategoryUnpaidLeave,
                Key::AbsenceStatusActive,
                Key::AbsenceStatusPlanned,
                Key::AbsenceStatusFinished,
                Key::AbsenceErrorRangeInverted,
                Key::AbsenceErrorSelfOverlapHeader,
                Key::AbsenceErrorVersionConflictHeader,
                Key::VacationCardSelfTitle,
                Key::VacationStatContract,
                Key::VacationStatCarryover,
                Key::VacationStatUsed,
                Key::VacationStatPending,
                Key::VacationStatRemaining,
                Key::AbsenceDeleteConfirmTitle,
                Key::AbsenceWarningAcknowledgeBtn,
                Key::AbsenceFilterShowPast,
            ] {
                let value = i18n.t(key);
                assert!(
                    !value.is_empty() && value.as_ref() != "??",
                    "missing translation for {:?} in {:?}: got `{}`",
                    key, locale, value
                );
            }
        }
    }

    #[test]
    fn i18n_absence_keys_match_german_reference() {
        let i18n = generate(Locale::De);
        assert_eq!(i18n.t(Key::AbsencePageTitle).as_ref(), "Abwesenheiten");
        assert_eq!(i18n.t(Key::AbsenceCategoryVacation).as_ref(), "Urlaub");
        assert_eq!(i18n.t(Key::AbsenceCategorySickLeave).as_ref(), "Krankheit");
        assert_eq!(i18n.t(Key::AbsenceCategoryUnpaidLeave).as_ref(), "Unbezahlt");
        assert_eq!(i18n.t(Key::AbsenceStatusActive).as_ref(), "Aktiv");
    }

    #[test]
    fn i18n_absence_keys_match_english_reference() {
        let i18n = generate(Locale::En);
        assert_eq!(i18n.t(Key::AbsencePageTitle).as_ref(), "Absences");
        assert_eq!(i18n.t(Key::AbsenceCategoryVacation).as_ref(), "Vacation");
        assert_eq!(i18n.t(Key::AbsenceCategorySickLeave).as_ref(), "Sick leave");
        assert_eq!(i18n.t(Key::AbsenceStatusActive).as_ref(), "Active");
    }

    #[test]
    fn i18n_absence_keys_match_czech_reference() {
        let i18n = generate(Locale::Cs);
        assert_eq!(i18n.t(Key::AbsencePageTitle).as_ref(), "Nepřítomnosti");
        assert_eq!(i18n.t(Key::AbsenceCategoryVacation).as_ref(), "Dovolená");
        assert_eq!(i18n.t(Key::AbsenceCategorySickLeave).as_ref(), "Nemoc");
        assert_eq!(i18n.t(Key::AbsenceStatusActive).as_ref(), "Aktivní");
    }
    ```

    Die "match_*_reference"-Tests pro Locale verifizieren (B-2 / Pitfall 2): kein Locale::En-Tag in de.rs, kein Locale::De-Tag in en.rs/cs.rs schleicht sich ein. Wenn Task 3b/c/d die add_text-Calls mit falschem Locale schreibt → diese Tests schlagen fehl mit "??"-Fallback.

    **B. `shifty-dioxus/Dioxus.toml` (modify):** Im `[[web.proxy]]`-Block (Z. 45-92) füge zwei neue Einträge hinzu:
    ```toml
    [[web.proxy]]
    backend = "http://localhost:3000/absence-period"
    [[web.proxy]]
    backend = "http://localhost:3000/vacation-balance"
    ```
    Position: am Ende der Proxy-Liste (nach `working-hours`).

    **HINWEIS:** Die Tests in mod.rs schlagen fehl, bis Task 3b/c/d die add_text-Calls in de.rs/en.rs/cs.rs ergänzt haben. Das ist erwartet — Task 3a allein erzeugt eine RED-Phase, Task 3b–d füllen das auf GREEN.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "// Absence management" shifty-dioxus/src/i18n/mod.rs` ergibt mindestens 1
    - `grep -c "AbsencePageTitle" shifty-dioxus/src/i18n/mod.rs` ergibt mindestens 1 (im Key-enum)
    - `grep -E "AbsencePageTitle|AbsenceMenuLabel|AbsenceCategoryVacation|VacationStatContract|AbsenceErrorSelfOverlapHeader|AbsenceWarningAcknowledgeBtn" shifty-dioxus/src/i18n/mod.rs | grep -vc '^//'` ergibt mindestens 6
    - `grep -c "fn i18n_absence_keys_present_in_all_locales" shifty-dioxus/src/i18n/mod.rs` ergibt 1
    - `grep -c "fn i18n_absence_keys_match_german_reference" shifty-dioxus/src/i18n/mod.rs` ergibt 1
    - `grep -c "fn i18n_absence_keys_match_english_reference" shifty-dioxus/src/i18n/mod.rs` ergibt 1
    - `grep -c "fn i18n_absence_keys_match_czech_reference" shifty-dioxus/src/i18n/mod.rs` ergibt 1
    - `grep -c "absence-period" shifty-dioxus/Dioxus.toml` ergibt mindestens 1
    - `grep -c "vacation-balance" shifty-dioxus/Dioxus.toml` ergibt mindestens 1
    - `cd shifty-dioxus && nix develop --command cargo check` Exit-Code 0 (Compile grün — Tests dürfen noch rot sein, das ist Task 3b–d)
  </acceptance_criteria>

  <done>Key-Enum-Variants + 4 i18n-Tests + Dx-Proxy-Einträge angelegt; cargo check grün. Tests schlagen fehl bis Task 3b–d. Plan 05 kann auf alle Keys via `Key::*` referenzieren.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3b: Locale::De-Befüllung in de.rs</name>

  <read_first>
    - shifty-dioxus/src/i18n/de.rs (Z. 1-80 für `add_text(Locale::De, ...)` Pattern)
    - shifty-dioxus/src/i18n/mod.rs (Output von Task 3a — Key-Enum-Variants existieren)
    - .planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md § "Copywriting Contract" Spalte "De" (verbatim Strings)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pitfall 2" (Locale::En-statt-Locale::De-Bug)
  </read_first>

  <files>shifty-dioxus/src/i18n/de.rs (modify)</files>

  <action>
    Füge nach dem letzten existierenden `add_text(Locale::De, ...)`-Aufruf einen neuen Comment-Block ein. **ALLE 60 add_text-Aufrufe explizit, KEIN Auslassen, KEIN "Beispiele":**

    ```rust
        // Absence management (Phase 8)
        i18n.add_text(Locale::De, Key::AbsencePageTitle, "Abwesenheiten");
        i18n.add_text(Locale::De, Key::AbsencePageSubtitle, "Urlaub, Krankheit und unbezahlte Freistellung als Zeiträume. Stunden pro Tag werden aus dem gültigen Arbeitsvertrag abgeleitet.");
        i18n.add_text(Locale::De, Key::AbsenceMenuLabel, "Abwesenheiten");
        i18n.add_text(Locale::De, Key::AbsenceNewBtn, "Neue Abwesenheit");
        i18n.add_text(Locale::De, Key::AbsenceModalCreateBtn, "Anlegen");
        i18n.add_text(Locale::De, Key::AbsenceModalSaveBtn, "Speichern");
        i18n.add_text(Locale::De, Key::AbsenceModalCancelBtn, "Abbrechen");
        i18n.add_text(Locale::De, Key::AbsenceModalDeleteBtn, "Löschen");
        i18n.add_text(Locale::De, Key::AbsenceEmptyFilterHeading, "Keine Treffer");
        i18n.add_text(Locale::De, Key::AbsenceEmptyFilterBody, "Keine Abwesenheiten passen zum aktuellen Filter. Filter zurücksetzen oder neue Abwesenheit anlegen.");
        i18n.add_text(Locale::De, Key::AbsenceEmptySelfHeading, "Noch keine Abwesenheiten");
        i18n.add_text(Locale::De, Key::AbsenceEmptySelfBody, "Lege deine erste Abwesenheit an (Urlaub, Krankheit oder unbezahlte Freistellung).");
        i18n.add_text(Locale::De, Key::AbsenceFieldEmployee, "Mitarbeiter");
        i18n.add_text(Locale::De, Key::AbsenceFieldCategory, "Kategorie");
        i18n.add_text(Locale::De, Key::AbsenceFieldFrom, "Von");
        i18n.add_text(Locale::De, Key::AbsenceFieldTo, "Bis (inklusiv)");
        i18n.add_text(Locale::De, Key::AbsenceFieldDescription, "Beschreibung");
        i18n.add_text(Locale::De, Key::AbsenceFieldDescriptionHint, "Optional — z. B. Reiseziel oder Anmerkung.");
        i18n.add_text(Locale::De, Key::AbsenceModalCreateSubtitle, "Ganztägiger Zeitraum. Stunden werden aus dem Vertrag abgeleitet.");
        i18n.add_text(Locale::De, Key::AbsenceModalEditSubtitle, "Änderungen werden mit optimistischem Locking gespeichert.");
        i18n.add_text(Locale::De, Key::AbsencePreviewHeader, "Vorschau");
        i18n.add_text(Locale::De, Key::AbsencePreviewFooter, "Feiertage im Bereich werden mit 0 h verrechnet. Stunden pro Tag stammen aus dem am jeweiligen Tag gültigen Arbeitsvertrag.");
        i18n.add_text(Locale::De, Key::AbsenceCategoryVacation, "Urlaub");
        i18n.add_text(Locale::De, Key::AbsenceCategorySickLeave, "Krankheit");
        i18n.add_text(Locale::De, Key::AbsenceCategoryUnpaidLeave, "Unbezahlt");
        i18n.add_text(Locale::De, Key::AbsenceFilterCategoryLabel, "Kategorie");
        i18n.add_text(Locale::De, Key::AbsenceFilterCategoryAll, "Alle");
        i18n.add_text(Locale::De, Key::AbsenceFilterPersonLabel, "Person");
        i18n.add_text(Locale::De, Key::AbsenceFilterPersonAll, "Alle Personen");
        i18n.add_text(Locale::De, Key::AbsenceFilterStatusLabel, "Status");
        i18n.add_text(Locale::De, Key::AbsenceFilterStatusAll, "Alle");
        i18n.add_text(Locale::De, Key::AbsenceStatusActive, "Aktiv");
        i18n.add_text(Locale::De, Key::AbsenceStatusPlanned, "Geplant");
        i18n.add_text(Locale::De, Key::AbsenceStatusFinished, "Beendet");
        i18n.add_text(Locale::De, Key::AbsenceColEmployee, "Mitarbeiter");
        i18n.add_text(Locale::De, Key::AbsenceColRange, "Zeitraum");
        i18n.add_text(Locale::De, Key::AbsenceColCategory, "Kategorie");
        i18n.add_text(Locale::De, Key::AbsenceColStatus, "Status");
        i18n.add_text(Locale::De, Key::AbsenceColWarnings, "Hinweise");
        i18n.add_text(Locale::De, Key::AbsenceDayUnit, "Tag");
        i18n.add_text(Locale::De, Key::AbsenceDaysUnit, "Tage");
        i18n.add_text(Locale::De, Key::VacationEntitlementHero, "Urlaubsanspruch {year}");
        i18n.add_text(Locale::De, Key::VacationDaysRemaining, "Tage verbleibend");
        i18n.add_text(Locale::De, Key::VacationCardSelfTitle, "Dein Urlaubskonto");
        i18n.add_text(Locale::De, Key::VacationCardSelfSubtitle, "Anspruch aus Vertrag + Übertrag aus dem Vorjahr.");
        i18n.add_text(Locale::De, Key::VacationCardTeamTitle, "Urlaubsanspruch Team · {count} Personen");
        i18n.add_text(Locale::De, Key::VacationCardTeamSubtitle, "Summe über alle bezahlten Mitarbeiter.");
        i18n.add_text(Locale::De, Key::VacationStatContract, "Vertrag");
        i18n.add_text(Locale::De, Key::VacationStatCarryover, "Übertrag '{year-1}'");
        i18n.add_text(Locale::De, Key::VacationStatUsed, "Genommen");
        i18n.add_text(Locale::De, Key::VacationStatPending, "Beantragt");
        i18n.add_text(Locale::De, Key::VacationStatRemaining, "Verbleibend");
        i18n.add_text(Locale::De, Key::VacationPerPersonHeader, "Pro Person · sortiert nach verbleibenden Tagen");
        i18n.add_text(Locale::De, Key::VacationPerPersonShowAll, "Alle ({count})");
        i18n.add_text(Locale::De, Key::VacationPerPersonShowLess, "Weniger");
        i18n.add_text(Locale::De, Key::AbsenceStatSickLeaveDays, "Krankheitstage {year}");
        i18n.add_text(Locale::De, Key::AbsenceStatUnpaidDays, "Unbezahlt {year}");
        i18n.add_text(Locale::De, Key::AbsenceStatActive, "Aktive Abwesenheiten");
        i18n.add_text(Locale::De, Key::AbsenceErrorRangeInverted, "Enddatum liegt vor Startdatum");
        i18n.add_text(Locale::De, Key::AbsenceErrorSelfOverlapHeader, "Selbst-Überlappung");
        i18n.add_text(Locale::De, Key::AbsenceErrorSelfOverlapBody, "{category}-Eintrag von {from} bis {to} überschneidet sich. Bitte Zeitraum oder Kategorie anpassen.");
        i18n.add_text(Locale::De, Key::AbsenceErrorVersionConflictHeader, "Eintrag wurde anderswo geändert");
        i18n.add_text(Locale::De, Key::AbsenceErrorVersionConflictBody, "Bitte erneut laden, dann nochmal speichern. Deine Eingabe bleibt erhalten.");
        i18n.add_text(Locale::De, Key::AbsenceErrorVersionConflictReload, "Erneut laden");
        i18n.add_text(Locale::De, Key::AbsenceErrorNetwork, "Netzwerkfehler. Bitte erneut versuchen.");
        i18n.add_text(Locale::De, Key::AbsenceWarningHeaderSingular, "Hinweis · 1 Konflikt (nicht blockierend)");
        i18n.add_text(Locale::De, Key::AbsenceWarningHeaderPlural, "Hinweis · {count} Konflikte (nicht blockierend)");
        i18n.add_text(Locale::De, Key::AbsenceWarningAcknowledgeBtn, "Verstanden");
        i18n.add_text(Locale::De, Key::AbsenceWarningOverlapsBooking, "Bestehende Buchung am {date} überschneidet sich mit dieser Abwesenheit.");
        i18n.add_text(Locale::De, Key::AbsenceWarningOverlapsManual, "Manuell als unverfügbar markierter Tag überschneidet sich. Nach dem Cutover ist dieser Eintrag redundant.");
        i18n.add_text(Locale::De, Key::AbsenceDeleteConfirmTitle, "Abwesenheit löschen?");
        i18n.add_text(Locale::De, Key::AbsenceDeleteConfirmBody, "Soft-Delete — der Eintrag bleibt für Audit-Logs erhalten, wird aber nicht mehr in Reports und im Shiftplan berücksichtigt.");
        i18n.add_text(Locale::De, Key::AbsenceDeleteConfirmBtn, "Löschen");
        i18n.add_text(Locale::De, Key::AbsenceDeleteCancelBtn, "Abbrechen");
        i18n.add_text(Locale::De, Key::AbsenceFilterShowPast, "Vergangene anzeigen");
        i18n.add_text(Locale::De, Key::AbsenceFilterCounter, "{n} von {m}");
    ```

    **PFLICHT (Pitfall 2):** ALLE add_text-Aufrufe in de.rs nutzen `Locale::De` als ersten Parameter. KEINE `Locale::En`/`Locale::Cs`-Tags im Block.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo test i18n_absence_keys_match_german_reference 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "// Absence management" shifty-dioxus/src/i18n/de.rs` ergibt mindestens 1
    - `grep -c "Locale::De, Key::Absence\|Locale::De, Key::Vacation" shifty-dioxus/src/i18n/de.rs` ergibt mindestens 60 (alle 60 neuen Keys)
    - **B-1 / Pitfall 2 Schutz:** Im Phase-8-Block dürfen weder `Locale::En` noch `Locale::Cs` vorkommen. Regex-Check für den neuen Block: `awk '/\/\/ Absence management \(Phase 8\)/,/^[[:space:]]*}[[:space:]]*$/' shifty-dioxus/src/i18n/de.rs | grep -c 'Locale::En\|Locale::Cs'` ergibt 0
    - `cd shifty-dioxus && nix develop --command cargo test i18n_absence_keys_match_german_reference` Exit-Code 0
  </acceptance_criteria>

  <done>de.rs enthält 60 Locale::De-add_text-Calls; deutsche Reference-Test grün; KEIN falscher Locale-Tag im Block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3c: Locale::En-Befüllung in en.rs</name>

  <read_first>
    - shifty-dioxus/src/i18n/en.rs (Z. 1-80 für `add_text(Locale::En, ...)` Pattern)
    - shifty-dioxus/src/i18n/mod.rs (Output von Task 3a — Key-Enum-Variants existieren)
    - .planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md § "Copywriting Contract" Spalte "En" (verbatim Strings)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pitfall 2"
  </read_first>

  <files>shifty-dioxus/src/i18n/en.rs (modify)</files>

  <action>
    Füge nach dem letzten existierenden `add_text(Locale::En, ...)`-Aufruf einen neuen Comment-Block ein. **ALLE 60 add_text-Aufrufe explizit aus UI-SPEC § "Copywriting Contract" Spalte "En" (verbatim, KEIN Beispielstil — komplette Liste):**

    ```rust
        // Absence management (Phase 8)
        i18n.add_text(Locale::En, Key::AbsencePageTitle, "Absences");
        i18n.add_text(Locale::En, Key::AbsencePageSubtitle, "Vacation, sick leave and unpaid leave as date ranges. Hours per day are derived from the active employment contract.");
        i18n.add_text(Locale::En, Key::AbsenceMenuLabel, "Absences");
        i18n.add_text(Locale::En, Key::AbsenceNewBtn, "New absence");
        i18n.add_text(Locale::En, Key::AbsenceModalCreateBtn, "Create");
        i18n.add_text(Locale::En, Key::AbsenceModalSaveBtn, "Save");
        i18n.add_text(Locale::En, Key::AbsenceModalCancelBtn, "Cancel");
        i18n.add_text(Locale::En, Key::AbsenceModalDeleteBtn, "Delete");
        i18n.add_text(Locale::En, Key::AbsenceEmptyFilterHeading, "No results");
        i18n.add_text(Locale::En, Key::AbsenceEmptyFilterBody, "No absences match the current filter. Reset filters or create a new absence.");
        i18n.add_text(Locale::En, Key::AbsenceEmptySelfHeading, "No absences yet");
        i18n.add_text(Locale::En, Key::AbsenceEmptySelfBody, "Create your first absence (vacation, sick leave or unpaid leave).");
        i18n.add_text(Locale::En, Key::AbsenceFieldEmployee, "Employee");
        i18n.add_text(Locale::En, Key::AbsenceFieldCategory, "Category");
        i18n.add_text(Locale::En, Key::AbsenceFieldFrom, "From");
        i18n.add_text(Locale::En, Key::AbsenceFieldTo, "To (inclusive)");
        i18n.add_text(Locale::En, Key::AbsenceFieldDescription, "Description");
        i18n.add_text(Locale::En, Key::AbsenceFieldDescriptionHint, "Optional — e.g. travel destination or note.");
        i18n.add_text(Locale::En, Key::AbsenceModalCreateSubtitle, "Full-day range. Hours are derived from the contract.");
        i18n.add_text(Locale::En, Key::AbsenceModalEditSubtitle, "Changes are saved with optimistic locking.");
        i18n.add_text(Locale::En, Key::AbsencePreviewHeader, "Preview");
        i18n.add_text(Locale::En, Key::AbsencePreviewFooter, "Holidays in the range count as 0 h. Hours per day come from the contract active on that day.");
        i18n.add_text(Locale::En, Key::AbsenceCategoryVacation, "Vacation");
        i18n.add_text(Locale::En, Key::AbsenceCategorySickLeave, "Sick leave");
        i18n.add_text(Locale::En, Key::AbsenceCategoryUnpaidLeave, "Unpaid leave");
        i18n.add_text(Locale::En, Key::AbsenceFilterCategoryLabel, "Category");
        i18n.add_text(Locale::En, Key::AbsenceFilterCategoryAll, "All");
        i18n.add_text(Locale::En, Key::AbsenceFilterPersonLabel, "Person");
        i18n.add_text(Locale::En, Key::AbsenceFilterPersonAll, "All people");
        i18n.add_text(Locale::En, Key::AbsenceFilterStatusLabel, "Status");
        i18n.add_text(Locale::En, Key::AbsenceFilterStatusAll, "All");
        i18n.add_text(Locale::En, Key::AbsenceStatusActive, "Active");
        i18n.add_text(Locale::En, Key::AbsenceStatusPlanned, "Planned");
        i18n.add_text(Locale::En, Key::AbsenceStatusFinished, "Finished");
        i18n.add_text(Locale::En, Key::AbsenceColEmployee, "Employee");
        i18n.add_text(Locale::En, Key::AbsenceColRange, "Range");
        i18n.add_text(Locale::En, Key::AbsenceColCategory, "Category");
        i18n.add_text(Locale::En, Key::AbsenceColStatus, "Status");
        i18n.add_text(Locale::En, Key::AbsenceColWarnings, "Warnings");
        i18n.add_text(Locale::En, Key::AbsenceDayUnit, "day");
        i18n.add_text(Locale::En, Key::AbsenceDaysUnit, "days");
        i18n.add_text(Locale::En, Key::VacationEntitlementHero, "Vacation entitlement {year}");
        i18n.add_text(Locale::En, Key::VacationDaysRemaining, "days remaining");
        i18n.add_text(Locale::En, Key::VacationCardSelfTitle, "Your vacation balance");
        i18n.add_text(Locale::En, Key::VacationCardSelfSubtitle, "Entitlement from contract + carryover from previous year.");
        i18n.add_text(Locale::En, Key::VacationCardTeamTitle, "Vacation entitlement team · {count} people");
        i18n.add_text(Locale::En, Key::VacationCardTeamSubtitle, "Sum across all paid employees.");
        i18n.add_text(Locale::En, Key::VacationStatContract, "Contract");
        i18n.add_text(Locale::En, Key::VacationStatCarryover, "Carryover '{year-1}'");
        i18n.add_text(Locale::En, Key::VacationStatUsed, "Used");
        i18n.add_text(Locale::En, Key::VacationStatPending, "Pending");
        i18n.add_text(Locale::En, Key::VacationStatRemaining, "Remaining");
        i18n.add_text(Locale::En, Key::VacationPerPersonHeader, "Per person · sorted by days remaining");
        i18n.add_text(Locale::En, Key::VacationPerPersonShowAll, "All ({count})");
        i18n.add_text(Locale::En, Key::VacationPerPersonShowLess, "Less");
        i18n.add_text(Locale::En, Key::AbsenceStatSickLeaveDays, "Sick days {year}");
        i18n.add_text(Locale::En, Key::AbsenceStatUnpaidDays, "Unpaid {year}");
        i18n.add_text(Locale::En, Key::AbsenceStatActive, "Active absences");
        i18n.add_text(Locale::En, Key::AbsenceErrorRangeInverted, "End date is before start date");
        i18n.add_text(Locale::En, Key::AbsenceErrorSelfOverlapHeader, "Self-overlap");
        i18n.add_text(Locale::En, Key::AbsenceErrorSelfOverlapBody, "A {category} entry from {from} to {to} overlaps. Please adjust the range or category.");
        i18n.add_text(Locale::En, Key::AbsenceErrorVersionConflictHeader, "Entry changed elsewhere");
        i18n.add_text(Locale::En, Key::AbsenceErrorVersionConflictBody, "Please reload, then save again. Your input is preserved.");
        i18n.add_text(Locale::En, Key::AbsenceErrorVersionConflictReload, "Reload");
        i18n.add_text(Locale::En, Key::AbsenceErrorNetwork, "Network error. Please try again.");
        i18n.add_text(Locale::En, Key::AbsenceWarningHeaderSingular, "Notice · 1 conflict (non-blocking)");
        i18n.add_text(Locale::En, Key::AbsenceWarningHeaderPlural, "Notice · {count} conflicts (non-blocking)");
        i18n.add_text(Locale::En, Key::AbsenceWarningAcknowledgeBtn, "Got it");
        i18n.add_text(Locale::En, Key::AbsenceWarningOverlapsBooking, "Existing booking on {date} overlaps with this absence.");
        i18n.add_text(Locale::En, Key::AbsenceWarningOverlapsManual, "Manually marked unavailable day overlaps. After cutover this entry is redundant.");
        i18n.add_text(Locale::En, Key::AbsenceDeleteConfirmTitle, "Delete absence?");
        i18n.add_text(Locale::En, Key::AbsenceDeleteConfirmBody, "Soft-delete — the entry stays in audit logs but is no longer counted in reports or shown in the shiftplan.");
        i18n.add_text(Locale::En, Key::AbsenceDeleteConfirmBtn, "Delete");
        i18n.add_text(Locale::En, Key::AbsenceDeleteCancelBtn, "Cancel");
        i18n.add_text(Locale::En, Key::AbsenceFilterShowPast, "Show past");
        i18n.add_text(Locale::En, Key::AbsenceFilterCounter, "{n} of {m}");
    ```

    **PFLICHT (Pitfall 2):** ALLE add_text-Aufrufe in en.rs nutzen `Locale::En`. Der neue Phase-8-Block enthält weder `Locale::De` noch `Locale::Cs`.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo test i18n_absence_keys_match_english_reference 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "// Absence management" shifty-dioxus/src/i18n/en.rs` ergibt mindestens 1
    - `grep -c "Locale::En, Key::Absence\|Locale::En, Key::Vacation" shifty-dioxus/src/i18n/en.rs` ergibt mindestens 60
    - **B-1 / Pitfall 2 Schutz:** `awk '/\/\/ Absence management \(Phase 8\)/,/^[[:space:]]*}[[:space:]]*$/' shifty-dioxus/src/i18n/en.rs | grep -c 'Locale::De\|Locale::Cs'` ergibt 0
    - `cd shifty-dioxus && nix develop --command cargo test i18n_absence_keys_match_english_reference` Exit-Code 0
  </acceptance_criteria>

  <done>en.rs enthält 60 Locale::En-add_text-Calls; englische Reference-Test grün; KEIN falscher Locale-Tag im Block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3d: Locale::Cs-Befüllung in cs.rs + WASM-Build-Gate</name>

  <read_first>
    - shifty-dioxus/src/i18n/cs.rs (Z. 1-80 für `add_text(Locale::Cs, ...)` Pattern)
    - shifty-dioxus/src/i18n/mod.rs (Output von Task 3a — Key-Enum-Variants existieren)
    - .planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md § "Copywriting Contract" Spalte "Cs" (verbatim Strings)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pitfall 2"
  </read_first>

  <files>shifty-dioxus/src/i18n/cs.rs (modify)</files>

  <action>
    Füge nach dem letzten existierenden `add_text(Locale::Cs, ...)`-Aufruf einen neuen Comment-Block ein. **ALLE 60 add_text-Aufrufe explizit aus UI-SPEC § "Copywriting Contract" Spalte "Cs" (verbatim, komplette Liste):**

    ```rust
        // Absence management (Phase 8)
        i18n.add_text(Locale::Cs, Key::AbsencePageTitle, "Nepřítomnosti");
        i18n.add_text(Locale::Cs, Key::AbsencePageSubtitle, "Dovolená, nemoc a neplacené volno jako období. Hodiny za den vycházejí z platné pracovní smlouvy.");
        i18n.add_text(Locale::Cs, Key::AbsenceMenuLabel, "Nepřítomnosti");
        i18n.add_text(Locale::Cs, Key::AbsenceNewBtn, "Nová nepřítomnost");
        i18n.add_text(Locale::Cs, Key::AbsenceModalCreateBtn, "Vytvořit");
        i18n.add_text(Locale::Cs, Key::AbsenceModalSaveBtn, "Uložit");
        i18n.add_text(Locale::Cs, Key::AbsenceModalCancelBtn, "Zrušit");
        i18n.add_text(Locale::Cs, Key::AbsenceModalDeleteBtn, "Smazat");
        i18n.add_text(Locale::Cs, Key::AbsenceEmptyFilterHeading, "Žádné výsledky");
        i18n.add_text(Locale::Cs, Key::AbsenceEmptyFilterBody, "Žádné nepřítomnosti neodpovídají filtru. Resetuj filtr nebo vytvoř novou nepřítomnost.");
        i18n.add_text(Locale::Cs, Key::AbsenceEmptySelfHeading, "Zatím žádné nepřítomnosti");
        i18n.add_text(Locale::Cs, Key::AbsenceEmptySelfBody, "Vytvoř svou první nepřítomnost (dovolenou, nemoc nebo neplacené volno).");
        i18n.add_text(Locale::Cs, Key::AbsenceFieldEmployee, "Zaměstnanec");
        i18n.add_text(Locale::Cs, Key::AbsenceFieldCategory, "Kategorie");
        i18n.add_text(Locale::Cs, Key::AbsenceFieldFrom, "Od");
        i18n.add_text(Locale::Cs, Key::AbsenceFieldTo, "Do (včetně)");
        i18n.add_text(Locale::Cs, Key::AbsenceFieldDescription, "Popis");
        i18n.add_text(Locale::Cs, Key::AbsenceFieldDescriptionHint, "Volitelné — např. místo cesty nebo poznámka.");
        i18n.add_text(Locale::Cs, Key::AbsenceModalCreateSubtitle, "Celodenní období. Hodiny vycházejí ze smlouvy.");
        i18n.add_text(Locale::Cs, Key::AbsenceModalEditSubtitle, "Změny se ukládají s optimistickým zamykáním.");
        i18n.add_text(Locale::Cs, Key::AbsencePreviewHeader, "Náhled");
        i18n.add_text(Locale::Cs, Key::AbsencePreviewFooter, "Svátky v období se počítají jako 0 h. Hodiny za den vycházejí ze smlouvy platné v daný den.");
        i18n.add_text(Locale::Cs, Key::AbsenceCategoryVacation, "Dovolená");
        i18n.add_text(Locale::Cs, Key::AbsenceCategorySickLeave, "Nemoc");
        i18n.add_text(Locale::Cs, Key::AbsenceCategoryUnpaidLeave, "Neplacené");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterCategoryLabel, "Kategorie");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterCategoryAll, "Všechny");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterPersonLabel, "Osoba");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterPersonAll, "Všichni lidé");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterStatusLabel, "Stav");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterStatusAll, "Všechny");
        i18n.add_text(Locale::Cs, Key::AbsenceStatusActive, "Aktivní");
        i18n.add_text(Locale::Cs, Key::AbsenceStatusPlanned, "Plánováno");
        i18n.add_text(Locale::Cs, Key::AbsenceStatusFinished, "Ukončeno");
        i18n.add_text(Locale::Cs, Key::AbsenceColEmployee, "Zaměstnanec");
        i18n.add_text(Locale::Cs, Key::AbsenceColRange, "Období");
        i18n.add_text(Locale::Cs, Key::AbsenceColCategory, "Kategorie");
        i18n.add_text(Locale::Cs, Key::AbsenceColStatus, "Stav");
        i18n.add_text(Locale::Cs, Key::AbsenceColWarnings, "Upozornění");
        i18n.add_text(Locale::Cs, Key::AbsenceDayUnit, "den");
        i18n.add_text(Locale::Cs, Key::AbsenceDaysUnit, "dny");
        i18n.add_text(Locale::Cs, Key::VacationEntitlementHero, "Nárok na dovolenou {year}");
        i18n.add_text(Locale::Cs, Key::VacationDaysRemaining, "dnů zbývá");
        i18n.add_text(Locale::Cs, Key::VacationCardSelfTitle, "Tvé saldo dovolené");
        i18n.add_text(Locale::Cs, Key::VacationCardSelfSubtitle, "Nárok ze smlouvy + převod z předchozího roku.");
        i18n.add_text(Locale::Cs, Key::VacationCardTeamTitle, "Nárok na dovolenou tým · {count} osob");
        i18n.add_text(Locale::Cs, Key::VacationCardTeamSubtitle, "Součet za všechny placené zaměstnance.");
        i18n.add_text(Locale::Cs, Key::VacationStatContract, "Smlouva");
        i18n.add_text(Locale::Cs, Key::VacationStatCarryover, "Převod '{year-1}'");
        i18n.add_text(Locale::Cs, Key::VacationStatUsed, "Vyčerpáno");
        i18n.add_text(Locale::Cs, Key::VacationStatPending, "Čeká");
        i18n.add_text(Locale::Cs, Key::VacationStatRemaining, "Zbývá");
        i18n.add_text(Locale::Cs, Key::VacationPerPersonHeader, "Podle osoby · seřazeno podle zbývajících dnů");
        i18n.add_text(Locale::Cs, Key::VacationPerPersonShowAll, "Všechny ({count})");
        i18n.add_text(Locale::Cs, Key::VacationPerPersonShowLess, "Méně");
        i18n.add_text(Locale::Cs, Key::AbsenceStatSickLeaveDays, "Dny nemoci {year}");
        i18n.add_text(Locale::Cs, Key::AbsenceStatUnpaidDays, "Neplacené {year}");
        i18n.add_text(Locale::Cs, Key::AbsenceStatActive, "Aktivní nepřítomnosti");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorRangeInverted, "Konec je před začátkem");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorSelfOverlapHeader, "Vlastní překryv");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorSelfOverlapBody, "Položka {category} od {from} do {to} se překrývá. Uprav období nebo kategorii.");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorVersionConflictHeader, "Položka byla změněna jinde");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorVersionConflictBody, "Načti znovu a ulož. Tvé zadání zůstane zachováno.");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorVersionConflictReload, "Znovu načíst");
        i18n.add_text(Locale::Cs, Key::AbsenceErrorNetwork, "Chyba sítě. Zkus to znovu.");
        i18n.add_text(Locale::Cs, Key::AbsenceWarningHeaderSingular, "Upozornění · 1 konflikt (neblokující)");
        i18n.add_text(Locale::Cs, Key::AbsenceWarningHeaderPlural, "Upozornění · {count} konfliktů (neblokující)");
        i18n.add_text(Locale::Cs, Key::AbsenceWarningAcknowledgeBtn, "Rozumím");
        i18n.add_text(Locale::Cs, Key::AbsenceWarningOverlapsBooking, "Existující rezervace dne {date} se s touto nepřítomností překrývá.");
        i18n.add_text(Locale::Cs, Key::AbsenceWarningOverlapsManual, "Den ručně označený jako nedostupný se překrývá. Po cutoveru je záznam nadbytečný.");
        i18n.add_text(Locale::Cs, Key::AbsenceDeleteConfirmTitle, "Smazat nepřítomnost?");
        i18n.add_text(Locale::Cs, Key::AbsenceDeleteConfirmBody, "Měkké smazání — položka zůstane v auditních záznamech, ale nebude se počítat v reportech ani zobrazovat v plánu směn.");
        i18n.add_text(Locale::Cs, Key::AbsenceDeleteConfirmBtn, "Smazat");
        i18n.add_text(Locale::Cs, Key::AbsenceDeleteCancelBtn, "Zrušit");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterShowPast, "Zobrazit minulé");
        i18n.add_text(Locale::Cs, Key::AbsenceFilterCounter, "{n} z {m}");
    ```

    **PFLICHT (Pitfall 2):** ALLE add_text-Aufrufe in cs.rs nutzen `Locale::Cs`. Der neue Phase-8-Block enthält weder `Locale::De` noch `Locale::En`.

    **WASM-Build-Gate (Final):** Nach 3a–d ist der i18n-Track komplett. Final-Verify im selben Task: `cargo build --target wasm32-unknown-unknown --manifest-path shifty-dioxus/Cargo.toml`.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo test 2>&1 | tail -20 &amp;&amp; nix develop --command cargo build --target wasm32-unknown-unknown --manifest-path shifty-dioxus/Cargo.toml 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "// Absence management" shifty-dioxus/src/i18n/cs.rs` ergibt mindestens 1
    - `grep -c "Locale::Cs, Key::Absence\|Locale::Cs, Key::Vacation" shifty-dioxus/src/i18n/cs.rs` ergibt mindestens 60
    - **B-1 / Pitfall 2 Schutz:** `awk '/\/\/ Absence management \(Phase 8\)/,/^[[:space:]]*}[[:space:]]*$/' shifty-dioxus/src/i18n/cs.rs | grep -c 'Locale::De\|Locale::En'` ergibt 0
    - `cd shifty-dioxus && nix develop --command cargo test i18n_absence_keys_match_czech_reference` Exit-Code 0
    - `cd shifty-dioxus && nix develop --command cargo test i18n_absence_keys_present_in_all_locales` Exit-Code 0 (alle drei Locales komplett)
    - `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` Exit-Code 0 (PFLICHT WASM-Build-Gate, Pitfall 1 + Pitfall 9)
  </acceptance_criteria>

  <done>cs.rs enthält 60 Locale::Cs-add_text-Calls; tschechische Reference-Test grün; alle drei Locale-Tests grün; WASM-Build grün. Plan 05 kann jeden Key direkt mit `i18n.t(Key::AbsenceFoo)` rendern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Frontend | Auth-Privilege wird im Backend enforced — Frontend `has_privilege("hr")`-Check ist nur UI-Hint |
| Frontend → Backend | reqwest-Calls über Dx-Proxy; reqwest-error_for_status_ref() handhabt 4xx/5xx; 422/409 werden semantisch in ShiftyError übersetzt |
| WASM-Boundary | rest-types darf NICHT service-impl-Feature ziehen — Pitfall 1 |
| i18n-Locale-Boundary | Drei Locales müssen synchron befüllt sein, sonst Locale::En-statt-De-Bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-01 | Elevation of Privilege | Frontend Permission-Check | mitigate (defense-in-depth) | Frontend liest `auth.has_privilege("hr")`. ECHTE Permission-Enforcement passiert im Backend (Plan 02). Frontend versteckt nur UI; Backend lehnt unauthorized Request ab. |
| T-8-AUTH-02 | Elevation of Privilege | Frontend HR-Filter | mitigate (defense-in-depth) | Backend enforced HR auf `/absence-period` (existing) und `/vacation-balance/team/*` (Plan 02). Frontend kann Filter zeigen, Backend liefert 403 falls Auth-Subject nicht HR. |
| T-8-IDOR-01 | Information Disclosure | Frontend GET /absence-period/{id} | mitigate | Backend prüft hr ∨ self auf jeder Resource. Frontend api.rs ist nur Pass-Through; api-Funktionen liefern 403 → ShiftyError → ERROR_STORE → ErrorView. |
| T-8-XSS-01 | Tampering | description Free-Text-Field rendering | mitigate | Plan 04 fügt `description` als `Arc<str>` in `AbsencePeriod`-State hinzu. Plan 05 rendert es via `{description}`-Interpolation in rsx! — Dioxus auto-escaped, KEIN `dangerous_inner_html`. Verhindert XSS via Description-Field. |
| T-8-INJECTION-01 | Injection | i18n-User-Content (z. B. {category} im SelfOverlap-Body) | mitigate | i18n-Strings haben `{placeholder}`-Tokens — beim Render via i18n.t() wird der Wert interpoliert. Plan 05 stellt sicher, dass Substitution-Werte aus `AbsenceCategory`-Enum (statisch) und `from_date`/`to_date` (Date-Format) kommen — nicht User-Input. Description geht NICHT durch Interpolation, sondern als separate Render-Span (auto-escaped). |
| T-8-WASM-CONTAINMENT | Information Disclosure | rest-types Cross-Crate | mitigate | Plan 04 verändert NICHT `shifty-dioxus/Cargo.toml` Z. 28-30 (default-features = false). WASM-Build-Gate bleibt grün → Pitfall 1 hold. |
| T-8-LOCALE-BUG | Information Disclosure (UX) | i18n/de.rs Locale-Tag | mitigate | Pre-Commit grep prüft `Locale::De` in de.rs (nicht `Locale::En`); i18n-Test `i18n_absence_keys_match_german_reference` bestätigt 5 Stichproben mit dem deutschen Original-String. |
</threat_model>

<verification>
**Layer 1 — Compile (Backend-Workspace):**
- `cargo check --workspace` (im Repo-Root, in `nix develop`) grün — Plan 04 darf das Backend nicht brechen.

**Layer 2 — Compile (Frontend-Subworkspace):**
- `cd shifty-dioxus && nix develop --command cargo check` grün
- `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` grün — KRITISCH (Pitfall 1 + 9)

**Layer 3 — i18n-Tests:**
- `cd shifty-dioxus && nix develop --command cargo test i18n_absence_keys_present_in_all_locales` grün — alle 3 Locales mit allen ausgewählten Keys ohne `??`-Fallback
- `cd shifty-dioxus && nix develop --command cargo test i18n_absence_keys_match_german_reference` grün — deutsche Stichprobe matched

**Layer 4 — Frontend-Workspace-Tests:**
- `cd shifty-dioxus && nix develop --command cargo test` grün — keine Regression

**Layer 5 — Dx-Proxy:**
- Dioxus.toml enthält beide Einträge, kein leerer Block
</verification>

<success_criteria>
- API-Layer + Error-Variant + State-Types + Loader + Coroutine-Services + i18n + Dx-Proxy alle vorhanden
- WASM-Build grün
- Backend `cargo check --workspace` grün
- 60 i18n-Keys in drei Locales befüllt (kein Locale-Bug)
- i18n-Test grün
- Plan 05 kann darauf die Page + Modal + Routing + TopBar bauen, ohne diese Layers anzufassen
</success_criteria>

<output>
After completion, create `.planning/phases/08-absence-crud-page-foundation/08-04-SUMMARY.md`.

User commited manuell. Empfohlene jj-Commits (logisch):
- `feat(08): absence frontend api, error variant, state types, loader`
- `feat(08): absence + vacation_balance coroutine services + app.rs`
- `feat(08): absence i18n keys (de/en/cs) + i18n test`
- `chore(08): proxy entries for absence-period + vacation-balance`
</output>
</content>
</invoke>