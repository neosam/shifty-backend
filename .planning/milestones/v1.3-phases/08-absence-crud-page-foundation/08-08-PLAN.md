---
plan: 08-08
phase: 08-absence-crud-page-foundation
title: Cutover-Response Drift-Details — interpretierbare Inline-Diagnostik
gap_closure: true
autonomous: true
wave: 8
requirements: []
files_modified:
  - service/src/cutover.rs
  - service_impl/src/cutover.rs
  - rest-types/src/lib.rs
  - rest/tests/openapi_surface.rs
  - shifty_bin/src/integration_test/cutover.rs
---

<objective>
Aktuell liefert `POST /admin/cutover/commit` (und `gate-dry-run`) nur Counts +
einen `diff_report_path` zurück. Wenn das Gate fehlschlägt, muss der User die
JSON-Datei auf dem Filesystem öffnen, um zu sehen welcher Eintrag den Drift
verursacht hat. Das ist im UAT-Workflow mühsam und beim späteren Cutover-UI
(Backlog-Item) gar nicht erreichbar (Browser kein FS-Zugriff auf Backend-Pfade).

**Ziel:** Wenn das Gate fehlschlägt, liefert der Response **inline alle relevanten
Drift-Details direkt im JSON**, in einer Form die ohne weitere Dokumentation
verständlich ist.

Konkrete Anforderungen:

1. **Vollständige Drift-Daten im Response** — `CutoverRunResultTO.gate_drift_report:
   Option<CutoverGateDriftReportTO>`. None wenn `gate_passed = true`, Some sonst.
   Damit ist der File-Pfad-Detour optional, nicht mehr zwingend.

2. **Pro-Eintrag-Details** — die aktuelle `CutoverGateDriftRowTO`-Aggregation
   pro `(sales_person, category, year)` reicht nicht. Es muss eine zusätzliche
   Liste der **einzelnen problematischen `extra_hours`-Einträge** dabei sein,
   damit der User den genauen Tag sieht. Felder pro Eintrag:
   - `extra_hours_id` (UUID — für gezieltes Löschen via `DELETE /extra-hours/{id}`)
   - `date` (ISO-8601 Date String, z.B. `"2026-05-08"`)
   - `weekday` (3-Letter-Code: `"Mon"`, `"Tue"`, ..., `"Sun"`)
   - `amount` (f32 — die gebuchten Stunden)
   - `reason_code` (stabiler Maschinen-Code, z.B. `"contract_hours_zero_for_day"`)
   - `reason_text` (Englisch, human-readable: "Employee has zero contract hours
     on this weekday per their working-hours contract")
   - `suggested_action` (Englisch: "Delete this entry, or move it to a contract
     workday before retrying the cutover")

3. **Reason-Mapping zentralisiert** — Statt im Mapper ad-hoc Strings zu erzeugen,
   bekommt `QuarantineReason` zwei neue Methoden:
   - `pub fn human_text(&self) -> &'static str`
   - `pub fn suggested_action(&self) -> &'static str`
   Pro enum-Variante eine sinnvolle Beschreibung. **Englisch** als
   Backend-Default — i18n übernimmt das spätere Cutover-UI auf Frontend-Seite
   (eigenes Backlog-Item).

4. **Backwards-compatibility** — neue Felder sind purely additive. `quarantine_reasons:
   Vec<String>` (existing) bleibt für Backwards-Compat erhalten und enthält
   weiterhin die distinct reason_codes des aggregierten Drift-Rows.

Das ist ein kleiner, fokussierter Polish-Plan im Sinne der Cutover-UX.
</objective>

<tasks>

### Task 1 — `QuarantineReason` Reason-Mapping

**File:** `service/src/cutover.rs`

Erweitere die `impl QuarantineReason`-Block um zwei neue Methoden:

```rust
impl QuarantineReason {
    // existing as_persisted_str() — keep unchanged

    pub fn human_text(&self) -> &'static str {
        match self {
            Self::AmountBelowContractHours => "Booked hours are below the employee's contract hours for this day",
            Self::AmountAboveContractHours => "Booked hours exceed the employee's contract hours for this day",
            Self::ContractHoursZeroForDay  => "Employee has zero contract hours on this weekday (e.g., 4-day contract with the booking falling on a non-workday)",
            Self::ContractNotActiveAtDate  => "Employee had no active working-hours contract on this date",
            Self::Iso53WeekGap             => "Booking falls into ISO calendar week 53, which the new range model does not represent",
        }
    }

    pub fn suggested_action(&self) -> &'static str {
        match self {
            Self::AmountBelowContractHours
            | Self::AmountAboveContractHours => "Adjust the entry's hours to match the employee's contract for that day, or delete the entry if it was a mistake",
            Self::ContractHoursZeroForDay    => "Delete the entry, or move it to a workday on which the employee's contract specifies > 0 hours",
            Self::ContractNotActiveAtDate    => "Verify the employee's working-hours contract covers this date, or delete the entry",
            Self::Iso53WeekGap               => "Move the entry to a date inside an ISO calendar week 1-52 (typically the first or last day of the surrounding week)",
        }
    }
}
```

Plus 1 neuer Unit-Test in `service_impl/src/test/cutover.rs` (oder analog), der
für jede enum-Variante checked, dass `human_text()` und `suggested_action()`
nicht-leere Strings liefern (defense-in-depth gegen "vergessene Variante").

**jj-Commit:** `feat(08-08): human-readable text + suggested-action per QuarantineReason`

---

### Task 2 — Per-Entry Quarantine-Details im Service-Layer

**Files:**
- `service/src/cutover.rs` — `CutoverRunResult` und Helper-Strukturen
- `service_impl/src/cutover.rs` — gate-Logik

In `service/src/cutover.rs`: Neuer struct `CutoverQuarantineEntry`:
```rust
#[derive(Clone, Debug, PartialEq)]
pub struct CutoverQuarantineEntry {
    pub extra_hours_id: uuid::Uuid,
    pub date: time::Date,                    // konvertiert aus QuarantinedRow.date_time
    pub amount: f32,
    pub reason: QuarantineReason,
}
```

Erweitere `CutoverGateDriftRow` (Service-Layer, vermutlich existiert das Pendant zu
`CutoverGateDriftRowTO` schon — verifizieren) um `quarantined_entries: Vec<CutoverQuarantineEntry>`.

Im `service_impl/src/cutover.rs`-Gate-Aufbau: Wenn die Drift-Row über dem Threshold
liegt (Linie ~661), die zugehörigen quarantine-Einträge pro `(sales_person, category, year)`
filtern und in `quarantined_entries` reichen.

Außerdem: `CutoverRunResult` (Service-Layer) bekommt `gate_drift_report: Option<CutoverGateDriftReport>`-Feld.
Das wird im commit-/dry-run-Pfad gesetzt, wenn `gate_passed = false`.

**jj-Commit:** `feat(08-08): per-entry quarantine details in CutoverRunResult`

---

### Task 3 — REST-DTO erweitern

**File:** `rest-types/src/lib.rs`

1. Neuer DTO:
```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct CutoverQuarantineEntryTO {
    pub extra_hours_id: Uuid,
    /// ISO-8601 date string (e.g. "2026-05-08").
    pub date: String,
    /// 3-letter weekday code: "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun".
    pub weekday: String,
    pub amount: f32,
    /// Stable machine code, e.g. "contract_hours_zero_for_day".
    pub reason_code: String,
    /// Human-readable explanation in English (default — frontend may translate).
    pub reason_text: String,
    /// Suggested action in English.
    pub suggested_action: String,
}
```

2. `CutoverGateDriftRowTO` erweitern:
```rust
pub struct CutoverGateDriftRowTO {
    // ... existing fields ...
    pub quarantined_entries: Vec<CutoverQuarantineEntryTO>,
}
```

3. `CutoverRunResultTO` erweitern:
```rust
pub struct CutoverRunResultTO {
    // ... existing fields ...
    pub gate_drift_report: Option<CutoverGateDriftReportTO>,
}
```

4. `From`-impls anpassen — Mapping vom Service-`QuarantineReason` zu den drei
   String-Feldern (`reason_code` = `as_persisted_str()`, `reason_text` =
   `human_text()`, `suggested_action` = `suggested_action()`).

5. Weekday-Berechnung: `time::Date::weekday()` → 3-Letter-Code-Mapping. Eine
   freie Helper-Function `fn weekday_code(date: time::Date) -> &'static str`
   in `rest-types/src/lib.rs` (oder einem geeigneten util-Modul) — 7 match-Arme.

**jj-Commit:** `feat(08-08): inline gate_drift_report in CutoverRunResultTO with per-entry details`

---

### Task 4 — OpenAPI surface test + integration test update

**Files:**
- `rest/tests/openapi_surface.rs` — neue Schemas in expected-list:
  - `CutoverQuarantineEntryTO`
- `shifty_bin/src/integration_test/cutover.rs` — neuer Test der den failed-gate-Pfad
  prüft und assertet, dass:
  - `gate_drift_report.is_some()`
  - `gate_drift_report.unwrap().drift[0].quarantined_entries.len() > 0`
  - Jeder Entry hat reason_text + suggested_action mit non-empty Strings
  - Weekday-Code ist eines von 7 erlaubten Werten

**jj-Commit:** `test(08-08): integration test + openapi surface for inline drift_report`

---

### Task 5 — Final-Gates + SUMMARY

- `nix develop -c cargo test --workspace` — alle grün, neue Tests inklusive
- `nix develop -c cargo build --bin shifty_bin` — grün
- `cd shifty-dioxus && nix-shell -p lld --run "cargo build --target wasm32-unknown-unknown"` — grün (FE nicht touched, sollte trivial green sein, aber zur Sicherheit)
- 08-08-SUMMARY.md schreiben
- STATE.md + ROADMAP.md updaten (Phase 8 plan_count: 7 → 8)

**jj-Commit:** `docs(08-08): summary + STATE + ROADMAP for cutover-response polish`

</tasks>

<acceptance>
- [ ] `QuarantineReason::human_text()` + `suggested_action()` für alle 5 Varianten
- [ ] Per-Variante Test der die zwei Methoden non-empty assertet
- [ ] `CutoverRunResult.gate_drift_report` gesetzt wenn gate fails
- [ ] Per-(sales_person, category, year)-Drift-Row hat liste der einzelnen problematischen `extra_hours`-Einträge mit date + weekday + amount + reason
- [ ] `CutoverRunResultTO.gate_drift_report: Option<CutoverGateDriftReportTO>` im JSON
- [ ] `CutoverQuarantineEntryTO` neu: extra_hours_id, date (ISO), weekday (3-letter), amount, reason_code, reason_text, suggested_action
- [ ] Bestehende `quarantine_reasons: Vec<String>` bleibt (backwards-compat)
- [ ] Integration-Test deckt failed-gate-Pfad ab
- [ ] OpenAPI-Surface-Test grün (neuer Schema-Eintrag)
- [ ] cargo test --workspace grün
- [ ] WASM build grün
- [ ] SUMMARY.md geschrieben
- [ ] STATE.md + ROADMAP.md updated (plan_count 8)
- [ ] Alle Commits via jj — kein git
</acceptance>
