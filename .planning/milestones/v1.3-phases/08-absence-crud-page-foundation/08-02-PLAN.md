---
phase: 08-absence-crud-page-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - service_impl/src/lib.rs
  - service_impl/src/vacation_balance.rs
  - service_impl/src/test/mod.rs
  - service_impl/src/test/vacation_balance.rs
  - rest/src/lib.rs
  - rest/src/vacation_balance.rs
  - shifty_bin/src/main.rs
autonomous: true
requirements: [FUI-A-04]
requirements_addressed: [FUI-A-04]
tags: [shifty-backend, vacation-balance, service-impl, rest, di-wiring, bl-tier]
must_haves:
  truths:
    - "VacationBalanceServiceImpl ist BL-Tier mit gen_service_impl!-Macro registriert; konsumiert AbsenceService + EmployeeWorkDetailsService + CarryoverService + SalesPersonService + PermissionService + ClockService + TransactionDao (per D-04)"
    - "REST-Endpoint `GET /vacation-balance/{sales_person_id}/{year}` mit `#[utoipa::path]` und Permission `HR ∨ self` (T-8-AUTH-01, T-8-IDOR-01) — D-03"
    - "REST-Endpoint `GET /vacation-balance/team/{year}` mit `#[utoipa::path]` und Permission `HR-only` (T-8-AUTH-02) — D-03"
    - "DI-Konstruktion in shifty_bin/src/main.rs konstruiert VacationBalanceServiceImpl NACH AbsenceServiceImpl + EmployeeWorkDetailsServiceImpl + CarryoverServiceImpl (Service-Tier-Konvention, Pitfall 3)"
    - "Mindestens 6 Unit-Tests in service_impl/src/test/vacation_balance.rs decken: Happy-Path-Subtraktion, HR-Path-OK, Self-Path-OK, Other-without-HR-Forbidden, get_team-without-HR-Forbidden, no-active-contract→0-entitlement"
    - "Permission-Pattern HR ∨ self ist via `join!(check_permission(HR), verify_user_is_sales_person).or()` implementiert (T-8-AUTH-01)"
  artifacts:
    - path: service_impl/src/vacation_balance.rs
      provides: "VacationBalanceServiceImpl + Deps via gen_service_impl! + Methode get/get_team"
      min_lines: 120
      contains: "gen_service_impl!"
    - path: service_impl/src/test/vacation_balance.rs
      provides: "≥6 Unit-Tests mit mockall-Stubs"
      min_lines: 200
      contains: "#[tokio::test]"
    - path: rest/src/vacation_balance.rs
      provides: "REST-Handler get + get_team + utoipa-path-Annotations + ApiDoc"
      min_lines: 80
      contains: "#[utoipa::path"
    - path: shifty_bin/src/main.rs
      provides: "VacationBalanceServiceDependencies impl + Konstruktor + RestStateImpl-Wiring"
      contains: "VacationBalanceServiceDependencies"
  key_links:
    - from: rest/src/lib.rs
      to: rest/src/vacation_balance.rs
      via: ".nest(\"/vacation-balance\", vacation_balance::generate_route())"
      pattern: "vacation_balance::generate_route"
    - from: rest/src/lib.rs
      to: VacationBalanceApiDoc
      via: "ApiDoc nest-entry"
      pattern: "vacation_balance::VacationBalanceApiDoc"
    - from: rest/src/vacation_balance.rs
      to: rest_state.vacation_balance_service()
      via: "RestStateDef getter"
      pattern: "rest_state.vacation_balance_service"
    - from: shifty_bin/src/main.rs
      to: service_impl::vacation_balance::VacationBalanceServiceImpl
      via: "type alias + Konstruktor + DI-Wiring"
      pattern: "VacationBalanceServiceDependencies"
---

<objective>
Wave-2-Backend-Implementation: Realisiere `VacationBalanceServiceImpl` (BL-Tier per D-04) mit `gen_service_impl!`, schreibe Unit-Tests, baue REST-Layer mit `#[utoipa::path]`-Annotationen, registriere ApiDoc und DI im `shifty_bin/src/main.rs`. Damit ist der Backend-Resturlaubs-Endpoint vollständig.

Purpose: Liefert den Backend-Endpoint, den Wave 3 (Frontend) konsumiert. `VacationEntitlementCard` und `VacationPerPersonList` brauchen einen autoritativen Resturlaubs-Wert (D-03/D-04). Permission HR ∨ self analog `/absence-period`.

Output:
- `service_impl/src/vacation_balance.rs` (BL-Tier-Service mit `gen_service_impl!`)
- `service_impl/src/test/vacation_balance.rs` (≥6 Unit-Tests)
- `rest/src/vacation_balance.rs` (Router + Handler + utoipa-Annotation + ApiDoc)
- `rest/src/lib.rs` Mod-decl + nest + ApiDoc-Eintrag + RestStateDef-Erweiterung
- `shifty_bin/src/main.rs` Deps-Impl + Konstruktor + RestStateImpl-Field + Getter
- `cargo test --workspace` grün (inkl. neue Tests)
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

## Project Constraints (verbindlich)

- **VCS:** jj-only. Niemals `git commit`/`git add`. Nutze `jj describe -m "feat(08): VacationBalanceServiceImpl + REST + tests + DI"` und ggf. `jj new` für Logical-Split. GSD ruft KEIN `git commit`.
- **NixOS:** `nix develop` (NICHT `nix-shell`). `cargo`, `sqlx` etc. nur in `nix develop` ausführen.
- **Service-Tier-Konvention** (CLAUDE.md): VacationBalanceServiceImpl ist BL-Tier — konsumiert AbsenceService (BL→BL ist erlaubt, kein Cycle weil AbsenceService VacationBalance nicht kennt; analog CarryoverRebuildService, siehe shifty_bin/src/main.rs:482-493). KEIN Basic-Tier (würde DI-Cycle erzeugen, Pitfall 3).
- **gen_service_impl!:** Macro Pflicht für Service-Impls (CLAUDE.md). Manuelle `impl Service for X` ist anti-pattern.
- **Transaction-Pattern:** Jede Methode startet mit `let tx = self.transaction_dao.use_transaction(tx).await?;` und endet mit `self.transaction_dao.commit(tx).await?;` — Pattern aus service_impl/src/absence.rs:88-101.
- **Permission-Pattern HR ∨ self:** `let (hr, sp) = join!(check_permission(HR), verify_user_is_sales_person(sp_id, ctx, tx.clone().into())); hr.or(sp)?;` — Pattern aus service_impl/src/absence.rs:110-119.
- **Permission-Pattern HR-only:** `self.permission_service.check_permission(HR_PRIVILEGE, context).await?;` — Pattern aus service_impl/src/absence.rs:88-93.
- **OpenAPI:** `#[utoipa::path]`-Annotation auf jedem Handler Pflicht. ApiDoc-Eintrag in `rest/src/lib.rs` Pflicht.
- **DI-Reihenfolge in main.rs:** `VacationBalanceServiceImpl`-Konstruktion MUSS NACH `AbsenceServiceImpl` (Z. 798), `EmployeeWorkDetailsServiceImpl` aka working_hours_service (Z. 788), `CarryoverServiceImpl` (Z. 843) erfolgen — alle drei sind Cross-Deps. Konkrete Position: nach Z. 843 (Pitfall 3).
- **Snapshot-Versioning:** Phase 8 fügt KEINE neuen `BillingPeriodValueType`-Varianten hinzu. `CURRENT_SNAPSHOT_SCHEMA_VERSION` MUSS nicht gebumpt werden.

## D-Mapping

- D-03: REST-Endpoint(s) für Resturlaub — implementiert hier.
- D-04: Service ist BL-Tier mit Cross-Entity-Aggregation — bestätigt durch Dep-Set (AbsenceService + EmployeeWorkDetailsService + CarryoverService).

## OpenAPI-Snapshot

Plan 03 macht den `cargo insta accept`-Snapshot-Refresh. Plan 02 schreibt nur den Code, der den Snapshot ändern würde. WENN der Snapshot-Test in Plan 02 fehlschlägt, ist das normales Verhalten — Plan 03 nimmt den Refresh vor.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-absence-crud-page-foundation/08-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md
@.planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md
@.planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md
@.planning/phases/08-absence-crud-page-foundation/08-01-PLAN.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Aus Plan 01 verfügbar: -->
<!-- service::vacation_balance::VacationBalance (struct) -->
<!-- service::vacation_balance::VacationBalanceService (trait) -->
<!-- service::vacation_balance::MockVacationBalanceService (automock) -->
<!-- rest_types::VacationBalanceTO (DTO) + From-Impls (gefeatured service-impl) -->

From service_impl/src/absence.rs (Tier-Vorlage — `gen_service_impl!`-Block Z. 45-63):
```rust
gen_service_impl! {
    struct AbsenceServiceImpl: AbsenceService = AbsenceServiceDeps {
        AbsenceDao: AbsenceDao<Transaction = Self::Transaction> = absence_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,
        ClockService: ClockService = clock_service,
        UuidService: UuidService = uuid_service,
        SpecialDayService: SpecialDayService<Context = Self::Context> = special_day_service,
        EmployeeWorkDetailsService: EmployeeWorkDetailsService<Context = Self::Context, Transaction = Self::Transaction> = employee_work_details_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        BookingService: BookingService<Context = Self::Context, Transaction = Self::Transaction> = booking_service,
        SalesPersonUnavailableService: SalesPersonUnavailableService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_unavailable_service,
        SlotService: SlotService<Context = Self::Context, Transaction = Self::Transaction> = slot_service,
    }
}
```

From service_impl/src/absence.rs:88-119 (Permission + Tx-Pattern):
```rust
// HR-only
let tx = self.transaction_dao.use_transaction(tx).await?;
self.permission_service.check_permission(HR_PRIVILEGE, context).await?;
// ... DAO-/Service-Calls ...
self.transaction_dao.commit(tx).await?;
Ok(result)

// HR ∨ self
let (hr, sp) = join!(
    self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
    self.sales_person_service.verify_user_is_sales_person(
        sales_person_id, context, tx.clone().into()
    ),
);
hr.or(sp)?;
```

From rest/src/absence.rs:30-77 (Router + handler + utoipa pattern, REQ-Body POST):
```rust
pub fn generate_route<RestState: RestStateDef>() -> Router<RestState> {
    Router::new()
        .route("/", post(create_absence_period::<RestState>))
        // ...
}

#[instrument(skip(rest_state))]
#[utoipa::path(
    post, path = "", tags = ["Absence"],
    request_body = AbsencePeriodTO,
    responses(
        (status = 201, description = "...", body = AbsencePeriodCreateResultTO),
        (status = 403, description = "Forbidden"),
        (status = 422, description = "Validation error"),
    ),
)]
pub async fn create_absence_period<RestState: RestStateDef>(
    rest_state: State<RestState>,
    Extension(context): Extension<Context>,
    Json(body): Json<AbsencePeriodTO>,
) -> Response {
    error_handler((async {
        let svc = rest_state.absence_service();
        let result = svc.create(&(&body).into(), context.into(), None).await?;
        let to = AbsencePeriodCreateResultTO::from(&result);
        Ok(Response::builder().status(201).header(...).body(...).unwrap())
    }).await)
}
```

From rest/src/absence.rs:240-261 (ApiDoc-Block):
```rust
#[derive(OpenApi)]
#[openapi(
    paths(create_absence_period, get_all_absence_periods, /* ... */),
    components(schemas(AbsencePeriodTO, AbsenceCategoryTO, AbsencePeriodCreateResultTO, WarningTO)),
    tags((name = "Absence", description = "Absence period management (range-based)")),
)]
pub struct AbsenceApiDoc;
```

From rest/src/lib.rs:270-390 (RestStateDef-Erweiterung — Pattern):
```rust
type AbsenceService: service::absence::AbsenceService<Context = Context> + Send + Sync + 'static;
fn absence_service(&self) -> Arc<Self::AbsenceService>;
```

From shifty_bin/src/main.rs:526-557 (verified — exact RestStateImpl + variable names, W-5 fix):
```rust
// Variable names verified from main.rs (W-5 fix — Plan 02 Task 3 uses these):
let transaction_dao = Arc::new(TransactionDao::new(pool.clone()));               // line 684
let permission_service = Arc::new(service_impl::PermissionServiceImpl { ... });  // line 707
let clock_service = Arc::new(service_impl::clock::ClockServiceImpl);              // line 711
let uuid_service = Arc::new(service_impl::uuid_service::UuidServiceImpl);         // line 712
let sales_person_service = Arc::new(...);                                          // line 726
let working_hours_service = Arc::new(...);  // EmployeeWorkDetailsServiceImpl     // line 788
let absence_service = Arc::new(service_impl::absence::AbsenceServiceImpl { ... }); // line 798
let carryover_service = Arc::new(service_impl::carryover::CarryoverServiceImpl { ... }); // line 843

// RestStateImpl-Struct-Felder (main.rs:527-557) — Reihenfolge in Struct:
//   permission_service, sales_person_service, special_day_service,
//   sales_person_unavailable_service, booking_service,
//   custom_extra_hours_service, booking_information_service, booking_log_service,
//   reporting_service, working_hours_service, absence_service, ...
// Plan 02 Task 3 fügt vacation_balance_service nach absence_service ein.

// Type-Alias für EmployeeWorkDetails ist `WorkingHoursService` (main.rs:540, 571).
// → In VacationBalanceServiceDependencies-impl: type EmployeeWorkDetailsService = WorkingHoursService;
// → Im Konstruktor: employee_work_details_service: working_hours_service.clone(),
```

From shifty_bin/src/main.rs:228-255 (Deps-Impl-Pattern) und :798-815 (Konstruktor):
```rust
pub struct AbsenceServiceDependencies;
impl service_impl::absence::AbsenceServiceDeps for AbsenceServiceDependencies {
    type Context = Context;
    type Transaction = Transaction;
    type AbsenceDao = AbsenceDao;
    // ... usw
}
type AbsenceService = service_impl::absence::AbsenceServiceImpl<AbsenceServiceDependencies>;

let absence_service = Arc::new(AbsenceServiceImpl {
    absence_dao: absence_dao.clone(),
    permission_service: permission_service.clone(),
    // ...
});
```

From service/src/employee_work_details.rs:230-278 (verified — exact trait signatures, B-4 fix):
```rust
#[automock(type Context=(); type Transaction=dao::MockTransaction;)]
#[async_trait]
pub trait EmployeeWorkDetailsService {
    type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
    type Transaction: dao::Transaction;

    async fn find_by_sales_person_id(
        &self,
        sales_person_id: Uuid,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<Arc<[EmployeeWorkDetails]>, ServiceError>;
    // (other methods: all, find_for_week, all_for_week, create, update, delete)
}

// HINWEIS: KEINE find_for_year-Methode — verwende find_by_sales_person_id und filter
// im Caller via EmployeeWorkDetails::vacation_days_for_year(year) (siehe employee_work_details.rs:156).
//
// EmployeeWorkDetails-Felder (employee_work_details.rs:14-36):
//   - from_year: u32, from_calendar_week: u8, from_day_of_week
//   - to_year: u32, to_calendar_week: u8, to_day_of_week
//   - vacation_days: u8
// pub fn vacation_days_for_year(&self, year: u32) -> f32  (Method auf EmployeeWorkDetails-Domain-Struct)
//   → liefert pro Vertrag den year-anteiligen vacation_days-Wert.
//   → Aggregation für VacationBalance: sum über alle Verträge die das Jahr berühren.
```

From service/src/carryover.rs:9-69 (verified — exact trait + struct, B-4 fix):
```rust
#[derive(Clone, Debug, PartialEq)]
pub struct Carryover {
    pub sales_person_id: Uuid,
    pub year: u32,
    pub carryover_hours: f32,
    pub vacation: i32,    // ← das ist das Field, das VacationBalance.carryover_days füttert
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
}

#[automock(type Context=(); type Transaction=MockTransaction;)]
#[async_trait]
pub trait CarryoverService {
    type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
    type Transaction: dao::Transaction;

    async fn get_carryover(
        &self,
        sales_person_id: Uuid,
        year: u32,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<Option<Carryover>, ServiceError>;

    async fn set_carryover(...) -> Result<(), ServiceError>;
}

// HINWEIS: Method heißt `get_carryover` (NICHT `find`). Liefert Option<Carryover>.
// Carryover-Year-Semantik: get_carryover(sp_id, year) liefert das year-Snapshot
// (NICHT year-1 wie ursprünglich spekuliert) — das `year`-Feld auf Carryover ist
// der Stichtag, ab dem das vacation-Saldo gilt. Plan-Phase nutzt year direkt;
// falls Semantik anders ist (year-1 für Carryover INTO year), passt das Test 1
// und das Aggregations-Schema an die existierende Konvention an
// (siehe service_impl/src/test/carryover.rs als Authority).
```

</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VacationBalanceServiceImpl (BL-Tier) + Unit-Tests</name>

  <read_first>
    - service_impl/src/absence.rs (Z. 1-150 mindestens — `gen_service_impl!`-Block + erste Methoden inkl. Permission HR ∨ self + Tx-Pattern; das ist die exakte Vorlage für die Service-Impl)
    - service_impl/src/lib.rs (existing pub mod-Liste — wo der neue Eintrag hinkommt)
    - service_impl/src/test/absence.rs (Z. 1-100 für Imports + Mock-Setup-Helper, Z. 256-565 für ~5-6 konkrete Test-Funktionen als Vorlage; insbesondere `expect_use_transaction()`, `expect_check_permission()`, `expect_verify_user_is_sales_person()`-Setups)
    - service_impl/src/test/mod.rs (existing `pub mod`-Liste — neuer Eintrag analog `pub mod absence;`)
    - service_impl/src/test/error_test.rs (für `test_forbidden`-Helper)
    - service_impl/src/test/carryover.rs (kurzer Read, falls relevant; Pattern-Sanity)
    - service/src/employee_work_details.rs (B-4 fix — VERIFIED Z. 230-278 Trait-Signatur + Z. 14-36 Struct-Felder + Z. 156-186 `vacation_days_for_year`-Method; **kein** `find_for_year`, nutze `find_by_sales_person_id` und filter im Caller)
    - service/src/carryover.rs (B-4 fix — VERIFIED Z. 51-61 Trait-Signatur: `get_carryover(sp_id, year, ctx, tx) -> Result<Option<Carryover>, ServiceError>` + Z. 9-18 Struct mit Field `vacation: i32`)
    - service/src/vacation_balance.rs (Output von Plan 01)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pattern 5" + "Code Example 7" + "Open Question 1" (Reporting vs direkte Aggregation — Plan-Phase entscheidet: direkte Aggregation per AbsenceService, weil per A4 die Default-Wahl)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "service_impl/src/vacation_balance.rs" + § "service_impl/src/test/vacation_balance.rs"
    - shifty-backend/CLAUDE.md § "Service-Tier-Konventionen" (BL-Tier-Definition)
  </read_first>

  <files>service_impl/src/vacation_balance.rs (NEW), service_impl/src/lib.rs (modify), service_impl/src/test/vacation_balance.rs (NEW), service_impl/src/test/mod.rs (modify)</files>

  <behavior>
    - Test 1 (`get_returns_entitlement_minus_used_minus_planned`): Self-Path mit MockSalesPersonService.verify_user_is_sales_person → Ok, MockPermissionService.check_permission(HR) → Forbidden. AbsenceService liefert 1 vergangene Vacation-Periode (5 Tage, to_date < today) und 1 zukünftige (10 Tage, from_date > today). MockEmployeeWorkDetailsService.find_by_sales_person_id → 1 EmployeeWorkDetails-Eintrag mit `vacation_days=25` der das angefragte Jahr voll abdeckt (`vacation_days_for_year(year) == 25.0`). MockCarryoverService.get_carryover → `Some(Carryover { vacation: 5, .. })`. Erwartung: `entitled_days==25.0`, `carryover_days==5`, `used_days==5.0`, `planned_days==10.0`, `remaining_days==15.0` (25+5-5-10).
    - Test 2 (`get_with_hr_succeeds`): HR-Path → check_permission(HR) → Ok; verify_user_is_sales_person darf NICHT aufgerufen werden ODER darf fehlschlagen — `or()` filtert das. Methode liefert Aggregat ohne Forbidden.
    - Test 3 (`get_other_sales_person_without_hr_is_forbidden`): check_permission(HR) → Forbidden, verify_user_is_sales_person(other_id) → Forbidden. Erwartung: `Result::Err(ServiceError::Forbidden)` (oder via `test_forbidden`-Helper).
    - Test 4 (`get_team_without_hr_is_forbidden`): check_permission(HR) → Forbidden. Erwartung: ServiceError::Forbidden, KEIN Aufruf an verify_user_is_sales_person (HR-only).
    - Test 5 (`get_team_aggregates_per_paid_sales_person`): check_permission(HR) → Ok, SalesPersonService liefert 2 Personen (paid=true), AbsenceService liefert je Person 1 Periode, EmployeeWorkDetailsService liefert je Person Contract, CarryoverService liefert je Person Carryover. Erwartung: Arc<[VacationBalance]> mit 2 Einträgen, je korrekt aggregiert.
    - Test 6 (`get_with_no_active_contract_returns_zero_entitlement`): Self-Path Ok, EmployeeWorkDetailsService liefert leere Liste. Erwartung: `entitled_days==0.0`, `carryover_days==0` (kein Crash, kein Panic).
    - (Optional Test 7) `get_year_without_carryover_returns_zero_carryover`: CarryoverService liefert None für gesuchtes Jahr. Erwartung: `carryover_days==0`.
  </behavior>

  <action>
    **A. `service_impl/src/vacation_balance.rs` (NEW):**

    1. Imports kopieren von service_impl/src/absence.rs Header (Arc, async_trait, dao::*, service::*, ServiceError, gen_service_impl, time, tokio::join).

    2. Domain-Aliasing: `use service::vacation_balance::{VacationBalance, VacationBalanceService};` und Cross-Service-Imports `use service::absence::AbsenceService; use service::employee_work_details::EmployeeWorkDetailsService; use service::carryover::CarryoverService; use service::sales_person::SalesPersonService; use service::PermissionService; use service::clock::ClockService;`.

    3. **`gen_service_impl!`-Block** mit BL-Tier-Deps (D-04):
       ```rust
       gen_service_impl! {
           struct VacationBalanceServiceImpl: VacationBalanceService = VacationBalanceServiceDeps {
               AbsenceService: AbsenceService<Context = Self::Context, Transaction = Self::Transaction> = absence_service,
               EmployeeWorkDetailsService: EmployeeWorkDetailsService<Context = Self::Context, Transaction = Self::Transaction> = employee_work_details_service,
               CarryoverService: CarryoverService<Context = Self::Context, Transaction = Self::Transaction> = carryover_service,
               SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,
               PermissionService: PermissionService<Context = Self::Context> = permission_service,
               ClockService: ClockService = clock_service,
               TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
           }
       }
       ```
       Falls eine konkrete Trait-Methoden-Signatur in einem dieser Services anders ist als hier erwartet (z. B. `EmployeeWorkDetailsService` heißt im Trait anders), passe nur den Deps-Eintrag an, behalte aber die Tier-Klassifizierung (BL).

    4. **`impl<Deps: VacationBalanceServiceDeps> VacationBalanceService for VacationBalanceServiceImpl<Deps>`** (oder analog dem `gen_service_impl!`-erweiterten Pattern in absence.rs Z. 75+):

       - `async fn get(...) -> Result<VacationBalance, ServiceError>`:
         1. `let tx = self.transaction_dao.use_transaction(tx).await?;`
         2. **Permission HR ∨ self** via `tokio::join!` + `.or()` — Pattern aus absence.rs:110-119.
         3. Lade Vacation-Periodeen via existierende Method-Signatur des AbsenceService (verifiziere Method-Name in service/src/absence.rs — vermutlich `find_by_sales_person(sales_person_id, ctx, tx)` oder `find_all_for_sales_person(...)`); übergib `Authentication::Full` weil Permission bereits gechecked, Tx-Reuse via `Some(tx.clone())`.
         4. Filter `category == AbsenceCategory::Vacation`.
         5. Bestimme `today` via `self.clock_service.date_time_now().await?.date()` (oder analoger Methode des ClockService — verifiziere in service/src/clock.rs).
         6. Berechne `used_days` = sum days über alle Vacation-Periodeen mit `to_date < today` (Day-count: `(to_date - from_date).whole_days() + 1` — falls UI-SPEC Special-Days berücksichtigt: KEINE Spezial-Day-Subtraktion in dieser ersten Iteration; A5-Note in RESEARCH.md). Begrenzt auf Periodeen, die im angefragten Jahr liegen.
         7. Berechne `planned_days` = sum days über Vacation-Periodeen mit `from_date >= today`, ebenfalls Jahr-gebunden.
         8. Lade Verträge (B-4 verified): **`let work_details = self.employee_work_details_service.find_by_sales_person_id(sales_person_id, Authentication::Full, Some(tx.clone())).await?;`** — gibt `Arc<[EmployeeWorkDetails]>` zurück (NICHT `find_for_year` — die Methode existiert nicht).
         9. `entitled_days` (B-4 verified): **`let entitled_days: f32 = work_details.iter().map(|wh| wh.vacation_days_for_year(year)).sum();`** — `vacation_days_for_year(year: u32) -> f32` ist eine Inherent-Method auf `EmployeeWorkDetails` (employee_work_details.rs:156); summiert über alle Verträge, die das Jahr berühren (Method liefert 0.0 für nicht-überlappende Jahre).
         10. Lade Carryover (B-4 verified): **`let carryover_opt = self.carryover_service.get_carryover(sales_person_id, year, Authentication::Full, Some(tx.clone())).await?;`** — Method heißt `get_carryover` (NICHT `find`); liefert `Option<Carryover>`. Dann: `let carryover_days = carryover_opt.map(|c| c.vacation).unwrap_or(0);` — Field heißt `vacation: i32` auf `Carryover` (carryover.rs:14). **Carryover-Year-Semantik:** Plan-Phase nutzt `year` direkt (NICHT `year - 1`); falls die Konvention im Repo `year-1` ist (Snapshot-Vorjahr für Carryover-INTO-year), muss das beim Schreiben aus `service_impl/src/test/carryover.rs` als Authority verifiziert werden — Test-Erwartung anpassen, KEINE Vermutung.
         11. `remaining_days = entitled_days + carryover_days as f32 - (used_days + planned_days)`.
         12. `self.transaction_dao.commit(tx).await?;`
         13. Return `Ok(VacationBalance { sales_person_id, year, entitled_days, carryover_days, used_days, planned_days, remaining_days })`.

       - `async fn get_team(...) -> Result<Arc<[VacationBalance]>, ServiceError>`:
         1. `let tx = self.transaction_dao.use_transaction(tx).await?;`
         2. **Permission HR-only:** `self.permission_service.check_permission(HR_PRIVILEGE, context.clone()).await?;` (analog absence.rs:88-93).
         3. Lade alle bezahlten SalesPerson: `let sps = self.sales_person_service.get_all_paid(Authentication::Full, Some(tx.clone())).await?;` (oder existierende Method-Signatur — verifiziere in `service/src/sales_person.rs`; Method heißt evtl. `get_all`/`find_all_paid` etc.).
         4. Aggregiere für jede Person: extract Logic aus `get` in eine private Helper-Methode `compute_balance(&self, sp_id, year, tx) -> Result<VacationBalance, ServiceError>` und rufe sie sequentiell für jede Person auf (NICHT join!_all — Tx-Reuse-Komplikation). Die Helper-Methode darf KEINE Permission-Checks machen — die sind im Public-Aufruf einmalig erledigt; Helper benutzt `Authentication::Full` für innere Service-Calls.
         5. `self.transaction_dao.commit(tx).await?;`
         6. Return `Ok(balances.into())`.

       Wenn beim Schreiben einzelne Methoden anderer Services nicht 1:1 wie hier vermutet existieren (z. B. `find_for_year` heißt anders), nutze die existierende Method-Signatur und kommentiere die Wahl.

    5. **Modul-Registrierung:** In `service_impl/src/lib.rs` füge `pub mod vacation_balance;` ein (Position: alphabetisch oder analog existierender Sortierung).

    **B. `service_impl/src/test/vacation_balance.rs` (NEW):**

    1. Imports analog `service_impl/src/test/absence.rs:13-38`: MockTransaction, MockTransactionDao, mockall::predicate::*, MockAbsenceService, MockEmployeeWorkDetailsService, MockCarryoverService, MockSalesPersonService, MockPermissionService, MockClockService, ServiceError, time::macros::date.

    2. Test-Helper-Konstanten (analog absence.rs:40-66): `default_sales_person_id`, `default_year` (z. B. 2026), `today` als fixed date.

    3. Implementiere mindestens die 6 Tests aus `<behavior>` (`get_returns_entitlement_minus_used_minus_planned`, `get_with_hr_succeeds`, `get_other_sales_person_without_hr_is_forbidden`, `get_team_without_hr_is_forbidden`, `get_team_aggregates_per_paid_sales_person`, `get_with_no_active_contract_returns_zero_entitlement`).

    4. Jeder Test:
       - `let mut deps = MockVacationBalanceServiceDeps::new();` (oder via gen_service_impl-erzeugte Mock-Helpers; falls macro keine MockDeps generiert, baue manuelles Setup mit `let svc = VacationBalanceServiceImpl { absence_service: Arc::new(...), ... }`)
       - Stub `expect_use_transaction()` auf TransactionDao
       - Stub `expect_check_permission()` mit `with(eq(HR_PRIVILEGE), always())`
       - Stub `expect_verify_user_is_sales_person()` für SalesPersonService
       - Stub Cross-Service-Methoden (find_by_sales_person, find_for_year, find für Carryover, get_all_paid)
       - Call `svc.get(...).await` bzw. `svc.get_team(...).await`
       - Assert auf Result + Field-Werte

    5. **Modul-Registrierung:** In `service_impl/src/test/mod.rs` füge `pub mod vacation_balance;` ein (analog `pub mod absence;`).

    KEINE neuen DAO-Methoden, KEIN neues Migration-File (Phase 8 fügt keine DB-Tables hinzu).
  </action>

  <verify>
    <automated>nix develop --command cargo test -p service_impl vacation_balance 2>&1 | tail -40</automated>
  </verify>

  <acceptance_criteria>
    - `service_impl/src/vacation_balance.rs` existiert
    - `grep -c "gen_service_impl!" service_impl/src/vacation_balance.rs` ergibt 1
    - `grep -c "VacationBalanceServiceImpl" service_impl/src/vacation_balance.rs` ergibt mindestens 1
    - `grep -E "AbsenceService|EmployeeWorkDetailsService|CarryoverService|PermissionService|TransactionDao" service_impl/src/vacation_balance.rs | grep -vc '^//'` ergibt mindestens 5 (alle Cross-Service-Deps registriert)
    - `grep -c "use_transaction" service_impl/src/vacation_balance.rs` ergibt mindestens 1 (Tx-Pattern angewendet)
    - `grep -c "join!" service_impl/src/vacation_balance.rs` ergibt mindestens 1 (HR ∨ self-Pattern in `get`)
    - `grep -c "HR_PRIVILEGE" service_impl/src/vacation_balance.rs` ergibt mindestens 2 (get + get_team)
    - `grep -c "pub mod vacation_balance" service_impl/src/lib.rs` ergibt 1
    - `grep -c "pub mod vacation_balance" service_impl/src/test/mod.rs` ergibt 1
    - `grep -c "#\[tokio::test\]" service_impl/src/test/vacation_balance.rs` ergibt mindestens 6
    - `grep -E "get_returns_entitlement_minus_used_minus_planned|get_with_hr_succeeds|get_other_sales_person_without_hr_is_forbidden|get_team_without_hr_is_forbidden|get_team_aggregates_per_paid_sales_person|get_with_no_active_contract_returns_zero_entitlement" service_impl/src/test/vacation_balance.rs | wc -l` ergibt mindestens 6
    - `nix develop --command cargo test -p service_impl vacation_balance` Exit-Code 0; Test-Anzahl ≥ 6 grün
    - `nix develop --command cargo test --workspace` Exit-Code 0 (keine Regression auf existierende 466+ Tests)
  </acceptance_criteria>

  <done>VacationBalanceServiceImpl ist BL-Tier-konform implementiert mit gen_service_impl!, Permission-Gates HR ∨ self bzw. HR-only enforced, Transaction-Pattern korrekt, mindestens 6 Unit-Tests grün.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: REST-Endpoints + utoipa-Annotations + RestStateDef-Erweiterung + ApiDoc</name>

  <read_first>
    - rest/src/absence.rs (gesamte Datei, 260 LOC — exakte Vorlage für Router + Handler + utoipa-Path + ApiDoc-Block)
    - rest/src/lib.rs (Z. 1-100 für Imports + mod-decl-Block; Z. 270-390 für RestStateDef-Trait; Z. 480-490 für ApiDoc-nest-entry; Z. 560-600 für Router-nest-Block — exakte Edit-Locations für Wiring)
    - rest/src/lib.rs `error_handler` (versteh dass ServiceError::Forbidden→403, EntityNotFound→404 automatisch gemappt wird)
    - service/src/vacation_balance.rs (Trait aus Plan 01)
    - rest-types/src/lib.rs (VacationBalanceTO aus Plan 01)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "rest/src/vacation_balance.rs" und § "rest/src/lib.rs"
  </read_first>

  <files>rest/src/vacation_balance.rs (NEW), rest/src/lib.rs (modify)</files>

  <action>
    **A. `rest/src/vacation_balance.rs` (NEW):**

    1. Imports kopiert von rest/src/absence.rs Header (axum::*, utoipa::*, RestStateDef, error_handler, Body, Response, OpenApi, instrument).

    2. **Router** (analog rest/src/absence.rs:30-41):
       ```rust
       pub fn generate_route<RestState: RestStateDef>() -> Router<RestState> {
           Router::new()
               .route("/team/{year}", get(get_team_vacation_balance::<RestState>))
               .route("/{sales_person_id}/{year}", get(get_vacation_balance::<RestState>))
       }
       ```
       (`/team/{year}` MUSS vor `/{sales_person_id}/{year}` stehen, sonst routet Axum `team` als sales_person_id.)

    3. **Handler `get_vacation_balance`** (HR ∨ self, ServiceError → 200/403/404):
       ```rust
       #[instrument(skip(rest_state))]
       #[utoipa::path(
           get,
           path = "/{sales_person_id}/{year}",
           tags = ["VacationBalance"],
           params(
               ("sales_person_id" = Uuid, Path, description = "Sales person ID"),
               ("year" = u32, Path, description = "Calendar year (e.g. 2026)"),
           ),
           responses(
               (status = 200, description = "Vacation balance for sales person + year", body = VacationBalanceTO),
               (status = 403, description = "Forbidden — not HR and not self"),
               (status = 404, description = "Sales person not found"),
           ),
       )]
       pub async fn get_vacation_balance<RestState: RestStateDef>(
           rest_state: State<RestState>,
           Extension(context): Extension<Context>,
           Path((sales_person_id, year)): Path<(Uuid, u32)>,
       ) -> Response {
           error_handler((async {
               let svc = rest_state.vacation_balance_service();
               let result = svc.get(sales_person_id, year, context.into(), None).await?;
               let to = VacationBalanceTO::from(&result);
               Ok(Response::builder()
                   .status(200)
                   .header("Content-Type", "application/json")
                   .body(Body::new(serde_json::to_string(&to).unwrap()))
                   .unwrap())
           }).await)
       }
       ```

    4. **Handler `get_team_vacation_balance`** (HR-only, ServiceError → 200/403):
       ```rust
       #[instrument(skip(rest_state))]
       #[utoipa::path(
           get,
           path = "/team/{year}",
           tags = ["VacationBalance"],
           params(("year" = u32, Path, description = "Calendar year")),
           responses(
               (status = 200, description = "Team vacation balance (HR aggregate)", body = Vec<VacationBalanceTO>),
               (status = 403, description = "Forbidden — HR-only"),
           ),
       )]
       pub async fn get_team_vacation_balance<RestState: RestStateDef>(
           rest_state: State<RestState>,
           Extension(context): Extension<Context>,
           Path(year): Path<u32>,
       ) -> Response {
           error_handler((async {
               let svc = rest_state.vacation_balance_service();
               let balances = svc.get_team(year, context.into(), None).await?;
               let tos: Vec<VacationBalanceTO> = balances.iter().map(VacationBalanceTO::from).collect();
               Ok(Response::builder()
                   .status(200)
                   .header("Content-Type", "application/json")
                   .body(Body::new(serde_json::to_string(&tos).unwrap()))
                   .unwrap())
           }).await)
       }
       ```

    5. **ApiDoc** (analog rest/src/absence.rs:240-261):
       ```rust
       #[derive(OpenApi)]
       #[openapi(
           paths(get_vacation_balance, get_team_vacation_balance),
           components(schemas(VacationBalanceTO)),
           tags((name = "VacationBalance", description = "Vacation balance aggregate (entitled / carryover / used / planned / remaining)")),
       )]
       pub struct VacationBalanceApiDoc;
       ```

    **B. `rest/src/lib.rs` (modify) — drei Edit-Locations:**

    1. **Mod-decl** (Top des Files, neben `mod absence;`): füge `mod vacation_balance;` ein.

    2. **RestStateDef-Trait-Erweiterung** (innerhalb des `pub trait RestStateDef`-Blocks, analog `type AbsenceService` + `fn absence_service`):
       ```rust
       type VacationBalanceService: service::vacation_balance::VacationBalanceService<Context = Context>
           + Send + Sync + 'static;
       fn vacation_balance_service(&self) -> Arc<Self::VacationBalanceService>;
       ```

    3. **ApiDoc nest-entry** (im OpenAPI-`paths(...)`-Block ca. Z. 485 bei nest-Definitionen): füge ein:
       ```rust
       (path = "/vacation-balance", api = vacation_balance::VacationBalanceApiDoc),
       ```

    4. **Router nest** (im axum `Router::new().nest(...)`-Block ca. Z. 567): füge ein:
       ```rust
       .nest("/vacation-balance", vacation_balance::generate_route())
       ```
       Position: irgendwo zwischen den existierenden nest-Calls — analog zu `/absence-period`.

    KEINE Änderung am `error_handler` — der mappt ServiceError automatisch korrekt.
  </action>

  <verify>
    <automated>nix develop --command cargo check -p rest 2>&1 | tail -20 &amp;&amp; nix develop --command cargo build --bin shifty 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `rest/src/vacation_balance.rs` existiert
    - `grep -c "#\[utoipa::path" rest/src/vacation_balance.rs` ergibt mindestens 2 (zwei Handler, zwei Annotationen)
    - `grep -c "tags = \[\"VacationBalance\"\]" rest/src/vacation_balance.rs` ergibt mindestens 2
    - `grep -E "pub async fn get_vacation_balance|pub async fn get_team_vacation_balance" rest/src/vacation_balance.rs | wc -l` ergibt 2
    - `grep -c "VacationBalanceApiDoc" rest/src/vacation_balance.rs` ergibt mindestens 1
    - `grep -c "pub fn generate_route" rest/src/vacation_balance.rs` ergibt 1
    - `grep -c "rest_state.vacation_balance_service" rest/src/vacation_balance.rs` ergibt mindestens 2
    - `grep -c "mod vacation_balance" rest/src/lib.rs` ergibt mindestens 1
    - `grep -c "type VacationBalanceService" rest/src/lib.rs` ergibt 1
    - `grep -c "fn vacation_balance_service" rest/src/lib.rs` ergibt mindestens 1
    - `grep -c "vacation_balance::VacationBalanceApiDoc" rest/src/lib.rs` ergibt mindestens 1
    - `grep -c "vacation_balance::generate_route" rest/src/lib.rs` ergibt mindestens 1
    - `grep -c ".nest(\"/vacation-balance\"" rest/src/lib.rs` ergibt mindestens 1
    - `nix develop --command cargo check -p rest` Exit-Code 0
  </acceptance_criteria>

  <done>REST-Layer rendert /vacation-balance/{sp}/{year} und /vacation-balance/team/{year} mit utoipa-Annotation; RestStateDef erweitert; ApiDoc registriert; Router-Nest aktiv; cargo check -p rest grün. Permission-Enforcement passiert im Service-Layer (Plan 02 Task 1) — REST-Layer ist dünner Wrapper.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DI-Wiring in shifty_bin/src/main.rs (Deps-Impl + Konstruktor + RestStateImpl-Field)</name>

  <read_first>
    - shifty_bin/src/main.rs Z. 194-355 (Deps-Impl-Block für alle existierenden Services — exakte Vorlage)
    - shifty_bin/src/main.rs Z. 480-510 (CarryoverRebuildService-Konstruktion als BL→ReportingService-Beispiel)
    - shifty_bin/src/main.rs Z. 526-557 (W-5 VERIFIED — `pub struct RestStateImpl` mit allen Service-Feldern; `working_hours_service: Arc<WorkingHoursService>` Z. 540, `absence_service: Arc<AbsenceService>` Z. 541)
    - shifty_bin/src/main.rs Z. 558-635 (W-5 VERIFIED — `impl rest::RestStateDef for RestStateImpl` mit Type-Assoc + Getter; `type WorkingHoursService = WorkingHoursService;` Z. 571, `type AbsenceService = AbsenceService;` Z. 572)
    - shifty_bin/src/main.rs Z. 684-845 (W-5 VERIFIED — Konstruktor-Reihenfolge: `transaction_dao` Z. 684, `permission_service` Z. 707, `clock_service` Z. 711, `uuid_service` Z. 712, `sales_person_service` Z. 726, `working_hours_service` Z. 788, `absence_service` Z. 798, `carryover_service` Z. 843)
    - service_impl/src/vacation_balance.rs (Output von Task 1 — `VacationBalanceServiceImpl<Deps>` und `VacationBalanceServiceDeps`-Trait)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "shifty_bin/src/main.rs"
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pitfall 3" (DI-Reihenfolge)
  </read_first>

  <files>shifty_bin/src/main.rs (modify)</files>

  <action>
    Drei Edits in `shifty_bin/src/main.rs`:

    **1. `VacationBalanceServiceDependencies` Struct + impl** (analog `AbsenceServiceDependencies` Z. 228-255).
    Position: nach allen anderen `*ServiceDependencies` Structs (idealerweise direkt nach `AbsenceServiceDependencies`).

    **W-5 VERIFIED Type-Alias-Namen (aus main.rs):**
    - In main.rs ist `WorkingHoursService` der Type-Alias für `service_impl::employee_work_details::EmployeeWorkDetailsServiceImpl<...>` (Z. 540, 571). Nutze diesen Namen verbatim — KEIN `EmployeeWorkDetailsService`-Alias existiert im main.rs-Scope.
    - `AbsenceService`, `CarryoverService`, `SalesPersonService`, `PermissionService`, `ClockService`, `TransactionDao` sind als Type-Aliases bereits definiert.

    ```rust
    pub struct VacationBalanceServiceDependencies;
    impl service_impl::vacation_balance::VacationBalanceServiceDeps for VacationBalanceServiceDependencies {
        type Context = Context;
        type Transaction = Transaction;
        type AbsenceService = AbsenceService;
        type EmployeeWorkDetailsService = WorkingHoursService;  // W-5: VERIFIED — Type-Alias heißt WorkingHoursService in main.rs:540,571
        type CarryoverService = CarryoverService;
        type SalesPersonService = SalesPersonService;
        type PermissionService = PermissionService;
        type ClockService = ClockService;
        type TransactionDao = TransactionDao;
    }
    type VacationBalanceService = service_impl::vacation_balance::VacationBalanceServiceImpl<VacationBalanceServiceDependencies>;
    ```

    **2. Konstruktor in `main()`-Setup** (W-5 VERIFIED — exakte Variable-Namen + Position):
    Position: NACH `let absence_service = Arc::new(...)` (Z. 798) UND NACH `let carryover_service = Arc::new(...)` (Z. 843). Mindest-Position: nach Z. 843. Variable-Namen aus main.rs verifiziert — alle existieren bereits im Scope:
    - `absence_service` (Z. 798)
    - `working_hours_service` (Z. 788) — Variable für EmployeeWorkDetailsServiceImpl
    - `carryover_service` (Z. 843)
    - `sales_person_service` (Z. 726)
    - `permission_service` (Z. 707)
    - `clock_service` (Z. 711)
    - `transaction_dao` (Z. 684)

    ```rust
    let vacation_balance_service = Arc::new(service_impl::vacation_balance::VacationBalanceServiceImpl::<VacationBalanceServiceDependencies> {
        absence_service: absence_service.clone(),
        employee_work_details_service: working_hours_service.clone(),  // W-5: VERIFIED — Variable-Name ist working_hours_service (NICHT employee_work_details_service)
        carryover_service: carryover_service.clone(),
        sales_person_service: sales_person_service.clone(),
        permission_service: permission_service.clone(),
        clock_service: clock_service.clone(),
        transaction_dao: transaction_dao.clone(),
    });
    ```

    Das Field-Mapping nutzt die im `gen_service_impl!`-Block deklarierten Field-Namen aus Plan 02 Task 1 (also `absence_service`, `employee_work_details_service`, `carryover_service`, `sales_person_service`, `permission_service`, `clock_service`, `transaction_dao`). Auf der LHS sind das die Plan-01/02 deklarierten Field-Namen; auf der RHS sind das die existierenden main.rs-Variable-Namen.

    **3. `RestStateImpl`-Field + Getter** (analog `absence_service: Arc<AbsenceService>` Field):
    a) Im struct-Body (suche nach `struct RestStateImpl {` und finde wo `absence_service: Arc<AbsenceService>` als Field steht): füge ein Field hinzu:
    ```rust
    vacation_balance_service: Arc<VacationBalanceService>,
    ```
    b) Im struct-Konstruktor (suche nach dem `RestStateImpl { absence_service: absence_service.clone(), ... }`-Aufruf): füge ein:
    ```rust
    vacation_balance_service: vacation_balance_service.clone(),
    ```
    c) Im `impl RestStateDef for RestStateImpl`-Block (oder analog impl-Block, suche nach `fn absence_service`): füge das Type-Assoc + Getter hinzu:
    ```rust
    type VacationBalanceService = VacationBalanceService;
    fn vacation_balance_service(&self) -> Arc<Self::VacationBalanceService> {
        self.vacation_balance_service.clone()
    }
    ```

    KEINE Änderung an Migration-Files. KEINE Änderung an Cargo.toml (Cargo deps für service_impl::vacation_balance werden via `service_impl/src/lib.rs::pub mod vacation_balance` automatisch verfügbar).

    **Pitfall-Reminder (Pitfall 3):** Wenn der Konstruktor VOR `absence_service`/`carryover_service`/`working_hours_service` platziert wird → Compiler-Error "use of undeclared local". Halte die Position NACH den drei Deps.
  </action>

  <verify>
    <automated>nix develop --command cargo build --bin shifty 2>&1 | tail -30 &amp;&amp; nix develop --command cargo test --workspace 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "VacationBalanceServiceDependencies" shifty_bin/src/main.rs` ergibt mindestens 3 (struct + impl + Konstruktor-Type-Param)
    - `grep -c "service_impl::vacation_balance::VacationBalanceServiceDeps" shifty_bin/src/main.rs` ergibt 1
    - `grep -c "type VacationBalanceService" shifty_bin/src/main.rs` ergibt mindestens 2 (type alias + RestStateImpl assoc type)
    - `grep -c "vacation_balance_service" shifty_bin/src/main.rs` ergibt mindestens 4 (`let vacation_balance_service = ...`, RestStateImpl-Field, Konstruktor-Field-Init, Getter-Body)
    - `grep -c "fn vacation_balance_service" shifty_bin/src/main.rs` ergibt mindestens 1
    - `nix develop --command cargo build --bin shifty` Exit-Code 0
    - `nix develop --command cargo test --workspace` Exit-Code 0 (Backend Full-Suite grün, inkl. neue Tests aus Task 1)
    - Manueller Smoke-Check (innerhalb dieses Tasks NICHT auszuführen, ist Plan 06): `cargo run` startet Backend, `curl http://localhost:3000/vacation-balance/00000000-0000-0000-0000-000000000000/2026` antwortet 401/403 (nicht 404 für unknown route) — Route ist registriert
  </acceptance_criteria>

  <done>VacationBalanceService ist im DI-Graph eingebunden; cargo build + cargo test --workspace grün; REST-Surface komplett; Plan 03 (OpenAPI-Snapshot) und Plan 04 (Frontend) können konsumieren.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST | Alle eingehenden Requests werden durch Axum's `Extension<Context>` mit Authentication-State gefüttert; Permission-Checks im Service-Layer enforced |
| REST handler → Service | `error_handler` wrapper mappt ServiceError-Varianten auf HTTP-Statuscodes; Forbidden→403 ist die Bypass-Schutzlinie |
| Service → DAO | `Authentication::Full` wird nach Outer-Permission-Check verwendet, um nicht erneut zu prüfen — Inner-Service-Calls vertrauen dem Outer-Gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-01 | Elevation of Privilege | VacationBalanceServiceImpl.get | mitigate | `let (hr, sp) = join!(check_permission(HR_PRIVILEGE), verify_user_is_sales_person(sales_person_id, ...)); hr.or(sp)?;` — wenn beide fehlschlagen, `or()` gibt das letzte Err (Forbidden) zurück. Test 3 (`get_other_sales_person_without_hr_is_forbidden`) verifiziert das. |
| T-8-AUTH-02 | Elevation of Privilege | VacationBalanceServiceImpl.get_team | mitigate | `self.permission_service.check_permission(HR_PRIVILEGE, context).await?;` — Test 4 (`get_team_without_hr_is_forbidden`) verifiziert das. |
| T-8-IDOR-01 | Information Disclosure | GET /vacation-balance/{sales_person_id}/{year} | mitigate | Owner-Resolve via `verify_user_is_sales_person(sales_person_id, ...)` — Backend prüft, ob der Auth-Subject der Owner des sales_person_id ist; bypass nur via HR-Privileg. Test 3 (forbidden für fremde sales_person_id ohne HR) deckt das ab. |
| T-8-XSS-01 | Tampering | VacationBalanceTO Response-Body | accept | DTO besteht ausschließlich aus numerischen Feldern (`f32`, `i32`, `Uuid`, `u32`). Kein User-String wird zurückgegeben. XSS-Surface ist 0. |
| T-8-INJECTION-01 | Injection | Path-Params (sales_person_id, year) | mitigate | Axum Path-Extractor parst `Uuid` und `u32` typsicher; ungültige Inputs liefern 400 Bad Request automatisch. Kein String-Interpolation in SQL — alle Cross-Service-Calls nutzen typsichere Trait-Methoden. |
| T-8-DI-CYCLE | Denial of Service | DI-Konstruktion in main.rs | mitigate | Pitfall 3: Konstruktor-Position NACH AbsenceService + CarryoverService + WorkingHoursService — sonst Compile-Fehler. Service-Tier-Konvention (CLAUDE.md) verbietet Cycles by-design. |
| T-8-OPENAPI-MISSED | Information Disclosure | `#[utoipa::path]` fehlt | mitigate | `cargo check -p rest` schlägt fehl, wenn ApiDoc Path nicht registriert ist; explizit in Acceptance-Criteria geprüft via grep. |
</threat_model>

<verification>
**Layer 1 — Compile + Unit-Tests:**
- `nix develop --command cargo build --bin shifty` grün
- `nix develop --command cargo test -p service_impl vacation_balance` ≥6 Tests grün
- `nix develop --command cargo test --workspace` Full-Suite grün, ≥466 Tests (v1.2-Baseline + ≥6 neue)

**Layer 2 — Integration (manueller Smoke, optional in Task 3 done-Section):**
- `cargo run` startet Backend
- `curl http://localhost:3000/vacation-balance/00000000-0000-0000-0000-000000000000/2026` antwortet 401 oder 403 (nicht 404), beweist dass Route registriert ist

**Layer 3 — OpenAPI:**
- Plan 03 macht den Insta-Snapshot-Refresh; hier nicht.

**Permission-Enforcement (Layer 1.5 — Test-getrieben):**
- Test 3 (`get_other_sales_person_without_hr_is_forbidden`) prüft T-8-AUTH-01 + T-8-IDOR-01.
- Test 4 (`get_team_without_hr_is_forbidden`) prüft T-8-AUTH-02.
</verification>

<success_criteria>
- VacationBalanceServiceImpl ist BL-Tier mit gen_service_impl!, Permission-Gates HR ∨ self bzw. HR-only enforced
- ≥6 Unit-Tests in service_impl/src/test/vacation_balance.rs grün
- REST-Endpoints `GET /vacation-balance/{sales_person_id}/{year}` und `GET /vacation-balance/team/{year}` mit utoipa-Annotation registriert
- ApiDoc-Eintrag `vacation_balance::VacationBalanceApiDoc` in rest/src/lib.rs
- DI-Wiring in shifty_bin/src/main.rs vollständig (Deps-Struct + impl, Konstruktor, RestStateImpl-Field + Getter)
- `cargo build --bin shifty` und `cargo test --workspace` sind beide grün
- Plan 03 (Snapshot) und Plan 04 (Frontend) können das fertige Backend konsumieren
</success_criteria>

<output>
After completion, create `.planning/phases/08-absence-crud-page-foundation/08-02-SUMMARY.md`.

Per `commit_docs: false` und jj-only-VCS: User entscheidet manuell. Empfohlene jj-Commits (logisch getrennt):
- `feat(08): VacationBalanceServiceImpl (BL-tier) + tests`
- `feat(08): /vacation-balance REST endpoints with utoipa`
- `feat(08): wire VacationBalanceService into DI`
</output>
</content>
</invoke>