---
phase: 08-absence-crud-page-foundation
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - shifty-dioxus/src/page/mod.rs
  - shifty-dioxus/src/page/absences.rs
  - shifty-dioxus/src/router.rs
  - shifty-dioxus/src/component/top_bar.rs
  - .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md
autonomous: true
requirements: [FUI-A-01, FUI-A-02, FUI-A-03, FUI-A-04]
requirements_addressed: [FUI-A-01, FUI-A-02, FUI-A-03, FUI-A-04]
tags: [shifty-dioxus, absence-page, modal, routing, top-bar, wasm-build-gate]
must_haves:
  truths:
    - "Voller Mockup-Umfang aus `absences.jsx` ist in `src/page/absences.rs` umgesetzt (AbsencePage + AbsenceModal + WarningList + CategoryBadge + StatusPill + VacationEntitlementCard + VacationPerPersonList) — D-01 — FUI-A-01..04"
    - "AbsenceModal nutzt Center-Variante aus `component/dialog.rs`, Form-Atoms aus `component/form/inputs.rs` (TextInput/SelectInput/TextareaInput) + `field.rs`, Buttons aus `component/atoms/btn.rs` (Primary/Ghost/Danger) — D-14"
    - "Route `/absences` ist via Top-Bar-Menü erreichbar für ALLE eingeloggten User (D-10) — FUI-A-01"
    - "AbsencePage rendert HR-Variante (Liste über alle Mitarbeiter + VacationPerPersonList) wenn `auth.has_privilege(\"hr\")` — D-09 — FUI-A-02"
    - "AbsencePage rendert Employee-Variante (eigene Liste + VacationEntitlementCard Self) wenn nicht HR — D-09 — FUI-A-02"
    - "AbsenceModal bietet Range-Picker (zwei `<input type=\"date\">` mit Cross-Field-Validation `to >= from`, D-05), Kategorie-Dropdown (Vacation/SickLeave/UnpaidLeave), Description-Textarea — FUI-A-03"
    - "AbsenceModal rendert Self-Overlap-Banner inline (D-11) bei `ShiftyError::Validation` — bad-soft-Background — FUI-A-03"
    - "AbsenceModal rendert Version-Conflict-Banner mit Reload-Btn (D-08) bei `ShiftyError::Conflict` — warn-soft-Background"
    - "AbsenceModal rendert WarningList nach erfolgreichem POST/PUT mit Acknowledge-Btn vor Modal-Close (D-12) — FUI-A-04"
    - "Delete im Edit-Modus öffnet zweiten Center-Dialog (Confirm) mit `Btn::Danger` (D-07, NICHT window.confirm)"
    - "Form sendet `body.id = Uuid::nil()` und `body.version = Uuid::nil()` bei Create (Pitfall 9 / Anti-Pattern)"
    - "Status-Berechnung Aktiv/Geplant/Beendet ist client-side reine Pure-Function `compute_status(from, to, today)` — D-06, Pitfall 8"
    - "Tailwind-Kategorie-Klassen sind statische match-Arme (Pitfall 5) — `text-good bg-good-soft`, `text-warn bg-warn-soft`, `text-ink-muted bg-surface-2`"
    - "AUTH `loading_done`-Check vor Render des HR/Employee-Branchs (Pitfall 4) — verhindert Flackern + Doppel-Fetch"
    - "Description wird via Dioxus-rsx-Interpolation (auto-escape) gerendert, NICHT `dangerous_inner_html` (T-8-XSS-01)"
    - "WASM-Build (cargo build --target wasm32-unknown-unknown) ist am Ende grün — Final-Compile-Gate"
    - "Mindestens 11 Component-Snapshot-/Pure-Function-Tests in absences.rs (3 CategoryBadge + 3 StatusPill + 3 compute_status + 2 AbsenceFilterBar) — Pattern dialog.rs:461 (B-2 Wave-0-Closure)"
    - "VALIDATION.md frontmatter ist auf nyquist_compliant: true UND wave_0_complete: true geflippt — B-2 closure"
  artifacts:
    - path: shifty-dioxus/src/page/absences.rs
      provides: "AbsencesPage + AbsenceModal + WarningList + CategoryBadge + StatusPill + VacationEntitlementCard + VacationPerPersonList + AbsenceList + AbsenceFilterBar + StatsGrid + DeleteConfirmDialog + #[cfg(test)] mod tests (11 Snapshot/Pure-Function-Tests)"
      min_lines: 600
      contains: "pub fn AbsencesPage"
    - path: shifty-dioxus/src/router.rs
      provides: "Route::Absences {} variant + pub use AbsencesPage"
      contains: "Absences"
    - path: shifty-dioxus/src/component/top_bar.rs
      provides: "NavTarget::Absences + NavVisibility.absences (logged_in) + nav_items push + is_active_for arm"
      contains: "absences"
  key_links:
    - from: shifty-dioxus/src/router.rs
      to: shifty-dioxus/src/page/absences.rs::AbsencesPage
      via: "Route::Absences {} → AbsencesPage component"
      pattern: "Route::Absences"
    - from: shifty-dioxus/src/component/top_bar.rs
      to: Route::Absences
      via: "nav_items push (Key::AbsenceMenuLabel)"
      pattern: "AbsenceMenuLabel"
    - from: shifty-dioxus/src/page/absences.rs::AbsenceModal
      to: shifty-dioxus/src/service/absence.rs::AbsenceAction::Create
      via: "absence_service.send(AbsenceAction::Create(body, on_done))"
      pattern: "AbsenceAction::Create"
    - from: shifty-dioxus/src/page/absences.rs::VacationEntitlementCard
      to: shifty-dioxus/src/service/vacation_balance.rs::VACATION_BALANCE_STORE
      via: "VACATION_BALANCE_STORE.read()"
      pattern: "VACATION_BALANCE_STORE"
---

<objective>
Wave-5-UI-Komposition: Realisiere die `AbsencesPage` mit allen Inline-Komponenten (Modal, WarningList, CategoryBadge, StatusPill, VacationEntitlementCard, VacationPerPersonList, AbsenceList, AbsenceFilterBar, StatsGrid, DeleteConfirmDialog, VersionConflictBanner, SelfOverlapBanner). Registriere Route + TopBar-Eintrag. Stelle den finalen WASM-Build sicher.

Purpose: Sichtbare End-User-Funktionalität — die Phase-Goal-Erfüllung.

Output:
- `shifty-dioxus/src/page/absences.rs` (gesamte Page mit allen 8+ Inline-Components)
- `shifty-dioxus/src/page/mod.rs` (Modul-Eintrag)
- `shifty-dioxus/src/router.rs` (Route::Absences)
- `shifty-dioxus/src/component/top_bar.rs` (NavTarget + nav_items)
- WASM-Build grün
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

## Project Constraints (verbindlich)

- **VCS:** jj-only. `jj describe -m "feat(08): absence page + modal + routing + top-bar"` empfohlen — User entscheidet.
- **NixOS:** `nix develop` für WASM-Toolchain (Pitfall 9).
- **i18n-Drei-Locales:** Plan 04 hat alle Keys angelegt — Plan 05 nutzt sie nur. Falls ein Key fehlt → in Plan 04 ergänzen, NICHT hier hard-coden.
- **Tailwind-Klassen statisch:** Pitfall 5 — Kategorie-Farben via `match`-Arme, KEIN `format!`-String.
- **AUTH `loading_done`:** Pitfall 4 — Page rendert "Loading..." bis `loading_done == true`.
- **Auto-Escape:** Description als `{value}`-Interpolation, NICHT `dangerous_inner_html` (T-8-XSS-01).
- **WASM-Build-Gate:** Final `cargo build --target wasm32-unknown-unknown` MUSS grün sein.

## D-Mapping

- D-01: Voller Mockup-Umfang (AbsencePage + AbsenceModal + WarningList + CategoryBadge + StatusPill + VacationEntitlementCard + VacationPerPersonList) — alle hier.
- D-05: Range-Picker = 2 native `<input type="date">` mit Cross-Field-Validation.
- D-06: HR-Filter mit Person + Kategorie + Status; Status client-side berechnet.
- D-07: Delete-Confirm via Center-Dialog (Width 360), NICHT `window.confirm`.
- D-08: 409 → Modal-lokaler Banner mit Reload-Btn (warn-Color).
- D-09: HR/Employee-Sicht aus `auth.has_privilege("hr")`.
- D-10: Menü-Eintrag für ALLE eingeloggten User.
- D-11: 422 → Inline-Banner unter Range-Feldern (bad-Color).
- D-12: Forward-Warnings nach erfolgreichem POST/PUT als nicht-blockierende Liste vor Modal-Close mit Acknowledge-Btn.
- D-14: Modal nutzt `component/dialog.rs`, Form-Atoms aus `component/form/inputs.rs` + `field.rs`, Buttons aus `component/atoms/btn.rs`.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-absence-crud-page-foundation/08-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md
@.planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md
@.planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md
@.planning/phases/08-absence-crud-page-foundation/08-04-PLAN.md
@CLAUDE.md
@CLAUDE.local.md
@shifty-dioxus/CLAUDE.md

<interfaces>
<!-- Aus Plan 04: -->
<!-- crate::api::{list_absence_periods, get_absence_period, create_absence_period, update_absence_period, delete_absence_period, list_absence_periods_by_sales_person, get_vacation_balance, get_team_vacation_balance} -->
<!-- crate::loader::{load_absence_periods_all, load_absence_periods_by_sales_person, load_vacation_balance, load_team_vacation} -->
<!-- crate::service::absence::{ABSENCE_STORE, ABSENCE_REFRESH, AbsenceAction, bump_absence_refresh, absence_service} -->
<!-- crate::service::vacation_balance::{VACATION_BALANCE_STORE, VACATION_TEAM_STORE, VacationBalanceAction, vacation_balance_service} -->
<!-- crate::state::absence_period::{AbsencePeriod, AbsenceCategory} -->
<!-- crate::state::vacation_balance::VacationBalance -->
<!-- crate::error::ShiftyError::{Validation, Conflict, Reqwest, ...} -->
<!-- crate::i18n::Key::{AbsencePageTitle, AbsenceCategoryVacation, ...} (60 Keys) -->

From shifty-dioxus/src/component/dialog.rs (existing — Dialog API):
```rust
Dialog {
    variant: DialogVariant::Center,
    width: 520,  // oder 360 für Confirm-Variante
    open: bool,
    on_close: EventHandler<()>,
    title: ImStr,
    subtitle: Option<ImStr>,
    children: Element,
}
```

From shifty-dioxus/src/component/form/inputs.rs (existing):
```rust
TextInput { value: ImStr, on_change: EventHandler<String>, input_type: ImStr, disabled: bool }
SelectInput { children: Element, on_change: EventHandler<String> }
TextareaInput { value: ImStr, on_change: EventHandler<String>, rows: u32 }
```

From shifty-dioxus/src/component/form/field.rs (existing):
```rust
Field {
    label: ImStr,
    span: Option<u8>,           // grid-span
    hint: Option<ImStr>,
    error: Option<ImStr>,        // bad-color border + below-text
    children: Element,
}
```

From shifty-dioxus/src/component/atoms/btn.rs (existing):
```rust
Btn {
    variant: BtnVariant,         // Primary | Secondary | Ghost | Danger
    on_click: EventHandler<()>,
    disabled: bool,
    children: Element,
}
```

From shifty-dioxus/src/component/extra_hours_modal.rs (existing — Form-Pattern, 597 LOC):
```rust
let mut category = use_signal(|| init_category.clone());
let mut amount = use_signal(|| init_amount);
// re-seed on edit-mode-switch:
let editing_key = props.editing.as_ref().map(|e| e.id);
let mut last_editing_key = use_signal(|| editing_key);
if *last_editing_key.peek() != editing_key {
    last_editing_key.set(editing_key);
    category.set(init_category.clone());
    /* ... */
}

let employee_service = try_consume_context::<Coroutine<EmployeeAction>>();
```

From shifty-dioxus/src/component/top_bar.rs (existing — D-10 Visibility):
```rust
pub(crate) struct NavVisibility {
    pub shiftplan: bool, pub my_shifts: bool, pub my_time: bool, pub year_overview: bool,
    pub employees: bool, pub billing_periods: bool, pub user_management: bool, pub templates: bool,
}
pub(crate) fn nav_visibility(auth_info: Option<&AuthInfo>, is_paid: bool) -> NavVisibility {
    let logged_in = auth_info.is_some();
    let has = |p: &str| auth_info.map(|a| a.has_privilege(p)).unwrap_or(false);
    NavVisibility {
        shiftplan: has("sales") || has("shiftplanner"),
        // ...
    }
}
pub(crate) enum NavTarget { Shiftplan, MyShifts, /* ... */ Templates }
```

From shifty-dioxus/src/router.rs (existing):
```rust
#[derive(Clone, Routable, Debug, PartialEq)]
pub enum Route {
    #[route("/")]
    Home {},
    #[route("/employees/")]
    Employees {},
    // ...
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Routing + TopBar-Wiring (Page-Stub)</name>

  <read_first>
    - shifty-dioxus/src/router.rs (gesamte Datei, 51 LOC — Route-Variants Z. 19-51, pub use-Block Z. 1-17)
    - shifty-dioxus/src/component/top_bar.rs (Z. 1-200 + Z. 300-400 — NavVisibility, nav_visibility, NavTarget, is_active_for, nav_items, Authentication-Reads)
    - shifty-dioxus/src/page/mod.rs (existing pub mod-Liste)
    - shifty-dioxus/src/state/auth_info.rs (`has_privilege`-Pattern)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "shifty-dioxus/src/router.rs" + § "shifty-dioxus/src/component/top_bar.rs"
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Code Example 5" + "Code Example 6"
  </read_first>

  <files>
    shifty-dioxus/src/page/absences.rs (NEW — Stub für Compile-Pfad),
    shifty-dioxus/src/page/mod.rs (modify),
    shifty-dioxus/src/router.rs (modify),
    shifty-dioxus/src/component/top_bar.rs (modify)
  </files>

  <action>
    **A. Page-Stub anlegen** in `shifty-dioxus/src/page/absences.rs` (Vollausbau in Task 2 — hier nur Compile-Stub):
    ```rust
    use dioxus::prelude::*;
    use crate::component::TopBar;

    #[component]
    pub fn AbsencesPage() -> Element {
        rsx! {
            TopBar {}
            div { class: "p-md", "Absences page (Plan 05 Task 2 will fill this in)" }
        }
    }
    ```

    **B. `shifty-dioxus/src/page/mod.rs` (modify):** Füge `pub mod absences;` und `pub use absences::AbsencesPage;` ein.

    **C. `shifty-dioxus/src/router.rs` (modify):**

    1. Im pub use-Block (Z. 1-17): füge `pub use crate::page::AbsencesPage;` ein.

    2. Im `Route`-enum (Z. 19-51): füge nach einem passenden bestehenden Variant ein:
       ```rust
       #[route("/absences/")]
       Absences {},
       ```
       (Trailing-Slash-Konvention der anderen Routes folgen.)

    **D. `shifty-dioxus/src/component/top_bar.rs` (modify) — fünf Edits (analog RESEARCH.md Code Example 5 + PATTERNS.md):**

    1. **`NavVisibility`-Struct** (Z. 21-30): füge das Feld `pub absences: bool,` ein (per D-10 für alle Eingeloggten).

    2. **`nav_visibility`-Builder** (Z. 32-45): setze
       ```rust
       absences: logged_in,  // D-10
       ```
       Das Variable `logged_in` wird im Builder bereits berechnet (`let logged_in = auth_info.is_some();`). Falls nicht — füge die Berechnung hinzu.

    3. **`NavTarget`-Enum** (Z. 47-57): füge `Absences,` ein.

    4. **`is_active_for` match-Arm** (Z. 59-82): füge ein:
       ```rust
       NavTarget::Absences => matches!(route, Route::Absences {}),
       ```

    5. **`nav_items` push** (Z. 312-371): füge an passender Position (z. B. nach `my_time`/`year_overview`-Block, vor `employees`) ein:
       ```rust
       if visibility.absences {
           items.push((
               NavTarget::Absences,
               Route::Absences {},
               i18n.t(Key::AbsenceMenuLabel).to_string(),
           ));
       }
       ```

    Verifiziere danach, dass `partition_nav_items` (Z. 386 in top_bar.rs) `Absences` NICHT in `is_admin_target` filtert (Top-Level-Position, nicht Admin-Dropdown). Per Default neue NavTargets sind nicht-admin — falls die Funktion eine Whitelist hat, prüfe sie; falls Blacklist, prüfe gegen `Absences`.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo check 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pub mod absences" shifty-dioxus/src/page/mod.rs` ergibt 1
    - `grep -c "pub use absences::AbsencesPage" shifty-dioxus/src/page/mod.rs` ergibt 1
    - `grep -c "pub use crate::page::AbsencesPage" shifty-dioxus/src/router.rs` ergibt 1
    - `grep -c "Absences {}" shifty-dioxus/src/router.rs` ergibt mindestens 1
    - `grep -c "#\[route(\"/absences" shifty-dioxus/src/router.rs` ergibt 1
    - `grep -c "absences: bool" shifty-dioxus/src/component/top_bar.rs` ergibt 1
    - `grep -c "NavTarget::Absences" shifty-dioxus/src/component/top_bar.rs` ergibt mindestens 2 (Variant + is_active_for + nav_items)
    - `grep -c "AbsenceMenuLabel" shifty-dioxus/src/component/top_bar.rs` ergibt mindestens 1
    - `grep -c "absences: logged_in" shifty-dioxus/src/component/top_bar.rs` ergibt mindestens 1
    - `cd shifty-dioxus && nix develop --command cargo check` Exit-Code 0
  </acceptance_criteria>

  <done>Routing + TopBar-Wiring fertig; Stub-Page kompiliert; `dx serve` würde unter `/absences` einen Stub-Render zeigen. Task 2 baut die Page voll aus.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AbsencesPage + AbsenceModal + alle Inline-Components (vollständige UI-Komposition)</name>

  <read_first>
    - shifty-dioxus/src/component/extra_hours_modal.rs (gesamte Datei, 597 LOC — exakte Form-Pattern-Vorlage: Coroutine-Konsum, use_signal-State, re-seed-Pattern, try_consume_context, Cross-Field-Validation, Form-Atoms-Composition)
    - shifty-dioxus/src/page/employee_details.rs (gesamte Datei, 213 LOC — Auth-Gate + Coroutine + Inner-Modal-State-Pattern)
    - shifty-dioxus/src/component/dialog.rs (Z. 1-150 für Dialog-API + Center-Variante Width-Param)
    - shifty-dioxus/src/component/form/inputs.rs (gesamte Datei — TextInput, SelectInput, TextareaInput Props)
    - shifty-dioxus/src/component/form/field.rs (gesamte Datei — Field-Props inkl. error-Slot)
    - shifty-dioxus/src/component/atoms/btn.rs (gesamte Datei — Btn-Variants)
    - shifty-dioxus/src/component/atoms/person_chip.rs (PersonChip-Props falls für HR-Liste verwendet)
    - shifty-dioxus/src/state/auth_info.rs (has_privilege-Pattern)
    - shifty-dioxus/src/auth.rs + shifty-dioxus/src/service/auth.rs (AUTH-Store mit `loading_done`)
    - shifty-dioxus/src/api.rs (`get_current_sales_person`-Funktion für Employee-Variant — RESEARCH.md OQ4)
    - .planning/phases/08-absence-crud-page-foundation/08-UI-SPEC.md (KOMPLETT lesen — Component-Inventory, Layout-Contract, Interaction-Contract, Color-Token-Mapping, Spacing-Exceptions; ist die Design-Authority)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Code Example 1" + "Pattern 1"; § "Pitfall 4" (AUTH loading_done); § "Pitfall 5" (statische Tailwind-match-Arme); § "Pitfall 7" (Forward-Warnings-Flow); § "Pitfall 8" (Status-Pure-Function)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "shifty-dioxus/src/page/absences.rs" (Auth + Coroutine + Form-Atoms + Tailwind-Match-Arme)
    - shifty-dioxus/shifty-design/project/absences.jsx (visual reference — NICHT 1:1 portieren wegen D-09/D-07; UI-SPEC ist authoritativ)
  </read_first>

  <files>shifty-dioxus/src/page/absences.rs (rewrite — Vollausbau)</files>

  <action>
    **W-2 fix:** Implementation in 5 nummerierten Sub-Steps (jeweils klare Abgrenzung). Erstelle die vollständige `AbsencesPage` mit allen Inline-Components.

    ---

    ### Sub-Step 1: AbsenceModal Foundation (Form + Cross-Field-Validation + 422/409-Banner)

    Liefert: AbsenceModal-Component (Form mit use_signal-State, Re-seed-Pattern, Cross-Field-Validation `to >= from`, SelfOverlapBanner für 422 inline, VersionConflictBanner für 409 mit Reload-Btn). Der Modal-Submit-Side-Channel (EventHandler<Result<…>>) ist Teil dieses Sub-Steps.

    ### Sub-Step 2: WarningList + CategoryBadge + StatusPill

    Liefert: drei Helper-Components (WarningList für D-12 Forward-Warnings inkl. Acknowledge-Btn-Flow; CategoryBadge mit STATISCHEN Tailwind-match-Armen Pitfall 5; StatusPill mit STATISCHEN Tailwind-match-Armen).

    ### Sub-Step 3: VacationEntitlementCard + VacationPerPersonList

    Liefert: VacationEntitlementCard mit Self/HR-Variant-Switch, fünf Stat-Boxen (Vertrag/Übertrag/Genommen/Beantragt/Verbleibend); VacationPerPersonList als HR-only-Section mit Sortierung + Show-All-Toggle.

    ### Sub-Step 4: AbsenceList + AbsenceFilterBar + StatsGrid + AbsenceStatus + compute_status

    Liefert: Pure-Function `compute_status(from, to, today)` (Pitfall 8); AbsenceList Container mit 5-Spalten-Grid + Empty-State-Variants; AbsenceFilterBar mit HR/Employee-Variant (Person-Dropdown HR-only); StatsGrid mit 3 Cards (Krankheit, Unbezahlt, Aktive).

    ### Sub-Step 5: DeleteConfirmDialog + AbsencesPage-Komposition + WASM-Build-Gate

    Liefert: DeleteConfirmDialog (Center, Width 360, NICHT window.confirm — D-07); AbsencesPage als Top-Level-Component mit Auth-Gate (Pitfall 4 `loading_done`-Check), Coroutine-Konsum, use_effect für Refresh-Token, Modal-State-Management. **Final-Compile-Gate:** WASM-Build (`cargo build --target wasm32-unknown-unknown`) MUSS am Ende dieses Sub-Steps grün sein.

    ---

    Im Folgenden die DETAILLIERTE Spezifikation pro Component (Sub-Step-Mapping in Klammern):

    **1. AbsenceStatus enum + compute_status (Sub-Step 4 — Pitfall 8 — pure function):**
    ```rust
    #[derive(Clone, Copy, Debug, PartialEq, Eq)]
    pub enum AbsenceStatus { Active, Planned, Finished }

    pub fn compute_status(from: time::Date, to: time::Date, today: time::Date) -> AbsenceStatus {
        if to < today { AbsenceStatus::Finished }
        else if from > today { AbsenceStatus::Planned }
        else { AbsenceStatus::Active }
    }

    #[cfg(target_arch = "wasm32")]
    fn current_date_for_init() -> time::Date {
        // Nutze existing js-Helper, analog extra_hours_modal.rs:50-58
        crate::js::current_date()
    }

    #[cfg(not(target_arch = "wasm32"))]
    fn current_date_for_init() -> time::Date {
        // Fixed date for SSR-tests
        time::macros::date!(2026-05-08)
    }
    ```

    **2. AbsencesPage Component** mit Auth-Gate + Coroutine-Konsum (analog RESEARCH.md Code Example 1):
    ```rust
    #[component]
    pub fn AbsencesPage() -> Element {
        let auth = AUTH.read().clone();
        if !auth.loading_done {
            return rsx! { TopBar {} div { class: "p-md text-ink-muted", "Loading..." } };
        }
        let is_hr = auth.auth_info.as_ref().map(|a| a.has_privilege("hr")).unwrap_or(false);

        // Self-User sales_person_id (für Employee-Variant) — via existing API/State; Plan-Phase OQ4
        let current_sp_id = use_signal(|| None::<Uuid>);
        // bei Mount: api::get_current_sales_person → set current_sp_id

        let absence_service = use_coroutine_handle::<AbsenceAction>();
        let vacation_service = use_coroutine_handle::<VacationBalanceAction>();
        let i18n = I18N.read().clone();
        let absences = ABSENCE_STORE.read().clone();
        let vacation_self = VACATION_BALANCE_STORE.read().clone();
        let vacation_team = VACATION_TEAM_STORE.read().clone();
        let refresh_token = ABSENCE_REFRESH.read();

        // Effect: bei Mount + bei refresh_token-Änderung → Liste laden
        use_effect(move || {
            let _ = refresh_token;
            if is_hr {
                absence_service.send(AbsenceAction::LoadAll);
                vacation_service.send(VacationBalanceAction::LoadTeam(current_year()));
            } else if let Some(sp_id) = *current_sp_id.read() {
                absence_service.send(AbsenceAction::LoadForSalesPerson(sp_id));
                vacation_service.send(VacationBalanceAction::LoadSelf(sp_id, current_year()));
            }
        });

        // Modal-State (page-lokal):
        let modal_open = use_signal(|| false);
        let modal_mode = use_signal(|| ModalMode::Create);  // Create | Edit(AbsencePeriod)
        let delete_confirm_open = use_signal(|| false);

        // Filter-State (HR-only Sichtbarkeit, aber State immer angelegt):
        let category_filter = use_signal(|| None::<AbsenceCategory>);
        let person_filter = use_signal(|| None::<Uuid>);
        let status_filter = use_signal(|| None::<AbsenceStatus>);
        let show_past = use_signal(|| true);

        // Derived: filtered list + counter
        // ...

        rsx! {
            TopBar {}
            ErrorView {}
            div { class: "p-md flex flex-col gap-3",
                // Header (Title + Subtitle + "Neue Abwesenheit"-Btn)
                AbsencePageHeader { is_hr, on_new: move |_| { /* open modal in Create mode */ } }
                // VacationEntitlementCard (HR oder Self)
                VacationEntitlementCard { is_hr, vacation_self: vacation_self.clone(), vacation_team: vacation_team.clone() }
                // StatsGrid (3 Cards)
                StatsGrid { absences: absences.clone() }
                // FilterBar (HR-only zeigt Person-Dropdown)
                AbsenceFilterBar { is_hr, /* ... */ }
                // Liste oder Empty-State
                AbsenceList { rows: filtered_absences, is_hr, on_row_click: move |a| { /* open modal Edit */ } }
            }

            if *modal_open.read() {
                AbsenceModal {
                    open: *modal_open.read(),
                    mode: modal_mode.read().clone(),
                    is_hr,
                    sales_persons: SALES_PERSONS.read().clone(),
                    current_sp_id: *current_sp_id.read(),
                    on_close: move |_| modal_open.set(false),
                    on_delete_request: move |_| delete_confirm_open.set(true),
                }
            }

            if *delete_confirm_open.read() {
                DeleteConfirmDialog {
                    open: *delete_confirm_open.read(),
                    on_close: move |_| delete_confirm_open.set(false),
                    on_confirm: move |id: Uuid| {
                        absence_service.send(AbsenceAction::Delete(id));
                        delete_confirm_open.set(false);
                        modal_open.set(false);
                    },
                    target: /* aktueller Edit-AbsencePeriod */,
                }
            }
        }
    }
    ```

    **3. AbsenceModal** (Center-Dialog Width 520):
    - Imports + use_signal-Form-State (category, from_date, to_date, description, employee_id).
    - Re-seed-Pattern bei Edit-Mode-Switch (analog extra_hours_modal.rs:140-148, vergleichen `last_editing_key` peek vs current).
    - Cross-Field-Validation: `to >= from` — Field-Error am `Bis`-Feld via `error: Option<ImStr>`-Slot.
    - Disabled Save-Btn falls Validation fehlschlägt oder Date leer.
    - Body-Layout per UI-SPEC § "AbsenceModal Layout": Grid `grid-cols-2 gap-3`, span-2 für Banner/Description/Vorschau/WarningList.
    - **VersionConflictBanner** (D-08): Conditional render bei `conflict_state == true` — Border-Left `border-warn`, Background `bg-warn-soft`. Reload-Btn per `Btn::Ghost` triggert `absence_service.send(AbsenceAction::LoadAll/LoadForSalesPerson)` (re-fetch der Edit-AbsencePeriod).
    - **SelfOverlapBanner** (D-11): Conditional render bei `validation_state == true` — Border-Left `border-bad`, Background `bg-bad-soft`. KEIN Reload-Btn.
    - **WarningList** (D-12): Conditional render NACH erfolgreichem POST/PUT mit `warnings.len() > 0`. Form-Felder DISABLED (`opacity-60`). Submit-Btn-Beschriftung wechselt zu `Key::AbsenceWarningAcknowledgeBtn`. Click → Modal-Close (KEIN zweiter API-Call, Daten sind schon persistiert).
    - **Submit-Logik:**
      ```rust
      let on_submit = move |_| {
          let body = AbsencePeriodTO {
              id: if let ModalMode::Edit(a) = mode.read() { a.id } else { Uuid::nil() },  // Pitfall 9
              version: if let ModalMode::Edit(a) = mode.read() { a.version } else { Uuid::nil() },
              sales_person_id: *employee_id.read(),
              category: (&*category.read()).into(),
              from_date: *from_date.read(),
              to_date: *to_date.read(),
              description: description.read().clone().into(),
              created: None,
              deleted: None,
          };
          let on_done = EventHandler::new(move |result: Result<AbsencePeriodCreateResultTO, ShiftyError>| {
              match result {
                  Ok(create_result) => {
                      if create_result.warnings.is_empty() {
                          modal_open.set(false);
                      } else {
                          warnings_state.set(create_result.warnings);
                          // Form-disabled, Submit-Btn → "Verstanden"
                      }
                  }
                  Err(ShiftyError::Validation(text)) => {
                      validation_state.set(Some(parse_self_overlap_payload(&text)));
                  }
                  Err(ShiftyError::Conflict(_)) => {
                      conflict_state.set(true);
                  }
                  Err(other) => {
                      // ERROR_STORE handled das (siehe service/absence.rs)
                  }
              }
          });
          let action = match *mode.read() {
              ModalMode::Create => AbsenceAction::Create(body, on_done),
              ModalMode::Edit(_) => AbsenceAction::Update(body, on_done),
          };
          absence_service.send(action);
      };
      ```
    - **Footer:** links Delete-Btn (nur Edit-Mode, `BtnVariant::Danger`, on_click → `on_delete_request`), Spacer, rechts Cancel-Btn (`BtnVariant::Ghost`) + Save-Btn (`BtnVariant::Primary`).

    **4. WarningList** (Helper-Component):
    - Tags: linker `border-l-[3px] border-warn`, Background `bg-warn-soft`, `rounded-md`, `p-3` (default) oder `p-2.5` (dense — Spacing-Exception).
    - Header `text-micro text-warn font-semibold` mit Singular/Plural-Logik via `Key::AbsenceWarningHeaderSingular` vs `Key::AbsenceWarningHeaderPlural`.
    - Body `text-body text-ink` mit `pl-4`.
    - Pro Warning: switch über `WarningTO`-Enum (Vacation-Booking-Overlap → `Key::AbsenceWarningOverlapsBooking`, Manual-Unavailable → `Key::AbsenceWarningOverlapsManual`).

    **5. CategoryBadge** (Helper-Component, Pitfall 5 — STATISCHE Tailwind-Klassen):
    ```rust
    let (text_class, bg_class) = match category {
        AbsenceCategory::Vacation    => ("text-good",      "bg-good-soft"),
        AbsenceCategory::SickLeave   => ("text-warn",      "bg-warn-soft"),
        AbsenceCategory::UnpaidLeave => ("text-ink-muted", "bg-surface-2"),
    };
    rsx! {
        span { class: "inline-flex items-center gap-1 rounded-full px-2 py-0.5 text-small font-semibold {text_class} {bg_class}",
            // 7×7px Dot
            span { class: "w-1.5 h-1.5 rounded-full bg-current" }
            "{i18n.t(category_key)}"
        }
    }
    ```

    **6. StatusPill** (Helper-Component, ähnlich CategoryBadge mit eigenen Token-Mappings):
    ```rust
    let (text_class, bg_class) = match status {
        AbsenceStatus::Active   => ("text-accent",   "bg-accent-soft"),
        AbsenceStatus::Planned  => ("text-ink-soft", "bg-surface-2"),
        AbsenceStatus::Finished => ("text-ink-muted","bg-surface-alt"),
    };
    ```

    **7. VacationEntitlementCard** (Helper-Component):
    - Self-Variante: 2-Spalten-Grid `[180px-240px] [1fr]`, Hero-Spalte `bg-good-soft` mit `text-display font-mono text-good {remaining}/{entitled}`. Sublabel `Key::VacationDaysRemaining`.
    - Team-Variante: 1-Spalten-Layout, Resturlaub `text-h1 font-mono text-good`. Sublabel `Key::VacationCardTeamSubtitle`.
    - Per UI-SPEC § "VacationEntitlementCard": Stat-Boxen Vertrag/Übertrag/Genommen/Beantragt/Verbleibend mit `Key::VacationStatContract`/`VacationStatCarryover`/`VacationStatUsed`/`VacationStatPending`/`VacationStatRemaining`.
    - HR-Variante: nach den Stat-Boxen die `VacationPerPersonList`-Section.

    **8. VacationPerPersonList** (Helper-Component, HR-only):
    - Sortiert nach `remaining_days` aufsteigend.
    - Initial-Limit 4, Toggle "Alle ({count})" / "Weniger" via `Key::VacationPerPersonShowAll`/`VacationPerPersonShowLess`.
    - Grid `grid-cols-[repeat(auto-fill,minmax(220px,1fr))] gap-2`.
    - Pro Person: Avatar 22×22 `rounded-full`, Name `text-body font-semibold truncate`, Resturlaub `font-mono`, Mini-Progress-Bar.
    - Low-Indicator (`remaining ≤ 3`): `text-warn`/`bg-warn`.

    **9. AbsenceList** (Container-Component):
    - `bg-surface border border-border rounded-lg overflow-hidden`.
    - Spaltenheader: `bg-surface-alt border-b border-border px-4 py-2 text-micro text-ink-muted` mit Keys `AbsenceColEmployee`/`Range`/`Category`/`Status`/`Warnings`.
    - Row: 5-Spalten-Grid `[1.5fr 170px 140px 90px 70px] gap-3.5 px-4 py-3.5` (gap-3.5/py-3.5 = Spacing-Exception per UI-SPEC). Hover `bg-surface-alt`.
    - Empty-State: Conditional render (Self/HR-Variante mit unterschiedlichen Keys per UI-SPEC).
    - Click-on-Row → öffnet Modal in Edit-Mode.
    - **PFLICHT XSS-Mitigation (T-8-XSS-01):** Description als `{absence.description}` rendern — Dioxus auto-escaped. KEIN `dangerous_inner_html`.

    **10. AbsenceFilterBar** (Container-Component, HR-only zeigt Person-Dropdown):
    - `bg-surface border border-border rounded-lg px-3.5 py-2.5 flex flex-wrap items-center gap-2.5`.
    - Kategorie-Pills auf `bg-surface-alt p-0.5 rounded-md`.
    - Person-Dropdown (HR-only) als `SelectInput`.
    - Status-Dropdown als `SelectInput`.
    - Trennlinie `w-px h-[22px] bg-border mx-1`.
    - "Vergangene anzeigen"-Checkbox rechts mit `ml-auto`. Counter "{n} von {m}" `text-small text-ink-muted`.

    **11. StatsGrid** (Container-Component):
    - Grid `grid-cols-[repeat(auto-fit,minmax(160px,1fr))] gap-2.5`.
    - 3 Cards: Krankheitstage Year, Unbezahlt Year, Aktive Abwesenheiten — clientseitige Aggregation aus `absences`.
    - Card: `bg-surface border border-border rounded-md p-3`, Header `text-micro text-ink-muted`, Wert `text-h1 font-mono`.

    **12. DeleteConfirmDialog** (Center-Dialog Width 360):
    - Title `Key::AbsenceDeleteConfirmTitle`, Body `Key::AbsenceDeleteConfirmBody`.
    - Footer: Cancel (`Btn::Ghost`, `Key::AbsenceDeleteCancelBtn`) + Confirm (`Btn::Danger`, `Key::AbsenceDeleteConfirmBtn`).
    - on_confirm → triggert `absence_service.send(AbsenceAction::Delete(id))` und schließt sowohl Confirm als auch Edit-Modal.

    **PFLICHT-Verifikationen während Implementation:**
    - Keine `dangerous_inner_html`-Aufrufe → grep-check.
    - Keine `format!("text-{} bg-{}-soft", ...)`-Strings → grep-check Pitfall 5.
    - Alle Tailwind-Kategorie-Klassen sind statische match-Arme.
    - Status-Berechnung via `compute_status` Pure-Function.
    - AUTH `loading_done`-Check vor HR/Employee-Branch.
    - Description per `{value}`-Interpolation.

    Page kann ~600-1200 LOC werden (UI-SPEC RESEARCH § "Code Example 7" / Assumption A7) — das ist ok. Falls > 1500 LOC → in Plan 06 retrospektiv extrahieren in `component/absence_modal.rs`.
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo build --target wasm32-unknown-unknown 2>&1 | tail -15 &amp;&amp; nix develop --command cargo test 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `wc -l shifty-dioxus/src/page/absences.rs | awk '{print $1}'` ergibt mindestens 400 (Vollausbau, kein Stub)
    - `grep -c "pub fn AbsencesPage" shifty-dioxus/src/page/absences.rs` ergibt 1
    - `grep -c "fn compute_status" shifty-dioxus/src/page/absences.rs` ergibt 1
    - `grep -c "loading_done" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (Pitfall 4)
    - **W-9 (hardcoded date isolation):** `grep -B 2 'time::macros::date!' shifty-dioxus/src/page/absences.rs` zeigt — wenn überhaupt vorhanden — nur in `#[cfg(not(target_arch = \"wasm32\"))]` ODER `#[cfg(test)]`-Kontexten; Production-Render-Path nutzt `crate::js::current_date()` (wasm32) — KEIN hardcoded Default-Date im Render-Pfad. Stark: `awk 'BEGIN{in=0} /#\[cfg\(test\)\]|#\[cfg\(not\(target_arch = "wasm32"\)\)\]/{in=1} /^\}/{in=0} {if(in==0 && /time::macros::date!\(2026/) print}' shifty-dioxus/src/page/absences.rs | wc -l` ergibt 0
    - `grep -c "has_privilege" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (D-09)
    - `grep -c "AbsenceAction::Create" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1
    - `grep -c "AbsenceAction::Update" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1
    - `grep -c "AbsenceAction::Delete" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1
    - `grep -c "VacationBalanceAction::LoadSelf\|VacationBalanceAction::LoadTeam" shifty-dioxus/src/page/absences.rs` ergibt mindestens 2
    - `grep -E "AbsenceCategory::Vacation.*text-good|text-good.*AbsenceCategory::Vacation" shifty-dioxus/src/page/absences.rs` zeigt mindestens 1 statischen match-Arm (Pitfall 5)
    - `grep -E "AbsenceCategory::SickLeave.*text-warn" shifty-dioxus/src/page/absences.rs` zeigt mindestens 1 (Pitfall 5)
    - `grep -E "AbsenceCategory::UnpaidLeave.*text-ink-muted" shifty-dioxus/src/page/absences.rs` zeigt mindestens 1 (Pitfall 5)
    - `grep -c 'format!("text-{}' shifty-dioxus/src/page/absences.rs` ergibt 0 (kein dynamischer Tailwind-String — Pitfall 5)
    - `grep -c "dangerous_inner_html" shifty-dioxus/src/page/absences.rs` ergibt 0 (T-8-XSS-01)
    - `grep -c "ShiftyError::Validation" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (D-11 SelfOverlapBanner)
    - `grep -c "ShiftyError::Conflict" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (D-08 VersionConflictBanner)
    - `grep -c "Uuid::nil()" shifty-dioxus/src/page/absences.rs` ergibt mindestens 2 (id + version bei Create — Pitfall 9)
    - `grep -c "AbsenceWarningAcknowledgeBtn" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (D-12 Forward-Warnings-Flow)
    - `grep -c "Btn::Danger\|BtnVariant::Danger" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (Delete-Btn — D-07)
    - `grep -c "window.confirm\|window\\.confirm" shifty-dioxus/src/page/absences.rs` ergibt 0 (D-07: NICHT)
    - `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` Exit-Code 0 (FINAL WASM-BUILD-GATE)
    - `cd shifty-dioxus && nix develop --command cargo test` Exit-Code 0 (Frontend-Test-Suite grün, inkl. existierende dialog-Tests + i18n-Tests aus Plan 04)
    - `cd shifty-dioxus && nix develop --command cargo check` Exit-Code 0
    - Repo-Root: `nix develop --command cargo check --workspace` Exit-Code 0 (keine Backend-Regression)
  </acceptance_criteria>

  <done>AbsencesPage rendert Vollumfang per UI-SPEC; alle Decisions D-01/D-05/D-06/D-07/D-08/D-09/D-10/D-11/D-12/D-14 umgesetzt; WASM-Build grün; Frontend + Backend cargo check beide grün. Task 3 (Snapshot-Tests) und Task 4 (VALIDATION.md sign-off) folgen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Component Snapshot Tests (Wave-0 fulfillment, B-2)</name>

  <read_first>
    - shifty-dioxus/src/component/dialog.rs Z. 449-510 (B-2 fix — VERIFIED Snapshot-Test-Pattern: `fn render(comp: fn() -> Element)` Z. 461-465 + `closed_dialog_renders_nothing` Z. 467 + `open_dialog_renders_title_and_close_button` Z. 490 — 1:1-Vorlage für dioxus-ssr Snapshot-Tests)
    - shifty-dioxus/src/page/absences.rs (Output von Task 2 — alle Components existieren)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Validation Architecture" (B-2 — Wave-0-Item 3 fordert dioxus-ssr Snapshot-Test)
    - .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md (Wave 0 Requirements: dioxus-ssr Snapshot-Test-Stub für absence-Components)
  </read_first>

  <files>
    shifty-dioxus/src/page/absences.rs (modify — `#[cfg(test)] mod tests { ... }` Block am Datei-Ende)
  </files>

  <behavior>
    - Test 1 (`category_badge_renders_vacation_label`): rendert CategoryBadge mit `AbsenceCategory::Vacation` für Locale::De → HTML enthält `Urlaub` UND `text-good` UND `bg-good-soft`.
    - Test 2 (`category_badge_renders_sick_leave_label`): CategoryBadge mit `AbsenceCategory::SickLeave` → HTML enthält `Krankheit` UND `text-warn` UND `bg-warn-soft`.
    - Test 3 (`category_badge_renders_unpaid_leave_label`): CategoryBadge mit `AbsenceCategory::UnpaidLeave` → HTML enthält `Unbezahlt` UND `text-ink-muted` UND `bg-surface-2`.
    - Test 4 (`status_pill_renders_active`): StatusPill mit `AbsenceStatus::Active` → HTML enthält `Aktiv` UND `text-accent`.
    - Test 5 (`status_pill_renders_planned`): StatusPill mit `AbsenceStatus::Planned` → HTML enthält `Geplant` UND `text-ink-soft`.
    - Test 6 (`status_pill_renders_finished`): StatusPill mit `AbsenceStatus::Finished` → HTML enthält `Beendet` UND `text-ink-muted`.
    - Test 7 (`compute_status_today_before_from_returns_planned`): pure function — `compute_status(today+1, today+5, today) == Planned`.
    - Test 8 (`compute_status_today_in_range_returns_active`): `compute_status(today-1, today+1, today) == Active`.
    - Test 9 (`compute_status_today_after_to_returns_finished`): `compute_status(today-5, today-1, today) == Finished`.
    - Test 10 (`absence_filter_bar_hr_variant_renders_person_dropdown`): rendert AbsenceFilterBar mit `is_hr=true` → HTML enthält `AbsenceFilterPersonLabel`-Text (`Person`).
    - Test 11 (`absence_filter_bar_employee_variant_omits_person_dropdown`): rendert AbsenceFilterBar mit `is_hr=false` → HTML enthält **NICHT** `AbsenceFilterPersonLabel`-Text.
  </behavior>

  <action>
    Im Datei-Ende von `shifty-dioxus/src/page/absences.rs` ergänze einen `#[cfg(test)] mod tests { ... }`-Block analog `dialog.rs:449-510`. Jeder Test verwendet das verifizierte Render-Pattern aus dialog.rs:

    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;
        use dioxus::prelude::*;

        fn render(comp: fn() -> Element) -> String {
            let mut vdom = VirtualDom::new(comp);
            vdom.rebuild_in_place();
            dioxus_ssr::render(&vdom)
        }

        // Pure-Function Tests (compute_status):
        #[test]
        fn compute_status_today_before_from_returns_planned() {
            let today = time::macros::date!(2026-05-08);
            let from = time::macros::date!(2026-05-09);
            let to = time::macros::date!(2026-05-13);
            assert_eq!(compute_status(from, to, today), AbsenceStatus::Planned);
        }

        #[test]
        fn compute_status_today_in_range_returns_active() {
            let today = time::macros::date!(2026-05-08);
            let from = time::macros::date!(2026-05-07);
            let to = time::macros::date!(2026-05-09);
            assert_eq!(compute_status(from, to, today), AbsenceStatus::Active);
        }

        #[test]
        fn compute_status_today_after_to_returns_finished() {
            let today = time::macros::date!(2026-05-08);
            let from = time::macros::date!(2026-05-03);
            let to = time::macros::date!(2026-05-07);
            assert_eq!(compute_status(from, to, today), AbsenceStatus::Finished);
        }

        // CategoryBadge Snapshot-Tests:
        #[test]
        fn category_badge_renders_vacation_label() {
            fn app() -> Element {
                rsx! { CategoryBadge { category: AbsenceCategory::Vacation } }
            }
            let html = render(app);
            assert!(html.contains("Urlaub"), "missing label: {html}");
            assert!(html.contains("text-good"), "missing text-good: {html}");
            assert!(html.contains("bg-good-soft"), "missing bg-good-soft: {html}");
        }

        // analog für SickLeave/UnpaidLeave + StatusPill (3 Statis) + AbsenceFilterBar (HR/Employee Variants).
        // ... (insgesamt 11 Tests aus <behavior>)
    }
    ```

    **WICHTIG (B-2):** AbsenceFilterBar erwartet als Props `is_hr: bool` + `i18n: I18N`-Handle (oder analog) — der Test stelle sicher, dass das Component für SSR mit Mock-i18n rendert. Falls ein Component zu viele GlobalSignal-Reads hat (z. B. `SALES_PERSONS.read()`), refaktorisiere ihn in eine Pure-Funktion-Variante mit `#[component]`-Props, die in Production aus den GlobalSignals gefüllt wird (Pattern: dialog.rs ist props-only).

    **i18n-Initialisierung im Test:** Falls Component i18n.t(Key::*) ruft, muss der Test zuerst `crate::i18n::init_for_test(Locale::De)` (oder analog) aufrufen ODER der Component nimmt ein `i18n`-Prop. Plan 05 entscheidet beim Schreiben — bevorzugt: Component nimmt i18n-Prop, weil das Test-Setup vereinfacht.

    **Insgesamt 11 Tests** (3 CategoryBadge + 3 StatusPill + 3 compute_status + 2 AbsenceFilterBar).
  </action>

  <verify>
    <automated>cd shifty-dioxus &amp;&amp; nix develop --command cargo test -p shifty-dioxus 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "fn category_badge_renders_vacation_label" shifty-dioxus/src/page/absences.rs` ergibt 1
    - `grep -c "fn status_pill_renders_active" shifty-dioxus/src/page/absences.rs` ergibt 1
    - `grep -c "fn compute_status_today_in_range_returns_active" shifty-dioxus/src/page/absences.rs` ergibt 1
    - `grep -c "fn absence_filter_bar_hr_variant_renders_person_dropdown" shifty-dioxus/src/page/absences.rs` ergibt 1
    - `grep -c "dioxus_ssr::render" shifty-dioxus/src/page/absences.rs` ergibt mindestens 1 (Snapshot-Pattern aus dialog.rs:461 etabliert)
    - `cd shifty-dioxus && nix develop --command cargo test -p shifty-dioxus -- --list 2>/dev/null | grep -c absence` ergibt mindestens 4
    - `cd shifty-dioxus && nix develop --command cargo test -p shifty-dioxus` Exit-Code 0
  </acceptance_criteria>

  <done>11 Snapshot-/Pure-Function-Tests in absences.rs etabliert; Wave-0-Item 3 aus VALIDATION.md erfüllt; alle Tests grün. VALIDATION.md kann jetzt auf `nyquist_compliant: true` geflippt werden — Sub-Task in Plan 06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Validation Sign-Off — VALIDATION.md flip auf nyquist_compliant: true (B-2 closure)</name>

  <read_first>
    - .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md (frontmatter: nyquist_compliant + wave_0_complete)
    - shifty-dioxus/src/page/absences.rs (Output von Task 3 — Tests existieren)
  </read_first>

  <files>.planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md (modify)</files>

  <action>
    Update VALIDATION.md frontmatter:
    - `nyquist_compliant: false` → `nyquist_compliant: true`
    - `wave_0_complete: false` → `wave_0_complete: true`

    Hake im "Wave 0 Requirements"-Abschnitt die Items ab:
    - [x] `service_impl/src/test/vacation_balance.rs` (durch Plan 02 Task 1)
    - [x] `rest/src/vacation_balance.rs` Integration-Tests (Plan 03 Snapshot covers OpenAPI)
    - [x] `shifty-dioxus/src/page/absences.rs` dioxus-ssr Snapshot-Test (Plan 05 Task 3)
    - [x] OpenAPI insta-Snapshot-Refresh (Plan 03)

    Hake im "Validation Sign-Off"-Abschnitt:
    - [x] All tasks have `<automated>` verify or Wave 0 dependencies
    - [x] Sampling continuity
    - [x] Wave 0 covers all MISSING references
    - [x] No watch-mode flags
    - [x] Feedback latency < 120s
    - [x] `nyquist_compliant: true` set in frontmatter

    Status `Approval: pending` → `Approval: granted`.
  </action>

  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "nyquist_compliant: true" .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md` ergibt 1
    - `grep -c "wave_0_complete: true" .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md` ergibt 1
    - `grep -c "nyquist_compliant: false" .planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md` ergibt 0
  </acceptance_criteria>

  <done>VALIDATION.md frontmatter zeigt Wave-0-Closure; nyquist_compliant: true; Phase 8 ist UAT-bereit (Plan 06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → AbsencesPage | Auth-Privilege wird im Backend enforced; Frontend `has_privilege("hr")`-Check ist UI-Hint (defense-in-depth) |
| AbsenceModal → Backend | reqwest-Calls über Dx-Proxy; 422/409/Forward-Warnings werden semantisch gehandhabt |
| Description-Field → DOM | Dioxus rsx-Interpolation auto-escaped — KEIN `dangerous_inner_html` |
| Tailwind-Build → Production | Static match-Arme garantieren, dass Klassen vom Tailwind-Purge gefunden werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-01 | Elevation of Privilege | Frontend HR-Filter | mitigate (defense-in-depth) | `is_hr = auth.has_privilege("hr")` ist nur UI-Display-Switch. Backend-Endpoints (`/absence-period`, `/vacation-balance/team/*`) enforced selbst HR-Privilege; Frontend kann Filter zeigen, Backend lehnt unauthorized Requests ab. |
| T-8-AUTH-02 | Elevation of Privilege | TopBar-Menü "Absences" | accept | D-10 sagt explizit: Menü-Eintrag für ALLE eingeloggten User sichtbar. HR-Privilege schaltet nur den Filter-Modus innerhalb der Page. Backend enforced bei jedem API-Call. UI-Visibility ist kein Threat. |
| T-8-IDOR-01 | Information Disclosure | Modal Edit/Delete via id-URL | mitigate (Backend) | Frontend lädt Edit-AbsencePeriod aus `ABSENCE_STORE` (vorher gefiltert) — kein direktes ID-Navigation. Backend prüft `hr ∨ self` bei jedem Resource-Zugriff (Plan 02 + existing v1.0). Frontend liefert 403 → ErrorView. |
| T-8-XSS-01 | Tampering | Description-Field rendering | mitigate | `{absence.description}`-Interpolation in rsx — Dioxus auto-escaped. KEIN `dangerous_inner_html`-Aufruf irgendwo. Acceptance-Criterion: `grep -c dangerous_inner_html == 0`. |
| T-8-INJECTION-01 | Injection | i18n-User-Content (Description-Echo bei Warnings, {category}/{from}/{to} in SelfOverlap-Body) | mitigate | i18n-Substitutions nutzen Werte aus `AbsenceCategory`-Enum (statisch) und `time::Date::format`-strings (typed). Description geht NICHT durch Interpolation, sondern als separate Render-Span (auto-escaped). |
| T-8-TAILWIND-PURGE | Denial of Service (visual) | CategoryBadge / StatusPill | mitigate | Pitfall 5: alle Kategorie-Farben als statische match-Arme. Acceptance-Criterion: `grep format!("text-{}` == 0. Verhindert dass Production-Build farblose Pills zeigt. |
| T-8-LOADING-FLICKER | Information Disclosure (UX) | Auth-Race | mitigate | Pitfall 4: Page rendert "Loading..." bis `auth.loading_done == true`. Verhindert Doppel-Fetch (Employee-API zuerst, dann HR-API) wenn Auth zwei Render-Cycles braucht. |
| T-8-VERSIONCONFLICT-LOSS | Tampering | 409 Optimistic-Lock | mitigate | D-08: Form-State bleibt bei 409 erhalten, User klickt Reload-Btn → re-fetch der einzelnen Edit-Resource → User entscheidet manuell. KEIN silent overwrite, KEIN Form-State-Verwerfen. |
</threat_model>

<verification>
**Layer 1 — Compile (Routing + TopBar nach Task 1):**
- `cd shifty-dioxus && nix develop --command cargo check` grün

**Layer 2 — Compile (Voll-Page nach Task 2):**
- `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` grün — KRITISCH (Final-Compile-Gate)

**Layer 3 — Frontend-Tests:**
- `cd shifty-dioxus && nix develop --command cargo test` grün, inkl. Plan 04 i18n-Tests

**Layer 4 — Backend-Workspace:**
- Repo-Root: `nix develop --command cargo check --workspace` grün — keine Backend-Regression

**Layer 5 — Manual Smoke (NICHT Task-2-Verify, ist Plan 06):**
- `dx serve` startet Frontend
- `cargo run` startet Backend
- Login als HR → Page lädt, Liste zeigt alle Mitarbeiter
- Click "Neue Abwesenheit" → Modal öffnet
- Range-Picker lehnt `to < from` ab (Save-Btn disabled)
- Submit mit overlapping vacation → 422 → SelfOverlapBanner inline
- Submit mit existing booking → 201 + WarningList → Acknowledge-Btn → Modal-Close
</verification>

<success_criteria>
- AbsencesPage ist vollständig implementiert, alle 12+ Inline-Components rendern
- Routing + TopBar zeigen Menü-Eintrag für alle eingeloggten User (D-10)
- HR/Employee-Sicht-Switch via `auth.has_privilege("hr")` (D-09)
- Modal CRUD funktioniert: Create/Edit/Delete mit Cross-Field-Validation, 422-Banner, 409-Banner, Forward-Warnings, Delete-Confirm-Dialog
- Tailwind-Klassen statisch (Pitfall 5)
- Status-Berechnung Pure-Function (Pitfall 8)
- Description auto-escaped (T-8-XSS-01)
- WASM-Build grün
- Backend `cargo check --workspace` grün
- Plan 06 ist nur noch UAT-Smoke
</success_criteria>

<output>
After completion, create `.planning/phases/08-absence-crud-page-foundation/08-05-SUMMARY.md`.

User commited manuell. Empfohlene jj-Commits:
- `feat(08): route /absences + top-bar menu entry`
- `feat(08): AbsencesPage with HR/Employee variants`
- `feat(08): AbsenceModal + WarningList + banners + DeleteConfirm`
- `feat(08): VacationEntitlementCard + VacationPerPersonList`
</output>
</content>
</invoke>