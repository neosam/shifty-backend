---
phase: 08.3-halbtag-support-f-r-absences
plan: 03
subsystem: i18n
tags: [i18n, locales, halftime, day-fraction, reference-matcher, fui-a-09, pitfall-2-guard]

requires:
  - phase: 08.3-01
    provides: "13 Key-Enum-Slots (6 Absence + 7 Cutover) in shifty-dioxus/src/i18n/mod.rs, leer (ohne add_text-Bodies)"

provides:
  - "13 add_text(...)-Bodies in en.rs / de.rs / cs.rs für alle 6 Absence + 7 Cutover Halbtag-Keys, 1:1 zur UI-SPEC.md Copywriting-Tabelle (Z. 150-178)"
  - "2 Presence-Tests (i18n_absence_day_fraction_keys_present_in_all_locales, i18n_cutover_day_fraction_keys_present_in_all_locales) — FUI-A-09 locale-coverage guard"
  - "6 Reference-Matcher-Tests (3 Absence + 3 Cutover × {Locale::De, Locale::En, Locale::Cs}) — Pitfall-2 defense gegen Locale-Tag-Verwechslung in den Locale-Files"
  - "WASM-Build (nix-shell -p lld) grün — Frontend ist deploybar"

affects: [08.3-06]

requirements_addressed: [FUI-A-09, FUI-A-10]

tech-stack:
  added: []
  patterns:
    - locale-coverage-presence-test (for-loop über [Locale::En, Locale::De, Locale::Cs] × Key-Array)
    - pitfall-2-reference-matcher (generate(Locale::X) + assert_eq! pro Referenz-String — Pattern aus Plan 08-04 D-26)
    - same-commit-locale-coverage (FUI-A-09 fordert simultanes Befüllen aller 3 Locales — Task 1 ist EIN jj-commit über alle drei Files)

key-files:
  created: []
  modified:
    - shifty-dioxus/src/i18n/en.rs
    - shifty-dioxus/src/i18n/de.rs
    - shifty-dioxus/src/i18n/cs.rs
    - shifty-dioxus/src/i18n/mod.rs

key-decisions:
  - "Keine Abweichung von der UI-SPEC.md Copywriting-Tabelle — alle 39 Strings (13 × 3 Locales) character-for-character übernommen, inklusive Sonderzeichen (ä, ě, ů, ½, ≈) und Format-Placeholder-Token `{amount:.2}h`."
  - "Format-Placeholder `{amount:.2}h ≈ ½ × ...` wird verbatim in der i18n-Tabelle hinterlegt; das Frontend (Plan 06) substituiert via `format!()`. Keine i18n-Macro-Interpolations-Syntax — analog zum existing `{count}`-Pattern bei AbsenceWarningHeaderPlural."
  - "Test-Naming spiegelt das existing `i18n_{absence,cutover}_keys_*`-Pattern (Phase 8, 8.2): `i18n_{absence,cutover}_day_fraction_{keys_present_in_all_locales, match_{german,english,czech}_reference}`."

patterns-established:
  - "Halbtag-Test-Sextett: ein neuer Domain-Block (Absence-Halbtag / Cutover-Halbtag) bekommt strukturell 1 Presence-Test + 3 Reference-Matcher (eine pro Locale) — minimiert Pitfall-2 + Coverage-Lücken."
  - "WASM-Build-Gate auf NixOS: plain `cargo build --target wasm32-unknown-unknown` schlägt fehl (lld nicht im PATH); `nix-shell -p lld --command 'cargo build --target wasm32-unknown-unknown'` ist der reproducible Workflow."

tests:
  added:
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_absence_day_fraction_keys_present_in_all_locales
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_cutover_day_fraction_keys_present_in_all_locales
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_absence_day_fraction_match_german_reference
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_absence_day_fraction_match_english_reference
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_absence_day_fraction_match_czech_reference
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_cutover_day_fraction_match_german_reference
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_cutover_day_fraction_match_english_reference
    - shifty-dioxus/src/i18n/mod.rs::tests::i18n_cutover_day_fraction_match_czech_reference
  results:
    - "cargo test day_fraction: 9 tests passed (8 i18n + 1 vorhandener state-Roundtrip aus Plan 01)"
    - "cargo test i18n: 28 tests passed (8 neue + 20 vorhandene); 0 failed"
    - "cargo build (native): green (2m 12s, 45 warnings — alle pre-existing)"
    - "nix-shell -p lld --command 'cargo build --target wasm32-unknown-unknown': green"
    - "cargo check --workspace (backend): green (i18n ist frontend-only, kein backend impact)"

jj-commits:
  - "owmstxmvpqzq feat(08.3-03): add 13 i18n bodies for half-day support across de/en/cs"
  - "vxmwsvstwpwk test(08.3-03): add 2 presence + 6 reference-matcher i18n tests for half-day keys"

duration: ~25min
completed: 2026-05-17
---

# Plan 08.3-03 — i18n-Bodies + Pitfall-2-Tests (FUI-A-09-Closure)

## Was gebaut

### Task 1 — i18n-Bodies in allen 3 Locales (commit `owmstxmvpqzq`)

In `shifty-dioxus/src/i18n/{en,de,cs}.rs` jeweils 13 neue `add_text(...)`-Calls
angehängt (am Ende der `generate(...)`-Funktion, nach dem Phase-8.2-Manual-Range-Block).
Gegliedert in zwei Sub-Blöcke mit Kommentar-Headern:

* **Phase 8.3 — Halbtag-Support (Absence)** — 6 Keys (`AbsenceFieldDayFraction`,
  `AbsenceDayFractionFull`, `AbsenceDayFractionHalf`, `AbsenceFieldDayFractionFullHint`,
  `AbsenceFieldDayFractionHalfHint`, `AbsencePreviewFooterHalfDay`).
* **Phase 8.3 — Halbtag-Support (Cutover)** — 7 Keys (`CutoverDriftColDayFraction`,
  `CutoverDriftDayFractionAria`, `CutoverBulkDayFractionLabel`,
  `CutoverDriftHalfDaySuggestion`, `CutoverManualConvertDayFractionLabel`,
  `CutoverDayFractionFull`, `CutoverDayFractionHalf`).

Alle 39 Strings (13 × 3 Locales) sind 1:1 aus der UI-SPEC.md Copywriting-Tabelle
(Z. 150-178) übernommen — character-for-character, inkl. UTF-8-Sonderzeichen
(`ä`, `ě`, `ů`, `½`, `≈`) und Format-Placeholder-Token `{amount:.2}h`.

Jeder `add_text`-Call nutzt den korrekten `Locale`-Tag als erstes Argument
(`Locale::En` in en.rs, `Locale::De` in de.rs, `Locale::Cs` in cs.rs) — das ist
die strukturelle Pitfall-2-Defense, gegen den historischen Bug, dass das de.rs-
Block versehentlich `Locale::En` verwendete (siehe `shifty-dioxus/CLAUDE.md`
"German translations previously had a bug").

### Task 2 — 2 Presence + 6 Reference-Matcher-Tests (commit `vxmwsvstwpwk`)

In `shifty-dioxus/src/i18n/mod.rs` im `#[cfg(test)] mod tests`-Block, am Ende
nach dem Phase-8.2-Cutover-Tests-Block angehängt:

* **2 Presence-Tests** — for-loop über `[Locale::En, Locale::De, Locale::Cs]`
  × Key-Array, asserten dass jeder Wert nicht-leer ist und ≠ `"??"` (FUI-A-09
  coverage-guard, gleicher Pattern wie `i18n_absence_keys_present_in_all_locales`
  aus Phase 8).
* **6 Reference-Matcher-Tests** — pro Locale ein dedizierter Test mit
  `generate(Locale::X)` + `assert_eq!` pro Referenz-String (Pitfall-2 defense
  aus Plan 08-04 D-26).

## Pitfall-2-Audit

Verifiziert durch die 6 Reference-Matcher (alle grün): keine Locale-Tag-
Verwechslung in den Locale-Files. Eine versehentliche `Locale::En`-Verwendung in
`de.rs` würde die German-Reference-Asserts mit "Day fraction"-statt-"Tageshälfte"-
String fehlschlagen lassen.

## Test-Status

```
cargo test day_fraction              → 9 passed; 0 failed
cargo test i18n                      → 28 passed; 0 failed
cargo build (native)                 → ok (2m 12s)
nix-shell -p lld --command \
  'cargo build --target wasm32-unknown-unknown'
                                     → ok (WASM-Build-Gate)
cargo check --workspace (backend)    → ok (i18n ist frontend-only)
```

## Erwartete Anschluss-Plans

* **Plan 04** (Reporting-Halbierung) — nutzt die Strings nicht direkt; läuft
  unabhängig in Wave 3.
* **Plan 05** (Cutover-Backend) — nutzt die Strings nicht direkt; läuft
  unabhängig in Wave 3.
* **Plan 06** (Frontend-Surface) — konsumiert die 13 Keys via `i18n.t(Key::*)`
  beim Rendern von `AbsenceModal`, `DriftEntryRow`, `ManualConvertModal`.
  Die Bodies sind ab jetzt vorhanden — Plan 06 wird keine `"??"`-Fallbacks
  zeigen.

## VCS

Zwei jj-commits, beide unter dem `feat(08.3-03)` / `test(08.3-03)`-Prefix
gemäß Plan-Frontmatter-Konvention. Keine STATE.md / ROADMAP.md-Touches
(GSD's `commit_docs: false` ist aktiv; State-Updates folgen in der Verification-
Phase nach Plan 08.3-06).
