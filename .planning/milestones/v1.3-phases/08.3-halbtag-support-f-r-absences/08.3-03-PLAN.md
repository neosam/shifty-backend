---
phase: 08.3
plan: 03
type: execute
wave: 2
depends_on: [08.3-01]
files_modified:
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/i18n/mod.rs
autonomous: true
requirements: [FUI-A-09, FUI-A-10]
requirements_addressed: [FUI-A-09, FUI-A-10]
tags: [i18n, locales, halftime, day-fraction, reference-matcher, fui-a-09, pitfall-2-guard]

must_haves:
  truths:
    - "Alle 13 in Plan 01 angelegten i18n-Key-Enum-Varianten (6 Absence + 7 Cutover) haben `add_text(...)`-Bodies in allen drei Locale-Files (en.rs, de.rs, cs.rs) — IM SELBEN COMMIT (FUI-A-09)."
    - "Die exakten Referenz-Strings entsprechen 1:1 der UI-SPEC.md Copywriting-Tabelle (Z. 150-178)."
    - "Locale-Block-Pflicht: jeder `de.rs`-Eintrag nutzt `Locale::De` als erstes Argument (NICHT `Locale::En` — historischer Bug-Guard aus Plan 08-04 D-26)."
    - "Presence-Tests: `i18n_absence_day_fraction_keys_present_in_all_locales` und `i18n_cutover_day_fraction_keys_present_in_all_locales` verifizieren für alle 13 Keys × 3 Locales, dass der Text nicht-leer und ≠ `\"??\"` ist."
    - "6 Reference-Matcher-Tests (3 Absence × 3 Locales + 3 Cutover × 3 Locales) verifizieren die exakten Referenz-Strings — `Locale::De` MUSS für German-Reference, `Locale::En` für English-Reference, `Locale::Cs` für Czech-Reference verwendet werden."
    - "WASM-Build der Frontend-Crate ist nach diesem Plan grün (`cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` exit 0) — i18n-Bodies brechen den Build nicht."
    - "Native Frontend-Tests `cargo test -p shifty-dioxus i18n` exit 0."
  artifacts:
    - path: "shifty-dioxus/src/i18n/en.rs"
      provides: "13 add_text(Locale::En, ...) Calls in den Phase-8.3-Blöcken (Absence + Cutover)"
      contains: "Key::AbsenceFieldDayFraction"
    - path: "shifty-dioxus/src/i18n/de.rs"
      provides: "13 add_text(Locale::De, ...) Calls in den Phase-8.3-Blöcken"
      contains: "Key::AbsenceFieldDayFraction"
    - path: "shifty-dioxus/src/i18n/cs.rs"
      provides: "13 add_text(Locale::Cs, ...) Calls in den Phase-8.3-Blöcken"
      contains: "Key::AbsenceFieldDayFraction"
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "2 Presence-Tests + 6 Reference-Matcher-Tests (Pitfall-2-Guard)"
      contains: "i18n_absence_day_fraction_match_german_reference"
  key_links:
    - from: "shifty-dioxus/src/i18n/de.rs (Locale::De-Block)"
      to: "shifty-dioxus/src/i18n/mod.rs::i18n_absence_day_fraction_match_german_reference"
      via: "Pitfall-2-Guard: Test verifiziert Locale-Tag und exakten String"
      pattern: "generate\\(Locale::De\\)"
    - from: "shifty-dioxus/src/i18n/{en,de,cs}.rs (alle 3 Locales)"
      to: "shifty-dioxus/src/i18n/mod.rs::i18n_absence_day_fraction_keys_present_in_all_locales"
      via: "Coverage-Test über for-loop Locale::{En,De,Cs}"
      pattern: "for locale in \\[Locale::En, Locale::De, Locale::Cs\\]"
---

<vcs_jj_only>
**VCS-Politik (jj-only) — verbindlich:** KEINE `git commit`/`git add`-Mutations. Verwende `jj`. Convention für diese Phase: `feat(08.3-03): add 13 i18n keys for half-day support across de/en/cs + reference-matcher tests`. WICHTIG: Alle drei Locale-Files MÜSSEN im selben jj-commit liegen — FUI-A-09 (locale-coverage) ist Phase-gate.
</vcs_jj_only>

<objective>
Befülle die in Plan 01 angelegten 13 i18n-Key-Enum-Slots mit den exakten Referenz-Strings aus der UI-SPEC.md Copywriting-Tabelle für alle drei Locales (De / En / Cs) und schreibe 2 Presence-Tests + 6 Reference-Matcher-Tests (Pitfall-2-Guard analog Plan 08-04 D-26) ins Test-Modul von `i18n/mod.rs`. Dieser Plan ist die FUI-A-09-Compliance-Closure für Phase 8.3.

Purpose: i18n-Bodies sind die User-facing-Strings, die Plan 06 (Frontend) konsumiert. Sie müssen vor Plan 06 landen, damit der Frontend-Code keine `"??"`-Fallbacks rendert. Pitfall-2 (Locale::En-statt-Locale::De-Bug) hat in v1.0 echte Bugs verursacht — die Reference-Matcher-Tests sind die strukturelle Defense.

Output: 3 modifizierte Locale-Files + Test-Erweiterung im mod.rs; alle Tests grün; WASM-Build grün.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-CONTEXT.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-UI-SPEC.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-01-PLAN.md
@.planning/phases/08-absence-crud-page-foundation/08-04-SUMMARY.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-02-SUMMARY.md

@shifty-dioxus/CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Live-Patterns aus existing i18n-Code -->

From shifty-dioxus/src/i18n/en.rs (existing Absence-Block — Z. 640-680, vorlage):
```rust
i18n.add_text(Locale::En, Key::AbsenceFieldCategory, "Category");
i18n.add_text(Locale::En, Key::AbsenceFieldFrom, "From");
i18n.add_text(Locale::En, Key::AbsenceFieldTo, "To (inclusive)");
i18n.add_text(
    Locale::En,
    Key::AbsenceFieldDescriptionHint,
    "Optional — e.g. travel destination or note.",
);
```

From shifty-dioxus/src/i18n/mod.rs (existing Presence-Test — Z. 680-721):
```rust
#[test]
fn i18n_absence_keys_present_in_all_locales() {
    for locale in [Locale::En, Locale::De, Locale::Cs] {
        let i18n = generate(locale);
        for key in [
            Key::AbsencePageTitle,
            // ... existing keys ...
        ] {
            let value = i18n.t(key);
            assert!(
                !value.is_empty() && value.as_ref() != "??",
                "missing translation for {:?} in {:?}: got `{}`",
                key, locale, value
            );
        }
    }
}
```

From shifty-dioxus/src/i18n/mod.rs (existing Reference-Matcher — Z. 723-755):
```rust
#[test]
fn i18n_absence_keys_match_german_reference() {
    // Pitfall-2 guard: ensures the de.rs block uses Locale::De.
    let i18n = generate(Locale::De);
    assert_eq!(i18n.t(Key::AbsencePageTitle).as_ref(), "Abwesenheiten");
    assert_eq!(i18n.t(Key::AbsenceCategoryVacation).as_ref(), "Urlaub");
    // ...
}
```

UI-SPEC Copywriting-Tabelle (08.3-UI-SPEC.md Z. 150-178) — Referenz-Strings:

Absence:
| Key                                | De                                           | En                                            | Cs                                                  |
| AbsenceFieldDayFraction            | Tageshälfte                                  | Day fraction                                  | Část dne                                            |
| AbsenceDayFractionFull             | Ganztag                                      | Full day                                      | Celý den                                            |
| AbsenceDayFractionHalf             | Halber Tag                                   | Half day                                      | Půl dne                                             |
| AbsenceFieldDayFractionFullHint    | Verbraucht den vollen Vertrags-Stundensatz pro Tag. | Uses the full contract-hours rate per day. | Spotřebovává plnou smluvní sazbu za každý den.   |
| AbsenceFieldDayFractionHalfHint    | Verbraucht 0,5 Urlaubstage pro Tag im Bereich.      | Uses 0.5 vacation days per day in the range. | Spotřebovává 0,5 dne dovolené za každý den v období. |
| AbsencePreviewFooterHalfDay        | Bei Halbtag: angezeigte Stunden sind bereits halbiert. | Half-day: shown hours are already halved. | Půldenní: zobrazené hodiny jsou již vydělené dvěma. |

Cutover:
| Key                                       | De                                            | En                                              | Cs                                                |
| CutoverDriftColDayFraction                | Tageshälfte                                   | Day fraction                                    | Část dne                                          |
| CutoverDriftDayFractionAria               | Tageshälfte für diesen Eintrag wählen         | Pick day fraction for this entry                | Zvol část dne pro tuto položku                   |
| CutoverBulkDayFractionLabel               | Halb/Ganz für ganze Gruppe                    | Half/Full for whole group                       | Půl/Celý pro celou skupinu                       |
| CutoverDriftHalfDaySuggestion             | Stunden deuten auf Halbtag hin ({amount:.2}h ≈ ½ × Vertragstag). | Hours suggest half-day ({amount:.2}h ≈ ½ × contract day). | Hodiny naznačují půldenní ({amount:.2}h ≈ ½ × smluvní den). |
| CutoverManualConvertDayFractionLabel      | Tageshälfte                                   | Day fraction                                    | Část dne                                          |
| CutoverDayFractionFull                    | Ganztag                                       | Full day                                        | Celý den                                          |
| CutoverDayFractionHalf                    | Halber Tag                                    | Half day                                        | Půl dne                                           |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 13 add_text-Bodies in en.rs + de.rs + cs.rs simultan ergänzen</name>
  <files>
    shifty-dioxus/src/i18n/en.rs,
    shifty-dioxus/src/i18n/de.rs,
    shifty-dioxus/src/i18n/cs.rs
  </files>
  <read_first>
    - shifty-dioxus/src/i18n/en.rs (gesamter File; insbesondere die existing Absence-Block Z. 640-680 und Cutover-Block — bestätige Locale-Tag-Verwendung)
    - shifty-dioxus/src/i18n/de.rs (gesamter File; existing Absence-Block Z. 718-745)
    - shifty-dioxus/src/i18n/cs.rs (gesamter File; existing Absence-Block Z. 698-725)
    - shifty-dioxus/src/i18n/mod.rs (nach Plan 01 — die 13 neuen Key-Enum-Varianten existieren bereits)
    - .planning/phases/08.3-halbtag-support-f-r-absences/08.3-UI-SPEC.md (Z. 150-178 für die exakten Referenz-Strings; die Tabelle oben in `<interfaces>` ist die authoritative Kopie davon)
  </read_first>
  <action>
    **WICHTIG — FUI-A-09:** Alle 13 Keys MÜSSEN gleichzeitig in allen drei Locales gepflegt werden. Wenn ein Locale eine Key vergisst, schlägt der Presence-Test fehl. Wenn ein Locale-Tag falsch ist (`Locale::En` in `de.rs`), schlägt der Reference-Matcher-Test fehl.

    **Schritt A — `en.rs`** ergänzen. Suche das Ende des existing Absence-Blocks (typisch nach `AbsenceCategoryUnpaidLeave` o. ä.) und füge dort hinzu:

    ```rust
    // Phase 8.3 — Halbtag-Support (Absence).
    i18n.add_text(Locale::En, Key::AbsenceFieldDayFraction, "Day fraction");
    i18n.add_text(Locale::En, Key::AbsenceDayFractionFull, "Full day");
    i18n.add_text(Locale::En, Key::AbsenceDayFractionHalf, "Half day");
    i18n.add_text(
        Locale::En,
        Key::AbsenceFieldDayFractionFullHint,
        "Uses the full contract-hours rate per day.",
    );
    i18n.add_text(
        Locale::En,
        Key::AbsenceFieldDayFractionHalfHint,
        "Uses 0.5 vacation days per day in the range.",
    );
    i18n.add_text(
        Locale::En,
        Key::AbsencePreviewFooterHalfDay,
        "Half-day: shown hours are already halved.",
    );
    ```

    Plus Cutover-Block ans Ende des Cutover-Bereichs:

    ```rust
    // Phase 8.3 — Halbtag-Support (Cutover).
    i18n.add_text(Locale::En, Key::CutoverDriftColDayFraction, "Day fraction");
    i18n.add_text(Locale::En, Key::CutoverDriftDayFractionAria, "Pick day fraction for this entry");
    i18n.add_text(Locale::En, Key::CutoverBulkDayFractionLabel, "Half/Full for whole group");
    i18n.add_text(
        Locale::En,
        Key::CutoverDriftHalfDaySuggestion,
        "Hours suggest half-day ({amount:.2}h ≈ ½ × contract day).",
    );
    i18n.add_text(Locale::En, Key::CutoverManualConvertDayFractionLabel, "Day fraction");
    i18n.add_text(Locale::En, Key::CutoverDayFractionFull, "Full day");
    i18n.add_text(Locale::En, Key::CutoverDayFractionHalf, "Half day");
    ```

    **Schritt B — `de.rs`** analog — `Locale::De` als erstes Argument:

    ```rust
    // Phase 8.3 — Halbtag-Support (Absence).
    i18n.add_text(Locale::De, Key::AbsenceFieldDayFraction, "Tageshälfte");
    i18n.add_text(Locale::De, Key::AbsenceDayFractionFull, "Ganztag");
    i18n.add_text(Locale::De, Key::AbsenceDayFractionHalf, "Halber Tag");
    i18n.add_text(
        Locale::De,
        Key::AbsenceFieldDayFractionFullHint,
        "Verbraucht den vollen Vertrags-Stundensatz pro Tag.",
    );
    i18n.add_text(
        Locale::De,
        Key::AbsenceFieldDayFractionHalfHint,
        "Verbraucht 0,5 Urlaubstage pro Tag im Bereich.",
    );
    i18n.add_text(
        Locale::De,
        Key::AbsencePreviewFooterHalfDay,
        "Bei Halbtag: angezeigte Stunden sind bereits halbiert.",
    );

    // Phase 8.3 — Halbtag-Support (Cutover).
    i18n.add_text(Locale::De, Key::CutoverDriftColDayFraction, "Tageshälfte");
    i18n.add_text(Locale::De, Key::CutoverDriftDayFractionAria, "Tageshälfte für diesen Eintrag wählen");
    i18n.add_text(Locale::De, Key::CutoverBulkDayFractionLabel, "Halb/Ganz für ganze Gruppe");
    i18n.add_text(
        Locale::De,
        Key::CutoverDriftHalfDaySuggestion,
        "Stunden deuten auf Halbtag hin ({amount:.2}h ≈ ½ × Vertragstag).",
    );
    i18n.add_text(Locale::De, Key::CutoverManualConvertDayFractionLabel, "Tageshälfte");
    i18n.add_text(Locale::De, Key::CutoverDayFractionFull, "Ganztag");
    i18n.add_text(Locale::De, Key::CutoverDayFractionHalf, "Halber Tag");
    ```

    **Pitfall-2-Defense:** Jede dieser 13 Zeilen MUSS `Locale::De` als ersten Parameter haben — NICHT `Locale::En`. Lies die Datei nach dem Edit erneut und verifiziere. Plan 08-04 D-26 hat genau diesen Bug verhindert (D-26-SUMMARY referenziert in 08-04-PLAN).

    **Schritt C — `cs.rs`** analog — `Locale::Cs` als erstes Argument:

    ```rust
    // Phase 8.3 — Halbtag-Support (Absence).
    i18n.add_text(Locale::Cs, Key::AbsenceFieldDayFraction, "Část dne");
    i18n.add_text(Locale::Cs, Key::AbsenceDayFractionFull, "Celý den");
    i18n.add_text(Locale::Cs, Key::AbsenceDayFractionHalf, "Půl dne");
    i18n.add_text(
        Locale::Cs,
        Key::AbsenceFieldDayFractionFullHint,
        "Spotřebovává plnou smluvní sazbu za každý den.",
    );
    i18n.add_text(
        Locale::Cs,
        Key::AbsenceFieldDayFractionHalfHint,
        "Spotřebovává 0,5 dne dovolené za každý den v období.",
    );
    i18n.add_text(
        Locale::Cs,
        Key::AbsencePreviewFooterHalfDay,
        "Půldenní: zobrazené hodiny jsou již vydělené dvěma.",
    );

    // Phase 8.3 — Halbtag-Support (Cutover).
    i18n.add_text(Locale::Cs, Key::CutoverDriftColDayFraction, "Část dne");
    i18n.add_text(Locale::Cs, Key::CutoverDriftDayFractionAria, "Zvol část dne pro tuto položku");
    i18n.add_text(Locale::Cs, Key::CutoverBulkDayFractionLabel, "Půl/Celý pro celou skupinu");
    i18n.add_text(
        Locale::Cs,
        Key::CutoverDriftHalfDaySuggestion,
        "Hodiny naznačují půldenní ({amount:.2}h ≈ ½ × smluvní den).",
    );
    i18n.add_text(Locale::Cs, Key::CutoverManualConvertDayFractionLabel, "Část dne");
    i18n.add_text(Locale::Cs, Key::CutoverDayFractionFull, "Celý den");
    i18n.add_text(Locale::Cs, Key::CutoverDayFractionHalf, "Půl dne");
    ```

    **Hinweis Unicode/UTF-8:** Die Strings enthalten Sonderzeichen (`ä`, `ě`, `ů`, `0,5`, `½`). Stelle sicher, dass die Files UTF-8-encoded gespeichert sind (Default in Rust-Code; falls Editor BOM erzwingt → BOM raus).

    **`{amount:.2}h ≈ ½ × ...`-Platzhalter:** Das `{amount:.2}h`-Token ist ein Format-Placeholder, der vom Frontend-Render-Code mit dem aktuellen `amount`-Wert ersetzt wird (Pattern wie bei anderen Hint-Keys mit Platzhaltern). Es ist KEIN i18n-Macro-Interpolations-Syntax — der String wird verbatim hinterlegt, das Frontend setzt's via `format!()` ein. Falls die existing i18n-Infrastruktur einen anderen Placeholder-Stil hat (z. B. `{amount}` statt `{amount:.2}h`), passe alle drei Locales konsistent an.
  </action>
  <verify>
    <automated>
      grep -c "Key::AbsenceFieldDayFraction" shifty-dioxus/src/i18n/en.rs | grep -qx 1 \
      && grep -c "Key::AbsenceFieldDayFraction" shifty-dioxus/src/i18n/de.rs | grep -qx 1 \
      && grep -c "Key::AbsenceFieldDayFraction" shifty-dioxus/src/i18n/cs.rs | grep -qx 1 \
      && grep -c "Key::CutoverDayFractionFull" shifty-dioxus/src/i18n/en.rs | grep -qx 1 \
      && grep -c "Key::CutoverDayFractionFull" shifty-dioxus/src/i18n/de.rs | grep -qx 1 \
      && grep -c "Key::CutoverDayFractionFull" shifty-dioxus/src/i18n/cs.rs | grep -qx 1 \
      && grep -c "\"Tageshälfte\"" shifty-dioxus/src/i18n/de.rs | awk '{ if ($1 >= 2) exit 0; else exit 1 }' \
      && grep -c "\"Day fraction\"" shifty-dioxus/src/i18n/en.rs | awk '{ if ($1 >= 2) exit 0; else exit 1 }' \
      && grep -c "\"Část dne\"" shifty-dioxus/src/i18n/cs.rs | awk '{ if ($1 >= 2) exit 0; else exit 1 }' \
      && grep -c "Locale::En" shifty-dioxus/src/i18n/en.rs | awk '{ if ($1 >= 13) exit 0; else exit 1 }' \
      && grep -c "Locale::De" shifty-dioxus/src/i18n/de.rs | awk '{ if ($1 >= 13) exit 0; else exit 1 }' \
      && grep -c "Locale::Cs" shifty-dioxus/src/i18n/cs.rs | awk '{ if ($1 >= 13) exit 0; else exit 1 }' \
      && ! grep -n "Locale::En" shifty-dioxus/src/i18n/de.rs | grep -i "day_fraction\|halftag\|halbtag\|halber" >/dev/null 2>&1 \
      && ! grep -n "Locale::En" shifty-dioxus/src/i18n/cs.rs | grep -i "day_fraction\|halftag\|halbtag\|halber" >/dev/null 2>&1 \
      && cd shifty-dioxus && cargo build
    </automated>
    Die letzten beiden `! grep`-Checks sind Pitfall-2-Defense: in den `de.rs`/`cs.rs`-Blöcken darf KEIN `Locale::En`-Eintrag in Halbtag-Nähe stehen (eine generelle `grep -v ... | wc -l == 13` würde existierende EN-Einträge fälschlich zählen; diese spezifische Heuristik erkennt nur die neuen Halbtag-Locale-Tag-Verwechslungen).
  </verify>
  <done>
    Alle 13 Keys haben `add_text`-Bodies in allen drei Locale-Files mit korrektem Locale-Tag-First-Argument. Sonderzeichen (`ä`, `ě`, `ů`, `½`) sind UTF-8-encoded. `cd shifty-dioxus && cargo build` exit 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 2 Presence-Tests + 6 Reference-Matcher-Tests in i18n/mod.rs</name>
  <files>shifty-dioxus/src/i18n/mod.rs</files>
  <behavior>
    - Presence-Test 1: `i18n_absence_day_fraction_keys_present_in_all_locales` — alle 6 Absence-Halbtag-Keys × 3 Locales sind non-empty und ≠ "??"
    - Presence-Test 2: `i18n_cutover_day_fraction_keys_present_in_all_locales` — alle 7 Cutover-Halbtag-Keys × 3 Locales sind non-empty und ≠ "??"
    - Reference-Matcher 1: `i18n_absence_day_fraction_match_german_reference` — `generate(Locale::De)` liefert für jeden Absence-Halbtag-Key den exakten De-Referenz-String
    - Reference-Matcher 2: `i18n_absence_day_fraction_match_english_reference` — `generate(Locale::En)` liefert exakte En-Referenz-Strings
    - Reference-Matcher 3: `i18n_absence_day_fraction_match_czech_reference` — `generate(Locale::Cs)` liefert exakte Cs-Referenz-Strings
    - Reference-Matcher 4-6: analog für Cutover-Block
  </behavior>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (existing Presence-Test Z. 680-721, existing Reference-Matcher Z. 723-755; nach Plan 01 sind die 13 Key-Enum-Slots bereits angelegt)
    - shifty-dioxus/src/i18n/de.rs (nach Task 1 — alle De-Bodies vorhanden)
    - shifty-dioxus/src/i18n/en.rs (nach Task 1 — alle En-Bodies vorhanden)
    - shifty-dioxus/src/i18n/cs.rs (nach Task 1 — alle Cs-Bodies vorhanden)
  </read_first>
  <action>
    Im `#[cfg(test)] mod tests { ... }` (oder ähnlich strukturierter Test-Modul-Bereich) von `shifty-dioxus/src/i18n/mod.rs`, am Ende der existierenden i18n-Tests:

    ```rust
    // ===== Phase 8.3 — Halbtag-Support i18n Tests =====

    #[test]
    fn i18n_absence_day_fraction_keys_present_in_all_locales() {
        // FUI-A-09 — alle 3 Locales (De / En / Cs) müssen alle 6 Absence-
        // Halbtag-Keys decken (non-empty + ≠ "??").
        for locale in [Locale::En, Locale::De, Locale::Cs] {
            let i18n = generate(locale);
            for key in [
                Key::AbsenceFieldDayFraction,
                Key::AbsenceDayFractionFull,
                Key::AbsenceDayFractionHalf,
                Key::AbsenceFieldDayFractionFullHint,
                Key::AbsenceFieldDayFractionHalfHint,
                Key::AbsencePreviewFooterHalfDay,
            ] {
                let value = i18n.t(key);
                assert!(
                    !value.is_empty() && value.as_ref() != "??",
                    "missing translation for {:?} in {:?}: got `{}`",
                    key, locale, value
                );
            }
        }
    }

    #[test]
    fn i18n_cutover_day_fraction_keys_present_in_all_locales() {
        // FUI-A-09 — alle 3 Locales × alle 7 Cutover-Halbtag-Keys.
        for locale in [Locale::En, Locale::De, Locale::Cs] {
            let i18n = generate(locale);
            for key in [
                Key::CutoverDriftColDayFraction,
                Key::CutoverDriftDayFractionAria,
                Key::CutoverBulkDayFractionLabel,
                Key::CutoverDriftHalfDaySuggestion,
                Key::CutoverManualConvertDayFractionLabel,
                Key::CutoverDayFractionFull,
                Key::CutoverDayFractionHalf,
            ] {
                let value = i18n.t(key);
                assert!(
                    !value.is_empty() && value.as_ref() != "??",
                    "missing translation for {:?} in {:?}: got `{}`",
                    key, locale, value
                );
            }
        }
    }

    #[test]
    fn i18n_absence_day_fraction_match_german_reference() {
        // Pitfall-2 guard: ensures the de.rs block uses Locale::De
        // (not accidentally Locale::En, which would still type-check but
        // route English copy through generate(Locale::De)). Pattern aus
        // Plan 08-04 D-26.
        let i18n = generate(Locale::De);
        assert_eq!(i18n.t(Key::AbsenceFieldDayFraction).as_ref(), "Tageshälfte");
        assert_eq!(i18n.t(Key::AbsenceDayFractionFull).as_ref(), "Ganztag");
        assert_eq!(i18n.t(Key::AbsenceDayFractionHalf).as_ref(), "Halber Tag");
        assert_eq!(
            i18n.t(Key::AbsenceFieldDayFractionFullHint).as_ref(),
            "Verbraucht den vollen Vertrags-Stundensatz pro Tag."
        );
        assert_eq!(
            i18n.t(Key::AbsenceFieldDayFractionHalfHint).as_ref(),
            "Verbraucht 0,5 Urlaubstage pro Tag im Bereich."
        );
        assert_eq!(
            i18n.t(Key::AbsencePreviewFooterHalfDay).as_ref(),
            "Bei Halbtag: angezeigte Stunden sind bereits halbiert."
        );
    }

    #[test]
    fn i18n_absence_day_fraction_match_english_reference() {
        let i18n = generate(Locale::En);
        assert_eq!(i18n.t(Key::AbsenceFieldDayFraction).as_ref(), "Day fraction");
        assert_eq!(i18n.t(Key::AbsenceDayFractionFull).as_ref(), "Full day");
        assert_eq!(i18n.t(Key::AbsenceDayFractionHalf).as_ref(), "Half day");
        assert_eq!(
            i18n.t(Key::AbsenceFieldDayFractionFullHint).as_ref(),
            "Uses the full contract-hours rate per day."
        );
        assert_eq!(
            i18n.t(Key::AbsenceFieldDayFractionHalfHint).as_ref(),
            "Uses 0.5 vacation days per day in the range."
        );
        assert_eq!(
            i18n.t(Key::AbsencePreviewFooterHalfDay).as_ref(),
            "Half-day: shown hours are already halved."
        );
    }

    #[test]
    fn i18n_absence_day_fraction_match_czech_reference() {
        let i18n = generate(Locale::Cs);
        assert_eq!(i18n.t(Key::AbsenceFieldDayFraction).as_ref(), "Část dne");
        assert_eq!(i18n.t(Key::AbsenceDayFractionFull).as_ref(), "Celý den");
        assert_eq!(i18n.t(Key::AbsenceDayFractionHalf).as_ref(), "Půl dne");
        assert_eq!(
            i18n.t(Key::AbsenceFieldDayFractionFullHint).as_ref(),
            "Spotřebovává plnou smluvní sazbu za každý den."
        );
        assert_eq!(
            i18n.t(Key::AbsenceFieldDayFractionHalfHint).as_ref(),
            "Spotřebovává 0,5 dne dovolené za každý den v období."
        );
        assert_eq!(
            i18n.t(Key::AbsencePreviewFooterHalfDay).as_ref(),
            "Půldenní: zobrazené hodiny jsou již vydělené dvěma."
        );
    }

    #[test]
    fn i18n_cutover_day_fraction_match_german_reference() {
        let i18n = generate(Locale::De);
        assert_eq!(i18n.t(Key::CutoverDriftColDayFraction).as_ref(), "Tageshälfte");
        assert_eq!(
            i18n.t(Key::CutoverDriftDayFractionAria).as_ref(),
            "Tageshälfte für diesen Eintrag wählen"
        );
        assert_eq!(
            i18n.t(Key::CutoverBulkDayFractionLabel).as_ref(),
            "Halb/Ganz für ganze Gruppe"
        );
        assert_eq!(
            i18n.t(Key::CutoverDriftHalfDaySuggestion).as_ref(),
            "Stunden deuten auf Halbtag hin ({amount:.2}h ≈ ½ × Vertragstag)."
        );
        assert_eq!(i18n.t(Key::CutoverManualConvertDayFractionLabel).as_ref(), "Tageshälfte");
        assert_eq!(i18n.t(Key::CutoverDayFractionFull).as_ref(), "Ganztag");
        assert_eq!(i18n.t(Key::CutoverDayFractionHalf).as_ref(), "Halber Tag");
    }

    #[test]
    fn i18n_cutover_day_fraction_match_english_reference() {
        let i18n = generate(Locale::En);
        assert_eq!(i18n.t(Key::CutoverDriftColDayFraction).as_ref(), "Day fraction");
        assert_eq!(
            i18n.t(Key::CutoverDriftDayFractionAria).as_ref(),
            "Pick day fraction for this entry"
        );
        assert_eq!(
            i18n.t(Key::CutoverBulkDayFractionLabel).as_ref(),
            "Half/Full for whole group"
        );
        assert_eq!(
            i18n.t(Key::CutoverDriftHalfDaySuggestion).as_ref(),
            "Hours suggest half-day ({amount:.2}h ≈ ½ × contract day)."
        );
        assert_eq!(i18n.t(Key::CutoverManualConvertDayFractionLabel).as_ref(), "Day fraction");
        assert_eq!(i18n.t(Key::CutoverDayFractionFull).as_ref(), "Full day");
        assert_eq!(i18n.t(Key::CutoverDayFractionHalf).as_ref(), "Half day");
    }

    #[test]
    fn i18n_cutover_day_fraction_match_czech_reference() {
        let i18n = generate(Locale::Cs);
        assert_eq!(i18n.t(Key::CutoverDriftColDayFraction).as_ref(), "Část dne");
        assert_eq!(
            i18n.t(Key::CutoverDriftDayFractionAria).as_ref(),
            "Zvol část dne pro tuto položku"
        );
        assert_eq!(
            i18n.t(Key::CutoverBulkDayFractionLabel).as_ref(),
            "Půl/Celý pro celou skupinu"
        );
        assert_eq!(
            i18n.t(Key::CutoverDriftHalfDaySuggestion).as_ref(),
            "Hodiny naznačují půldenní ({amount:.2}h ≈ ½ × smluvní den)."
        );
        assert_eq!(i18n.t(Key::CutoverManualConvertDayFractionLabel).as_ref(), "Část dne");
        assert_eq!(i18n.t(Key::CutoverDayFractionFull).as_ref(), "Celý den");
        assert_eq!(i18n.t(Key::CutoverDayFractionHalf).as_ref(), "Půl dne");
    }
    ```

    Hinweis: Sollte das existing Test-Modul nicht im selben File `i18n/mod.rs` liegen, sondern in einem `i18n/tests.rs`-Submodul, ergänze dort. Die 8 Tests müssen sichtbar als top-level `#[test]`-Funktionen über `cargo test -p shifty-dioxus i18n_*_day_fraction_*` ausführbar sein.
  </action>
  <verify>
    <automated>
      grep -c "i18n_absence_day_fraction_keys_present_in_all_locales" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_cutover_day_fraction_keys_present_in_all_locales" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_absence_day_fraction_match_german_reference" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_absence_day_fraction_match_english_reference" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_absence_day_fraction_match_czech_reference" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_cutover_day_fraction_match_german_reference" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_cutover_day_fraction_match_english_reference" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && grep -c "i18n_cutover_day_fraction_match_czech_reference" shifty-dioxus/src/i18n/mod.rs | grep -qx 1 \
      && cd shifty-dioxus && cargo test i18n_absence_day_fraction \
      && cd shifty-dioxus && cargo test i18n_cutover_day_fraction \
      && cd shifty-dioxus && cargo build --target wasm32-unknown-unknown
    </automated>
    Hinweis: Bei WASM-Build-Gate ggf. `nix-shell -p openssl pkg-config lld --command "cargo build --target wasm32-unknown-unknown"` (Pattern aus 08.2-02-Plan); auf NixOS-Setup adaptieren falls plain `cargo build --target wasm32-unknown-unknown` aus Toolchain-Gründen fehlschlägt (siehe Memory `reference_local_dev_commands.md`).
  </verify>
  <done>
    8 neue Tests (2 Presence + 6 Reference-Matcher) deklariert und alle grün. WASM-Build der shifty-dioxus-Crate grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n-Tabelle → Frontend-Render | User sieht Strings basierend auf Locale; alle Strings sind in der Codebase hardcoded (kein User-Supply) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-10 | I (Information Disclosure) | i18n-Strings — User-supplied content? | accept | KEIN user-supplied content in den neuen Strings. Alle 13 Texte sind statisch in der Codebase. `{amount:.2}h`-Placeholder wird vom Frontend-Render-Code via `format!()` mit numerischen Werten (f32) ersetzt — kein Format-String-Injection-Risiko (Rust-`format!()` ist compile-time-checked). |
| T-08.3-11 | T (Tampering) | Locale-Tag-Verwechslung (`Locale::En` in `de.rs`) | mitigate | 6 Reference-Matcher-Tests (Pitfall-2-Guard aus Plan 08-04 D-26) blockieren das mechanisch. Jeder Test verwendet `generate(Locale::X)` und assertet die X-Referenz-Strings — eine Tag-Verwechslung würde die Strings durcheinander bringen und sofort fehlschlagen. |
</threat_model>

<verification>
**Plan-Level-Check nach Task 2:**

```bash
cd shifty-dioxus
cargo test i18n_absence_day_fraction
cargo test i18n_cutover_day_fraction
cargo test i18n   # alle existing i18n-Tests bleiben grün
cargo build --target wasm32-unknown-unknown
```

Wenn ein Presence-Test fehlschlägt → `add_text`-Body in einem Locale fehlt oder Locale-Tag falsch (Pitfall 2).
Wenn ein Reference-Matcher fehlschlägt → String stimmt nicht 1:1 mit UI-SPEC-Referenz (auch typischer Pitfall-2-Indikator).
</verification>

<success_criteria>
- [ ] 13 `add_text`-Bodies in `shifty-dioxus/src/i18n/en.rs` (`Locale::En`)
- [ ] 13 `add_text`-Bodies in `shifty-dioxus/src/i18n/de.rs` (`Locale::De`)
- [ ] 13 `add_text`-Bodies in `shifty-dioxus/src/i18n/cs.rs` (`Locale::Cs`)
- [ ] Alle Sonderzeichen (`ä`, `ě`, `ů`, `½`, `≈`) UTF-8-encoded und visuell korrekt
- [ ] 2 Presence-Tests (`i18n_absence_day_fraction_keys_present_in_all_locales`, `i18n_cutover_day_fraction_keys_present_in_all_locales`) grün
- [ ] 6 Reference-Matcher-Tests (3 Absence × 3 Locales + 3 Cutover × 3 Locales) grün — Pitfall-2-Guard
- [ ] `cd shifty-dioxus && cargo test i18n_*_day_fraction_*` exit 0
- [ ] `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` exit 0
- [ ] Alle existing i18n-Tests (außerhalb der Halbtag-Tests) bleiben grün
</success_criteria>

<output>
Nach Plan-Abschluss `.planning/phases/08.3-halbtag-support-f-r-absences/08.3-03-SUMMARY.md` mit:
- 13 × 3 = 39 neue `add_text`-Calls; 8 neue Tests
- Bestätigung dass kein `Locale::En` in `de.rs`/`cs.rs`-Halbtag-Block (Pitfall-2-Audit)
- WASM-Build-Gate Output (Exit-Code 0)
- VCS: EIN jj-commit `feat(08.3-03): add 13 i18n keys for half-day support across de/en/cs + 8 i18n tests` (FUI-A-09 verlangt atomicity)
</output>
