---
phase: 08.3
plan: 05
type: execute
wave: 3
depends_on: [08.3-01, 08.3-02]
files_modified:
  - rest-types/src/lib.rs
  - service/src/cutover.rs
  - service_impl/src/cutover.rs
  - rest/src/cutover.rs
  - service_impl/src/test/cutover.rs
  - shifty_bin/src/integration_test/cutover.rs
autonomous: true
requirements: [FUI-A-10]
requirements_addressed: [FUI-A-10]
tags: [cutover, backend, day-fraction, convert-quarantine, bulk-convert, manual-range, rest, integration-test]

must_haves:
  truths:
    - "`CutoverConvertQuarantineEntryRequest.day_fraction: Option<DayFractionTO>` mit `#[serde(default)]` ergänzt — backwards-compat: alte Clients ohne das Feld konvertieren als `Full` (CONTEXT.md no-drift)."
    - "`CutoverBulkConvertQuarantineRowsRequest.day_fraction: Option<DayFractionTO>` mit `#[serde(default)]` ergänzt — gilt einheitlich für alle Rows der Bulk-Operation."
    - "`service::cutover::CutoverService::convert_quarantine_entry`-Trait-Signatur erweitert um `day_fraction: Option<crate::absence::DayFraction>` als separater Parameter (per PATTERNS.md Z. 423-429 Empfehlung c — separater Parameter, weil sowohl Heuristic- als auch Manual-Pfad das Feld brauchen)."
    - "`service::cutover::CutoverService::bulk_convert_quarantine_rows`-Trait-Signatur erweitert um `day_fraction: Option<crate::absence::DayFraction>`."
    - "`detect_weekly_lump_sum` BLEIBT UNVERÄNDERT (Signatur `Option<(Date, Date)>` — KEIN Auto-Vorschlag-Branch). Pro CONTEXT.md OQ-2 und PATTERNS.md Z. 483 Empfehlung: Heuristik liefert immer `Full`, Operator entscheidet im Frontend. Frontend zeigt einen `CutoverDriftHalfDaySuggestion`-Hint wenn die Heuristik anschlägt; Backend-Default ist `Full`."
    - "`convert_quarantine_entry`-Impl Step 6 (AbsencePeriodEntity-Konstruktion Z. 529-543) erweitert um `day_fraction: day_fraction.map(|f| (&f).into()).unwrap_or(DayFractionEntity::Full)`."
    - "`bulk_convert_quarantine_rows`-Impl analog erweitert (alle erzeugten AbsencePeriodEntities derselben Bulk-Operation teilen denselben `day_fraction`-Wert)."
    - "REST-Handler `cutover_convert_quarantine_entry_handler` und `cutover_bulk_convert_quarantine_rows_handler` extrahieren `day_fraction` aus dem Request-Body und reichen es als `Option<service::absence::DayFraction>` an den Service durch."
    - "`ManualRange`-Domain-Struct BLEIBT UNVERÄNDERT (`day_fraction` ist separater Parameter, nicht im Range — PATTERNS.md Z. 423-429 Empfehlung c)."
    - "Existing Phase-8.1/8.2-Tests (`manual_range_resolves_karin_quarantine`, alle weekly_lump_sum-Tests) bleiben grün — `day_fraction` neuer Parameter ist `None`-Default für alte Call-Sites."
    - "2 neue Tests: (a) `convert_quarantine_entry_with_half_day_persists_day_fraction` — Mockall: Heiligabend-Pattern (4h Vacation, 8h-Vertrag, manual_range=Some, day_fraction=Some(Half)) → AbsencePeriodEntity.day_fraction == Half persistiert; (b) `bulk_convert_quarantine_rows_with_half_applies_to_all_rows` — Mockall: 2 Rows, day_fraction=Some(Half) → BEIDE created Entities haben Half."
    - "1 REST-Integration-Test `convert_with_day_fraction_via_rest` in `shifty_bin/src/integration_test/cutover.rs` — Karin-ähnliche Setup, POST /admin/cutover/convert-quarantine-entry mit `{ extra_hours_id, day_fraction: \"Half\" }`, Response 200, gegen-validate das geschriebene `absence_period` via DAO-Read."
    - "OpenAPI-Surface-Test bleibt grün (Cutover-DTOs sind transitiv durch das neue `Option<DayFractionTO>`-Feld erweitert — `DayFractionTO` ist bereits in EXPECTED_SCHEMAS aus Plan 01)."
    - "`cargo test --workspace` exit 0 (alle Backend-Tests grün — Frontend kommt in Plan 06)."
  artifacts:
    - path: "rest-types/src/lib.rs"
      provides: "CutoverConvertQuarantineEntryRequest + CutoverBulkConvertQuarantineRowsRequest mit day_fraction: Option<DayFractionTO> + #[serde(default)] + 2 erweiterte Roundtrip-Tests"
    - path: "service/src/cutover.rs"
      provides: "convert_quarantine_entry + bulk_convert_quarantine_rows Trait-Signaturen erweitert um day_fraction: Option<DayFraction>"
    - path: "service_impl/src/cutover.rs"
      provides: "convert_quarantine_entry Step 6 + bulk_convert_quarantine_rows: AbsencePeriodEntity.day_fraction-Feld aus Service-Parameter"
    - path: "rest/src/cutover.rs"
      provides: "Field-Forwarding day_fraction: req.day_fraction.as_ref().map(...) in beiden REST-Handlern"
    - path: "service_impl/src/test/cutover.rs"
      provides: "2 neue Mockall-Tests für day_fraction-Persistierung (single + bulk)"
      min_lines: 100
    - path: "shifty_bin/src/integration_test/cutover.rs"
      provides: "convert_with_day_fraction_via_rest — REST-Roundtrip-Test gegen In-Memory-SQLite"
      min_lines: 50
  key_links:
    - from: "rest-types::CutoverConvertQuarantineEntryRequest"
      to: "service::cutover::CutoverService::convert_quarantine_entry"
      via: "REST-Handler-Field-Forwarding mit Option<DayFractionTO> -> Option<DayFraction>"
      pattern: "day_fraction:\\s*req\\.day_fraction\\.as_ref\\(\\)\\.map"
    - from: "service_impl::cutover::CutoverServiceImpl::convert_quarantine_entry (Step 6)"
      to: "absence_dao.create (AbsencePeriodEntity-Insert)"
      via: "Entity-Konstruktion mit day_fraction aus Service-Parameter, Default Full"
      pattern: "day_fraction:\\s*day_fraction\\.map"
---

<vcs_jj_only>
**VCS-Politik (jj-only) — verbindlich:** KEINE `git commit`/`git add`-Mutations. Verwende `jj`. Convention für diese Phase: `feat(08.3-05): thread day_fraction through CutoverService convert + bulk-convert`, `test(08.3-05): add half-day cutover tests + REST integration test`. Trait-Signatur-Änderung und Impl gehören in den gleichen jj-commit (sonst bricht der Build dazwischen).
</vcs_jj_only>

## Plan-Phase decisions

- **D-08.3-06** (resolves CONTEXT OQ-2 / Cutover-Auto-Vorschlag): Backend bleibt passiv. `detect_weekly_lump_sum` und alle Heuristik-Code-Pfade in `service_impl/src/cutover.rs` (Z. 1518-1579 + Call-Sites Z. 513, 676, 864) bleiben UNVERÄNDERT — keine Halbtag-Heuristik im Backend, kein Auto-Vorschlag-Branch. Bei fehlendem `day_fraction`-Parameter im Request defaultet der Service zu `Full` (via `unwrap_or(DayFractionEntity::Full)`). Frontend (Plan 06) zeigt einen visuellen Hint (`CutoverDriftHalfDaySuggestion`) wenn `(amount - contract_hours_per_day * 0.5).abs() < 0.5`; Operator entscheidet explizit per Toggle. Begründung: minimale Backend-Risikooberfläche; klare Operator-Verantwortung; konsistent mit dem "no surprise, no implicit"-Prinzip aus Phase 8.1/8.2 Cutover-Pattern.

- **D-08.3-07** (resolves CONTEXT OQ-4 + OQ-5 / Range + Conflict in Cutover-Kontext): Halbtag-Konsistenz auch in Cutover — bulk_convert_quarantine_rows applied `day_fraction` einheitlich auf ALLE konvertierten AbsencePeriods derselben Operation (siehe Test `bulk_convert_quarantine_rows_with_half_applies_to_all_rows`). Range-Verhalten bei `manual_range` + `Half` ist konsistent zu D-08.3-04 (uniform per-day halving). Konflikt-Warning-Suppression (D-08.3-05) ist Plan-04-Zuständigkeit (`shiftplan_edit`-Layer), nicht Cutover-Zuständigkeit — Cutover persistiert nur das Feld, ohne die Booking-Konflikt-Warnung selbst zu evaluieren.

<objective>
Erweitere den Cutover-Backend-Pfad (`convert_quarantine_entry` + `bulk_convert_quarantine_rows`) um einen optionalen `day_fraction`-Parameter, der auf das neue `AbsencePeriodEntity.day_fraction`-Feld persistiert wird. Backwards-compat: alte Clients (vor 8.3) ohne das Feld konvertieren weiter als `Full` (CONTEXT.md no-drift). Heuristik (`detect_weekly_lump_sum`) bleibt unverändert — Plan-Phase-Entscheidung: Operator entscheidet im Frontend, Backend ist passiv. Frontend (Plan 06) zeigt den `CutoverDriftHalfDaySuggestion`-Hint wenn die Heuristik visuell anschlägt.

Purpose: Cutover ist der Pfad, über den alte halbtägige Vacation-Einträge (Heiligabend, Silvester aus `extra_hours`-Legacy) ins neue `absence_period`-Schema überführt werden. Ohne `day_fraction`-Threading würden alle migrierten Halbtage als Volltage in der neuen Welt landen — die Phase wäre gescheitert.

Output: 6 modifizierte Source-Files (DTO + Trait + Impl + REST + 2 Tests); alle existing Cutover-Tests bleiben grün; 2 neue Mockall-Tests + 1 REST-Integration-Test verifizieren das Threading.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-CONTEXT.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-RESEARCH.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-01-PLAN.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-02-PLAN.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-04-PLAN.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-01-SUMMARY.md
@.planning/phases/08.1-cutover-migration-ui/08.1-02-SUMMARY.md

@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Bestehende Cutover-Signaturen aus dem Live-Code -->

From service/src/cutover.rs (Z. 272-294 — current convert_quarantine_entry trait):
```rust
async fn convert_quarantine_entry(
    &self,
    extra_hours_id: Uuid,
    manual_range: Option<ManualRange>,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<ConvertQuarantineEntryOutcome, ServiceError>;
```

From service/src/cutover.rs (Z. ~285-294 — current bulk_convert_quarantine_rows trait):
```rust
async fn bulk_convert_quarantine_rows(
    &self,
    sales_person_id: Uuid,
    category: crate::absence::AbsenceCategory,
    year: u32,
    explicit_ids: Option<Arc<[Uuid]>>,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<BulkConvertQuarantineRowsOutcome, ServiceError>;
```

From rest-types/src/lib.rs (Z. 2339-2370 — current Cutover-Request-DTOs after 8.2):
```rust
pub struct CutoverConvertQuarantineEntryRequest {
    pub extra_hours_id: Uuid,
    #[serde(default)]
    pub manual_range: Option<ManualRangeTO>,
}

pub struct CutoverBulkConvertQuarantineRowsRequest {
    pub sales_person_id: Uuid,
    pub category: AbsenceCategoryTO,
    pub year: u32,
    #[serde(default)]
    pub extra_hours_ids: Option<Vec<Uuid>>,
}
```

From service_impl/src/cutover.rs (Z. 529-543 — current AbsencePeriodEntity Konstruktion in convert):
```rust
let absence_period_id = Uuid::new_v4();
let entity = AbsencePeriodEntity {
    id: absence_period_id,
    logical_id: absence_period_id,
    sales_person_id: row.sales_person_id,
    category: extra_hours_category_to_absence(&row.category),
    from_date,
    to_date,
    description: Arc::from(""),
    created: migrated_at,
    deleted: None,
    version: Uuid::new_v4(),
};
```

From .planning/phases/08.2-manual-range-convert-quarantine/08.2-01-SUMMARY.md:
Karin-Pattern + ManualRange-Branch sind in 8.2 etabliert; 8.3 erweitert orthogonal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DTOs + Service-Trait + REST-Handler erweitern (Wire-Surface-First)</name>
  <files>
    rest-types/src/lib.rs,
    service/src/cutover.rs,
    rest/src/cutover.rs
  </files>
  <read_first>
    - rest-types/src/lib.rs (Z. 2323-2390 — Cutover-Request-DTOs nach 8.2 Plan 01)
    - service/src/cutover.rs (Z. 190-300 — alle Cutover-Trait-Methoden + ManualRange-Struct)
    - rest/src/cutover.rs (Z. 190-300 — cutover_convert_quarantine_entry_handler und bulk-Pendant; insbesondere die Field-Forwarding-Stellen aus 8.2 für manual_range)
    - rest/tests/openapi_surface.rs (nach Plan 01 — DayFractionTO ist bereits in EXPECTED_SCHEMAS)
    - .planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md (Z. 568-635 — DTO + REST-Pattern; Z. 404-484 — Service-Trait + ManualRange-Pattern)
    - .planning/phases/08.2-manual-range-convert-quarantine/08.2-01-PLAN.md (als Format-Vorlage für die Surface-First-Threading-Sequenz)
  </read_first>
  <action>
    **Schritt A — `CutoverConvertQuarantineEntryRequest` in `rest-types/src/lib.rs`** (Z. 2339-2343):

    ```rust
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct CutoverConvertQuarantineEntryRequest {
        pub extra_hours_id: Uuid,
        #[serde(default)]
        pub manual_range: Option<ManualRangeTO>,
        // Phase 8.3: optionale Tageshälfte. None / Default = Full (no-drift).
        #[serde(default)]
        pub day_fraction: Option<DayFractionTO>,
    }
    ```

    **Schritt B — `CutoverBulkConvertQuarantineRowsRequest`** (Z. 2364-2370):

    ```rust
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct CutoverBulkConvertQuarantineRowsRequest {
        pub sales_person_id: Uuid,
        pub category: AbsenceCategoryTO,
        pub year: u32,
        #[serde(default)]
        pub extra_hours_ids: Option<Vec<Uuid>>,
        // Phase 8.3: gilt einheitlich für alle Rows der Bulk-Operation.
        #[serde(default)]
        pub day_fraction: Option<DayFractionTO>,
    }
    ```

    **Schritt C — Roundtrip-Tests in rest-types** erweitern bzw. ergänzen. Suche existing `cutover_convert_dto_tests` und ergänze:

    ```rust
    #[cfg(test)]
    #[test]
    fn cutover_convert_quarantine_entry_request_roundtrip_with_day_fraction_half() {
        let req = CutoverConvertQuarantineEntryRequest {
            extra_hours_id: Uuid::nil(),
            manual_range: None,
            day_fraction: Some(DayFractionTO::Half),
        };
        let json = serde_json::to_string(&req).expect("serialize");
        assert!(json.contains("\"day_fraction\":\"Half\""), "json was: {}", json);
        let parsed: CutoverConvertQuarantineEntryRequest = serde_json::from_str(&json).expect("deserialize");
        assert_eq!(parsed.day_fraction, Some(DayFractionTO::Half));
    }

    #[cfg(test)]
    #[test]
    fn cutover_convert_quarantine_entry_request_omits_day_fraction_backwards_compat() {
        // Alte Clients ohne day_fraction-Feld werden mit Option::None deserialisiert.
        let json = r#"{"extra_hours_id":"00000000-0000-0000-0000-000000000000"}"#;
        let parsed: CutoverConvertQuarantineEntryRequest = serde_json::from_str(json).expect("deserialize without day_fraction");
        assert_eq!(parsed.day_fraction, None);
        assert_eq!(parsed.manual_range, None);
    }
    ```

    **Schritt D — `service::cutover::CutoverService::convert_quarantine_entry`-Trait** (service/src/cutover.rs Z. 272-294):

    ```rust
    /// Convert a single quarantined extra_hours row into an `absence_period`
    /// in one atomic Tx (D-01). Range derived via `detect_weekly_lump_sum`
    /// heuristic (Plan 08-09); frontend never supplies dates UNLESS
    /// `manual_range` is set (Phase 8.2, D-29). Privilege: `cutover_admin`.
    ///
    /// Phase 8.2: optional `manual_range` skipt die Heuristik.
    /// Phase 8.3: optional `day_fraction` setzt die Tageshälfte auf dem
    /// resultierenden `absence_period`. `None` / Default = `Full` (no-drift).
    /// Gilt orthogonal zu `manual_range`: beide Pfade (heuristic + manual)
    /// akzeptieren `day_fraction`.
    async fn convert_quarantine_entry(
        &self,
        extra_hours_id: Uuid,
        manual_range: Option<ManualRange>,
        day_fraction: Option<crate::absence::DayFraction>,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<ConvertQuarantineEntryOutcome, ServiceError>;
    ```

    **WICHTIG — Parameter-Reihenfolge:** `day_fraction` zwischen `manual_range` und `context` ist die Konvention für Phase 8.3. Eine andere Reihenfolge ist erlaubt, solange sie konsistent durchgezogen wird (Impl, alle existing Call-Sites, Mock).

    **Schritt E — `service::cutover::CutoverService::bulk_convert_quarantine_rows`-Trait** analog:

    ```rust
    async fn bulk_convert_quarantine_rows(
        &self,
        sales_person_id: Uuid,
        category: crate::absence::AbsenceCategory,
        year: u32,
        explicit_ids: Option<Arc<[Uuid]>>,
        day_fraction: Option<crate::absence::DayFraction>,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<BulkConvertQuarantineRowsOutcome, ServiceError>;
    ```

    **Hinweis Mockall-Update:** Das `#[automock]`-Attribut auf dem Trait generiert `MockCutoverService` automatisch — die Mock-Methoden-Signaturen passen sich an. Alle existing Test-Call-Sites müssen den neuen `day_fraction: None`-Parameter ergänzen, das ist Teil von Task 2.

    **`ManualRange`-Struct BLEIBT UNVERÄNDERT** (Z. 196-211) — `day_fraction` ist separater Service-Parameter, nicht im Range-Struct (PATTERNS.md Z. 423-429 Empfehlung c). Begründung: auch der Heuristic-Pfad (ohne `manual_range`) braucht `day_fraction`.

    **Schritt F — `rest/src/cutover.rs`** — Handler-Field-Forwarding für beide Endpoints:

    **F.1 — `cutover_convert_quarantine_entry_handler`** (Z. 190-242). Suche die existing Service-Call-Site (Z. 229-232) und erweitere sie:

    ```rust
    let day_fraction: Option<service::absence::DayFraction> = req
        .day_fraction
        .as_ref()
        .map(service::absence::DayFraction::from);

    let outcome = rest_state
        .cutover_service()
        .convert_quarantine_entry(
            req.extra_hours_id,
            manual_range,
            day_fraction,
            context.into(),
            None,
        )
        .await?;
    ```

    Pre-existing `manual_range`-Mapping (aus 8.2) bleibt unverändert.

    **F.2 — `cutover_bulk_convert_quarantine_rows_handler`** (Z. 265-299) analog:

    ```rust
    let day_fraction: Option<service::absence::DayFraction> = req
        .day_fraction
        .as_ref()
        .map(service::absence::DayFraction::from);

    let outcome = rest_state
        .cutover_service()
        .bulk_convert_quarantine_rows(
            req.sales_person_id,
            (&req.category).into(),
            req.year,
            req.extra_hours_ids.map(|ids| ids.into_iter().collect::<Arc<[_]>>()),
            day_fraction,
            context.into(),
            None,
        )
        .await?;
    ```

    Beachte: `extra_hours_ids.map(...)` muss zur existing Bulk-Handler-Konvertierungs-Logik passen (typischerweise `Option<Vec<Uuid>>` → `Option<Arc<[Uuid]>>` per `.map(|v| Arc::from(v.as_slice()))` oder ähnlich — kopiere aus der existing Live-Code-Stelle).

    **F.3 — `utoipa::path`-Annotationen** brauchen KEINE Änderung — `DayFractionTO` ist transitiv durch die Request-Struct eingebunden.

    **Sanity-Build:**
    ```bash
    cargo build -p rest-types
    cargo build -p service
    cargo build -p rest
    # service_impl wird in Task 2 noch nicht kompilieren (Trait-Signatur erweitert, Impl noch alt)
    ```
  </action>
  <verify>
    <automated>
      grep -c "pub day_fraction: Option<DayFractionTO>" rest-types/src/lib.rs | awk '{ if ($1 >= 2) exit 0; else exit 1 }' \
      && grep -c "cutover_convert_quarantine_entry_request_roundtrip_with_day_fraction_half" rest-types/src/lib.rs | grep -qx 1 \
      && grep -c "cutover_convert_quarantine_entry_request_omits_day_fraction_backwards_compat" rest-types/src/lib.rs | grep -qx 1 \
      && grep -cE "day_fraction:\s*Option<crate::absence::DayFraction>" service/src/cutover.rs | awk '{ if ($1 >= 2) exit 0; else exit 1 }' \
      && grep -c "day_fraction: req.day_fraction.as_ref().map" rest/src/cutover.rs | awk '{ if ($1 >= 1) exit 0; else exit 1 }' \
      && cargo build -p rest-types \
      && cargo build -p service \
      && cargo test -p rest-types cutover_convert_quarantine_entry_request_
    </automated>
    Hinweis: `cargo build -p rest` schlägt voraussichtlich fehl, weil `service_impl::CutoverServiceImpl` noch die alte Trait-Signatur implementiert. Das wird in Task 2 gelöst. Sequenziell: erst Trait erweitern → dann Impl. Build wird zwischen Task 1 und Task 2 vorübergehend rot.
  </verify>
  <done>
    Beide Cutover-Request-DTOs haben `day_fraction: Option<DayFractionTO>` mit `#[serde(default)]`. 2 neue Roundtrip-Tests grün. Beide Service-Traits haben `day_fraction: Option<DayFraction>` als Parameter. Beide REST-Handler forwarden das Feld. rest-types- und service-Crates compilen einzeln.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service-Impl erweitern + alle existing Call-Sites + 2 neue Mockall-Tests + 1 REST-Integration-Test</name>
  <files>
    service_impl/src/cutover.rs,
    service_impl/src/test/cutover.rs,
    shifty_bin/src/integration_test/cutover.rs
  </files>
  <behavior>
    - convert_quarantine_entry mit day_fraction = Some(Half) persistiert AbsencePeriodEntity mit DayFractionEntity::Half
    - convert_quarantine_entry mit day_fraction = None persistiert AbsencePeriodEntity mit DayFractionEntity::Full (backwards-compat)
    - bulk_convert_quarantine_rows mit day_fraction = Some(Half) → ALLE konvertierten Entities haben Half
    - REST POST mit Body { extra_hours_id, day_fraction: "Half" } → 200, DB hat day_fraction='half'
    - Existing Tests (Karin, weekly-lump-sum) bleiben grün mit day_fraction = None Default
  </behavior>
  <read_first>
    - service_impl/src/cutover.rs (Z. 413-600 — convert_quarantine_entry impl; Z. 614+ — bulk_convert_quarantine_rows impl; Z. 1518-1579 — detect_weekly_lump_sum)
    - service_impl/src/test/cutover.rs (Z. 2236-2500 — existing Karin-Pattern + manual_range_resolves_karin_quarantine + Helper-Fixtures wie karin_row, legacy_row_with_id, build_dependencies)
    - shifty_bin/src/integration_test/cutover.rs (existing REST-Integration-Tests; nach 8.2 Plan 01 sollte convert_with_manual_range_via_rest existieren)
    - .planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md (Z. 437-456 — AbsencePeriodEntity-Konstruktion Pattern; Z. 783-862 — Cutover-Test-Pattern)
    - service/src/cutover.rs (nach Task 1 — neue Trait-Signaturen sind verbindlich)
  </read_first>
  <action>
    **Schritt A — `convert_quarantine_entry`-Impl in `service_impl/src/cutover.rs`** (Z. 413-600). Funktion-Signatur an Trait anpassen:

    ```rust
    async fn convert_quarantine_entry(
        &self,
        extra_hours_id: Uuid,
        manual_range: Option<ManualRange>,
        day_fraction: Option<crate::absence::DayFraction>,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<ConvertQuarantineEntryOutcome, ServiceError> {
    ```

    Innerhalb Step 6 (AbsencePeriodEntity-Konstruktion Z. 529-543) — am Ende des Struct-Literals vor `}`:

    ```rust
            day_fraction: day_fraction
                .as_ref()
                .map(dao::absence::DayFractionEntity::from)
                .unwrap_or(dao::absence::DayFractionEntity::Full),
    ```

    Begründung: `day_fraction.map(|f| (&f).into()).unwrap_or(Full)`-Variante kompiliert auch, aber `as_ref` ist nicht-konsumierend und konsistent mit dem REST-Handler-Pattern.

    **Schritt B — `bulk_convert_quarantine_rows`-Impl** (Z. 614+). Funktion-Signatur an Trait anpassen + identische `day_fraction`-Map-Logik in die Per-Row-`AbsencePeriodEntity`-Konstruktion. Beachte: der `day_fraction`-Wert gilt für ALLE Rows der Operation — entweder einmal vor der Schleife in eine `let bulk_day_fraction: DayFractionEntity = day_fraction.as_ref().map(...).unwrap_or(Full);`-Variable resolven, oder pro Row neu — beides funktional gleich, die einmal-vor-der-Schleife-Variante ist klarer:

    ```rust
    let bulk_day_fraction: dao::absence::DayFractionEntity = day_fraction
        .as_ref()
        .map(dao::absence::DayFractionEntity::from)
        .unwrap_or(dao::absence::DayFractionEntity::Full);

    for row in rows_to_convert {
        // ... existing AbsencePeriodEntity-Konstruktion ...
        let entity = AbsencePeriodEntity {
            // ... bestehende Felder ...
            day_fraction: bulk_day_fraction,
        };
        // ... existing create + soft_delete ...
    }
    ```

    **Schritt C — `detect_weekly_lump_sum` BLEIBT UNVERÄNDERT** (Z. 1518-1579). Signatur, Match-Arme, Rückgabetyp sind alle unangetastet. CONTEXT.md OQ-2 + PATTERNS.md Z. 483 Empfehlung: Auto-Vorschlag-Heuristik wird NICHT in Backend implementiert; Frontend zeigt einen Hint, Operator entscheidet.

    **Schritt D — Existing Call-Sites updaten.** Suche alle Aufrufe von `convert_quarantine_entry` und `bulk_convert_quarantine_rows` in der gesamten Codebase:

    ```bash
    grep -rn "convert_quarantine_entry\|bulk_convert_quarantine_rows" --include="*.rs" .
    ```

    An jedem Call-Site (Tests, REST-Handler ist schon updated in Task 1, ggf. weitere Service-Call-Sites) den `day_fraction: None`-Parameter ergänzen. **WICHTIG:** Die existing Tests (Karin-Pattern, weekly-lump-sum, alle Phase-8.1/8.2-Tests) MÜSSEN mit `None`-Default unverändertes Verhalten zeigen — das ist die Backwards-Compat-Verifikation.

    **Schritt E — 2 neue Mockall-Tests in `service_impl/src/test/cutover.rs`** (am Ende des `manual_range_convert_quarantine_tests`-Moduls oder in einem neuen `day_fraction_convert_tests`-Submodul):

    ```rust
    mod day_fraction_convert_tests {
        use super::*; // bringt build_dependencies, permission_service_allow_cutover_admin etc. ein
        use dao::absence::DayFractionEntity;
        use crate::absence::DayFraction;
        use time::macros::date;

        #[tokio::test]
        async fn convert_quarantine_entry_with_half_day_persists_day_fraction() {
            let mut deps = build_dependencies();
            deps.permission_service = permission_service_allow_cutover_admin();

            // Heiligabend-Pattern: 4h Vacation am 2026-12-24 (8h-Vertrag).
            let extra_hours_id = Uuid::new_v4();
            let row = legacy_row_with_id(extra_hours_id, date!(2026 - 12 - 24), 4.0);
            // ... setup absence_dao expect_create mit Halb-Verification + sonstige mocks
            //     wie im existing manual_range_resolves_karin_quarantine-Test ...

            deps.absence_dao
                .expect_create()
                .withf(|entity, _, _| {
                    entity.from_date == date!(2026 - 12 - 24)
                        && entity.to_date == date!(2026 - 12 - 24)
                        && entity.day_fraction == DayFractionEntity::Half
                })
                .times(1)
                .returning(|_, _, _| Ok(()));

            // ... rest des setup analog manual_range_resolves_karin_quarantine ...

            let manual = ManualRange {
                start_date: date!(2026 - 12 - 24),
                end_date: date!(2026 - 12 - 24),
            };

            let service = build_service(deps);
            let outcome = service
                .convert_quarantine_entry(
                    extra_hours_id,
                    Some(manual),
                    Some(DayFraction::Half),
                    ().auth(),
                    None,
                )
                .await
                .expect("convert should succeed");

            assert_eq!(outcome.deleted_extra_hours_id, extra_hours_id);
            // absence_dao.expect_create.withf verifiziert day_fraction = Half persistiert wurde.
        }

        #[tokio::test]
        async fn convert_quarantine_entry_without_day_fraction_defaults_to_full() {
            // Backwards-compat: None → Full
            let mut deps = build_dependencies();
            deps.permission_service = permission_service_allow_cutover_admin();

            let extra_hours_id = Uuid::new_v4();
            // ... gleiches Setup ...

            deps.absence_dao
                .expect_create()
                .withf(|entity, _, _| entity.day_fraction == DayFractionEntity::Full)
                .times(1)
                .returning(|_, _, _| Ok(()));

            let service = build_service(deps);
            service
                .convert_quarantine_entry(
                    extra_hours_id,
                    Some(ManualRange {
                        start_date: date!(2026 - 12 - 24),
                        end_date: date!(2026 - 12 - 24),
                    }),
                    None,  // <-- day_fraction = None → Default Full
                    ().auth(),
                    None,
                )
                .await
                .expect("should succeed with None day_fraction");
        }

        #[tokio::test]
        async fn bulk_convert_quarantine_rows_with_half_applies_to_all_rows() {
            // Setup: 2 Rows derselben Person/Kategorie/Jahr, bulk-convert mit Half.
            // Erwartung: BEIDE created AbsencePeriodEntities haben Half.
            let mut deps = build_dependencies();
            deps.permission_service = permission_service_allow_cutover_admin();

            let sales_person_id = Uuid::new_v4();
            let row1_id = Uuid::new_v4();
            let row2_id = Uuid::new_v4();
            // ... 2 rows setup, both Vacation, same year ...

            deps.absence_dao
                .expect_create()
                .withf(|entity, _, _| entity.day_fraction == DayFractionEntity::Half)
                .times(2)  // Beide Rows müssen Half haben.
                .returning(|_, _, _| Ok(()));

            let service = build_service(deps);
            let outcome = service
                .bulk_convert_quarantine_rows(
                    sales_person_id,
                    AbsenceCategory::Vacation,
                    2026,
                    None,  // alle Rows
                    Some(DayFraction::Half),
                    ().auth(),
                    None,
                )
                .await
                .expect("bulk convert should succeed");

            assert_eq!(outcome.converted_absence_periods.len(), 2);
            for ap in &outcome.converted_absence_periods {
                assert_eq!(ap.day_fraction, DayFractionEntity::Half, "all bulk-converted entities must have Half");
            }
        }
    }
    ```

    **Falls Test-Helper wie `legacy_row_with_id` / `build_service` / `().auth()` nicht 1:1 vorhanden sind:** Nimm das Pattern aus `manual_range_resolves_karin_quarantine` (8.2-Plan-01) und kopiere die Setup-Sequenz. Die genauen Helper-Namen erschließt der Executor aus dem File-Read.

    **Schritt F — REST-Integration-Test in `shifty_bin/src/integration_test/cutover.rs`** ergänzen (am Ende, oder im 8.2-Plan-01-`convert_with_manual_range_via_rest`-Pattern):

    ```rust
    // Setup-Pattern 1:1 von `convert_with_manual_range_via_rest` (Phase 8.2,
    // shifty_bin/src/integration_test/cutover.rs ab Z. 2211) übernommen:
    // TestSetup::new() + add_user_with_role + create_sales_person + Contract
    // + create_extra_hour + axum::Router::new().nest(...) + Request::builder
    // + sqlx::query(...).bind(uuid.as_bytes().to_vec()).fetch_one(test_setup.pool.as_ref()).
    //
    // Heiligabend-Pattern statt Karin: 4h Vacation am 2026-12-24 mit 8h-Vertrag.

    #[tokio::test]
    async fn convert_with_day_fraction_via_rest() {
        let test_setup = TestSetup::new().await;
        add_user_with_role(
            &test_setup,
            "cutover_user",
            "admin",
            Some(CUTOVER_ADMIN_PRIVILEGE),
        )
        .await;

        let person = create_sales_person(&test_setup, "Heiligabend HalbTag").await;

        // 8h/Tag-Vertrag (Mo-Fr) für 2026.
        let contract = EmployeeWorkDetails {
            id: Uuid::nil(),
            sales_person_id: person.id,
            expected_hours: 40.0,
            from_year: 2024,
            from_calendar_week: 1,
            from_day_of_week: DayOfWeek::Monday,
            to_year: 2026,
            to_calendar_week: 52,
            to_day_of_week: DayOfWeek::Sunday,
            is_dynamic: false,
            cap_planned_hours_to_expected: false,
            workdays_per_week: 5,
            monday: true,
            tuesday: true,
            wednesday: true,
            thursday: true,
            friday: true,
            saturday: false,
            sunday: false,
            vacation_days: 30,
            created: None,
            deleted: None,
            version: Uuid::nil(),
        };
        test_setup
            .rest_state
            .working_hours_service()
            .create(&contract, Authentication::Full, None)
            .await
            .unwrap();

        // 4h Vacation am Heiligabend 2026-12-24 (Donnerstag) — halbtägiger
        // Eintrag im Legacy-Schema.
        let entry = create_extra_hour(
            &test_setup,
            person.id,
            ExtraHoursCategory::Vacation,
            date!(2026 - 12 - 24),
            4.0,
        )
        .await;

        // POST /admin/cutover/convert-quarantine-entry mit manual_range
        // {2026-12-24, 2026-12-24} und day_fraction = Half.
        let router = axum::Router::new()
            .nest(
                "/admin/cutover",
                generate_route::<crate::RestStateImpl>(),
            )
            .with_state(test_setup.rest_state.clone())
            .layer(Extension(
                Some(Arc::<str>::from("cutover_user")) as RestContext
            ));

        let body = serde_json::to_vec(&CutoverConvertQuarantineEntryRequest {
            extra_hours_id: entry.id,
            manual_range: Some(ManualRangeTO {
                start_date: "2026-12-24".into(),
                end_date: "2026-12-24".into(),
            }),
            day_fraction: Some(DayFractionTO::Half),
        })
        .unwrap();
        let req = Request::builder()
            .method("POST")
            .uri("/admin/cutover/convert-quarantine-entry")
            .header("content-type", "application/json")
            .body(Body::from(body))
            .unwrap();
        let resp = router.oneshot(req).await.unwrap();
        assert_eq!(
            resp.status(),
            StatusCode::OK,
            "day_fraction=Half convert must return 200"
        );

        // Validate: lese geschriebenes absence_period aus DB; day_fraction
        // Spalte muss 'half' sein. Pattern aus Phase 8.2-Plan-01 (Z. 2367-2386):
        // sqlx::query(...).bind(&uuid_bytes).fetch_one(test_setup.pool.as_ref()).
        let person_bytes = person.id.as_bytes().to_vec();
        let day_fraction_str: String = sqlx::query(
            "SELECT day_fraction FROM absence_period \
             WHERE sales_person_id = ? AND from_date = ? AND deleted IS NULL",
        )
        .bind(&person_bytes)
        .bind("2026-12-24")
        .fetch_one(test_setup.pool.as_ref())
        .await
        .expect("should find absence_period row")
        .get("day_fraction");
        assert_eq!(
            day_fraction_str, "half",
            "day_fraction column must store 'half' (D-08.3-06: backend persists Operator-supplied value)"
        );
    }
    ```

    Falls die existing 8.2-Integration-Test `convert_with_manual_range_via_rest` ein anderes Setup-Pattern verwendet (z. B. `TestApp::new_with_admin()`-Style), passe das neue Test 1:1 daran an. Das wichtige ist: POST schickt das `day_fraction`-Feld; nach 200-Response wird aus der DB gelesen, dass `day_fraction = 'half'` persistiert.

    **Sanity-Build + Test-Run:**
    ```bash
    cargo build --workspace
    cargo test --workspace cutover
    ```
  </action>
  <verify>
    <automated>
      grep -cE "day_fraction:\s*day_fraction\s*\.as_ref\(\)\s*\.map" service_impl/src/cutover.rs | awk '{ if ($1 >= 1) exit 0; else exit 1 }' \
      && grep -c "convert_quarantine_entry_with_half_day_persists_day_fraction" service_impl/src/test/cutover.rs | grep -qx 1 \
      && grep -c "convert_quarantine_entry_without_day_fraction_defaults_to_full" service_impl/src/test/cutover.rs | grep -qx 1 \
      && grep -c "bulk_convert_quarantine_rows_with_half_applies_to_all_rows" service_impl/src/test/cutover.rs | grep -qx 1 \
      && grep -c "convert_with_day_fraction_via_rest" shifty_bin/src/integration_test/cutover.rs | grep -qx 1 \
      && cargo build --workspace \
      && cargo test --workspace cutover \
      && cargo test --workspace manual_range_resolves_karin_quarantine \
      && cargo test --workspace day_fraction_convert
    </automated>
    Die `manual_range_resolves_karin_quarantine`-Assertion verifiziert dass der existing Phase-8.2-Test mit dem neuen `None`-Parameter weiter grün ist (Backwards-Compat im Test-Layer).
  </verify>
  <done>
    Service-Impl beider Cutover-Methoden persistiert `day_fraction` aus dem Parameter (Default `Full`). 2 neue Mockall-Tests + 1 REST-Integration-Test grün. Alle existing Cutover-Tests (Karin, weekly-lump-sum, bulk) grün. `cargo build --workspace && cargo test --workspace cutover` exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST→service (Cutover-Request-DTO) | `day_fraction: Option<DayFractionTO>` aus untrusted JSON (User mit cutover_admin-Privilege) |
| service→DAO (Cutover-Persist) | `Option<DayFraction>` aus Service-Parameter → `DayFractionEntity::Full` per `.unwrap_or` |
| Cutover-Privilege-Gate | `CUTOVER_ADMIN_PRIVILEGE` (existing, unverändert in Phase 8.3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-16 | T (Tampering) | `day_fraction`-Feld auf Cutover-Request-Body | mitigate | `DayFractionTO`-Deserialization rejected unknown values (serde default). `#[serde(default)]` setzt None bei missing field → Default `Full`. Beide gut. |
| T-08.3-17 | E (Elevation of Privilege) | User ohne cutover_admin sendet Request mit day_fraction | mitigate | UNVERÄNDERT: existing `CUTOVER_ADMIN_PRIVILEGE`-Gate in `convert_quarantine_entry` Z. 421-423 fired ZUERST, bevor irgendein day_fraction-Code läuft. Phase 8.3 ändert das nicht. |
| T-08.3-18 | T (Tampering) | day_fraction mismatch zwischen UI-Hint und tatsächlichem Persist (Operator klickt Half, Backend persistiert Full) | mitigate | Mockall-Tests (`convert_quarantine_entry_with_half_day_persists_day_fraction`) + REST-Integration-Test (`convert_with_day_fraction_via_rest`) gegen-verifizieren das Persistier-Verhalten. |
| T-08.3-19 | I (Information Disclosure) | bulk_convert mit day_fraction = Some(Half) aber operator wollte einzelne Rows als Full | accept | DESIGN-Entscheidung: bulk gilt einheitlich für alle Rows (CONTEXT.md OQ und PATTERNS.md). Operator-UI (Plan 06) zeigt explizit "Half/Full for whole group"-Label (CutoverBulkDayFractionLabel) — informierte Entscheidung. Falls feinere Granularität gebraucht: Operator nutzt Per-Row-Convert statt Bulk. |
</threat_model>

<verification>
**Plan-Level-Check nach Task 2:**

```bash
cargo build --workspace
cargo test --workspace
cargo test --workspace cutover
cargo test -p rest openapi_surface
```

**Erwartetes Verhalten:** Backend ist nach Plan 05 vollständig grün. Frontend (Plan 06) ist die letzte offene Surface.
</verification>

<success_criteria>
- [ ] Beide Cutover-Request-DTOs in rest-types haben `day_fraction: Option<DayFractionTO>` + `#[serde(default)]`
- [ ] 2 neue rest-types-Roundtrip-Tests grün (with-half, omits-backwards-compat)
- [ ] Beide Cutover-Service-Traits haben `day_fraction: Option<DayFraction>` als Parameter (zwischen manual_range/extra_hours_ids und context)
- [ ] `ManualRange`-Struct UNVERÄNDERT (separater Parameter, kein Range-Feld)
- [ ] `detect_weekly_lump_sum` UNVERÄNDERT (kein Auto-Vorschlag-Branch)
- [ ] Beide Service-Impls persistieren `day_fraction` aus Parameter; Default `Full` bei `None`
- [ ] Beide REST-Handler forwarden `day_fraction` (Option-Map-Pattern)
- [ ] Alle existing Cutover-Tests (Karin, weekly-lump-sum, bulk, manual-range-resolves-karin) grün mit `None`-Default
- [ ] 3 neue Mockall-Tests grün (with-half, without-day-fraction-defaults-full, bulk-with-half-applies-to-all)
- [ ] 1 neuer REST-Integration-Test grün (convert_with_day_fraction_via_rest)
- [ ] `cargo build --workspace && cargo test --workspace` exit 0
- [ ] OpenAPI-Surface-Test bleibt grün (transitive DayFractionTO-Einbindung via Cutover-Request-DTOs)
</success_criteria>

<output>
Nach Plan-Abschluss `.planning/phases/08.3-halbtag-support-f-r-absences/08.3-05-SUMMARY.md` mit:
- Bestätigung dass Heuristik unangetastet bleibt (Frontend-Hint statt Backend-Auto-Vorschlag)
- Liste aller geupdateten Call-Sites in der Codebase (typischerweise Test-Files in service_impl + ggf. shifty_bin)
- Mockall-Test-Strategie Notiz: warum `withf`-Predicate auf `entity.day_fraction == ...` statt full-payload-equality
- VCS: jj-commit-Topologie
  - `feat(08.3-05): extend Cutover request DTOs + service trait with day_fraction parameter`
  - `feat(08.3-05): thread day_fraction through CutoverServiceImpl convert + bulk-convert`
  - `feat(08.3-05): forward day_fraction in cutover REST handlers`
  - `test(08.3-05): add 3 mockall + 1 REST integration test for half-day cutover`
</output>
