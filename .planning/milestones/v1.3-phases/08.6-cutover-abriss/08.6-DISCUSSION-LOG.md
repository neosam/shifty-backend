# Phase 8.6: Cutover-Abriss - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-11
**Phase:** 8.6-cutover-abriss
**Areas discussed:** Feature-Flag-Subsystem-Tiefe, Absences-Menü-Gate, Cutover-Backup-Tabelle, Test-/Verifikations-Umfang

---

## Feature-Flag-Subsystem-Tiefe (GA-A)

| Option | Description | Selected |
|--------|-------------|----------|
| Komplett-Abriss (Empfohlen) | Backend feature_flag DAO/Service/REST + Tabelle + Frontend state/service/loader/api/top_bar-Kopplung alle weg. Keine tote Infra. | |
| Generische Infra behalten | Nur `absence_range_source_active`-Seed + Cutover-Schreiber entfernen; generisches Flag-Subsystem bleibt als wiederverwendbarer Mechanismus. | ✓ |

**User's choice:** Generische Infra behalten
**Notes:** Gegen meine Komplett-Abriss-Empfehlung — der generische Flag-Mechanismus soll für künftige Flags bereitstehen. Folge: Tension mit dem No-Dead-Code-Gate am Frontend (Konsument fällt weg) → Planner löst via `#[allow(dead_code)]` oder Shell-Trim (CONTEXT D-02). Backend bleibt sauber (Endpoint registriert).

---

## Absences-Menü-Gate (GA-B)

| Option | Description | Selected |
|--------|-------------|----------|
| Nur `logged_in` (Empfohlen) | Absence-Seite ist dauerhaftes Haupt-Feature; Menü für alle Eingeloggten sichtbar, Backend `hr ∨ self` autoritativ. | ✓ |
| Explizites Privileg-Gate | Menü nur bei bestimmtem Privileg (z.B. hr ∨ sales) — restriktiver, inkonsistent mit Self-Service-Ziel. | |

**User's choice:** Nur `logged_in`
**Notes:** Löst den aktuellen Defekt (Absence-Menü hängt an `cutover_active`, in Prod versteckt). `cutover_active`-Param von `nav_visibility` entfällt.

---

## Cutover-Backup-Tabelle (GA-C)

| Option | Description | Selected |
|--------|-------------|----------|
| Mit-droppen (Empfohlen) | `employee_yearly_carryover_pre_cutover_backup` mit-droppen — reines Cutover-Artefakt, in Prod nie befüllt. | ✓ |
| Behalten | Als historisches Backup stehen lassen. | |

**User's choice:** Mit-droppen
**Notes:** Erweitert SC3 (das nur `absence_migration_quarantine` nennt) bewusst um die Backup-Tabelle. `absence_period_migration_source` bleibt.

---

## Test-/Verifikations-Umfang (GA-D)

| Option | Description | Selected |
|--------|-------------|----------|
| Forward-only + ersatzlos (Empfohlen) | Drop-Migrations forward-only; Cutover-/Flag-/Diagnose-Tests ersatzlos raus; Gate: cargo test + WASM + cargo check ohne Dead-Code + OpenAPI-Surface. | ✓ |
| Vorsichtiger | Down-Migrations und/oder Tests konservativer (umbauen statt löschen). | |

**User's choice:** Forward-only + ersatzlos
**Notes:** INT wipebar, kein Prod-Data → Down-Migrations unnötig. Generische `feature_flag`-Tests bleiben (auf neutralen Key umstellen, falls Seed wegfällt).

---

## Claude's Discretion

- Wave-Topologie der Subtraktion (eine große vs. getrennt Backend/Frontend/Migrations).
- Frontend-Dead-Code-Auflösung `#[allow(dead_code)]` vs. Shell-Trim.
- Ob `absence_range_source_active`-Seed via neue `DELETE`-Migration entfernt oder harmlos stehen gelassen wird.
- DI-Auflösung in `shifty_bin/src/main.rs` nach Wegfall von `CutoverService`/`CutoverDao`.

## Deferred Ideas

- `add_vacation`-Aufräumen erst nach Ablösung von `extra_hours_modal.rs` (Phase 12).
- Späterer Komplett-Abriss des Flag-Subsystems, falls nie wieder ein Flag gebraucht wird.
- Un-Convert (automatischer Rückweg) — optional, nicht v1.3.
- Phasen 10 & 12 „Both"-Marker / Kategorie-Sichtbarkeit an Koexistenz anpassen.

## Vor der Diskussion geklärte Analyse-Funde (keine Frage nötig)

- `cutover_run_id` bereits via 8.5-Migration `20260609120000` entfernt + Backlink-DAO nach `dao/src/migration_source.rs` umgezogen.
- `add_vacation` ist NICHT tot — `extra_hours_modal.rs:200` ruft `api::add_vacation` live → bleibt drin.
- `absence_range_source_active` ist der einzige Feature-Flag; Reader sind seit 8.4 (M-03) bereits entfernt, nur Kommentar-Reste + Cutover-Schreiber blieben.
</content>
