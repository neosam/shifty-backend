---
phase: 08.6-cutover-abriss
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/page/cutover_admin.rs
  - shifty-dioxus/src/service/cutover.rs
  - shifty-dioxus/src/state/cutover_state.rs
  - shifty-dioxus/src/page.rs
  - shifty-dioxus/src/service.rs
  - shifty-dioxus/src/state.rs
  - shifty-dioxus/src/router.rs
  - shifty-dioxus/src/app.rs
  - shifty-dioxus/src/component/top_bar.rs
  - shifty-dioxus/src/state/feature_flag.rs
  - shifty-dioxus/src/service/feature_flag.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC1: Frontend hat keine cutover_admin-Page/Route/Menü-Eintrag/i18n-Keys mehr"
    - "D-03: Absences-Menü-Gate ist reines logged_in (kein cutover_active)"
    - "D-02: generisches feature_flag-Subsystem bleibt als Shell warnungsfrei erhalten"
    - "Survivor: api::add_vacation bleibt (live in extra_hours_modal.rs:200)"
    - "WASM-Build (cargo build --target wasm32-unknown-unknown) grün, cargo test -p shifty-dioxus grün"
  artifacts:
    - path: "shifty-dioxus/src/component/top_bar.rs"
      provides: "nav_visibility mit 2 Params, absences: logged_in"
      contains: "absences: logged_in"
    - path: "shifty-dioxus/src/state/feature_flag.rs"
      provides: "leerer FeatureFlagsState-Shell-Mechanismus"
      contains: "pub struct FeatureFlagsState"
  key_links:
    - from: "shifty-dioxus/src/component/top_bar.rs (TopBarRouted)"
      to: "nav_visibility"
      via: "Aufruf mit (auth_info, is_paid) — ohne cutover_active"
      pattern: "nav_visibility\\(auth_info"
---

<objective>
Frontend-Abriss der Cutover-Admin-Page samt Route, Menü-Eintrag, i18n-Keys und api-Calls. Das Absences-Menü-Gate wird von `logged_in && cutover_active` auf reines `logged_in` umgestellt (D-03); das generische feature_flag-Subsystem bleibt als warnungsfreie Shell erhalten (D-02, Option B Shell-Trim).

Purpose: SC1 — Frontend-Cutover-Maschinerie ersatzlos entfernen, Absence-Seite dauerhaft sichtbar machen, Survivor-Mechanismus (feature_flag) sauber konservieren.
Output: WASM-Build grün, `cargo test -p shifty-dioxus` grün, kein Frontend-Dead-Code-Warning.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.6-cutover-abriss/08.6-CONTEXT.md
@.planning/phases/08.6-cutover-abriss/08.6-RESEARCH.md
@.planning/phases/08.6-cutover-abriss/08.6-PATTERNS.md
@.planning/phases/08.6-cutover-abriss/08.6-VALIDATION.md

<interfaces>
<!-- Frontend-Survivor-Kontrakte und Gate-Umbau-Zielsignatur. Aus Live-Code extrahiert. -->

top_bar.rs nav_visibility — Zielsignatur (D-03):
  pub(crate) fn nav_visibility(auth_info: Option<&AuthInfo>, is_paid: bool) -> NavVisibility
  Body: `absences: logged_in,` (kein `&& cutover_active`). NavVisibility verliert `admin_cutover`-Feld.
  NavTarget verliert `AdminCutover`-Variante. is_admin_target verliert AdminCutover-matches!-Arm.
  Call-Site Z. 374: `nav_visibility(auth_info.as_ref(), is_paid)`.

state/feature_flag.rs Survivor-Mechanismus (D-02):
  FeatureFlag-Struct + From<&FeatureFlagTO>-Impl (Z. 14–29) BLEIBEN. FeatureFlagsState wird leerer Struct.

Survivor (NICHT entfernen):
  api::add_vacation (api.rs:959) — live in component/extra_hours_modal.rs:200.
  FEATURE_FLAGS_STORE (service/feature_flag.rs) — bleibt als Mechanismus.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cutover-Frontend-Dateien löschen + Route/App/api/i18n bereinigen</name>
  <files>shifty-dioxus/src/page/cutover_admin.rs, shifty-dioxus/src/service/cutover.rs, shifty-dioxus/src/state/cutover_state.rs, shifty-dioxus/src/page.rs, shifty-dioxus/src/service.rs, shifty-dioxus/src/state.rs, shifty-dioxus/src/router.rs, shifty-dioxus/src/app.rs, shifty-dioxus/src/api.rs, shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/page/cutover_admin.rs, service/cutover.rs, state/cutover_state.rs (zu löschen — bestätigen, dass kein Survivor-Symbol re-exportiert wird)
    - shifty-dioxus/src/page.rs / service.rs / state.rs (mod-Deklarationen für cutover_admin/cutover/cutover_state)
    - shifty-dioxus/src/router.rs (CutoverAdminPage re-export + Route::AdminCutover-Variante + Match-Arm)
    - shifty-dioxus/src/app.rs (Z. 28 use_coroutine cutover_service, Z. 32–40 feature_flag_handle + .send)
    - shifty-dioxus/src/api.rs (Cutover-Calls; add_vacation Z. 959 BLEIBT)
    - shifty-dioxus/src/i18n/mod.rs (Cutover-Key-Enum-Blöcke + 8 Cutover-Tests — siehe RESEARCH § 5.6)
    - 08.6-RESEARCH.md § 5.6, 08.6-PATTERNS.md § "Frontend-Abriss-Muster"
  </read_first>
  <action>
    VOLLSTÄNDIG LÖSCHEN:
    - shifty-dioxus/src/page/cutover_admin.rs
    - shifty-dioxus/src/service/cutover.rs
    - shifty-dioxus/src/state/cutover_state.rs

    MODUL-DEKLARATIONEN ENTFERNEN (in page.rs / service.rs / state.rs — exakte Datei per grep "mod cutover" finden):
    - `mod cutover_admin;` / `pub mod cutover_admin;` (page)
    - `mod cutover;` / `pub mod cutover;` (service)
    - `mod cutover_state;` / `pub mod cutover_state;` (state)

    router.rs:
    - `pub use crate::page::CutoverAdminPage;` und `pub use crate::page::CutoverAdminPage as AdminCutover;` entfernen
    - `Route::AdminCutover {}`-Variante (und zugehörigen `#[route(...)]`) entfernen

    app.rs:
    - Z. 28 `use_coroutine(service::cutover::cutover_service);` entfernen
    - Z. 38–40 `feature_flag_handle.send(service::feature_flag::FeatureFlagAction::LoadAbsenceRangeSourceActive);` entfernen
    - Z. 32–33 `let feature_flag_handle = use_coroutine(service::feature_flag::feature_flag_service);`
      entfernen (leere Coroutine ohne Dispatch → kein Nutzen; Modul bleibt als Shell für Task 2).
    - `use_coroutine(service::vacation_balance::vacation_balance_service);` und alle anderen Coroutinen BLEIBEN.

    api.rs:
    - Alle Cutover-Calls (cutover_gate_dry_run, cutover_commit, cutover_profile,
      cutover_convert_quarantine_entry, cutover_bulk_convert_quarantine_rows + zugehörige Request/Response-
      Typ-Importe aus rest-types) entfernen. `add_vacation` (Z. 959) UNVERÄNDERT lassen (Out of Scope).

    i18n/mod.rs:
    - Aus dem Key-Enum die 3 Cutover-Blöcke entfernen (RESEARCH § 5.6): Block "Cutover migration (Phase 8.1)"
      (CutoverMenuLabel..CutoverPrivilegeStage3), Block "Cutover Manual Range (Phase 8.2)"
      (CutoverManualConvert*), Block "Phase 8.3 — Halbtag-Support (Cutover)" (CutoverDriftColDayFraction..
      CutoverDayFractionHalf).
    - Die 8 Cutover-i18n-Tests entfernen: i18n_cutover_keys_present_in_all_locales,
      i18n_cutover_keys_match_{german,english,czech}_reference,
      i18n_cutover_day_fraction_keys_present_in_all_locales,
      i18n_cutover_day_fraction_match_{german,english,czech}_reference.

    i18n/de.rs, en.rs, cs.rs:
    - Alle `add_text(Locale::*, Key::Cutover*, ...)`-Aufrufe entfernen (alle 3 Locales synchron).
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --no-run 2>&1 | grep -Ei "error\[|error:" | head; echo "---"; grep -rc "Cutover\|cutover" src/i18n/mod.rs src/api.rs</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f shifty-dioxus/src/page/cutover_admin.rs && test ! -f shifty-dioxus/src/service/cutover.rs && test ! -f shifty-dioxus/src/state/cutover_state.rs && echo GONE` → GONE
    - `grep -rn "page::cutover\|service::cutover\|state::cutover\|NavTarget::AdminCutover\|Route::AdminCutover\|CutoverAdminPage" shifty-dioxus/src/` → 0 Treffer
    - `grep -c "Cutover" shifty-dioxus/src/i18n/mod.rs shifty-dioxus/src/i18n/de.rs shifty-dioxus/src/i18n/en.rs shifty-dioxus/src/i18n/cs.rs` → 0 in allen 4
    - `grep -rn "add_vacation" shifty-dioxus/src/` → Treffer in api.rs UND extra_hours_modal.rs (Survivor lebt)
    - `grep -c "FeatureFlagAction::LoadAbsenceRangeSourceActive\|service::cutover::cutover_service" shifty-dioxus/src/app.rs` → 0
  </acceptance_criteria>
  <done>Cutover-Frontend-Dateien gelöscht; Route/App/api/i18n bereinigt; add_vacation-Survivor intakt. top_bar-Gate noch offen für Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: top_bar.rs Gate-Umbau (D-03) + FeatureFlagsState Shell-Trim (D-02)</name>
  <files>shifty-dioxus/src/component/top_bar.rs, shifty-dioxus/src/state/feature_flag.rs, shifty-dioxus/src/service/feature_flag.rs</files>
  <read_first>
    - shifty-dioxus/src/component/top_bar.rs (Z. 22–37 NavVisibility, 39–71 nav_visibility, 73–86 NavTarget, 137–146 is_admin_target, 362–374 Call-Site, 749–1400 Tests — alle nav_visibility(..., bool)-Call-Sites)
    - shifty-dioxus/src/state/feature_flag.rs (Z. 14–29 FeatureFlag+From, 41–56 FeatureFlagsState, 60–89 Tests)
    - shifty-dioxus/src/service/feature_flag.rs (Z. 23–24 STORE, 28 KEY-const, 30–34 FeatureFlagAction, 45–84 feature_flag_service, 113–137 Tests)
    - 08.6-PATTERNS.md § 5 (exakte Shell-Trim- + Gate-Umbau-Vorlagen)
    - 08.6-RESEARCH.md § 3 + Pitfall 3 (alle Call-Sites in Tests anpassen)
  </read_first>
  <action>
    top_bar.rs Gate-Umbau (D-03):
    - nav_visibility-Signatur: dritten Parameter `cutover_active: bool` ENTFERNEN → `fn nav_visibility(auth_info: Option<&AuthInfo>, is_paid: bool) -> NavVisibility`
    - Body Z. 59: `absences: logged_in && cutover_active,` → `absences: logged_in,`
    - NavVisibility-Struct (Z. 22–37): Feld `admin_cutover: bool` ENTFERNEN
    - nav_visibility-Body: die Zeile `admin_cutover: has("cutover_admin") || has("hr"),` ENTFERNEN
    - NavTarget-Enum (Z. 73–86): Variante `AdminCutover` ENTFERNEN
    - is_admin_target (Z. 137–146): `NavTarget::AdminCutover` aus dem `matches!`-Ausdruck ENTFERNEN
    - Call-Site TopBarRouted (Z. 362–374): `let cutover_active = FEATURE_FLAGS_STORE.read().absence_range_source_active();`
      ENTFERNEN; `let visibility = nav_visibility(auth_info.as_ref(), is_paid, cutover_active);` →
      `let visibility = nav_visibility(auth_info.as_ref(), is_paid);`
    - Falls `FEATURE_FLAGS_STORE`-Import (Z. 14) danach ungenutzt ist: Import entfernen (cargo check zeigt es).
    - Render-Code, der `NavTarget::AdminCutover` / `visibility.admin_cutover` / `nav_entry(NavTarget::AdminCutover, ...)`
      verwendet, sowie ggf. `Route::AdminCutover {}`-Match-Arm in einer Routing-Hilfsfunktion ENTFERNEN.

    top_bar.rs Tests anpassen (Pitfall 3 — alle 3-arg-Calls auf 2 args):
    - Alle `nav_visibility(X, Y, Z)`-Aufrufe (Z. 750, 766, 785, 789, 793, 800, 810, 827, 837, 840, 844,
      851, 1190, 1210, 1247, 1264, 1274, 1284, 1303, 1338, 1351, 1371, 1381, 1392, 1400 …) auf 2 Argumente
      reduzieren (drittes Arg streichen).
    - Tests, die obsolet werden, ENTFERNEN: nav_visibility_absences_hidden_when_cutover_inactive,
      nav_visibility_admin_cutover_for_cutover_admin_user, nav_visibility_admin_cutover_for_hr_user,
      nav_visibility_admin_cutover_hidden_for_plain_user, is_admin_target_classifies_admin_cutover.
    - is_admin_target_classifies_each_variant: AdminCutover-Assert entfernen.
    - hr_admin_user_partitions_into_top_level_and_full_admin_group + sales_only_user_yields_no_admin_group:
      "Datenmigration"/AdminCutover aus den erwarteten admin_labels entfernen.
    - nav_entry(NavTarget::AdminCutover, ...) aus Test-Hilfsfunktion entfernen.
    - Ein neuer/angepasster Test soll absichern, dass `absences` für jeden logged_in-User true ist
      (z.B. nav_visibility_sales_*-Test asserted nun `v.absences == true`).

    state/feature_flag.rs Shell-Trim (D-02, Option B):
    - FeatureFlagsState: Feld `absence_range_source_active: Option<bool>` + Methode `absence_range_source_active()`
      ENTFERNEN → leerer Struct:
      ```rust
      /// Generischer Frontend-Spiegel für Backend-Feature-Flags.
      /// Aktuell keine aktiven Flags (Phase 8.6 — Cutover-Abriss).
      #[derive(Clone, Debug, Default, PartialEq)]
      pub struct FeatureFlagsState {}
      ```
    - FeatureFlag-Struct + From<&FeatureFlagTO>-Impl (Z. 14–29) BLEIBEN.
    - Tests default_state_returns_false_for_known_flags + convenience_lookup_returns_inner_when_some
      ENTFERNEN; from_to_preserves_all_fields BLEIBT.

    service/feature_flag.rs Shell-Trim:
    - `pub const ABSENCE_RANGE_SOURCE_ACTIVE_KEY: ...` ENTFERNEN
    - FeatureFlagAction-Enum: Variante LoadAbsenceRangeSourceActive ENTFERNEN → leeres Enum mit `#[allow(dead_code)]`:
      ```rust
      #[allow(dead_code)]
      #[derive(Debug)]
      pub enum FeatureFlagAction {}
      ```
    - feature_flag_service-Coroutine: Body zu Shell leeren (kein match-Arm mehr):
      ```rust
      pub async fn feature_flag_service(mut _rx: UnboundedReceiver<FeatureFlagAction>) {
          // Kein aktiver Flag mehr — Mechanismus als Shell erhalten (Phase 8.6).
      }
      ```
    - FEATURE_FLAGS_STORE, wait_for_config_ready, loader-Import BLEIBEN.
    - Tests default_config_has_empty_backend + populated_config_unblocks_wait_predicate BLEIBEN.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test 2>&1 | tail -5; echo "---WASM---"; nix develop --command cargo build --target wasm32-unknown-unknown 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "absences:" shifty-dioxus/src/component/top_bar.rs` → "absences: logged_in," (kein cutover_active)
    - `grep -rn "cutover_active\|admin_cutover\|AdminCutover\|absence_range_source_active" shifty-dioxus/src/` → 0 Treffer
    - `grep -c "pub struct FeatureFlagsState {}" shifty-dioxus/src/state/feature_flag.rs` → 1
    - `grep -c "pub enum FeatureFlagAction {}" shifty-dioxus/src/service/feature_flag.rs` → 1
    - `grep -c "From<&FeatureFlagTO>\|fn feature_flag_service\|FEATURE_FLAGS_STORE" shifty-dioxus/src/service/feature_flag.rs shifty-dioxus/src/state/feature_flag.rs` → ≥1 (Survivor-Mechanismus erhalten)
    - `cd shifty-dioxus && cargo test` grün; `nix develop --command cargo build --target wasm32-unknown-unknown` exit 0 ohne dead-code-Warnung
  </acceptance_criteria>
  <done>Absences-Gate ist reines logged_in; nav_visibility hat 2 Params; FeatureFlagsState+FeatureFlagAction als warnungsfreie Shell; WASM-Build + Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend-Menü-Sichtbarkeit (top_bar) | KOSMETISCH — Menü-Gate bestimmt nur Anzeige, nicht Datenzugriff |
| Backend rest/src/absence.rs (hr ∨ self) | AUTORITATIVE Datengrenze — bleibt unverändert: Self sieht eigene, HR alle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-03 | E (Elevation) | Absences-Menü-Gate-Relaxation (logged_in) | accept | KEINE Privilege-Escalation: das Frontend-Menü-Gate ist kosmetisch. Der autoritative Daten-Boundary ist Backend `hr ∨ self` in rest/src/absence.rs (D-03, unverändert). Ein User, der das Menü jetzt sieht, kann nur eigene Daten (self) bzw. bei HR alle Daten lesen — exakt wie vor 8.6 per Direkt-URL. Das Gate verbreitert KEINEN Datenzugriff. |
| T-086-04 | I (Disclosure) | entfernte Cutover-Admin-Page | accept | Subtraktiv: cutover_admin-gated Page entfernt → Angriffsfläche sinkt. |

**Subtraktive Posture:** Menü-Relaxation ist rein kosmetisch; die reale Sichtbarkeits-/Datengrenze bleibt Backend `hr ∨ self`. Keine neue Angriffsfläche, keine HIGH-severity-Threats.
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test` grün; `nix develop --command cargo build --target wasm32-unknown-unknown` exit 0
- Negative-Space: `grep -rn "page::cutover\|service::cutover\|state::cutover\|NavTarget::AdminCutover\|cutover_active\|absence_range_source_active" shifty-dioxus/src/` → 0
- Survivor: `grep -rn "add_vacation" shifty-dioxus/src/` → Treffer in api.rs + extra_hours_modal.rs
- Gate: `grep -n "absences:" shifty-dioxus/src/component/top_bar.rs` → "absences: logged_in"
</verification>

<success_criteria>
- SC1: Frontend-Cutover-Page/Route/Menü/i18n entfernt; `cargo test -p shifty-dioxus` grün
- D-03: Absences-Menü für alle logged_in-User sichtbar; Backend hr ∨ self bleibt autoritativ
- D-02: feature_flag-Mechanismus warnungsfrei als Shell erhalten; WASM-Build grün
</success_criteria>

<output>
After completion, create `.planning/phases/08.6-cutover-abriss/08.6-02-SUMMARY.md`
</output>
