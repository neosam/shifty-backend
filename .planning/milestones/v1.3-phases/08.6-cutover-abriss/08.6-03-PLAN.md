---
phase: 08.6-cutover-abriss
plan: 03
type: execute
wave: 2
depends_on: ["08.6-01"]
files_modified:
  - migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql
  - migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql
  - migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql
  - .sqlx/
autonomous: false
requirements: []
must_haves:
  truths:
    - "SC3: Drop-Migrations für absence_migration_quarantine + employee_yearly_carryover_pre_cutover_backup existieren (forward-only)"
    - "SC4: absence_range_source_active Seed-Row wird per DELETE-Migration entfernt"
    - "absence_period_migration_source-Tabelle wird NICHT gedroppt (lebender 8.5-Backlink)"
    - "D-05: keine Down-Migrations"
    - ".sqlx/-Offline-Cache enthält keine verwaisten Query-Fingerprints für gedroppte Tabellen"
  artifacts:
    - path: "migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql"
      provides: "forward-only DROP TABLE IF EXISTS absence_migration_quarantine"
      contains: "DROP TABLE IF EXISTS absence_migration_quarantine"
    - path: "migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql"
      provides: "forward-only DROP TABLE IF EXISTS employee_yearly_carryover_pre_cutover_backup"
      contains: "DROP TABLE IF EXISTS employee_yearly_carryover_pre_cutover_backup"
    - path: "migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql"
      provides: "DELETE FROM feature_flag WHERE key = 'absence_range_source_active'"
      contains: "DELETE FROM feature_flag"
  key_links:
    - from: "sqlx migrate run"
      to: "Dev-DB Schema"
      via: "forward-only Migration ohne Down-File"
      pattern: "DROP TABLE IF EXISTS"
---

<objective>
Forward-only Drop-Migrations für die beiden toten Cutover-Tabellen (`absence_migration_quarantine`, `employee_yearly_carryover_pre_cutover_backup`) plus eine DELETE-Migration für den `absence_range_source_active`-Seed-Row. Die `feature_flag`-Tabelle und `absence_period_migration_source` bleiben. Anschließend `.sqlx/`-Offline-Cache regenerieren, damit keine verwaisten Query-Fingerprints für die gedroppten Tabellen verbleiben.

Purpose: SC3 + SC4 (DB-Seite) — totes Schema entfernen, Survivor-Tabellen behalten, sqlx-Cache sauber.
Output: Drei neue forward-only Migrationen; Dev-DB ohne die zwei Tabellen; `.sqlx/` ohne orphaned Fingerprints.

Hinweis: Diese DB-Operationen sind additiv (`sqlx migrate run`), NICHT destruktiv (`reset`/`drop database`). Falls ein `sqlx database reset` erwogen wird, vorher User-Confirmation einholen (Dev-DB nicht recoverbar).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.6-cutover-abriss/08.6-CONTEXT.md
@.planning/phases/08.6-cutover-abriss/08.6-RESEARCH.md
@.planning/phases/08.6-cutover-abriss/08.6-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drei forward-only Drop-/Delete-Migrations anlegen</name>
  <files>migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql, migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql, migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql</files>
  <read_first>
    - migrations/sqlite/20260609120000_migration-source-drop-cutover-run-id.sql (Analog: Header-Kommentar-Stil + DROP TABLE IF EXISTS, forward-only)
    - migrations/sqlite/20260503000000_create-absence-migration-quarantine.sql (Create bleibt unverändert — nur zur Bestätigung des Tabellennamens)
    - migrations/sqlite/20260503000002_create-employee-yearly-carryover-pre-cutover-backup.sql (Tabellenname bestätigen)
    - migrations/sqlite/20260501000000_add-feature-flag-table.sql (INSERT-Seed-Zeile für absence_range_source_active — Key-Wert bestätigen)
    - 08.6-PATTERNS.md § 1 (exakte Migrations-Vorlagen)
    - CONTEXT.md D-04, D-05 (Drei-Tabellen-Bilanz, forward-only)
  </read_first>
  <action>
    Drei neue Migrationsdateien anlegen (Timestamps nach der neuesten Migration 20260609120000; KEINE Down-Files, D-05):

    migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql:
    ```sql
    -- Phase 8.6 (D-04): Drop absence_migration_quarantine.
    -- Tabelle wurde ausschliesslich von dao_impl_sqlite/src/cutover.rs beschrieben (in 8.6 geloescht).
    -- In Prod nie befuellt (CONTEXT.md Modell-Fakt 2026-06-11). INT wipeable. Forward-only (D-05).
    DROP TABLE IF EXISTS absence_migration_quarantine;
    ```

    migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql:
    ```sql
    -- Phase 8.6 (D-04): Drop employee_yearly_carryover_pre_cutover_backup.
    -- Reines Cutover-Backup-Artefakt; nur von dao_impl_sqlite/src/cutover.rs:327 (Cutover-Commit)
    -- je beschrieben (in 8.6 geloescht). In Prod nie befuellt. INT wipeable. Forward-only (D-05).
    DROP TABLE IF EXISTS employee_yearly_carryover_pre_cutover_backup;
    ```

    migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql:
    ```sql
    -- Phase 8.6 (D-01): Entferne absence_range_source_active Seed-Row.
    -- Alle Reader (top_bar.rs) und Writer (service_impl/cutover.rs:1319) sind in 8.6 entfernt.
    -- Tabelle feature_flag BLEIBT als generischer Mechanismus erhalten. Forward-only.
    DELETE FROM feature_flag WHERE key = 'absence_range_source_active';
    ```

    NICHT droppen: absence_period_migration_source (20260503000001) — lebender 8.5-Backlink.
    Alte Create-Migrationen NICHT editieren.
  </action>
  <verify>
    <automated>ls migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei Migrationsdateien existieren mit korrekten Timestamp-Namen
    - `grep -c "DROP TABLE IF EXISTS absence_migration_quarantine" migrations/sqlite/20260611000000_*.sql` → 1
    - `grep -c "DROP TABLE IF EXISTS employee_yearly_carryover_pre_cutover_backup" migrations/sqlite/20260611000001_*.sql` → 1
    - `grep -c "DELETE FROM feature_flag WHERE key = 'absence_range_source_active'" migrations/sqlite/20260611000002_*.sql` → 1
    - `grep -rl "DROP TABLE IF EXISTS absence_period_migration_source" migrations/sqlite/2026061100000*.sql` → 0 Treffer (Survivor NICHT gedroppt)
    - Keine `*.down.sql`-Datei für die drei neuen Migrationen
  </acceptance_criteria>
  <done>Drei forward-only Migrationen angelegt; absence_period_migration_source nicht betroffen.</done>
</task>

<task type="auto">
  <name>Task 2: Migrationen anwenden + .sqlx-Cache regenerieren</name>
  <files>.sqlx/</files>
  <read_first>
    - 08.6-RESEARCH.md § 4 (SQLx-Compile-Time-Check, .sqlx-Regeneration) + Pitfall 5
    - 08.6-VALIDATION.md § "Manual-Only Verifications" (Tabellen-Drop ist Laufzeit-Effekt)
    - CLAUDE.md / CLAUDE.local.md (NixOS: nix develop, NICHT nix-shell; sqlx migrate run additiv, reset destruktiv)
  </read_first>
  <action>
    Die drei neuen Migrationen gegen die Dev-DB anwenden und den sqlx-Offline-Cache regenerieren — alles
    via `nix develop` (NICHT nix-shell, shell.nix kaputt):

    1. `nix develop --command sqlx migrate run --source migrations/sqlite` (additiv, forward-only — NICHT
       `sqlx database reset` ohne User-Confirmation; Dev-DB ist destruktiv-nicht-recoverbar).
    2. `nix develop --command cargo sqlx prepare --workspace` mit gesetztem DATABASE_URL (z.B.
       `DATABASE_URL=sqlite:./localdb.sqlite3`), damit `.sqlx/query-*.json` keine verwaisten Fingerprints
       für die gedroppten Tabellen (absence_migration_quarantine, employee_yearly_carryover_pre_cutover_backup)
       mehr enthalten — diese stammten aus dem in Plan 01 gelöschten dao_impl_sqlite/src/cutover.rs (8 Query-Makros).
    3. Verifizieren, dass `absence_migration_quarantine` + `employee_yearly_carryover_pre_cutover_backup`
       NICHT mehr in `.tables` erscheinen, `absence_period_migration_source` + `feature_flag` weiterhin schon.

    Falls die Dev-DB-Tooling nicht verfügbar ist (sqlx-cli fehlt trotz nix develop), als
    checkpoint:human-verify pausieren mit den exakten Befehlen für den User.
  </action>
  <verify>
    <automated>nix develop --command sqlite3 localdb.sqlite3 ".tables" 2>/dev/null | tr ' ' '\n' | grep -E "absence_migration_quarantine|employee_yearly_carryover_pre_cutover_backup|absence_period_migration_source|feature_flag" || echo "sqlite-check-skipped"</automated>
  </verify>
  <acceptance_criteria>
    - `nix develop --command sqlite3 localdb.sqlite3 ".tables"` listet WEDER `absence_migration_quarantine` NOCH `employee_yearly_carryover_pre_cutover_backup`
    - Dieselbe Ausgabe listet `absence_period_migration_source` UND `feature_flag` (Survivor)
    - `grep -rl "absence_migration_quarantine\|employee_yearly_carryover_pre_cutover_backup" .sqlx/` → 0 Treffer (kein orphaned Fingerprint)
    - `cargo check --workspace` (mit SQLX_OFFLINE=true) bleibt grün gegen den regenerierten Cache
  </acceptance_criteria>
  <done>Migrationen angewendet; gedroppte Tabellen weg; Survivor-Tabellen da; .sqlx-Cache ohne verwaiste Fingerprints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB-Schema | forward-only DROP/DELETE; keine Daten in Prod betroffen (Modell-Fakt: nie befüllt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-05 | D (Denial/Data-Loss) | Drop-Migrationen | accept | Gedroppte Tabellen sind reine Cutover-Artefakte, in Prod nie befüllt (CONTEXT Modell-Fakt). INT wipeable. `DROP TABLE IF EXISTS` ist idempotent. absence_period_migration_source (lebend) wird NICHT gedroppt. |
| T-086-06 | T (Tampering) | feature_flag Seed-DELETE | accept | DELETE betrifft nur den toten absence_range_source_active-Key; Tabelle + Mechanismus bleiben. |

**Subtraktive Posture:** Nur totes Schema wird entfernt; keine neue Angriffsfläche. Keine HIGH-severity-Threats.
</threat_model>

<verification>
- Migrationsdateien existieren, forward-only (keine Down-Files)
- Dev-DB: gedroppte Tabellen weg, Survivor-Tabellen da
- `.sqlx/` ohne orphaned Fingerprints für gedroppte Tabellen
- Survivor: `grep -rl "absence_period_migration_source" migrations/sqlite/2026061100000*.sql` → 0 (nicht gedroppt)
</verification>

<success_criteria>
- SC3: Drop-Migrations für beide toten Tabellen vorhanden + angewendet
- SC4 (DB): absence_range_source_active-Seed entfernt
- absence_period_migration_source + feature_flag-Tabelle bleiben
- D-05: keine Down-Migrations
</success_criteria>

<output>
After completion, create `.planning/phases/08.6-cutover-abriss/08.6-03-SUMMARY.md`
</output>
