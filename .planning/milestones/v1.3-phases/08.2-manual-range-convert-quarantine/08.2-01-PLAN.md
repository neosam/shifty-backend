---
phase: 08.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rest-types/src/lib.rs
  - service/src/cutover.rs
  - service_impl/src/cutover.rs
  - service_impl/src/test/cutover.rs
  - rest/src/cutover.rs
  - rest/tests/openapi_surface.rs
  - shifty_bin/src/integration_test/cutover.rs
autonomous: true
requirements: [SC-8.2-01, SC-8.2-02, SC-8.2-03, SC-8.2-05, SC-8.2-06]
tags: [cutover, manual-range, quarantine, karin-pattern, gap-1a-closure, backend, rest-types, service-impl, integration-test]

must_haves:
  truths:
    - "POST /admin/cutover/convert-quarantine-entry akzeptiert ein optionales `manual_range` ohne den heuristischen Pfad zu brechen (alte Clients ohne das Feld funktionieren weiter)."
    - "Wenn `manual_range = Some(...)`, skipt der Service `detect_weekly_lump_sum` komplett und schreibt eine `absence_period` direkt mit dem gegebenen Range — gleicher synthetic_run_id-Pfad, gleicher inline-`refreshed_drift_report`."
    - "Backend rejects `start_date > end_date`, year-mismatch zwischen Range und Quarantäne-Eintrag-Jahr, und Overlap mit existing `absence_period`-Rows derselben Person/Kategorie als `ServiceError::ValidationError` (oder `DateOrderWrong` für inverted Ranges)."
    - "`manual_range_resolves_karin_quarantine` Test belegt: Karin-Quarantäne (40h Vacation, mid-week-Vertragswechsel) wird durch manual_range = Some({start, end}) ohne Heuristik aufgelöst — Surface-isoliert (D-35 Empfehlung a aus RESEARCH Open Question 3)."
    - "Privilege-Gate bleibt unverändert `CUTOVER_ADMIN_PRIVILEGE` (existing check Z. 419-421); D-33 cutover_admin OR hr ist auf der FE-Side gewired und braucht keine Backend-Änderung."
    - "OpenAPI surface: `ManualRangeTO` ist als named sub-struct in `EXPECTED_SCHEMAS` ergänzt (D-34 + RESEARCH Open Question 2 — sub-struct gewählt für Type-Safety)."
  artifacts:
    - path: "rest-types/src/lib.rs"
      provides: "ManualRangeTO sub-struct + manual_range: Option<ManualRangeTO> auf CutoverConvertQuarantineEntryRequest mit #[serde(default)] + 2 erweiterte Roundtrip-Tests in cutover_convert_dto_tests"
      contains: "pub struct ManualRangeTO"
    - path: "service/src/cutover.rs"
      provides: "ManualRange { start_date: time::Date, end_date: time::Date } domain-type + erweiterte convert_quarantine_entry Trait-Signatur"
      contains: "pub struct ManualRange"
    - path: "service_impl/src/cutover.rs"
      provides: "manual_range Match-Branch in Step 5 (Z. 459-471 ersetzt) + Validation (parse, year-match, DateRange, find_overlapping); Steps 6-10 unverändert"
      min_lines: 70
    - path: "service_impl/src/test/cutover.rs"
      provides: "mod manual_range_convert_quarantine_tests mit 4 mockall tests (happy-path Karin, inverted, year-boundary, overlap)"
      min_lines: 200
    - path: "rest/src/cutover.rs"
      provides: "Field-Forwarding in cutover_convert_quarantine_entry_handler — TO->Domain mapping für manual_range"
    - path: "rest/tests/openapi_surface.rs"
      provides: "ManualRangeTO in EXPECTED_SCHEMAS"
    - path: "shifty_bin/src/integration_test/cutover.rs"
      provides: "convert_with_manual_range_via_rest — REST-Roundtrip-Test gegen In-Memory-SQLite mit Karin-Fixtures"
  key_links:
    - from: "rest/src/cutover.rs::cutover_convert_quarantine_entry_handler"
      to: "service_impl/src/cutover.rs::convert_quarantine_entry"
      via: "manual_range field-forwarding (TO->Domain mapping)"
      pattern: "request\\.manual_range\\.as_ref\\(\\)\\.map"
    - from: "service_impl/src/cutover.rs::convert_quarantine_entry (manual branch)"
      to: "absence_dao.find_overlapping"
      via: "Overlap-Validation vor absence_dao.create"
      pattern: "absence_dao\\.find_overlapping"
    - from: "service_impl/src/cutover.rs::convert_quarantine_entry (manual branch)"
      to: "absence_dao.create + cutover_dao.upsert_migration_source + extra_hours_service.soft_delete_bulk"
      via: "Steps 6-10 unverändert (existing 8.1-02 flow)"
      pattern: "synthetic_run_id"
---

<objective>
Erweitere den existierenden `convert_quarantine_entry`-Pfad (Phase 8.1-02 / 8.1-04) um einen optionalen `manual_range`-Branch, der die Heuristik (`detect_weekly_lump_sum`) komplett überspringt und stattdessen eine `absence_period` mit dem operator-gegebenen Range schreibt. Schließt gap-1a (Karin-Pattern aus 08.1-10-SUMMARY.md) strukturell, ohne die in 8.1 verifizierten Lila/Anina-Tests anzufassen. Liefert die Backend-Surface, die Plan 08.2-02 (Frontend-Modal) konsumiert — DTO landet zuerst, damit der Frontend-Coroutine-Branch deserialisieren kann.

Purpose: Operator gibt den Ziel-Range direkt vor; Backend validiert (start<=end, year-match, no-overlap) und persistiert. Karin-Quarantäne wird auflösbar ohne neue Heuristik.
Output: Erweitertes DTO + Service-Trait + Service-Impl-Branch + 4 Unit-Tests + 1 Integration-Test + OpenAPI-Schema-Eintrag.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-CONTEXT.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-RESEARCH.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-PATTERNS.md
@.planning/phases/08.1-cutover-migration-ui/08.1-02-SUMMARY.md
@.planning/phases/08.1-cutover-migration-ui/08.1-10-SUMMARY.md

@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Existing contracts der zu erweiternden Surface — extracted aus dem Live-Code,
     damit der Executor nicht erst herumlesen muss. -->

From service/src/cutover.rs (existing trait — current Signatur):
```rust
async fn convert_quarantine_entry(
    &self,
    extra_hours_id: Uuid,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<ConvertQuarantineEntryOutcome, ServiceError>;
```

From rest-types/src/lib.rs (Z. 2322-2325 — current DTO, vor 8.2):
```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct CutoverConvertQuarantineEntryRequest {
    pub extra_hours_id: Uuid,
}
```

From rest-types/src/lib.rs (Z. 2344-2351 — backwards-compat-Vorlage):
```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct CutoverBulkConvertQuarantineRowsRequest {
    pub sales_person_id: Uuid,
    pub category: AbsenceCategoryTO,
    pub year: u32,
    #[serde(default)]
    pub extra_hours_ids: Option<Vec<Uuid>>,    // ← Pattern für 8.2 Optional-Field
}
```

From service_impl/src/cutover.rs (Z. 459-471 — der zu replacing Step-5 Heuristik-Block):
```rust
// 5. Range-auto-compute via Plan 08-09 heuristic. File-private fns
//    are reachable in the same module (RESEARCH P-01).
let (from_date, to_date) =
    detect_weekly_lump_sum(&row, all_legacy.as_ref(), |d| {
        lookup_active_contract(work_details.as_ref(), d)
    })
    .ok_or_else(|| {
        ServiceError::ValidationError(Arc::from([
            service::ValidationFailureItem::InvalidValue(Arc::from(
                "Row does not match weekly-lump-sum heuristic; manual edit required",
            )),
        ]))
    })?;
```

From dao/src/absence.rs (Z. 78-86 — existing DAO surface, nicht zu ändern):
```rust
async fn find_overlapping(
    &self,
    sales_person_id: Uuid,
    category: AbsenceCategoryEntity,
    range: DateRange,
    exclude_logical_id: Option<Uuid>,
    tx: Self::Transaction,
) -> Result<Arc<[AbsencePeriodEntity]>, crate::DaoError>;
```

From service_impl/src/cutover.rs (Z. 481, file-private helper — reuse, nicht ändern):
```rust
fn extra_hours_category_to_absence(c: &ExtraHoursCategoryEntity) -> AbsenceCategoryEntity;
```
</interfaces>
</context>

<vcs_jj_only>
This repo is jj-managed (co-located with git). Per `CLAUDE.local.md` + 8.1 phase precedent:
- Use `jj describe -m "..."` then `jj new` for per-task atomic commits, OR `jj commit -m "..."` (compound).
- NEVER use `git commit` / `git add`. GSD auto-commit ist deaktiviert (`commit_docs: false`).
- Commit-message-Prefix pro Task siehe <output> section.
</vcs_jj_only>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Erweitere rest-types DTO + Service-Trait um manual_range</name>
  <files>rest-types/src/lib.rs, service/src/cutover.rs</files>
  <read_first>
    - rest-types/src/lib.rs Z. 2318-2470 (CutoverConvertQuarantineEntryRequest + cutover_convert_dto_tests)
    - service/src/cutover.rs (Trait `CutoverService` + `ManualRange` neu)
    - 08.2-PATTERNS.md § "rest-types/src/lib.rs — DTO-Erweiterung" (Z. 128-185 in PATTERNS)
    - 08.2-RESEARCH.md § "Code Examples → Range-Validation-Skeleton" (Z. 410-459 in RESEARCH)
  </read_first>
  <behavior>
    - rest-types Roundtrip-Test 1: `convert_request_omits_manual_range_for_backwards_compat` — JSON ohne manual_range deserialisiert sauber → `req.manual_range.is_none()` (D-29 Backwards-Compat).
    - rest-types Roundtrip-Test 2: `convert_request_with_manual_range_roundtrips` — `Some(ManualRangeTO { start_date: "2026-05-04".into(), end_date: "2026-05-08".into() })` serialisiert + deserialisiert mit korrekten Strings.
    - service-Tier `ManualRange` ist `Clone + Debug + PartialEq` (für Mock-Eq in Tests).
  </behavior>
  <action>
    Per D-29 + RESEARCH Open Question 2 (sub-struct gewählt) + PATTERNS § rest-types DTO-Erweiterung:

    **In `rest-types/src/lib.rs`** unmittelbar VOR `CutoverConvertQuarantineEntryRequest` (Z. 2322):

    ```rust
    /// Phase 8.2 (D-29) — manual range override für convert_quarantine_entry.
    /// ISO-8601 (`YYYY-MM-DD`) konsistent zu `CutoverQuarantineEntryTO.date`.
    /// Sub-struct (nicht flach) verhindert via Type-System die "nur eines der
    /// Felder gesetzt"-Edge-Klasse.
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct ManualRangeTO {
        pub start_date: String,
        pub end_date: String,
    }
    ```

    **Modifiziere `CutoverConvertQuarantineEntryRequest`** (Z. 2322-2325) auf:
    ```rust
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct CutoverConvertQuarantineEntryRequest {
        pub extra_hours_id: Uuid,
        /// Phase 8.2 (D-29): wenn Some, skipt Backend die Heuristik und
        /// verwendet den gegebenen Range. Validation: start_date <= end_date;
        /// beide im Quarantäne-Eintrag-Jahr; kein Overlap mit existing
        /// absence_period. Old clients tolerate via #[serde(default)].
        #[serde(default)]
        pub manual_range: Option<ManualRangeTO>,
    }
    ```

    **In `cutover_convert_dto_tests`** (mod ab Z. 2436 — füge zwei Tests an, mirroring `bulk_response_defaults_optional_fields`):
    - `convert_request_omits_manual_range_for_backwards_compat`: deserialisiere `r#"{"extra_hours_id":"00000000-0000-0000-0000-000000000000"}"#` → assert `req.manual_range.is_none()`.
    - `convert_request_with_manual_range_roundtrips`: konstruiere mit `Some(ManualRangeTO { start_date: "2026-05-04".into(), end_date: "2026-05-08".into() })`, serde-roundtrip, assert `parsed.manual_range.as_ref().unwrap().start_date == "2026-05-04"`.

    **In `service/src/cutover.rs`** vor dem `CutoverService`-Trait-Block:
    ```rust
    /// Phase 8.2 (D-29) — domain-tier Manual-Range. REST-Layer parsed
    /// `ManualRangeTO` (String-ISO-8601) und mapped auf diese Domain-Struct.
    /// Backend-Service akzeptiert pre-parsed `time::Date`s — kein doppeltes
    /// Parsing in REST + Service.
    #[derive(Clone, Debug, PartialEq)]
    pub struct ManualRange {
        pub start_date: time::Date,
        pub end_date: time::Date,
    }
    ```

    **Erweitere die `convert_quarantine_entry`-Trait-Methode** um den neuen Parameter (zwischen `extra_hours_id` und `context`):
    ```rust
    async fn convert_quarantine_entry(
        &self,
        extra_hours_id: Uuid,
        manual_range: Option<ManualRange>,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<ConvertQuarantineEntryOutcome, ServiceError>;
    ```

    Update den DocComment auf der Trait-Methode: "Phase 8.2: optional `manual_range` skipt die Heuristik und schreibt direkt eine `absence_period` mit dem gegebenen Range. Validation: start <= end, beide im Quarantäne-Eintrag-Jahr, kein Overlap. `None` (Default) → 8.1-Heuristik-Verhalten unverändert."

    **NICHT** den ServiceImpl-Body in diesem Task ändern — der erstmalige Build-Break wird in Task 2 zusammen mit der Implementation behoben (`cargo check` schlägt nach diesem Task fehl, bis Task 2 ausgeführt ist; das ist erwartet und der Per-Task-Commit-Trigger).

    **Per D-23 (inherited 8.1) bleibt die Privilege-Logik unverändert**: keine Permission-Service-Anpassung in service/src/cutover.rs; Plan 08.2-02 wired das auf der FE-Side.

    **Test-Driven Reihenfolge (TDD per `tdd="true"`):**
    1. Schreibe die zwei rest-types-Tests ZUERST → `cargo test -p rest-types --lib cutover_convert_dto_tests` muss fehlschlagen (Tests referenzieren `ManualRangeTO`/`manual_range`, die noch nicht existieren).
    2. Füge `ManualRangeTO` + Field hinzu → Tests grün.
    3. Erst dann das Service-Trait erweitern (rein syntaktischer Build-Break, der in Task 2 behoben wird).

    **jj-Commit:** `jj commit -m "feat(08.2-01): add ManualRangeTO DTO + ManualRange domain-type + service trait extension"` nach erfolgreichem `cargo test -p rest-types --lib cutover_convert_dto_tests`. Der workspace-build wird in diesem Task absichtlich rot — der nächste Task macht ihn wieder grün.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p rest-types --lib cutover_convert_dto_tests 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `ManualRangeTO` ist exportiert mit `Clone, Debug, Serialize, Deserialize, ToSchema`.
    - `CutoverConvertQuarantineEntryRequest.manual_range: Option<ManualRangeTO>` mit `#[serde(default)]`.
    - 2 neue Tests in `cutover_convert_dto_tests` grün.
    - `service::cutover::ManualRange` exists mit `Clone + Debug + PartialEq`.
    - `convert_quarantine_entry`-Trait-Sig hat zusätzlichen `manual_range: Option<ManualRange>`-Param zwischen `extra_hours_id` und `context`.
    - jj-Commit erstellt mit message-prefix `feat(08.2-01):`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementiere manual_range Service-Branch + REST-Forwarding + 4 mockall Tests</name>
  <files>service_impl/src/cutover.rs, service_impl/src/test/cutover.rs, rest/src/cutover.rs, rest/tests/openapi_surface.rs</files>
  <read_first>
    - service_impl/src/cutover.rs Z. 412-547 (existing convert_quarantine_entry Body — EXAKT der zu erweiternde Block)
    - service_impl/src/cutover.rs Z. 481, 1394-1410 (`extra_hours_category_to_absence` helper — reuse)
    - service_impl/src/absence.rs Z. 189-207 (DAO overlap-validation reference — `ValidationFailureItem::OverlappingPeriod`)
    - service_impl/src/test/cutover.rs (komplette Datei, insbesondere die Karin-Diagnose-Tests am Ende: `diagnose_int_drift_pattern_karin_*` aus Plan 8.1-10) für Fixture-Reuse
    - service_impl/src/test/cutover.rs (8.1-02 Block `convert_quarantine_entry_*_tests`) für Mock-Pattern-Vorlage
    - rest/src/cutover.rs (existing `cutover_convert_quarantine_entry_handler`) für Field-Forwarding
    - rest/tests/openapi_surface.rs Z. 22-29 (Header-Doc) + Z. 92-136 (`EXPECTED_SCHEMAS`)
    - 08.2-PATTERNS.md § "service_impl/src/cutover.rs — Splice manual-range branch" (Z. 33-100 in PATTERNS) + § "service_impl/src/test/cutover.rs — 4 neue mockall-Tests" (Z. 244-289)
    - 08.2-RESEARCH.md § "Code Examples → Range-Validation-Skeleton" (Z. 410-459 in RESEARCH) + § "Pattern Findings 3 — absence_period DAO Overlap-Validation" + § Common Pitfalls P-1, P-2, P-4, P-5
  </read_first>
  <behavior>
    Test 1 — `manual_range_resolves_karin_quarantine` (Surface-isolated per RESEARCH Open Q 3 / D-35):
    - Arrange: Karin-Fixtures (1 Quarantäne-Row 40h Vacation am Wed 2026-05-06; Contract A 8h/d Mo-Fr ending Wed 2026-05-06; Contract B 6h/d Mo-Fr starting Thu 2026-05-07).
    - **Direkt-Mocks (manual-path):** `cutover_dao.find_legacy_extra_hours_not_yet_migrated` → `Arc::from([karin_row])`. `employee_work_details_service.find_by_sales_person_id` → 2 Verträge. `absence_dao.find_overlapping` → `Arc::from([])`. `absence_dao.create` → `.times(1)` mit `from_date=date!(2026-05-04), to_date=date!(2026-05-08), category=Vacation`. `cutover_dao.upsert_migration_source` → `.times(1)`. `extra_hours_service.soft_delete_bulk` → `.times(1)` mit der karin-row-id. (Permission-Service mock: ok für CUTOVER_ADMIN_PRIVILEGE.)
    - **Inline-Replay-Surface (W-3 fix):** `migrate_legacy_extra_hours_to_clusters` und `compute_gate_diagnostic` sind keine Service-Mocks, sondern file-private Helfer auf `CutoverServiceImpl`. Der manual-path ruft sie über `compute_refreshed_drift_report(...)` (Step 9 aus 8.1-02) intern auf. Der Test setzt deren intern-aufgerufene DAO-Calls auf das happy-path Minimum (`find_legacy_extra_hours_not_yet_migrated → []` beim 2. Aufruf, also empty nach soft_delete) — analog 8.1-02 Test 1. **Falls das happy-path-DAO-Setup zu fragil wird,** weicht der Test auf folgende Assertion-Lockerung aus: `outcome.refreshed_drift_report.is_some() OR outcome.refreshed_drift_report.is_none()` (also: nicht testen) und nur strict prüfen: `outcome.absence_period.from_date/to_date/category`. Begründung: Replay ist non-fatal-by-design (8.1-02 hat denselben Fallback dokumentiert) und das ist nicht der Karin-fix den dieser Test absichern soll.
    - Act: `convert_quarantine_entry(karin_id, Some(ManualRange { start: 2026-05-04, end: 2026-05-08 }), Authentication::Full, None)`.
    - Assert (strict): `result.is_ok()`, `outcome.absence_period.from_date == date!(2026-05-04)`, `to_date == date!(2026-05-08)`, `outcome.absence_period.category == AbsenceCategory::Vacation`. Heuristik wird NICHT aufgerufen (keine `detect_weekly_lump_sum`-Mock nötig — der Pfad wird nicht erreicht).
    - Assert (best-effort): `outcome.refreshed_drift_report` darf `Some` ODER `None` sein (siehe Inline-Replay-Surface oben).

    Test 2 — `manual_range_rejects_inverted_range`:
    - manual_range = `Some({ start: 2026-05-08, end: 2026-05-04 })`.
    - Erwarte `Err(ServiceError::DateOrderWrong(start, end))` (per `DateRange::new`).
    - Mock-Assertion: `absence_dao.create.times(0)`, `cutover_dao.upsert_migration_source.times(0)`, `extra_hours_service.soft_delete_bulk.times(0)`.

    Test 3 — `manual_range_rejects_year_boundary_crossing`:
    - Karin-Row im Jahr 2026; manual_range = `Some({ start: 2025-12-29, end: 2026-01-04 })`.
    - Erwarte `Err(ServiceError::ValidationError(...))` mit `InvalidValue` der Klartext "manual_range must lie inside the quarantine row's calendar year" enthält.
    - Keine DAO-Writes (alle `.times(0)`).

    Test 4 — `manual_range_rejects_when_existing_absence_period_overlaps`:
    - Mock `absence_dao.find_overlapping` → `Arc::from([conflict_entity])` (z.B. logical_id = `Uuid::new_v4()`).
    - Erwarte `Err(ServiceError::ValidationError([ValidationFailureItem::OverlappingPeriod(_)]))`.
    - `absence_dao.create.times(0)`, etc.

    Test 5 — REST-Roundtrip-Integration `convert_with_manual_range_via_rest` (in `shifty_bin/src/integration_test/cutover.rs`):
    - Arrange: In-Memory-SQLite mit Karin-Fixtures (analog 8.1-04 / 8.1-10 Setup); cutover_admin user.
    - Act: `POST /admin/cutover/convert-quarantine-entry` mit JSON-Body `{ "extra_hours_id": "<karin>", "manual_range": { "start_date": "2026-05-04", "end_date": "2026-05-08" } }`.
    - Assert (a): HTTP 200.
    - Assert (b): `SELECT count(*) FROM absence_period WHERE sales_person_id = '<karin>' AND deleted IS NULL` → 1 Row mit `from_date='2026-05-04'`, `to_date='2026-05-08'`.
    - Assert (c): `SELECT deleted FROM extra_hours WHERE id = '<karin>'` → IS NOT NULL (soft-deleted).
    - Assert (d): Response-JSON enthält `refreshed_drift_report` (kann None oder Some sein, aber Field MUSS im JSON erscheinen — by `#[serde(default)]`).
  </behavior>
  <action>
    **A) Service-Impl Splice (`service_impl/src/cutover.rs`):**

    Modifiziere die Methoden-Signatur (Z. 412-417) um den neuen Parameter — exakt wie das Trait in Task 1:
    ```rust
    async fn convert_quarantine_entry(
        &self,
        extra_hours_id: Uuid,
        manual_range: Option<service::cutover::ManualRange>,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<ConvertQuarantineEntryOutcome, ServiceError> { ... }
    ```

    Ersetze den existing Step-5-Block (Z. 459-471) — den 13-Zeilen-Block, der `detect_weekly_lump_sum` aufruft — durch eine `match`-Branching-Form:

    ```rust
    // 5. Range-derive: manual override (Phase 8.2, D-29) ODER 8.1 heuristik.
    let (from_date, to_date) = match manual_range {
        Some(r) => {
            // D-30 validation chain.
            let row_year = row.date_time.date().year();
            if r.start_date.year() != row_year || r.end_date.year() != row_year {
                return Err(ServiceError::ValidationError(Arc::from([
                    service::ValidationFailureItem::InvalidValue(Arc::from(
                        "manual_range must lie inside the quarantine row's calendar year",
                    )),
                ])));
            }
            let range = service::DateRange::new(r.start_date, r.end_date)
                .map_err(|_| ServiceError::DateOrderWrong(r.start_date, r.end_date))?;
            let dao_category = extra_hours_category_to_absence(&row.category);
            let conflicts = self
                .absence_dao
                .find_overlapping(row.sales_person_id, dao_category, range, None, tx.clone())
                .await?;
            if !conflicts.is_empty() {
                return Err(ServiceError::ValidationError(Arc::from([
                    service::ValidationFailureItem::OverlappingPeriod(conflicts[0].logical_id),
                ])));
            }
            (r.start_date, r.end_date)
        }
        None => {
            // Existing heuristic path — unchanged from 8.1-02.
            detect_weekly_lump_sum(&row, all_legacy.as_ref(), |d| {
                lookup_active_contract(work_details.as_ref(), d)
            })
            .ok_or_else(|| {
                ServiceError::ValidationError(Arc::from([
                    service::ValidationFailureItem::InvalidValue(Arc::from(
                        "Row does not match weekly-lump-sum heuristic; manual edit required",
                    )),
                ]))
            })?
        }
    };
    ```

    **NICHT ändern**: Steps 6-10 (Z. 473-540 — `absence_dao.create`, `upsert_migration_source` mit `synthetic_run_id`, `soft_delete_bulk`, inline `refreshed_drift_report`-Replay, `transaction_dao.commit`) bleiben byte-identisch. Per D-28 ist die Manual-Path-Persistenz ununterscheidbar von der Heuristik-Path-Persistenz.

    Imports prüfen: `time::Date` (already used Z. 426-427), `service::DateRange`, `service::ValidationFailureItem` (already in scope von 8.1-02 — siehe `OverlappingPeriod`-usage in `absence.rs:189-207`).

    **B) REST-Handler-Forwarding (`rest/src/cutover.rs`):**

    Im existing `cutover_convert_quarantine_entry_handler` baue das TO→Domain-Mapping vor dem service-call:
    ```rust
    let manual_range = req.manual_range.as_ref().map(|to_range| {
        let fmt = time::macros::format_description!("[year]-[month]-[day]");
        // Parse-Errors hier statt im Service: konsistent mit existing Date-Felder
        // (CutoverQuarantineEntryTO.date wird auch im handler-edge geparsed).
        // Beide Felder MÜSSEN parsen — bei Fail werfe ValidationError für "saubere" 422.
        Ok::<service::cutover::ManualRange, ServiceError>(service::cutover::ManualRange {
            start_date: time::Date::parse(&to_range.start_date, fmt).map_err(|_| {
                ServiceError::ValidationError(Arc::from([
                    service::ValidationFailureItem::InvalidValue(Arc::from(
                        "manual_range.start_date is not a valid ISO-8601 date",
                    )),
                ]))
            })?,
            end_date: time::Date::parse(&to_range.end_date, fmt).map_err(|_| {
                ServiceError::ValidationError(Arc::from([
                    service::ValidationFailureItem::InvalidValue(Arc::from(
                        "manual_range.end_date is not a valid ISO-8601 date",
                    )),
                ]))
            })?,
        })
    })
    .transpose()?;
    ```

    Übergebe `manual_range` an den Service-Call: `rest_state.cutover_service().convert_quarantine_entry(req.extra_hours_id, manual_range, context.into(), None).await?`.

    Note: P-5 Pitfall — Frontend sendet `<input type="date">`-Value strikt im `YYYY-MM-DD`-Format; Backend muss freundliche `ServiceError::ValidationError` werfen statt panic. Das ist hier sichergestellt.

    **C) OpenAPI-Surface-Test (`rest/tests/openapi_surface.rs`):**

    Per D-34 + RESEARCH § 4 + PATTERNS § openapi_surface: ergänze `"ManualRangeTO"` als neuen Eintrag in der `EXPECTED_SCHEMAS`-Liste. Plain-Append nach `"CutoverConvertQuarantineEntryResponse"`. Path-Liste unverändert (Path bleibt `/admin/cutover/convert-quarantine-entry`). Der Test-Header (Z. 22-29 "Schema bodies are intentionally NOT pinned") rechtfertigt das additive Field auf der Request-Struct ohne weitere Änderung.

    **D) Backend-Unit-Tests (`service_impl/src/test/cutover.rs`):**

    Append am Dateiende (vor dem `_suppress_unused`-Shim falls vorhanden, sonst als letztes mod):

    ```rust
    mod manual_range_convert_quarantine_tests {
        use super::*;
        // ... 4 Tests wie in <behavior> spezifiziert.
    }
    ```

    Fixture-Reuse: nutze die existing Karin-Helpers aus den `diagnose_int_drift_pattern_karin_*`-Tests aus Plan 8.1-10 (Vertragswechsel-Setup). Falls die Helpers private sind, baue identische Inline-Fixtures (50-80 LOC pro Test ist OK; PATTERNS § 6 erlaubt das).

    Für alle 4 Tests: Permission-Service-Mock muss `check_permission(CUTOVER_ADMIN_PRIVILEGE, _)` → `Ok(())` returnen (existing 8.1-02-Pattern).

    **E) Integration-Test (`shifty_bin/src/integration_test/cutover.rs`):**

    Append `convert_with_manual_range_via_rest`. Vorlage: das existing `test_weekly_lump_sum_commit_succeeds_end_to_end` aus 8.1-04 (siehe `08.1-04-SUMMARY` für die Test-Builder-Helpers + Fixture-Konvention). Diff zum Original:
    - Body enthält `manual_range`-Field.
    - Karin-Fixtures statt einfacher 5-Tage-Lump-Sum-Fixture.
    - Assertion (b)+(c)+(d) wie in <behavior>.

    Pitfall-Defense (P-2 RESEARCH): Test asserted NUR die manual_range-Surface — NICHT post-Convert-Drift = 0. Per RESEARCH Open Q 3 / D-35 ist das die isolierte 8.2-Surface; Drift-Cleanup ist Operator-Verantwortung über mehrere UI-Aktionen.

    **F) TDD-Reihenfolge:**
    1. Schreibe die 4 Unit-Tests ZUERST (RED) → `cargo test -p service_impl manual_range_convert_quarantine_tests` muss fehlschlagen weil Service-Branch noch nicht existiert.
    2. Implementiere Service-Splice + REST-Forwarding (GREEN) → 4 Unit-Tests grün.
    3. Schreibe Integration-Test → läuft grün gegen die fertige Implementation.
    4. Update OpenAPI-Schema-Liste → `cargo test -p rest --test openapi_surface` grün.

    **jj-Commits:**
    Nach erfolgreichem GREEN-Step (Service-Splice + 4 Unit-Tests grün):
    `jj commit -m "feat(08.2-01): implement manual_range branch in convert_quarantine_entry + 4 mockall tests"`

    Nach Integration-Test + OpenAPI-Update grün:
    `jj commit -m "test(08.2-01): rest integration test + openapi surface update for ManualRangeTO"`
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test --workspace --no-fail-fast 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `service_impl/src/cutover.rs::convert_quarantine_entry` hat den manual_range-Branch in Step 5 (~50 LOC ersetzt den 13-LOC Heuristik-only Block).
    - 4 mockall-Tests in `mod manual_range_convert_quarantine_tests` grün.
    - REST-Handler in `rest/src/cutover.rs` mapped `req.manual_range` (ManualRangeTO) auf `service::cutover::ManualRange` (Domain) per ISO-8601-Parse.
    - `rest/tests/openapi_surface.rs::EXPECTED_SCHEMAS` enthält `"ManualRangeTO"`.
    - Integration-Test `convert_with_manual_range_via_rest` grün.
    - `cargo test --workspace` grün, keine Regressions im 8.1-Heuristik-Pfad (siehe Karin-Diagnose-Test bleibt unverändert pass).
    - `cargo check --workspace` grün.
    - 2 jj-Commits mit `feat(08.2-01):` und `test(08.2-01):` Prefixen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backend-Surface-Verifikation + Workspace-Regression-Gate</name>
  <files>(none — verification-only)</files>
  <read_first>
    - 08.2-RESEARCH.md § "Validation Architecture → Phase Requirements → Test Map" (Z. 638-650 in RESEARCH)
    - 08.2-RESEARCH.md § "Sampling Rate" (Z. 651-655 in RESEARCH)
    - .planning/ROADMAP.md Success Criteria 1-3 + 5-6 für Phase 8.2
  </read_first>
  <action>
    Verification-only Task — keine Code-Änderung. Führe folgende Gates aus, dokumentiere die Outputs in der späteren SUMMARY.md:

    1. **Backend-Test-Suite (`cargo test --workspace`)** mit Output-Tail:
       - Gate: 0 failures.
       - Gate: existing Karin-Diagnose-Test (`diagnose_int_drift_pattern_karin_*` aus 8.1-10) bleibt grün — beweist dass die heuristik-only Pfad-Semantik nicht regressiert ist.
       - Gate: `convert_quarantine_entry_*_tests` aus 8.1-02 bleiben alle grün — beweist Backwards-Compat des None-Pfads.

    2. **Workspace-`cargo check`**:
       ```fish
       cargo check --workspace 2>&1 | tail -10
       ```
       - Gate: 0 errors.

    3. **OpenAPI-Surface-Determinismus**:
       ```fish
       for i in 1 2 3
         cargo test -p rest --test openapi_surface 2>&1 | tail -3
       end
       ```
       - Gate: 3 Runs alle grün, identische Output (Plan 08-03-Pattern).

    4. **rest-types DTO Backwards-Compat**:
       ```fish
       cargo test -p rest-types --lib cutover_convert_dto_tests 2>&1 | tail -10
       ```
       - Gate: alle Tests grün, inklusive der zwei in Task 1 hinzugefügten Roundtrip-Tests.

    5. **manual_range-Targeted Test-Suite**:
       ```fish
       cargo test -p service_impl manual_range 2>&1 | tail -10
       cargo test -p shifty_bin convert_with_manual_range 2>&1 | tail -10
       ```
       - Gate: 4 unit + 1 integration = 5 grün.

    Falls irgendein Gate fehlschlägt: STOP, dokumentiere in SUMMARY (Deviations-Section), keine weiteren jj-Commits in diesem Plan. User entscheidet ob Plan revision (siehe revision_mode in planner) nötig ist.

    **Note on WASM-Build-Gate:** Plan 08.2-01 ist pure Backend (rest-types Erweiterung baut WASM-tolerant — `#[serde(default)]` + `Option<T>` ist immer WASM-safe). Der WASM-Build-Gate liegt in Plan 08.2-02 (Frontend), nicht hier.

    **jj-Commit für diese Task** ist optional (verification-only, keine Code-Änderung) — falls die SUMMARY.md committed wird, geschieht das beim PLAN-completion durch den User, nicht innerhalb dieses Tasks.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test --workspace 2>&1 | grep -E "(test result|^error)" | tail -20 ; cargo test -p service_impl manual_range 2>&1 | tail -5 ; cargo test -p shifty_bin convert_with_manual_range 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `cargo test --workspace`: 0 failures.
    - `cargo check --workspace`: 0 errors.
    - 8.1-Karin-Diagnose-Test bleibt grün (kein Heuristik-Pfad regressiert).
    - 8.1-02 `convert_quarantine_entry_*`-Tests bleiben grün (None-Pfad backwards-compat).
    - rest-types-Roundtrip-Tests grün (Backwards-Compat-Garantie).
    - 4 Unit + 1 Integration = 5 manual_range-spezifische Tests grün.
    - OpenAPI-Surface-Test 3-run-deterministisch grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST API | Operator-supplied manual_range ist untrusted (Strings via JSON-Body). Validation am Service-Edge. |
| REST → Service | Domain-tier ManualRange ist parsed time::Date — keine String-Validation mehr nötig im Service. |
| Service → DAO | DAO operations laufen unverändert in single-Tx (atomic per existing Cutover-Pattern). |

## STRIDE Threat Register

8.2 erbt das komplette 8.1-Threat-Model (siehe 08.1-04-PLAN.md threat_model). Manual-range ist eine selektive Branch-Erweiterung; alle 8.1-Mitigations bleiben gültig (Privilege-Gate Z. 419-421, atomic-Tx, soft-delete-Idempotenz, ON CONFLICT DO NOTHING auf migration_source). Hier nur die NEUEN Threats durch das additive `manual_range`-Feld:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-01 | Tampering | REST-Handler ISO-8601-Parse | mitigate | Per `time::Date::parse(format_description!("[year]-[month]-[day]"))` + Fail → `ServiceError::ValidationError` (HTTP 422). Kein panic. RESEARCH P-5 audit. |
| T-08.2-02 | Tampering | Service Range-Validation | mitigate | `start.year() != row_year`-Check VOR dem Tx-write-Pfad → kein DB-State leakt bei Cross-Year-Range (Pitfall P-1). Validation-Errors VOR DAO-writes → impliziter Rollback per Drop. |
| T-08.2-03 | Information Disclosure | OverlappingPeriod-Error gibt logical_id leak | accept | `conflicts[0].logical_id` ist auf demselben sales_person_id beschränkt; Operator hat per D-23 cutover_admin-Privileg, das HR-äquivalent ist (D-33 cutover_admin OR hr — Operator darf alle absence_periods sehen). Kein cross-tenant leak. |
| T-08.2-04 | Denial of Service | Operator-induzierte überlappende Range-Validation als DoS | accept | `find_overlapping` ist indexed auf `idx_absence_period_sales_person_from`. Per-request-Cost ist O(absences-of-that-person). Privilege-Gate (cutover_admin) reduziert die Angriffsoberfläche auf vertraute Operatoren. |
| T-08.2-05 | Repudiation | Manual vs. Heuristik-Pfad nicht im Audit-Trail unterscheidbar | accept | Per D-28 + RESEARCH § 1: beide Pfade schreiben dieselbe `absence_period_migration_source`-Row mit demselben Schema. Ein optionales `migration_strategy: TEXT`-Field wäre eigene Phase (out-of-scope per CONTEXT). Operator-Repudiation gegen sich selbst ist akzeptiert (Operator hat cutover_admin und ist trusted). |
| T-08.2-06 | Elevation of Privilege | manual_range bypassed Heuristik — könnte als Privilege-Escalation missbraucht werden? | mitigate | Privilege-Check (`CUTOVER_ADMIN_PRIVILEGE`) bleibt unverändert in Step 1 (Z. 419-421); manual_range ändert nichts an der Privilege-Surface, sondern nur am Range-Source. Per D-33 ist cutover_admin=hr+admin, kein Endbenutzer hat manual_range-Zugriff. |
</threat_model>

<verification>
Map auf ROADMAP Phase-8.2 Success Criteria 1-6 (Backend-Anteil — Frontend-Anteil von SC2/SC4 in Plan 08.2-02):

- [ ] **SC-1**: `CutoverConvertQuarantineEntryRequest.manual_range: Option<ManualRangeTO>` neu; bei `Some` skipt Heuristik und nutzt gegebenen Range; gleicher synthetic_run_id-Pfad → verifiziert durch Task 1 + Task 2.
- [ ] **SC-2** (Backend-Anteil): Per-Eintrag-API mit Range-Validation und `cutover_admin`-Privilege → verifiziert durch Task 2 unit-tests + integration test.
- [ ] **SC-3**: Karin-Diagnose-Test bleibt; neuer Test `manual_range_resolves_karin_quarantine` löst Karin-Quarantäne ohne Heuristik-Anpassung → verifiziert durch Task 2 Test 1.
- [ ] **SC-5**: OpenAPI-Surface-Test bleibt grün; neuer `ManualRangeTO`-Schema-Eintrag → verifiziert durch Task 2 Section C + Task 3 Gate 3.
- [ ] **SC-6** (Backend-Anteil): `cargo test --workspace` grün; Snapshot-Schema-Version unverändert → verifiziert durch Task 3 Gate 1.
- [ ] (out-of-scope für Plan-01): SC-2 (Frontend-Modal), SC-4 (FE Modal-Replacement), SC-6 (WASM-Build) — Plan 08.2-02.
</verification>

<success_criteria>
After this plan:
- 4 mockall unit tests + 1 REST integration test cover the manual_range branch surface.
- The Karin-Pattern (gap-1a) is structurally resolvable via `convert_quarantine_entry(id, Some(ManualRange { ... }), ...)` without touching the 8.1-Heuristik.
- Backend builds + tests + opaapi-surface-tests grün, no regression in the 8.1 paths.
- Plan 08.2-02 hat eine stabile Backend-Surface (DTO + Endpoint + Validation-Errors) zum Konsumieren.
</success_criteria>

<output>
After plan completion, create `.planning/phases/08.2-manual-range-convert-quarantine/08.2-01-SUMMARY.md`.

Per-task jj-commits (cumulative across the plan):
- `feat(08.2-01): add ManualRangeTO DTO + ManualRange domain-type + service trait extension` — Task 1
- `feat(08.2-01): implement manual_range branch in convert_quarantine_entry + 4 mockall tests` — Task 2 GREEN-step
- `test(08.2-01): rest integration test + openapi surface update for ManualRangeTO` — Task 2 final-step
- `docs(08.2-01): plan summary` — Plan-completion summary commit (User-controlled)

VCS-policy: jj-only (CLAUDE.local.md). NO `git commit`/`git add` calls. The User reviews each `jj describe`-message before `jj new` if working interactively, otherwise `jj commit -m "..."` is fine for autonomous execution.
</output>
