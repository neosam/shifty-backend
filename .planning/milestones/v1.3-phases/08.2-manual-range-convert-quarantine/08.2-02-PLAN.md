---
phase: 08.2
plan: 02
type: execute
wave: 2
depends_on: ["08.2-01"]
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/service/cutover.rs
  - shifty-dioxus/src/page/cutover_admin.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [SC-8.2-02, SC-8.2-04, SC-8.2-06, FUI-A-09]
tags: [cutover, manual-range, frontend, dioxus, modal, i18n, snapshot, wasm-build]

must_haves:
  truths:
    - "Operator klickt 'Edit' auf einer Karin-artigen Quarantäne-Row → ManualConvertModal öffnet sich mit zwei `<input type=date>`-Feldern (start, end), read-only category + read-only amount."
    - "Submit dispatcht `CutoverAction::ConvertSingleManualRange { extra_hours_id, start_date, end_date }`; Coroutine ruft `api::cutover_convert_quarantine_entry(config, id, Some(ManualRangeTO))` und schreibt die `refreshed_drift_report` aus der Response zurück in `CUTOVER_STORE.last_dry_run`."
    - "Wenn `refreshed_drift_report.is_none()` (Backend-Replay-Failure, Pitfall P-6), fallback auf separaten `cutover_gate_dry_run`-Call (analog `UpdateExtraHours`-Branch in service/cutover.rs:142)."
    - "Inline-Validation im Modal: start > end → i18n-Error-Text `CutoverManualConvertErrStartAfterEnd` wird gerendert, Submit blockiert; ungültiges Datum-Format → 'Invalid date format'-Error (KEIN unwrap_or_else-Fallback auf hardcoded Date wie der 8.1-09-Smell, RESEARCH P-7)."
    - "Submit-Erfolg → Modal schließt (`manual_convert_open.set(false)`), Drift-Liste re-rendert via `bump_cutover_refresh()` (RESEARCH Open Q 7)."
    - "8 neue i18n-Keys × 3 Locales (De/En/Cs); Per-Locale-Reference-Matcher-Tests fangen 'Locale::En statt Locale::De'-Bug (Pitfall P-2 inherited aus 8.1-06)."
    - "WASM-Build (`cd shifty-dioxus && nix-shell -p openssl pkg-config lld --command 'cargo build --target wasm32-unknown-unknown'`) liefert Exit-Code 0."
  artifacts:
    - path: "shifty-dioxus/src/api.rs"
      provides: "Erweiterte cutover_convert_quarantine_entry-fn mit `manual_range: Option<ManualRangeTO>`-Parameter"
    - path: "shifty-dioxus/src/service/cutover.rs"
      provides: "Neue `CutoverAction::ConvertSingleManualRange { extra_hours_id, start_date, end_date }`-Variante + Coroutine-Branch (analog ConvertSingle Z. 115-120 aber mit ManualRangeTO-Build und P-6-Fallback)"
    - path: "shifty-dioxus/src/page/cutover_admin.rs"
      provides: "ManualConvertModal-Component (replaces EditExtraHoursModal-Stub Z. 817-897); DriftEntryRow.on_save dispatcht ConvertSingleManualRange; 4 dioxus-ssr-Snapshot-Tests"
      min_lines: 200
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "8 neue Cutover-Keys im Key-Enum + erweiterte Per-Locale-Reference-Matcher-Tests (Pitfall-2-Guard)"
    - path: "shifty-dioxus/src/i18n/de.rs"
      provides: "8 neue add_text(Locale::De, ...)-Calls mit deutschen Texten aus RESEARCH § 6 i18n-Tabelle"
    - path: "shifty-dioxus/src/i18n/en.rs"
      provides: "8 neue add_text(Locale::En, ...)-Calls mit englischen Texten"
    - path: "shifty-dioxus/src/i18n/cs.rs"
      provides: "8 neue add_text(Locale::Cs, ...)-Calls mit tschechischen Texten"
  key_links:
    - from: "page/cutover_admin.rs::DriftEntryRow.on_save"
      to: "service/cutover.rs::CutoverAction::ConvertSingleManualRange"
      via: "cutover_handle.send(CutoverAction::ConvertSingleManualRange { ... })"
      pattern: "ConvertSingleManualRange\\s*\\{"
    - from: "service/cutover.rs::CutoverAction::ConvertSingleManualRange-Branch"
      to: "api::cutover_convert_quarantine_entry"
      via: "format!-ed start/end + ManualRangeTO-Konstruktor"
      pattern: "ManualRangeTO\\s*\\{"
    - from: "page/cutover_admin.rs::ManualConvertModal"
      to: "i18n Key::CutoverManualConvert*"
      via: "i18n.t(Key::...)-Lookups in label/title/error spans"
      pattern: "CutoverManualConvert"
    - from: "i18n/{de,en,cs}.rs"
      to: "i18n/mod.rs Key-Enum + Reference-Matcher-Tests"
      via: "8 neue Keys × 3 Locales = 24 add_text-Calls + Test-Erweiterungen"
      pattern: "CutoverManualConvert(ModalTitle|Help|StartLabel|EndLabel|BtnSubmit|ErrStartAfterEnd|ErrYearMismatch|ErrOverlap)"
---

<objective>
Ersetze den in Plan 08.1-09 absichtlich als Stub belassenen `EditExtraHoursModal` (cutover_admin.rs Z. 817-897 — `on_save` ist no-op) durch ein neues `ManualConvertModal` mit Date-Range-Picker, das per `CutoverAction::ConvertSingleManualRange` den in Plan 08.2-01 gebauten Backend-Pfad konsumiert. Liefert die FE-Surface, die gap-1a (Karin) operativ auflöst, plus 8 neue i18n-Keys × 3 Locales und 4 dioxus-ssr-Snapshot-Tests.

Purpose: Operator gibt im Modal Datum-von / Datum-bis ein, Backend (Plan 01) validiert + persistiert. Modal-UX schließt nach Submit, Drift-Liste re-rendered.
Output: Modal-Replacement + Coroutine-Branch + API-Erweiterung + 8 i18n-Keys × 3 Locales + Snapshot-Tests + WASM-Build-Gate.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-CONTEXT.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-RESEARCH.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-PATTERNS.md
@.planning/phases/08.2-manual-range-convert-quarantine/08.2-01-SUMMARY.md  # Backend-Surface ist gelandet
@.planning/phases/08.1-cutover-migration-ui/08.1-09-SUMMARY.md             # Modal-Stub-Reference

@CLAUDE.md
@CLAUDE.local.md
@shifty-dioxus/CLAUDE.md

<interfaces>
<!-- Critical existing types — der Executor referenziert die DIREKT,
     keine Codebase-Exploration nötig. -->

From rest-types (gelandet in Plan 08.2-01):
```rust
pub struct ManualRangeTO {
    pub start_date: String,    // ISO-8601 "YYYY-MM-DD"
    pub end_date: String,
}

pub struct CutoverConvertQuarantineEntryRequest {
    pub extra_hours_id: Uuid,
    #[serde(default)]
    pub manual_range: Option<ManualRangeTO>,
}

pub struct CutoverConvertQuarantineEntryResponse {
    pub absence_period: AbsencePeriodTO,
    pub deleted_extra_hours_id: Uuid,
    #[serde(default)]
    pub refreshed_drift_report: Option<CutoverGateDriftReportTO>,
}
```

From shifty-dioxus/src/service/cutover.rs Z. 59-72 (existing CutoverAction enum):
```rust
pub enum CutoverAction {
    LoadProfile,
    RunDryRun,
    Commit,
    ConvertSingle(Uuid),
    BulkConvert { /* ... */ },
    UpdateExtraHours(/* ExtraHoursTO */),
    DeleteExtraHours(Uuid),
    Skip(Uuid),
}
```

From shifty-dioxus/src/service/cutover.rs Z. 115-120 (existing ConvertSingle-Branch — direkte Vorlage):
```rust
CutoverAction::ConvertSingle(extra_hours_id) => {
    let resp = api::cutover_convert_quarantine_entry(config, extra_hours_id).await?;
    CUTOVER_STORE.write().last_dry_run = resp.refreshed_drift_report;
    bump_cutover_refresh();
}
```

From shifty-dioxus/src/page/cutover_admin.rs Z. 696-812 (existing DriftEntryRow + EditExtraHoursModal-Stub):
```rust
#[component]
fn DriftEntryRow(entry: EntryRef, drift_row_meta: DriftRowRef, is_cutover_admin: bool) -> Element {
    let cutover_handle = try_consume_context::<Coroutine<CutoverAction>>();
    let mut edit_open = use_signal(|| false);   // ← rename → manual_convert_open
    // ... ConvertSingle-Button (Z. 740-755) — bleibt unverändert (heuristik path)
    // ... Edit-Button (Z. 756-769) — öffnet edit_open=true → Modal
    if *edit_open.read() {
        EditExtraHoursModal {                   // ← REPLACE durch ManualConvertModal
            entry: entry_for_modal.clone(),
            on_save: move |(_eh_id, _amount_h, _date): (Uuid, f64, time::Date)| {
                edit_open.set(false);          // ← Stub — no dispatch
            },
            on_cancel: move |_| { edit_open.set(false); },
        }
    }
}
```

From shifty-dioxus/src/page/cutover_admin.rs Z. 817-897 (existing EditExtraHoursModal — Backdrop-Pattern reusable, Inputs ersetzen):
```rust
fn EditExtraHoursModal(
    entry: EntryRef,
    on_save: EventHandler<(Uuid, f64, time::Date)>,
    on_cancel: EventHandler<()>,
) -> Element {
    rsx! {
        div { class: "fixed inset-0 bg-modal-veil flex items-center justify-center z-50",
            onclick: move |_| on_cancel.call(()),                  // backdrop click → close
            div { class: "bg-surface rounded-lg p-6 ... border border-border",
                onclick: move |ev| ev.stop_propagation(),          // prevent close on inner
                // ... amount input (Z. 855-865, will be REMOVED — read-only span instead per D-32) ...
                // ... date input (Z. 866-873, becomes 2× start + end inputs) ...
                button { onclick: move |_| {
                    // Z. 884-888 — DON'T COPY THE unwrap_or_else FALLBACK (RESEARCH P-7)
                }
            }
        }
    }
}
```

From shifty-dioxus/src/api.rs (existing cutover_convert_quarantine_entry signature):
```rust
pub async fn cutover_convert_quarantine_entry(
    config: Config,
    extra_hours_id: Uuid,
) -> Result<CutoverConvertQuarantineEntryResponse, ShiftyError>;
```

From shifty-dioxus/src/i18n/mod.rs Z. 497-531 (existing 34 Cutover-Keys — append after `CutoverPrivilegeStage3`):
```rust
// Existing keys (Phase 8.1): CutoverMenuLabel, CutoverPageTitle, CutoverPageSubtitle, ...
// Reuse for 8.2:
//   CutoverEditBtnCancel — als Cancel-Button im ManualConvertModal
//   CutoverErrorApiFailure — als generisches API-Failure-Toast
//   CutoverEditAmountLabel — als read-only Stunden-Anzeige-Label (D-32)
```
</interfaces>
</context>

<vcs_jj_only>
This repo is jj-managed (co-located with git). Per `CLAUDE.local.md` + 8.1 phase precedent:
- Use `jj describe -m "..."` then `jj new` for per-task atomic commits, OR `jj commit -m "..."` (compound).
- NEVER use `git commit` / `git add`. GSD auto-commit ist deaktiviert (`commit_docs: false`).
- Commit-message-Prefix pro Task siehe <output> section.
</vcs_jj_only>

<tasks>

<task type="auto">
  <name>Task 1: API-Erweiterung + CutoverAction-Variante + Coroutine-Branch</name>
  <files>shifty-dioxus/src/api.rs, shifty-dioxus/src/service/cutover.rs</files>
  <read_first>
    - shifty-dioxus/src/api.rs (existing `cutover_convert_quarantine_entry`-fn — die genaue Line-Range ist im File ~1467; vor 8.2 nimmt sie nur `extra_hours_id`)
    - shifty-dioxus/src/service/cutover.rs (komplett — 165 LOC; insbesondere Z. 59-72 enum + Z. 95-160 Action-Handler-Match)
    - 08.2-PATTERNS.md § "shifty-dioxus/src/api.rs" (Z. 318-340 in PATTERNS) + § "shifty-dioxus/src/service/cutover.rs" (Z. 343-385 in PATTERNS)
    - 08.2-RESEARCH.md § "Code Examples → Coroutine-Branch (Plan-2)" (Z. 561-585 in RESEARCH)
    - 08.2-RESEARCH.md § Common Pitfalls P-5, P-6
  </read_first>
  <action>
    **A) API-Layer-Erweiterung (`shifty-dioxus/src/api.rs`):**

    Modifiziere die existing `cutover_convert_quarantine_entry`-fn-Signatur additiv (RESEARCH § api.rs DRY-Empfehlung):

    ```rust
    pub async fn cutover_convert_quarantine_entry(
        config: Config,
        extra_hours_id: Uuid,
        manual_range: Option<rest_types::ManualRangeTO>,    // NEW (Phase 8.2, D-29)
    ) -> Result<CutoverConvertQuarantineEntryResponse, ShiftyError> {
        info!("POST /admin/cutover/convert-quarantine-entry id={extra_hours_id}");
        let url = format!("{}/admin/cutover/convert-quarantine-entry", config.backend);
        let body = CutoverConvertQuarantineEntryRequest { extra_hours_id, manual_range };
        let client = reqwest::Client::new();
        let response = client.post(url).json(&body).send().await?;
        // existing 422 + error_for_status_ref + json-parse — UNVERÄNDERT
        // ...
    }
    ```

    Update den existing call-site in `service/cutover.rs::ConvertSingle`-Branch (Z. 116) auf den neuen Param:
    ```rust
    let resp = api::cutover_convert_quarantine_entry(config, extra_hours_id, None).await?;
    //                                                                  ^^^^ Heuristik-Pfad
    ```

    **B) CutoverAction-Variante + Branch (`shifty-dioxus/src/service/cutover.rs`):**

    Füge neue Variante zum `CutoverAction`-Enum (Z. 59-72), nach `ConvertSingle(Uuid)`:
    ```rust
    pub enum CutoverAction {
        // ... existing ...
        ConvertSingle(Uuid),
        ConvertSingleManualRange {
            extra_hours_id: Uuid,
            start_date: time::Date,
            end_date: time::Date,
        },
        // ... existing ...
    }
    ```

    Füge Branch-Body in der Action-Handler-Match (Z. 95-160 — nach dem `ConvertSingle`-Branch Z. 115-120):
    ```rust
    CutoverAction::ConvertSingleManualRange { extra_hours_id, start_date, end_date } => {
        let fmt = time::macros::format_description!("[year]-[month]-[day]");
        let manual_range = rest_types::ManualRangeTO {
            start_date: start_date.format(&fmt).unwrap_or_default(),
            end_date: end_date.format(&fmt).unwrap_or_default(),
        };
        let resp = api::cutover_convert_quarantine_entry(
            config.clone(),
            extra_hours_id,
            Some(manual_range),
        ).await?;
        // P-6 Fallback: wenn Backend-Replay scheitert → separater gate-dry-run.
        let drift = match resp.refreshed_drift_report {
            Some(r) => Some(r),
            None => api::cutover_gate_dry_run(config)
                .await
                .ok()
                .and_then(|r| r.gate_drift_report),
        };
        CUTOVER_STORE.write().last_dry_run = drift;
        bump_cutover_refresh();
    }
    ```

    Pitfall-Defense (P-5): `start_date.format(&fmt).unwrap_or_default()` würde theoretisch leeren String produzieren bei Fail — Backend würde dann Validation-Error werfen (Plan 01 Task 2 Section B). Der Operator-Pfad ist resilient. ABER: per Modal-Submit-Logik (Task 2) sind beide Dates GARANTIERT geparsed `time::Date`s — die Modal-Submit-Validation lehnt invalid Dates ab. `unwrap_or_default()` ist defensive-belt-and-braces, nicht primary validation.

    **C) Existing-Tests-Smoke (kein neuer Test in diesem Task):**

    Keine neuen Test-Files in Task 1 — der Coroutine-Branch wird durch den Snapshot-Test in Task 2 (`manual_convert_modal_dispatches_action_on_valid_submit`) indirekt verifiziert (try_consume_context-Pattern aus 8.1-09 Test 11).

    **jj-Commit:** `jj commit -m "feat(08.2-02): add ConvertSingleManualRange action + api manual_range param"` nach erfolgreichem `cargo check --bin shifty-dioxus`.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo check 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `api::cutover_convert_quarantine_entry` hat zusätzlichen `manual_range: Option<ManualRangeTO>`-Param.
    - Existing `ConvertSingle`-Branch ruft nun mit `None` als 3. Arg auf (heuristik path unchanged).
    - `CutoverAction::ConvertSingleManualRange { extra_hours_id, start_date, end_date }`-Variante neu.
    - Action-Handler-Branch dispatched API-Call + P-6-Fallback + bump_cutover_refresh.
    - `cargo check` im `shifty-dioxus/`-Subordner grün.
    - jj-Commit erstellt.
  </done>
</task>

<task type="auto">
  <name>Task 2: i18n 8 Keys × 3 Locales + Reference-Matcher-Test-Erweiterung</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs Z. 497-531 (existing Cutover-Key-Block — neue Keys appenden) + Z. 752+ (Per-Locale-Reference-Matcher-Tests aus 8.1-06)
    - shifty-dioxus/src/i18n/de.rs (komplett, scan auf existing Cutover-Key-Block)
    - shifty-dioxus/src/i18n/en.rs (komplett)
    - shifty-dioxus/src/i18n/cs.rs (komplett)
    - 08.2-PATTERNS.md § "shifty-dioxus/src/i18n/{mod,de,en,cs}.rs" (Z. 501-590 in PATTERNS)
    - 08.2-RESEARCH.md § "Pattern Findings 6 — i18n-Key-Estimate" (Z. 270-307 in RESEARCH) — hat fertige DE/EN-Strings; Plan-2 wählt CS
  </read_first>
  <action>
    **A) Key-Enum (`shifty-dioxus/src/i18n/mod.rs`):**

    Append am Ende des existing Cutover-Key-Blocks (Z. 497-531):
    ```rust
    // Cutover Manual Range (Phase 8.2)
    CutoverManualConvertModalTitle,
    CutoverManualConvertHelp,
    CutoverManualConvertStartLabel,
    CutoverManualConvertEndLabel,
    CutoverManualConvertBtnSubmit,
    CutoverManualConvertErrStartAfterEnd,
    CutoverManualConvertErrYearMismatch,
    CutoverManualConvertErrOverlap,
    ```

    **B) DE-Locale (`shifty-dioxus/src/i18n/de.rs`):**

    Append 8 add_text-Calls (RESEARCH § 6 fertige Strings):
    ```rust
    add_text(Locale::De, Key::CutoverManualConvertModalTitle, "Urlaub manuell anlegen");
    add_text(Locale::De, Key::CutoverManualConvertHelp,
        "Du legst den Zeitraum direkt fest, statt die Heuristik raten zu lassen.");
    add_text(Locale::De, Key::CutoverManualConvertStartLabel, "Datum von");
    add_text(Locale::De, Key::CutoverManualConvertEndLabel, "Datum bis");
    add_text(Locale::De, Key::CutoverManualConvertBtnSubmit, "Anlegen");
    add_text(Locale::De, Key::CutoverManualConvertErrStartAfterEnd,
        "Startdatum muss vor oder gleich Enddatum sein.");
    add_text(Locale::De, Key::CutoverManualConvertErrYearMismatch,
        "Datum muss im selben Jahr wie der Quarantäne-Eintrag liegen.");
    add_text(Locale::De, Key::CutoverManualConvertErrOverlap,
        "Bestehende Abwesenheit überschneidet sich mit diesem Zeitraum.");
    ```

    **C) EN-Locale (`shifty-dioxus/src/i18n/en.rs`):**
    ```rust
    add_text(Locale::En, Key::CutoverManualConvertModalTitle, "Set absence range manually");
    add_text(Locale::En, Key::CutoverManualConvertHelp,
        "You set the date range directly instead of letting the heuristic guess.");
    add_text(Locale::En, Key::CutoverManualConvertStartLabel, "Start date");
    add_text(Locale::En, Key::CutoverManualConvertEndLabel, "End date");
    add_text(Locale::En, Key::CutoverManualConvertBtnSubmit, "Create");
    add_text(Locale::En, Key::CutoverManualConvertErrStartAfterEnd,
        "Start date must be on or before end date.");
    add_text(Locale::En, Key::CutoverManualConvertErrYearMismatch,
        "Date must be in the same year as the quarantine entry.");
    add_text(Locale::En, Key::CutoverManualConvertErrOverlap,
        "An existing absence overlaps with this range.");
    ```

    **D) CS-Locale (`shifty-dioxus/src/i18n/cs.rs`):**

    Tschechische Übersetzungen (RESEARCH lieferte DE/EN; CS-Strings hier neu — konsistent mit existing CS-Cutover-Vokabular):
    ```rust
    add_text(Locale::Cs, Key::CutoverManualConvertModalTitle, "Ručně nastavit dovolenou");
    add_text(Locale::Cs, Key::CutoverManualConvertHelp,
        "Rozsah dat zadáváš přímo, místo aby ho hádala heuristika.");
    add_text(Locale::Cs, Key::CutoverManualConvertStartLabel, "Od");
    add_text(Locale::Cs, Key::CutoverManualConvertEndLabel, "Do");
    add_text(Locale::Cs, Key::CutoverManualConvertBtnSubmit, "Vytvořit");
    add_text(Locale::Cs, Key::CutoverManualConvertErrStartAfterEnd,
        "Datum začátku musí být před koncem nebo stejné.");
    add_text(Locale::Cs, Key::CutoverManualConvertErrYearMismatch,
        "Datum musí být ve stejném roce jako karanténní záznam.");
    add_text(Locale::Cs, Key::CutoverManualConvertErrOverlap,
        "Existující nepřítomnost se překrývá s tímto rozsahem.");
    ```

    **E) Per-Locale-Reference-Matcher-Test-Erweiterung (`shifty-dioxus/src/i18n/mod.rs` Z. 752+):**

    Erweitere die existing 4 Tests (`i18n_cutover_keys_match_german_reference`, `..._english_reference`, `..._czech_reference`, `..._present_in_all_locales`) um die 8 neuen Keys (PATTERNS § Reference-Matcher-Test-Erweiterung Z. 543-588):

    Im DE-Test 2-3 Stichproben:
    ```rust
    assert_eq!(i18n.t(Key::CutoverManualConvertModalTitle).as_ref(), "Urlaub manuell anlegen");
    assert_eq!(i18n.t(Key::CutoverManualConvertStartLabel).as_ref(), "Datum von");
    assert_eq!(i18n.t(Key::CutoverManualConvertBtnSubmit).as_ref(), "Anlegen");
    ```

    Im EN-Test 2 Stichproben:
    ```rust
    assert_eq!(i18n.t(Key::CutoverManualConvertModalTitle).as_ref(), "Set absence range manually");
    assert_eq!(i18n.t(Key::CutoverManualConvertStartLabel).as_ref(), "Start date");
    ```

    Im CS-Test 2 Stichproben:
    ```rust
    assert_eq!(i18n.t(Key::CutoverManualConvertModalTitle).as_ref(), "Ručně nastavit dovolenou");
    assert_eq!(i18n.t(Key::CutoverManualConvertEndLabel).as_ref(), "Do");
    ```

    Im `i18n_cutover_keys_present_in_all_locales`-Test: erweitere die `for key in [...]`-Schleife um die 8 neuen Keys (jeder muss in allen drei Locales `!is_empty() && != "??"` ergeben). Pitfall-2-Guard: würde fangen, wenn `de.rs` versehentlich `add_text(Locale::En, ...)` aufruft.

    **jj-Commit:** `jj commit -m "feat(08.2-02): add 8 i18n keys for manual-convert modal across de/en/cs + reference-matcher tests"` nach `cargo test --bin shifty-dioxus i18n_cutover_keys` grün.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo test --bin shifty-dioxus i18n_cutover_keys 2>&1 | tail -15</automated>
  </verify>
  <done>
    - 8 neue Key-Enum-Varianten in `i18n/mod.rs`.
    - 8 add_text-Calls pro Locale (de/en/cs) = 24 Calls total.
    - 4 Reference-Matcher-Tests + Present-in-all-locales-Test grün, alle 8 neuen Keys covered.
    - Pitfall-2-Guard (per-Locale-Reference-Matcher) fängt potential Locale::En-statt-Locale::De-Bug.
    - jj-Commit erstellt.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ManualConvertModal-Component + DriftEntryRow-Wiring + 4 dioxus-ssr Snapshot-Tests + WASM-Build-Gate</name>
  <files>shifty-dioxus/src/page/cutover_admin.rs</files>
  <read_first>
    - shifty-dioxus/src/page/cutover_admin.rs Z. 696-812 (DriftEntryRow + Stub-Modal-Wiring)
    - shifty-dioxus/src/page/cutover_admin.rs Z. 817-897 (existing EditExtraHoursModal — Backdrop-Pattern reusable, Inputs replaced)
    - shifty-dioxus/src/page/cutover_admin.rs Z. 920+ (ssr-Test-Helpers wie `pin_de_locale` aus 8.1-09)
    - shifty-dioxus/src/page/cutover_admin.rs Z. 1284+ (zweite EditExtraHoursModal-Aufruf-Stelle, falls vorhanden — beide call-sites müssen migrieren)
    - shifty-dioxus/src/page/absences.rs Z. 1066-1076 (TextInput date-Wrapper-Pattern für Konsistenz-Empfehlung A aus RESEARCH Open Q 4)
    - 08.2-PATTERNS.md § "shifty-dioxus/src/page/cutover_admin.rs — Replace EditExtraHoursModal Stub" (Z. 389-498 in PATTERNS) + § Frontend-Patterns
    - 08.2-RESEARCH.md § "Code Examples → Frontend Manual-Convert-Modal-Skeleton" (Z. 461-559 in RESEARCH) + § Common Pitfalls P-7
  </read_first>
  <behavior>
    Test 1 — `manual_convert_modal_renders_two_date_inputs`:
    - SSR-Render in `Locale::De` mit Karin-artigem `EntryRef` (extra_hours_id, date="2026-05-06", amount=40.0, weekday="Wed").
    - Assert: rendered HTML enthält `type="date"` mind. 2× (start + end).
    - Assert: rendered HTML enthält `40.00h` als statischen Text (kein `<input value="40">` für amount — D-32 Negativ-Assertion).
    - Assert: rendered HTML enthält keinen Kategorie-`<input>` (D-31 Negativ-Assertion — Kategorie nur als statische `<span>`).
    - Assert: rendered HTML enthält "Datum von" + "Datum bis" (DE-Locale Labels).
    - Assert: rendered HTML enthält "Urlaub manuell anlegen" (DE-Modal-Title).

    Test 2 — `manual_convert_modal_renders_validation_error_when_start_after_end`:
    - SSR-Render mit pre-set `start_str=2026-05-08`, `end_str=2026-05-04`, `error_msg=Some(i18n.t(Key::CutoverManualConvertErrStartAfterEnd).to_string())` (test simuliert post-submit-state).
    - Assert: rendered HTML enthält "Startdatum muss vor oder gleich Enddatum sein." (DE) im `<span class="text-bad ...">`.

    Test 3 — `manual_convert_modal_not_rendered_when_closed` (Plan-Checker W-2 fix):
    - SSR ist render-only — echte Click-Handler-Auslösung des `manual_convert_open`-Signals
      ist nicht möglich (8.1-09 Test 11 dokumentiert dasselbe Limit).
    - Stattdessen: Render `DriftEntryRow` zweimal — einmal mit `manual_convert_open == false`
      (default), einmal mit dem Modal explizit aufgemacht.
    - Assert: bei `false` enthält das HTML KEIN `bg-modal-veil` und KEIN `<input r#type="date">`;
      bei aufgemachtem Modal enthält das HTML beide. Das deckt den open/close-Toggle auf
      Render-Level ab — Cancel-Click selbst ist UAT-Territory (8.1-12 / Phase-8.2-UAT).

    Test 4 — `manual_convert_modal_dispatches_action_on_valid_submit`:
    - Pattern aus 8.1-09 Test 11 (`try_consume_context::<Coroutine<CutoverAction>>()`-Negativ-Path):
    - SSR-Render ohne registrierten Coroutine → `cutover_handle.is_none()` → render proceeds without dispatch.
    - Assert: rendered HTML enthält Submit-Button (`<button>...Anlegen</button>`).
    - (Echter Action-Dispatch wird in WASM-Runtime tested durch UAT in Phase 8.1-12 oder folgenden UAT-Subsumption — der ssr-Test verifiziert nur das Render-Surface.)
  </behavior>
  <action>
    **A) ManualConvertModal-Component (`shifty-dioxus/src/page/cutover_admin.rs`):**

    Ersetze die existing `EditExtraHoursModal`-Definition (Z. 817-897) komplett mit `ManualConvertModal`. Vorlage: RESEARCH § "Frontend Manual-Convert-Modal-Skeleton" (Z. 461-559 in RESEARCH) + PATTERNS § Z. 393-498 — die fertigen Skeletons sind verifizierte Live-Patterns, hier wörtlich übertragen mit den 8 i18n-Keys aus Task 2.

    Wichtige Konventionen:
    - **Custom-Backdrop** wie 8.1-09 (Z. 846-895): `fixed inset-0 bg-modal-veil flex items-center justify-center z-50` + outer `onclick = on_cancel.call(())` + inner `onclick = ev.stop_propagation()`. PATTERNS § Custom-Backdrop-Modal Pattern. (RESEARCH Open Q 4 Empfehlung A wäre TextInput-Atom, aber CONTEXT.md/Open Q 4 lässt Discretion offen — wir wählen die Custom-Backdrop-Variante mit rohen `<input type="date">` plus stabilem ssr-Snapshot. Begründung: direkter Nachbar-Stil ist verifiziert SSR-test-stabil aus 8.1-09; Migration der existing Patterns würde Scope erweitern.)
    - **Read-only context-row (D-31 + D-32)**: `<span>` mit `i18n.t(Key::CutoverEditAmountLabel)` + `{amount:.2}h`-Format + Kategorie-Display. KEIN `<input>` für amount oder category.
    - **2 Date-Inputs**: `<input r#type="date" value="{start_str}" oninput=...>` und `<input r#type="date" value="{end_str}" oninput=...>` mit Labels per `i18n.t(Key::CutoverManualConvertStartLabel/EndLabel)`.
    - **Inline-Error-Render**: `if let Some(e) = error_msg.read().clone() { span { class: "text-bad text-small", "{e}" } }`.
    - **Submit-Button** (RESEARCH P-7 Defense — KEIN `unwrap_or_else(|_| date!(2026-01-01))`-Smell):
      ```rust
      onclick: move |_| {
          let fmt = time::macros::format_description!("[year]-[month]-[day]");
          match (
              time::Date::parse(start_str.read().as_str(), fmt),
              time::Date::parse(end_str.read().as_str(), fmt),
          ) {
              (Ok(s), Ok(e)) if s <= e => on_submit.call((entry_id, s, e)),
              (Ok(s), Ok(e)) /* s > e */ => error_msg.set(Some(i18n.t(Key::CutoverManualConvertErrStartAfterEnd).to_string())),
              _ => error_msg.set(Some("Invalid date format".to_string())),
          }
      }
      ```
    - **Cancel-Button** (Reuse aus 8.1): `i18n.t(Key::CutoverEditBtnCancel)` (RESEARCH § 6 Reuse-Plan).
    - **Submit-Button-Label**: `i18n.t(Key::CutoverManualConvertBtnSubmit)`.

    Component-Signatur:
    ```rust
    #[component]
    fn ManualConvertModal(
        entry: EntryRef,
        category: AbsenceCategoryTO,
        on_submit: EventHandler<(Uuid, time::Date, time::Date)>,
        on_cancel: EventHandler<()>,
    ) -> Element { ... }
    ```

    **STATIC Tailwind Match-Arms (Pitfall 5 inherited)**: alle conditional class-strings sind String-Literal-Match-Arms, niemals `format!("text-{}-soft", ...)`.

    **B) DriftEntryRow-Wiring (`shifty-dioxus/src/page/cutover_admin.rs` Z. 696-812):**

    Renames:
    - `let mut edit_open = use_signal(|| false);` → `let mut manual_convert_open = use_signal(|| false);`
    - Edit-Button-Branch (Z. 763-768): `edit_open.set(true)` → `manual_convert_open.set(true)`. Button-Label bleibt `CutoverRowBtnEdit` (oder optional umbenennen — bleibt aber Edit-Button-Surface; UX-Discretion).

    Replace Modal-Aufruf (Z. 796-811):
    ```rust
    if *manual_convert_open.read() {
        ManualConvertModal {
            entry: entry_for_modal.clone(),
            // Category lives on the parent DriftRow (not on the entry itself).
            // CutoverQuarantineEntryTO has NO `category` field — see rest-types/src/lib.rs:1943-1959.
            category: drift_row_meta.category.clone(),
            on_submit: move |(eh_id, start, end): (Uuid, time::Date, time::Date)| {
                if let Some(h) = cutover_handle.as_ref() {
                    h.send(CutoverAction::ConvertSingleManualRange {
                        extra_hours_id: eh_id,
                        start_date: start,
                        end_date: end,
                    });
                }
                manual_convert_open.set(false);    // RESEARCH Open-Question 7: close on submit
            },
            on_cancel: move |_| { manual_convert_open.set(false); },
        }
    }
    ```

    **Wichtig zur Category-Quelle (Plan-Checker W-1):** `CutoverQuarantineEntryTO` enthält **kein** `category`-Feld (verifiziert gegen `rest-types/src/lib.rs:1943-1959` — die struct hat nur `extra_hours_id`, `date`, `weekday`, `amount`, `reason_code`, `reason_text`, `suggested_action`). Die Kategorie lebt ausschließlich auf der Parent-`CutoverGateDriftRowTO` (`category: AbsenceCategoryTO`, Z. 1965). `DriftEntryRow` hat den Parent-Drift-Row als `drift_row_meta: DriftRowRef` Prop — die einzige korrekte Quelle für `category_for_modal` ist daher `drift_row_meta.category.clone()`. Keine Fallbacks; `entry.0.category` existiert nicht und compiliert nicht.

    Falls die zweite EditExtraHoursModal-Aufruf-Stelle (Z. 1284+, falls vorhanden) existiert, migriere sie analog.

    **C) 4 dioxus-ssr Snapshot-Tests (`shifty-dioxus/src/page/cutover_admin.rs` am Test-Block-Ende):**

    Append ans existing Test-Module (im selben Pattern wie 8.1-09 Test 11). Helper `pin_de_locale` ist bereits aus 8.1-09 vorhanden (Z. 920+). Tests wie in <behavior> spezifiziert — TDD-Reihenfolge:
    1. Schreibe Test 1 (`manual_convert_modal_renders_two_date_inputs`) ZUERST → fails weil Component-File noch keine `ManualConvertModal` exportiert.
    2. Implementiere Component → Test 1 grün.
    3. Schreibe Test 2 (validation-error-state) → grün gegen Component (error_msg-Signal-Inspect via SSR vom pre-set state).
    4. Schreibe Test 3 + Test 4 → grün.

    Snapshot-Pattern (8.1-09 Vorlage):
    ```rust
    let mut vdom = VirtualDom::new(/* root with Coroutine optional */);
    pin_de_locale(&mut vdom);
    let _ = vdom.rebuild_in_place();
    let html = dioxus_ssr::render(&vdom);
    assert!(html.contains(r#"type="date""#));
    // ...
    ```

    **D) WASM-Build-Gate:**

    Nach allen Tests grün, führe vor dem letzten jj-Commit aus:
    ```fish
    cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus
    nix-shell -p openssl pkg-config lld --command "cargo build --target wasm32-unknown-unknown" 2>&1 | tail -10
    ```
    Gate: Exit-Code 0, keine Errors. (8.1 Convention — siehe 8.1-09 + ROADMAP § Phase 8.1 SC-11 + § Phase 8.2 SC-6.)

    **jj-Commits:**
    Nach Component + Wiring + 4 Tests grün:
    `jj commit -m "feat(08.2-02): replace EditExtraHoursModal stub with ManualConvertModal + 4 ssr snapshot tests"`

    Nach WASM-Build-Gate grün:
    `jj commit -m "test(08.2-02): wasm build gate verification (passes — green per ROADMAP SC-6)"` (optional, kann auch im SUMMARY-Commit zusammengefasst werden falls keine Code-Änderung nötig war).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo test --bin shifty-dioxus manual_convert_modal 2>&1 | tail -15 && nix-shell -p openssl pkg-config lld --command "cargo build --target wasm32-unknown-unknown" 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `EditExtraHoursModal` aus Z. 817-897 ist durch `ManualConvertModal` ersetzt — keine `EditExtraHoursModal`-Definition oder `-Aufruf` mehr im File (grep auf `EditExtraHoursModal` liefert nur historische Kommentare in 8.1-SUMMARY-Files, nicht im Code).
    - 4 dioxus-ssr Snapshot-Tests grün (`manual_convert_modal_*`).
    - DriftEntryRow.on_save dispatcht `CutoverAction::ConvertSingleManualRange` und schließt das Modal.
    - `cargo test --bin shifty-dioxus` workspace grün.
    - WASM-Build (`cargo build --target wasm32-unknown-unknown`) Exit-Code 0.
    - Pitfall-7-Defense: kein `unwrap_or_else(|_| time::macros::date!(...))` im Submit-Body. Validation-Error wird inline gerendert oder Submit ist blockiert.
    - jj-Commit(s) erstellt (1 oder 2 je nach Aufteilung).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

8.2 fügt KEINE neuen Trust-Boundaries hinzu — die einzige Surface (`POST /admin/cutover/convert-quarantine-entry` mit `manual_range`-Field) ist in Plan 08.2-01 threat-modelled. Hier nur Frontend-spezifische Threats:

| Boundary | Description |
|----------|-------------|
| Browser DOM ↔ Operator | `<input type="date">`-Values sind von Operator-Eingabe abhängig — keine externen Quellen. |
| FE → Backend REST | Operator-supplied dates werden in `time::Date` geparsed VOR dispatch (Modal-Submit-Validation); ungültige Dates werden inline angezeigt, nicht ans Backend gesendet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.2-FE-01 | Tampering | Modal-Submit-Validation | mitigate | `time::Date::parse(..., format_description!("[year]-[month]-[day]"))` Match-Arms; bei Parse-Fail → "Invalid date format"-Inline-Error, Dispatch blockiert (RESEARCH P-7 Defense — KEIN Hardcoded-Fallback wie der 8.1-09-Smell). |
| T-08.2-FE-02 | Information Disclosure | Backend Validation-Error wird inline gerendert | accept | Backend-Errors aus `ServiceError::ValidationError` (Plan 01) werden über die existing `ErrorStore`-Pipeline (8.1) gerendert — kein cross-tenant data leak möglich, weil REST-Layer nur eigene-Person-Sicht hat. |
| T-08.2-FE-03 | Denial of Service | Operator klickt Submit mehrfach schnell | accept | Per `manual_convert_open.set(false)` schließt das Modal nach Submit; Race-Conditions sind Backend-Side absorbiert (Plan 01 P-3 — `find_legacy_extra_hours_not_yet_migrated` filter + `ON CONFLICT DO NOTHING`). |
| T-08.2-FE-04 | Elevation of Privilege | Operator ohne `cutover_admin` öffnet Modal | mitigate | Edit-Button-Branch (Z. 763) checked `is_cutover_admin` — nicht-Cutover-Admin können das Modal nicht öffnen (existing 8.1-Behavior). Plus: Backend lehnt mit `PermissionDenied` ab (defense-in-depth). |

8.1-Threat-Model (siehe 08.1-09-PLAN.md) ist hier vollständig geerbt — manual-range erweitert die Surface nicht über das in Plan 01 modelled Backend-Field hinaus.
</threat_model>

<verification>
Map auf ROADMAP Phase-8.2 Success Criteria 1-6 (Frontend-Anteil):

- [ ] **SC-2** (Frontend-Anteil): Per-Eintrag-Modal in der Cutover-Page; Date-Range-Picker (von / bis); Category read-only; Submit dispatcht `CutoverAction::ManualConvert` (hier: `ConvertSingleManualRange`); `refreshed_drift_report` aus Response landet im `CUTOVER_STORE` → verifiziert durch Task 1 (Coroutine-Branch) + Task 3 (Modal + Wiring + 4 Snapshot-Tests).
- [ ] **SC-4**: `EditExtraHoursModal`-Stub aus 8.1-09 wird zum `ManualConvertModal`. Read-only Category + Stunden; Submit dispatcht; same `CUTOVER_STORE`-Mechanik wie 8.1-09 Convert/Bulk-Convert → verifiziert durch Task 3.
- [ ] **SC-6** (Frontend-Anteil): WASM-Build (`cd shifty-dioxus && cargo build --target wasm32-unknown-unknown`) grün → verifiziert durch Task 3 WASM-Build-Gate.
- [ ] **FUI-A-09 (i18n De/En/Cs Vollständigkeit)**: 8 neue Cutover-Manual-* Keys in allen drei Locales mit Per-Locale-Reference-Matcher-Tests → verifiziert durch Task 2.
- [ ] (out-of-scope für Plan-02 — Backend): SC-1 (DTO + Service-Branch), SC-3 (Karin-Test), SC-5 (OpenAPI-Schema-Eintrag), SC-6 (Backend cargo test --workspace) — Plan 08.2-01.
</verification>

<success_criteria>
After this plan:
- ManualConvertModal ist live; Operator klickt Edit auf Karin-artige Quarantäne-Row → Modal mit Date-Range-Picker erscheint.
- Submit dispatcht `ConvertSingleManualRange` an die Coroutine; Coroutine ruft Backend (Plan 01); Drift-Liste aktualisiert sich via `bump_cutover_refresh()`.
- Wenn Backend Validation-Error wirft (start>end, year-mismatch, overlap), Frontend rendert ihn inline im Modal.
- Wenn Backend success ohne `refreshed_drift_report` (Replay-Failure), Coroutine fallback auf separaten `cutover_gate_dry_run`.
- 4 dioxus-ssr Snapshot-Tests grün; 8 i18n-Keys × 3 Locales mit Reference-Matcher-Tests grün.
- WASM-Build grün, kein Regression.
- Karin-Pattern (gap-1a) ist operativ auflösbar — ROADMAP SC-3 vollständig (zusammen mit Plan 01).
</success_criteria>

<output>
After plan completion, create `.planning/phases/08.2-manual-range-convert-quarantine/08.2-02-SUMMARY.md`.

Per-task jj-commits (cumulative across the plan):
- `feat(08.2-02): add ConvertSingleManualRange action + api manual_range param` — Task 1
- `feat(08.2-02): add 8 i18n keys for manual-convert modal across de/en/cs + reference-matcher tests` — Task 2
- `feat(08.2-02): replace EditExtraHoursModal stub with ManualConvertModal + 4 ssr snapshot tests` — Task 3 GREEN-step
- `test(08.2-02): wasm build gate verification` — Task 3 final-step (optional, falls als separater commit gewünscht)
- `docs(08.2-02): plan summary` — Plan-completion summary commit (User-controlled)

VCS-policy: jj-only (CLAUDE.local.md). NO `git commit`/`git add` calls.

After both plans (08.2-01 + 08.2-02) ship, the parent gap-1a referenced from `08.1-10-SUMMARY.md` (Karin-Pattern bleibender gap) is marked `resolved-by-8.2` in the final phase-SUMMARY for 8.2.
</output>
