---
phase: 38-frontend-build-hygiene
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - shifty-dioxus/src/state/shiftplan.rs
  - "shifty-dioxus/src/** (dead-code sites across the tree — exact set determined at execution from the live warning list)"
autonomous: true
requirements: [HYG-01, HYG-02]
must_haves:
  truths:
    - "`cargo build` (shifty-dioxus) emits zero warning lines — all remaining dead-code warnings are resolved (HYG-01, D-01)"
    - "Dead symbols are DELETED by default rather than blanket-suppressed; the frontend is actually fixed, not just allowed (D-01, D-04)"
    - "Each symbol that is kept carries `#[allow(dead_code)] // reason: <why>` inline at the symbol, justified only by trait/signature symmetry, a planned API tied to an open requirement, or scope-guard against a larger restructure (D-03, D-06)"
    - "The set of kept `#[allow]` exceptions with their reasons is collected in the phase SUMMARY as a gate overview (D-07)"
    - "All four gates pass — dioxus `cargo build` 0 warnings, backend `cargo clippy --workspace -- -D warnings` green, `cargo test -p shifty-dioxus` green (excluding the pre-existing `i18n_impersonation_keys_match_german_reference` failure), and the wasm32 build green; an optional `dx serve` smoke is available as a non-gate safeguard (HYG-01, HYG-02, D-10)"
  artifacts:
    - "shifty-dioxus/src/state/shiftplan.rs (dead `has_sunday_slots` method removed unless a D-03 reason applies)"
    - ".planning/phases/38-frontend-build-hygiene/38-02-SUMMARY.md (lists every kept #[allow] exception + confirms dioxus clippy untouched)"
  key_links:
    - "each dead-code warning → per-symbol grep verification → delete-or-keep decision (D-01/D-03)"
    - "final dioxus cargo build warning count → 0 (HYG-01)"
    - "backend cargo clippy --workspace -D warnings → still green as regression gate (HYG-02)"
---

<objective>
Resolve the remaining dead-code warnings in shifty-dioxus (the ~34 bucket, minus anything
now referenced by phases 36/37) using delete-by-default (D-01), keeping a symbol only with
an inline `#[allow(dead_code)] // reason: <why>` when a genuine D-03 exception applies. Then
run the full four-gate verification (D-10) and record the kept exceptions in the SUMMARY (D-07).

Purpose: Land the core of HYG-01 (dioxus warning-free) by actually fixing the frontend, and
confirm HYG-02 (backend clippy regression gate) plus the FE test + WASM build gates stay green.

Output: A shifty-dioxus tree whose `cargo build` emits zero warnings; a 38-02-SUMMARY.md that
lists every deliberately-kept `#[allow(dead_code)]` site with its reason and confirms the
~198 dioxus clippy lints were left untouched (D-08/D-09).

Artifacts this phase produces: Predominantly REMOVES symbols. The only reliably-predictable
new artifact is the SUMMARY; any kept `#[allow]` sites are decided per-symbol at execution and
enumerated there (D-06/D-07).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-frontend-build-hygiene/38-CONTEXT.md
@.planning/phases/38-frontend-build-hygiene/38-01-SUMMARY.md
@shifty-dioxus/CLAUDE.md
@CLAUDE.md
</context>

<build_conventions>
<!-- planner-discipline-allow: warning: -->
- The `grep -c 'warning:'` gate below counts lines in `cargo build` STDOUT, not lines in any
  committed source file — the literal is a build-output filter, so it is safe to reference.
- dioxus builds/tests run from `shifty-dioxus/` via `nix develop ../ --command <cmd>`.
- Backend clippy runs from the repo ROOT (shifty-backend/) in the BACKEND dev shell —
  `nix develop --command cargo clippy --workspace -- -D warnings`. NEVER run clippy in the
  dioxus shell (E0514). This is the HYG-02 regression gate (D-08).
- HYG-01 core signal: from shifty-dioxus/, `cargo build 2>&1 | grep -c 'warning:'` == 0.
- Pre-existing UNRELATED test failure: `i18n_impersonation_keys_match_german_reference`
  predates v1.11 and is OUT of scope — do NOT try to fix it, but this cleanup must NOT
  introduce any NEW test failure.
</build_conventions>

<tasks>

<task type="execute">
  <name>Task 1: Remove dead code (delete by default; #[allow]+reason only as exception)</name>
  <files>shifty-dioxus/src/state/shiftplan.rs, shifty-dioxus/src/** (dead-code sites from the live list)</files>
  <read_first>
    - .planning/phases/38-frontend-build-hygiene/38-01-SUMMARY.md (the post-38-01 dead-code bucket)
    - .planning/phases/38-frontend-build-hygiene/38-CONTEXT.md (D-01, D-03, D-04, D-06 policy; the named candidate list — STALE, verify each)
    - shifty-dioxus/src/state/shiftplan.rs (the dead `has_sunday_slots` at ~line 315)
  </read_first>
  <action>
    First re-run the build to get the CURRENT remaining dead-code warnings (post-38-01):
    `nix develop ../ --command cargo build 2>&1`. Work from THIS list, not the stale CONTEXT
    enumeration — phases 36/37 may have started referencing some previously-dead symbols.

    For EACH dead-code warning:
    1. Locate the flagged symbol (function / method / enum variant / struct / const / field).
    2. Verify it is genuinely unreferenced: grep the whole shifty-dioxus tree for the
       identifier, excluding its own definition. If ANY live reference exists (e.g. a
       phase-36/37 change now uses it), do NOT delete — the warning likely no longer applies;
       re-check the live list.
    3. DEFAULT = DELETE the symbol (D-01, D-04). Also delete anything that becomes orphaned by
       the removal (now-unused imports, private helpers that only the deleted symbol called).
       For never-constructed enum variants, remove the variant and any dead match arms.
    4. EXCEPTION (D-03) = keep with an inline `#[allow(dead_code)] // reason: <why>` at the
       symbol (D-06) ONLY when one of these holds: (a) trait/signature symmetry, (b) an
       obviously-planned API tied to a concrete open requirement, or (c) deleting would
       trigger a larger restructure that blows the hygiene scope. No blanket keep-list —
       named candidates (register_user_to_slot, load_slots, get_bookings_for_week,
       has_sunday_slots, etc.) are deletion candidates unless a reason above applies.
    5. Rebuild after each batch: removing one symbol can expose newly-dead symbols or
       newly-unused imports — iterate until the build is clean.

    DISAMBIGUATION — there are TWO `has_sunday_slots`: the dead one is the METHOD at
    src/state/shiftplan.rs:~315 (deletion candidate). The FUNCTION at
    src/component/day_aggregate_view.rs:194 HAS tests and is live — do NOT touch it.

    Keep every kept-exception decision recorded for the SUMMARY (symbol, file, reason).
  </action>
  <verify>
    <automated>cd shifty-dioxus && nix develop ../ --command cargo build 2>&1 | grep -c 'warning:'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` (shifty-dioxus) emits 0 warning lines (`grep -c 'warning:'` == 0).
    - Every dead symbol was either deleted (default) or kept with an inline `#[allow(dead_code)] // reason: <why>` justified by a D-03 case.
    - No live symbol was deleted: the build still compiles (exit 0) and no NEW test failures are introduced.
    - The dead METHOD `has_sunday_slots` (state/shiftplan.rs) was handled; the tested FUNCTION `has_sunday_slots` (component/day_aggregate_view.rs) is untouched.
    - The list of kept `#[allow]` exceptions (symbol + reason) is captured for the SUMMARY.
  </acceptance_criteria>
  <done>dioxus `cargo build` emits 0 warning lines; dead symbols deleted by default with kept exceptions carrying inline `#[allow(dead_code)] // reason:`; the tested `has_sunday_slots` function is untouched.</done>
</task>

<task type="execute">
  <name>Task 2: Full four-gate verification + record kept exceptions</name>
  <files>.planning/phases/38-frontend-build-hygiene/38-02-SUMMARY.md</files>
  <read_first>
    - CLAUDE.md (backend clippy hard-gate rules)
    - shifty-dioxus/CLAUDE.md (WASM build-gate)
  </read_first>
  <action>
    Run all four D-10 gates and record the results in the SUMMARY:

    Gate 1 (HYG-01) — from shifty-dioxus/: `nix develop ../ --command cargo build 2>&1 | grep -c 'warning:'` MUST be 0.

    Gate 2 (HYG-02, regression) — from the repo ROOT in the BACKEND dev shell:
    `nix develop --command cargo clippy --workspace -- -D warnings` MUST be green. (Backend
    only — dioxus is a separate workspace and stays out of the clippy gate per D-08/D-09.)

    Gate 3 — from shifty-dioxus/: `nix develop ../ --command cargo test -p shifty-dioxus`
    MUST be green EXCEPT the pre-existing unrelated failure
    `i18n_impersonation_keys_match_german_reference` (predates v1.11, out of scope). No NEW
    failures may appear.

    Gate 4 — from shifty-dioxus/: `nix develop ../ --command cargo build --target wasm32-unknown-unknown`
    MUST be green (proves compilation for the WASM target).

    Optional non-gate safeguard (D-10): a `dx serve` manual smoke (app loads, main pages
    render) guards against the unlikely case that "dead" code was runtime-reachable. This is
    NOT a hard gate — mention it as an optional follow-up in the SUMMARY, do not block on it.

    Then write the SUMMARY: enumerate every kept `#[allow(dead_code)] // reason:` site with
    its reason (D-07), confirm the ~198 dioxus clippy lints were NOT touched and dioxus
    remains out of the CI clippy gate (D-08/D-09), and note no new deps / no snapshot bump /
    no migration / no behavior change.
  </action>
  <verify>
    <automated>cd shifty-dioxus && nix develop ../ --command cargo build 2>&1 | grep -c 'warning:' ; nix develop ../ --command cargo build --target wasm32-unknown-unknown 2>&1 | tail -1 ; nix develop ../ --command cargo test -p shifty-dioxus 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Gate 1: dioxus `cargo build` warning count is 0 (HYG-01).
    - Gate 2: backend `cargo clippy --workspace -- -D warnings` (from repo root, backend shell) is green (HYG-02).
    - Gate 3: `cargo test -p shifty-dioxus` is green apart from the pre-existing `i18n_impersonation_keys_match_german_reference` failure; no NEW failures.
    - Gate 4: `cargo build --target wasm32-unknown-unknown` is green.
    - SUMMARY lists every kept `#[allow(dead_code)]` exception + reason (D-07) and confirms dioxus clippy untouched (D-08/D-09).
  </acceptance_criteria>
  <done>All four D-10 gates pass (dioxus build 0 warnings, backend clippy green, FE tests green sans the pre-existing i18n failure, wasm32 build green); SUMMARY records kept exceptions and confirms dioxus clippy untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Dead-code removal only: no new inputs, endpoints, deps, or data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-02 | Tampering | deletion of a symbol that was actually reachable | low | mitigate | Rust has no reflection — dead-code warnings are compiler-proven unreferenced; per-symbol grep + rebuild before each delete; optional `dx serve` smoke (D-10) as extra safeguard. |
| T-38-SC | Tampering | package installs | n/a | accept | No new dependencies installed this phase (no new deps) — supply-chain gate not applicable. |
</threat_model>

<verification>
All four D-10 gates pass:
1. dioxus `cargo build` — 0 warnings (HYG-01).
2. backend `cargo clippy --workspace -- -D warnings` — green (HYG-02 regression gate).
3. `cargo test -p shifty-dioxus` — green except the pre-existing, out-of-scope `i18n_impersonation_keys_match_german_reference`.
4. `cargo build --target wasm32-unknown-unknown` — green.
Optional (non-gate): `dx serve` manual smoke per D-10.
</verification>

<success_criteria>
- shifty-dioxus is rustc-warning-free (HYG-01).
- Backend clippy gate stays green (HYG-02).
- Dead code deleted by default; kept exceptions carry inline `#[allow] // reason:` and are enumerated in the SUMMARY (D-01/D-03/D-06/D-07).
- No new deps, no snapshot bump, no migration, no behavior change; dioxus clippy untouched (D-08/D-09).
</success_criteria>

<output>
Create `.planning/phases/38-frontend-build-hygiene/38-02-SUMMARY.md` when done —
enumerate the kept `#[allow(dead_code)]` exception sites (symbol, file, reason), record the
four gate results, and confirm dioxus clippy was left untouched.
</output>