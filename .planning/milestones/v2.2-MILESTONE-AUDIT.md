---
milestone: v2.2
milestone_name: Aufräumen, WebDAV-Export & Wochentag-Muster
status: passed
audited_at: 2026-07-03
phases: [43, 44, 45, 46, 47, 48]
requirements_total: 16
requirements_complete: 16
snapshot_schema_version: 12
snapshot_bump: false
---

# v2.2 Milestone Audit — Aufräumen, WebDAV-Export & Wochentag-Muster

## Executive Summary

**Status: passed.** Alle 16 Requirements (SDF-03/04/05, BUG-01/02/03, HYG-03/04/05, IMP-05, RPT-01/02/03, EXP-01/02/03) sind über die 6 Phasen 43–48 abgedeckt und in REQUIREMENTS.md als Complete markiert. Alle Phasen-VERIFICATIONs sind `passed` (43: 8/8, 44: 3/3, 45: 5/5, 46: 3/3, 47: 11/11, 48: 30/30). Cross-Phase-Wiring ist verifiziert (neue RPT-Wochentag-DTO im FE konsumiert, neue PDF-Export-REST-Endpoints im FE-Settings-Card 4 konsumiert). Snapshot-Schema-Version bleibt 12 (kein Bump — grep-verifiziert). Alle vier Abschluss-Gates grün: Backend `cargo test --workspace`, Backend `cargo clippy --workspace -- -D warnings`, FE WASM-Build, FE `cargo test -p shifty-dioxus` (787 passed, 0 failed — inkl. der zuvor gebrochenen `i18n_impersonation_keys_match_german_reference` via Phase 46/IMP-05).

Neue Deps in Phase 48: `printpdf`, `reqwest_dav`, `tokio-cron-scheduler`. Eine neue Migration in Phase 48: `pdf_export_config`-Single-Row-Tabelle. Kein Snapshot-Bump. Keine Doc-Drift, die den Milestone-Abschluss blockiert (kleine informative Drift in REQUIREMENTS.md-Status-Tabelle für HYG-04/05, die aber laut Phase-46-Report ohnehin schon aufgelöst wurde und derzeit korrekt auf Complete steht).

## Requirements Coverage

| REQ-ID | Phase | Titel | Status | Evidence |
|--------|-------|-------|--------|----------|
| SDF-03 | 43 | Special-Days-Loader Kalenderjahr (1.1.-Bug) | Complete | 43-VERIFICATION Truths #1/#2, `sd_year_after_create` pure fn + 4 Tests |
| SDF-04 | 43 | Duplikat-Hinweis-Copy an Replace-Verhalten | Complete | 43-VERIFICATION Truths #3/#4, Anti-Wording-Test 3 Locales |
| SDF-05 | 43 | Feiertag↔Kurzer-Tag Umschalter ohne Fehler | Complete | 43-VERIFICATION Truths #5/#6, Backend-Roundtrip-Test + FE `special_day_error_after_create` |
| BUG-01 | 44 | `save_slot_edit` ohne write-borrow across `.await` | Complete | Snapshot/Apply-Pattern in `service/slot_edit.rs`, 6 Regressionstests |
| BUG-02 | 44 | `list_user_invitations` sichtbarer Parse-Fehler | Complete | `ShiftyError::InvitationParse` + Inline-Banner + i18n + 5 Tests |
| BUG-03 | 44 | Backdrop-Press-Audit + durable Grep-Invariant | Complete | `mod backdrop_invariant` in `dialog.rs`, 2 grep-basierte Invariant-Tests |
| HYG-03 | 45 | shifty-dioxus 0 Warnings + Clippy `-D warnings` | Complete | 45-VERIFICATION, `cargo clippy -p shifty-dioxus -- -D warnings` exit 0, 177→0 Warnings |
| HYG-04 | 46 | „Edit structure" i18n de/en/cs | Complete | 3 neue Keys (`Shiftplan{EditStructure,NormalMode,NewSlot}`) + Call-Sites in `page/shiftplan.rs` |
| HYG-05 | 46 | REST Content-Type-Surface-Test | Complete | `rest/tests/content_type_surface.rs` iteriert 120 utoipa-Operationen, 2 Tests grün |
| IMP-05 | 46 | i18n_impersonation-Test grün (kanonische Copy) | Complete | Test in `i18n/mod.rs:1585` auf shipped 🥸-Copy angepasst, 3/3 Impersonation-Tests grün |
| RPT-01 | 47 | Wochentag-Anwesenheits-Zeile im HR-Stats-Block | Complete | pure fn `weekday_attendance_distribution` + `format_weekday_attendance_line` + SSR-Test |
| RPT-02 | 47 | `average_hours_per_attendance_day` ersetzt | Complete | grep über alle Krates = 0 Hits; DTO-Shape umgestellt (`EmployeeAttendanceStatisticsTO`) |
| RPT-03 | 47 | Snapshot bleibt 12 + i18n de/en/cs | Complete | `CURRENT_SNAPSHOT_SCHEMA_VERSION = 12`, `phase_47_weekday_i18n_presence` grün (9 Keys × 3 Locales) |
| EXP-01 | 48 | Backend-Task PDF-Rendering + WebDAV-Upload | Complete | `pdf_render.rs` (10 Tests) + `webdav_client.rs` (12 Tests) + `pdf_export_scheduler.rs` (7 Tests, inkl. E2E `boot_trigger_reload_flow`) |
| EXP-02 | 48 | Config in DB (Single-Row), admin-gated Card | Complete | Migration `pdf_export_config` + `PdfExportConfigService` (Basic-Tier, admin-gated) + Settings-Card 4 |
| EXP-03 | 48 | Retry 2s/4s/8s + Status-Persistenz + Server-Resilience | Complete | `DEFAULT_RETRY_DELAYS` + `record_success`/`record_error` + Card zeigt LastSuccess/LastError |

**Coverage: 16/16 Complete.** Keine deferred Requirements innerhalb v2.2.

## Cross-Phase Integration

| Connection | From | To | Status | Evidence |
|------------|------|-----|--------|----------|
| Phase 47 DTO → FE-Component | `rest-types::EmployeeAttendanceStatisticsTO { attendance_by_weekday, counted_calendar_weeks }` | `shifty-dioxus/src/component/employee_view.rs:47/56/584/1676` | WIRED | Formatter iteriert `stats.attendance_by_weekday`, Call-Site im HR-Stats-Block, 3 SSR-Tests + Formatter-Unit-Test |
| Phase 47 REST → FE-Loader | `GET /report/{id}/attendance-statistics` (neue Shape) | FE-Report-Consumer | WIRED | Handler in `rest/src/report.rs:214` returned `Option<EmployeeAttendanceStatisticsTO>`, utoipa-Schema registriert; FE ruft Attendance-Statistics-Loader auf und verzweigt via `if let Some(att)` |
| Phase 48 REST → FE-API | `GET/PUT /pdf-export-config` + `POST /pdf-export-config/trigger` | `shifty-dioxus/src/api.rs:1871-1907` | WIRED | 3 API-Fns (`get_pdf_export_config`, `put_pdf_export_config`, `trigger_pdf_export`) rufen exakt die 3 REST-Pfade |
| Phase 48 REST → FE-Loader/Card | FE API → Settings-Card 4 | `shifty-dioxus/src/page/settings.rs:769/794/816` | WIRED | `loader::get_pdf_export_config`, `save_pdf_export_config`, `trigger_pdf_export_now` an Card 4 gebunden (admin-gated inside outer `is_admin`-Rücksprung, D-48-UI-GATE) |
| Phase 48 Migration → DAO | `pdf_export_config`-Tabelle (Single-Row, INSERT-OR-IGNORE-Seed) | `dao_impl_sqlite::pdf_export_config` | WIRED | Migration ausgeführt in In-Memory-Test-Setup; 3 Integrationstests seedn Row erfolgreich |
| Phase 48 Scheduler → 3 pure Module | `PdfExportSchedulerImpl` | `pdf_render` + `webdav_client` + `PdfExportConfigService` | WIRED | 7 Scheduler-Tests + E2E `boot_trigger_reload_flow` mit wiremock-WebDAV-Upload-Assertion |
| Snapshot-Version-Konstanz | `CURRENT_SNAPSHOT_SCHEMA_VERSION` | `billing_period_report.rs:117` | UNVERÄNDERT | grep bestätigt `= 12`; keine RPT- oder EXP-Änderung berührt einen persistierten `BillingPeriodValueType` |

**3 E2E-Flows verifiziert:**

1. **Wochentag-Anwesenheits-Muster:** DTO-Umbau → REST-Endpoint → FE-Loader → HR-Stats-Zeile → i18n. Alte AVG-Kennzahl grep-verifiziert entfernt (0 Hits über alle Krates).
2. **PDF-Export-Config-Roundtrip:** Admin-Settings-Card 4 → PUT `/pdf-export-config` → DB → Scheduler `reload_from_db` → nächster Cron-Slot rendert + uploadet.
3. **PDF-Export-Trigger-Now:** Card-Button „Jetzt exportieren" → POST `/pdf-export-config/trigger` (admin-gated) → `tokio::spawn(run_once_now(Full))` → Retry 2s/4s/8s bei transient → Status-Persistenz (`last_success_at` / `last_error_at`) → Card zeigt Status.

**Keine BROKEN Connections. Keine orphaned Exports.**

## Aggregate Deferred / Tech Debt

Innerhalb der v2.2-Phasen fällt genau **ein** dokumentiertes Deferred-Item an (Phase 48, `deferred-items.md`):

- **Pre-existing `doc_lazy_continuation`-Clippy-Warning** in `service_impl/src/test/shiftplan_edit_lock.rs:6` (Origin Phase 40, aus v2.1). Fires nur mit `--all-targets`; nicht Teil des Standard-Clippy-Gates oder `nix build`. Trivial: 2-space-Indent oder module-scope-`#[allow]`. **Non-blocking.**

Verbleibende Carry-over-Items aus STATE.md (Historisches Tech-Debt-Backlog aus v1.x/v2.1 — 20+ Items) sind unverändert acknowledged und wurden bei früheren Closeouts deferred. Keines davon wurde in v2.2 in Scope gezogen — d.h. der v2.2-Milestone erzeugt **kein neues** Tech-Debt.

Zusätzliche implizite Beobachtungen aus den VERIFICATION-Reports (nicht als Debt gezählt):

- **Phase 45 Scope-Caveat:** 13 pre-existing `#[allow(dead_code)]`/`#[allow(non_snake_case)]` ohne reason-Kommentar bleiben — Clippy `-D warnings` ist trotzdem grün, ROADMAP-SC1–3 erfüllt. Optionale Nach-Hygiene-Runde.
- **Phase 48 Deviation:** PDF-Determinismus-Test normalisiert die `/ID`-Array-Bytes test-seitig (dokumentiert im SUMMARY) — Payload/Layout bleibt byte-gleich, nur der printpdf-internal /ID-Fingerprint ist runspezifisch.

## Final Gate Results

| Gate | Kommando | Ergebnis |
|------|----------|----------|
| Backend Tests | `nix develop --command cargo test --workspace` | **PASS** (exit 0; keine Fehler-Blöcke; alle Doc-Test-Suites grün, Unit-Tests laut Phase-VERIFICATIONs 698+ passed 0 failed) |
| Backend Clippy Hard Gate | `nix develop --command cargo clippy --workspace -- -D warnings` | **PASS** (exit 0, 0 Warnings) |
| Frontend WASM Build | `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` | **PASS** (exit 0) |
| Frontend Tests (inkl. IMP-05) | `cargo test -p shifty-dioxus` | **PASS** — `test result: ok. 787 passed; 0 failed; 0 ignored` (der zuvor gebrochene `i18n_impersonation_keys_match_german_reference`-Test ist jetzt grün via Phase 46/IMP-05) |

Alle vier Abschluss-Gates grün. Milestone v2.2 ist bereit zum Archivieren.

---

_Auditor: Claude (gsd-audit-milestone)_
_Auditiert: 2026-07-03_
