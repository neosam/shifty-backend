---
phase: 48-nextcloud-pdf-webdav
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260703000000_create-pdf-export-config.sql
  - dao/src/lib.rs
  - dao/src/pdf_export_config.rs
  - dao_impl_sqlite/src/lib.rs
  - dao_impl_sqlite/src/pdf_export_config.rs
  - service/src/lib.rs
  - service/src/pdf_export_config.rs
  - service_impl/src/lib.rs
  - service_impl/src/pdf_export_config.rs
  - rest-types/src/lib.rs
  - rest/src/lib.rs
  - rest/src/pdf_export_config.rs
  - shifty_bin/src/main.rs
  - service_impl/src/test/mod.rs
  - service_impl/src/test/pdf_export_config.rs
autonomous: true
requirements:
  - EXP-02
  - EXP-03

must_haves:
  truths:
    - "D-48-CONFIG (per Q3): Migration 20260703000000_create-pdf-export-config.sql legt Single-Row-Tabelle pdf_export_config an — Felder enabled BOOLEAN NOT NULL DEFAULT 0, nextcloud_url/webdav_user/webdav_app_token/target_folder TEXT NULL, weeks_horizon INTEGER NOT NULL DEFAULT 8, cron_schedule TEXT NOT NULL DEFAULT '0 6 * * 1', last_success_at/last_error_at TEXT NULL, last_error_message TEXT NULL, update_process TEXT NOT NULL, update_version BLOB NOT NULL — und seedet EINE Zeile mit enabled=0 (Single-Row per fixed UUID PK)"
    - "D-48-01 (Ops-Entscheidung, per Q3a-i): webdav_app_token wird KLARTEXT gespeichert; DB-File-Schutz reicht — kein Feld-Encrypt in v2.2"
    - "D-48-BASIC (per CONTEXT Discretion + Service-Tier-Konvention): PdfExportConfigService ist Basic-Tier — konsumiert AUSSCHLIESSLICH PermissionService + PdfExportConfigDao + TransactionDao (KEIN Domain-Service als Dep)"
    - "D-48-ADMIN (per Q8): PdfExportConfigService::get/update erzwingt check_permission(\"admin\", ...) VOR jeder DAO-Operation — Non-Admin bekommt ServiceError::Forbidden (HTTP 403)"
    - "D-48-REST (per CONTEXT Discretion): GET /pdf-export-config liefert PdfExportConfigTO (Token maskiert leer, sonst Konfig + Status), PUT /pdf-export-config akzeptiert PdfExportConfigTO — leerer webdav_app_token behält den existierenden Wert (per Specifics-Passage: 'leer speichern lässt den existierenden Wert stehen')"
    - "D-48-ROADMAP (per CONTEXT deferred → begleitende Aktion): ROADMAP.md Phase 48 Success Criterion 2 wird von 'Env-Variablen … kein Klartext-Passwort im Repo/DB' auf 'Admin-UI-Card + DB-Persistenz; App-Token Klartext in DB (bewusste Ops-Entscheidung)' umformuliert"
  artifacts:
    - migrations/sqlite/20260703000000_create-pdf-export-config.sql
    - dao/src/pdf_export_config.rs
    - dao_impl_sqlite/src/pdf_export_config.rs
    - service/src/pdf_export_config.rs
    - service_impl/src/pdf_export_config.rs
    - rest/src/pdf_export_config.rs
    - service_impl/src/test/pdf_export_config.rs
  key_links:
    - "PdfExportConfigService.update() persistiert cron_schedule/weeks_horizon Änderungen; 48-04 Scheduler liest sie beim nächsten Cron-Tick per get() ein — Änderungen wirken ohne Server-Restart (per CONTEXT Q4)"
    - "PdfExportConfigService.record_success()/record_error() Methoden werden vom 48-04 Scheduler nach jedem Lauf aufgerufen und persistieren last_success_at bzw. last_error_at + last_error_message"
    - "ApiDoc nest-Registrierung in rest/src/lib.rs macht GET/PUT /pdf-export-config Swagger-sichtbar und schaltet die 48-05 Admin-UI-Card frei"
---

<objective>
Persistenz-Fundament für den Nextcloud-PDF-Export: neue Single-Row-Tabelle `pdf_export_config` (Migration), DAO-Trait + SQLite-Impl, Basic-Tier-Service `PdfExportConfigService` (admin-gated get/update, plus interne `record_success`/`record_error`-Methoden für den Scheduler in 48-04), REST-Endpoints `GET /pdf-export-config` + `PUT /pdf-export-config` mit DTO in `rest-types`, DI-Wiring in `shifty_bin/src/main.rs` und ROADMAP-Success-Criterion-2-Anpassung (per D-48-ROADMAP).

Purpose: Erfüllt EXP-02 (DB-persistente Konfig statt Env-Vars) und den EXP-03-Status-Anteil (Status-Felder in der DB), plus schaltet 48-04 (Scheduler liest Config) und 48-05 (Admin-UI-Card liest/schreibt) frei.

Output: Migration + DAO + Service + REST + rest-types-DTO + Test-Suite `pdf_export_config.rs` unter `service_impl/src/test/`. Kein Snapshot-Bump (grep-verifiziert, D-48-NO-SNAPSHOT).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-nextcloud-pdf-webdav/48-CONTEXT.md

# Vorbild-Muster für Single-Row-Config + admin-gate + Basic-Service:
@migrations/sqlite/20260629000000_create-vacation-entitlement-offset.sql
@dao/src/vacation_entitlement_offset.rs
@dao_impl_sqlite/src/vacation_entitlement_offset.rs
@service/src/vacation_entitlement_offset.rs
@rest/src/vacation_entitlement_offset.rs

# REST-Layer + DI-Wiring:
@rest/src/lib.rs
@shifty_bin/src/main.rs

# Permission API (check_permission("admin", ...)):
@service/src/permission.rs

# Snapshot-Konstante (grep-Gate — MUSS ungebumpt bleiben):
@service_impl/src/billing_period_report.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + DAO Trait + SQLite-Impl für pdf_export_config</name>
  <files>migrations/sqlite/20260703000000_create-pdf-export-config.sql, dao/src/lib.rs, dao/src/pdf_export_config.rs, dao_impl_sqlite/src/lib.rs, dao_impl_sqlite/src/pdf_export_config.rs</files>
  <behavior>
    - Test 1 (DAO get() gegen frische DB): nach `sqlx::migrate!` liefert `PdfExportConfigDaoImpl::get(tx)` genau EINE Row zurück (Seed) mit enabled=false, weeks_horizon=8, cron_schedule="0 6 * * 1", allen Text-Feldern None, allen Status-Feldern None
    - Test 2 (update() persistiert): nach `update(entity_with_enabled_true_and_url_set)` liefert ein frisches `get()` die neuen Werte, `version` (Uuid) ist neu gesetzt
    - Test 3 (record_success setzt last_success_at, clear last_error_*): nach `record_success(now)` liefert `get()` last_success_at=Some(now), last_error_at=None, last_error_message=None
    - Test 4 (record_error setzt last_error_*, hält last_success_at unverändert): nach vorherigem record_success + record_error(now2, "boom") liefert `get()` last_success_at=Some(now), last_error_at=Some(now2), last_error_message=Some("boom")
  </behavior>
  <action>
Task 1 implementiert per D-48-CONFIG das Persistenz-Fundament. Reihenfolge: Migration schreiben → DAO-Trait definieren → SQLite-Impl mit Tests (unit + `sqlx::test`).

1) Migration `migrations/sqlite/20260703000000_create-pdf-export-config.sql`:
   - `CREATE TABLE IF NOT EXISTS pdf_export_config (id BLOB NOT NULL PRIMARY KEY, enabled INTEGER NOT NULL DEFAULT 0, nextcloud_url TEXT, webdav_user TEXT, webdav_app_token TEXT, target_folder TEXT, weeks_horizon INTEGER NOT NULL DEFAULT 8, cron_schedule TEXT NOT NULL DEFAULT '0 6 * * 1', last_success_at TEXT, last_error_at TEXT, last_error_message TEXT, update_process TEXT NOT NULL, update_version BLOB NOT NULL);`
   - `INSERT OR IGNORE INTO pdf_export_config (id, enabled, weeks_horizon, cron_schedule, update_process, update_version) VALUES (X'00000000000000000000000000000048', 0, 8, '0 6 * * 1', 'phase-48-migration', X'00000000000000000000000000000048');`
   - Kommentar-Header erklärt Single-Row-Pattern (fixed UUID) analog Toggle. **KEIN** Klartext-Passwort/Token in der Migration (nur enabled=0, URL/User/Token bleiben NULL bis Admin sie über die UI setzt).

2) `dao/src/pdf_export_config.rs`:
   - `#[derive(Clone, Debug, PartialEq, Eq)] pub struct PdfExportConfigEntity { pub id: uuid::Uuid, pub enabled: bool, pub nextcloud_url: Option<Arc<str>>, pub webdav_user: Option<Arc<str>>, pub webdav_app_token: Option<Arc<str>>, pub target_folder: Option<Arc<str>>, pub weeks_horizon: u32, pub cron_schedule: Arc<str>, pub last_success_at: Option<time::PrimitiveDateTime>, pub last_error_at: Option<time::PrimitiveDateTime>, pub last_error_message: Option<Arc<str>>, pub version: uuid::Uuid }`
   - `#[automock(type Transaction = crate::MockTransaction;)] #[async_trait] pub trait PdfExportConfigDao { type Transaction: crate::Transaction; async fn get(&self, tx: Self::Transaction) -> Result<PdfExportConfigEntity, DaoError>; async fn update(&self, entity: &PdfExportConfigEntity, process: &str, tx: Self::Transaction) -> Result<(), DaoError>; async fn record_success(&self, at: time::PrimitiveDateTime, process: &str, tx: Self::Transaction) -> Result<(), DaoError>; async fn record_error(&self, at: time::PrimitiveDateTime, message: &str, process: &str, tx: Self::Transaction) -> Result<(), DaoError>; }` — kein Uuid-Argument, weil Single-Row.
   - `pub mod pdf_export_config;` in `dao/src/lib.rs` unter den existierenden Modul-Deklarationen einfügen (alphabetisch nach `permission`).

3) `dao_impl_sqlite/src/pdf_export_config.rs`: analog zu `vacation_entitlement_offset.rs`:
   - Private `PdfExportConfigDb` struct mit `Vec<u8>`-Ids und `String`-Timestamps, `TryFrom<&PdfExportConfigDb> for PdfExportConfigEntity` via `time::format_description::well_known::Iso8601::DATE_TIME`.
   - `PdfExportConfigDaoImpl { _pool: Arc<sqlx::SqlitePool> }` + `pub fn new(pool)`.
   - `get()` per `query_as!`: `SELECT id, enabled, nextcloud_url, webdav_user, webdav_app_token, target_folder, weeks_horizon, cron_schedule, last_success_at, last_error_at, last_error_message, update_version FROM pdf_export_config LIMIT 1`. Bei Zero-Row → `DaoError::DatabaseQueryError` mit „pdf_export_config seed row missing" (defensive; Migration seeded immer).
   - `update()` per `query!` (bewahrt fixed Id, überschreibt alle editierbaren Felder + update_process + update_version).
   - `record_success()`: `UPDATE pdf_export_config SET last_success_at = ?, last_error_at = NULL, last_error_message = NULL, update_process = ?, update_version = ?`.
   - `record_error()`: `UPDATE pdf_export_config SET last_error_at = ?, last_error_message = ?, update_process = ?, update_version = ?` (last_success_at bleibt).
   - `pub mod pdf_export_config;` in `dao_impl_sqlite/src/lib.rs` einfügen.

4) `sqlx prepare` nach den neuen `query!`/`query_as!`-Aufrufen: `cargo sqlx prepare --workspace` (aus `nix develop`) und `.sqlx/` mit-committen — sonst CI (`SQLX_OFFLINE=true`) fällt um (feedback_sqlx_prepare_after_new_query.md).

Kein Panic! kein unwrap!, keine unbegründeten allow(...). Rustfmt clean. Der `#[cfg(test)]`-Block bleibt in dieser Task minimal — die Tests laufen an dieser Stelle nur als Struct-/Signatur-Sanity über das mock-Interface; die echten DB-Tests kommen in Task 3 (`service_impl/src/test/pdf_export_config.rs`), wo bereits `sqlx::test` + in-memory SQLite verfügbar sind (Muster aus `test/vacation_entitlement_offset.rs`).
  </action>
  <verify>
    <automated>cargo sqlx prepare --workspace &amp;&amp; cargo build -p dao -p dao_impl_sqlite &amp;&amp; cargo clippy -p dao -p dao_impl_sqlite -- -D warnings</automated>
  </verify>
  <done>
    - Migration-Datei existiert und ist syntaktisch valides SQL (Migration-Anwendung wird in Task 3 gegen in-memory SQLite geprüft).
    - `PdfExportConfigDao`-Trait + `PdfExportConfigEntity` in `dao/`.
    - `PdfExportConfigDaoImpl` in `dao_impl_sqlite/` mit allen 4 Methoden implementiert.
    - `pub mod pdf_export_config;` in `dao/src/lib.rs` und `dao_impl_sqlite/src/lib.rs` eingefügt.
    - `.sqlx/`-Query-Cache aktualisiert und mit-committet.
    - `cargo build -p dao -p dao_impl_sqlite` grün, `cargo clippy -p dao -p dao_impl_sqlite -- -D warnings` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Basic-Service PdfExportConfigService + rest-types-DTO + REST-Endpoints</name>
  <files>service/src/lib.rs, service/src/pdf_export_config.rs, service_impl/src/lib.rs, service_impl/src/pdf_export_config.rs, rest-types/src/lib.rs, rest/src/lib.rs, rest/src/pdf_export_config.rs</files>
  <behavior>
    - Test 1 (admin-gate get, non-admin abgelehnt): PermissionService-Mock lehnt `check_permission("admin", …)` mit Forbidden ab → Service.get() liefert `ServiceError::Forbidden` (verifiziert D-48-ADMIN)
    - Test 2 (admin-gate update, non-admin abgelehnt): analog `update()` liefert Forbidden vor jedem DAO-Aufruf
    - Test 3 (update mit leerem Token = keine Änderung des Token-Felds): Ausgangszustand webdav_app_token=Some("alt"). Update-Aufruf mit `webdav_app_token=None`. Erwartung: DAO.update wird mit `webdav_app_token=Some("alt")` aufgerufen (leer = keep). Update-Aufruf mit `webdav_app_token=Some("neu")` — Erwartung: DAO.update wird mit Some("neu") aufgerufen.
    - Test 4 (get() returns entity as-is, admin ok): Mock-Permission = ok, DAO.get returns entity → Service liefert `PdfExportConfig` (Domain-Struct), das entity-äquivalent ist
  </behavior>
  <action>
Task 2 baut den Basic-Service, das rest-types-DTO und die REST-Handler auf. Vorbild: `vacation_entitlement_offset` (Basic-Service-Muster, admin-analog HR-gate).

1) `service/src/pdf_export_config.rs`:
   - Domain-Struct `PdfExportConfig` (id, enabled, nextcloud_url, webdav_user, webdav_app_token, target_folder, weeks_horizon, cron_schedule, last_success_at, last_error_at, last_error_message, version) + `From<&PdfExportConfigEntity>` und `From<&PdfExportConfig>` (zurück in Entity via TryFrom nicht nötig — der Service konstruiert die Entity direkt).
   - `PdfExportConfigUpdate` DTO ohne Status-Felder — nur die admin-editierbaren Werte: enabled, nextcloud_url, webdav_user, `webdav_app_token: Option<Arc<str>>` (None ⇒ keep, Some(v) ⇒ set), target_folder, weeks_horizon, cron_schedule.
   - `#[automock(type Context=(); type Transaction=dao::MockTransaction;)] #[async_trait] pub trait PdfExportConfigService { type Context; type Transaction; async fn get(&self, context, tx) -> Result<PdfExportConfig, ServiceError>; async fn update(&self, update: PdfExportConfigUpdate, context, tx) -> Result<PdfExportConfig, ServiceError>; async fn record_success(&self, at: time::PrimitiveDateTime, tx: Option<Self::Transaction>) -> Result<(), ServiceError>; async fn record_error(&self, at: time::PrimitiveDateTime, message: Arc<str>, tx: Option<Self::Transaction>) -> Result<(), ServiceError>; }`
   - `record_success`/`record_error` sind explizit KEIN admin-gated Public-API (per D-48-ADMIN nur für den Scheduler-Task in 48-04) — sie erlauben `Authentication::Full` (Muster wie `scheduler.rs`).
   - `pub mod pdf_export_config;` in `service/src/lib.rs`.

2) `service_impl/src/pdf_export_config.rs` mit `gen_service_impl!`:
   ```
   gen_service_impl! {
       struct PdfExportConfigServiceImpl: service::pdf_export_config::PdfExportConfigService = PdfExportConfigServiceDeps {
           PdfExportConfigDao: dao::pdf_export_config::PdfExportConfigDao = pdf_export_config_dao,
           PermissionService: service::PermissionService = permission_service,
           ClockService: service::clock::ClockService = clock_service,
           UuidService: service::uuid_service::UuidService = uuid_service,
           TransactionDao: dao::TransactionDao = transaction_dao
       }
   }
   ```
   Reine Basic-Tier-Deps (kein Domain-Service als Dep, per D-48-BASIC).
   - `get()`: `check_permission("admin", context)` → `use_transaction` → `dao.get` → `commit` → Return `(&entity).into()`.
   - `update(update: PdfExportConfigUpdate)`: `check_permission("admin", context)` → use_transaction → DAO.get (aktuelle Row) → Merge (`webdav_app_token`: `update.webdav_app_token.or(current.webdav_app_token)`) → neue update_version = `uuid_service.new_uuid()` → DAO.update → commit → return neu-gelesene Domain-Struct (get() nach update für Konsistenz).
   - `record_success`: use_transaction → DAO.record_success(at, "pdf-export-scheduler", tx) → commit. Kein Permission-Check (nur `Full`-Auth erlaubt via `check_only_full_authentication`).
   - `record_error`: analog.
   - `pub mod pdf_export_config;` in `service_impl/src/lib.rs`.

3) `rest-types/src/lib.rs`: `#[derive(Serialize, Deserialize, Clone, Debug, PartialEq, Eq, ToSchema)] pub struct PdfExportConfigTO { pub enabled: bool, pub nextcloud_url: Option<Arc<str>>, pub webdav_user: Option<Arc<str>>, pub webdav_app_token: Option<Arc<str>>, pub target_folder: Option<Arc<str>>, pub weeks_horizon: u32, pub cron_schedule: Arc<str>, pub last_success_at: Option<PrimitiveDateTime>, pub last_error_at: Option<PrimitiveDateTime>, pub last_error_message: Option<Arc<str>> }`. `#[cfg(feature = "service-impl")]`-`From<&PdfExportConfig>`-Impl konvertiert — **das Response-Objekt maskiert `webdav_app_token` zu `None`** (never leak the token back over the wire — auch nicht an den Admin; Placeholder-Verhalten in 48-05).

4) `rest/src/pdf_export_config.rs`:
   - `pub fn generate_route<RestState>() -> Router<RestState>` mit `GET /` (`get_config`) + `PUT /` (`update_config`).
   - `#[utoipa::path]` mit `tags = ["PdfExportConfig"]`, `content_type = "application/json"`, response body = PdfExportConfigTO.
   - Handler-Muster wie `vacation_entitlement_offset.rs`: `error_handler(async { … }.await)`. Response mit Header `Content-Type: application/json` (HYG-05 gate).
   - PUT-Handler: `Json(to): Json<PdfExportConfigTO>` → mappe auf `PdfExportConfigUpdate` → `service.update(update, context.into(), None).await?` → serialize masked response.
   - GET-Handler: `service.get(context.into(), None).await?` → serialize masked response.
   - `#[derive(OpenApi)] pub struct PdfExportConfigApiDoc` mit beiden paths + component schemas(PdfExportConfigTO).

5) `rest/src/lib.rs`:
   - `mod pdf_export_config;` einfügen.
   - `RestStateDef` trait: neue assoziierte Typ `PdfExportConfigService: service::pdf_export_config::PdfExportConfigService<Context = Context> + Send + Sync + 'static;` plus Getter `fn pdf_export_config_service(&self) -> Arc<Self::PdfExportConfigService>;`.
   - `nest("/pdf-export-config", pdf_export_config::generate_route())` unter die anderen `.nest`-Calls in `start_server` einfügen.
   - `(path = "/pdf-export-config", api = pdf_export_config::PdfExportConfigApiDoc)` in den `#[openapi(nest(...))]`-Block eintragen.

6) `shifty_bin/src/main.rs`:
   - `PdfExportConfigDao::new(pool.clone())` neben den anderen DAO-Instanzen.
   - `PdfExportConfigServiceDeps`-Impl analog `VacationEntitlementOffsetServiceDeps`.
   - `PdfExportConfigServiceImpl::new(...)` unter den Basic-Services (VOR den Business-Logic-Services — Service-Tier-Konvention).
   - `RestStateImpl` bekommt Feld `pdf_export_config_service: Arc<PdfExportConfigServiceImpl>` + Getter-Impl.

Clippy hard gate. Kein `expect()` in Prod-Code (nur in Tests). Der REST-Handler MUSS `Content-Type: application/json` setzen — HYG-05 aus v2.2 audit-testet das (Phase 46).
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
    - `PdfExportConfigService`-Trait + Domain-Struct + Update-DTO in `service/`.
    - `PdfExportConfigServiceImpl` mit `gen_service_impl!` in `service_impl/`.
    - `PdfExportConfigTO` in `rest-types/` (Token maskiert im Response).
    - `rest/src/pdf_export_config.rs` mit GET+PUT + utoipa + Content-Type-Header.
    - `RestStateDef` erweitert; `nest()` + ApiDoc-Registrierung in `rest/src/lib.rs`; DI-Wiring in `main.rs`.
    - `cargo build --workspace` + `cargo clippy --workspace -- -D warnings` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrationstest-Suite pdf_export_config + Snapshot-Grep-Gate + ROADMAP-Anpassung</name>
  <files>service_impl/src/test/mod.rs, service_impl/src/test/pdf_export_config.rs, .planning/ROADMAP.md</files>
  <behavior>
    - Test 1 (fresh_db_returns_seed_row): frische in-memory-DB nach `sqlx::migrate!` → `service.get(Authentication::Full, None)` liefert enabled=false, weeks_horizon=8, cron_schedule="0 6 * * 1", token/url/user/target None
    - Test 2 (non_admin_context_forbidden): Context ohne "admin"-Privilege → `service.get(context, None)` liefert `ServiceError::Forbidden`; ebenso `service.update(any_update, context, None)`
    - Test 3 (admin_update_persists_and_masks_token_in_get_response): Admin-Context, update mit enabled=true, url/user/target/token gesetzt → nächstes `get()` liefert alle Werte inkl. token; verify: die zusätzliche direkte `dao.get(tx)` liefert token gesetzt (Persistenz-Beweis) — die Token-Maskierung passiert erst im REST-Layer (Task 2), NICHT im Service
    - Test 4 (update_with_none_token_keeps_existing): Admin setzt Token = Some("secret") → dann Update mit token=None + geänderter url → dao.get() liefert weiterhin Some("secret"), aber neue url
    - Test 5 (record_success_and_record_error_persist): after `service.record_success(now_a)`; dao.get last_success_at=Some(now_a), last_error_*=None. Then `service.record_error(now_b, "boom")`; dao.get last_success_at=Some(now_a), last_error_at=Some(now_b), last_error_message=Some("boom")
    - Test 6 (snapshot_version_unchanged_grep_gate): reine Datei-Assert: der `CURRENT_SNAPSHOT_SCHEMA_VERSION`-Wert aus `service_impl/src/billing_period_report.rs` MUSS `12` sein (RPT/EXP nicht Teil von `BillingPeriodValueType`)
  </behavior>
  <action>
Task 3 ist der Integrations-Beweis (in-memory SQLite gegen die echte Migration) plus die begleitende ROADMAP-Anpassung per D-48-ROADMAP.

1) `service_impl/src/test/pdf_export_config.rs` neu anlegen:
   - Muster: `service_impl/src/test/vacation_entitlement_offset.rs` (in-memory Pool, Migrationen laufen lassen, echte DAO + PermissionService-Mock, echter `PdfExportConfigServiceImpl`).
   - `async fn setup()` liefert Test-Fixture mit SqlitePool + PdfExportConfigDaoImpl + gemockter PermissionService (Admin vs Non-Admin) + TransactionDao + ClockService-Mock (feste Zeit) + UuidService-Mock (Sequenz-UUIDs).
   - Sechs Tests wie in `<behavior>` beschrieben, jeweils mit `#[tokio::test]` und aussagekräftigen Assertions.
   - Test 6 (`snapshot_version_unchanged_grep_gate`) verifiziert:
     - `include_str!("../billing_period_report.rs")` lesen und via `grep`-analoge string-search `pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12;` finden. Assert liefert bei Ausnahme klare Message „Phase 48 must NOT bump snapshot version — EXP is not a persisted BillingPeriodValueType".
   - `pub mod pdf_export_config;` in `service_impl/src/test/mod.rs` einfügen (alphabetisch).

2) ROADMAP.md-Anpassung (per D-48-ROADMAP): in `.planning/ROADMAP.md` unter der `Phase 48:`-Sektion, Erfolgs-Kriterium **2** umschreiben. Aktueller Text:
   ```
   2. Konfiguration liegt in der Datenbank (neue Tabelle `pdf_export_config`, Single-Row): URL, User, App-Token (Klartext — bewusste Ops-Entscheidung, DB-File filesystem-geschützt), Zielordner, Wochen-Horizont (Default 8), Cron-Schedule (Default „0 6 * * 1"), Enabled-Toggle. Bearbeitung ausschließlich über die admin-gated Settings-Card. Keine Env-Variablen für dieses Feature.
   ```
   Ist BEREITS in D-48-ROADMAP-Form (steht so in Zeile 132 der ROADMAP). Verify: exakter Text (inkl. Klartext-Wort) muss unter Phase 48 stehen; wenn ja, KEINE weitere ROADMAP-Änderung nötig — sonst mit `Edit` auf oben stehenden Text angleichen. **Nur Edit-Tool nutzen, NIE Write** (ROADMAP-Whole-File-Rule).

3) `cargo sqlx prepare --workspace` erneut ausführen falls Test-Queries neue query!/query_as! einführen (unwahrscheinlich, da Tests via existierende DAO-Methoden gehen).
  </action>
  <verify>
    <automated>cargo test -p service_impl --test '*' pdf_export_config &amp;&amp; cargo test -p service_impl pdf_export_config &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
    - `service_impl/src/test/pdf_export_config.rs` mit 6 Tests, alle grün.
    - Snapshot-Version bleibt 12 (Test 6 belegt).
    - ROADMAP.md Phase 48 Success Criterion 2 = D-48-ROADMAP-konform (schon so — nur verify, ggf. Edit).
    - Full test suite grün: `cargo test --workspace` + `cargo clippy --workspace -- -D warnings`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → REST (admin session) | Non-admin User könnte per Direct-URL GET/PUT `/pdf-export-config` versuchen; der Bearer/Session-Layer authentisiert, aber Autorisierung ist Service-Verantwortung |
| REST → DB (webdav_app_token) | App-Token liegt Klartext in DB; nur DB-File-Zugriff (Betriebssystem-Ebene) schützt |
| Service (`get`-Response) → REST-Layer | Token muss vor Serialisierung maskiert werden — Admin darf den Token NICHT über die HTTP-Response zurücklesen (D-48-01a) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Elevation of Privilege | `PdfExportConfigService::get`/`update` | high | mitigate | Beide Public-API-Methoden rufen `permission_service.check_permission("admin", context)` als ERSTE Operation, bevor `use_transaction`; Test 2 aus Task 3 belegt Non-Admin=Forbidden. `record_success`/`record_error` sind `Authentication::Full`-only (nur Scheduler-Task in 48-04). |
| T-48-02 | Information Disclosure | REST `GET /pdf-export-config` Response | high | mitigate | `PdfExportConfigTO`-Conversion aus `PdfExportConfig` maskiert `webdav_app_token` immer zu `None`. Admin sieht bei Bearbeitung leeres Feld — Update mit leerem Token behält den bestehenden Wert (per Specifics + D-48-REST). Explizit dokumentiert im Modul-Header von `rest/src/pdf_export_config.rs`. |
| T-48-03 | Tampering | DB-File (Klartext-Token) | medium | accept | Bewusste Ops-Entscheidung (D-48-01/Q3a-i): DB-File-Zugriff ist Ops-Verantwortung; kein Feld-Encrypt in v2.2. `webdav_app_token` steht neben anderen Klartext-Feldern der DB. Encrypted-at-rest ist als Deferred-Item vermerkt (48-CONTEXT deferred). |
| T-48-04 | Repudiation | Config-Änderung ohne Audit-Trail | low | accept | `update_process = "phase-48-…"` wird pro update gesetzt (analog paid_limit_config); kein separater Audit-Log für v1. Impersonation-Audit-Layer (Phase 32) protokolliert die Session-Real-User bereits. |
| T-48-05 | Denial of Service | Malformed cron_schedule (`update` speichert kaputten Ausdruck) | low | mitigate | Validation: der Scheduler in 48-04 parst den Cron-String beim Trigger; Parser-Fehler landet in `record_error` und lässt den Server weiterlaufen (per EXP-03). Kein pre-persist-Reject in 48-01 (K.I.S.S., Admin ist Trusted). |
</threat_model>

<verification>
- Migration 20260703000000_create-pdf-export-config.sql wendet sich sauber auf frische in-memory-DB an (durch `service_impl/src/test/pdf_export_config.rs::fresh_db_returns_seed_row` implizit gedeckt).
- Alle 6 Test-Behaviors in Task 3 grün.
- `.sqlx/` Query-Cache mit neuen `query!`/`query_as!`-Fingerprints committet.
- `cargo test --workspace` grün.
- `cargo clippy --workspace -- -D warnings` grün (Backend Clippy Hard-Gate).
- ROADMAP.md Phase 48 Success Criterion 2 D-48-ROADMAP-konform.
- Kein Snapshot-Bump: `service_impl/src/billing_period_report.rs::CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt 12 (Test 6 als durables Gate).
- GET /pdf-export-config und PUT /pdf-export-config sind über die Swagger-UI unter `/swagger-ui` sichtbar (utoipa-Registrierung erfolgt).
</verification>

<success_criteria>
- EXP-02 („Konfiguration über die Admin-UI + DB-Persistenz") ist auf Persistenz-Ebene erfüllt: Tabelle + DAO + Service + REST-Endpoints; die UI folgt in 48-05.
- EXP-03 Status-Anteil („letzter Erfolg / letzter Fehler") ist auf Persistenz-Ebene erfüllt: `record_success`/`record_error` in Service+DAO; der Cron-Task in 48-04 ruft sie auf, die UI zeigt sie in 48-05.
- Backend `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` grün.
- ROADMAP Success Criterion 2 auf DB-Persistenz (nicht Env-Vars) aktualisiert (per D-48-ROADMAP).
</success_criteria>

<output>
Create `.planning/phases/48-nextcloud-pdf-webdav/48-01-SUMMARY.md` when done
</output>
