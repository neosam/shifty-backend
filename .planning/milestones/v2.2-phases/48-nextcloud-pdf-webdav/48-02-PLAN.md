---
phase: 48-nextcloud-pdf-webdav
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - service_impl/Cargo.toml
  - service_impl/src/lib.rs
  - service_impl/src/pdf_render.rs
autonomous: true
requirements:
  - EXP-01

must_haves:
  truths:
    - "D-48-PDF (per Q1): PDF-Renderer nutzt printpdf (pure Rust); render_shiftplan_week_pdf(week: &ShiftplanWeek, sales_persons: &[SalesPerson], header_year: u32, header_week: u8) -> Result<Vec<u8>, ServiceError> liefert die PDF-Bytes"
    - "D-48-PDF-LAYOUT (per Specifics + Q1): Landscape A4 (297×210 mm), Header 'Schichtplan KW {NN} ({JJJJ})', tabellarisches Layout Mo–So Tagesspalten × Sales-Person-Zeilen mit Zeitfenstern und Buchungs-Namen"
    - "D-48-PDF-DETERMINISM (per EXP-01 Success Criterion): gleiche Inputs ⇒ byte-gleiche PDF-Bytes (fixed embedded font, fixed timestamp/producer/creator felder, deterministische Iteration Sales-Person nach id-Reihenfolge, Tage nach DayOfWeek-Reihenfolge)"
    - "D-48-PDF-PURE: das Modul importiert KEIN DAO, KEIN sqlx, KEIN reqwest und KEIN service::pdf_export_config — reine Rendering-Funktion; alle Test-Inputs sind Domain-Structs"
    - "D-48-PDF-ACTIVE-ONLY (per Q1): nur `sales_persons` mit `deleted.is_none()` erscheinen als Zeile — die Filterung passiert im Aufrufer (Scheduler in 48-04), aber `render_shiftplan_week_pdf` doku-vermerkt: 'Caller must pre-filter to active sales persons'"
  artifacts:
    - service_impl/src/pdf_render.rs
  key_links:
    - "48-04 Scheduler importiert `use service_impl::pdf_render::render_shiftplan_week_pdf` und ruft es pro Woche im horizon-Range auf; die zurückgegebenen Bytes gehen 1:1 an den WebDAV-Client aus 48-03"
    - "printpdf ist pure Rust — kein System-Chrome, keine libc-Deps über cargo-Standard hinaus — nix-build bleibt reproduzierbar (verifiziert per `cargo build` unter nix develop)"
---

<objective>
Reine PDF-Rendering-Bibliotheks-Funktion in `service_impl/src/pdf_render.rs`: nimmt einen `ShiftplanWeek` (Domain-Struct aus `service::shiftplan`) plus eine sortierte Liste aktiver `SalesPerson` und gibt deterministische PDF-Bytes im Landscape-A4-Format zurück. TDD-Plan — RED-Test schreibt zuerst die erwartete Byte-Sequenz-Prüfung (Golden-Test mit stabilem Hash), dann implementiert.

Purpose: Erfüllt den Renderer-Teil von EXP-01 (deterministisches Layout, alle aktiven Sales-Persons enthalten, ein PDF pro Woche). Isoliert-testbar ohne DB, ohne HTTP, ohne Cron.

Output: `service_impl/src/pdf_render.rs` mit `pub fn render_shiftplan_week_pdf(...) -> Result<Vec<u8>, ServiceError>`, `printpdf` als neue Cargo-Dep in `service_impl/Cargo.toml`, plus RED/GREEN/REFACTOR-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-nextcloud-pdf-webdav/48-CONTEXT.md

# Domain-Structs, die als Input verwendet werden:
@service/src/shiftplan.rs
@service/src/sales_person.rs
@service/src/slot.rs
@service/src/booking.rs

# Cargo-Manifest-Muster für neue Deps (vergleiche tokio-cron in Zeile 12):
@service_impl/Cargo.toml
</context>

<feature>
  <name>PDF-Renderer für Wochen-Schichtplan (pure Rendering, keine I/O)</name>
  <files>service_impl/src/pdf_render.rs, service_impl/Cargo.toml, service_impl/src/lib.rs</files>
  <behavior>
    - **Test A (RED first)** `pdf_render::test::empty_week_yields_valid_pdf_signature`: Input = `ShiftplanWeek { year: 2026, calendar_week: 27, days: [7 Days with empty slots] }` + `sales_persons: []`. Erwartung: `render_shiftplan_week_pdf(&week, &[], 2026, 27)` returns Ok, die ersten 4 Bytes sind `%PDF` (`b"%PDF"`), Länge > 500 Bytes (Header/Info+Struktur).
    - **Test B** `pdf_render::test::header_contains_year_and_week`: mit einem PDF-Text-Extract (String::from_utf8_lossy auf die produzierten Bytes) muss der Header-String „Schichtplan KW 27 (2026)" enthalten sein (printpdf embeddet Strings direkt; ein einfacher `bytes.windows(N).any(|w| w == b"Schichtplan KW 27 (2026)")` oder analog auf UTF-8 String-Substring-Suche mit `String::from_utf8_lossy`).
    - **Test C** `pdf_render::test::all_active_sales_persons_appear`: Input mit 3 Sales-Persons (Namen „Alice", „Bob", „Charlie"), jede hat 1 Buchung in einem Slot am Montag → die produzierten PDF-Bytes enthalten alle 3 Namen als Substring (via lossy UTF-8-Search).
    - **Test D** `pdf_render::test::deterministic_bytes_for_same_input`: zwei Aufrufe mit identischem Input produzieren byte-gleiche Vec<u8>. printpdf setzt eine `.creation_date()` / `producer` — beide werden auf konstante Werte gesetzt: `.with_creation_date(time::PrimitiveDateTime::new(time::Date::from_calendar_date(2000, time::Month::January, 1).unwrap(), time::Time::MIDNIGHT))` (oder analog gemäß printpdf-API); `producer("shifty-pdf-export")`.
    - **Test E** `pdf_render::test::sales_persons_sorted_by_id`: Input mit 3 Sales-Persons in „falscher" Reihenfolge — Renderer sortiert intern nach `sales_person.id` und produziert die gleichen Bytes wie ein Aufruf mit vorsortiertem Input.
    - **REFACTOR-Phase**: Extract-Helper `fn build_page_header(...)`, `fn build_day_column_headers(...)`, `fn build_sales_person_row(...)` — reine private Fns, mind. 2 unit-tests darüber (`build_page_header_produces_expected_text`, `build_sales_person_row_lists_bookings_time_ranges`).
  </behavior>
  <implementation>
1) `service_impl/Cargo.toml` erweitern: `printpdf = "0.7"` (aktuelle stabile 0.7er-Reihe; wenn 0.8 draußen ist verifizieren dass API kompatibel — sonst pin auf letzten 0.7.x). Vor dem Bearbeiten: `nix develop -c cargo search printpdf` bestätigt die letzte Version. **Kein `[dev-dependencies]`-Eintrag** — Renderer ist Prod-Code, Tests hängen an der gleichen Krate.

2) `service_impl/src/pdf_render.rs` neu:
   ```rust
   //! Deterministic PDF renderer for weekly shift plans (Phase 48 EXP-01).
   //!
   //! Pure module: takes domain structs, returns bytes. No I/O, no DAO,
   //! no HTTP. `printpdf` is the only external dep (pure Rust, D-48-PDF).
   //! Caller must pre-filter `sales_persons` to active (non-deleted) rows.
   ```
   - `pub fn render_shiftplan_week_pdf(week: &service::shiftplan::ShiftplanWeek, sales_persons: &[service::sales_person::SalesPerson], header_year: u32, header_week: u8) -> Result<Vec<u8>, service::ServiceError>`
   - Interne Sortierung: `let mut sp: Vec<_> = sales_persons.iter().collect(); sp.sort_by_key(|s| s.id);`
   - Landscape A4: `PdfDocument::new("Schichtplan", Mm(297.0), Mm(210.0), "Layer")` bzw. äquivalent zur printpdf-API der gewählten Version.
   - Deterministic-Metadata (per D-48-PDF-DETERMINISM): `.with_creation_date(fixed)`, `.with_modification_date(fixed)`, `.with_producer("shifty-pdf-export")`, `.with_document_title(format!("Schichtplan KW {header_week:02} ({header_year})"))`. Falls printpdf-Version die Metadata nicht überschreiben lässt → post-processing des Byte-Buffers ist explizit VERBOTEN; alternative Wahl: `printpdf::PdfDocumentReference::save_to_bytes` + `.metadata()`-Builder wie im letzten stabilen Release.
   - Font: eingebetteter Standard-PDF-Font (Helvetica) via `document.add_builtin_font(BuiltinFont::Helvetica)?` — deterministisch, keine System-Deps.
   - Layout:
     - Header y=195mm: `use_text(format!("Schichtplan KW {header_week:02} ({header_year})"), 16.0, Mm(20.0), Mm(195.0), &font)`
     - 7 Tagesspalten von x=20 bis x=277 (Breite pro Tag ≈ 37mm), Header-Zeile y=180mm: Mo/Di/Mi/Do/Fr/Sa/So (nutze `shifty_utils::DayOfWeek` short_label).
     - Sales-Person-Zeilen y=170mm-Schritt −8mm pro Zeile: Name links (Mm(15.0)) + pro Tag Zeitfenster + Booking-Namen (aus `ShiftplanDay.slots` iteriert; für jede Booking, deren `sales_person.id == sp.id`, füge in die entsprechende Zellen-Position `"{start}-{end}"` ein).
     - Wenn mehr Sales-Persons als eine Seite tragen: `document.add_page(...)` mit gleichem Header (v1: ein Overflow-Test pro Seite, für v1 20 Sales-Persons genügen 1 Seite; multi-page Kandidat für Iteration).
   - Errors: printpdf-Fehler in `ServiceError::InternalError` mappen (analog `TimeFormatError` etc.); dokumentiere den Mapping-Schritt.
   - `pub mod pdf_render;` in `service_impl/src/lib.rs` einfügen — als `pub` (Scheduler in 48-04 muss reinschauen können).

3) Deterministic-Property-Sicherung (RED-first-Reihenfolge):
   - Zuerst Test D (`deterministic_bytes_for_same_input`) schreiben mit `let a = render(...)?; let b = render(...)?; assert_eq!(a, b);` — auf leerem Renderer wird das erste Mal fehlschlagen (weil printpdf per Default `Utc::now()` in die Metadata setzt). GREEN durch Fixed-Metadata.
   - Dann Test A/B/C/E in Reihenfolge, jeder mit atomarem Commit.

4) Kein `#[allow(...)]` ohne Kommentar. `cargo clippy -p service_impl -- -D warnings` grün als letzter Gate.

5) Wenn printpdf 0.7 die Deterministic-Metadata-API nicht bietet (`.with_creation_date` etc. existieren erst in 0.8), dann **pin auf 0.8** — nicht die Determinismus-Anforderung aufweichen. Bei zwingender Wahl auf 0.7 und fehlender API: post-process der PDF-`Trailer /Info`-Referenz per binary-search+replace der Timestamp-Bytes ist NICHT erlaubt (fragil). Alternative in dem Fall: renderer liefert die Metadata-Timestamp als Parameter → Caller (48-04) setzt fixed. → **Vor der Implementation: `cargo doc --open` von printpdf gegenchecken; pin so, dass Deterministic-Metadata First-Class funktioniert.**
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

Rein interner Pure-Rust-Renderer — keine Trust Boundary. Empfängt nur Domain-Structs, produziert `Vec<u8>`. Keine externe I/O, keine User-Inputs.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06 | Denial of Service | `render_shiftplan_week_pdf` mit sehr vielen Sales-Persons (>1000) → OOM/CPU-Blow | low | accept | Shifty hat <100 aktive Sales-Persons realistisch; Scheduler-Task in 48-04 läuft asynchron (Tokio) und blockiert den Server nicht. Kein Pre-Limit in dieser Task; Kandidat für Follow-up wenn nötig. |
| T-48-07 | Tampering | Third-party Crate `printpdf` (Supply-Chain) | medium | mitigate | Package-Legitimacy-Gate: `printpdf` ist auf crates.io seit 2016, >2M downloads, gepflegt (fzimmerhof/printpdf) — legitimate. Human-Verify Checkpoint in Task 1 (Cargo.toml-Bump) bestätigt vor `cargo add`. |
</threat_model>

<verification>
- `cargo test -p service_impl pdf_render` grün (alle RED-then-GREEN Tests A/B/C/D/E).
- `cargo build -p service_impl` grün.
- `cargo clippy -p service_impl -- -D warnings` grün.
- Deterministic-Assertion: zwei aufeinanderfolgende `render_shiftplan_week_pdf`-Aufrufe mit gleichem Input liefern `Vec<u8>` mit `assert_eq!` → Test D belegt es.
- Header enthält „Schichtplan KW {NN} ({JJJJ})" — Test B belegt es.
</verification>

<success_criteria>
- EXP-01 Renderer-Anteil erfüllt: pure Fn liefert deterministisches PDF für einen ShiftplanWeek + aktive Sales-Persons.
- `printpdf`-Dep gezogen und in `Cargo.lock` gepinnt.
- Alle Renderer-Tests grün; Clippy hard gate grün.
</success_criteria>

<output>
Create `.planning/phases/48-nextcloud-pdf-webdav/48-02-SUMMARY.md` when done
</output>
