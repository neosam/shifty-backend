---
phase: 48-nextcloud-pdf-webdav
plan: 05
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-04
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/page/settings.rs
  - shifty-dioxus/src/state/pdf_export_config.rs
autonomous: true
requirements:
  - EXP-02
  - EXP-03

must_haves:
  truths:
    - "D-48-UI-CARD (per Q8): eine neue admin-gated Card in shifty-dioxus/src/page/settings.rs, platziert NACH den bestehenden 3 Cards (Paid-Limit / Holiday-Cutoff / Special-Days), Titel-Key SettingsPdfExportTitle"
    - "D-48-UI-FIELDS (per Q8 + Specifics): Toggle 'PDF-Export aktiviert' (enabled), TextInputs für nextcloud_url / webdav_user / target_folder, PasswordInput für webdav_app_token mit Placeholder 'settings.pdf_export.token.placeholder' (leer speichern = keep), NumberInput weeks_horizon (Default 8, Range 1-52), TextInput cron_schedule (Default '0 6 * * 1'), Save-Button, 'Jetzt exportieren'-Button, read-only Status: 'Letzter Erfolg: {timestamp}' + 'Letzter Fehler: {timestamp} — {message}'"
    - "D-48-UI-TOKEN-KEEP (per Specifics 'leer speichern lässt existierenden Wert stehen'): das FE sendet im PUT-Body webdav_app_token=None wenn das Passwort-Feld leer war, Some(new) wenn eingetippt — der BE-Service (48-01) merged entsprechend"
    - "D-48-UI-I18N: neue Keys in de/en/cs für Title, Enabled-Label, alle 6 Feldnamen, Placeholder, Save, Save-Success, Save-Error, TriggerNow, TriggerNow-Success, LastSuccess-Prefix, LastError-Prefix, EmptyStatus"
    - "D-48-UI-GATE (WR-02-Muster aus SettingsPage): Card zeigt sich nur für Admin (bestehendes `is_admin`-Check am Anfang von `SettingsPage` deckt das ab); der neue Card-Block ist ohne zusätzlichen inneren Gate innerhalb des Admin-Blocks aufgehängt"
    - "D-48-UI-FE-CLIPPY (per Phase 45 D-45-01): FE-Clippy-Gate `cargo clippy -p shifty-dioxus -- -D warnings` bleibt grün (0 warnings)"
  artifacts:
    - shifty-dioxus/src/state/pdf_export_config.rs
    - shifty-dioxus/src/api.rs (Extension)
    - shifty-dioxus/src/loader.rs (Extension)
    - shifty-dioxus/src/i18n/{en,de,cs}.rs (Extension)
    - shifty-dioxus/src/page/settings.rs (Extension)
  key_links:
    - "PUT /pdf-export-config im FE-API-Layer sendet PdfExportConfigTO mit token=None wenn Passwort-Feld leer → BE 48-01 mergt (behält bestehenden Token)"
    - "POST /pdf-export-config/trigger im FE-API-Layer löst den admin-gated 'Jetzt exportieren'-Button aus → BE 48-04 spawnt run_once_now, Response 202"
    - "GET /pdf-export-config-Response liefert token=null (BE 48-01 maskiert immer) → FE stellt Passwort-Feld leer dar, Placeholder erklärt das Verhalten"
---

<objective>
Admin-gated Settings-Card in `shifty-dioxus/src/page/settings.rs` für den Nextcloud-PDF-Export: Konfig-Formular (Toggle + 6 Felder), Save-Button, „Jetzt exportieren"-Button, Read-only Status-Anzeige (letzter Erfolg / letzter Fehler). i18n de/en/cs. Nutzt die REST-Endpoints aus 48-01 + 48-04.

Purpose: Erfüllt EXP-02 Admin-UI-Anteil und EXP-03 Admin-Panel-Status-Anzeige. Die Card ist der bewusste Ersatz für Env-Variablen — Admin editiert Config live in der UI.

Output: Neuer Card-Block in `settings.rs`, Frontend-API-Funktionen in `api.rs`/`loader.rs`, Domain-Type in `state/pdf_export_config.rs`, i18n-Keys in allen 3 Sprachen.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/48-nextcloud-pdf-webdav/48-CONTEXT.md
@.planning/phases/48-nextcloud-pdf-webdav/48-01-PLAN.md
@.planning/phases/48-nextcloud-pdf-webdav/48-04-PLAN.md

# Existierende Settings-Cards als Vorbild:
@shifty-dioxus/src/page/settings.rs

# Frontend-API/Loader-Muster (fetch/put):
@shifty-dioxus/src/api.rs
@shifty-dioxus/src/loader.rs

# i18n-Keys-Enum:
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/i18n/en.rs
@shifty-dioxus/src/i18n/de.rs
@shifty-dioxus/src/i18n/cs.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Frontend-API/Loader + Domain-Type + i18n-Keys</name>
  <files>shifty-dioxus/src/state/pdf_export_config.rs, shifty-dioxus/src/state/mod.rs (if applicable), shifty-dioxus/src/api.rs, shifty-dioxus/src/loader.rs, shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <behavior>
    - Test 1 (i18n_presence): pure fn Test analog `settings.rs`-Muster für SDF-04: für jede der 3 Locales muss jeder neuer PdfExport-Key einen non-empty String liefern
    - Test 2 (config_form_to_put_body_omits_empty_token): pure fn `pdf_export_form_to_put_body(form: &PdfExportForm) -> PdfExportConfigTO` — bei form.token_input.is_empty() → resulting TO.webdav_app_token = None; bei nicht-leer → Some(form.token_input.clone())
    - Test 3 (config_form_from_response_leaves_token_input_empty): pure fn `pdf_export_form_from_response(response: &PdfExportConfigTO) -> PdfExportForm` — response.webdav_app_token ist immer None (BE maskiert); form.token_input muss "" sein
    - Test 4 (weeks_horizon_clamp): pure fn `clamp_weeks_horizon(input: i32) -> u32` — 0 → 1, 60 → 52, 8 → 8 (Range 1..=52 aus D-48-UI-FIELDS)
  </behavior>
  <action>
1) `shifty-dioxus/src/state/pdf_export_config.rs` neu:
   - `pub struct PdfExportForm { pub enabled: bool, pub nextcloud_url: String, pub webdav_user: String, pub token_input: String, pub target_folder: String, pub weeks_horizon: u32, pub cron_schedule: String, pub last_success_at: Option<PrimitiveDateTime>, pub last_error_at: Option<PrimitiveDateTime>, pub last_error_message: Option<String> }` mit `Default`.
   - Pure Fns: `pdf_export_form_to_put_body`, `pdf_export_form_from_response`, `clamp_weeks_horizon`. Alle mit `#[cfg(test)]`-Tests wie in `<behavior>`.
   - `pub mod pdf_export_config;` in `shifty-dioxus/src/state/mod.rs` (falls das Modul-Register-Muster so ist; sonst direkt in `main.rs` oder `lib.rs`).

2) `shifty-dioxus/src/api.rs`:
   - `pub async fn get_pdf_export_config(config: &AppConfig) -> Result<PdfExportConfigTO, ShiftyError>` — GET /pdf-export-config.
   - `pub async fn put_pdf_export_config(config: &AppConfig, body: PdfExportConfigTO) -> Result<PdfExportConfigTO, ShiftyError>` — PUT /pdf-export-config.
   - `pub async fn trigger_pdf_export(config: &AppConfig) -> Result<(), ShiftyError>` — POST /pdf-export-config/trigger; Erwartete Response 202 → Ok(()); 4xx/5xx → `ShiftyError::…`.

3) `shifty-dioxus/src/loader.rs`:
   - `pub async fn get_pdf_export_config(config: AppConfig) -> Result<PdfExportForm, ShiftyError>` — kombiniert `api::get_pdf_export_config` + `pdf_export_form_from_response`.
   - `pub async fn save_pdf_export_config(config: AppConfig, form: PdfExportForm) -> Result<PdfExportForm, ShiftyError>` — kombiniert `pdf_export_form_to_put_body` + `api::put_pdf_export_config` + `pdf_export_form_from_response`.
   - `pub async fn trigger_pdf_export_now(config: AppConfig) -> Result<(), ShiftyError>` — Thin-Wrapper um `api::trigger_pdf_export`.

4) `shifty-dioxus/src/i18n/mod.rs` — neue Enum-Varianten in `Key`:
   - `SettingsPdfExportTitle`, `SettingsPdfExportEnabled`, `SettingsPdfExportUrl`, `SettingsPdfExportUser`, `SettingsPdfExportToken`, `SettingsPdfExportTokenPlaceholder`, `SettingsPdfExportTargetFolder`, `SettingsPdfExportWeeksHorizon`, `SettingsPdfExportCronSchedule`, `SettingsPdfExportSave`, `SettingsPdfExportSaveSuccess`, `SettingsPdfExportSaveError`, `SettingsPdfExportTriggerNow`, `SettingsPdfExportTriggerNowSuccess`, `SettingsPdfExportTriggerNowError`, `SettingsPdfExportLastSuccess`, `SettingsPdfExportLastError`, `SettingsPdfExportStatusEmpty`, `SettingsPdfExportHelp`.

5) Translations in en.rs/de.rs/cs.rs:
   - **de** (Referenz): „PDF-Export nach Nextcloud"; „Export aktiviert"; „Nextcloud-URL"; „WebDAV-Benutzer"; „App-Token"; „(unverändert, hier neues Token eintippen)"; „Zielordner"; „Wochen-Horizont"; „Cron-Zeitplan"; „Speichern"; „Konfiguration gespeichert"; „Speichern fehlgeschlagen"; „Jetzt exportieren"; „Export angestoßen (läuft im Hintergrund)"; „Export konnte nicht angestoßen werden"; „Letzter Erfolg:"; „Letzter Fehler:"; „Kein Lauf bisher"; „Der Backend-Task rendert die Wochen-Schichtpläne als PDF und lädt sie regelmäßig nach Nextcloud hoch."
   - **en** (idiomatisch): „PDF export to Nextcloud"; „Export enabled"; „Nextcloud URL"; „WebDAV user"; „App token"; „(unchanged, enter a new token here)"; „Target folder"; „Weeks horizon"; „Cron schedule"; „Save"; „Configuration saved"; „Failed to save"; „Export now"; „Export started (running in background)"; „Failed to trigger export"; „Last success:"; „Last error:"; „No runs yet"; „The backend task renders the weekly shift plans as PDF and uploads them to Nextcloud regularly."
   - **cs** (idiomatisch): „PDF export do Nextcloudu"; „Export povolen"; „URL Nextcloudu"; „Uživatel WebDAV"; „Aplikační token"; „(beze změny, sem zadejte nový token)"; „Cílová složka"; „Horizont týdnů"; „Cron plán"; „Uložit"; „Konfigurace uložena"; „Uložení selhalo"; „Exportovat nyní"; „Export spuštěn (běží na pozadí)"; „Export se nepodařilo spustit"; „Poslední úspěch:"; „Poslední chyba:"; „Zatím žádné spuštění"; „Backendová úloha renderuje týdenní směnové plány jako PDF a pravidelně je nahrává do Nextcloudu."

6) `PdfExportConfigTO` in shifty-dioxus: bereits aus dem `rest-types`-Crate wiederverwendet (der Workspace teilt `rest-types` — verify: shifty-dioxus Cargo.toml zieht `rest-types`; sonst rest-types-DTO in FE-Muster inlinen — Präzedenz-Check der bestehenden `PdfExportConfigTO`-Import-Chain).

Alle Tests sind pure Rust-Tests (`#[test]` nicht `#[wasm_bindgen_test]`), laufen im `cargo test -p shifty-dioxus`-Gate.
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo test pdf_export &amp;&amp; cargo build --target wasm32-unknown-unknown &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings</automated>
  </verify>
  <done>
    - `state/pdf_export_config.rs` mit `PdfExportForm` + 3 pure Fns + 4 unit-tests grün.
    - `api.rs` + `loader.rs` erweitert um GET/PUT/Trigger-Funktionen.
    - i18n mod.rs + de.rs + en.rs + cs.rs erweitert um 19 neue Keys, jeweils mit non-empty Wert (Presence-Test grün).
    - WASM-Build (`cargo build --target wasm32-unknown-unknown`) grün.
    - FE-Clippy `cargo clippy -p shifty-dioxus -- -D warnings` grün (0 warnings, per Phase 45 D-45-01 hard gate).
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings-Card 4 „PDF-Export nach Nextcloud" — Form, Save, Trigger, Status</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <action>
1) In `SettingsPage` (nach den bestehenden 3 Cards) einen 4. Card-Block einfügen. Vorbild-Struktur: Card 1 (Paid-Limit) für Toggle-Muster, Card 2 (Holiday-Cutoff) für Input-Value-Loading, Card 3 (Special-Days) für Form-Reset-nach-Save.

2) Card-Struktur:
   ```rust
   // ── Card 4: PDF-Export nach Nextcloud (Phase 48) ──────────────────────
   let config_for_pdf = config.clone();
   let pdf_resource = use_resource(move || loader::get_pdf_export_config(config_for_pdf.clone()));

   let mut pdf_form: Signal<PdfExportForm> = use_signal(PdfExportForm::default);
   let mut pdf_saving = use_signal(|| false);
   let mut pdf_save_result: Signal<Option<Result<(), String>>> = use_signal(|| None);
   let mut pdf_triggering = use_signal(|| false);
   let mut pdf_trigger_result: Signal<Option<Result<(), String>>> = use_signal(|| None);

   use_effect(move || {
       if let Some(Ok(form)) = &*pdf_resource.read_unchecked() {
           pdf_form.set(form.clone());
       }
   });
   ```

3) Handler-Callbacks:
   - `on_save`: aus `pdf_form.read().clone()` → `spawn { loader::save_pdf_export_config(config, form).await }` → auf `pdf_save_result` schreiben, `pdf_form` mit Response updaten (token_input bleibt leer nach Save = geänderter Token ist übertragen).
   - `on_trigger`: `spawn { loader::trigger_pdf_export_now(config).await }` → auf `pdf_trigger_result` schreiben. **Kein Blocken der Save-Handler**: `pdf_triggering.set(true)` vor dem spawn, false danach.

4) UI-Elements (rsx!):
   - Toggle für `enabled` — analog Paid-Limit-Card-Muster (Button mit `aria-pressed`).
   - `<input type="text">` für nextcloud_url mit `oninput`-Handler → `pdf_form.write().nextcloud_url = ...`.
   - `<input type="text">` für webdav_user analog.
   - `<input type="password">` für token_input mit i18n-Placeholder (SettingsPdfExportTokenPlaceholder).
   - `<input type="text">` für target_folder.
   - `<input type="number" min="1" max="52">` für weeks_horizon, `on_change` clampt via `clamp_weeks_horizon`.
   - `<input type="text">` für cron_schedule.
   - Save-Button (disabled während `pdf_saving`).
   - „Jetzt exportieren"-Button (disabled während `pdf_triggering` oder wenn `!pdf_form.read().enabled`).
   - Status-Anzeige:
     ```
     if let Some(ts) = pdf_form.read().last_success_at { div { class: "text-body text-good", "{i18n.t(Key::SettingsPdfExportLastSuccess)} {format_ts(ts)}" } }
     if let (Some(ts), Some(msg)) = (pdf_form.read().last_error_at, &pdf_form.read().last_error_message) { div { class: "text-body text-bad", "{i18n.t(Key::SettingsPdfExportLastError)} {format_ts(ts)} — {msg}" } }
     if pdf_form.read().last_success_at.is_none() && pdf_form.read().last_error_at.is_none() { div { class: "text-body text-ink-muted", "{i18n.t(Key::SettingsPdfExportStatusEmpty)}" } }
     ```
   - Save-Result-Banner + Trigger-Result-Banner (analog `save_result`-Muster in Card 1).

5) **Inline-Result-Banner statt Modal-Dialog** (per memory `feedback_warnings_inline_not_dialog.md`): kein Bestätigungs-Dialog beim „Jetzt exportieren"-Klick. Direkt spawn.

6) `use` imports oben in `settings.rs`: `use crate::state::pdf_export_config::{PdfExportForm, clamp_weeks_horizon};`, `use crate::i18n::Key;` (schon da).

7) **Kein zusätzlicher Admin-Check** innerhalb der Card — der äußere `is_admin`-Return in `SettingsPage` (Zeile 464-475) deckt es.

8) FE-Clippy hard gate (Phase 45 D-45-01) + WASM-Build.
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings &amp;&amp; cargo test -p shifty-dioxus</automated>
  </verify>
  <done>
    - Card 4 in `settings.rs` gerendert nach den bestehenden 3 Cards, mit Toggle + 6 Feldern + Save + „Jetzt exportieren" + Status.
    - GET-Load, PUT-Save, POST-Trigger jeweils funktional an die Loader-Fns gebunden.
    - Inline-Result-Banner, keine Dialoge.
    - WASM-Build grün, FE-Clippy 0 warnings, FE-Test-Suite grün.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Card 4 „PDF-Export nach Nextcloud" in der Admin-Settings-Seite mit Konfig-Formular, Save-Button, „Jetzt exportieren"-Button und Status-Anzeige. Backend läuft, Frontend rendert.</what-built>
  <how-to-verify>
    Browser-Smoke (nicht test-automatisierbar wg. D-25-06 WASM-Datepicker-Klasse; hier eher Live-UI-Verifikation):
    1. Backend starten (`cd shifty-backend && cargo run`), Frontend starten (`cd shifty-dioxus && npx tailwindcss ... & dx serve --hot-reload`).
    2. Login als Admin (DEVUSER / admin).
    3. Navigate zu `/settings` → Card 4 „PDF-Export nach Nextcloud" ist sichtbar unter den 3 bestehenden Cards.
    4. Prüfen: enabled = OFF, alle Felder leer/Default, weeks_horizon = 8, cron = „0 6 * * 1", Status = „Kein Lauf bisher".
    5. Setze Test-Werte: URL = `http://localhost:8081/dav` (dummy), User = `test`, Token = `secret123`, target_folder = `Schichtplaene`. Speichern. Erwartung: „Konfiguration gespeichert"-Banner.
    6. Refresh — Token-Feld ist leer (BE hat maskiert), aber URL/User/Ordner bleiben. Placeholder erklärt „(unverändert, hier neues Token eintippen)".
    7. Enabled=OFF lassen, „Jetzt exportieren" klicken. Erwartung: 202-Response (Trigger-Success-Banner), Server-Logs zeigen „skipped (disabled)".
    8. Enabled=ON, Save, „Jetzt exportieren" — Server-Logs zeigen Aufruf des WebDavClient gegen die Dummy-URL; Fehler (nicht erreichbar oder unbekannt) landet in `last_error_at`. Refresh → Card zeigt „Letzter Fehler:"-Banner.
    9. Non-Admin-User (falls verfügbar) → Settings-Route ist bereits durch äußeren is_admin-Return gesperrt: „Not authorized." bleibt.

    Falls User Live-Test nicht mag: bestätige nur, dass strukturelle Verifikation (WASM-Build + Clippy grün + Presence-Tests grün + Backend-E2E-Test aus 48-04 grün) ausreicht.
  </how-to-verify>
  <resume-signal>Type "approved" or describe UI issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Admin) → REST | Admin-User via Session; äußerer is_admin-Return in SettingsPage + Backend admin-Gate schützen |
| FE-Save → BE PUT | webdav_app_token in Klartext im JSON-Request-Body (HTTPS erwartet in Prod); Placeholder-Verhalten für „unchanged" |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-16 | Elevation of Privilege | Non-Admin lädt /settings direkt | high | mitigate | Bereits durch WR-02 äußeren is_admin-Return in SettingsPage (Zeile 464-475) abgedeckt; kein zusätzlicher Gate nötig. Backend-Gate in 48-01 ist die zweite Verteidigungslinie. |
| T-48-17 | Information Disclosure | Password-Feld type="password" verhindert visuellen Leak, aber DevTools zeigen den Wert im Klartext beim Eintippen | medium | accept | Standard-Browser-Verhalten; DevTools sind Admin-Trust-Level. Autocomplete=off via `autocomplete="new-password"` auf dem Input, um Passwort-Manager-Autofill zu vermeiden. |
| T-48-18 | Tampering | JS-Konsole ändert form-signal, sendet manipulierten PUT | low | accept | Backend validiert (admin-gate + Cron-Parse in 48-04); FE ist Trust-relative. |
| T-48-19 | Information Disclosure | last_error_message enthält Klartext-URL/User-Angaben in FE-Response | low | accept | v1: Fehler-Message enthält HTTP-Status + WebDavError-String; kein Token darin (T-48-08 in 48-03 verhindert). Fine-Grained-Scrubbing als Follow-up. |
</threat_model>

<verification>
- `cargo build --target wasm32-unknown-unknown` grün (WASM-Build-Gate).
- `cargo test -p shifty-dioxus pdf_export` grün (state-tests + i18n-presence).
- `cargo test -p shifty-dioxus` (gesamte FE-Suite) grün.
- `cargo clippy -p shifty-dioxus -- -D warnings` grün (Phase 45 D-45-01 hard gate).
- Browser-Smoke-Checkpoint (blocking) bestätigt Card sichtbar, Save+Trigger funktionieren, Status wird angezeigt.
</verification>

<success_criteria>
- EXP-02 vollständig: Admin editiert PDF-Export-Config live in der UI, Save persistiert in DB.
- EXP-03 vollständig auf UI-Seite: Admin sieht letzten Erfolg + letzten Fehler in der Card; „Jetzt exportieren"-Button verfügbar.
- 3 Sprachen (de/en/cs) mit non-empty Werten für alle neuen Keys.
- FE-Build- und Clippy-Gates grün.
</success_criteria>

<output>
Create `.planning/phases/48-nextcloud-pdf-webdav/48-05-SUMMARY.md` when done
</output>
