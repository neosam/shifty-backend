---
phase: 43-special-days-feintuning
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/page/settings.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements:
  - SDF-03
  - SDF-04
tags:
  - frontend
  - special-days
  - i18n
  - bugfix

must_haves:
  truths:
    - "SDF-03: Nach einem erfolgreichen Special-Day-Create springt sd_year auf das KALENDER-Jahr (`date.year() as u32`) des gepickten Datums, nicht auf das ISO-Wochenjahr — so bleibt ein am 2027-01-01 angelegter Feiertag im 2027er-Jahres-Picker sichtbar, statt still in 2026 zu verschwinden."
    - "SDF-04: Der Text von `Key::SettingsSpecialDaysDuplicateHint` in de/en/cs beschreibt das Replace-Verhalten (in-place-Ersetzung) und nutzt kein blockierendes Wording wie \"already exists\" / \"existiert bereits\" / \"již existuje\" mehr."
    - "SDF-03: Pure-fn `sd_year_after_create(date_str)` (oder analoge Extraktion) mappt einen ISO-Datumstring auf das erwartete Kalenderjahr und ist per Unit-Test grün, inkl. Jahreswechsel-Grenzfall 2027-01-01 (Kalenderjahr 2027, ISO-Woche 53 von 2026)."
    - "SDF-04: i18n-Presence-Test verifiziert, dass in allen drei Locales (de/en/cs) unter `Key::SettingsSpecialDaysDuplicateHint` ein nicht-leerer Text hinterlegt ist und dass der Text KEINE der Blocking-Substrings \"already exists\", \"existiert bereits\", \"již existuje\" enthält."
    - "Beide Fixes brechen weder `cargo build --target wasm32-unknown-unknown` noch `cargo test -p shifty-dioxus` noch `cargo clippy -p shifty-dioxus -- -D warnings` (Backend unberührt, Backend-Clippy-Gate bleibt grün)."
  artifacts:
    - shifty-dioxus/src/page/settings.rs (SDF-03 sd_year-Fix + pure fn + Unit-Test)
    - shifty-dioxus/src/i18n/de.rs (SDF-04 neuer DE-Text)
    - shifty-dioxus/src/i18n/en.rs (SDF-04 neuer EN-Text)
    - shifty-dioxus/src/i18n/cs.rs (SDF-04 neuer CS-Text)
  key_links:
    - "Success handler des Create-Pfads (`settings.rs:627-655`) → dort steht der aktuelle `sd_year.set(iso_year)` (D-42-01 Kommentar), der auf `date.year()` umgezogen wird."
    - "Render-Zweig Row D (`settings.rs:884`) rendert `Key::SettingsSpecialDaysDuplicateHint` — Copy muss zum SDF-01-Replace-Verhalten passen."
    - "`SettingsSpecialDaysDuplicateHint` Definition in `shifty-dioxus/src/i18n/mod.rs:678` + 3 Locale-Registrationen (de.rs:1195, en.rs:1108, cs.rs:1185)."
---

<objective>
Zwei präzise, pure-FE Bugfixes am Special-Days-Card in den Einstellungen:

- **SDF-03**: Der Post-Create Sprung von `sd_year` in `page/settings.rs` nutzt aktuell das **ISO-Wochen-Jahr** (`iso_year`) — dadurch verschwindet ein am 2027-01-01 angelegter Feiertag scheinbar spurlos in 2026 (ISO-Woche 53). Der Roadmap-Success-Kriterium fordert **Kalender-Jahr-Semantik**: `date.year()`. Das ist ein 1-Zeilen-Bugfix plus reine, unit-getestete Extraktion.
- **SDF-04**: Der Duplikat-Hinweis-Text (`SettingsSpecialDaysDuplicateHint`) suggeriert in allen drei Locales blockierende Semantik ("already exists"). Seit SDF-01/Phase 36 ist der Backend-Pfad aber **Replace-in-place** (siehe `service_impl/src/special_days.rs:137-163`) — der Hinweis muss diese Semantik widerspiegeln und darf nicht mehr wie ein Fehler wirken. i18n-Presence-Test + Anti-Wording-Test gegen die alten Substrings.

Purpose:
- 1.1.-Anzeige-Bug beim Jahreswechsel abstellen (User picked 2027-01-01, Picker sollte auf 2027 gehen, nicht 2026).
- Nicht-blockierendes Wording für den Duplikat-Hinweis, konsistent zum Backend-Replace-Verhalten (WR-02 v1.11 auflösen).

Output:
- Verschobenes `sd_year.set(...)` (Kalenderjahr) plus pure fn `sd_year_after_create` (im gleichen `pub(crate)` Muster wie `special_day_form_after_create` / D-42-01).
- Drei angepasste i18n-Texte + Presence/Anti-Wording-Test.
- Kein Backend-Touch, kein Snapshot-Bump, keine Migration, keine neuen Deps.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-special-days-feintuning/43-CONTEXT.md

# Source files (interfaces this plan touches)
@shifty-dioxus/src/page/settings.rs
@shifty-dioxus/src/i18n/de.rs
@shifty-dioxus/src/i18n/en.rs
@shifty-dioxus/src/i18n/cs.rs
@shifty-dioxus/src/i18n/mod.rs
</context>

<interfaces>
## Existing interfaces this plan reuses (do NOT redefine)

- `parse_date_to_iso_parts(date_str: &str) -> Option<(u32, u8, DayOfWeekTO)>` — `page/settings.rs:33`. Liefert `(iso_year, iso_week, weekday)`. Die iso_year-Komponente ist genau das, was SDF-03 NICHT mehr fürs `sd_year`-Set haben will. Weiterhin für Body-Konstruktion nutzen (das persistierte `special_day.year` bleibt ISO-Wochen-Jahr, das ist eine Backend-Schema-Frage und nicht Scope dieser Phase).
- Pure-fn Muster für Card-3 (Phase 42): `special_day_form_after_create(before: &SpecialDayForm) -> SpecialDayForm`, `is_special_day_form_valid`, `should_show_duplicate_hint` — alle `pub(crate)` in `settings.rs:105-151`, alle unit-getestet in `#[cfg(test)] mod tests`. SDF-03 folgt exakt diesem Muster.
- `use time::macros::format_description` + `time::Date::parse(date_str, format_description!("[year]-[month]-[day]"))` — bereits importiert oben in `settings.rs`.
- `time::Date::year() -> i32` — Kalenderjahr eines `time::Date` (nicht ISO-Wochen-Jahr).
- `Key::SettingsSpecialDaysDuplicateHint` — deklariert in `shifty-dioxus/src/i18n/mod.rs:678`, registriert in `de.rs:1195`, `en.rs:1108`, `cs.rs:1185`.

## Do NOT create

- Neuen i18n-Key. Der Text unter dem existierenden Key wird umgeschrieben, damit alle Verwendungsstellen (aktuell nur `settings.rs:886`) automatisch profitieren.
- Neuen REST-Endpoint, keinen Backend-Touch.
- Neuen Storage-Ort für `sd_year` — der `Signal<u32>` bleibt bestehen, nur der eingesetzte Wert ändert sich im Success-Handler.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SDF-03 — sd_year post-create nutzt Kalenderjahr statt ISO-Jahr</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <read_first>
    - shifty-dioxus/src/page/settings.rs (Zeilen 1-40 für time-Import + parse_date_to_iso_parts; Zeilen 100-152 für existierende pure-fn Muster D-42-01/D-42-05; Zeilen 627-655 für den Create-Success-Handler wo `sd_year.set(iso_year)` steht; Zeilen 153-330 für existierendes `#[cfg(test)] mod tests` Layout)
  </read_first>
  <behavior>
    Für SDF-03 (per Roadmap-Success-Kriterium 1: „`settings.rs` lädt `special_days` nach `date.year()` — 1.1.-Test grün"):

    - Neue `pub(crate) fn sd_year_after_create(date_str: &str) -> Option<u32>` in `settings.rs`, unmittelbar unter `special_day_form_after_create` (~ Zeile 137). Verhalten:
      * Parst `date_str` als ISO-Datum `[year]-[month]-[day]` mit `time::Date::parse` und dem existierenden `format_description!` Muster.
      * Bei Erfolg: `Some(date.year() as u32)` — das ist das **Kalenderjahr** (im Gegensatz zu `to_iso_week_date().0`).
      * Bei Parse-Fehler: `None`.
    - Unit-Tests im existierenden `#[cfg(test)] mod tests`:
      * Test 1 (Happy-Path Mid-Year): `sd_year_after_create("2026-08-15") == Some(2026)`.
      * Test 2 (Jahreswechsel — Kernpunkt SDF-03): `sd_year_after_create("2027-01-01") == Some(2027)`. Zusatz-Assertion daneben: `parse_date_to_iso_parts("2027-01-01").unwrap().0 == 2026` — dokumentiert im gleichen Test-Body den Unterschied Kalenderjahr↔ISO-Jahr; wenn `time::util::iso_week` einmal die ISO-Semantik ändert, fällt der Test auf und der Fix bleibt verstanden.
      * Test 3 (Silvester): `sd_year_after_create("2026-12-31") == Some(2026)` (ISO-Woche 53 → ISO-Jahr 2026 auch hier gleich, aber wichtig als Symmetriegegenprobe zu Test 2).
      * Test 4 (Invalid): `sd_year_after_create("not-a-date") == None`.

    Anschließend die vorhandene Zeile im Create-Success-Handler (`settings.rs:635`, im Block der ~ 2026-06-30 als WR-04 kommentiert ist) von

        sd_year.set(iso_year);

    auf einen Aufruf des pure fn umstellen, der das Kalenderjahr liefert. Fallback auf `iso_year` nur falls `sd_year_after_create` `None` liefert (Parse hat aber vorher schon geklappt, sonst wäre der Handler nicht bis hier gekommen; die `unwrap_or(iso_year)`-Rückfallebene macht den Fix defensiv). Kommentar auf D-42-01/WR-04 aktualisieren und Grund nennen: "SDF-03: date.year() ist das Kalenderjahr — verhindert, dass ein am 2027-01-01 angelegter Feiertag in 2026 verschwindet."
  </behavior>
  <action>
    Umsetzung SDF-03 per D-43 in `shifty-dioxus/src/page/settings.rs` (nur diese Datei):

    1. **Neue pure fn** — unmittelbar unter `special_day_form_after_create` (~ Zeile 137) `pub(crate) fn sd_year_after_create(date_str: &str) -> Option<u32>` mit den Semantik oben. Nutze das bereits importierte `time::macros::format_description!` und `time::Date::parse`. Docstring erklärt: „Post-create Ziel-Jahr für den Card-3 Jahres-Picker. Anders als `parse_date_to_iso_parts` liefert diese fn das **Kalenderjahr** (`date.year()`), nicht das ISO-Wochen-Jahr. Behebt SDF-03: am 2027-01-01 angelegte Feiertage bleiben im 2027er-Picker sichtbar (statt still in 2026 zu verschwinden, wo `to_iso_week_date().0` sie hinschickt)."
    2. **Success-Handler-Fix** — im `spawn(async move { match api::create_special_day(...) { Ok(_) => { ... sd_year.set(iso_year); ... } } })`-Block (~ Zeile 635) den `sd_year.set(iso_year)`-Call ersetzen: `sd_year.set(sd_year_after_create(&date_s).unwrap_or(iso_year));`. `date_s` ist im gleichen Closure vorhandenes lokales `String` mit dem User-gepickten ISO-Datum (siehe Zeile 575 `let date_s = sd_date_str.read().clone();`). Kommentar-Block über der Zeile (bisher „WR-04: … Switch the picker to the entry's ISO year …") anpassen auf: „SDF-03: Sprung auf das **Kalenderjahr** des gepickten Datums (nicht ISO-Wochen-Jahr) — sonst verschwindet ein am 2027-01-01 angelegter Feiertag scheinbar in 2026."
    3. **Unit-Tests** — vier `#[test]`-Funktionen im existierenden `#[cfg(test)] mod tests`-Block: `sd_year_after_create_mid_year`, `sd_year_after_create_new_year_calendar_vs_iso`, `sd_year_after_create_silvester`, `sd_year_after_create_invalid_returns_none`. Test 2 asserted ZUSÄTZLICH die ISO-Sicht: `let iso = parse_date_to_iso_parts("2027-01-01").unwrap().0; assert_eq!(iso, 2026, "sanity: 2027-01-01 falls into ISO year 2026");` — genau der Grenzfall, den SDF-03 abstellt.
    4. **Keine anderen Codeänderungen** — insbesondere `parse_date_to_iso_parts` bleibt unverändert (wird weiter für `iso_week` und `weekday` im Body gebraucht). Der Jahres-Picker-onchange-Handler (~ Zeile 793) bleibt unverändert (User setzt Zahl direkt, Kalenderjahr-Semantik ist dort schon korrekt).
    5. Nach dem Edit `cd shifty-dioxus && cargo fmt` laufen lassen; anschließend Gates aus `<verify>`.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test -p shifty-dioxus --lib sd_year_after_create && cargo build --target wasm32-unknown-unknown && cargo clippy -p shifty-dioxus --lib -- -D warnings</automated>
  </verify>
  <done>
    - `sd_year_after_create` als `pub(crate) fn` in `shifty-dioxus/src/page/settings.rs` existiert und hat einen Docstring, der Kalender- vs. ISO-Jahr-Semantik nennt.
    - Vier Unit-Tests (`sd_year_after_create_mid_year`, `_new_year_calendar_vs_iso`, `_silvester`, `_invalid_returns_none`) sind grün; Test 2 dokumentiert `2027-01-01 → Some(2027)` UND `parse_date_to_iso_parts("2027-01-01").unwrap().0 == 2026`.
    - Der Success-Handler-Zweig (`Ok(_) =>`) im Create-Pfad ruft `sd_year_after_create(&date_s).unwrap_or(iso_year)` statt `iso_year` direkt.
    - `cargo build --target wasm32-unknown-unknown` grün.
    - `cargo clippy -p shifty-dioxus --lib -- -D warnings` grün.
    - Grep-Guard: `grep -n "sd_year.set(iso_year)" shifty-dioxus/src/page/settings.rs` liefert 0 Treffer (die alte Zeile ist ersetzt).
  </done>
  <acceptance_criteria>
    - SDF-03 Success-Kriterium erfüllt: 1.1.-Grenzfall verifiziert (`sd_year_after_create("2027-01-01") == Some(2027)` — Kalenderjahr, nicht ISO-Woche-53-von-2026).
    - Backward-Compat: Für Mid-Year-Datumsangaben (z. B. 2026-08-15) liefert die neue fn dasselbe Jahr wie `parse_date_to_iso_parts(…).unwrap().0` — der Fix ändert nur das Verhalten am Jahreswechsel-Grenzfall.
    - Keine Regressions in existierenden Tests: `cargo test -p shifty-dioxus --lib settings` grün.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SDF-04 — Duplikat-Hinweis-Copy in de/en/cs auf Replace-Semantik umformulieren + Anti-Wording-Test</name>
  <files>shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs, shifty-dioxus/src/page/settings.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/de.rs (Zeilen 1190-1200: aktuelle DE-Registration von `Key::SettingsSpecialDaysDuplicateHint` — Text: "An diesem Tag ist bereits ein Sondertag eingetragen.")
    - shifty-dioxus/src/i18n/en.rs (Zeilen 1105-1115: EN-Registration — "A special day already exists for this date.")
    - shifty-dioxus/src/i18n/cs.rs (Zeilen 1180-1195: CS-Registration — "Pro toto datum již existuje zvláštní den.")
    - shifty-dioxus/src/i18n/mod.rs (Zeile 678: `Key::SettingsSpecialDaysDuplicateHint` Deklaration)
    - shifty-dioxus/src/page/settings.rs (Zeilen 880-890: Verwendungsstelle Row D, gerendert via `should_show_duplicate_hint`)
  </read_first>
  <action>
    Umsetzung SDF-04 in drei i18n-Dateien plus ein Presence/Anti-Wording-Test in `settings.rs`:

    1. **DE-Copy** (`shifty-dioxus/src/i18n/de.rs`, ~ Zeile 1195): `SettingsSpecialDaysDuplicateHint` von `"An diesem Tag ist bereits ein Sondertag eingetragen."` auf **Replace-taugliches Wording** umschreiben, das signalisiert: „der bestehende Eintrag wird beim Anlegen atomar überschrieben, kein Fehler". Vorschlag (Claude's Discretion, DE ist die Referenz-Locale): `"An diesem Tag ist bereits ein Sondertag eingetragen — er wird beim Anlegen ersetzt."` Der Text muss die Blocking-Substrings `"bereits ein Sondertag eingetragen."` NICHT als Satzende stehen lassen — er muss klarstellen, dass „Anlegen" die Ersetzung auslöst.
    2. **EN-Copy** (`shifty-dioxus/src/i18n/en.rs`, ~ Zeile 1108): analog von `"A special day already exists for this date."` auf z. B. `"A special day is already set for this date — creating will replace it."`
    3. **CS-Copy** (`shifty-dioxus/src/i18n/cs.rs`, ~ Zeile 1185): analog von `"Pro toto datum již existuje zvláštní den."` auf z. B. `"Pro toto datum již existuje zvláštní den — vytvořením bude nahrazen."`
    4. **Anti-Wording-Test** in `shifty-dioxus/src/page/settings.rs` im `#[cfg(test)] mod tests`-Block, neue Funktion `duplicate_hint_copy_signals_replace_semantics()`. Die fn baut in-process ein `crate::i18n::I18n` (analog Muster aus anderen HYG/i18n-Presence-Tests des Repos — bei Bedarf `crate::i18n::i18n::new()`/Init-Helper suchen; falls kein Helper existiert, direkt die drei Locale-Init-Fns rufen: `crate::i18n::de::init(&mut i18n); crate::i18n::en::init(&mut i18n); crate::i18n::cs::init(&mut i18n);`). Danach für JEDE der drei Locales `let text = i18n.t_locale(Locale::De, Key::SettingsSpecialDaysDuplicateHint);` (bzw. `.t_with_locale` — die korrekte Signatur im Nachbar-Test in demselben Repo nachsehen; keine neue API erfinden). Assertions PRO LOCALE:
       * `!text.is_empty()` (Presence)
       * Die Blocking-Substrings sind KEINE Endung des Satzes und werden vom Replace-Cue neutralisiert. Konkreter Anti-Match: der Text MUSS mindestens einen der Replace-Cues enthalten:
         * de: `"ersetzt"` ODER `"überschrieben"`
         * en: `"replace"` (case-insensitive)
         * cs: `"nahrazen"` ODER `"přepsán"`
       * Der Text darf sich NICHT ausschließlich als Fehler lesen: darf keinen der reinen Blocker-Sätze mehr sein — als robuster Guard reicht die Positivbedingung „Replace-Cue enthalten".
    5. Nach den Edits `cd shifty-dioxus && cargo fmt`, dann Gates aus `<verify>`.

    (Die konkreten Endstrings darf Claude nach Bedarf feinjustieren, solange sie die drei Cues aus Punkt 4 enthalten und im Ton neutral bleiben. Referenz-Präzedenz IMP-05: „de-Referenz gewinnt" aus 43-CONTEXT.md `<specifics>` — DE-Formulierung ist Master, EN/CS ziehen den Ton nach.)
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test -p shifty-dioxus --lib duplicate_hint_copy_signals_replace_semantics && cargo build --target wasm32-unknown-unknown && cargo clippy -p shifty-dioxus --lib -- -D warnings</automated>
  </verify>
  <done>
    - Alle drei i18n-Dateien (de.rs, en.rs, cs.rs) haben den `SettingsSpecialDaysDuplicateHint`-Text auf Replace-Semantik umformuliert (Cue enthalten: „ersetzt"/„replace"/„nahrazen").
    - `duplicate_hint_copy_signals_replace_semantics` Unit-Test grün, prüft alle 3 Locales auf Nicht-Leere + Replace-Cue.
    - `cargo build --target wasm32-unknown-unknown` grün.
    - `cargo clippy -p shifty-dioxus --lib -- -D warnings` grün.
    - Grep-Verifikation: `grep -c "replace\|ersetzt\|nahrazen\|přepsán" shifty-dioxus/src/i18n/de.rs shifty-dioxus/src/i18n/en.rs shifty-dioxus/src/i18n/cs.rs` liefert ≥ 1 Treffer je Datei in der Nähe des `SettingsSpecialDaysDuplicateHint`-Blocks (Bestätigung, dass der neue Cue tatsächlich in jeder Locale steht).
  </done>
  <acceptance_criteria>
    - SDF-04 Roadmap-Success-Kriterium 2 erfüllt: i18n-Copy des Duplikat-Hinweises in de/en/cs entspricht dem Replace-Verhalten, kein „blockiert"/„already exists"-Endsatz mehr.
    - Presence-Test verifiziert alle drei Locales in einem Testlauf.
    - Anti-Wording: neuer Replace-Cue („ersetzt" / „replace" / „nahrazen") ist per Grep in jeder Datei nachweisbar.
    - Kein Key-Rename, keine neuen i18n-Keys — Backward-Compat mit `Key::SettingsSpecialDaysDuplicateHint`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→FE (Card-3 create-form) | User picked ein Datum + Type; Frontend validiert Form vor POST. |
| FE→backend (`POST /special-days`) | Bereits existent seit SDF-01. Kein neuer Boundary in diesem Plan (SDF-03 ändert nur einen Signal-Wert nach Success; SDF-04 ändert nur i18n-Copy). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Information Disclosure | i18n-Copy für Duplikat-Hinweis | low | accept | Der neue Text enthüllt keinerlei sensitives Detail — er beschreibt UI-Verhalten (Ersetzung), das dem User ohnehin sichtbar ist. Kein privileged-only Info-Leak. |
| T-43-02 | Tampering | `sd_year`-Signal-Wert | low | accept | Rein clientseitiger Wert; steuert nur, welches Jahr im Picker angezeigt wird. Kein Sicherheitseffekt — Backend liefert nur veröffentlichte Special-Days (`get_by_year` ist read-only + shiftplanner-gated bereits vorhanden). |

Kein `T-43-SC` — dieser Plan installiert keine Packages (nur Rust-Code-Edits + i18n-String-Edits, alle Deps bereits im Workspace).
</threat_model>

<verification>
## Phase-Level Gates (nach Task 1 + Task 2 zusammen)

```bash
cd shifty-dioxus
cargo test -p shifty-dioxus --lib
cargo build --target wasm32-unknown-unknown
cargo clippy -p shifty-dioxus --lib -- -D warnings

# Backend darf nicht kaputt sein (Cross-Workspace-Sicherheitscheck)
cd ..
cargo build --workspace
cargo test --workspace
cargo clippy --workspace -- -D warnings
```

Alle sechs Kommandos MÜSSEN grün sein.

## Regression-Gates (Grep-Guards)

```bash
# SDF-03: alter Zeilenkörper darf nicht mehr existieren
grep -n "sd_year.set(iso_year)" shifty-dioxus/src/page/settings.rs
# Erwartet: kein Treffer

# SDF-04: alter Blocking-Endsatz darf nicht mehr existieren (die drei alten Strings)
grep -c 'A special day already exists for this date' shifty-dioxus/src/i18n/en.rs
# Erwartet: 0
grep -c 'An diesem Tag ist bereits ein Sondertag eingetragen\.$' shifty-dioxus/src/i18n/de.rs
# Erwartet: 0
grep -c 'Pro toto datum již existuje zvláštní den\.$' shifty-dioxus/src/i18n/cs.rs
# Erwartet: 0
```
</verification>

<success_criteria>
- SDF-03: Der Post-Create Sprung von `sd_year` nutzt Kalenderjahr — pure fn + 4 Unit-Tests belegen den Grenzfall 2027-01-01.
- SDF-04: Alle drei Duplikat-Hinweis-Texte signalisieren Replace-Semantik (Cue-Presence-Test); kein Blocking-Endsatz mehr.
- WASM-Build + Backend-Clippy-Gate grün.
- Kein Snapshot-Bump, keine Migration, keine neuen Deps.
- Backend unberührt.
</success_criteria>

<output>
Nach Abschluss beider Tasks: `.planning/phases/43-special-days-feintuning/43-01-SUMMARY.md` schreiben (Muster aus Phase 42-01-SUMMARY.md wenn vorhanden; sonst `templates/summary.md`). Enthält:
- Was gefixt wurde (SDF-03 + SDF-04)
- Welche pure fns + Tests hinzukamen
- Grep-Verifikations-Output (sd_year.set + alte Blocking-Strings = 0)
- WASM-Build + Backend-Clippy-Gate-Status
</output>

## Artifacts this phase produces

**New pure functions (extraction pattern D-42-05):**

- `pub(crate) fn sd_year_after_create(date_str: &str) -> Option<u32>` in `shifty-dioxus/src/page/settings.rs`

**Modified i18n text (existing key, new content):**

- `Key::SettingsSpecialDaysDuplicateHint` — new copy in de/en/cs

**New unit tests (in `settings.rs` `#[cfg(test)] mod tests`):**

- `sd_year_after_create_mid_year`
- `sd_year_after_create_new_year_calendar_vs_iso`
- `sd_year_after_create_silvester`
- `sd_year_after_create_invalid_returns_none`
- `duplicate_hint_copy_signals_replace_semantics`

**Files modified (no new files):**

- `shifty-dioxus/src/page/settings.rs`
- `shifty-dioxus/src/i18n/de.rs`
- `shifty-dioxus/src/i18n/en.rs`
- `shifty-dioxus/src/i18n/cs.rs`
