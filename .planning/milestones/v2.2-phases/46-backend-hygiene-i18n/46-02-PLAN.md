---
phase: 46-backend-hygiene-i18n
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/page/shiftplan.rs
autonomous: true
requirements:
  - HYG-04
must_haves:
  truths:
    - "HYG-04: das Struktur-Umschalter-Label im Schichtplan-Dropdown ist in de/en/cs übersetzt — Deutsch zeigt nicht mehr das englische Literal `Edit structure`/`Normal mode` an"
    - "HYG-04: `New slot` im gleichen Dropdown (`shiftplan.rs:1171`) ist ebenfalls i18n-gebunden (Miterfassung, damit der Block frei von English-Only-Literalen ist)"
    - "HYG-04: die drei neuen Keys sind in allen drei Locales (De/En/Cs) mit nicht-leerer, nicht-`??` Copy gemappt"
    - "HYG-04: `shifty-dioxus/src/page/shiftplan.rs` liest die Labels via `i18n.t(Key::…)` statt via String-Literal"
  artifacts:
    - shifty-dioxus/src/i18n/mod.rs
    - shifty-dioxus/src/i18n/de.rs
    - shifty-dioxus/src/i18n/en.rs
    - shifty-dioxus/src/i18n/cs.rs
    - shifty-dioxus/src/page/shiftplan.rs
  key_links:
    - "`shiftplan.rs:1165` Ternary-Ausdruck ↔ neue Keys `ShiftplanNormalMode` / `ShiftplanEditStructure`"
    - "`shiftplan.rs:1171` String-Literal `New slot` ↔ neuer Key `ShiftplanNewSlot`"
    - "Copy-Herkunft: pre-existing englische Literale in shiftplan.rs sind das Reference-Wording für den En-Locale"
---

<objective>
HYG-04 — Der Schichtplaner-Dropdown im Schichtplan-Header (`shifty-dioxus/src/page/shiftplan.rs:1161-1186`) enthält
drei nicht i18n-gebundene englische Literale (`"Edit structure"`, `"Normal mode"`, `"New slot"`), sichtbar für alle
`is_shiftplanner`-Nutzer in allen Locales. Für die De-/Cs-Nutzer bricht das die durchgängige Lokalisierung.

Purpose: alle drei Literale werden durch i18n-Keys ersetzt; in `en.rs`/`de.rs`/`cs.rs` werden je drei
Übersetzungen ergänzt; der Dropdown rendert das lokalisierte Label. Kein Presence-Test (per CONTEXT.md HYG-04 Q2:
User explizit „brauchst du echt nicht testen"). Skope-treu: nur diese eine Dropdown-Region, kein Feld-für-Feld-Audit.

Output: eine Enum-Erweiterung (`Key`) in `mod.rs`, drei `add_text`-Blöcke in `en.rs`/`de.rs`/`cs.rs`, eine
kleine Region-Edit in `page/shiftplan.rs`.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-backend-hygiene-i18n/46-CONTEXT.md
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/i18n/de.rs
@shifty-dioxus/src/i18n/en.rs
@shifty-dioxus/src/i18n/cs.rs
@shifty-dioxus/src/page/shiftplan.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: HYG-04 — drei neue i18n-Keys für Schichtplan-Struktur-Dropdown anlegen (Enum + 3 Locales)</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <action>
    Drei neue Varianten am **Ende** des `Key`-Enums in `shifty-dioxus/src/i18n/mod.rs` (aktuell endet mit
    `WeekStatusChangeAriaLabel` bei Zeile ~709, `}` bei ~710) hinzufügen — Muster analog v2.1 (Doc-Kommentar
    `// Phase 46 (HYG-04): …` ist ok, aber kein Grep-Gate-Literal):

      * `ShiftplanEditStructure`
      * `ShiftplanNormalMode`
      * `ShiftplanNewSlot`

    Danach in jedem der drei Locale-Files am Ende der letzten Phase-33/39-Sektion drei `add_text`-Aufrufe
    ergänzen (Muster wie in `de.rs:1148-1156` / `en.rs:1071-1073` / `cs.rs:1144-1146`), Copy per Locale:

      * En (übernommen aus pre-existing Literalen in `shiftplan.rs`):
        - `ShiftplanEditStructure` → `"Edit structure"`
        - `ShiftplanNormalMode` → `"Normal mode"`
        - `ShiftplanNewSlot` → `"New slot"`
      * De (neue Übersetzungen im Ton des bestehenden Domain-Vokabulars — `Struktur bearbeiten` ist bereits
        die etablierte Domain-Bezeichnung für den structure-mode; `Normalansicht` ist Domain-üblich für den
        Wechsel „Bearbeitungsmodus → normale Anzeige"; `Neuer Slot` folgt der bestehenden Naming-Konvention
        für Slot-Aktionen):
        - `ShiftplanEditStructure` → `"Struktur bearbeiten"`
        - `ShiftplanNormalMode` → `"Normalansicht"`
        - `ShiftplanNewSlot` → `"Neuer Slot"`
      * Cs (im Ton der bestehenden Domain-Copy in `cs.rs`, z.B. Analogon zu `SettingsSpecialDaysSectionLabel`;
        „upravit strukturu" ist die etablierte Cs-Domain-Phrase für structure-edit, „normální zobrazení" für
        den Rückschalter, „nový slot" für Slot-Neuanlage):
        - `ShiftplanEditStructure` → `"Upravit strukturu"`
        - `ShiftplanNormalMode` → `"Normální zobrazení"`
        - `ShiftplanNewSlot` → `"Nový slot"`

    Alle drei Locale-Files bekommen dieselben drei Keys — sonst rennt der Test
    `i18n_no_missing_keys_across_locales` (Presence-Guard in `mod.rs`, siehe Muster ab Zeile 1500) rot.
    Enum-Variante MUSS in allen drei `add_text`-Files gemappt sein — vor Commit per
    `grep -c "add_text(Locale::De, Key::ShiftplanEditStructure" shifty-dioxus/src/i18n/de.rs` etc.
    prüfen (Erwartung: 1/1/1).
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo build -p shifty-dioxus 2>&amp;1 | tee /tmp/hyg04-build.log &amp;&amp; grep -c 'warning' /tmp/hyg04-build.log | grep -qE '^0$'</automated>
    <automated>grep -c 'ShiftplanEditStructure\|ShiftplanNormalMode\|ShiftplanNewSlot' shifty-dioxus/src/i18n/mod.rs</automated>
    <automated>grep -c 'add_text(Locale::De, Key::Shiftplan\(EditStructure\|NormalMode\|NewSlot\)' shifty-dioxus/src/i18n/de.rs</automated>
    <automated>grep -c 'add_text(Locale::En, Key::Shiftplan\(EditStructure\|NormalMode\|NewSlot\)' shifty-dioxus/src/i18n/en.rs</automated>
    <automated>grep -c 'add_text(Locale::Cs, Key::Shiftplan\(EditStructure\|NormalMode\|NewSlot\)' shifty-dioxus/src/i18n/cs.rs</automated>
  </verify>
  <done>
    `Key` enthält die drei neuen Varianten; jedes Locale-File mappt sie mit nicht-leerer Copy;
    `cargo build -p shifty-dioxus` weiterhin warnungsfrei (Baseline aus Phase 45 D-45-01: 0 warnings).
  </done>
</task>

<task type="auto">
  <name>Task 2: HYG-04 — Dropdown in `page/shiftplan.rs` auf neue Keys umstellen + Full-Gate</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <action>
    Die drei Literale im `DropdownTrigger`-Block (`shifty-dioxus/src/page/shiftplan.rs` Zeilen 1163-1178,
    innerhalb des `if is_shiftplanner` Guards) ersetzen. Vor Ort ist `i18n` im scope (bereits genutzt für
    `i18n.t(Key::BookingLogTitle)` in derselben Region Zeile 1155-1157 — siehe Read der Datei), also der
    gleiche Aufruf-Pattern gilt.

    Konkret in dem Ternary-Ausdruck der ersten Tuple:
      * die Literalseite `"Normal mode"` wird zu `i18n.t(Key::ShiftplanNormalMode).as_ref()`
      * die Literalseite `"Edit structure"` wird zu `i18n.t(Key::ShiftplanEditStructure).as_ref()`
      * in der zweiten Tuple wird das Literal `"New slot"` zu `i18n.t(Key::ShiftplanNewSlot).as_ref()`

    Der Typ, den `DropdownTrigger`/`(String, Box<Fn…>, bool).into()` erwartet, kann `&str` verlangen — falls
    der Compiler `as_ref()` nicht akzeptiert (weil `.into()` einen owned `String` nimmt), stattdessen
    `i18n.t(Key::…).to_string()` verwenden. Genau eine der beiden Varianten wählt der Executor entsprechend
    der Fehlermeldung von `cargo check -p shifty-dioxus`. Kein weiterer Umbau der Dropdown-Struktur — nur die
    drei Textwerte tauschen.

    Anschließend Full-Gate: FE-Build warnungsfrei, FE-Clippy `-D warnings` grün, FE-Test-Suite grün,
    WASM-Build grün.
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo check -p shifty-dioxus</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo test -p shifty-dioxus</automated>
    <automated>grep -c 'ShiftplanEditStructure\|ShiftplanNormalMode\|ShiftplanNewSlot' shifty-dioxus/src/page/shiftplan.rs</automated>
    <automated>grep -vE '^\s*//' shifty-dioxus/src/page/shiftplan.rs | grep -cE '"Edit structure"|"Normal mode"|"New slot"'</automated>
  </verify>
  <done>
    `page/shiftplan.rs` referenziert die drei neuen Keys statt der englischen Literale (letzter Grep-Gate
    liefert `0`, den Kommentar-Filter beachtet). Alle vier FE-Gates grün.
  </done>
</task>

</tasks>

<verification>
- Enum-Erweiterung + drei Locale-Mappings + Call-Site-Umstellung committet.
- FE-WASM-Build + FE-Clippy `-D warnings` + FE-Test-Suite grün.
- Manuelle Sichtprüfung: `grep "Edit structure\|Normal mode\|New slot" shifty-dioxus/src/` liefert nur noch
  Test- oder Doc-Referenzen (falls überhaupt) — nicht mehr als produktives Literal in der Dropdown-Region.
</verification>

<success_criteria>
1. „Edit structure" / „Normal mode" / „New slot" werden im Schichtplan-Dropdown nicht mehr als hartcodiertes
   englisches Literal gerendert; stattdessen kommt der lokalisierte Wert aus `i18n.t(Key::…)`.
2. De-Nutzer sehen „Struktur bearbeiten" / „Normalansicht" / „Neuer Slot"; Cs-Nutzer sehen
   „Upravit strukturu" / „Normální zobrazení" / „Nový slot"; En-Nutzer sehen weiterhin die Original-Copy.
3. FE-WASM-Build + FE-Clippy `-D warnings` + FE-Test-Suite bleiben grün.
</success_criteria>

<output>
Create `.planning/phases/46-backend-hygiene-i18n/46-02-SUMMARY.md` when done.
</output>
