---
phase: 46-backend-hygiene-i18n
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
autonomous: true
requirements:
  - IMP-05
must_haves:
  truths:
    - "IMP-05: `cargo test -p shifty-dioxus i18n_impersonation_keys_match_german_reference` grün, Test-Referenz auf shipped 🥸-De-Copy angepasst"
    - "IMP-05: pre-existing Presence-Test `i18n_impersonation_keys_present_in_all_locales` bleibt grün (keine Regression)"
    - "IMP-05: pre-existing Placeholder-Test `i18n_impersonation_banner_carries_user_placeholder` bleibt grün (`{user}`-Marker weiter enthalten)"
    - "IMP-05: `shifty-dioxus/src/i18n/de.rs` bleibt byte-genau unverändert (kanonische Copy)"
  artifacts:
    - shifty-dioxus/src/i18n/mod.rs
  key_links:
    - "Test-Assertion ↔ `de.rs:1154-1156` shipped Copy (`🥸 Agieren` / `Du agierst als {user}.` / `Impersonation beenden`)"
    - "Test-Präzedenz Phase 37-02 `i18n_contract_help_keys_match_german_reference` (mod.rs:1600 ff.) — gleiche Struktur"
---

<objective>
IMP-05 — Der pre-v2.1 Carry-over-Test `i18n_impersonation_keys_match_german_reference`
(`shifty-dioxus/src/i18n/mod.rs:1578`) ist rot, weil er gegen eine idealisierte
Prosaform assertet (`"Als diese Person agieren"`), während die shipped De-Copy in
`de.rs:1154` die kompakte 🥸-Form (`"🥸 Agieren"`) verwendet. Die kanonische
Entscheidung (CONTEXT.md IMP-05, Q1) ist getroffen: **Test anpassen an de.rs**,
Copy nicht ändern.

Purpose: pre-existing Carry-over-Todo `2026-07-02-i18n-impersonation-key-test-mismatch`
abhaken; Test-Referenz und UI-sichtbare Copy sind wieder konsistent; das i18n-Copy-
Pinning-Muster (Phase 32 / Phase 37-02) bleibt funktional erhalten.

Output: eine Datei modifiziert — `shifty-dioxus/src/i18n/mod.rs`, nur der
`i18n_impersonation_keys_match_german_reference`-Test-Body (Zeile ~1578-1593).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-backend-hygiene-i18n/46-CONTEXT.md
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/i18n/de.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: IMP-05 — Test-Referenz auf shipped 🥸-De-Copy angleichen (RED → GREEN)</name>
  <files>shifty-dioxus/src/i18n/mod.rs</files>
  <behavior>
    - RED: Vor der Änderung `cargo test -p shifty-dioxus i18n_impersonation_keys_match_german_reference` laufen lassen und die Failure verifizieren (Ist-Zustand: assert_eq!-Panik mit erwartet "Als diese Person agieren" vs. tatsächlich "🥸 Agieren"). Failure-Ausgabe kurz im SUMMARY erwähnen.
    - GREEN: die drei `assert_eq!`-Erwartungen im Test-Body (mod.rs Zeile ~1578-1593) auf die shipped De-Strings aus `de.rs:1154-1156` umstellen — per IMP-05 D-CONTEXT-Q1 (Copy in de.rs ist kanonisch):
        * `Key::ImpersonateActAs` erwartet: `"🥸 Agieren"` (de.rs:1154)
        * `Key::ImpersonateBanner` erwartet: `"Du agierst als {user}."` (de.rs:1155)
        * `Key::ImpersonateStop` erwartet: `"Impersonation beenden"` (de.rs:1156)
    - **Vor dem Schreiben** die drei Strings mit `Read` bzw. `Grep` aus `de.rs` byte-genau übernehmen (Emoji + Punkt am Bannerende). Nicht aus dem Gedächtnis rekonstruieren.
    - Der Kommentar „Pitfall-2 guard: pins the De copy so a locale-swap bug fails fast." bleibt inhaltlich; er darf um einen Hinweis auf die Copy-Entscheidung ergänzt werden (z.B. „// Phase 46 (IMP-05): pins the shipped 🥸-De-Copy; test aligned to de.rs, not vice versa.") — Prosaform, keine wortwörtliche Wiederholung eines Grep-Gate-Literals.
    - `de.rs` NICHT modifizieren (Constraint IMP-05 D-CONTEXT-Q1).
    - Keine anderen Tests in `mod.rs` anfassen (Presence-Test 1538 + Placeholder-Test 1562 + Contract-Help-Test 1600 bleiben unverändert).
  </behavior>
  <action>
    Zuerst RED verifizieren: `cargo test -p shifty-dioxus i18n_impersonation_keys_match_german_reference` — die aktuelle Failure sichtbar machen (Assert-Panik). Danach die drei `assert_eq!`-Vergleichsstrings im Test-Body auf die shipped Copy anpassen. Umsetzung per `Edit` (scoped) auf `shifty-dioxus/src/i18n/mod.rs`; Suchstring ist der aktuelle Prosa-Assertion-Block, Ersatz sind die drei 🥸-Strings byte-genau aus `de.rs`. Anschließend GREEN verifizieren: derselbe Test muss grün werden, und die zwei benachbarten Impersonation-Tests dürfen nicht in Regression laufen. `de.rs` NICHT anfassen — CONTEXT-Constraint.
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo test -p shifty-dioxus i18n_impersonation_keys_match_german_reference -- --exact</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo test -p shifty-dioxus i18n_impersonation -- --nocapture</automated>
  </verify>
  <done>
    `i18n_impersonation_keys_match_german_reference` grün. `i18n_impersonation_keys_present_in_all_locales` bleibt grün. `i18n_impersonation_banner_carries_user_placeholder` bleibt grün. Kein Diff auf `shifty-dioxus/src/i18n/de.rs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-Gate — cargo build + cargo clippy -D warnings + cargo test (shifty-dioxus)</name>
  <files>shifty-dioxus/</files>
  <action>
    Volle Frontend-Gates fahren, um sicherzustellen, dass die Test-Edit keine Nebeneffekte hat. WASM-Build-Gate + FE-Clippy-Gate (D-45-01 scharf, `-D warnings`) + kompletter FE-Test-Lauf. Ausgeführt aus dem `shifty-dioxus/`-Workspace-Root (eigener Cargo-Workspace). Backend-Gates sind für diesen Plan nicht relevant (Backend-Code unberührt).
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo test -p shifty-dioxus</automated>
  </verify>
  <done>
    Alle drei Gates grün. Ein IMP-05-Test-Fix darf keine Regression in FE-Warnings, WASM-Build oder anderen FE-Tests auslösen.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p shifty-dioxus i18n_impersonation_keys_match_german_reference` grün (IMP-05 primär).
- Alle drei FE-Gates grün (WASM + Clippy -D warnings + Test-Suite).
- `git diff shifty-dioxus/src/i18n/de.rs` leer (Constraint IMP-05).
- Verbleibendes Diff nur in `shifty-dioxus/src/i18n/mod.rs` und ausschließlich im `i18n_impersonation_keys_match_german_reference`-Test-Body.
</verification>

<success_criteria>
1. Der bisher rote Test `i18n_impersonation_keys_match_german_reference` ist grün — Test-Referenz stimmt mit shipped De-Copy überein.
2. `shifty-dioxus/src/i18n/de.rs` ist unverändert (kanonische Copy respektiert, IMP-05 D-Q1).
3. FE-WASM-Build + FE-Clippy `-D warnings` + FE-Test-Suite bleiben grün.
4. Pre-existing Impersonation-Nachbartests (Presence + Placeholder) bleiben grün.
</success_criteria>

<output>
Create `.planning/phases/46-backend-hygiene-i18n/46-01-SUMMARY.md` when done.
</output>
