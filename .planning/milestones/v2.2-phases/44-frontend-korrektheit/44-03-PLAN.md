---
phase: 44-frontend-korrektheit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/component/dialog.rs
autonomous: true
requirements:
  - BUG-03
must_haves:
  truths:
    - "BUG-03: Alle Modal-artigen Komponenten in `shifty-dioxus/src/component/`, die den Backdrop-Close-Pfad nutzen, gehen entweder über die zentrale `Dialog`-Shell (die intern `BackdropPress` nutzt) oder implementieren `BackdropPress` inline — Grep-Verifikation ist grün."
    - "BUG-03: Ein durable Grep-Invariant-Test (`#[test]` mit `include_str!`/`std::fs::read_dir`-Scan) in `component/dialog.rs` würde einen zukünftigen Regress (neuer Modal mit `fixed inset-0`-Backdrop + naivem `onclick`-Close ohne `BackdropPress`) beim Test-Lauf sofort rot färben."
    - "BUG-03: Modal-Files ohne Backdrop-Close-Pfad (reine In-Line-Karten, dropdown-Menüs, Tooltip-Timing-Handler) sind explizit vom Invariant ausgenommen mit einem Kommentar oder einer Allowlist im Test — keine False Positives."
  artifacts:
    - shifty-dioxus/src/component/dialog.rs (neuer `#[cfg(test)] mod backdrop_invariant`-Modul mit Grep-Scan)
  key_links:
    - "Dialog-Shell (`component/dialog.rs`) + inline `absence_convert_modal.rs` sind die einzigen zwei Backdrop-Close-Pfade in der Codebase — der Invariant-Test wacht darüber, dass es keine dritte, un-migrierte Instanz gibt."
    - "Phase 37 MOD-01 lieferte die zentrale `BackdropPress`-Logik; dieser Plan ist die Absicherung der Vollständigkeit."
---

<objective>
BUG-03 (v2.2) abschließen: laut Phase-44-Success-Criterion 3 müssen ALLE
Dialoge/Modals unterhalb `component/`, die den Backdrop-Close-Pfad nutzen,
über `BackdropPress` gehen (drag-safe — Panel-Drag mit mouseup außerhalb
schließt das Modal nicht mehr). Der Ist-Zustand-Audit (2026-07-02) zeigt:

  - `component/dialog.rs::Dialog`: nutzt `BackdropPress` zentral (Zeile 239 ff.).
    ContractModal, ExtraHoursModal, SlotEdit → nutzen `Dialog` als Shell → sind
    damit implizit migriert.
  - `component/absence_convert_modal.rs::AbsenceConvertModal`: nutzt inline
    `BackdropPress` (Zeile 90–107).
  - `component/week_view.rs`: `onmousedown`-Handler existieren, betreffen
    aber Tooltip-Timing auf Slot-Zellen (Zeile 145, 867) — KEIN Backdrop-
    Close-Pfad, out of scope.
  - `component/overlay.rs::Overlay`: reines `fixed inset-0`-Wrapper-Element
    ohne Close-Handler, kein Backdrop-Close-Verhalten, nicht verwendet als
    Modal-Backdrop. Out of scope.

Aktuell ist die Codebase also BEREITS in dem Zustand, den BUG-03 fordert.
Was fehlt: ein durable Grep-Invariant-Test, der genau dieses Ergebnis
zukunftssicher fixiert — ein neuer Modal mit `fixed inset-0`-Backdrop und
naivem `onclick: on_close.call(())`-Handler (ohne `BackdropPress`-Guard)
würde sonst wieder das MOD-01-Panel-Drag-Bug einschleppen.

Purpose: Grep-Verifikation als executor-lesbares Gate + durable Regression-Guard.
Output: `#[cfg(test)] mod backdrop_invariant` in `component/dialog.rs` + Audit-Notiz.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-frontend-korrektheit/44-CONTEXT.md
@shifty-dioxus/src/component/dialog.rs
@shifty-dioxus/src/component/absence_convert_modal.rs
@shifty-dioxus/src/component/overlay.rs
@shifty-dioxus/src/component/week_view.rs
@shifty-dioxus/src/component/extra_hours_modal.rs
@shifty-dioxus/src/component/contract_modal.rs
@shifty-dioxus/src/component/slot_edit.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codebase-Audit + Backdrop-Invariant-Test in dialog.rs</name>
  <files>shifty-dioxus/src/component/dialog.rs</files>
  <action>
    Schritt 1 — Live-Audit ausführen (executor tut das als erstes und schreibt die Ergebnisse in die SUMMARY):

    Kommandos:
      a) `grep -rln "fixed inset-0" shifty-dioxus/src/component/*.rs shifty-dioxus/src/page/*.rs`
      b) `grep -rln "BackdropPress" shifty-dioxus/src/`
      c) Für jede in (a) gefundene Datei: manuelle Sichtprüfung, ob es ein Backdrop-Close-Pfad ist (Div mit `onclick` auf `on_close` oder analoger Handler). Wenn ja UND `BackdropPress` fehlt → BUG. Wenn ja UND `BackdropPress` da → OK. Wenn nein (reines Wrapper-Element, kein Close-Handler) → out-of-scope-Notiz in SUMMARY.

    Erwartetes Ergebnis (Baseline gemäß Ist-Audit 2026-07-02):
      - `component/dialog.rs` — OK (BackdropPress in Dialog::rsx).
      - `component/absence_convert_modal.rs` — OK (inline BackdropPress).
      - `component/overlay.rs` — Out of scope: Wrapper ohne Handler.
      - Keine weiteren Treffer in `page/` (bestätigt via `grep -rln "fixed inset-0" shifty-dioxus/src/page/`).

    Wenn der Audit einen abweichenden Zustand ergibt (z.B. ein neuer Modal ist seit dem 2026-07-02-Snapshot dazugekommen und nutzt `BackdropPress` noch nicht): (a) migriere ihn nach dem Muster von `absence_convert_modal.rs` (backdrop `onmousedown` → `press_backdrop`, backdrop `onclick` → `if release() { on_close }`, Panel `onmousedown` → `stop_propagation` + `press_panel`, Panel `onclick` → `stop_propagation`); (b) dokumentiere die Migration in der SUMMARY. Andernfalls fahre direkt mit Schritt 2 fort.

    Schritt 2 — Durable Invariant-Test in `shifty-dioxus/src/component/dialog.rs`:

    Ans Datei-Ende (unterhalb des bestehenden `#[cfg(test)] mod tests`-Blocks für `BackdropPress`) einen NEUEN Modul-Block hängen:

    ```rust
    #[cfg(test)]
    mod backdrop_invariant {
        //! BUG-03 (v2.2 Phase 44) — Grep-Invariant.
        //!
        //! Jede Modal-artige Komponente unter `shifty-dioxus/src/component/`,
        //! die einen Backdrop-Close-Pfad implementiert (div mit `fixed inset-0`
        //! + onclick, das `on_close` / `on_cancel` triggert), MUSS entweder
        //! die zentrale `Dialog`-Shell nutzen oder inline `BackdropPress`
        //! verwenden. Sonst kehrt das Phase-37 MOD-01 Panel-Drag-Bug zurück
        //! (Text-Selektion im Panel schließt das Modal beim Loslassen
        //! außerhalb).
        //!
        //! Der Test scannt zur Compile-Zeit die relevanten Component-Files
        //! via `include_str!` (keine `std::fs`-Runtime-Abhängigkeit — WASM-
        //! Test-Runner freundlich) und stellt sicher, dass die Menge der
        //! Files mit `fixed inset-0` == der Menge {dialog.rs, absence_convert_modal.rs}
        //! ∪ jede Datei, die explizit in `ALLOWED_NON_MODAL` steht (nur
        //! Wrapper wie overlay.rs).

        // Kompilezeit-eingebundene Component-Dateien.  Dieser Test läuft
        // absichtlich auf einem festen Set — jede NEUE Component-Datei
        // muss hier eingetragen werden, was ein Sichtbarkeits-Signal ist:
        // Ein neues Modal ohne BackdropPress-Adopt-Entscheidung fällt sofort
        // durch, sobald der Autor das Test-Set aktualisieren muss.
        const FILES: &[(&str, &str)] = &[
            ("dialog.rs", include_str!("dialog.rs")),
            ("absence_convert_modal.rs", include_str!("absence_convert_modal.rs")),
            ("contract_modal.rs", include_str!("contract_modal.rs")),
            ("extra_hours_modal.rs", include_str!("extra_hours_modal.rs")),
            ("slot_edit.rs", include_str!("slot_edit.rs")),
            ("overlay.rs", include_str!("overlay.rs")),
            ("week_view.rs", include_str!("week_view.rs")),
            ("day_aggregate_view.rs", include_str!("day_aggregate_view.rs")),
            ("employee_view.rs", include_str!("employee_view.rs")),
        ];

        /// Dateien mit `fixed inset-0` die KEIN Backdrop-Close-Pfad sind
        /// (reine Wrapper / kein Close-Handler auf dem Backdrop).
        const ALLOWED_NON_MODAL: &[&str] = &[
            "overlay.rs",           // Wrapper-Element ohne Close-Handler
        ];

        /// Dateien, die als Backdrop-Close-Pfad-Träger anerkannt sind und
        /// nachweislich `BackdropPress` verwenden.
        const KNOWN_MIGRATED: &[&str] = &[
            "dialog.rs",
            "absence_convert_modal.rs",
        ];

        #[test]
        fn every_backdrop_close_path_uses_backdrop_press() {
            for (name, body) in FILES {
                let has_backdrop_marker = body.contains("fixed inset-0");
                if !has_backdrop_marker {
                    continue;
                }
                if ALLOWED_NON_MODAL.contains(name) {
                    continue;
                }
                assert!(
                    KNOWN_MIGRATED.contains(name),
                    "BUG-03-Invariant verletzt: {name} enthält `fixed inset-0` \
                     ist aber weder als ALLOWED_NON_MODAL noch als \
                     KNOWN_MIGRATED gelistet — bitte auf BackdropPress \
                     migrieren (siehe absence_convert_modal.rs als Vorlage) \
                     UND in KNOWN_MIGRATED aufnehmen.",
                    name = name,
                );
                assert!(
                    body.contains("BackdropPress"),
                    "BUG-03-Invariant verletzt: {name} ist als migriert \
                     gelistet, verwendet aber `BackdropPress` nicht — Regress \
                     zum Phase-37 Panel-Drag-Bug.",
                    name = name,
                );
            }
        }

        #[test]
        fn known_migrated_files_actually_contain_backdrop_press() {
            for (name, body) in FILES {
                if KNOWN_MIGRATED.contains(name) {
                    assert!(
                        body.contains("BackdropPress"),
                        "{name} ist als migriert markiert, enthält aber \
                         `BackdropPress` nicht.",
                    );
                }
            }
        }
    }
    ```

    Schritt 3 — `cargo test -p shifty-dioxus` einmal laufen lassen und beide neuen Tests als grün bestätigen.

    Wichtige Klarstellung: Der Test fängt bewusst NICHT jeden `onclick`-Handler in der Codebase ein — das würde false-positive auf allen Buttons feuern. Er fixiert stattdessen die **kleinste** durable Invariante: "Jede Datei mit `fixed inset-0` ist entweder explizit als kein-Modal-Wrapper freigegeben oder muss `BackdropPress` enthalten." Der `FILES`-Konstant-Set zwingt den Autor eines neuen Modals, den Test zu aktualisieren — was das Review-Auge trainiert.

    Discipline-Notiz: die Literale `"fixed inset-0"` und `"BackdropPress"` sind bewusst als Rust-`&str` im Code — keine Gefahr, dass ein anderer Gate sie als "verbotene Substring in Kommentaren"-Fund gegen den Plan verwendet, weil sie hier als Programmzustand geprüft werden, nicht als Comment-Text.
  </action>
  <verify>
    <automated>cd shifty-dioxus &amp;&amp; cargo test -p shifty-dioxus --lib component::dialog::backdrop_invariant 2&gt;&amp;1 | tail -20</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo test -p shifty-dioxus 2&gt;&amp;1 | tail -10</automated>
    <automated>cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown 2&gt;&amp;1 | tail -10</automated>
    <human-check>
      Nur informativ (nicht-blockierend): Manuelle Panel-Drag-Sanity in einem Modal deiner Wahl (z.B. ContractModal öffnen, Text im Panel markieren, mouseup außerhalb im Backdrop-Bereich loslassen → Modal darf NICHT schließen). Wird bei einem regulären Live-Smoke der v2.2-Phasen ohnehin mit durchgespielt.
    </human-check>
  </verify>
  <done>
    - `component/dialog.rs` enthält Modul `backdrop_invariant` mit zwei `#[test]`-Funktionen.
    - Beide Tests grün beim `cargo test -p shifty-dioxus`-Lauf.
    - `FILES`-Set listet alle heutigen Component-Files, die relevant sein könnten (inkl. `overlay.rs` als ALLOWED_NON_MODAL).
    - `ALLOWED_NON_MODAL = ["overlay.rs"]`, `KNOWN_MIGRATED = ["dialog.rs", "absence_convert_modal.rs"]` — Baseline entspricht Ist-Audit.
    - Sollte im Audit ein weiterer un-migrierter Modal aufgetaucht sein, ist dieser (a) migriert und (b) in `KNOWN_MIGRATED` ergänzt worden.
    - `cargo build --target wasm32-unknown-unknown` grün.
  </done>
</task>

</tasks>

<threat_model>
Security-relevant threats: keine — reine Test-Datei + optionale Migration.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | Kein Trust-Boundary betroffen. Reine Client-side UX-Regression-Guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-03-Regression-1 | Tampering (Regressions-Schutz) | Component-Modals | low | mitigate | Durable Grep-Invariant-Test wacht darüber, dass zukünftige Modals das MOD-01 Panel-Drag-Bug nicht erneut einschleppen. |
</threat_model>

<verification>
Die zwei automated Tests im backdrop_invariant-Modul fixieren die Grep-
Verifikation permanent im Test-Lauf. Manuelle Live-Panel-Drag-Verifikation
ist D-25-06-Klasse (browser-e2e nicht deterministisch triggerbar) und wird
akzeptiert als optionaler Live-Smoke — nicht blockierend.
</verification>

<success_criteria>
1. Grep-verifiziert: alle Modal-artigen Components mit Backdrop-Close-Pfad nutzen `BackdropPress` (BUG-03 Success Criterion 3, „verifiziert per Grep").
2. Durable Invariant-Test in `component/dialog.rs` läuft grün und würde einen zukünftigen Regress rot färben.
3. `cargo build --target wasm32-unknown-unknown` + `cargo test -p shifty-dioxus` grün.
</success_criteria>

<output>
Erstelle `.planning/phases/44-frontend-korrektheit/44-03-SUMMARY.md` mit dem Audit-Ergebnis (welche Files `fixed inset-0` enthalten und wie sie klassifiziert wurden), dem neuen Testmodul-Diff, Test-Zählung, Verify-Ergebnis und ggf. der Notiz, ob eine Zusatz-Migration nötig war (Baseline: nein).
</output>
