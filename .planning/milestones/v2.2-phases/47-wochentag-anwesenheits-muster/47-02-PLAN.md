---
phase: 47-wochentag-anwesenheits-muster
plan: 2
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/component/employee_view.rs
autonomous: true
requirements: [RPT-02, RPT-03]

must_haves:
  truths:
    - "In the HR-stats block of the employee report, the v2.1 line labelled `Ø Std/Anwesenheitstag` is gone and a single new weekday-distribution line appears at the same slot (RPT-02)"
    - "The weekday line renders as `Mo: 8 (80%) · Di: 3 (30%) · Mi: 7 (70%) · Do: 5 (50%) · Fr: 2 (20%) · Sa: 0 (0%) · So: 0 (0%)` with 7 comma-separated segments joined by `·` (RPT-02 layout per D-47-CONTEXT)"
    - "When `counted_calendar_weeks < 1`, the row shows a single localized placeholder text instead of the weekday segments (RPT-02 empty-state)"
    - "Row is only rendered when `attendance_statistics.is_some()` (preserves v2.1 D-AVG-05 visibility gate)"
    - "The row carries a localized tooltip on hover (RPT-03 tooltip presence)"
    - "i18n de/en/cs have the seven weekday-short keys + one tooltip key + one empty-state key — presence-tested (RPT-03)"
  artifacts:
    - "i18n::Key::WeekdayShortMon..WeekdayShortSun (7 new keys)"
    - "i18n::Key::WeekdayAttendanceTooltip (1 new key)"
    - "i18n::Key::WeekdayAttendanceEmpty (1 new key)"
    - "Deleted i18n::Key::AvgHoursPerAttendanceDay / AvgHoursPerAttendanceDayDescription / AvgHoursPerAttendanceDayEmpty (3 keys removed with their de/en/cs bodies)"
    - "Pure formatter `format_weekday_attendance_line(stats: &EmployeeAttendanceStatisticsTO, i18n: &I18nAccess) -> String` in `component/employee_view.rs` (unit-testable, no dioxus state)"
    - "SSR test module `weekday_attendance_row_ssr_tests` inside `component/employee_view.rs` covering non-empty, empty-state, absent-row"
  key_links:
    - "rest_types::EmployeeAttendanceStatisticsTO (new shape from 47-01) drives the FE render"
    - "The formatter helper receives the whole DTO and calls i18n.t(Key::WeekdayShortMon..Sun) directly"
    - "Weekday order is preserved from the backend (Mon..Sun); FE does NOT reorder"
---

<objective>
Complete RPT-02 (FE rendering swap) and RPT-03 (FE i18n): remove the v2.1 `Ø Std/Anwesenheitstag` line from the HR-stats block of `component/employee_view.rs`, replace it in-place with the new per-weekday distribution line, add 9 new i18n keys (7 weekday short labels + tooltip + empty-state) in de/en/cs, and delete the three now-orphaned v2.1 keys. Ship SSR-tests that pin the visible output.

Purpose: With 47-01 delivering the new DTO shape, this plan makes the change visible in the UI at the SAME slot and satisfies the presence-test contract in the phase Success Criteria.
Output: Modified FE component + 9 new i18n keys × 3 locales + 3 deleted keys × 3 locales + 3 SSR tests + green FE gates.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-wochentag-anwesenheits-muster/47-CONTEXT.md
@.planning/phases/47-wochentag-anwesenheits-muster/47-01-SUMMARY.md
@shifty-dioxus/src/component/employee_view.rs
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/i18n/de.rs
@shifty-dioxus/src/i18n/en.rs
@shifty-dioxus/src/i18n/cs.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 9 new i18n keys (de/en/cs) + delete 3 v2.1 AVG keys</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <behavior>
    New keys to add to `Key` enum in `shifty-dioxus/src/i18n/mod.rs` (placed near the existing `Monday..Sunday` block for co-location, or near the deleted `AvgHoursPerAttendanceDay*` block — either is fine, be consistent):

    - `WeekdayShortMon`
    - `WeekdayShortTue`
    - `WeekdayShortWed`
    - `WeekdayShortThu`
    - `WeekdayShortFri`
    - `WeekdayShortSat`
    - `WeekdayShortSun`
    - `WeekdayAttendanceTooltip`
    - `WeekdayAttendanceEmpty`

    Keys to DELETE from `Key` enum:
    - `AvgHoursPerAttendanceDay`
    - `AvgHoursPerAttendanceDayDescription`
    - `AvgHoursPerAttendanceDayEmpty`

    Locale bodies to ADD (per de/en/cs — one `i18n.add_text(Locale::X, Key::Y, "..." )` per key):

    De:
    - `WeekdayShortMon` → `Mo`
    - `WeekdayShortTue` → `Di`
    - `WeekdayShortWed` → `Mi`
    - `WeekdayShortThu` → `Do`
    - `WeekdayShortFri` → `Fr`
    - `WeekdayShortSat` → `Sa`
    - `WeekdayShortSun` → `So`
    - `WeekdayAttendanceTooltip` → `Anzahl Anwesenheitstage pro Wochentag und Anteil an den gezählten Kalenderwochen im Zeitraum`
    - `WeekdayAttendanceEmpty` → `Keine gezählten Kalenderwochen im Zeitraum`

    En:
    - `WeekdayShortMon` → `Mon`
    - `WeekdayShortTue` → `Tue`
    - `WeekdayShortWed` → `Wed`
    - `WeekdayShortThu` → `Thu`
    - `WeekdayShortFri` → `Fri`
    - `WeekdayShortSat` → `Sat`
    - `WeekdayShortSun` → `Sun`
    - `WeekdayAttendanceTooltip` → `Attendance-day count per weekday and share relative to counted calendar weeks in the range`
    - `WeekdayAttendanceEmpty` → `No counted calendar weeks in range`

    Cs:
    - `WeekdayShortMon` → `Po`
    - `WeekdayShortTue` → `Út`
    - `WeekdayShortWed` → `St`
    - `WeekdayShortThu` → `Čt`
    - `WeekdayShortFri` → `Pá`
    - `WeekdayShortSat` → `So`
    - `WeekdayShortSun` → `Ne`
    - `WeekdayAttendanceTooltip` → `Počet pracovních dní podle dne v týdnu a podíl vůči započítaným kalendářním týdnům v období`
    - `WeekdayAttendanceEmpty` → `Žádné započítané kalendářní týdny v období`

    Presence test (write BEFORE removing the old AVG keys to keep the module compilable — TDD-lite ordering):
    - Add a `#[test] fn phase_47_weekday_i18n_presence()` at the bottom of `shifty-dioxus/src/i18n/mod.rs` (or in a new `#[cfg(test)] mod phase47_i18n_tests` inside the same file). Test constructs a fresh `I18n` (mirror the pattern from any existing i18n presence test — grep for `Locale::De`+`add_text` in existing test blocks; if none exists, follow the `Locale::De` add pattern already used by the runtime init function and call the same init to populate).
    - Asserts, for each locale in `[Locale::De, Locale::En, Locale::Cs]`, that `i18n.t(Key::WeekdayShortMon)` .. `Key::WeekdayShortSun` are all non-empty strings, `Key::WeekdayAttendanceTooltip` is non-empty, `Key::WeekdayAttendanceEmpty` is non-empty (9 * 3 = 27 lookups minimum).
    - Also asserts weekday-short values are DISTINCT within a locale (7-element `HashSet<String>::from_iter(...).len() == 7`).

    Ordering in this task:
    1. Add the 9 new Key enum variants first.
    2. Add all 27 `add_text` calls in de.rs / en.rs / cs.rs.
    3. Add the presence test at the bottom of `mod.rs`. Run `cargo test -p shifty-dioxus phase_47_weekday_i18n_presence` — MUST be green.
    4. In `shifty-dioxus/src/component/employee_view.rs`, replace the current call-sites `Key::AvgHoursPerAttendanceDay`, `Key::AvgHoursPerAttendanceDayDescription`, `Key::AvgHoursPerAttendanceDayEmpty` with a placeholder call to `Key::WeekdayAttendanceEmpty` (single-line stub — full render logic comes in Task 2). This keeps the compile green during the enum-delete step. NOTE: leave the SSR tests for the OLD attendance row in place for now (Task 2 replaces them).
    5. Remove the 3 old Key variants (`AvgHoursPerAttendanceDay` + `AvgHoursPerAttendanceDayDescription` + `AvgHoursPerAttendanceDayEmpty`) from `mod.rs`, remove their `add_text` calls in de.rs / en.rs / cs.rs, and any array/`for`-loop listing them (grep line 928 in de.rs — there is an existing `[Key::AvgHoursPerAttendanceDay, Key::AvgHoursPerAttendanceDayDescription, Key::AvgHoursPerAttendanceDayEmpty]` array-style block; delete it or refit).
    6. Run `cargo check -p shifty-dioxus` — MUST pass.

    Task 1 committed as one atomic commit:
    `feat(47-02): add weekday-short + tooltip + empty i18n keys, retire v2.1 AVG-attendance keys (RPT-03)`
  </behavior>
  <action>
    Follow steps 1..6 in the behavior block, verifying compile after step 5 with `cargo check -p shifty-dioxus`.

    Grep to find all AVG-attendance-day references before deletion (to be sure step 4 covers everything):
    ```
    grep -rn 'AvgHoursPerAttendanceDay' shifty-dioxus/src/
    ```
    Every hit must either be (a) replaced with the WeekdayAttendanceEmpty stub call-site in step 4, or (b) deleted in step 5. If a hit remains in the SSR-test module inside `component/employee_view.rs` — leave it: Task 2 rewrites that test module wholesale.

    Actually, since Task 2 rewrites the SSR-test module wholesale, prefer to also delete the entire v2.1 SSR test block (`--- SSR tests for the Ø-Anwesenheit attendance row (Phase 41) ---` at line ~1626 up to end of file, keeping module footer `}`) in this task. That removes all `AvgHoursPerAttendanceDay*` references cleanly. Task 2 adds the new SSR test block.
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus phase_47_weekday_i18n_presence 2>&1 | tail -20 && echo "---leftover-old-keys---" && (grep -rn 'AvgHoursPerAttendanceDay' shifty-dioxus/src/ && exit 1 || echo OK-none-left) && echo "---compile---" && cargo check -p shifty-dioxus 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 9 new `Key` variants exist in `mod.rs`.
    - Each of de.rs/en.rs/cs.rs has 9 new `add_text` entries.
    - 3 old AVG-attendance `Key` variants + their locale bodies are gone.
    - `cargo check -p shifty-dioxus` passes.
    - Presence test `phase_47_weekday_i18n_presence` passes.
    - One atomic commit pushed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace HR-stats attendance row with weekday-distribution line + SSR tests</name>
  <files>shifty-dioxus/src/component/employee_view.rs</files>
  <behavior>
    Rewrite the HR-stats-block attendance row (around line 537–558 in `component/employee_view.rs`) so it renders the new `attendance_by_weekday` payload from `EmployeeAttendanceStatisticsTO` in the SAME slot the v2.1 `Ø Std/Anwesenheitstag` row occupied.

    Extract a pure formatter helper adjacent to `format_hours` (near line 7):

    ```
    /// Format the phase-47 weekday-attendance line.
    ///
    /// - `counted_calendar_weeks == 0` → empty-state placeholder.
    /// - Otherwise: 7 segments `"<short-label>: <count> (<pct>%)"` joined by " · ".
    /// - `pct` = `(share * 100.0).round() as i32` (share is already 0.0..=1.0 from BE).
    /// - Weekday-short labels come from i18n: Mon..Sun → WeekdayShortMon..WeekdayShortSun.
    pub fn format_weekday_attendance_line(
        stats: &EmployeeAttendanceStatisticsTO,
        i18n: &I18nAccess, // whatever type the read-lock returns; see existing i18n.t call sites
    ) -> String
    ```

    Note on `i18n` type: the existing code uses `let i18n = crate::service::i18n::I18N.read().clone();` and then calls `i18n.t(Key::...).as_ref()`. The concrete type is `Rc<I18n>` or similar. Match the actual type at the current call sites — grep for `crate::service::i18n::I18N.read()` and mirror. If the formatter helper cannot cleanly take a borrow due to lifetime issues, take it by `Rc<I18n>` (owned clone) and call `.t(...)`.

    The 7-segment mapping table (translate weekday enum → key):

    ```
    DayOfWeekTO::Monday    → Key::WeekdayShortMon
    DayOfWeekTO::Tuesday   → Key::WeekdayShortTue
    DayOfWeekTO::Wednesday → Key::WeekdayShortWed
    DayOfWeekTO::Thursday  → Key::WeekdayShortThu
    DayOfWeekTO::Friday    → Key::WeekdayShortFri
    DayOfWeekTO::Saturday  → Key::WeekdayShortSat
    DayOfWeekTO::Sunday    → Key::WeekdayShortSun
    ```

    HR-stats-block replacement (was lines 537–558):

    ```rust
    // Phase 47: weekday attendance distribution (replaces v2.1 Ø Std/Anwesenheitstag)
    // (RPT-01/02/03 per D-47-BE and D-47-CONTEXT). Rendered only when the server
    // delivered the statistic (Some) — non-flexible employees get None and no row at all
    // (D-AVG-05 gate preserved).
    if let Some(att) = props.attendance_statistics.as_ref() {
        let line = format_weekday_attendance_line(att.as_ref(), &i18n);
        TupleRow {
            label: ImStr::from(i18n.t(Key::WeekdayAttendanceTooltip).as_ref()),
            value: rsx! {
                span {
                    class: "font-mono tabular-nums",
                    title: "{i18n.t(Key::WeekdayAttendanceTooltip)}",
                    "{line}"
                }
            },
        }
    }
    ```

    Concrete formatter behavior (exact string outputs — pinned by SSR tests):
    - counted_calendar_weeks == 0 OR attendance_by_weekday empty → return `i18n.t(Key::WeekdayAttendanceEmpty).to_string()`.
    - Otherwise, iterate `stats.attendance_by_weekday` in order (BE guarantees Mon..Sun order — do NOT re-sort). For each entry:
      - `label = i18n.t(short_key_for(entry.weekday))`
      - `pct = (entry.share * 100.0).round() as i32`
      - segment = `"{label}: {count} ({pct}%)"`
    - Join segments with `" · "` (space, U+00B7 middle dot, space).

    SSR test module — replace the deleted v2.1 SSR block (`--- SSR tests for the Ø-Anwesenheit attendance row (Phase 41) ---` at line ~1626) with three new tests inside the same `#[cfg(test)] mod` scope. All tests use `render_weekday_attendance_row(props) -> String` mirroring the v2.1 `render_attendance_row` helper (see 47-01-SUMMARY for the equivalent BE test scaffold pattern).

    Test 1 `weekday_row_renders_all_seven_segments_when_populated`:
    - Build `EmployeeAttendanceStatisticsTO { attendance_by_weekday: vec![Mon(count=8,share=0.8), Tue(3,0.3), Wed(7,0.7), Thu(5,0.5), Fri(2,0.2), Sat(0,0.0), Sun(0,0.0)], counted_calendar_weeks: 10 }`.
    - HTML MUST contain `Mo: 8 (80%)` AND `Di: 3 (30%)` AND `Mi: 7 (70%)` AND `Do: 5 (50%)` AND `Fr: 2 (20%)` AND `Sa: 0 (0%)` AND `So: 0 (0%)`.
    - HTML MUST contain `·` (middle dot) at least 6 times (6 separators between 7 segments).
    - HTML MUST contain `title="..."` attribute (tooltip present).
    - HTML MUST NOT contain the old label text `Ø Std/Anwesenheitstag` (regression proof).

    Test 2 `weekday_row_renders_empty_state_when_counted_weeks_zero`:
    - Build `EmployeeAttendanceStatisticsTO { attendance_by_weekday: vec![Mon..Sun all zero], counted_calendar_weeks: 0 }`.
    - HTML MUST contain the localized De empty text `Keine gezählten Kalenderwochen im Zeitraum` (i18n default locale during tests is De per the existing test-setup, or whichever is set — mirror existing SSR test).
    - HTML MUST NOT contain any segment like `Mo: 0 (0%)`.

    Test 3 `weekday_row_absent_when_statistics_is_none`:
    - `attendance_statistics: None`.
    - HTML MUST NOT contain `font-mono` and MUST NOT contain any weekday-short label — the row is not rendered at all.

    Also delete the three v2.1 tests `attendance_row_shows_number_when_some`, `attendance_row_shows_endash_when_inner_none`, `attendance_row_absent_when_none` — they reference the deleted DTO fields and are semantically superseded.

    Gates: FE clippy is `-D warnings` per the post-Phase-45 policy — mind unused imports after the swap. WASM build gate mandatory (per the top-level CLAUDE.md).
  </behavior>
  <action>
    1. Add `format_weekday_attendance_line` at the top of `shifty-dioxus/src/component/employee_view.rs` next to the existing `format_hours` import block. Import `DayOfWeekTO` from `rest_types` if not already imported.
    2. Replace the HR-stats-block attendance-row `if let Some(att) = ...` block (lines ~541–558) with the new snippet above.
    3. Replace the three deleted SSR test fns with the three new SSR tests. Rebuild the `render_weekday_attendance_row(props: WeekdayAttendanceRowProps) -> String` helper using the same `VirtualDom::new_with_props` + `dioxus_ssr::render` pattern as the deleted `render_attendance_row`. Give the new props struct a distinct name so nothing collides.
    4. Add a 4th, pure-fn test at the same module scope: `formatter_handles_odd_percents_correctly` — build a DTO with `share = 0.333` → assert the output segment contains `(33%)` (i.e. `(share * 100).round() → 33`).
    5. Run the FE gate chain:
       ```
       cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -20
       cargo test -p shifty-dioxus 2>&1 | tail -20
       cargo clippy -p shifty-dioxus -- -D warnings 2>&1 | tail -20
       ```
    6. Also confirm the backend workspace clippy is still clean (touched files are FE-only, but the rest-types crate is shared):
       ```
       cd .. && cargo clippy --workspace -- -D warnings 2>&1 | tail -10
       ```

    Commit as one atomic commit:
    `feat(47-02): render weekday-attendance distribution in HR-stats block + SSR tests (RPT-01/02 FE)`
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test -p shifty-dioxus weekday_attendance 2>&1 | tail -20 && echo "---wasm-build---" && cargo build --target wasm32-unknown-unknown 2>&1 | tail -10 && echo "---clippy-fe---" && cargo clippy -p shifty-dioxus -- -D warnings 2>&1 | tail -10 && cd .. && echo "---clippy-be---" && cargo clippy --workspace -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <done>
    - The v2.1 `Ø Std/Anwesenheitstag` row is gone from `component/employee_view.rs`.
    - `format_weekday_attendance_line` is a pure fn with a unit test (`formatter_handles_odd_percents_correctly`).
    - The new HR-stats-block row renders the 7 segments joined by ` · ` with `title="..."` tooltip when the DTO is non-empty.
    - Empty-state placeholder shown when `counted_calendar_weeks == 0`.
    - 3 SSR tests + 1 pure-fn formatter test all green.
    - `cargo build --target wasm32-unknown-unknown` in `shifty-dioxus` — green.
    - `cargo clippy -p shifty-dioxus -- -D warnings` — green.
    - `cargo clippy --workspace -- -D warnings` at repo root — green.
    - One atomic commit pushed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → browser (Dioxus WASM) | JSON payload boundary; already HR-gated at the BE (47-01). FE only renders what the server delivered. |
| i18n bundle → rendered DOM | User-controlled locale selection consumes static bundled strings; no template injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-05 | Information Disclosure | HR-stats block visibility | high | mitigate | Row is only rendered when `attendance_statistics.is_some()`; BE returns `None` for non-flexible or non-HR (v2.1 D-AVG-05 gate preserved in 47-01). SSR-test `weekday_row_absent_when_statistics_is_none` pins this. |
| T-47-06 | Tampering | Weekday order in FE | low | accept | BE guarantees Mon..Sun order; FE does NOT reorder. Documented in formatter fn docs. If BE regresses to a different order, SSR test 1 fails (label sequence pinned). |
| T-47-07 | Input Validation | share > 1.0 or share < 0.0 | low | mitigate | BE clamps share to [0, 1] and rounds to 2 decimals; formatter clamps pct via `.round() as i32` regardless — no panic, no rendering surprise. |
| T-47-08 | UX | Empty-state confusion | low | accept | `counted_calendar_weeks == 0` triggers the localized empty-state string; not silent-empty. Presence-tested. |
| T-47-SC | Tampering | Package installs | high | mitigate | No new deps introduced. i18n keys are static string constants. |
</threat_model>

<verification>
Full FE gate chain per CLAUDE.md hard-gate rules (`shifty-dioxus` policy post-Phase-45):

1. `cargo build --target wasm32-unknown-unknown` (WASM-build gate, from `shifty-dioxus/` workspace) — clean.
2. `cargo test -p shifty-dioxus` — all green including new SSR tests + presence test + pure-fn formatter test.
3. `cargo clippy -p shifty-dioxus -- -D warnings` (post-Phase-45 hard gate) — clean.
4. `cargo clippy --workspace -- -D warnings` at repo root (backend hard gate — touched `rest-types` is a shared crate) — clean.
5. `grep -rn 'AvgHoursPerAttendanceDay' shifty-dioxus/src/` — empty (RPT-02 payload removal complete on the FE).
6. `grep -rn 'WeekdayShortMon\|WeekdayShortSun' shifty-dioxus/src/i18n/` — at least 6 hits (Key def + 3 locales × 2 = 6+).

Manual UI proof (optional D-25-06 browser smoke — see phase Deferred Ideas policy):
- Backend running with a flexible employee that has attendance data.
- HR user opens `/employees/{id}` (or the equivalent employee_view route).
- The v2.1 `Ø Std/Anwesenheitstag` line is gone.
- A single new line appears at the same slot, formatted `Mo: N (P%) · Di: N (P%) · Mi: … · So: N (P%)`.
- Hovering shows the tooltip in the current locale.
- Switching to a non-flexible employee: line disappears entirely.
</verification>

<success_criteria>
- [x] `Key::AvgHoursPerAttendanceDay*` (3 keys) removed from `shifty-dioxus/src/i18n/mod.rs` and all three locale files (RPT-02).
- [x] `Key::WeekdayShortMon..Sun` + `Key::WeekdayAttendanceTooltip` + `Key::WeekdayAttendanceEmpty` present in mod.rs + de.rs + en.rs + cs.rs (RPT-03).
- [x] `component/employee_view.rs` HR-stats-block renders the new weekday-distribution line via `format_weekday_attendance_line` (RPT-02).
- [x] Empty-state placeholder line rendered when `counted_calendar_weeks == 0` (RPT-02 layout).
- [x] 3 SSR tests + 1 formatter test + 1 i18n presence test all green (RPT-01/02/03 verification).
- [x] `cargo build --target wasm32-unknown-unknown` + `cargo clippy -p shifty-dioxus -- -D warnings` + `cargo clippy --workspace -- -D warnings` all green.
</success_criteria>

<output>
Create `.planning/phases/47-wochentag-anwesenheits-muster/47-02-SUMMARY.md` describing:
- Formatter fn signature + behavior (with the D-47-CONTEXT layout decision cited).
- List of the 9 new i18n keys with their de/en/cs bodies (concise table).
- List of the 3 deleted v2.1 keys + fully removed AVG references (RPT-02 completeness proof).
- Screenshot-optional note: browser smoke deferred per D-25-06 class (WASM interactive testing is unreliable) — SSR tests are the structural pinning.
</output>

    <automated>cargo test -p shifty-dioxus phase_47_weekday_i18n_presence 2>&1 | tail -20 && echo "---leftover-old-keys---" && (grep -rn 'AvgHoursPerAttendanceDay' shifty-dioxus/src/ || echo OK-none-left) && echo "---compile---" && cargo check -p shifty-dioxus 2>&1 | tail -10