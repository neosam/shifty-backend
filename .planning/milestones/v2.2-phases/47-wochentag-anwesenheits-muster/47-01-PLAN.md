---
phase: 47-wochentag-anwesenheits-muster
plan: 1
type: tdd
wave: 1
depends_on: []
files_modified:
  - service_impl/src/reporting.rs
  - service/src/reporting.rs
  - service_impl/src/test/mod.rs
  - service_impl/src/test/reporting_weekday_attendance.rs
  - rest-types/src/lib.rs
  - rest/src/report.rs
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]

must_haves:
  truths:
    - "GET /report/{id}/attendance-statistics returns a payload of shape { attendance_by_weekday: [7], counted_calendar_weeks } for flexible employees, and JSON null otherwise (RPT-01, RPT-02 per D-47-BE)"
    - "attendance_by_weekday is ordered Monday..Sunday with { weekday: DayOfWeekTO, count: u32, share: f32 in 0.0..=1.0 } (RPT-01)"
    - "The response no longer contains average_hours_per_attendance_day, attendance_days, total_worked_hours (RPT-02)"
    - "HR privilege check is still the FIRST await; is_dynamic filter still short-circuits to Ok(None) (RPT-01 gate preservation)"
    - "CURRENT_SNAPSHOT_SCHEMA_VERSION stays at 12 and no new BillingPeriodValueType variant is added (RPT-03)"
  artifacts:
    - "service::reporting::WeekdayAttendanceStat struct (weekday: shifty_utils::DayOfWeek, count: u32, share: f32)"
    - "service::reporting::EmployeeAttendanceStatistics struct (attendance_by_weekday: [WeekdayAttendanceStat; 7], counted_calendar_weeks: u32) — same struct name, new shape"
    - "service::reporting::weekday_attendance_distribution(days: &[WorkingHoursDay], counted_calendar_weeks: u32) -> EmployeeAttendanceStatistics pure fn"
    - "rest_types::WeekdayAttendanceTO struct + rest_types::EmployeeAttendanceStatisticsTO with attendance_by_weekday + counted_calendar_weeks fields"
    - "service_impl/src/test/reporting_weekday_attendance.rs — TDD-first pure-fn tests"
  key_links:
    - "service_impl::reporting::ReportingServiceImpl::get_employee_attendance_statistics calls weekday_attendance_distribution and counts calendar weeks via all_days iso_week ordering"
    - "rest-types EmployeeAttendanceStatisticsTO::from(&service::reporting::EmployeeAttendanceStatistics) maps the new shape 1:1"
    - "rest/src/report.rs #[utoipa::path] response description updated to reflect the new payload shape"
---

<objective>
Rebuild the backend side of the HR-gated attendance statistic: replace the v2.1 scalar `average_hours_per_attendance_day` metric with a per-weekday distribution (count of attendance days + share of counted calendar weeks) at the SAME endpoint `/report/{id}/attendance-statistics`. Reuse the v2.1 "Anwesenheitstag" definition (Shiftplan / ExtraWork / VolunteerWork with hours > 0). The endpoint stays HR-gated, still filters `is_dynamic`, still returns JSON `null` for non-flexible employees, and produces `attendance_by_weekday: [Mon..Sun; 7]` plus `counted_calendar_weeks`. No snapshot bump — grep-verified.

Purpose: Deliver RPT-01 and the BE half of RPT-02 (removal of the AVG scalar from the payload) plus the RPT-03 snapshot-invariant grep gate in a single wave. The FE consumes this new shape in Plan 47-02.
Output: Pure fn + updated service trait + updated impl + updated DTO + updated REST handler + TDD test file + grep gate proving Snapshot Version 12 is untouched.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-wochentag-anwesenheits-muster/47-CONTEXT.md
@service/src/reporting.rs
@service_impl/src/reporting.rs
@service_impl/src/test/reporting_avg_attendance.rs
@rest-types/src/lib.rs
@rest/src/report.rs
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD pure fn weekday_attendance_distribution (RED → GREEN)</name>
  <files>service_impl/src/test/reporting_weekday_attendance.rs, service_impl/src/test/mod.rs, service/src/reporting.rs</files>
  <behavior>
    Write the failing test file FIRST, then add the pure fn to make it pass. Model the API on the existing v2.1 pattern in `service_impl/src/test/reporting_avg_attendance.rs`.

    Public API being tested (add to `service/src/reporting.rs`, do NOT yet remove the old `average_hours_per_attendance_day` fn — that removal happens in Task 2):

    ```
    #[derive(Clone, Debug, PartialEq)]
    pub struct WeekdayAttendanceStat {
        pub weekday: shifty_utils::DayOfWeek,
        pub count: u32,
        pub share: f32, // 0.0..=1.0, rounded to 2 decimals
    }

    #[derive(Clone, Debug, PartialEq)]
    pub struct EmployeeAttendanceStatistics {
        pub attendance_by_weekday: [WeekdayAttendanceStat; 7], // ordered Mon..Sun
        pub counted_calendar_weeks: u32,
    }

    pub fn weekday_attendance_distribution(
        days: &[WorkingHoursDay],
        counted_calendar_weeks: u32,
    ) -> EmployeeAttendanceStatistics
    ```

    Semantics (locked in 47-CONTEXT):
    - A day is an attendance day iff `d.hours > 0.0 && matches!(d.category, Shiftplan | ExtraWork | VolunteerWork)` (D-AVG-02/03 from v2.1, byte-identical filter).
    - Distinct-date rule: same calendar date with multiple work entries counts ONCE per weekday (dedupe via `BTreeSet<time::Date>` per weekday bucket, mirroring the v2.1 numerator dedupe).
    - `share` = `count as f32 / counted_calendar_weeks as f32`, clamped to `0.0..=1.0`, rounded to 2 decimals via `(x * 100.0).round() / 100.0`. When `counted_calendar_weeks == 0` → `share = 0.0` (no NaN, no div-by-zero).
    - Result array is ALWAYS length 7, ordered `Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday` — every weekday present even when count=0.
    - Weekday derivation: `day.date.weekday()` (from `time` crate) → map via existing `From<time::Weekday> for shifty_utils::DayOfWeek` if available, else expand inline the 7-arm match.

    Tests to write in `service_impl/src/test/reporting_weekday_attendance.rs` (each is a separate `#[test]` fn, all MUST fail before the pure fn exists):

    - Test 1 `empty_input_all_zero_and_zero_weeks`: `days = []`, `counted_calendar_weeks = 0` → all 7 counts = 0, all 7 shares = 0.0, `counted_calendar_weeks == 0`.
    - Test 2 `even_distribution_over_5_weeks`: 5 Mondays (Shiftplan, 8h each) at dates 2026-01-05, 2026-01-12, 2026-01-19, 2026-01-26, 2026-02-02, `counted_calendar_weeks = 5` → Monday: count=5 share=1.0; Tue..Sun: count=0 share=0.0.
    - Test 3 `partial_share_rounds_to_two_decimals`: 8 Mondays / 10 weeks → Monday: count=8 share=0.80; 3 Tuesdays / 10 weeks → Tuesday: count=3 share=0.30. Assert `(share - 0.80).abs() < 0.0001`.
    - Test 4 `mixed_categories_and_absence_excluded`: on 2026-03-02 (Mon): Shiftplan 4h + Vacation 4h + ExtraWork 0h. Expect Monday count=1 (Vacation excluded, 0h ExtraWork excluded), Custom(_) excluded — add a Custom(_) day on 2026-03-03 (Tue) and assert Tuesday count=0.
    - Test 5 `same_date_multiple_work_entries_counts_once`: on 2026-03-09 (Mon): Shiftplan 4h + ExtraWork 4h + VolunteerWork 1h → Monday count=1 (dedupe by date).
    - Test 6 `all_seven_weekdays_present`: iterate a full week (2026-04-06..2026-04-12), Shiftplan 4h each, `counted_calendar_weeks = 1` → each weekday count=1 share=1.0; array indices 0..7 must have weekday Mon, Tue, Wed, Thu, Fri, Sat, Sun in that order (assert on `stats.attendance_by_weekday[0].weekday == DayOfWeek::Monday` … `[6] == DayOfWeek::Sunday`).
    - Test 7 `zero_weeks_yields_zero_shares_not_nan`: 3 Mondays with `counted_calendar_weeks = 0` → count=3, share=0.0 (NOT NaN, NOT +inf). Assert `stat.share.is_finite() && stat.share == 0.0`.
    - Test 8 `share_never_exceeds_one`: 3 Mondays with `counted_calendar_weeks = 2` → count=3, share clamped to 1.0 (not 1.5). Assert `stat.share <= 1.0`.

    Register the new test module in `service_impl/src/test/mod.rs` alongside the existing `reporting_avg_attendance` entry (grep for `mod reporting_avg_attendance;` and add `mod reporting_weekday_attendance;` below it).

    Implementation notes for the pure fn (Task 1 GREEN phase, in `service/src/reporting.rs`):
    - Import: `use shifty_utils::DayOfWeek; use std::collections::BTreeSet; use ExtraHoursReportCategory::{ExtraWork, Shiftplan, VolunteerWork};`
    - Build a `[BTreeSet<time::Date>; 7]` bucket, one per weekday index (Mon=0..Sun=6). Weekday index derivation: match on `d.date.weekday()` (from `time::Weekday`) with the 7-arm expansion.
    - After filling buckets, produce `[WeekdayAttendanceStat; 7]` in order Mon..Sun. For each bucket: `count = bucket.len() as u32; share = if counted_calendar_weeks == 0 { 0.0 } else { ((count as f32 / counted_calendar_weeks as f32).min(1.0) * 100.0).round() / 100.0 };`
    - Return `EmployeeAttendanceStatistics { attendance_by_weekday, counted_calendar_weeks }`.

    Do NOT delete the old `average_hours_per_attendance_day` fn or the old `EmployeeAttendanceStatistics` struct in this task — Task 2 mutates the struct. Instead, RENAME the old struct in-place to a temporary throwaway name for this task's compile step, OR keep it and let Task 2 do the removal. Simplest: temporarily name the new struct `WeekdayEmployeeAttendanceStatistics` for the TDD-Task-1 window (tests reference that name), and Task 2 does the swap. Choose whichever keeps the test file untouched by Task 2 — prefer the temporary name approach so Task 1 stands alone, then Task 2 does a rename-only mechanical replace.
  </behavior>
  <action>
    RED phase:
    1. Create `service_impl/src/test/reporting_weekday_attendance.rs` with the 8 tests above, importing `service::reporting::{WeekdayEmployeeAttendanceStatistics, WeekdayAttendanceStat, weekday_attendance_distribution, ExtraHoursReportCategory, WorkingHoursDay}` and `shifty_utils::DayOfWeek`.
    2. Register `mod reporting_weekday_attendance;` in `service_impl/src/test/mod.rs`.
    3. Run `cargo test -p service_impl reporting_weekday_attendance 2>&1 | head -40` — MUST report unresolved-import errors (module + symbols do not exist yet). This is the required RED state.

    GREEN phase:
    4. In `service/src/reporting.rs`, add the new structs `WeekdayAttendanceStat` and `WeekdayEmployeeAttendanceStatistics` and the pure fn `weekday_attendance_distribution` per the behavior spec above. Leave the existing `EmployeeAttendanceStatistics` and `average_hours_per_attendance_day` UNTOUCHED (Task 2 removes them).
    5. Run `cargo test -p service_impl reporting_weekday_attendance` — MUST be all 8 green.
    6. Run `cargo clippy -p service --no-deps -- -D warnings` — MUST pass on the new fn.

    Commit as two atomic commits per TDD contract:
    - `test(47-01): add failing pure-fn tests for weekday_attendance_distribution`
    - `feat(47-01): implement weekday_attendance_distribution pure fn (RPT-01)`

    NB: Do NOT modify `rest-types`, `rest/`, or the impl file in this task. That is Task 2.
  </action>
  <verify>
    <automated>cargo test -p service_impl reporting_weekday_attendance 2>&1 | tail -25</automated>
  </verify>
  <done>
    - `service_impl/src/test/reporting_weekday_attendance.rs` exists with 8 tests, all green.
    - `service::reporting::weekday_attendance_distribution` exists and matches the signature in the behavior block.
    - Pure fn is unit-tested only (no service/DAO deps).
    - Old v2.1 `average_hours_per_attendance_day` fn is still present (Task 2 removes it).
    - Two atomic commits pushed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire new struct through service trait + REST + DTO, remove v2.1 AVG scalar</name>
  <files>service/src/reporting.rs, service_impl/src/reporting.rs, rest-types/src/lib.rs, rest/src/report.rs, service_impl/src/test/reporting_weekday_attendance.rs</files>
  <behavior>
    Retire the v2.1 AVG scalar shape end-to-end. The endpoint URL stays `/report/{id}/attendance-statistics`; only the response body changes. RPT-02 removes the scalar from the payload; RPT-01 is now visible to REST consumers.

    Step-by-step swap:

    1. In `service/src/reporting.rs`:
       - DELETE the old struct `EmployeeAttendanceStatistics` (the v2.1 shape with `average_hours_per_attendance_day` / `attendance_days` / `total_worked_hours`).
       - RENAME `WeekdayEmployeeAttendanceStatistics` → `EmployeeAttendanceStatistics` (new v2.2 shape with `attendance_by_weekday: [WeekdayAttendanceStat; 7]` and `counted_calendar_weeks: u32`).
       - DELETE the old pure fn `average_hours_per_attendance_day(days: &[WorkingHoursDay]) -> EmployeeAttendanceStatistics`. Its 130-line unit-test file `service_impl/src/test/reporting_avg_attendance.rs` must also be deleted, and the `mod reporting_avg_attendance;` line removed from `service_impl/src/test/mod.rs`.
       - Update `weekday_attendance_distribution` return type to the renamed `EmployeeAttendanceStatistics`.
       - Update the trait `ReportingService::get_employee_attendance_statistics` doc comments to describe the new payload shape (per-weekday distribution + counted_calendar_weeks). Signature stays: `-> Result<Option<EmployeeAttendanceStatistics>, ServiceError>`.

    2. In `service_impl/src/reporting.rs::get_employee_attendance_statistics` (around line 1194):
       - HR_PRIVILEGE stays the FIRST await (preserve D-AVG-05).
       - is_dynamic short-circuit stays: non-flexible → `Ok(None)`.
       - Replace the call `service::reporting::average_hours_per_attendance_day(&all_days)` with `service::reporting::weekday_attendance_distribution(&all_days, counted_calendar_weeks)`.
       - Compute `counted_calendar_weeks: u32` = length of `report.by_week`, cast to u32. Rationale: `by_week` already contains exactly the calendar weeks the report displays (year + until_week clamped inside `get_report_for_employee`); this matches the CONTEXT decision "gezählte Kalenderwochen identisch v2.1" — same window, one row per counted week.
       - Return `Ok(Some(stats))`.

    3. In `rest-types/src/lib.rs`:
       - REPLACE `EmployeeAttendanceStatisticsTO` (currently around line 625) with the new shape:
         ```
         #[derive(Clone, Debug, PartialEq, Serialize, Deserialize, ToSchema)]
         pub struct WeekdayAttendanceTO {
             pub weekday: DayOfWeekTO,
             pub count: u32,
             pub share: f32, // 0.0..=1.0
         }

         #[derive(Clone, Debug, PartialEq, Serialize, Deserialize, ToSchema)]
         pub struct EmployeeAttendanceStatisticsTO {
             pub attendance_by_weekday: Vec<WeekdayAttendanceTO>, // always length 7, Mon..Sun
             pub counted_calendar_weeks: u32,
         }
         ```
         (Vec instead of [T; 7] because utoipa `ToSchema` support for fixed-size arrays is fragile. Length invariant is documented and enforced by the server.)
       - REPLACE the `From<&service::reporting::EmployeeAttendanceStatistics>` impl (currently around line 637) to map the new fields:
         ```
         attendance_by_weekday: stats.attendance_by_weekday.iter().map(|s| WeekdayAttendanceTO {
             weekday: DayOfWeekTO::from(s.weekday),
             count: s.count,
             share: s.share,
         }).collect(),
         counted_calendar_weeks: stats.counted_calendar_weeks,
         ```
       - Add `WeekdayAttendanceTO` to any `pub use` list next to `EmployeeAttendanceStatisticsTO` if such a list exists.

    4. In `rest/src/report.rs`:
       - Update `#[utoipa::path]` for `get_attendance_statistics` (around line 199): change the 200 description to describe the new payload (per-weekday distribution over the report range for flexible employees; null for non-flexible). Keep `body = inline(Option<EmployeeAttendanceStatisticsTO>)` — that's still correct since we replaced the struct in place.
       - The imports list at the top of `rest/src/report.rs` (line 11 area) already exposes `EmployeeAttendanceStatisticsTO`. Also import `WeekdayAttendanceTO` and add it to the `components(schemas(...))` block in `ReportApiDoc` alongside `EmployeeAttendanceStatisticsTO` (around line 261). This makes the nested schema visible in Swagger UI.

    5. Update `service_impl/src/test/reporting_weekday_attendance.rs` (from Task 1): rename the `WeekdayEmployeeAttendanceStatistics` import to `EmployeeAttendanceStatistics`. Tests stay behaviorally identical.

    RPT-03 grep gates (added as automated verify at the bottom of this task):
    - `grep -n 'CURRENT_SNAPSHOT_SCHEMA_VERSION' service_impl/src/billing_period_report.rs` MUST show the value is still `12u32` (or `pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12;`).
    - `grep -rn 'BillingPeriodValueType::' service/src service_impl/src --include='*.rs' | grep -v test | wc -l` MUST equal its pre-phase-47 value (baseline captured before edits) — no new persisted `value_type` introduced.
    - `grep -rn 'average_hours_per_attendance_day' service/src service_impl/src rest-types/src rest/src --include='*.rs' | wc -l` MUST equal 0 (RPT-02 payload removal complete on the BE).
  </behavior>
  <action>
    Execute the 5 steps in order. Prefer small, reviewable edits — one file per Edit call where feasible.

    Concretely:
    1. `service/src/reporting.rs`: delete old struct + old fn; rename new struct; touch trait doc.
    2. `service_impl/src/reporting.rs`: swap fn call in `get_employee_attendance_statistics`; compute `counted_calendar_weeks = report.by_week.len() as u32;` right before the call.
    3. `service_impl/src/test/reporting_avg_attendance.rs`: DELETE file. `service_impl/src/test/mod.rs`: remove `mod reporting_avg_attendance;`.
    4. `rest-types/src/lib.rs`: swap struct + From impl. Add `WeekdayAttendanceTO`.
    5. `rest/src/report.rs`: update utoipa response description; add `WeekdayAttendanceTO` to schemas block.
    6. `service_impl/src/test/reporting_weekday_attendance.rs`: rename import.

    Then run gates in this order and fix issues as they surface (do NOT proceed to next gate until the current one is clean):

    ```
    cargo check --workspace 2>&1 | tail -20
    cargo test --workspace 2>&1 | tail -30
    cargo clippy --workspace -- -D warnings 2>&1 | tail -20
    ```

    Then RPT-03 grep invariants (each MUST match its expected value):

    ```
    grep -n 'CURRENT_SNAPSHOT_SCHEMA_VERSION' service_impl/src/billing_period_report.rs
    grep -v '^#' service/src/reporting.rs service_impl/src/reporting.rs rest-types/src/lib.rs rest/src/report.rs | grep -c 'average_hours_per_attendance_day'
    grep -rn '^pub enum BillingPeriodValueType' service_impl/src/ service/src/
    ```

    Commit as one atomic commit (all files touched together — struct name change, DTO shape, REST wiring, impl call site, test file rename import, deleted v2.1 test file):
    `feat(47-01): swap v2.1 AVG scalar for RPT-01 per-weekday distribution (RPT-02, RPT-03 grep-verified snapshot=12)`

    NB: No SQLx queries added → no `.sqlx` regeneration needed. Confirm by grepping the diff for `query!` / `query_as!`: `git diff HEAD~1 -- '*.rs' | grep -E 'query!|query_as!'` MUST be empty.
  </action>
  <verify>
    <automated>cargo test --workspace 2>&1 | tail -15 && echo "---clippy---" && cargo clippy --workspace -- -D warnings 2>&1 | tail -10 && echo "---snapshot---" && grep -n 'CURRENT_SNAPSHOT_SCHEMA_VERSION' service_impl/src/billing_period_report.rs && echo "---leftover-avg---" && (grep -v '^#' service/src/reporting.rs service_impl/src/reporting.rs rest-types/src/lib.rs rest/src/report.rs | grep -c 'average_hours_per_attendance_day' || true)</automated>
  </verify>
  <done>
    - `cargo test --workspace` passes (569+ backend tests + the 8 new weekday tests, minus the 7 deleted AVG tests).
    - `cargo clippy --workspace -- -D warnings` passes.
    - `CURRENT_SNAPSHOT_SCHEMA_VERSION` is still `12` (grep-verified, RPT-03).
    - Zero occurrences of `average_hours_per_attendance_day` in `service/src`, `service_impl/src`, `rest-types/src`, `rest/src` (excluding comment lines).
    - `EmployeeAttendanceStatisticsTO` in `rest-types` has fields `attendance_by_weekday: Vec<WeekdayAttendanceTO>` and `counted_calendar_weeks: u32`.
    - `WeekdayAttendanceTO` is registered in `ReportApiDoc::components(schemas(...))`.
    - `service_impl/src/test/reporting_avg_attendance.rs` is deleted; `mod reporting_avg_attendance;` removed from `test/mod.rs`.
    - One atomic commit pushed with all sync'd changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST client → `/report/{id}/attendance-statistics` | Authenticated HTTP boundary; response now exposes per-weekday attendance distribution to authorized (HR) callers only. |
| REST handler → ReportingService | In-process trait boundary; HR_PRIVILEGE check + is_dynamic filter enforce the disclosure contract. |
| Service impl → reporting DAO (via `get_report_for_employee`) | Existing v2.1 boundary, unchanged. No new query paths introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Information Disclosure | `get_employee_attendance_statistics` handler | high | mitigate | HR_PRIVILEGE is FIRST await (v2.1 invariant preserved); non-HR → 403 before any DB read; verified by existing `test/reporting_attendance_gate.rs`. |
| T-47-02 | Information Disclosure | Response body for non-flexible employee | medium | mitigate | Server-side `is_dynamic` filter short-circuits to `Ok(None)` → JSON `null`. No weekday counts leak for salaried staff. |
| T-47-03 | Tampering | Snapshot schema drift | high | mitigate | RPT-03 grep gates in Task 2 verify: (a) `CURRENT_SNAPSHOT_SCHEMA_VERSION == 12`, (b) no new `BillingPeriodValueType::*` variant, (c) zero `average_hours_per_attendance_day` references outside comments. Fails the phase if any invariant breaks. |
| T-47-04 | Denial of Service | Pure fn `weekday_attendance_distribution` | low | accept | `O(N)` over report days, N bounded by report range; no allocation surprise (7 BTreeSets bounded by distinct dates). |
| T-47-SC | Tampering | Package installs | high | mitigate | This plan adds ZERO new dependencies (grep the diff for `Cargo.toml` changes — expected empty). No install checkpoint needed. |
</threat_model>

<verification>
Full backend gate chain per CLAUDE.md hard-gate rules:

1. `cargo test --workspace` — all tests green (v2.1 AVG-attendance tests are deleted, new weekday tests are green).
2. `cargo clippy --workspace -- -D warnings` — clean.
3. `cargo build --workspace` — clean.
4. `grep -n 'CURRENT_SNAPSHOT_SCHEMA_VERSION' service_impl/src/billing_period_report.rs` — still `12`.
5. `grep -v '^#' service/src/reporting.rs service_impl/src/reporting.rs rest-types/src/lib.rs rest/src/report.rs | grep -c 'average_hours_per_attendance_day'` — equals `0`.
6. `git diff HEAD~1 -- 'Cargo.toml' '**/Cargo.toml'` — empty (no new deps).

Interface contract for Plan 47-02 consumers:
- REST endpoint URL unchanged: `GET /report/{id}/attendance-statistics?year={year}&until_week={until_week}`.
- Response body when flexible + HR: `{"attendance_by_weekday": [{"weekday": "MONDAY", "count": 8, "share": 0.8}, ..., {"weekday": "SUNDAY", "count": 0, "share": 0.0}], "counted_calendar_weeks": 10}`.
- Response body when non-flexible or non-HR: JSON `null`.
- `DayOfWeekTO` enum values are the existing `MONDAY..SUNDAY` (SCREAMING_SNAKE, as already serialized by v2.1).
</verification>

<success_criteria>
- [x] Pure fn `weekday_attendance_distribution` exists, is unit-tested (8 tests), and matches the signature in must_haves (RPT-01).
- [x] `EmployeeAttendanceStatistics` service struct has fields `attendance_by_weekday: [WeekdayAttendanceStat; 7]` + `counted_calendar_weeks: u32` (RPT-01).
- [x] `EmployeeAttendanceStatisticsTO` DTO has fields `attendance_by_weekday: Vec<WeekdayAttendanceTO>` + `counted_calendar_weeks: u32` (RPT-01, RPT-02 payload).
- [x] `get_employee_attendance_statistics` REST endpoint still HR-gated, still returns `Option`, response body swapped (RPT-02).
- [x] v2.1 fields `average_hours_per_attendance_day` / `attendance_days` / `total_worked_hours` are gone from the backend surface (RPT-02).
- [x] Snapshot schema version stays `12`; no new `BillingPeriodValueType` — grep-verified (RPT-03).
- [x] `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` both green.
</success_criteria>

<output>
Create `.planning/phases/47-wochentag-anwesenheits-muster/47-01-SUMMARY.md` describing:
- New pure fn signature + semantics (with the CONTEXT decision cites: D-47-BE endpoint reuse, D-AVG-02/03 attendance-day definition preserved from v2.1).
- Interface contract for Plan 47-02: exact REST response shape + DayOfWeekTO enum values.
- RPT-03 grep-gate evidence (CURRENT_SNAPSHOT_SCHEMA_VERSION line + `average_hours_per_attendance_day` occurrence count = 0).
- What was deleted (v2.1 AVG scalar struct + fn + test file) for the record.
</output>
