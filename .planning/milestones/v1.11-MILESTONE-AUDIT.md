---
milestone: v1.11
audited: 2026-07-01
status: passed
scores:
  requirements: 6/6
  phases: 3/3
  integration: 6/6
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 36-special-days-bugfixes
    items:
      - "WR-01: special-day create-vs-replace is a non-transactional read-then-write and there is no UNIQUE index on (year, calendar_week, day_of_week) — partial replace possible under concurrency/pre-existing duplicate active rows. Proper fix needs a DB migration (out of scope for v1.11: 'keine Migration'). Real-world risk low (single shiftplanner)."
      - "WR-02: Settings special-days 'already exists' hint copy is now semantically stale (create replaces instead of blocking). Fix needs an i18n copy change (out of scope: SC5 'i18n unberührt'). Candidate for a small follow-up."
      - "SDF-02 live-browser button re-enable smoke (D-25-06 class) — deferred optional; structurally verified by SSR/component tests; user accepted structural verification 2026-07-01."
  - phase: 37-modal-ux-politur
    items:
      - "MOD-01 live-browser drag smoke (D-25-06 class) — deferred optional; structurally verified by 5 BackdropPress unit tests per D-10; user precedent (phase 36) accepted structural verification."
      - "WR-02 (pre-existing): contract_modal.rs:77-81 dead `cancel_label` conditional (identical branches, likely a lost 'Close' label in read-only mode). Not in the phase-37 diff, unrelated to MOD-01/MOD-02. Candidate for a small follow-up (needs a Close i18n key)."
      - "WR-01: the panel onmousedown stop_propagation added for MOD-01 blocks document-level outside-click detection for any future Dialog child — inherent to the locked D-01 approach, no current victim."
  - phase: 38-frontend-build-hygiene
    items:
      - "IN-01/IN-02: a few #[allow(dead_code)] keeps (has_sunday_slots free helper, DialogVariant::Sheet, AddExtraHoursFormAction) are weaker/deferred suppressions of known-dead code — candidates for the deferred dioxus-clippy cleanup phase."
      - "IN-03: billing_period_details swapped a coroutine for use_effect (a minor refactor inside a deletions-only phase) — code-review-verified functionally equivalent (effect runs once on mount)."
  - project-wide
    items:
      - "Pre-existing frontend test failure `i18n_impersonation_keys_match_german_reference` predates v1.11 (present at the milestone base commit) — out of scope; carry forward."
      - "dioxus workspace remains excluded from the CI clippy gate (~198 pre-existing clippy lints) — explicitly deferred (D-08/D-09); candidate for its own future phase."
---

# Milestone v1.11 — Stabilisierung & UX-Politur — Audit

**Audited:** 2026-07-01
**Status:** ✅ passed
**Definition of done (ROADMAP):** Consolidation after the v1.7–v1.10 feature wave — clear the reported bugs and make the frontend build warning-free. No new capabilities, no snapshot bump (stays 12), no migration, no new deps.

## Requirements Coverage (3-source cross-reference)

All six requirements satisfied. VERIFICATION.md (status: passed) + SUMMARY frontmatter (requirements-completed) + REQUIREMENTS.md traceability (`[x]` / Complete) agree for every REQ-ID.

| REQ-ID | Phase | VERIFICATION | Traceability | Evidence | Final |
|--------|-------|--------------|--------------|----------|-------|
| SDF-01 | 36 | passed | Complete `[x]` | Atomic in-place UPDATE replaces same-date special_day (`service_impl/special_days.rs`, `dao_impl_sqlite/special_day.rs:187`); RED→GREEN switch tests both directions; pre-fix HTTP 422 → post-fix success recorded; .sqlx regenerated | **satisfied** |
| SDF-02 | 36 | passed | Complete `[x]` | Controlled `SelectInput.value` prop (`inputs.rs:83`) + Settings Card-3 binding (`settings.rs`); post-create reset re-enables button; SSR/component tests | **satisfied** |
| MOD-01 | 37 | passed | Complete `[x]` | `BackdropPress` state machine (`dialog.rs`) central + `absence_convert_modal.rs` inline; 5 unit tests; ESC/X untouched; drag-inside→release-outside stays open | **satisfied** |
| MOD-02 | 37 | passed | Complete `[x]` | 6 `*Help` i18n keys in de/en/cs (verbatim German) rendered as sibling spans under contract-modal fields; From/To excluded; SSR/i18n tests | **satisfied** |
| HYG-01 | 38 | passed | Complete `[x]` | `cargo build` (shifty-dioxus) **0 warnings** (14 auto-fixed, 2 deprecated→parse_borrowed, ~34 dead-code removed / 11 justified #[allow]) | **satisfied** |
| HYG-02 | 38 | passed | Complete `[x]` | Backend `cargo clippy --workspace -- -D warnings` green (regression gate); dioxus clippy documented out-of-scope | **satisfied** |

**Coverage:** 6/6 satisfied · 0 unsatisfied · 0 orphaned.

## Phases

| Phase | Plans | VERIFICATION | Score |
|-------|-------|--------------|-------|
| 36 Special-Days-Bugfixes (BE+FE) | 2/2 | passed | 14/15 (1 deferred optional browser smoke) |
| 37 Modal-UX-Politur (FE) | 2/2 | passed | 14/14 |
| 38 Frontend-Build-Hygiene | 2/2 | passed | 9/9 |

## Cross-Phase Integration

Integration checker: **PASS**. The three phases are functionally independent (no phase depends on another's output). Verified at HEAD:
- Backend `cargo test --workspace` (528 unit + 64 integration) + `cargo clippy --workspace -- -D warnings` — green.
- Frontend `cargo build` 0 warnings, `cargo test -p shifty-dioxus` 727 passed (1 pre-existing OOS failure), `cargo build --target wasm32-unknown-unknown` — green.
- Phase-38 dead-code deletions did NOT remove any symbol phase 36/37 introduced: `BackdropPress`, `SelectInput.value`, the atomic special-day UPDATE path, and the 6 `*Help` keys are all present and wired.

E2E flows (structural): special-day create/replace, controlled SelectInput / Settings Card-3, modal backdrop close, contract-modal help spans — all COMPLETE.

## Snapshot / Migration / Deps discipline

- **Snapshot schema version: 12 (unchanged)** — no persisted `BillingPeriodValueType` path touched. ✅
- **No migration**, **no new deps** — confirmed across all three phases. ✅

## Tech Debt / Deferred (non-blocking)

See frontmatter `tech_debt`. All items are either (a) out-of-scope-by-milestone-constraint (WR-01 needs migration; WR-02/36 needs i18n copy change), (b) pre-existing (WR-02/37 cancel_label; the impersonation test failure; the ~198 dioxus clippy lints), or (c) optional D-25-06-class browser smokes with full structural test coverage. None block the milestone's definition of done.

## Verdict

Milestone v1.11 met its definition of done: all 6 requirements shipped and verified, cross-phase integration clean, both workspaces green at HEAD, no snapshot bump / migration / new deps. Tech debt is minor and consciously deferred. **Ready to complete.**
