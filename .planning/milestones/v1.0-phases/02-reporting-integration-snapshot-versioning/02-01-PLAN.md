---
id: 02-01
phase: 02
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - service_impl/src/test/reporting_phase2_fixtures.rs
  - service_impl/src/test/billing_period_snapshot_locking.rs
  - service_impl/src/test/absence_derive_hours_range.rs
  - service_impl/src/test/reporting_flag_off_bit_identity.rs
  - service_impl/src/test/reporting_flag_on_integration.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements:
  - SNAP-01
  - SNAP-02
  - REP-02
tags: [rust, testing, scaffolding, mockall, locking-test]

must_haves:
  truths:
    - "Vor Wave 2 existieren Test-Skelette, die ROT sind: locking-test scheitert weil CURRENT_SNAPSHOT_SCHEMA_VERSION noch 2 ist statt 3."
    - "Vor Wave 2 existiert eine Bit-Identitaets-Test-Datei mit Mock-Setup, die nach Wave-2-Code GREEN wird, aber jetzt noch nicht kompiliert (UnpaidLeave-Variante fehlt) oder rot ist."
    - "Eine zentrale Fixtures-Datei haelt die deterministischen Test-Daten (Sales-Person, Vertrag 8h/Tag Mo-Fr, Range 2024-06-03..2024-06-09, 1 Vacation 06-03..05, 1 SickLeave 06-04, 1 ExtraWork +2h am 06-06) als wiederverwendbare Funktionen vor."
    - "Der Compiler-Exhaustive-Match-Locking-Test listet alle 12 BillingPeriodValueType-Varianten inkl. UnpaidLeave (zukuenftig) und schlaegt mit Compile-Error fehl, sobald Wave 2 die UnpaidLeave-Variante hinzufuegt - bis zur Test-Anpassung."
  artifacts:
    - path: "service_impl/src/test/reporting_phase2_fixtures.rs"
      provides: "Wiederverwendbare Test-Fixtures (Sales-Person, EmployeeWorkDetails, AbsencePeriods, ExtraHours-Twin) fuer Phase-2-Tests."
      contains: "pub fn fixture_sales_person, pub fn fixture_work_details_8h_mon_fri, pub fn fixture_vacation_period, pub fn fixture_sick_period, pub fn fixture_extra_work_entry"
    - path: "service_impl/src/test/billing_period_snapshot_locking.rs"
      provides: "Locking-Test-Scaffold: Pin-Map (rot pre-Wave-2) + Exhaustive-Match (compile-error post-UnpaidLeave-Variante bis zur Anpassung)."
      contains: "test_billing_period_value_type_surface_locked, test_snapshot_schema_version_pinned"
    - path: "service_impl/src/test/absence_derive_hours_range.rs"
      provides: "Stub-Tests fuer derive_hours_for_range (3 Faelle: basic, holiday=0, contract-change)."
      contains: "test_derive_hours_for_range_basic, test_derive_hours_holiday_is_zero, test_derive_hours_contract_change"
    - path: "service_impl/src/test/reporting_flag_off_bit_identity.rs"
      provides: "Test-Datei: Flag=off → values-Map identisch zu Pre-Phase-2-Erwartung (vacation_hours/sick_leave_hours/unpaid_leave_hours)."
      contains: "test_flag_off_produces_identical_values_to_pre_phase2"
    - path: "service_impl/src/test/reporting_flag_on_integration.rs"
      provides: "Test-Datei: Flag=on → AbsencePeriod-derived Stunden ueberschreiben ExtraHours-Pfad fuer Vacation/Sick/UnpaidLeave."
      contains: "test_flag_on_uses_absence_source"
  key_links:
    - from: "service_impl/src/test/billing_period_snapshot_locking.rs"
      to: "service_impl/src/billing_period_report.rs::CURRENT_SNAPSHOT_SCHEMA_VERSION"
      via: "use service_impl::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION; assert_eq!(CURRENT_SNAPSHOT_SCHEMA_VERSION, 3);"
      pattern: "CURRENT_SNAPSHOT_SCHEMA_VERSION"
    - from: "service_impl/src/test/billing_period_snapshot_locking.rs"
      to: "service/src/billing_period.rs::BillingPeriodValueType"
      via: "match value_type { ... }-block listet alle Varianten inkl. UnpaidLeave"
      pattern: "BillingPeriodValueType::UnpaidLeave"
    - from: "service_impl/src/test/mod.rs"
      to: "alle 5 neuen Testdateien"
      via: "pub mod reporting_phase2_fixtures; pub mod billing_period_snapshot_locking; pub mod absence_derive_hours_range; pub mod reporting_flag_off_bit_identity; pub mod reporting_flag_on_integration;"
      pattern: "pub mod (reporting_phase2_fixtures|billing_period_snapshot_locking|absence_derive_hours_range|reporting_flag_off_bit_identity|reporting_flag_on_integration)"
---

<objective>
Wave 0 Test-Scaffolding fuer Phase 2: Erstelle 5 neue Test-Dateien (1 Fixtures-Datei + 1 Locking-Test + 3 Test-Stubs fuer derive_hours_for_range/Flag-off/Flag-on), registriere sie in `service_impl/src/test/mod.rs`, sorge dafuer dass sie kompilieren oder als RED-Tests Wave 2 zwingen, den Snapshot-Bump und die UnpaidLeave-Variante hinzuzufuegen.

Purpose: Nyquist-Verifikation, Bit-Identitaets-Garantie und Build-Time-Gate sind erst beweisbar wenn die Test-Infrastruktur VOR der Code-Aenderung steht. Wave 2 wird die Test-Bodies vervollstaendigen — Wave 0 schafft das Geruest, gegen das Wave 2 implementiert.

Output: 5 neue Testdateien + 1 mod.rs-Patch. Pin-Map-Test (`test_snapshot_schema_version_pinned`) muss aktuell ROT sein (`CURRENT_SNAPSHOT_SCHEMA_VERSION = 2 != 3`); der Test wird in Wave 2 GRUEN, sobald der Bump erfolgt. Die Stub-Tests fuer derive_hours/Flag-off/Flag-on rufen `unimplemented!()` auf (kompilieren, ignorieren) — Wave 1 und Wave 2 ersetzen `unimplemented!()` durch echte Assertions.
</objective>

<execution_context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reporting-integration-snapshot-versioning/02-CONTEXT.md
@.planning/phases/02-reporting-integration-snapshot-versioning/02-RESEARCH.md
@.planning/phases/02-reporting-integration-snapshot-versioning/02-PATTERNS.md
@.planning/phases/02-reporting-integration-snapshot-versioning/02-VALIDATION.md
@CLAUDE.md
@CLAUDE.local.md
</execution_context>

<context>
<interfaces>
<!-- Bestehende Test-Konventionen, von Phase 1 etabliert -->

From service_impl/src/test/mod.rs (current state):
```rust
// Listet alle Test-Submodule, alphabetisch gruppiert.
// Beispiel: pub mod absence; pub mod billing_period; pub mod billing_period_report; ...
```

From service/src/billing_period.rs:33-46 (current 11 variants — UnpaidLeave fehlt noch):
```rust
pub enum BillingPeriodValueType {
    Balance,
    Overall,
    ExpectedHours,
    ExtraWork,
    VacationHours,
    SickLeave,
    Holiday,
    Volunteer,
    CustomExtraHours(Arc<str>),
    VacationDays,
    VacationEntitlement,
}
```

From service_impl/src/billing_period_report.rs:37 (current state):
```rust
pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 2;
```

From service_impl/src/test/billing_period_report.rs (existing patterns to copy):
- MockDeps-Struct + impl BillingPeriodReportServiceDeps
- build_service-Pattern
- setup_build_and_persist_mocks helper

From service_impl/src/test/absence.rs (existing patterns to copy):
- AbsenceDependencies-Struct mit MockAbsenceDao, MockPermissionService, etc.
- build_service()-Methode
- mock_authenticated_permission_service helper
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0.1: Fixtures-Datei + mod.rs Registrierung</name>
  <files>service_impl/src/test/reporting_phase2_fixtures.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/mod.rs (komplette Datei lesen — Modulorder verstehen, alphabetische Sortierung)
    - service_impl/src/test/absence.rs (Zeilen 1-200 — AbsenceDependencies-Struct + build_service-Pattern fuer Mock-Setup)
    - service_impl/src/test/billing_period_report.rs (Zeilen 1-100 — MockDeps-Pattern, setup-Helper)
    - .planning/phases/02-reporting-integration-snapshot-versioning/02-CONTEXT.md (Section <specifics> — Pin-Map-Fixture-Vorschlag)
    - service/src/sales_person.rs (SalesPerson-Struct fuer Fixture-Werte)
    - service/src/employee_work_details.rs (EmployeeWorkDetails-Struct: Felder expected_hours, workdays_per_week, from_year/from_calendar_week, to_year/to_calendar_week, monday..sunday Flags)
    - service/src/absence.rs (AbsencePeriod-Struct, AbsenceCategory-Enum nach Phase 1)
    - service/src/extra_hours.rs (ExtraHours-Struct, ExtraHoursCategory-Enum)
  </read_first>
  <behavior>
    - Datei `service_impl/src/test/reporting_phase2_fixtures.rs` exportiert `pub fn`s die deterministische Test-Daten zurueckgeben
    - Test-Person: `pub fn fixture_sales_person_id() -> Uuid` returns `Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0001)` (deterministisch)
    - `pub fn fixture_work_details_8h_mon_fri() -> EmployeeWorkDetails` mit `expected_hours = 40.0`, `workdays_per_week = 5.0`, `monday=true..friday=true, saturday=false, sunday=false`, von KW 22/2024 bis KW 25/2024
    - `pub fn fixture_vacation_period() -> AbsencePeriod` mit `from_date = 2024-06-03 (Mo)`, `to_date = 2024-06-05 (Mi)`, `category = AbsenceCategory::Vacation`
    - `pub fn fixture_sick_period() -> AbsencePeriod` mit `from_date = 2024-06-04 (Di)`, `to_date = 2024-06-04`, `category = AbsenceCategory::SickLeave` (ueberlappt 1 Tag mit Vacation)
    - `pub fn fixture_extra_work_entry() -> ExtraHours` mit `date_time = 2024-06-06 (Do) 09:00`, `category = ExtraHoursCategory::ExtraWork`, `amount = 2.0`
    - `pub fn fixture_report_range() -> (time::Date, time::Date)` returns `(2024-06-03, 2024-06-09)`
    - Datei kompiliert allein, ohne dass die anderen 4 neuen Testdateien existieren
    - mod.rs-Patch: `pub mod reporting_phase2_fixtures;` an alphabetisch korrekter Stelle einfuegen (zwischen `mod reporting` falls vorhanden und `mod sales_person`)
  </behavior>
  <action>
Erstelle die Datei `service_impl/src/test/reporting_phase2_fixtures.rs` mit folgendem Inhalt-Skelett:

```rust
//! Wiederverwendbare deterministische Fixtures fuer Phase-2-Reporting-Tests.
//!
//! Quelle: .planning/phases/02-reporting-integration-snapshot-versioning/02-CONTEXT.md
//! Section <specifics> -- Pin-Map-Fixture-Vorschlag.

use std::sync::Arc;
use time::macros::date;
use uuid::Uuid;

use service::absence::{AbsencePeriod, AbsenceCategory};
use service::employee_work_details::EmployeeWorkDetails;
use service::extra_hours::{ExtraHours, ExtraHoursCategory};
use service::sales_person::SalesPerson;

/// Deterministische SalesPerson-Id fuer alle Phase-2-Tests.
pub fn fixture_sales_person_id() -> Uuid {
    Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0001)
}

/// Sales-Person mit Test-Werten.
pub fn fixture_sales_person() -> SalesPerson {
    SalesPerson {
        id: fixture_sales_person_id(),
        name: Arc::from("Test Person"),
        background_color: Arc::from("#000000"),
        is_paid: Some(true),
        inactive: false,
        deleted: None,
        version: Uuid::nil(),
    }
}

/// 8h/Tag, Mo-Fr, gueltig von KW 22/2024 bis KW 25/2024 (deckt 2024-06-03..09).
pub fn fixture_work_details_8h_mon_fri() -> EmployeeWorkDetails {
    EmployeeWorkDetails {
        id: Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0010),
        sales_person_id: fixture_sales_person_id(),
        expected_hours: 40.0,
        from_calendar_week: 22,
        from_year: 2024,
        to_calendar_week: 25,
        to_year: 2024,
        workdays_per_week: 5,
        monday: true,
        tuesday: true,
        wednesday: true,
        thursday: true,
        friday: true,
        saturday: false,
        sunday: false,
        days_per_week: 5,
        vacation_days: 30,
        holiday_hours: 0.0,
        // weitere Felder ggf. via Default::default(); siehe service/src/employee_work_details.rs
        ..Default::default()
    }
}

/// Vacation Mo-Mi (3 Werktage).
pub fn fixture_vacation_period() -> AbsencePeriod {
    AbsencePeriod {
        id: Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0100),
        logical_id: Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0100),
        sales_person_id: fixture_sales_person_id(),
        category: AbsenceCategory::Vacation,
        from_date: date!(2024 - 06 - 03),
        to_date: date!(2024 - 06 - 05),
        version: Uuid::nil(),
        deleted: None,
    }
}

/// SickLeave Di (ueberlappt mit Vacation -> BUrlG §9 Test).
pub fn fixture_sick_period() -> AbsencePeriod {
    AbsencePeriod {
        id: Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0200),
        logical_id: Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_0200),
        sales_person_id: fixture_sales_person_id(),
        category: AbsenceCategory::SickLeave,
        from_date: date!(2024 - 06 - 04),
        to_date: date!(2024 - 06 - 04),
        version: Uuid::nil(),
        deleted: None,
    }
}

/// ExtraWork +2h am Do.
pub fn fixture_extra_work_entry() -> ExtraHours {
    ExtraHours {
        id: Uuid::from_u128(0x0000_0000_0000_0000_0000_0000_0000_1000),
        sales_person_id: fixture_sales_person_id(),
        amount: 2.0,
        category: ExtraHoursCategory::ExtraWork,
        description: Arc::from(""),
        date_time: time::PrimitiveDateTime::new(
            date!(2024 - 06 - 06),
            time::Time::from_hms(9, 0, 0).unwrap(),
        ),
        created: None,
        deleted: None,
        version: Uuid::nil(),
    }
}

/// Report-Range Mo 2024-06-03 bis So 2024-06-09 (Kalenderwoche 23/2024).
pub fn fixture_report_range() -> (time::Date, time::Date) {
    (date!(2024 - 06 - 03), date!(2024 - 06 - 09))
}
```

**WICHTIG:**
- Falls `EmployeeWorkDetails` kein `Default::default()` impl hat: lies die Datei `service/src/employee_work_details.rs` einmal vollstaendig und initialisiere alle Felder explizit. Verzichte auf den `..Default::default()`-Spread.
- Falls einzelne Feldnamen abweichen (z.B. `is_paid` heisst anders): adapter-mappe nach dem tatsaechlichen Struct.
- ExtraHours-Felder: pruefe `service/src/extra_hours.rs` — falls `created`-Feld nicht optional ist, weglassen.
- Sales-Person-Felder: pruefe `service/src/sales_person.rs` — pflicht-Felder vollstaendig setzen.
- Datei muss `cargo build -p service_impl` gruen lassen.

Patche `service_impl/src/test/mod.rs`: fuege `pub mod reporting_phase2_fixtures;` an alphabetisch korrekter Stelle ein (zwischen `pub mod permission_test;` und `pub mod sales_person;` — pruefe die existierende Reihenfolge in der Datei).
  </action>
  <verify>
    <automated>cargo build -p service_impl 2>&amp;1 | tail -20 ; test $(cargo build -p service_impl 2>/dev/null; echo $?) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -f service_impl/src/test/reporting_phase2_fixtures.rs` true
    - `grep -c "pub fn fixture_sales_person_id" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1
    - `grep -c "pub fn fixture_work_details_8h_mon_fri" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1
    - `grep -c "pub fn fixture_vacation_period" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1
    - `grep -c "pub fn fixture_sick_period" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1
    - `grep -c "pub fn fixture_extra_work_entry" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1
    - `grep -c "pub fn fixture_report_range" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1
    - `grep -c "pub mod reporting_phase2_fixtures" service_impl/src/test/mod.rs` >= 1
    - `cargo build -p service_impl` exit 0
  </acceptance_criteria>
  <done>
    Fixtures-Datei kompiliert. Wave-1 und Wave-2-Tests koennen die Funktionen importieren via `use crate::test::reporting_phase2_fixtures::*;`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.2: Locking-Test-Scaffold (Pin-Map-Skelett + Compiler-Exhaustive-Match)</name>
  <files>service_impl/src/test/billing_period_snapshot_locking.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/mod.rs (vorheriger Patch-Status nach Task 0.1)
    - service_impl/src/test/billing_period_report.rs (Zeilen 1-100 fuer Imports + Zeilen 1080-1149 fuer bestehende Schema-Version-Test-Pattern)
    - service/src/billing_period.rs (Zeilen 33-90 — alle 11 BillingPeriodValueType-Varianten)
    - service_impl/src/billing_period_report.rs (Zeile 37 — CURRENT_SNAPSHOT_SCHEMA_VERSION = 2)
    - .planning/phases/02-reporting-integration-snapshot-versioning/02-CONTEXT.md (Section <specifics> — Compiler-Exhaustive-Match-Skelett verbatim)
    - .planning/phases/02-reporting-integration-snapshot-versioning/02-PATTERNS.md (Section "Wave 2 — Locking-Tests" — Locking-Test-2-Skelett)
  </read_first>
  <behavior>
    - Datei `service_impl/src/test/billing_period_snapshot_locking.rs` enthaelt zwei `#[test]`-Funktionen:
      1. `test_snapshot_schema_version_pinned` — assertion `assert_eq!(CURRENT_SNAPSHOT_SCHEMA_VERSION, 3, "CURRENT_SNAPSHOT_SCHEMA_VERSION muss 3 sein nach Phase-2-Bump (siehe CLAUDE.md § Snapshot Schema Versioning)")`. **Schlaegt jetzt fehl (Pre-Wave-2: 2 != 3) — wird in Wave 2 GRUEN.**
      2. `test_billing_period_value_type_surface_locked` — definiert eine inner `fn ensure_locked(value_type: &BillingPeriodValueType)` mit `match`-Block der alle 11 (Pre-Wave-2) bzw. 12 (Post-Wave-2) Varianten exhaustiv listet. Pre-Wave-2 listet die 11 bestehenden Varianten OHNE UnpaidLeave (kompiliert). **Wenn Wave 2 die UnpaidLeave-Variante hinzufuegt, wirft der Compiler einen Error fuer fehlenden Match-Arm — Wave 2 muss den Test um `BillingPeriodValueType::UnpaidLeave => {}` erweitern.**
    - Beide Funktionen tragen den Header-Kommentar (verbatim aus 02-CONTEXT.md `<specifics>`):
      ```
      /// LOCKING TEST -- DO NOT NAIVELY UPDATE.
      /// If this test fails after a code change:
      ///   - Did you intentionally change the snapshot computation?
      ///   - If yes, you MUST also bump CURRENT_SNAPSHOT_SCHEMA_VERSION
      ///     in service_impl/src/billing_period_report.rs.
      ///   - See CLAUDE.md § "Billing Period Snapshot Schema Versioning"
      ///     for the bump-trigger rules.
      ```
    - Pre-Wave-2 Status: Datei KOMPILIERT (exhaustive-match listet 11 Varianten, das stimmt), aber `cargo test -p service_impl billing_period_snapshot_locking::test_snapshot_schema_version_pinned` ist ROT (Version=2, erwartet=3). Das ist Wave-2-Forcing.
  </behavior>
  <action>
Erstelle die Datei `service_impl/src/test/billing_period_snapshot_locking.rs` mit:

```rust
//! Locking-Test-Suite fuer Snapshot-Schema-Versionierung (SNAP-02).
//!
//! Diese Datei verhindert stille Drift zwischen der Snapshot-Berechnung und
//! `CURRENT_SNAPSHOT_SCHEMA_VERSION`. Siehe CLAUDE.md § "Billing Period Snapshot
//! Schema Versioning" fuer die Bump-Trigger-Regeln.
//!
//! - `test_snapshot_schema_version_pinned`: expected=3 (Wave-2-Forcing — pre-Wave-2 ROT, post-Wave-2 GRUEN).
//! - `test_billing_period_value_type_surface_locked`: Compile-Error wenn Enum-Variante hinzu/weg ohne Test-Update.

use service::billing_period::BillingPeriodValueType;
use service_impl::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION;

/// LOCKING TEST -- DO NOT NAIVELY UPDATE.
///
/// If this test fails after a code change:
///   - Did you intentionally change the snapshot computation?
///   - If yes, you MUST also bump CURRENT_SNAPSHOT_SCHEMA_VERSION
///     in service_impl/src/billing_period_report.rs.
///   - See CLAUDE.md § "Billing Period Snapshot Schema Versioning"
///     for the bump-trigger rules.
#[test]
fn test_snapshot_schema_version_pinned() {
    assert_eq!(
        CURRENT_SNAPSHOT_SCHEMA_VERSION, 3,
        "CURRENT_SNAPSHOT_SCHEMA_VERSION muss 3 sein nach Phase-2-Bump \
         (Trigger: neuer value_type UnpaidLeave + AbsencePeriod-derived Vacation/Sick/UnpaidLeave). \
         Siehe CLAUDE.md § Snapshot Schema Versioning."
    );
}

/// LOCKING TEST -- DO NOT NAIVELY UPDATE.
///
/// Wenn der Compiler hier eine fehlende Variante meldet:
/// bist du sicher, dass du nicht `CURRENT_SNAPSHOT_SCHEMA_VERSION` bumpen wolltest?
/// Siehe CLAUDE.md § "Billing Period Snapshot Schema Versioning".
#[test]
fn test_billing_period_value_type_surface_locked() {
    fn ensure_locked(value_type: &BillingPeriodValueType) {
        // Wenn der Compiler hier `non-exhaustive patterns` meldet: Wave 2 hat
        // die `UnpaidLeave`-Variante hinzugefuegt — fuege einen neuen Arm
        // `BillingPeriodValueType::UnpaidLeave => {}` ein und behalte den
        // Snapshot-Bump 2 -> 3 in `service_impl/src/billing_period_report.rs:37`.
        match value_type {
            BillingPeriodValueType::Overall => {}
            BillingPeriodValueType::Balance => {}
            BillingPeriodValueType::ExpectedHours => {}
            BillingPeriodValueType::ExtraWork => {}
            BillingPeriodValueType::VacationHours => {}
            BillingPeriodValueType::SickLeave => {}
            BillingPeriodValueType::Holiday => {}
            BillingPeriodValueType::Volunteer => {}
            BillingPeriodValueType::VacationDays => {}
            BillingPeriodValueType::VacationEntitlement => {}
            BillingPeriodValueType::CustomExtraHours(_) => {}
            // POST-WAVE-2: BillingPeriodValueType::UnpaidLeave => {}
        }
    }
    // Compiler-only Check: ensure_locked wird nie aufgerufen.
    let _ = ensure_locked;
}
```

Patche `service_impl/src/test/mod.rs`: fuege `pub mod billing_period_snapshot_locking;` an alphabetisch korrekter Stelle ein (zwischen `pub mod billing_period_report;` und `pub mod block;` — pruefe).

**Erwartetes Verhalten nach Wave 0 commit:**
- `cargo build -p service_impl --tests` exit 0 (Pre-Wave-2 kompiliert weil 11 Varianten match)
- `cargo test -p service_impl test::billing_period_snapshot_locking::test_snapshot_schema_version_pinned` ROT (Version=2, expected=3)
- `cargo test -p service_impl test::billing_period_snapshot_locking::test_billing_period_value_type_surface_locked` GRUEN (kompiliert, leere Body)

**Wave-2-Forcing-Sequenz:**
- Wave 2 bumpt CURRENT_SNAPSHOT_SCHEMA_VERSION 2→3 → Pin-Test wird GRUEN.
- Wave 2 fuegt `BillingPeriodValueType::UnpaidLeave` hinzu → Match-Test wird COMPILE-ERROR bis der executor den Match-Arm uncomment macht.
  </action>
  <verify>
    <automated>cargo build -p service_impl --tests 2>&amp;1 | tail -10 &amp;&amp; cargo test -p service_impl test::billing_period_snapshot_locking::test_billing_period_value_type_surface_locked 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f service_impl/src/test/billing_period_snapshot_locking.rs` true
    - `grep -c "fn test_snapshot_schema_version_pinned" service_impl/src/test/billing_period_snapshot_locking.rs` == 1
    - `grep -c "fn test_billing_period_value_type_surface_locked" service_impl/src/test/billing_period_snapshot_locking.rs` == 1
    - `grep -c "assert_eq!" service_impl/src/test/billing_period_snapshot_locking.rs` >= 1 (Pin-Map-Assertion erwartet 3)
    - `grep -c "LOCKING TEST" service_impl/src/test/billing_period_snapshot_locking.rs` >= 2 (beide Tests haben den Header-Kommentar)
    - `grep -c "pub mod billing_period_snapshot_locking" service_impl/src/test/mod.rs` == 1
    - `cargo build -p service_impl --tests` exit 0 (kompiliert pre-Wave-2)
    - `cargo test -p service_impl test::billing_period_snapshot_locking::test_snapshot_schema_version_pinned` exit != 0 (ROT als Wave-2-Forcing)
    - `cargo test -p service_impl test::billing_period_snapshot_locking::test_billing_period_value_type_surface_locked` exit 0 (kompiliert + GRUEN)
  </acceptance_criteria>
  <done>
    Locking-Test-Scaffold steht. Pin-Test ist ROT (Wave-2-Forcing). Compiler-Match-Test ist GRUEN aber wird Wave 2 zwingen, den Match um UnpaidLeave zu erweitern.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.3: Stub-Tests fuer derive_hours_for_range, Flag-off und Flag-on</name>
  <files>service_impl/src/test/absence_derive_hours_range.rs, service_impl/src/test/reporting_flag_off_bit_identity.rs, service_impl/src/test/reporting_flag_on_integration.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/mod.rs (Status nach Task 0.2)
    - service_impl/src/test/absence.rs (Zeilen 100-200 — AbsenceDependencies-Struct + build_service-Pattern)
    - service_impl/src/test/billing_period_report.rs (MockDeps-Pattern)
    - service_impl/src/test/reporting_phase2_fixtures.rs (Task 0.1 Output — Fixture-Funktionen)
    - .planning/phases/02-reporting-integration-snapshot-versioning/02-RESEARCH.md (Section "Bit-Identity Test Strategy")
  </read_first>
  <behavior>
    - 3 neue Test-Dateien, jede mit `#[ignore]`-markierten oder `unimplemented!()`-Body-Tests:
      1. `absence_derive_hours_range.rs`: drei Test-Funktionen `test_derive_hours_for_range_basic`, `test_derive_hours_holiday_is_zero`, `test_derive_hours_contract_change` — jeder Body ist `unimplemented!("Wave 1 implements REP-01")` oder `#[ignore = "Wave 1: REP-01"]` mit minimalem Setup
      2. `reporting_flag_off_bit_identity.rs`: ein Test `test_flag_off_produces_identical_values_to_pre_phase2` — Body `unimplemented!("Wave 2: REP-02 bit identity")` oder `#[ignore]`
      3. `reporting_flag_on_integration.rs`: ein Test `test_flag_on_uses_absence_source` — Body `unimplemented!("Wave 2: REP-03 switch path")` oder `#[ignore]`
    - Alle Dateien KOMPILIEREN (kein Compile-Error)
    - Alle Tests sind entweder mit `#[ignore]` markiert ODER der Body panickt mit `unimplemented!()` — beide Optionen sind akzeptabel; `#[ignore]` ist sauberer (Tests laufen nicht im default-run)
    - Imports zeigen die geplanten Mock-Dependencies (auch wenn der Body leer ist) — das macht Wave 1/2 die Implementation einfacher
  </behavior>
  <action>
Erstelle drei Dateien mit Stub-Inhalt:

**Datei 1: `service_impl/src/test/absence_derive_hours_range.rs`**

```rust
//! Stub-Tests fuer `AbsenceService::derive_hours_for_range` (REP-01).
//! Wave 1 implementiert die Bodies; aktuell `#[ignore]` damit `cargo test` gruen bleibt.

use crate::test::reporting_phase2_fixtures::*;

#[ignore = "Wave 1: REP-01 derive_hours_for_range basic case"]
#[tokio::test]
async fn test_derive_hours_for_range_basic() {
    // Wave 1 Implementation:
    // 1. AbsenceDependencies mit MockAbsenceDao + MockSpecialDayService + MockEmployeeWorkDetailsService aufbauen
    // 2. expect_find_by_sales_person -> Vacation+Sick fixtures
    // 3. expect_find_by_sales_person_id (work_details) -> 8h_mon_fri fixture
    // 4. expect_get_by_week (special_day) -> empty Arc<[]>
    // 5. service.derive_hours_for_range(2024-06-03, 2024-06-09, sales_person_id, Auth, None)
    // 6. Erwartung: BTreeMap mit 5 Werktag-Eintraegen (Mo+Mi+Do+Fr Vacation/leer; Di SickLeave 8h)
    // 7. assert_eq! pro Tag auf ResolvedAbsence { category, hours }
    let _ = (fixture_sales_person_id(), fixture_vacation_period(), fixture_sick_period());
    unimplemented!("Wave 1 implements REP-01");
}

#[ignore = "Wave 1: REP-01 holiday=0 case"]
#[tokio::test]
async fn test_derive_hours_holiday_is_zero() {
    // Wave 1: SpecialDayService liefert Feiertag fuer 2024-06-04
    //         -> derive_hours fuer 2024-06-04 = ResolvedAbsence { hours: 0.0, category: <prio> }
    unimplemented!("Wave 1 implements REP-01 holiday case");
}

#[ignore = "Wave 1: REP-01 contract change mid-range"]
#[tokio::test]
async fn test_derive_hours_contract_change() {
    // Wave 1: 2 Vertraege (8h/Tag bis KW 23, 4h/Tag ab KW 24)
    //         -> 2024-06-03..05 = 8h/Tag, 2024-06-10..14 = 4h/Tag
    unimplemented!("Wave 1 implements REP-01 contract change");
}
```

**Datei 2: `service_impl/src/test/reporting_flag_off_bit_identity.rs`**

```rust
//! Stub-Test fuer Flag=off Bit-Identitaet (REP-02). Wave 2 implementiert.

use crate::test::reporting_phase2_fixtures::*;

#[ignore = "Wave 2: REP-02 bit identity flag-off"]
#[tokio::test]
async fn test_flag_off_produces_identical_values_to_pre_phase2() {
    // Wave 2 Implementation:
    // 1. ReportingService-Mocks aufsetzen mit MockFeatureFlagService::expect_is_enabled returning Ok(false)
    // 2. MockAbsenceService::expect_derive_hours_for_range MUSS .times(0) sein (darf nicht aufgerufen werden)
    // 3. ExtraHours-Mocks liefern Vacation 8h, SickLeave 4h, UnpaidLeave 2h
    // 4. service.get_report_for_employee_range(...).await
    // 5. Vergleiche NUR report.vacation_hours, sick_leave_hours, unpaid_leave_hours -- NICHT snapshot_schema_version
    // 6. Erwartet: vacation_hours=8.0, sick_leave_hours=4.0, unpaid_leave_hours=2.0 (identisch zu pre-Phase-2-Verhalten)
    let _ = fixture_sales_person_id();
    unimplemented!("Wave 2 implements REP-02");
}
```

**Datei 3: `service_impl/src/test/reporting_flag_on_integration.rs`**

```rust
//! Stub-Test fuer Flag=on Switch-Integration (REP-03). Wave 2 implementiert.

use crate::test::reporting_phase2_fixtures::*;

#[ignore = "Wave 2: REP-03 flag-on uses AbsencePeriod source"]
#[tokio::test]
async fn test_flag_on_uses_absence_source() {
    // Wave 2 Implementation:
    // 1. MockFeatureFlagService::expect_is_enabled returning Ok(true)
    // 2. MockAbsenceService::expect_derive_hours_for_range returning fixture-derived BTreeMap
    //    (Mo: Vacation 8h, Di: SickLeave 8h, Mi: Vacation 8h)
    // 3. ExtraHours-Mocks liefern noch Vacation/Sick/UnpaidLeave-ExtraHours-Eintraege ABER diese muessen IGNORIERT werden
    // 4. report.vacation_hours = 16.0 (Mo+Mi aus AbsencePeriod), report.sick_leave_hours = 8.0 (Di), report.unpaid_leave_hours = 0.0
    // 5. ExtraWork (Do) bleibt aus ExtraHours-Quelle: report.extra_work = 2.0
    let _ = fixture_sales_person_id();
    unimplemented!("Wave 2 implements REP-03");
}
```

Patche `service_impl/src/test/mod.rs`: fuege drei neue Module-Eintraege ein (alphabetisch sortiert):
- `pub mod absence_derive_hours_range;` (zwischen `pub mod absence;` und `pub mod billing_period;`)
- `pub mod reporting_flag_off_bit_identity;` und `pub mod reporting_flag_on_integration;` (zwischen `pub mod reporting_phase2_fixtures;` und `pub mod sales_person;`)

**Hinweis:** Falls `crate::test::reporting_phase2_fixtures::*` nicht funktioniert (Test-Modul-Path-Konvention abweicht), benutze den vollen Path den die anderen Test-Dateien benutzen — z.B. `super::reporting_phase2_fixtures::*` oder `crate::reporting_phase2_fixtures::*`. Pruefe die existierenden Imports in `service_impl/src/test/billing_period_report.rs`.
  </action>
  <verify>
    <automated>cargo build -p service_impl --tests 2>&amp;1 | tail -10 &amp;&amp; cargo test -p service_impl test::absence_derive_hours_range 2>&amp;1 | tail -10 &amp;&amp; cargo test -p service_impl test::reporting_flag_off_bit_identity 2>&amp;1 | tail -10 &amp;&amp; cargo test -p service_impl test::reporting_flag_on_integration 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f service_impl/src/test/absence_derive_hours_range.rs` true
    - `test -f service_impl/src/test/reporting_flag_off_bit_identity.rs` true
    - `test -f service_impl/src/test/reporting_flag_on_integration.rs` true
    - `grep -c "fn test_derive_hours_for_range_basic" service_impl/src/test/absence_derive_hours_range.rs` == 1
    - `grep -c "fn test_derive_hours_holiday_is_zero" service_impl/src/test/absence_derive_hours_range.rs` == 1
    - `grep -c "fn test_derive_hours_contract_change" service_impl/src/test/absence_derive_hours_range.rs` == 1
    - `grep -c "fn test_flag_off_produces_identical_values_to_pre_phase2" service_impl/src/test/reporting_flag_off_bit_identity.rs` == 1
    - `grep -c "fn test_flag_on_uses_absence_source" service_impl/src/test/reporting_flag_on_integration.rs` == 1
    - `grep -c "#\[ignore" service_impl/src/test/absence_derive_hours_range.rs` >= 3 (oder unimplemented!()-Pattern)
    - `grep -c "pub mod absence_derive_hours_range" service_impl/src/test/mod.rs` == 1
    - `grep -c "pub mod reporting_flag_off_bit_identity" service_impl/src/test/mod.rs` == 1
    - `grep -c "pub mod reporting_flag_on_integration" service_impl/src/test/mod.rs` == 1
    - `cargo build -p service_impl --tests` exit 0
    - `bash -c 'cargo test -p service_impl --no-fail-fast 2>&1 | tee /tmp/cargo-test-out.log; grep -c "^test .* FAILED" /tmp/cargo-test-out.log'` == 1 (genau EIN Test schlaegt fehl — der Pre-Wave-2-Pin-Test)
    - `grep test_snapshot_schema_version_pinned /tmp/cargo-test-out.log | grep -q FAILED` exit 0 (der eine fehlschlagende Test ist test_snapshot_schema_version_pinned; alle Stubs sind ignored, alle bestehenden Tests gruen)
  </acceptance_criteria>
  <done>
    Drei Stub-Test-Dateien existieren und kompilieren. Tests sind `#[ignore]`d, sodass der Workspace-Test-Lauf gruen bleibt fuer alles ausser dem Snapshot-Pin-Test (Wave-2-Forcing). Wave 1 entfernt `#[ignore]` von `absence_derive_hours_range`, Wave 2 entfernt sie von Flag-off/Flag-on und vom Snapshot-Pin-Test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test-Code → Production-Code | Test-Module importieren `BillingPeriodValueType` und `CURRENT_SNAPSHOT_SCHEMA_VERSION` aus den Production-Crates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T (Tampering) | Locking-Test wird umgangen durch Auskommentieren | accept | CLAUDE.md ist die normative Regel; Code-Review-Pflicht; Header-Kommentar erinnert User explizit. |
| T-02-01-02 | I (Information Disclosure) | Test-Fixture enthaelt Sales-Person-Name | accept | Werte sind synthetisch ("Test Person"); kein PII. |
| T-02-01-03 | D (DoS) | Test-Setup-Helper koennten in Production-Code geraten | mitigate | Alle Helper hinter `#[cfg(test)]`; mod.rs-Eintrag in `pub mod test` (test-only). |
</threat_model>

<verification>
- `cargo build -p service_impl --tests` exit 0 (alle 5 neuen Dateien kompilieren)
- `cargo test -p service_impl test::reporting_phase2_fixtures` exit 0 (Fixtures-Datei hat keinen #[test], lediglich pub fns — kompiliert ok)
- `cargo test -p service_impl test::billing_period_snapshot_locking::test_billing_period_value_type_surface_locked` exit 0 (kompiliert + GRUEN)
- `cargo test -p service_impl test::billing_period_snapshot_locking::test_snapshot_schema_version_pinned` exit != 0 (ROT — Wave-2-Forcing). **Dieser Test wird in Wave 2 GRUEN, nicht in Wave 0/1.**
- `cargo test -p service_impl --workspace` ohne `--ignored` exit 0 (alle ignored Stubs werden uebersprungen; nur Pin-Test ist rot, aber das ist erwartet — Phase-2-Wave-Boundary-Verification dokumentiert das)
- Phase-2-Workspace-Build muss zu jedem Zeitpunkt ausser am Pin-Test gruen sein (siehe RESEARCH "Wave 0 Gaps")
</verification>

<success_criteria>
1. Fixtures-Datei + Locking-Test-Datei + 3 Stub-Test-Dateien existieren in `service_impl/src/test/`.
2. mod.rs ist um 5 neue `pub mod`-Eintraege erweitert.
3. `cargo build -p service_impl --tests` ist gruen.
4. Pin-Test `test_snapshot_schema_version_pinned` ist ROT (assertion 2!=3) — Wave-2-Forcing dokumentiert.
5. Compiler-Exhaustive-Match-Test ist GRUEN — wird in Wave 2 COMPILE-ERROR sobald UnpaidLeave-Variante hinzugefuegt wird.
6. Stub-Tests sind `#[ignore]` markiert und blockieren keinen `cargo test`-Lauf.
7. **Single Wave-0-Commit:** Alle 6 Datei-Aenderungen (5 neu + 1 mod.rs-Patch) werden vom Executor als ein Change-Set fuer den jj-commit handler bereit gestellt.
</success_criteria>

<output>
Nach Abschluss schreibt der Executor ein Summary nach `.planning/phases/02-reporting-integration-snapshot-versioning/02-01-SUMMARY.md` mit:
- Liste der 5 erstellten Dateien + 1 mod.rs-Patch
- Status der einzelnen Tests (kompiliert: ja/nein, gruen: ja/nein/ignored)
- Bestaetigung dass `test_snapshot_schema_version_pinned` aktuell ROT ist (intentional Wave-2-Forcing)
- Hinweis fuer Wave 1: Tests in `absence_derive_hours_range.rs` koennen jetzt `#[ignore]` entfernen und implementiert werden
- Hinweis fuer Wave 2: Tests in `reporting_flag_off_bit_identity.rs` und `reporting_flag_on_integration.rs` koennen jetzt `#[ignore]` entfernen; locking-Test wird gruen sobald Bump erfolgt; Match-Test wird COMPILE-ERROR sobald UnpaidLeave-Variante hinzugefuegt wird (auskommentierten Arm aktivieren)
</output>
