---
phase: 03-booking-shift-plan-konflikt-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - service_impl/src/test/mod.rs
  - service_impl/src/test/shiftplan_edit.rs
  - shifty_bin/src/integration_test.rs
  - shifty_bin/src/integration_test/booking_absence_conflict.rs
autonomous: true
nyquist_compliant: false
requirements:
  - BOOK-01
  - BOOK-02
  - PLAN-01
must_haves:
  truths:
    - "Eine neue Test-Datei `service_impl/src/test/shiftplan_edit.rs` existiert mit `#[ignore]`-markierten Tests, die die Pflicht-Coverage aus 03-VALIDATION.md (Reverse-Warning + copy_week-Aggregation + _forbidden) namentlich auflisten."
    - "Eine neue Integration-Test-Datei `shifty_bin/src/integration_test/booking_absence_conflict.rs` existiert mit `#[ignore]`-Markern für Cross-Source + Pitfall-1 (SC4) + copy_week-Aggregation."
    - "Modul-Wiring (`pub mod shiftplan_edit;` in `service_impl/src/test/mod.rs`; `mod booking_absence_conflict;` in `shifty_bin/src/integration_test.rs`) ist ergänzt."
    - "`cargo build --workspace` ist GRÜN (Test-Stubs kompilieren mit `#[ignore]` + `unimplemented!()`-Bodies); `cargo test --workspace` zeigt die ignorierten Tests in der Liste, aber bricht nicht."
  artifacts:
    - path: "service_impl/src/test/shiftplan_edit.rs"
      provides: "Test-Modul mit 6 #[ignore]-Stubs für Wave 3 (Reverse-Warning, copy_week-Aggregation, _forbidden, Pitfall-1)"
      contains: "test_book_slot_warning_on_absence_day"
    - path: "shifty_bin/src/integration_test/booking_absence_conflict.rs"
      provides: "Integration-Test-Stubs für Cross-Source + Pitfall-1 (SC4)"
      contains: "test_double_source_two_warnings_one_booking"
  key_links:
    - from: "service_impl/src/test/mod.rs"
      to: "service_impl/src/test/shiftplan_edit.rs"
      via: "pub mod shiftplan_edit;"
      pattern: "pub mod shiftplan_edit"
    - from: "shifty_bin/src/integration_test.rs"
      to: "shifty_bin/src/integration_test/booking_absence_conflict.rs"
      via: "mod booking_absence_conflict;"
      pattern: "mod booking_absence_conflict"
---

<objective>
Wave-0-Test-Scaffolding: Erzeuge die NEUEN Test-Modul-Files mit `#[ignore]`-markierten Test-Stubs für ALLE Phase-3-Pflicht-Tests aus `03-VALIDATION.md`. Wire die Module korrekt ein, damit `cargo build --workspace` GRÜN bleibt. Forciere damit Wave 1+2+3, die Tests zu aktivieren (Tests müssen sowohl `#[ignore]` entfernt werden als auch `unimplemented!()`-Body durch echten Test ersetzen — Compiler hilft nicht, aber die Test-Liste ist sichtbar).

Purpose: Ohne Wave-0-Stubs gibt es keine "MISSING"-Marker für Wave 3 — Tests könnten vergessen werden. Wave 0 etabliert die Test-Surface ein für allemal.

Output: 4 patches/Dateien (zwei NEW, zwei PATCH).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Test-Wiring-Pattern aus Phase 1/2 — direkt wiederverwendbar -->

Aus `service_impl/src/test/mod.rs:1-50`:
```rust
#[cfg(test)]
pub mod absence;
#[cfg(test)]
pub mod absence_derive_hours_range;
// ...
#[cfg(test)]
pub mod sales_person_unavailable;
#[cfg(test)]
pub mod shiftplan;        // <-- NEU davor zwischen sales_person_unavailable und shiftplan
#[cfg(test)]
pub mod shiftplan_catalog;
```

Aus `shifty_bin/src/integration_test.rs:1432-1437`:
```rust
#[cfg(test)]
mod absence_period;
#[cfg(test)]
mod billing_period_custom_reports;
#[cfg(test)]
mod billing_period_snapshot_versioning;
mod dev_seed;
```

Stub-Test-Pattern (aus `#[ignore]`-Konvention):
```rust
#[tokio::test]
#[ignore = "wave-3 — implement after Warning-enum + ShiftplanEditService::book_slot_with_conflict_check landen"]
async fn test_book_slot_warning_on_absence_day() {
    unimplemented!("wave-3-target");
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Service-Impl-Test-Modul `shiftplan_edit` mit allen Reverse-Warning-Stubs anlegen + wiren</name>
  <read_first>
    - service_impl/src/test/mod.rs
    - service_impl/src/test/booking.rs (Mock-Setup-Idiom; Z. 113-192)
    - service_impl/src/test/shiftplan.rs (Mock-DI-Pattern; Z. 59-100)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md (Tabelle "Per-Task Verification Map" — exakte Test-Namen)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (§ "service_impl/src/test/shiftplan_edit.rs (NEW)" — Z. 1387-1518, Modul-Layout)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-04, D-Phase3-12, D-Phase3-15, D-Phase3-18)
    - shifty-backend/CLAUDE.md (§ "Service-Tier-Konventionen: Basic vs. Business-Logic Services" — referenz für die Doc-Comments)
  </read_first>
  <files>service_impl/src/test/shiftplan_edit.rs, service_impl/src/test/mod.rs</files>
  <action>
    1. **Erstelle NEUE Datei** `service_impl/src/test/shiftplan_edit.rs` mit folgendem **vollständigen** Inhalt (kein Mock-Setup nötig — alle Tests sind Stubs):

    ```rust
    //! Wave-0-Stubs für Phase-3-Reverse-Warning-Tests.
    //!
    //! ALLE Tests sind aktuell `#[ignore]` + `unimplemented!()`-Body. Wave 3
    //! muss pro Test:
    //!  1. `#[ignore]`-Attribut ENTFERNEN.
    //!  2. `unimplemented!()` durch echten Mock-DI-Setup + Assertions ersetzen
    //!     (Pattern aus `service_impl/src/test/booking.rs:113-192` +
    //!      `service_impl/src/test/shiftplan.rs:59-100`).
    //!
    //! Abdeckung pro Test verbindlich aus 03-VALIDATION.md:
    //!  - test_book_slot_warning_on_absence_day        (BOOK-02 / D-Phase3-14 BookingOnAbsenceDay)
    //!  - test_book_slot_warning_on_manual_unavailable (BOOK-02 / D-Phase3-14 BookingOnUnavailableDay)
    //!  - test_book_slot_no_warning_when_softdeleted_absence (SC4 / Pitfall-1)
    //!  - test_copy_week_aggregates_warnings           (BOOK-02 / D-Phase3-02, D-Phase3-15: KEINE De-Dup)
    //!  - test_book_slot_with_conflict_check_forbidden (D-Phase3-12 HR ∨ self)
    //!  - test_copy_week_with_conflict_check_forbidden (D-Phase3-12 HR ∨ self)

    #[tokio::test]
    #[ignore = "wave-3 target — Warning enum + ShiftplanEditService::book_slot_with_conflict_check"]
    async fn test_book_slot_warning_on_absence_day() {
        unimplemented!("wave-3 — see 03-PATTERNS.md § 'service_impl/src/test/shiftplan_edit.rs'");
    }

    #[tokio::test]
    #[ignore = "wave-3 target — Warning enum + sales_person_unavailable lookup"]
    async fn test_book_slot_warning_on_manual_unavailable() {
        unimplemented!("wave-3 — see 03-PATTERNS.md");
    }

    #[tokio::test]
    #[ignore = "wave-3 target — Pitfall-1 / SC4: soft-deleted AbsencePeriod MUST NOT trigger warning"]
    async fn test_book_slot_no_warning_when_softdeleted_absence() {
        unimplemented!("wave-3 — Mock returns empty Vec (DAO filtert deleted IS NULL)");
    }

    #[tokio::test]
    #[ignore = "wave-3 target — D-Phase3-02 + D-Phase3-15: aggregate warnings, KEINE De-Dup"]
    async fn test_copy_week_aggregates_warnings() {
        unimplemented!("wave-3 — 3 source bookings, 2 on absence days → 2 warnings + 3 copied");
    }

    #[tokio::test]
    #[ignore = "wave-3 target — D-Phase3-12 Permission HR ∨ self"]
    async fn test_book_slot_with_conflict_check_forbidden() {
        unimplemented!("wave-3 — both permission probes return Forbidden");
    }

    #[tokio::test]
    #[ignore = "wave-3 target — D-Phase3-12 Permission HR ∨ self"]
    async fn test_copy_week_with_conflict_check_forbidden() {
        unimplemented!("wave-3 — both permission probes return Forbidden");
    }
    ```

    2. **Patch** `service_impl/src/test/mod.rs`: Füge nach der bestehenden Zeile `pub mod shiftplan;` (Z. 32) eine neue Zeile ein, exakt:
    ```rust
    #[cfg(test)]
    pub mod shiftplan_edit;
    ```
    Beachte das `#[cfg(test)]`-Attribut auf eigener Zeile — gleiche Form wie alle anderen `pub mod`-Einträge in der Datei.

    3. KEINE Anpassungen an anderen Files — alle bestehenden Tests bleiben unangetastet.

    Note: Die `#[ignore]`-Marker sorgen dafür, dass `cargo test` die Tests in der Liste anzeigt, aber sie nicht laufen lässt; `unimplemented!()` ist trotzdem im Body, damit ein versehentliches Entfernen des `#[ignore]` ohne Implementation sofort knallt.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5</automated>
    <automated>cargo test --workspace -- --list 2>&1 | grep -E "test_book_slot_warning_on_absence_day|test_book_slot_warning_on_manual_unavailable|test_book_slot_no_warning_when_softdeleted_absence|test_copy_week_aggregates_warnings|test_book_slot_with_conflict_check_forbidden|test_copy_week_with_conflict_check_forbidden" | wc -l</automated>
  </verify>
  <done>
    - cargo build --workspace exits 0
    - 6 Stub-Tests sind in der `cargo test -- --list`-Ausgabe (zweites Verify-Kommando MUSS 6 zurückgeben)
    - service_impl/src/test/mod.rs enthält `pub mod shiftplan_edit;`
    - service_impl/src/test/shiftplan_edit.rs existiert mit ≥6 `#[ignore]`-Stub-Tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration-Test-Datei `booking_absence_conflict.rs` mit Cross-Source + Pitfall-1-Stubs anlegen + wiren</name>
  <read_first>
    - shifty_bin/src/integration_test.rs (Z. 1430-1440 — Modul-Wiring-Pattern)
    - shifty_bin/src/integration_test/absence_period.rs (Phase-1-TestSetup-Pattern; Z. 1-100)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md (Test-IDs 03-XX-07, 03-XX-08, 03-XX-14)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (§ "shifty_bin/src/integration_test/booking_absence_conflict.rs (NEW)" — Z. 1663-1797)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (Pitfall 6 / SC4)
  </read_first>
  <files>shifty_bin/src/integration_test/booking_absence_conflict.rs, shifty_bin/src/integration_test.rs</files>
  <action>
    1. **Erstelle NEUE Datei** `shifty_bin/src/integration_test/booking_absence_conflict.rs` mit folgendem **vollständigen** Inhalt:

    ```rust
    //! Wave-0-Stubs für Phase-3-Cross-Source-End-to-End-Tests.
    //!
    //! ALLE Tests sind aktuell `#[ignore]` + `unimplemented!()`-Body. Wave 5
    //! (Final-Tests-Plan) muss:
    //!  1. `#[ignore]`-Attribut entfernen.
    //!  2. `TestSetup`-Setup analog `shifty_bin/src/integration_test/absence_period.rs`
    //!     bauen.
    //!  3. Den NEUEN konflikt-aware-Endpunkt
    //!     (`ShiftplanEditService::book_slot_with_conflict_check`) gegen echte
    //!     In-Memory-SQLite + REST-Layer treiben.
    //!
    //! Abdeckung pro Test verbindlich aus 03-VALIDATION.md:
    //!  - test_double_source_two_warnings_one_booking         (BOOK-02 / Cross-Source)
    //!  - test_softdeleted_absence_no_warning_no_marker       (SC4 / Pitfall-1)
    //!  - test_copy_week_three_bookings_two_warnings          (BOOK-02 / D-Phase3-02)
    //!  - test_shiftplan_marker_softdeleted_absence_none      (PLAN-01 + SC4 — Read-Pfad)

    #[tokio::test]
    #[ignore = "wave-5 target — Cross-Source: AbsencePeriod + sales_person_unavailable on same day → 2 warnings"]
    async fn test_double_source_two_warnings_one_booking() {
        unimplemented!("wave-5 — see 03-PATTERNS.md § 'shifty_bin/src/integration_test/booking_absence_conflict.rs'");
    }

    #[tokio::test]
    #[ignore = "wave-5 target — Pitfall-1 / SC4: soft-deleted AbsencePeriod produces NO warning"]
    async fn test_softdeleted_absence_no_warning_no_marker() {
        unimplemented!("wave-5 — assert result.warnings.is_empty() AND ShiftplanDay.unavailable.is_none()");
    }

    #[tokio::test]
    #[ignore = "wave-5 target — D-Phase3-02: 3 source bookings, 2 on absence days → 3 copied + 2 warnings"]
    async fn test_copy_week_three_bookings_two_warnings() {
        unimplemented!("wave-5 — full-stack copy_week_with_conflict_check");
    }

    #[tokio::test]
    #[ignore = "wave-5 target — PLAN-01 + SC4: ShiftplanDay-Marker None für soft-deleted absence"]
    async fn test_shiftplan_marker_softdeleted_absence_none() {
        unimplemented!("wave-5 — get_shiftplan_week_for_sales_person check");
    }
    ```

    2. **Patch** `shifty_bin/src/integration_test.rs`: Suche die bestehende Zeile `mod dev_seed;` (Z. ~1437); füge UNMITTELBAR DAVOR ein:
    ```rust
    #[cfg(test)]
    mod booking_absence_conflict;
    ```
    Beachte: `mod dev_seed;` selbst hat KEIN `#[cfg(test)]` — das ist OK, dev_seed ist anders strukturiert. `booking_absence_conflict` MUSS mit `#[cfg(test)]` gegated werden, weil es nur Test-Code enthält.

    3. KEINE anderen Änderungen.

    Note: `TestSetup`-Imports etc. werden erst in Wave 5 hinzugefügt — Wave 0 hat NUR die ignorierten Stub-Tests, damit `cargo build --workspace` GRÜN bleibt.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5</automated>
    <automated>cargo test -p shifty_bin --tests -- --list 2>&1 | grep -E "test_double_source_two_warnings_one_booking|test_softdeleted_absence_no_warning_no_marker|test_copy_week_three_bookings_two_warnings|test_shiftplan_marker_softdeleted_absence_none" | wc -l</automated>
    <automated>grep -c "mod booking_absence_conflict" /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty_bin/src/integration_test.rs</automated>
  </verify>
  <done>
    - cargo build --workspace exits 0
    - 4 Integration-Test-Stubs sind in der test-list (zweites Verify-Kommando MUSS 4 zurückgeben)
    - drittes Verify-Kommando MUSS ≥1 zurückgeben (mod-Eintrag vorhanden)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keiner in Wave 0 | Wave 0 berührt nur Test-Files; keine Auth-, Input- oder DB-Pfade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-W0-01 | Tampering | `service_impl/src/test/shiftplan_edit.rs` Stubs | accept | Tests sind `#[ignore]` + `unimplemented!()` — bei versehentlichem Aktivieren ohne Implementation panic'd `cargo test` sofort, kein silent-pass-Risiko. |
| T-3-W0-02 | Information Disclosure | Stub-Doc-Comments referenzieren CONTEXT.md/PATTERNS.md | accept | Doc-Comments sind im Repo (jj-versioniert); kein externes Leak. |
</threat_model>

<verification>
Wave-0-Verifikation:
- `cargo build --workspace` GRÜN
- `cargo test --workspace -- --list` listet die 6 service_impl- und 4 shifty_bin-Stubs
- `cargo test --workspace` läuft GRÜN (alle ignorierten Tests werden NICHT ausgeführt; Status: `running 0 tests` für die ignorierten + die bestehenden Phase-1+2-Tests laufen normal weiter)
- KEINE bestehenden Tests gebrochen (Phase 1+2 = 381+ passing tests bleiben grün)
</verification>

<success_criteria>
- [ ] `service_impl/src/test/shiftplan_edit.rs` existiert mit ≥6 `#[ignore]`-Stub-Tests, die exakt die Namen aus 03-VALIDATION.md tragen
- [ ] `shifty_bin/src/integration_test/booking_absence_conflict.rs` existiert mit ≥4 `#[ignore]`-Stub-Tests
- [ ] `service_impl/src/test/mod.rs` enthält `pub mod shiftplan_edit;` mit `#[cfg(test)]`
- [ ] `shifty_bin/src/integration_test.rs` enthält `mod booking_absence_conflict;` mit `#[cfg(test)]`
- [ ] `cargo build --workspace` GRÜN
- [ ] `cargo test --workspace` GRÜN — Phase-1+2-Tests unverändert grün; neue Tests `ignored`
- [ ] `nyquist_compliant: true` ist in 03-VALIDATION.md NACH diesem Plan setzbar (Wave 0 Requirements erfüllt)
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-shift-plan-konflikt-integration/03-01-SUMMARY.md` summarizing:
- Files created (2 NEW): `service_impl/src/test/shiftplan_edit.rs`, `shifty_bin/src/integration_test/booking_absence_conflict.rs`
- Files patched (2 PATCH): `service_impl/src/test/mod.rs`, `shifty_bin/src/integration_test.rs`
- Total stub tests added: 10 (6 service_impl + 4 shifty_bin)
- Status of `cargo build --workspace` and `cargo test --workspace`
- Note for Wave 1+2+3 executors: which test names map to which Wave
</output>
