---
phase: 03-booking-shift-plan-konflikt-integration
plan: 06
type: execute
wave: 5
depends_on:
  - 05
files_modified:
  - service_impl/src/test/shiftplan_edit.rs
  - service_impl/src/test/shiftplan.rs
  - service_impl/src/test/absence.rs
  - shifty_bin/src/integration_test/booking_absence_conflict.rs
  - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md
  - .planning/ROADMAP.md
autonomous: true
nyquist_compliant: true
requirements:
  - BOOK-01
  - BOOK-02
  - PLAN-01
must_haves:
  truths:
    - "Alle 6 Stub-Tests aus `service_impl/src/test/shiftplan_edit.rs` (Plan 01) sind aktiviert (`#[ignore]` entfernt) und laufen GRÜN — Reverse-Warning + copy_week-Aggregation + _forbidden + Pitfall-1."
    - "`service_impl/src/test/absence.rs` hat 4 neue Forward-Warning-Tests: `test_create_warning_for_booking_in_range`, `test_create_warning_for_manual_unavailable_in_range`, `test_update_returns_warnings_for_full_new_range` (D-Phase3-04), `test_find_overlapping_for_booking_forbidden`."
    - "`service_impl/src/test/shiftplan.rs` hat 5 neue per-sales-person-Tests: marker_absence_only, marker_manual_only, marker_both (D-Phase3-10), marker_softdeleted_absence_none (Pitfall-1 / SC4), forbidden."
    - "Alle 4 Stub-Tests aus `shifty_bin/src/integration_test/booking_absence_conflict.rs` (Plan 01) sind aktiviert und laufen GRÜN — Cross-Source-Doppel-Quelle, Pitfall-1-full-stack (SC4), copy_week-Aggregation, ShiftplanDay-Marker-Pitfall-1."
    - "`cargo test --workspace` GRÜN (alle Phase-1+2-Tests + alle neuen Phase-3-Tests + alle Stub-Tests aus Plan 01)."
    - "`cargo run` startet ohne Panic; OpenAPI exposes alle Phase-3-Endpunkte und -Schemas."
    - "**D-Phase3-18 Regression-Lock final**: `jj diff service/src/booking.rs service_impl/src/booking.rs rest/src/booking.rs service_impl/src/test/booking.rs` ist leer; alle bestehenden BookingService-Tests laufen unverändert grün."
    - "`03-VALIDATION.md`-Tabelle ist aktualisiert: alle Test-IDs auf Status ✅ green; `nyquist_compliant: true` setzbar."
    - "`.planning/ROADMAP.md` Phase-3-Sektion ist aktualisiert: 6-Pläne-Liste finalisiert, Phase-Status korrekt."
  artifacts:
    - path: "service_impl/src/test/shiftplan_edit.rs"
      provides: "6 aktivierte Reverse-Warning-Tests mit echtem Mock-DI-Setup"
      contains: "test_book_slot_warning_on_absence_day"
    - path: "service_impl/src/test/absence.rs"
      provides: "4 neue Forward-Warning-Tests"
      contains: "test_create_warning_for_booking_in_range"
    - path: "service_impl/src/test/shiftplan.rs"
      provides: "5 neue per-sales-person-Tests"
      contains: "test_per_sales_person_marker_both"
    - path: "shifty_bin/src/integration_test/booking_absence_conflict.rs"
      provides: "4 aktivierte Cross-Source + Pitfall-1 Integration-Tests"
      contains: "test_double_source_two_warnings_one_booking"
    - path: ".planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md"
      provides: "Test-ID-Tabelle alle ✅ green, nyquist_compliant: true, Approval: approved"
      contains: "nyquist_compliant: true"
    - path: ".planning/ROADMAP.md"
      provides: "Phase-3 Plans-Liste auf 6 Pläne finalisiert + Status"
      contains: "Phase 3"
  key_links:
    - from: "service_impl/src/test/shiftplan_edit.rs"
      to: "service::shiftplan_edit::ShiftplanEditService"
      via: "MockShiftplanEditService + build_dependencies"
      pattern: "MockShiftplanEditService|build_dependencies"
    - from: "shifty_bin/src/integration_test/booking_absence_conflict.rs"
      to: "rest_state.shiftplan_edit_service().book_slot_with_conflict_check"
      via: "TestSetup + REST-Layer-State-Pattern"
      pattern: "shiftplan_edit_service.*book_slot_with_conflict_check"
---

<objective>
Wave-5-Final-Tests + Verifikation: Aktiviere alle Stub-Tests aus Plan 01 mit echten Mock-/Integration-Bodies. Ergänze Forward-Warning-Tests in `test/absence.rs` (für AbsenceService-Erweiterungen aus Plan 03) und per-sales-person-Tests in `test/shiftplan.rs` (für ShiftplanViewService-Erweiterungen aus Plan 04). Verifiziere D-Phase3-18 Regression-Lock final + alle 4 Success-Criteria aus ROADMAP.md (SC1 Forward-Warning, SC2 Reverse-Warning, SC3 Shift-Plan-Markierung, SC4 soft-deleted-Filter). Schließe `03-VALIDATION.md` mit allen Tests auf grün ab und finalisiere die ROADMAP-Phase-3-Sektion.

Purpose: Phase 3 ist erst dann verifiziert, wenn jeder Pitfall + jede Cross-Source-Konstellation durch echte Tests gegen den NEUEN Endpunkt getrieben wurde. Plan 06 schließt das.

Output: 6 Files patched (4 Test-Files vervollständigt + Stubs aktiviert; 03-VALIDATION.md + ROADMAP.md finalisiert).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-05-SUMMARY.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-04-SUMMARY.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-03-SUMMARY.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-02-SUMMARY.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-01-SUMMARY.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Mock-DI-Patterns aus Phase 1+2 (verbatim wiederverwendbar) -->

`service_impl/src/test/booking.rs:113-192` — `build_dependencies` mit Mock-Setup für:
- MockSlotService.expect_get_slot
- MockUuidService.expect_new_uuid
- MockSalesPersonService.expect_check_permission, .expect_verify_user_is_sales_person
- MockTransactionDao.expect_use_transaction (returns Ok(MockTransaction)), .expect_commit
- MockBookingDao.expect_*

`service_impl/src/test/shiftplan.rs:59-100` — `ShiftplanViewServiceDependencies` Struct + `build_dependencies` + `build_service`-Pattern.

`service_impl/src/test/error_test.rs` — `pub fn test_forbidden<T>(result: &Result<T, ServiceError>)` Helper.

`service_impl/src/test/absence.rs:121-300` — Phase-1+2 AbsenceService-Tests (AbsenceDependencies + Mocks). Plan 03 hat hier schon BookingService + SalesPersonUnavailableService + SlotService Mocks ergänzt — Plan 06 ergänzt nur Tests, nicht Struct-Erweiterungen.

<!-- Integration-Test-Pattern aus Phase 1 -->

`shifty_bin/src/integration_test/absence_period.rs` — `TestSetup::new().await`, dann `test_setup.rest_state.absence_service()....` Direkter Service-Call (kein HTTP-Round-Trip nötig). `RestStateImpl` aus `shifty_bin/src/main.rs` ist über `TestSetup.rest_state` zugreifbar.

```rust
// Pattern aus absence_period.rs
let test_setup = TestSetup::new().await;
let sp = create_sales_person(&test_setup, "Name").await;
let result = test_setup.rest_state.absence_service().create(...).await.unwrap();
```

<!-- Booking-Konstruktion aus existing Tests -->

`service_impl/src/test/booking.rs` zeigt das `Booking::default()`/`Booking { id, sales_person_id, slot_id, year, calendar_week }`-Pattern. Genaue Felder verifizieren mit `grep "pub struct Booking" service/src/booking.rs`.

<!-- AbsenceCategory + AbsencePeriod Konstruktion -->

`AbsencePeriod { id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, version }`. `AbsenceCategory::Vacation` etc.

<!-- SalesPersonUnavailable -->

`SalesPersonUnavailable { id, sales_person_id, year, calendar_week, day_of_week, version, deleted, created }` — verifiziere mit grep.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `service_impl/src/test/shiftplan_edit.rs` — Stub-Tests aktivieren mit vollem Mock-DI-Setup</name>
  <read_first>
    - service_impl/src/test/shiftplan_edit.rs (aktueller Stub-Stand aus Plan 01)
    - service_impl/src/test/booking.rs (Z. 113-192 — Mock-DI-Setup)
    - service_impl/src/test/shiftplan.rs (Z. 59-100 — Dependencies-Struct)
    - service_impl/src/test/error_test.rs (test_forbidden-Helper)
    - service/src/shiftplan_edit.rs (BookingCreateResult, CopyWeekResult aus Plan 04)
    - service/src/warning.rs (Warning aus Plan 02)
    - service_impl/src/shiftplan_edit.rs (Plan 04 — gen_service_impl-Form mit AbsenceService-Dep)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 1387-1525 — Test-File-Skizze)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md (Test-IDs 03-XX-04, 05, 06, 13)
  </read_first>
  <behavior>
    - Test 1: AbsencePeriod-Mock-Hit + leere ManualUnavailables + BookingService::create-Mock → BookingCreateResult mit 1× Warning::BookingOnAbsenceDay.
    - Test 2: leere AbsencePeriods + ManualUnavailable-Mock-Hit (matching day_of_week) → 1× Warning::BookingOnUnavailableDay.
    - Test 3: leere AbsencePeriods + leere ManualUnavailables (Pitfall-1: soft-deleted bereits gefiltert) → BookingCreateResult.warnings.is_empty().
    - Test 4: copy_week — 3 Source-Bookings, 2 mit Absence-Hit → CopyWeekResult.copied_bookings.len() == 3 + warnings.len() == 2.
    - Test 5: Permission-Mocks beide Forbidden → test_forbidden.
  </behavior>
  <files>service_impl/src/test/shiftplan_edit.rs</files>
  <action>
    1. **REPLACE** `service_impl/src/test/shiftplan_edit.rs` (überschreibe komplett — Stub-Form aus Plan 01 wird durch reale Tests ersetzt). Schreib-Pattern (Plan-Phase erwartet kompilierbares Code; Executor passt Mock-Argument-Typen an die echten Sigs an):

    ```rust
    //! Service-Tests für `ShiftplanEditServiceImpl::book_slot_with_conflict_check`
    //! und `copy_week_with_conflict_check` (Phase 3, Wave 5).

    use std::sync::Arc;

    use dao::{MockTransaction, MockTransactionDao};
    use mockall::predicate::{always, eq};
    use service::{
        absence::{AbsenceCategory, AbsencePeriod, MockAbsenceService},
        booking::{Booking, MockBookingService},
        carryover::MockCarryoverService,
        employee_work_details::MockEmployeeWorkDetailsService,
        extra_hours::MockExtraHoursService,
        permission::{Authentication, HR_PRIVILEGE},
        reporting::MockReportingService,
        sales_person::MockSalesPersonService,
        sales_person_unavailable::{MockSalesPersonUnavailableService, SalesPersonUnavailable},
        shiftplan_edit::{BookingCreateResult, ShiftplanEditService},
        slot::{MockSlotService, Slot},
        uuid_service::MockUuidService,
        warning::Warning,
        MockPermissionService, ServiceError,
    };
    use shifty_utils::DayOfWeek;
    use time::macros::date;
    use uuid::{uuid, Uuid};

    use crate::shiftplan_edit::{ShiftplanEditServiceDeps, ShiftplanEditServiceImpl};
    use crate::test::error_test::test_forbidden;

    // ID-Helpers
    fn default_sales_person_id() -> Uuid { uuid!("BB000000-0000-0000-0000-000000000001") }
    fn default_slot_id() -> Uuid { uuid!("7A7FF57A-782B-4C2E-A68B-4E2D81D79380") }
    fn default_absence_id() -> Uuid { uuid!("AB000000-0000-0000-0000-000000000001") }
    fn default_unavailable_id() -> Uuid { uuid!("DD000000-0000-0000-0000-000000000001") }
    fn default_booking_id() -> Uuid { uuid!("CEA260A0-112B-4970-936C-F7E529955BD0") }

    // Fixtures
    fn fixture_slot_monday() -> Slot {
        // Plan-Phase: an existing Slot-Struct anpassen — verifiziere mit grep "pub struct Slot" service/src/slot.rs
        // Felder vermutlich: { id, day_of_week, from, to, valid_from, valid_to?, deleted?, version, ... }
        unimplemented!("executor sets up fixture matching service::slot::Slot fields")
    }

    fn fixture_booking_monday_kw18_2026() -> Booking {
        // exakte Felder aus service::booking::Booking
        unimplemented!("executor: id=Uuid::nil(), sales_person_id=default_sales_person_id(), slot_id=default_slot_id(), year=2026, calendar_week=18, ...")
    }

    fn fixture_absence_period_kw18_monday() -> AbsencePeriod {
        AbsencePeriod {
            id: default_absence_id(),
            logical_id: default_absence_id(),
            sales_person_id: default_sales_person_id(),
            category: AbsenceCategory::Vacation,
            from_date: date!(2026 - 04 - 27),  // KW 18 Monday 2026
            to_date: date!(2026 - 04 - 30),
            description: Arc::from(""),
            created: time::PrimitiveDateTime::new(date!(2026 - 04 - 01), time::Time::MIDNIGHT),
            deleted: None,
            version: Uuid::nil(),
        }
    }

    fn fixture_manual_unavailable_kw18_monday() -> SalesPersonUnavailable {
        // Plan-Phase: an existing SalesPersonUnavailable-Struct anpassen
        unimplemented!("executor: sales_person_id=default_sales_person_id(), year=2026, calendar_week=18, day_of_week=Monday, ...")
    }

    // Mock-DI-Struktur (Pattern aus shiftplan.rs:59-100)
    pub struct ShiftplanEditDependencies {
        pub permission_service: MockPermissionService,
        pub slot_service: MockSlotService,
        pub booking_service: MockBookingService,
        pub carryover_service: MockCarryoverService,
        pub reporting_service: MockReportingService,
        pub sales_person_service: MockSalesPersonService,
        pub sales_person_unavailable_service: MockSalesPersonUnavailableService,
        pub employee_work_details_service: MockEmployeeWorkDetailsService,
        pub extra_hours_service: MockExtraHoursService,
        pub uuid_service: MockUuidService,
        pub transaction_dao: MockTransactionDao,
        pub absence_service: MockAbsenceService,
    }

    impl ShiftplanEditServiceDeps for ShiftplanEditDependencies {
        type Context = ();
        type Transaction = MockTransaction;
        type PermissionService = MockPermissionService;
        type SlotService = MockSlotService;
        type BookingService = MockBookingService;
        type CarryoverService = MockCarryoverService;
        type ReportingService = MockReportingService;
        type SalesPersonService = MockSalesPersonService;
        type SalesPersonUnavailableService = MockSalesPersonUnavailableService;
        type EmployeeWorkDetailsService = MockEmployeeWorkDetailsService;
        type ExtraHoursService = MockExtraHoursService;
        type UuidService = MockUuidService;
        type TransactionDao = MockTransactionDao;
        type AbsenceService = MockAbsenceService;
    }

    fn build_dependencies() -> ShiftplanEditDependencies {
        let mut transaction_dao = MockTransactionDao::new();
        transaction_dao.expect_use_transaction().returning(|_| Ok(MockTransaction::new()));
        transaction_dao.expect_commit().returning(|_| Ok(()));

        let mut permission_service = MockPermissionService::new();
        permission_service.expect_check_permission().returning(|_, _| Ok(()));

        let mut sales_person_service = MockSalesPersonService::new();
        sales_person_service.expect_verify_user_is_sales_person().returning(|_, _, _| Ok(()));

        let mut slot_service = MockSlotService::new();
        slot_service.expect_get_slot().returning(|_, _, _| Ok(fixture_slot_monday()));

        let mut booking_service = MockBookingService::new();
        booking_service.expect_create().returning(|b, _, _| {
            // BookingService::create echtes Persistierung: Booking mit echter ID zurückgeben
            let mut persisted = (*b).clone();
            persisted.id = default_booking_id();
            Ok(persisted)
        });
        booking_service.expect_get_for_week().returning(|_, _, _, _| Ok(Arc::from([])));

        let mut absence_service = MockAbsenceService::new();
        absence_service
            .expect_find_overlapping_for_booking()
            .returning(|_, _, _, _| Ok(Arc::from([])));

        let mut sales_person_unavailable_service = MockSalesPersonUnavailableService::new();
        sales_person_unavailable_service
            .expect_get_by_week_for_sales_person()
            .returning(|_, _, _, _, _| Ok(Arc::from([])));

        ShiftplanEditDependencies {
            permission_service,
            slot_service,
            booking_service,
            carryover_service: MockCarryoverService::new(),
            reporting_service: MockReportingService::new(),
            sales_person_service,
            sales_person_unavailable_service,
            employee_work_details_service: MockEmployeeWorkDetailsService::new(),
            extra_hours_service: MockExtraHoursService::new(),
            uuid_service: MockUuidService::new(),
            transaction_dao,
            absence_service,
        }
    }

    fn build_service(deps: ShiftplanEditDependencies) -> ShiftplanEditServiceImpl<ShiftplanEditDependencies> {
        // Pattern aus shiftplan.rs:80-100 — adapt to ShiftplanEditServiceImpl-Felder
        unimplemented!("executor: konstruiere ShiftplanEditServiceImpl { permission_service: deps.permission_service.into(), slot_service: deps.slot_service.into(), ... }")
    }

    #[tokio::test]
    async fn test_book_slot_warning_on_absence_day() {
        let mut deps = build_dependencies();
        // Override AbsenceService-Mock — 1 AbsencePeriod
        deps.absence_service = MockAbsenceService::new();
        deps.absence_service
            .expect_find_overlapping_for_booking()
            .returning(|_, _, _, _| Ok(Arc::from([fixture_absence_period_kw18_monday()])));
        // Re-attach permission/sales_person mocks (each Mock instance is fresh)
        // Plan-Phase: executor sets up the override-pattern completely

        let service = build_service(deps);
        let result = service
            .book_slot_with_conflict_check(&fixture_booking_monday_kw18_2026(), Authentication::Full, None)
            .await
            .expect("should succeed");
        assert_eq!(result.warnings.len(), 1, "expected 1 warning for absence overlap");
        assert!(matches!(
            &result.warnings[0],
            Warning::BookingOnAbsenceDay { .. }
        ));
        assert_eq!(result.booking.id, default_booking_id());
    }

    #[tokio::test]
    async fn test_book_slot_warning_on_manual_unavailable() {
        let mut deps = build_dependencies();
        deps.sales_person_unavailable_service = MockSalesPersonUnavailableService::new();
        deps.sales_person_unavailable_service
            .expect_get_by_week_for_sales_person()
            .returning(|_, _, _, _, _| Ok(Arc::from([fixture_manual_unavailable_kw18_monday()])));

        let service = build_service(deps);
        let result = service
            .book_slot_with_conflict_check(&fixture_booking_monday_kw18_2026(), Authentication::Full, None)
            .await
            .expect("should succeed");
        assert_eq!(result.warnings.len(), 1);
        assert!(matches!(
            &result.warnings[0],
            Warning::BookingOnUnavailableDay { .. }
        ));
    }

    #[tokio::test]
    async fn test_book_slot_no_warning_when_softdeleted_absence() {
        // Pitfall-1: deleted-Filter passiert IM DAO (Plan 02). Mock returnt einfach
        // empty Vec — verifiziert die Default-Build-Dependencies-Form.
        let deps = build_dependencies();
        let service = build_service(deps);
        let result = service
            .book_slot_with_conflict_check(&fixture_booking_monday_kw18_2026(), Authentication::Full, None)
            .await
            .expect("should succeed");
        assert!(result.warnings.is_empty(), "soft-deleted absence MUST NOT trigger warning (filtered at DAO)");
    }

    #[tokio::test]
    async fn test_copy_week_aggregates_warnings() {
        let mut deps = build_dependencies();
        // 3 Source-Bookings — 2 davon auf Absence-Tagen
        deps.booking_service = MockBookingService::new();
        deps.booking_service.expect_get_for_week().returning(|_, _, _, _| {
            // Plan-Phase: executor 3 fixture-bookings konstruieren
            unimplemented!("executor: 3 Booking-Fixtures, davon 2 mit slot_id=monday-slot")
        });
        deps.booking_service.expect_create().returning(|b, _, _| Ok((*b).clone()));
        // AbsenceService gibt für Days {Mo, Tu} eine AbsencePeriod zurück, für andere nicht
        deps.absence_service = MockAbsenceService::new();
        deps.absence_service.expect_find_overlapping_for_booking().returning(|_, range, _, _| {
            // Plan-Phase: range.from()/.to() prüfen — MO+TU treffen die fixture
            unimplemented!("executor: returns 1 AbsencePeriod wenn range.from() in {Mo, Tu}, sonst empty")
        });

        let service = build_service(deps);
        let result = service
            .copy_week_with_conflict_check(17, 2026, 18, 2026, Authentication::Full, None)
            .await
            .expect("should succeed");
        assert_eq!(result.copied_bookings.len(), 3, "all 3 bookings copied");
        assert_eq!(result.warnings.len(), 2, "2 absence-day-warnings (D-Phase3-15: KEINE De-Dup)");
    }

    #[tokio::test]
    async fn test_book_slot_with_conflict_check_forbidden() {
        let mut deps = build_dependencies();
        deps.permission_service = MockPermissionService::new();
        deps.permission_service.expect_check_permission().returning(|_, _| Err(ServiceError::Forbidden));
        deps.sales_person_service = MockSalesPersonService::new();
        deps.sales_person_service.expect_verify_user_is_sales_person().returning(|_, _, _| Err(ServiceError::Forbidden));

        let service = build_service(deps);
        let result = service
            .book_slot_with_conflict_check(&fixture_booking_monday_kw18_2026(), Authentication::Full, None)
            .await;
        test_forbidden(&result);
    }

    #[tokio::test]
    async fn test_copy_week_with_conflict_check_forbidden() {
        let mut deps = build_dependencies();
        deps.permission_service = MockPermissionService::new();
        deps.permission_service.expect_check_permission().returning(|_, _| Err(ServiceError::Forbidden));

        let service = build_service(deps);
        let result = service
            .copy_week_with_conflict_check(17, 2026, 18, 2026, Authentication::Full, None)
            .await;
        test_forbidden(&result);
    }
    ```

    2. **Wichtige Hinweise für den Executor:**
    - `unimplemented!("executor: ...")` markiert Stellen, wo der Executor existing-Repo-Pattern (z.B. `Slot`-Struct-Felder, `Booking`-Struct-Felder, `SalesPersonUnavailable`-Struct-Felder) finden und einfügen muss. Nicht raten — `grep "pub struct Slot" service/src/slot.rs` und kopieren.
    - Mock-Typen wie `MockBookingService`, `MockAbsenceService` etc. werden automatisch von `#[automock]` auf den Trait generiert — verifiziert in Phase 1.
    - Der `build_service`-Helper konvertiert `MockX` → `Arc<MockX>` (per `into()` oder `Arc::new(...)`) für die `ShiftplanEditServiceImpl`-Felder.
    - **Bei `unimplemented!()`-Pfaden:** Plan-Phase verlangt, dass der Executor sie alle vor dem nächsten `cargo test`-Run auflöst.

    3. **PRE-cargo-test-Gate (W-4):** Bevor `cargo test` läuft, MUSS verifiziert sein, dass keine `unimplemented!()`-Marker mehr in der Datei stehen. Das zwingt den Executor, ALLE Fixture-Stubs auszufüllen, bevor er auf grüne Tests stößt. Wenn `grep -c "unimplemented!" service_impl/src/test/shiftplan_edit.rs` ungleich `0` ist, MUSS der Executor zunächst die Marker auflösen, bevor er mit Schritt 4 fortfährt.

    4. **Run** `cargo test -p service_impl test::shiftplan_edit` — alle 6 Tests MÜSSEN grün laufen.
  </action>
  <verify>
    <automated>grep -c "unimplemented!" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan_edit.rs</automated>
    <automated>cargo test -p service_impl test::shiftplan_edit 2>&1 | tail -20</automated>
    <automated>grep -c "#\[ignore" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan_edit.rs</automated>
    <automated>grep -c "#\[tokio::test\]" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan_edit.rs</automated>
  </verify>
  <done>
    - PRE-cargo-test-Gate: KEINE `unimplemented!()`-Marker mehr (Verify-1 == 0; alle Fixture-Stubs aufgelöst)
    - cargo test -p service_impl test::shiftplan_edit GRÜN (Verify-2; alle 6 Tests pass)
    - KEINE `#[ignore]`-Marker mehr (Verify-3 == 0)
    - 6+ `#[tokio::test]`-Functions (Verify-4 ≥ 6)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward-Warning-Tests in `test/absence.rs` + per-sales-person-Tests in `test/shiftplan.rs`</name>
  <read_first>
    - service_impl/src/test/absence.rs (Plan-03-Form mit MockBookingService etc.)
    - service_impl/src/test/shiftplan.rs (heutige `ShiftplanViewServiceDependencies` — Plan-04-Form mit MockAbsenceService + MockSalesPersonUnavailableService)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 1528-1660 — Test-Diff-Skizzen)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md (Test-IDs 03-XX-01..03, 10..13)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-04 für update-Test, D-Phase3-10 für Both-Marker, SC4 für softdeleted)
  </read_first>
  <files>service_impl/src/test/absence.rs, service_impl/src/test/shiftplan.rs</files>
  <action>
    1. **Patch** `service_impl/src/test/absence.rs`:

    Füge AM ENDE der Datei (oder nach den bestehenden Tests aus Phase 1+2) folgende neuen `#[tokio::test]`-Funktionen hinzu:

    ```rust
    // ──────────────────────────────────────────────────────────────────────
    // Phase 3 — Forward-Warning-Tests
    // ──────────────────────────────────────────────────────────────────────

    #[tokio::test]
    async fn test_create_warning_for_booking_in_range() {
        let mut deps = build_dependencies();
        // 1 Booking im Range, sales_person_id matching, slot_id auf Monday
        deps.booking_service = MockBookingService::new();
        deps.booking_service.expect_get_for_week().returning(|_, _, _, _| {
            unimplemented!("executor: 1 Booking-Fixture mit sales_person_id matching create-input, slot_id->Monday")
        });
        deps.slot_service.expect_get_slot().returning(|_, _, _| {
            unimplemented!("executor: Slot mit day_of_week=Monday")
        });
        // bestehende find_overlapping (Self-Overlap-Check) returns empty
        deps.absence_dao.expect_find_overlapping().returning(|_, _, _, _, _| Ok(Arc::from([])));
        deps.absence_dao.expect_create().returning(|_, _, _| Ok(()));

        let service = build_service(deps);
        let result = service
            .create(&default_create_request(), Authentication::Full, None)
            .await
            .expect("create succeeds");
        assert!(
            result.warnings.iter().any(|w| matches!(w, service::warning::Warning::AbsenceOverlapsBooking { .. })),
            "expected at least one AbsenceOverlapsBooking warning"
        );
    }

    #[tokio::test]
    async fn test_create_warning_for_manual_unavailable_in_range() {
        let mut deps = build_dependencies();
        deps.sales_person_unavailable_service = MockSalesPersonUnavailableService::new();
        deps.sales_person_unavailable_service.expect_get_all_for_sales_person().returning(|_, _, _| {
            unimplemented!("executor: 1 SalesPersonUnavailable-Fixture innerhalb der Create-Range")
        });
        deps.absence_dao.expect_find_overlapping().returning(|_, _, _, _, _| Ok(Arc::from([])));
        deps.absence_dao.expect_create().returning(|_, _, _| Ok(()));

        let service = build_service(deps);
        let result = service.create(&default_create_request(), Authentication::Full, None).await.expect("create succeeds");
        assert!(
            result.warnings.iter().any(|w| matches!(w, service::warning::Warning::AbsenceOverlapsManualUnavailable { .. })),
        );
    }

    #[tokio::test]
    async fn test_update_returns_warnings_for_full_new_range() {
        // D-Phase3-04: update-Warnings für ALLE Tage in NEUER Range, kein Diff-Modus.
        let mut deps = build_dependencies();
        // existing absence-row + neue Range mit overlappendem Booking
        deps.absence_dao.expect_find_by_logical_id().returning(|_, _| {
            unimplemented!("executor: bestehende AbsencePeriod-Row mit alter Range")
        });
        deps.booking_service = MockBookingService::new();
        deps.booking_service.expect_get_for_week().returning(|_, _, _, _| {
            unimplemented!("executor: 1 Booking innerhalb der NEUEN Range")
        });
        deps.absence_dao.expect_find_overlapping().returning(|_, _, _, _, _| Ok(Arc::from([])));
        deps.absence_dao.expect_update().returning(|_, _, _| Ok(()));
        deps.absence_dao.expect_create().returning(|_, _, _| Ok(()));

        let service = build_service(deps);
        let result = service.update(&default_update_request(), Authentication::Full, None).await.expect("update succeeds");
        assert!(!result.warnings.is_empty(), "update MUST return warnings for booking in new range");
    }

    #[tokio::test]
    async fn test_find_overlapping_for_booking_forbidden() {
        let mut deps = build_dependencies();
        deps.permission_service = MockPermissionService::new();
        deps.permission_service.expect_check_permission().returning(|_, _| Err(service::ServiceError::Forbidden));
        deps.sales_person_service = MockSalesPersonService::new();
        deps.sales_person_service.expect_verify_user_is_sales_person().returning(|_, _, _| Err(service::ServiceError::Forbidden));

        let service = build_service(deps);
        let result = service.find_overlapping_for_booking(
            default_sales_person_id(),
            shifty_utils::DateRange::new(time::macros::date!(2026-04-27), time::macros::date!(2026-04-30)).unwrap(),
            Authentication::Full,
            None,
        ).await;
        crate::test::error_test::test_forbidden(&result);
    }
    ```

    `default_create_request()`, `default_update_request()`, `default_sales_person_id()` sind Helpers, die in `test/absence.rs` aus Phase 1 bereits existieren — Executor verifiziert mit grep und passt Inputs (Range = einzelne Woche so, dass `iter_days` einen einzelnen Booking-Tag durchläuft) an.

    2. **Patch** `service_impl/src/test/shiftplan.rs`:

    Füge AM ENDE der Datei folgende neuen Tests hinzu:

    ```rust
    // ──────────────────────────────────────────────────────────────────────
    // Phase 3 — per-sales-person-Sicht-Tests
    // ──────────────────────────────────────────────────────────────────────

    #[tokio::test]
    async fn test_per_sales_person_marker_absence_only() {
        let mut deps = build_dependencies();
        deps.absence_service.expect_find_by_sales_person().returning(|_, _, _| {
            unimplemented!("executor: 1 active AbsencePeriod overlapping Monday KW18 2026")
        });
        deps.sales_person_unavailable_service.expect_get_by_week_for_sales_person()
            .returning(|_, _, _, _, _| Ok(std::sync::Arc::from([])));

        let service = build_service(deps);
        let week = service
            .get_shiftplan_week_for_sales_person(default_shiftplan_id(), 2026, 18, default_sales_person_id(), Authentication::Full, None)
            .await
            .expect("ok");
        let monday = week.days.iter().find(|d| d.day_of_week == shifty_utils::DayOfWeek::Monday).unwrap();
        assert!(matches!(
            monday.unavailable,
            Some(service::shiftplan::UnavailabilityMarker::AbsencePeriod { .. })
        ));
    }

    #[tokio::test]
    async fn test_per_sales_person_marker_manual_only() {
        let mut deps = build_dependencies();
        deps.absence_service.expect_find_by_sales_person().returning(|_, _, _| Ok(std::sync::Arc::from([])));
        deps.sales_person_unavailable_service.expect_get_by_week_for_sales_person().returning(|_, _, _, _, _| {
            unimplemented!("executor: 1 SalesPersonUnavailable, day_of_week=Monday")
        });

        let service = build_service(deps);
        let week = service.get_shiftplan_week_for_sales_person(default_shiftplan_id(), 2026, 18, default_sales_person_id(), Authentication::Full, None).await.expect("ok");
        let monday = week.days.iter().find(|d| d.day_of_week == shifty_utils::DayOfWeek::Monday).unwrap();
        assert!(matches!(monday.unavailable, Some(service::shiftplan::UnavailabilityMarker::ManualUnavailable)));
    }

    #[tokio::test]
    async fn test_per_sales_person_marker_both() {
        // D-Phase3-10 Both-Variante
        let mut deps = build_dependencies();
        deps.absence_service.expect_find_by_sales_person().returning(|_, _, _| {
            unimplemented!("executor: 1 active AbsencePeriod, Monday KW18 2026")
        });
        deps.sales_person_unavailable_service.expect_get_by_week_for_sales_person().returning(|_, _, _, _, _| {
            unimplemented!("executor: 1 SalesPersonUnavailable, day_of_week=Monday")
        });

        let service = build_service(deps);
        let week = service.get_shiftplan_week_for_sales_person(default_shiftplan_id(), 2026, 18, default_sales_person_id(), Authentication::Full, None).await.expect("ok");
        let monday = week.days.iter().find(|d| d.day_of_week == shifty_utils::DayOfWeek::Monday).unwrap();
        assert!(matches!(monday.unavailable, Some(service::shiftplan::UnavailabilityMarker::Both { .. })));
    }

    #[tokio::test]
    async fn test_per_sales_person_marker_softdeleted_absence_none() {
        // Pitfall-1 / SC4
        let mut deps = build_dependencies();
        deps.absence_service.expect_find_by_sales_person().returning(|_, _, _| {
            unimplemented!("executor: 1 AbsencePeriod mit deleted = Some(...)")
        });
        deps.sales_person_unavailable_service.expect_get_by_week_for_sales_person().returning(|_, _, _, _, _| Ok(std::sync::Arc::from([])));

        let service = build_service(deps);
        let week = service.get_shiftplan_week_for_sales_person(default_shiftplan_id(), 2026, 18, default_sales_person_id(), Authentication::Full, None).await.expect("ok");
        let monday = week.days.iter().find(|d| d.day_of_week == shifty_utils::DayOfWeek::Monday).unwrap();
        assert!(monday.unavailable.is_none(), "soft-deleted absence MUST NOT produce marker (SC4)");
    }

    #[tokio::test]
    async fn test_get_shiftplan_week_for_sales_person_forbidden() {
        let mut deps = build_dependencies();
        deps.permission_service = MockPermissionService::new();
        deps.permission_service.expect_check_permission().returning(|_, _| Err(service::ServiceError::Forbidden));
        deps.sales_person_service.expect_verify_user_is_sales_person().returning(|_, _, _| Err(service::ServiceError::Forbidden));

        let service = build_service(deps);
        let result = service.get_shiftplan_week_for_sales_person(default_shiftplan_id(), 2026, 18, default_sales_person_id(), Authentication::Full, None).await;
        crate::test::error_test::test_forbidden(&result);
    }
    ```

    `default_shiftplan_id()` als Helper ergänzen falls nicht vorhanden.

    3. Imports am Datei-Anfang ergänzen, falls fehlt: `use service::shiftplan::UnavailabilityMarker;`, `use shifty_utils::DayOfWeek;` etc.
  </action>
  <verify>
    <automated>cargo test -p service_impl test::absence 2>&1 | tail -15</automated>
    <automated>cargo test -p service_impl test::shiftplan 2>&1 | tail -15</automated>
    <automated>grep -cE "test_create_warning_for_booking_in_range|test_create_warning_for_manual_unavailable_in_range|test_update_returns_warnings_for_full_new_range|test_find_overlapping_for_booking_forbidden" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/absence.rs</automated>
    <automated>grep -cE "test_per_sales_person_marker_absence_only|test_per_sales_person_marker_manual_only|test_per_sales_person_marker_both|test_per_sales_person_marker_softdeleted_absence_none|test_get_shiftplan_week_for_sales_person_forbidden" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan.rs</automated>
    <automated>grep -c "unimplemented!" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/absence.rs /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan.rs 2>&1 | awk -F: '{sum+=$2} END {print sum}'</automated>
  </verify>
  <done>
    - cargo test -p service_impl test::absence GRÜN (Phase-1+2-Tests + 4 neue Phase-3-Tests)
    - cargo test -p service_impl test::shiftplan GRÜN (existing-Tests + 5 neue Phase-3-Tests)
    - 4+ neue Forward-Warning-Test-Namen in absence.rs (Verify-3 == 4)
    - 5+ neue per-sales-person-Test-Namen in shiftplan.rs (Verify-4 == 5)
    - KEINE `unimplemented!()` mehr (Verify-5 == 0; alle Stub-Helpers durch echte Fixtures ersetzt)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration-Test-Implementation + Smoke-Boot + cargo test --workspace</name>
  <read_first>
    - shifty_bin/src/integration_test/booking_absence_conflict.rs (Plan-01-Stubs)
    - shifty_bin/src/integration_test/absence_period.rs (Phase-1-TestSetup-Pattern; Z. 1-200)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 1663-1797 — Integration-Test-Skizze)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md (Test-IDs 03-XX-07, 08, 14)
  </read_first>
  <files>shifty_bin/src/integration_test/booking_absence_conflict.rs</files>
  <action>
    1. **REPLACE** `shifty_bin/src/integration_test/booking_absence_conflict.rs` (überschreibe komplett — Stub-Form aus Plan 01 wird durch reale Tests ersetzt). Pattern wie 03-PATTERNS.md Z. 1670-1772, mit den 4 verlangten Tests:

    ```rust
    //! End-to-End-Integrationstests für Phase 3 (Booking ⇄ Absence ⇄ ManualUnavailable).
    //!
    //! Pflicht-Coverage:
    //! - test_double_source_two_warnings_one_booking         (BOOK-02 / Cross-Source)
    //! - test_softdeleted_absence_no_warning_no_marker       (SC4 / Pitfall-1 — full-stack)
    //! - test_copy_week_three_bookings_two_warnings          (BOOK-02 / D-Phase3-02)
    //! - test_shiftplan_marker_softdeleted_absence_none      (PLAN-01 + SC4 — Read-Pfad)

    use rest::RestStateDef;
    use service::{
        absence::{AbsenceCategory, AbsencePeriod, AbsenceService},
        booking::Booking,
        permission::Authentication,
        sales_person::SalesPersonService,
        sales_person_unavailable::{SalesPersonUnavailable, SalesPersonUnavailableService},
        shiftplan_edit::ShiftplanEditService,
        slot::{Slot, SlotService},
        warning::Warning,
    };
    use shifty_utils::DayOfWeek;
    use time::macros::date;
    use uuid::Uuid;

    use crate::integration_test::TestSetup;

    // Helper-Funktionen — Pattern aus integration_test/absence_period.rs:1-100
    async fn create_sales_person(test_setup: &TestSetup, name: &str) -> service::sales_person::SalesPerson {
        unimplemented!("executor: pattern aus absence_period.rs:19-38 — passt name + erstellt SalesPerson via test_setup.rest_state.sales_person_service().create(...)")
    }

    async fn create_slot_monday(test_setup: &TestSetup) -> Slot {
        unimplemented!("executor: erstellt Slot mit day_of_week=Monday via test_setup.rest_state.slot_service().create_slot(...) — verifiziere die existing slot_service-API")
    }

    async fn create_absence_period_at(
        test_setup: &TestSetup,
        sales_person_id: Uuid,
        from_date: time::Date,
        to_date: time::Date,
    ) -> AbsencePeriod {
        unimplemented!("executor: pattern aus absence_period.rs:40-61 — sales_person_id + Range")
    }

    async fn create_sales_person_unavailable_at(
        test_setup: &TestSetup,
        sales_person_id: Uuid,
        year: u32,
        week: u8,
        day_of_week: DayOfWeek,
    ) -> SalesPersonUnavailable {
        unimplemented!("executor: via test_setup.rest_state.sales_person_unavailable_service().create(...)")
    }

    fn make_booking(sales_person_id: Uuid, slot_id: Uuid, year: u32, calendar_week: u32) -> Booking {
        unimplemented!("executor: Booking { id: Uuid::nil(), sales_person_id, slot_id, year, calendar_week, ... }")
    }

    #[tokio::test]
    async fn test_double_source_two_warnings_one_booking() {
        let test_setup = TestSetup::new().await;
        let sp = create_sales_person(&test_setup, "DoubleSource").await;
        let slot = create_slot_monday(&test_setup).await;
        let _ap = create_absence_period_at(&test_setup, sp.id, date!(2026 - 04 - 27), date!(2026 - 04 - 30)).await;
        let _mu = create_sales_person_unavailable_at(&test_setup, sp.id, 2026, 18, DayOfWeek::Monday).await;

        let booking = make_booking(sp.id, slot.id, 2026, 18);
        let result = test_setup
            .rest_state
            .shiftplan_edit_service()
            .book_slot_with_conflict_check(&booking, Authentication::Full, None)
            .await
            .expect("book ok");

        assert_eq!(result.warnings.len(), 2, "expected 2 cross-source warnings");
        assert!(result.warnings.iter().any(|w| matches!(w, Warning::BookingOnAbsenceDay { .. })));
        assert!(result.warnings.iter().any(|w| matches!(w, Warning::BookingOnUnavailableDay { .. })));

        // Booking persistiert
        let bookings = test_setup
            .rest_state
            .booking_service()
            .get_for_week(18, 2026, Authentication::Full, None)
            .await
            .expect("get ok");
        assert_eq!(bookings.len(), 1);
    }

    #[tokio::test]
    async fn test_softdeleted_absence_no_warning_no_marker() {
        let test_setup = TestSetup::new().await;
        let sp = create_sales_person(&test_setup, "Pitfall1").await;
        let slot = create_slot_monday(&test_setup).await;
        let ap = create_absence_period_at(&test_setup, sp.id, date!(2026 - 04 - 27), date!(2026 - 04 - 30)).await;
        // soft-delete
        test_setup.rest_state.absence_service().delete(ap.id, Authentication::Full, None).await.expect("delete");

        // Booking-Pfad: keine Warning
        let booking = make_booking(sp.id, slot.id, 2026, 18);
        let result = test_setup
            .rest_state
            .shiftplan_edit_service()
            .book_slot_with_conflict_check(&booking, Authentication::Full, None)
            .await
            .expect("book ok");
        assert!(result.warnings.is_empty(), "soft-deleted absence MUST NOT trigger warning (SC4)");
    }

    #[tokio::test]
    async fn test_copy_week_three_bookings_two_warnings() {
        // D-Phase3-02 — full-stack copy_week
        unimplemented!("executor: 3 source-bookings, 2 absence-tagen, 1 ohne; copy aus KW17 → KW18; assert copied=3 + warnings=2")
    }

    #[tokio::test]
    async fn test_shiftplan_marker_softdeleted_absence_none() {
        // PLAN-01 + SC4 — Read-Pfad
        let test_setup = TestSetup::new().await;
        let sp = create_sales_person(&test_setup, "ReadPathSC4").await;
        let slot = create_slot_monday(&test_setup).await;
        let ap = create_absence_period_at(&test_setup, sp.id, date!(2026 - 04 - 27), date!(2026 - 04 - 30)).await;
        test_setup.rest_state.absence_service().delete(ap.id, Authentication::Full, None).await.expect("delete");

        // shiftplan_id aus dem Slot — verifiziere Slot-Struct, ob shiftplan_id-Feld existiert
        let shiftplan_id = unimplemented!("executor: aus slot.shiftplan_id oder via shiftplan_service.find_by_slot(slot.id)");
        let week = test_setup
            .rest_state
            .shiftplan_view_service()
            .get_shiftplan_week_for_sales_person(shiftplan_id, 2026, 18, sp.id, Authentication::Full, None)
            .await
            .expect("read ok");
        let monday = week.days.iter().find(|d| d.day_of_week == DayOfWeek::Monday).unwrap();
        assert!(monday.unavailable.is_none(), "soft-deleted absence MUST NOT produce shiftplan marker (SC4)");
    }
    ```

    Executor löst alle `unimplemented!()`-Markierungen auf, indem er existing Helpers aus `integration_test/absence_period.rs` adaptiert.

    2. **PRE-cargo-test-Gate (W-4):** Bevor `cargo test` läuft, MUSS verifiziert sein, dass keine `unimplemented!()`-Marker mehr in der Datei stehen. Wenn `grep -c "unimplemented!" shifty_bin/src/integration_test/booking_absence_conflict.rs` ungleich `0` ist, MUSS der Executor zunächst die Marker auflösen, bevor er mit Schritt 3 fortfährt.

    3. **Smoke-Tests** ausführen:
    ```bash
    cargo test --workspace 2>&1 | tail -30
    timeout 12 cargo run > /tmp/cargo-run.log 2>&1
    ```
    Erwartet: alle Tests grün; Boot startet; `head /tmp/cargo-run.log` zeigt keine Panic.

    4. **D-Phase3-18 Regression-Lock final verifizieren:**
    ```bash
    jj diff service/src/booking.rs service_impl/src/booking.rs rest/src/booking.rs service_impl/src/test/booking.rs 2>&1 | wc -l
    ```
    MUSS 0 sein.
  </action>
  <verify>
    <automated>grep -c "unimplemented!" /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty_bin/src/integration_test/booking_absence_conflict.rs</automated>
    <automated>cargo test --workspace 2>&1 | tail -20</automated>
    <automated>timeout 12 cargo run > /tmp/run-final.log 2>&1; head -10 /tmp/run-final.log</automated>
    <automated>jj diff service/src/booking.rs service_impl/src/booking.rs rest/src/booking.rs service_impl/src/test/booking.rs 2>&1 | wc -l</automated>
    <automated>grep -c "#\[ignore" /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty_bin/src/integration_test/booking_absence_conflict.rs</automated>
    <automated>cargo test -p shifty_bin --tests integration_test::booking_absence_conflict 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PRE-cargo-test-Gate: KEINE `unimplemented!()` in Integration-Test (Verify-1 == 0; alle Stubs aufgelöst)
    - cargo test --workspace GRÜN — alle Phase-1+2+3 Tests inklusive Cross-Source + Pitfall-1 grün
    - cargo run startet ohne Panic (Verify-3 zeigt erfolgreichen Boot-Log)
    - **D-Phase3-18 Regression-Lock**: 4 BookingService-Files unangetastet (Verify-4 == 0)
    - KEINE `#[ignore]` in Integration-Test (Verify-5 == 0)
    - Integration-Test-Suite GRÜN (Verify-6)
  </done>
</task>

<task type="auto">
  <name>Task 4: 03-VALIDATION.md-Tabelle finalisieren + ROADMAP.md-Update + Roadmap-SC1-4-Verifikation</name>
  <read_first>
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md (heutiger Stand — alle ⬜ pending Test-IDs sammeln)
    - .planning/ROADMAP.md (Phase-3-Sektion + Goal + Success Criteria 1-4)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (SC-Definitionen)
  </read_first>
  <files>.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md, .planning/ROADMAP.md</files>
  <action>
    1. **Update** `.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md`:
    - Setze in der "Per-Task Verification Map"-Tabelle alle ⬜ pending → ✅ green.
    - Setze `nyquist_compliant: false` → `nyquist_compliant: true` im Frontmatter.
    - Setze `wave_0_complete: false` → `wave_0_complete: true`.
    - Setze "Approval: pending" → "Approval: approved (Plan 06 abgeschlossen)".
    - Update `Plan` und `Task ID`-Suffix-Spalten gemäß Plan-Set: 03-01-* (Plan 01), 03-03-* (AbsenceService-Tests), 03-04-* (Shiftplan-Tests), 03-05-* (REST), 03-06-* (Final Tests).

    2. **Update** `.planning/ROADMAP.md`:
    - Phase 3 Plans-Liste auf 6 Pläne setzen mit korrekten brief-objectives (z.B. `- [x] 03-01-PLAN.md — Wave 0: Stubs/Skeletons + Foundation-Lock`, `- [x] 03-02-PLAN.md — Wave 1: Warning-Enum + AbsenceDao-Surface`, `- [x] 03-03-PLAN.md — Wave 2: AbsenceService Wrapper + Forward-Warning-Loop`, `- [x] 03-04-PLAN.md — Wave 3: ShiftplanEditService Reverse-Warning + ShiftplanViewService Marker`, `- [x] 03-05-PLAN.md — Wave 4: REST-Wrapper-DTO + OpenAPI`, `- [x] 03-06-PLAN.md — Wave 5: Final-Tests + Verifikation`).
    - Phase 3 Status `[ ] Phase 3` → `[/]` während Wave 5 läuft, dann `[x]` nach Verifikation grün.

    3. **Roadmap-SC-Verifikation** (informell — Plan 06 Output, dokumentiert im Summary):
    - SC1 (Forward-Warning + Wrapper-Result mit Booking-IDs): verifiziert durch `test_create_warning_for_booking_in_range` (service_impl) + `test_double_source_two_warnings_one_booking` (integration).
    - SC2 (Reverse-Warning beim Booking auf absence-day OR sales_person_unavailable): verifiziert durch `test_book_slot_warning_on_absence_day`, `test_book_slot_warning_on_manual_unavailable`, `test_double_source_two_warnings_one_booking`.
    - SC3 (Shift-Plan-Anzeige-Markierung mit De-Dup): verifiziert durch `test_per_sales_person_marker_*` Suite (5 Tests).
    - SC4 (Soft-deleted ⇒ KEINE Warning + KEINE Markierung): verifiziert durch `test_book_slot_no_warning_when_softdeleted_absence` + `test_softdeleted_absence_no_warning_no_marker` + `test_per_sales_person_marker_softdeleted_absence_none` + `test_shiftplan_marker_softdeleted_absence_none`.

    4. Verifiziere die Mutationen mit grep + diff-Sichtkontrolle.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" /home/neosam/programming/rust/projects/shifty/shifty-backend/.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md</automated>
    <automated>grep -c "wave_0_complete: true" /home/neosam/programming/rust/projects/shifty/shifty-backend/.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md</automated>
    <automated>grep -c "⬜" /home/neosam/programming/rust/projects/shifty/shifty-backend/.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md</automated>
    <automated>grep -cE "^- \[x\] 03-0[1-6]-PLAN\.md" /home/neosam/programming/rust/projects/shifty/shifty-backend/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    - 03-VALIDATION.md ist auf `nyquist_compliant: true` und `wave_0_complete: true` gesetzt (Verify-1 ≥ 1, Verify-2 ≥ 1)
    - KEINE ⬜-Marker mehr in 03-VALIDATION.md (Verify-3 == 0; alle Test-IDs ✅)
    - ROADMAP.md Phase-3-Plans-Liste auf 6 Pläne aktualisiert (Verify-4 == 6)
    - Alle 4 SCs (SC1-SC4) sind durch konkrete Test-IDs in 03-VALIDATION.md abgedeckt
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Service-Mock-Layer | Mocks simulieren externe Quellen; Tests bestätigen Service-Layer-Verhalten ohne DB. |
| Integration-Test → REST-State (TestSetup) | echte In-Memory-SQLite + echte Service-Konstruktion; verifiziert end-to-end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SoftDel | Information Disclosure | soft-deleted Daten leaken in Warning oder Marker | mitigate | Pitfall-1-Tests in Service-Layer (test_book_slot_no_warning_when_softdeleted_absence) UND in Integration-Layer (test_softdeleted_absence_no_warning_no_marker, test_shiftplan_marker_softdeleted_absence_none) — verifiziert SC4 explizit. |
| T-3-PermHRSelf | Spoofing | Permission HR ∨ self könnte räsoniert sein bei beidseitigem Forbidden | mitigate | _forbidden-Tests pro neue public Service-Method (5 Tests insgesamt — siehe Plan 04 + Plan 06). |
| T-3-Cross-Validation | Tampering | Cross-Source-Doppel-Quelle-Logik produziert falsche Anzahl von Warnings | mitigate | `test_double_source_two_warnings_one_booking` verifiziert exact 2 Warnings (eine pro Quelle, KEINE De-Dup per D-Phase3-15). |
| T-3-D18-Regression | Tampering | versehentlicher Diff in BookingService-Files | mitigate | Verify-4 in Task 3 final: `jj diff` produziert 0 Zeilen. |
</threat_model>

<verification>
Wave-5-Final-Verifikation (Phase-3-Closure):
- `cargo build --workspace` GRÜN
- `cargo test --workspace` GRÜN — Phase-1+2+3 alle grün; bestehende 381+ Tests + neue Phase-3-Tests
- `cargo run` startet ohne Panic; OpenAPI exposes alle 4+ Phase-3-Endpunkte mit korrekten Schemas
- Alle 4 Roadmap-Phase-3-Success-Criteria (SC1, SC2, SC3, SC4) sind durch konkrete Test-IDs verifiziert
- `nyquist_compliant: true` in 03-VALIDATION.md gesetzt
- ROADMAP.md Phase-3-Section aktualisiert (6 Pläne, Status korrekt)
- **D-Phase3-18 Regression-Lock final verifiziert**: BookingService-Files (4) unangetastet; alle Phase-1+2-BookingService-Tests grün
</verification>

<success_criteria>
- [ ] `service_impl/src/test/shiftplan_edit.rs` hat 6 aktive Tests (KEIN `#[ignore]`, KEIN `unimplemented!()` außer in Helper-Markern, die alle aufgelöst sind)
- [ ] `service_impl/src/test/absence.rs` hat 4 neue Forward-Warning-Tests
- [ ] `service_impl/src/test/shiftplan.rs` hat 5 neue per-sales-person-Tests
- [ ] `shifty_bin/src/integration_test/booking_absence_conflict.rs` hat 4 aktive Tests (alle `#[ignore]`/`unimplemented!()` aufgelöst)
- [ ] `cargo test --workspace` GRÜN
- [ ] `cargo run` boot-smoke ohne Panic
- [ ] **D-Phase3-18 Regression-Lock final**: `jj diff service/src/booking.rs service_impl/src/booking.rs rest/src/booking.rs service_impl/src/test/booking.rs` produziert 0 Zeilen
- [ ] 03-VALIDATION.md: alle Tasks ✅ green; `nyquist_compliant: true`; Approval: approved
- [ ] ROADMAP.md: Phase-3-Plans-Liste auf 6 finalisiert; SCs verifiziert
- [ ] **SC1 verifiziert**: Forward-Warning-Tests bestätigen Wrapper mit konkreten Booking-IDs + Daten; Persistenz unverändert.
- [ ] **SC2 verifiziert**: Reverse-Warning-Tests bestätigen Warnings via NEUEN Endpunkt; klassische `BookingService::create`-Tests bleiben grün (Regression-Lock).
- [ ] **SC3 verifiziert**: per-sales-person-Marker-Tests bestätigen alle 4 De-Dup-Pfade (None/AbsencePeriod/ManualUnavailable/Both).
- [ ] **SC4 verifiziert**: Pitfall-1-Tests (Service-Layer + Integration-Layer) bestätigen soft-deleted produziert weder Warning noch Marker.
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-shift-plan-konflikt-integration/03-06-SUMMARY.md` summarizing:
- Tests aktiviert: 6 in test/shiftplan_edit.rs (alle Stubs), 4 neue in test/absence.rs, 5 neue in test/shiftplan.rs, 4 in integration_test/booking_absence_conflict.rs (alle Stubs)
- Total Phase-3-Tests: 19 neue passing Tests + 0 newly broken existing tests
- D-Phase3-18 Regression-Lock final: 4 BookingService-Files unangetastet, jj diff empty
- 03-VALIDATION.md: nyquist_compliant: true; Approval: approved
- ROADMAP.md: Phase-3-Plans-Liste auf 6 finalisiert
- Roadmap-SCs 1-4: explizit per Test-ID verifiziert
- Phase 3 ist abschluss-reif für `/gsd-verify-phase 03`
</output>
