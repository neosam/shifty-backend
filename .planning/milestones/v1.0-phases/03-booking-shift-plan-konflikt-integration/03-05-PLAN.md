---
phase: 03-booking-shift-plan-konflikt-integration
plan: 05
type: execute
wave: 4
depends_on:
  - 04
files_modified:
  - rest-types/src/lib.rs
  - rest/src/absence.rs
  - rest/src/shiftplan_edit.rs
  - rest/src/shiftplan.rs
  - rest/src/lib.rs
autonomous: true
nyquist_compliant: false
requirements:
  - BOOK-01
  - BOOK-02
  - PLAN-01
must_haves:
  truths:
    - "`rest-types/src/lib.rs` enthält 5 neue inline DTOs: `WarningTO` (Tag-Enum mit 4 Varianten, `#[serde(tag = \"kind\", content = \"data\", rename_all = \"snake_case\")]`), `UnavailabilityMarkerTO` (Tag-Enum mit 3 Varianten), `BookingCreateResultTO`, `CopyWeekResultTO`, `AbsencePeriodCreateResultTO`."
    - "`ShiftplanDayTO` hat `unavailable: Option<UnavailabilityMarkerTO>`-Feld; das From-Impl `ShiftplanDayTO::from(&ShiftplanDay)` mappt das Feld."
    - "From-Impls für die 5 neuen DTOs sind unter `#[cfg(feature = \"service-impl\")]` gegated und nutzen `service::warning::Warning`, `service::shiftplan::UnavailabilityMarker`, `service::shiftplan_edit::{BookingCreateResult, CopyWeekResult}`, `service::absence::AbsencePeriodCreateResult`."
    - "`POST /absence-period` und `PATCH /absence-period/{id}` geben `AbsencePeriodCreateResultTO` (mit Warnings) zurück; Status-Code bleibt 201/200; ApiDoc-Schema ist aktualisiert."
    - "Neue REST-Endpunkte `POST /shiftplan-edit/booking` und `POST /shiftplan-edit/copy-week` existieren und geben `BookingCreateResultTO` / `CopyWeekResultTO` zurück; ApiDoc + utoipa-Annotation Pflicht."
    - "Neue REST-Endpunkte `GET /shiftplan/{shiftplan_id}/{year}/{week}/sales-person/{sales_person_id}` und `GET /shiftplan/day/{year}/{week}/{day_of_week}/sales-person/{sales_person_id}` existieren und liefern `ShiftplanWeekTO` / `ShiftplanDayAggregateTO` mit `unavailable`-Feld."
    - "Bestehende Endpunkte `POST /booking` und `POST /booking/copy-week` sowie `GET /shiftplan/{id}/{year}/{week}` und `GET /shiftplan/day/{year}/{week}/{day_of_week}` bleiben **unverändert** (`jj diff rest/src/booking.rs` empty; bestehende `get_shiftplan_week`/`get_shiftplan_day`-Handler ungeändert)."
    - "ApiDoc ist erweitert um `ShiftplanEditApiDoc` (neu) im obersten `nest`-Block in `rest/src/lib.rs`; `AbsenceApiDoc` und `ShiftplanApiDoc` ergänzen schemas um die neuen Wrapper-DTOs."
  artifacts:
    - path: "rest-types/src/lib.rs"
      provides: "5 neue inline DTOs + From-Impls + ShiftplanDayTO-Field"
      contains: "WarningTO"
    - path: "rest/src/shiftplan_edit.rs"
      provides: "2 neue Endpunkte (POST /booking, POST /copy-week) + ShiftplanEditApiDoc"
      contains: "book_slot_with_conflict_check"
    - path: "rest/src/shiftplan.rs"
      provides: "2 neue per-sales-person-Endpunkte"
      contains: "get_shiftplan_week_for_sales_person"
    - path: "rest/src/lib.rs"
      provides: "ApiDoc-Aggregation mit ShiftplanEditApiDoc"
      contains: "ShiftplanEditApiDoc"
  key_links:
    - from: "rest-types/src/lib.rs"
      to: "service::warning::Warning"
      via: "From<&Warning> for WarningTO"
      pattern: "impl From<&service::warning::Warning>"
    - from: "rest/src/shiftplan_edit.rs"
      to: "ShiftplanEditService::book_slot_with_conflict_check"
      via: "rest_state.shiftplan_edit_service().book_slot_with_conflict_check"
      pattern: "shiftplan_edit_service.*book_slot_with_conflict_check"
    - from: "rest/src/shiftplan.rs"
      to: "ShiftplanViewService::get_shiftplan_week_for_sales_person"
      via: "rest_state.shiftplan_view_service().get_shiftplan_week_for_sales_person"
      pattern: "shiftplan_view_service.*get_shiftplan_week_for_sales_person"
---

<objective>
Wave-4-REST-Layer: Lege die 5 neuen Wrapper-DTOs inline in `rest-types/src/lib.rs` an (Repo-Konvention aus Phase 1 Override). Stelle den `POST /absence-period` + `PATCH /absence-period/{id}`-Body auf `AbsencePeriodCreateResultTO` um (jetzt MIT Warnings; Plan 03 hatte `.absence` unwrappt). Lege 2 neue Endpunkte `POST /shiftplan-edit/booking` + `POST /shiftplan-edit/copy-week` an (Naming C-Phase3-09 Vorgabe: neue Route-Gruppe `/shiftplan-edit`). Lege 2 neue per-sales-person-Endpunkte unter `/shiftplan/...` an (D-Phase3-12). Erweitere ApiDoc + utoipa-Schemas. **`POST /booking` und `POST /booking/copy-week` bleiben strikt unangetastet** (D-Phase3-18, Frontend-Migration im Frontend-Workstream).

Purpose: REST-Surface Phase-3-konform machen, sodass das Frontend (separater Workstream) auf die neuen Wrapper-DTOs migrieren kann.

Output: 5 Files patched.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-04-SUMMARY.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- AbsencePeriodTO + From-Impl, heutige Form aus rest-types/src/lib.rs:1572-1620 -->

```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct AbsencePeriodTO {
    pub id: Uuid,
    pub logical_id: Uuid,
    pub sales_person_id: Uuid,
    pub category: AbsenceCategoryTO,
    pub from_date: time::Date,
    pub to_date: time::Date,
    pub description: String,
    pub created: time::PrimitiveDateTime,
    pub deleted: Option<time::PrimitiveDateTime>,
}

#[cfg(feature = "service-impl")]
impl From<&service::absence::AbsencePeriod> for AbsencePeriodTO { ... }
```

<!-- AbsenceCategoryTO + From-Impl, heutige Form aus rest-types/src/lib.rs:1543-1570 -->

```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema, ...)]
pub enum AbsenceCategoryTO {
    Vacation, SickLeave, UnpaidLeave,
}

#[cfg(feature = "service-impl")]
impl From<&service::absence::AbsenceCategory> for AbsenceCategoryTO { ... }
```

<!-- BookingTO + From-Impl, heutige Form (verifiziere mit grep "pub struct BookingTO" rest-types/src/lib.rs) -->

`BookingTO::from(&service::booking::Booking)` ist verfügbar.

<!-- DayOfWeekTO + From, heutige Form aus rest-types/src/lib.rs:240-280 -->

```rust
#[derive(Clone, Copy, Debug, Serialize, Deserialize, ToSchema, PartialEq, Eq)]
pub enum DayOfWeekTO { Monday, Tuesday, ..., Sunday }

#[cfg(feature = "service-impl")]
impl From<shifty_utils::DayOfWeek> for DayOfWeekTO { ... }
```

<!-- ShiftplanDayTO heutige Form (vor Plan 05) ungefähr Z. 973-977 -->

```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct ShiftplanDayTO {
    pub day_of_week: DayOfWeekTO,
    pub slots: Vec<ShiftplanSlotTO>,
    // <-- Plan 05 ergänzt: pub unavailable: Option<UnavailabilityMarkerTO>,
}

#[cfg(feature = "service-impl")]
impl From<&service::shiftplan::ShiftplanDay> for ShiftplanDayTO {
    fn from(d: &service::shiftplan::ShiftplanDay) -> Self {
        Self {
            day_of_week: d.day_of_week.into(),
            slots: d.slots.iter().map(...).collect(),
            // <-- Plan 05 ergänzt: unavailable: d.unavailable.as_ref().map(UnavailabilityMarkerTO::from),
        }
    }
}
```

<!-- ApiDoc-Form heutige (rest/src/lib.rs:460-484) -->

`#[derive(OpenApi)]` mit `nest(...)` für die 19 bestehenden API-Subdomains.

<!-- error_handler heutige Form (rest/src/lib.rs:120) -->

```rust
fn error_handler<F: Future<Output = Result<Response, ServiceError>>>(f: F) -> Response { ... }
```
Bleibt unverändert — Warnings sind Erfolgs-Pfad.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5 neue inline DTOs + ShiftplanDayTO-Field-Add + From-Impls in `rest-types/src/lib.rs`</name>
  <read_first>
    - rest-types/src/lib.rs (gezielt: Z. 240-280 DayOfWeekTO, Z. 973-1020 ShiftplanDayTO + From-Impl, Z. 1543-1620 AbsenceCategory + AbsencePeriodTO + From-Impls; identifiziere `pub struct BookingTO`-Block + dessen From-Impl)
    - service/src/warning.rs (Plan 02)
    - service/src/shiftplan.rs (UnavailabilityMarker aus Plan 02)
    - service/src/shiftplan_edit.rs (BookingCreateResult + CopyWeekResult aus Plan 04)
    - service/src/absence.rs (AbsencePeriodCreateResult aus Plan 03)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 895-1083 — vollständige inline DTO-Skizzen + From-Impls)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-03, C-Phase3-04)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-RESEARCH.md (Pattern 4 — Tag-Enum mit utoipa)
  </read_first>
  <files>rest-types/src/lib.rs</files>
  <action>
    1. **Patch** `rest-types/src/lib.rs`:

    a) Suche nach `pub struct ShiftplanDayTO {` (ca. Z. 973-977). Ergänze ein neues Feld AM ENDE des Structs (vor `}`):
    ```rust
        /// Phase-3-Marker — gesetzt nur durch die per-sales-person-Sicht.
        /// Globale Schichtplan-Endpunkte lassen das Feld immer `None`.
        pub unavailable: Option<UnavailabilityMarkerTO>,
    ```

    Suche das passende From-Impl `impl From<&service::shiftplan::ShiftplanDay> for ShiftplanDayTO` (gleicher Block oder weiter unten — mit grep finden) und ergänze im `Self { ... }`-Block:
    ```rust
            unavailable: d.unavailable.as_ref().map(UnavailabilityMarkerTO::from),
    ```

    b) AM ENDE des Files (oder NACH `AbsencePeriodTO`-Block, ca. Z. 1620 — Plan-Phase wählt; Pattern aus 03-PATTERNS.md Z. 906-1070) die 5 neuen DTOs ergänzen:

    ```rust
    // ──────────────────────────────────────────────────────────────────────
    // Phase-3 — Cross-Source-Warning + Wrapper-Result-DTOs
    // ──────────────────────────────────────────────────────────────────────

    /// Cross-Source-Konflikt-Warning für REST. Tag-Enum für stabiles
    /// OpenAPI-Schema (utoipa-5-Support für `#[serde(tag, content)]`).
    /// JSON-Form: `{ "kind": "booking_on_absence_day", "data": { ... } }`.
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    #[serde(tag = "kind", content = "data", rename_all = "snake_case")]
    pub enum WarningTO {
        BookingOnAbsenceDay {
            booking_id: Uuid,
            #[schema(value_type = String, format = "date")]
            date: time::Date,
            absence_id: Uuid,
            category: AbsenceCategoryTO,
        },
        BookingOnUnavailableDay {
            booking_id: Uuid,
            year: u32,
            week: u8,
            day_of_week: DayOfWeekTO,
        },
        AbsenceOverlapsBooking {
            absence_id: Uuid,
            booking_id: Uuid,
            #[schema(value_type = String, format = "date")]
            date: time::Date,
        },
        AbsenceOverlapsManualUnavailable {
            absence_id: Uuid,
            unavailable_id: Uuid,
        },
    }

    #[cfg(feature = "service-impl")]
    impl From<&service::warning::Warning> for WarningTO {
        fn from(w: &service::warning::Warning) -> Self {
            match w {
                service::warning::Warning::BookingOnAbsenceDay { booking_id, date, absence_id, category } => Self::BookingOnAbsenceDay {
                    booking_id: *booking_id,
                    date: *date,
                    absence_id: *absence_id,
                    category: category.into(),
                },
                service::warning::Warning::BookingOnUnavailableDay { booking_id, year, week, day_of_week } => Self::BookingOnUnavailableDay {
                    booking_id: *booking_id,
                    year: *year,
                    week: *week,
                    day_of_week: (*day_of_week).into(),
                },
                service::warning::Warning::AbsenceOverlapsBooking { absence_id, booking_id, date } => Self::AbsenceOverlapsBooking {
                    absence_id: *absence_id,
                    booking_id: *booking_id,
                    date: *date,
                },
                service::warning::Warning::AbsenceOverlapsManualUnavailable { absence_id, unavailable_id } => Self::AbsenceOverlapsManualUnavailable {
                    absence_id: *absence_id,
                    unavailable_id: *unavailable_id,
                },
            }
        }
    }

    /// Per-Tag-Marker für die per-sales-person-Sicht. Tag-Enum analog
    /// `WarningTO`.
    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    #[serde(tag = "kind", content = "data", rename_all = "snake_case")]
    pub enum UnavailabilityMarkerTO {
        AbsencePeriod {
            absence_id: Uuid,
            category: AbsenceCategoryTO,
        },
        ManualUnavailable,
        Both {
            absence_id: Uuid,
            category: AbsenceCategoryTO,
        },
    }

    #[cfg(feature = "service-impl")]
    impl From<&service::shiftplan::UnavailabilityMarker> for UnavailabilityMarkerTO {
        fn from(m: &service::shiftplan::UnavailabilityMarker) -> Self {
            match m {
                service::shiftplan::UnavailabilityMarker::AbsencePeriod { absence_id, category } => Self::AbsencePeriod {
                    absence_id: *absence_id,
                    category: category.into(),
                },
                service::shiftplan::UnavailabilityMarker::ManualUnavailable => Self::ManualUnavailable,
                service::shiftplan::UnavailabilityMarker::Both { absence_id, category } => Self::Both {
                    absence_id: *absence_id,
                    category: category.into(),
                },
            }
        }
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct BookingCreateResultTO {
        pub booking: BookingTO,
        pub warnings: Vec<WarningTO>,
    }

    #[cfg(feature = "service-impl")]
    impl From<&service::shiftplan_edit::BookingCreateResult> for BookingCreateResultTO {
        fn from(r: &service::shiftplan_edit::BookingCreateResult) -> Self {
            Self {
                booking: BookingTO::from(&r.booking),
                warnings: r.warnings.iter().map(WarningTO::from).collect(),
            }
        }
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct CopyWeekResultTO {
        pub copied_bookings: Vec<BookingTO>,
        pub warnings: Vec<WarningTO>,
    }

    #[cfg(feature = "service-impl")]
    impl From<&service::shiftplan_edit::CopyWeekResult> for CopyWeekResultTO {
        fn from(r: &service::shiftplan_edit::CopyWeekResult) -> Self {
            Self {
                copied_bookings: r.copied_bookings.iter().map(BookingTO::from).collect(),
                warnings: r.warnings.iter().map(WarningTO::from).collect(),
            }
        }
    }

    #[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
    pub struct AbsencePeriodCreateResultTO {
        pub absence: AbsencePeriodTO,
        pub warnings: Vec<WarningTO>,
    }

    #[cfg(feature = "service-impl")]
    impl From<&service::absence::AbsencePeriodCreateResult> for AbsencePeriodCreateResultTO {
        fn from(r: &service::absence::AbsencePeriodCreateResult) -> Self {
            Self {
                absence: AbsencePeriodTO::from(&r.absence),
                warnings: r.warnings.iter().map(WarningTO::from).collect(),
            }
        }
    }
    ```

    c) **Verifiziere** mit `cargo build -p rest-types --features service-impl`. Bei Fehler "missing trait bounds" → Plan-Phase erlaubt minimale Anpassungen am `#[derive(...)]`-Set; primary verifiziere `Serialize, Deserialize, ToSchema` + `Debug, Clone`.

    d) Imports am Datei-Anfang ggf. ergänzen — `use uuid::Uuid;`, `use serde::{Deserialize, Serialize};`, `use utoipa::ToSchema;` (sind alle bereits drin per Phase 1).
  </action>
  <verify>
    <automated>cargo build -p rest-types --features service-impl 2>&1 | tail -10</automated>
    <automated>cargo build --workspace 2>&1 | tail -10</automated>
    <automated>grep -c "pub enum WarningTO" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs</automated>
    <automated>grep -c "pub enum UnavailabilityMarkerTO" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs</automated>
    <automated>grep -c "pub struct BookingCreateResultTO\|pub struct CopyWeekResultTO\|pub struct AbsencePeriodCreateResultTO" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs</automated>
    <automated>grep -c "pub unavailable: Option<UnavailabilityMarkerTO>" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs</automated>
  </verify>
  <done>
    - cargo build -p rest-types --features service-impl GRÜN
    - cargo build --workspace GRÜN
    - WarningTO, UnavailabilityMarkerTO, BookingCreateResultTO, CopyWeekResultTO, AbsencePeriodCreateResultTO präsent (Verify-3 == 1, Verify-4 == 1, Verify-5 == 3)
    - ShiftplanDayTO hat `unavailable`-Feld (Verify-6 == 1)
  </done>
</task>

<task type="auto">
  <name>Task 2: REST-Handler für `POST /absence-period` + `PATCH /absence-period/{id}` auf `AbsencePeriodCreateResultTO` umstellen + 2 neue Endpunkte unter `/shiftplan-edit` + ApiDoc</name>
  <read_first>
    - rest/src/absence.rs (vollständig — heutige Wrapper-DTO-Stelle aus Plan 03)
    - rest/src/shiftplan_edit.rs (vollständig — heutige `edit_slot`/`add_vacation`-Routes)
    - rest/src/lib.rs (Z. 460-484 — ApiDoc-nest, Z. 100-130 — error_handler-Pattern)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 1086-1264 — REST-Handler-Templates)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-03, C-Phase3-09)
  </read_first>
  <files>rest/src/absence.rs, rest/src/shiftplan_edit.rs, rest/src/lib.rs</files>
  <action>
    1. **Patch** `rest/src/absence.rs`:

    a) `create_absence_period`-Handler (Z. ~43-74; Plan-03-Form mit `.absence`-Unwrap). Update auf vollen Wrapper-DTO:
    ```rust
    #[utoipa::path(
        post,
        path = "",
        tags = ["Absence"],
        request_body = AbsencePeriodTO,
        responses(
            (status = 201, description = "Absence period created (with warnings if any)", body = AbsencePeriodCreateResultTO),
            (status = 403, description = "Forbidden"),
            (status = 422, description = "Validation error"),
        ),
    )]
    pub async fn create_absence_period<RestState: RestStateDef>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Json(body): Json<AbsencePeriodTO>,
    ) -> Response {
        error_handler(
            (async {
                let svc = rest_state.absence_service();
                let result = svc.create(&(&body).into(), context.into(), None).await?;
                let to = AbsencePeriodCreateResultTO::from(&result);
                Ok(Response::builder()
                    .status(201)
                    .header("Content-Type", "application/json")
                    .body(Body::new(serde_json::to_string(&to).unwrap()))
                    .unwrap())
            })
            .await,
        )
    }
    ```

    b) `update_absence_period`-Handler symmetrisch — Status 200, Body `AbsencePeriodCreateResultTO`. Plan-03-TODO entfernen.

    c) ApiDoc-Schema-Liste in `AbsenceApiDoc` (Z. ~246) erweitern um `AbsencePeriodCreateResultTO` und `WarningTO`. Pattern:
    ```rust
    #[openapi(
        paths(...),
        components(schemas(
            AbsencePeriodTO, AbsenceCategoryTO,
            AbsencePeriodCreateResultTO, WarningTO,  // NEU für Phase 3
        )),
        ...
    )]
    pub struct AbsenceApiDoc;
    ```

    Imports am Datei-Anfang ergänzen, falls fehlt: `use rest_types::{AbsencePeriodCreateResultTO, WarningTO};` (oder den Crate-Pfad an Phase-1-Konvention anpassen).

    2. **Patch** `rest/src/shiftplan_edit.rs`:

    a) Imports ergänzen am Datei-Anfang:
    ```rust
    use axum::routing::post;
    use serde::Deserialize;
    use rest_types::{BookingCreateResultTO, CopyWeekResultTO, BookingTO};
    use service::booking::Booking;
    use utoipa::OpenApi;
    ```

    b) `generate_route()`-Funktion suchen und um zwei Routen erweitern:
    ```rust
    pub fn generate_route<RestState: RestStateDef>() -> Router<RestState> {
        Router::new()
            .route("/slot/{year}/{week}", put(edit_slot::<RestState>))
            .route("/slot/{slot_id}/{year}/{week}", delete(delete_slot::<RestState>))
            .route("/vacation", put(add_vacation::<RestState>))
            // NEU für Phase 3 (C-Phase3-09):
            .route("/booking", post(book_slot_with_conflict_check::<RestState>))
            .route("/copy-week", post(copy_week_with_conflict_check::<RestState>))
    }
    ```

    c) Neuer Handler `book_slot_with_conflict_check`:
    ```rust
    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post,
        path = "/booking",
        tags = ["ShiftplanEdit"],
        request_body = BookingTO,
        responses(
            (status = 201, description = "Booking created (with cross-source warnings if any)", body = BookingCreateResultTO),
            (status = 403, description = "Forbidden"),
            (status = 422, description = "Validation error"),
        ),
    )]
    pub async fn book_slot_with_conflict_check<RestState: RestStateDef>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Json(body): Json<BookingTO>,
    ) -> Response {
        error_handler(
            (async {
                let svc = rest_state.shiftplan_edit_service();
                let booking: Booking = (&body).into();
                let result = svc
                    .book_slot_with_conflict_check(&booking, context.into(), None)
                    .await?;
                let to = BookingCreateResultTO::from(&result);
                Ok(Response::builder()
                    .status(201)
                    .header("Content-Type", "application/json")
                    .body(axum::body::Body::new(serde_json::to_string(&to).unwrap()))
                    .unwrap())
            })
            .await,
        )
    }
    ```

    d) Neuer Handler `copy_week_with_conflict_check` mit zugehöriger `CopyWeekRequest`-Struct:
    ```rust
    #[derive(Debug, Deserialize, utoipa::ToSchema)]
    pub struct CopyWeekRequest {
        pub from_year: u32,
        pub from_calendar_week: u8,
        pub to_year: u32,
        pub to_calendar_week: u8,
    }

    #[instrument(skip(rest_state))]
    #[utoipa::path(
        post,
        path = "/copy-week",
        tags = ["ShiftplanEdit"],
        request_body = CopyWeekRequest,
        responses(
            (status = 200, description = "Bookings copied (with cross-source warnings if any)", body = CopyWeekResultTO),
            (status = 403, description = "Forbidden"),
        ),
    )]
    pub async fn copy_week_with_conflict_check<RestState: RestStateDef>(
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
        Json(req): Json<CopyWeekRequest>,
    ) -> Response {
        error_handler(
            (async {
                let svc = rest_state.shiftplan_edit_service();
                let result = svc
                    .copy_week_with_conflict_check(
                        req.from_calendar_week,
                        req.from_year,
                        req.to_calendar_week,
                        req.to_year,
                        context.into(),
                        None,
                    )
                    .await?;
                let to = CopyWeekResultTO::from(&result);
                Ok(Response::builder()
                    .status(200)
                    .header("Content-Type", "application/json")
                    .body(axum::body::Body::new(serde_json::to_string(&to).unwrap()))
                    .unwrap())
            })
            .await,
        )
    }
    ```

    e) **NEUER ApiDoc-Struct** `ShiftplanEditApiDoc` am Ende der Datei (existing rest/src/shiftplan_edit.rs hat heute keinen ApiDoc — Plan 05 ergänzt):
    ```rust
    #[derive(OpenApi)]
    #[openapi(
        paths(
            book_slot_with_conflict_check,
            copy_week_with_conflict_check,
        ),
        components(schemas(
            BookingTO, BookingCreateResultTO, CopyWeekResultTO, CopyWeekRequest,
            WarningTO, AbsenceCategoryTO, DayOfWeekTO,
        )),
        tags(
            (name = "ShiftplanEdit", description = "Shiftplan edit operations (conflict-aware)"),
        ),
    )]
    pub struct ShiftplanEditApiDoc;
    ```

    Bestehende `edit_slot`, `delete_slot`, `add_vacation`-Handler benötigen für ApiDoc-Inclusion `#[utoipa::path]` — falls sie das heute nicht haben (existing tech-debt), Plan-Phase **erlaubt** das Auslassen aus `paths(...)` (nur die neuen Phase-3-Handler einschließen). Begründung: utoipa-paths ohne `#[utoipa::path]`-Annotation würden Compile-Errors produzieren — Plan 05 belässt die existing untouched-Handler ohne Annotation und nimmt sie NICHT in ShiftplanEditApiDoc::paths auf.

    3. **Patch** `rest/src/lib.rs`:

    a) Im obersten `#[derive(OpenApi)] pub struct ApiDoc;` (Z. 460-484) den `nest`-Block erweitern um:
    ```rust
    (path = "/shiftplan-edit", api = shiftplan_edit::ShiftplanEditApiDoc),
    ```

    b) Verifiziere, dass die Route-Mounting-Stelle (`Router::new().nest(...)` weiter unten in `lib.rs`) bereits `/shiftplan-edit` mounted (sollte aus existing config) — falls nicht: ergänze entsprechend.

    Bei Bedarf: `use shiftplan_edit;`-Import oder `mod shiftplan_edit;` ist bereits vorhanden (verifiziere mit grep).

    4. **Boot-Smoke + OpenAPI-Smoke**:
    ```bash
    timeout 12 cargo run > /tmp/cargo-run.log 2>&1 &
    BOOT_PID=$!
    sleep 6
    curl -s http://localhost:3000/openapi.json 2>/dev/null | jq '.paths | keys' 2>/dev/null | grep -E "shiftplan-edit/booking|shiftplan-edit/copy-week" | wc -l
    kill $BOOT_PID 2>/dev/null
    wait 2>/dev/null
    ```
    Erwartet: ≥ 2 Treffer für die neuen Endpunkte. Falls 0 → ApiDoc nicht korrekt nested. Plan-Phase: bei Fehler debug `/openapi.json` direkt + ApiDoc-nest-Block in `rest/src/lib.rs` korrigieren.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -10</automated>
    <automated>cargo test --workspace 2>&1 | tail -10</automated>
    <automated>grep -c "AbsencePeriodCreateResultTO::from" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/absence.rs</automated>
    <automated>grep -c "book_slot_with_conflict_check\|copy_week_with_conflict_check" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/shiftplan_edit.rs</automated>
    <automated>grep -c "pub struct ShiftplanEditApiDoc\|ShiftplanEditApiDoc" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/shiftplan_edit.rs</automated>
    <automated>grep -c "shiftplan_edit::ShiftplanEditApiDoc" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/lib.rs</automated>
    <automated>jj diff rest/src/booking.rs 2>&1 | wc -l</automated>
  </verify>
  <done>
    - cargo build --workspace + cargo test --workspace GRÜN
    - rest/src/absence.rs nutzt vollen `AbsencePeriodCreateResultTO::from(&result)` (Verify-3 ≥ 2 — create + update)
    - rest/src/shiftplan_edit.rs hat 2 neue Handler-Funktionen (Verify-4 ≥ 2)
    - ShiftplanEditApiDoc existiert (Verify-5 ≥ 1)
    - rest/src/lib.rs hat ShiftplanEditApiDoc im nest-Block (Verify-6 ≥ 1)
    - **D-Phase3-18**: rest/src/booking.rs unangetastet (Verify-7 == 0)
  </done>
</task>

<task type="auto">
  <name>Task 3: 2 neue per-sales-person-Endpunkte unter `/shiftplan/...` + ApiDoc-Erweiterung</name>
  <read_first>
    - rest/src/shiftplan.rs (vollständig — Routes-Definition + heutige Handler-Pattern)
    - service/src/shiftplan.rs (Plan-04-Trait-Methoden `get_shiftplan_*_for_sales_person`)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 1280-1349 — Routes + Handler + ApiDoc)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-12, D-Phase3-13)
  </read_first>
  <files>rest/src/shiftplan.rs</files>
  <action>
    1. **Patch** `rest/src/shiftplan.rs`:

    a) `generate_route()` um zwei neue Routen erweitern:
    ```rust
    pub fn generate_route<RestState: RestStateDef>() -> Router<RestState> {
        Router::new()
            .route("/{shiftplan_id}/{year}/{week}", get(get_shiftplan_week::<RestState>))
            .route("/day/{year}/{week}/{day_of_week}", get(get_shiftplan_day::<RestState>))
            // NEU für Phase 3 (D-Phase3-12):
            .route(
                "/{shiftplan_id}/{year}/{week}/sales-person/{sales_person_id}",
                get(get_shiftplan_week_for_sales_person::<RestState>),
            )
            .route(
                "/day/{year}/{week}/{day_of_week}/sales-person/{sales_person_id}",
                get(get_shiftplan_day_for_sales_person::<RestState>),
            )
    }
    ```

    b) Neuer Handler `get_shiftplan_week_for_sales_person`:
    ```rust
    #[utoipa::path(
        get,
        path = "/{shiftplan_id}/{year}/{week}/sales-person/{sales_person_id}",
        params(
            ("shiftplan_id" = Uuid, Path, description = "Shift plan ID"),
            ("year" = u32, Path, description = "Year"),
            ("week" = u8, Path, description = "Calendar week (1-53)"),
            ("sales_person_id" = Uuid, Path, description = "Sales person id (HR ∨ self)"),
        ),
        responses(
            (status = 200, description = "Shift plan week with per-day unavailable marker", body = ShiftplanWeekTO),
            (status = 401, description = "Unauthorized"),
            (status = 403, description = "Forbidden"),
        ),
        tag = "shiftplan"
    )]
    async fn get_shiftplan_week_for_sales_person<RestState: RestStateDef>(
        Path((shiftplan_id, year, week, sales_person_id)): Path<(uuid::Uuid, u32, u8, uuid::Uuid)>,
        rest_state: State<RestState>,
        Extension(context): Extension<Context>,
    ) -> Response {
        error_handler(
            async {
                let week_value = rest_state
                    .shiftplan_view_service()
                    .get_shiftplan_week_for_sales_person(
                        shiftplan_id, year, week, sales_person_id,
                        Authentication::Context(context),
                        None,
                    )
                    .await?;
                let to = ShiftplanWeekTO::from(&week_value);
                Ok(Response::builder()
                    .status(200)
                    .header("Content-Type", "application/json")
                    .body(axum::body::Body::from(serde_json::to_string(&to).unwrap()))
                    .unwrap())
            }
            .await,
        )
    }
    ```

    c) Neuer Handler `get_shiftplan_day_for_sales_person` analog mit `Path((year, week, day_of_week, sales_person_id))`. Body-Type `ShiftplanDayAggregateTO::from(&day_aggregate)`.

    d) `ShiftplanApiDoc` (Z. ~101-117) `paths(...)` und `components(schemas(...))` erweitern:
    - paths: `get_shiftplan_week_for_sales_person`, `get_shiftplan_day_for_sales_person` ergänzen.
    - schemas: `UnavailabilityMarkerTO`, `AbsenceCategoryTO` ergänzen (falls noch nicht vorhanden).

    Verifiziere mit grep, dass `Authentication`, `RestStateDef`, `error_handler`, `Path`, `State`, `Extension` etc. importiert sind (sind sie bereits per existing Handler).

    2. KEINE Anpassung der globalen `get_shiftplan_week`/`get_shiftplan_day`-Handler (additiv).

    3. **OpenAPI-Smoke**:
    ```bash
    timeout 12 cargo run > /tmp/cargo-run.log 2>&1 &
    BOOT_PID=$!
    sleep 6
    curl -s http://localhost:3000/openapi.json 2>/dev/null | jq '.paths | keys[]' 2>/dev/null | grep -E "sales-person" | wc -l
    kill $BOOT_PID 2>/dev/null
    wait 2>/dev/null
    ```
    Erwartet: ≥ 2 Treffer (Week + Day-Variant).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -10</automated>
    <automated>cargo test --workspace 2>&1 | tail -10</automated>
    <automated>grep -c "get_shiftplan_week_for_sales_person\|get_shiftplan_day_for_sales_person" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/shiftplan.rs</automated>
    <automated>grep -c "sales-person" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/shiftplan.rs</automated>
    <automated>grep -c "UnavailabilityMarkerTO" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/shiftplan.rs</automated>
  </verify>
  <done>
    - cargo build --workspace + cargo test --workspace GRÜN
    - rest/src/shiftplan.rs hat 2 neue Handler-Funktionen (Verify-3 ≥ 4 — Funktion-Definitions + Route-Referenzen)
    - "sales-person" appears mehrfach in shiftplan.rs (Routes + Handler + ApiDoc) (Verify-4 ≥ 4)
    - UnavailabilityMarkerTO im ApiDoc-schemas (Verify-5 ≥ 1)
    - Globale `get_shiftplan_week`/`get_shiftplan_day` unangetastet
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → REST handler | utoipa-Pflicht für jede public Route; OpenAPI-Schema bestimmt Frontend-Generator-Output. |
| HTTP request → `POST /shiftplan-edit/booking` | Handler delegiert direkt an Service-Layer; Permission HR ∨ self gated im Service (D-Phase3-12). |
| HTTP request → `GET /shiftplan/.../sales-person/...` | Handler delegiert; Permission gated im Service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-REST-Spoof | Spoofing | Frontend könnte falsche `sales_person_id` in Path injizieren | mitigate | `verify_user_is_sales_person` im Service-Layer (D-Phase3-12); Plan-06-Tests verifizieren _forbidden. |
| T-3-OpenAPI-Drift | Tampering | utoipa-Schemas weichen von Service-Domain ab | mitigate | OpenAPI-Smoke (Boot + curl /openapi.json) in Task 2 + 3 verifiziert Endpunkte. From-Impls am `service::*`-Domain-Pfad gekoppelt — Compiler erzwingt Konsistenz. |
| T-3-Frontend-Migration | Tampering | Bestehender `POST /booking`-Endpunkt wird vom Frontend weiter genutzt; doppelte Wirkung? | accept | D-Phase3-18 — alter Endpunkt bleibt Basic-Surface; neuer Endpunkt parallel. Frontend-Migration im Frontend-Workstream. KEIN Doppel-Effekt: alter Endpunkt persistiert ohne Warnings, neuer mit. |
| T-3-Tag-Enum-utoipa | Tampering | utoipa erzeugt nicht-deterministisches Schema für `#[serde(tag, content)]` | accept | RESEARCH.md Pattern 4 + utoipa-5 docs.rs bestätigen Support. Falls Schema-Bug → Plan-Phase erlaubt Fallback auf plain-enum + extra `kind`-Field-Variante. |
</threat_model>

<verification>
Wave-4-REST-Layer-Verifikation:
- `cargo build --workspace` + `cargo test --workspace` GRÜN
- `cargo run` startet ohne Panic
- `curl http://localhost:3000/openapi.json | jq '.paths | keys'` zeigt:
  - `/absence-period` (existing — jetzt mit AbsencePeriodCreateResultTO Schema)
  - `/shiftplan-edit/booking` (NEU)
  - `/shiftplan-edit/copy-week` (NEU)
  - `/shiftplan/{shiftplan_id}/{year}/{week}/sales-person/{sales_person_id}` (NEU)
  - `/shiftplan/day/{year}/{week}/{day_of_week}/sales-person/{sales_person_id}` (NEU)
  - `/booking` (UNVERÄNDERT — gleiches Schema)
- `curl http://localhost:3000/openapi.json | jq '.components.schemas | keys'` enthält:
  - `WarningTO`, `UnavailabilityMarkerTO`, `BookingCreateResultTO`, `CopyWeekResultTO`, `AbsencePeriodCreateResultTO`
- **D-Phase3-18 Regression-Lock**: `jj diff service/src/booking.rs service_impl/src/booking.rs rest/src/booking.rs service_impl/src/test/booking.rs` = 0 Zeilen
</verification>

<success_criteria>
- [ ] `rest-types/src/lib.rs` hat 5 neue inline DTOs + From-Impls + ShiftplanDayTO-Field
- [ ] `rest/src/absence.rs` ruft `AbsencePeriodCreateResultTO::from(&result)` (Wrapper-DTO mit Warnings)
- [ ] `rest/src/shiftplan_edit.rs` hat 2 neue Handler + ShiftplanEditApiDoc
- [ ] `rest/src/shiftplan.rs` hat 2 neue per-sales-person-Handler + ApiDoc-Erweiterung
- [ ] `rest/src/lib.rs` hat `ShiftplanEditApiDoc` im obersten `nest`-Block
- [ ] `cargo build --workspace` + `cargo test --workspace` GRÜN
- [ ] `cargo run` boot-smoke ohne Panic; OpenAPI listet 4 neue Endpunkte (`shiftplan-edit/booking`, `shiftplan-edit/copy-week`, 2× `sales-person/...`)
- [ ] OpenAPI-Schemas enthalten alle 5 neuen DTOs (WarningTO, UnavailabilityMarkerTO, BookingCreateResultTO, CopyWeekResultTO, AbsencePeriodCreateResultTO)
- [ ] **D-Phase3-18 Regression-Lock**: 4 BookingService-Files unangetastet
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-shift-plan-konflikt-integration/03-05-SUMMARY.md` summarizing:
- Patched: rest-types/src/lib.rs (5 neue DTOs + ShiftplanDayTO-Field)
- Patched: rest/src/absence.rs (AbsencePeriodCreateResultTO-Body)
- Patched: rest/src/shiftplan_edit.rs (2 neue Handler + ShiftplanEditApiDoc)
- Patched: rest/src/shiftplan.rs (2 neue per-sales-person-Handler)
- Patched: rest/src/lib.rs (ApiDoc-Aggregation)
- OpenAPI-Smoke: 4 neue Endpunkte sind im Schema
- Confirmation: D-Phase3-18 Regression-Lock (4 BookingService-Files) unangetastet
- Note for Plan 06: REST-Surface ist Phase-3-konform; Plan 06 implementiert Cross-Source-Tests (full-stack via REST), aktiviert Stub-Tests aus Plan 01, finalisiert Verifikation.
</output>
