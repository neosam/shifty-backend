---
phase: 01-absence-domain-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - shifty_bin/src/main.rs
  - shifty_bin/src/integration_test/absence_period.rs
  - shifty_bin/src/integration_test.rs
autonomous: true
requirements: [ABS-01, ABS-02, ABS-03, ABS-04, ABS-05]
must_haves:
  truths:
    - "D-01, D-08, D-10: `AbsenceServiceDependencies`-Block in `shifty_bin/src/main.rs` ist analog `ExtraHoursServiceDependencies` definiert — folgt Modul-Naming-Konvention D-01, ohne `BookingService`-Dependency (D-08), ohne `SalesPersonShiftplanService` (D-10 via A2 Option A)."
    - "`RestStateImpl` enthält `absence_service: Arc<AbsenceService>`-Feld."
    - "`RestStateImpl` implementiert `absence_service()`-Methode aus `RestStateDef`."
    - "`AbsenceDaoImpl::new(pool.clone())` und `AbsenceServiceImpl { … }`-Instanz werden in `RestStateImpl::new` konstruiert."
    - "`cargo build --workspace` kompiliert ohne Errors."
    - "Integration-Test `test_create_assigns_id_equal_to_logical_id` läuft End-to-End gegen In-Memory-SQLite."
    - "Integration-Test `test_check_constraint_rejects_inverted_range` zeigt, dass DB-CHECK greift (defense-in-depth)."
    - "Integration-Test `test_partial_unique_index_enforces_one_active_per_logical_id` zeigt, dass partial unique index greift."
    - "Integration-Test `test_update_creates_tombstone_and_new_active_row` verifiziert logical_id-Update-Flow."
    - "Integration-Test `test_create_overlapping_same_category_returns_validation_error` verifiziert Self-Overlap-Detection End-to-End."
    - "Integration-Test `test_update_can_extend_range_without_self_collision` verifiziert D-15 in echter DB."
    - "Integration-Test `test_delete_softdeletes_row` verifiziert Soft-Delete-Pfad."
    - "Integration-Test `test_create_overlapping_different_category_succeeds` verifiziert D-12 (Cross-Category-Overlap erlaubt)."
    - "`cargo test --workspace` ist final grün (Phase-1-Erfolgskriterium 4)."
    - "Bestehende Reporting-/Booking-/Snapshot-Pfade unverändert (Phase-1-Erfolgskriterium 5; CC-07)."
  artifacts:
    - path: "shifty_bin/src/main.rs"
      provides: "DI-Verdrahtung für AbsenceService."
      contains: "AbsenceServiceDependencies"
    - path: "shifty_bin/src/integration_test/absence_period.rs"
      provides: "End-to-End-Tests für absence_period CRUD + Schema-Constraints."
      contains: "test_create_assigns_id_equal_to_logical_id"
    - path: "shifty_bin/src/integration_test.rs"
      provides: "Modul-Deklaration für integration_test/absence_period.rs."
      contains: "mod absence_period"
  key_links:
    - from: "shifty_bin/src/main.rs::RestStateImpl"
      to: "AbsenceServiceImpl"
      via: "Arc::new(service_impl::absence::AbsenceServiceImpl { … })"
      pattern: "AbsenceServiceImpl"
    - from: "shifty_bin/src/main.rs::RestStateImpl::absence_service"
      to: "Arc<AbsenceService>"
      via: "self.absence_service.clone()"
      pattern: "self.absence_service.clone"
    - from: "shifty_bin/src/integration_test/absence_period.rs"
      to: "TestSetup::new()"
      via: "echte In-Memory-SQLite + sqlx::migrate!"
      pattern: "TestSetup::new"
---

<objective>
Wave 4 — Plan 04 (Final): DI-Verdrahtung der Absence-Domain in `shifty_bin/src/main.rs` analog zum `ExtraHoursService`-Block, plus Integration-Tests in `shifty_bin/src/integration_test/absence_period.rs`. Final-Gate: `cargo test --workspace` grün; bestehende Pfade additiv unverändert (Phase-1-Erfolgskriterium 5).

Purpose: Phase 1 ist fertig, sobald (a) der Workspace inkl. shifty_bin baut, (b) Integration-Tests gegen echte DB CRUD + Schema-Constraints + Self-Overlap-Detection abdecken, und (c) keine Diffs in den Bestand-Files vorliegen.

Output: 1 main.rs-Patch (mehrere Stellen), 1 neue Datei (integration_test/absence_period.rs), 1 Patch in integration_test.rs. Phase-Boundary durch Final-Smoke-Gate verifiziert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-absence-domain-foundation/01-CONTEXT.md
@.planning/phases/01-absence-domain-foundation/01-RESEARCH.md
@.planning/phases/01-absence-domain-foundation/01-PATTERNS.md
@.planning/phases/01-absence-domain-foundation/01-VALIDATION.md
@.planning/phases/01-absence-domain-foundation/01-00-SUMMARY.md
@.planning/phases/01-absence-domain-foundation/01-01-SUMMARY.md
@.planning/phases/01-absence-domain-foundation/01-02-SUMMARY.md
@.planning/phases/01-absence-domain-foundation/01-03-SUMMARY.md
@shifty_bin/src/main.rs
@shifty_bin/src/integration_test.rs
@shifty_bin/src/integration_test/extra_hours_update.rs

<interfaces>
<!-- Verifizierte Anchor-Lines aus shifty_bin/src/main.rs (`grep -n "ExtraHoursServiceDependencies\\|extra_hours" main.rs`). -->

`shifty_bin/src/main.rs:10` — Import-Block:
```rust
use dao_impl_sqlite::{
    /* … */ extra_hours::ExtraHoursDaoImpl, sales_person::SalesPersonDaoImpl, /* … */
};
```

`shifty_bin/src/main.rs:38` — DAO-Type-Alias:
```rust
type ExtraHoursDao = ExtraHoursDaoImpl;
```

`shifty_bin/src/main.rs:223-236` — ServiceDependencies-Block (Vorlage für `AbsenceServiceDependencies`):
```rust
pub struct ExtraHoursServiceDependencies;
impl service_impl::extra_hours::ExtraHoursServiceDeps for ExtraHoursServiceDependencies {
    type Context = Context;
    type Transaction = Transaction;
    type ExtraHoursDao = ExtraHoursDao;
    type PermissionService = PermissionService;
    type SalesPersonService = SalesPersonService;
    type CustomExtraHoursService = CustomExtraHoursService;
    type ClockService = ClockService;
    type UuidService = UuidService;
    type TransactionDao = TransactionDao;
}
type ExtraHoursService =
    service_impl::extra_hours::ExtraHoursServiceImpl<ExtraHoursServiceDependencies>;
```

`shifty_bin/src/main.rs:447` — RestStateImpl-Feld (Vorlage):
```rust
extra_hours_service: Arc<ExtraHoursService>,
```

`shifty_bin/src/main.rs:476, 534-535` — RestStateDef-Impl (Type + Methode):
```rust
type ExtraHoursService = ExtraHoursService;
fn extra_hours_service(&self) -> Arc<Self::ExtraHoursService> { self.extra_hours_service.clone() }
```

`shifty_bin/src/main.rs:587, 680-688` — DAO-Init + Service-Konstruktion (Vorlage):
```rust
let extra_hours_dao = Arc::new(ExtraHoursDao::new(pool.clone()));
// …
let extra_hours_service = Arc::new(service_impl::extra_hours::ExtraHoursServiceImpl {
    extra_hours_dao,
    permission_service: permission_service.clone(),
    sales_person_service: sales_person_service.clone(),
    custom_extra_hours_service: custom_extra_hours_service.clone(),
    clock_service: clock_service.clone(),
    uuid_service: uuid_service.clone(),
    transaction_dao: transaction_dao.clone(),
});
```

`shifty_bin/src/integration_test.rs:1437` — Modul-Deklarationen (Pattern; KEIN `mod.rs`):
```rust
mod billing_period_custom_reports;
mod billing_period_snapshot_versioning;
mod extra_hours_update;
mod dev_seed;
```

`shifty_bin/src/integration_test/extra_hours_update.rs:1-128` — Integration-Test-Vorlage mit `TestSetup::new()`, `create_sales_person`, Tombstone-Verify per direktem SQLx-Query.
</interfaces>

<additivity>
- KEIN Editieren von `service_impl/src/extra_hours.rs`, `service_impl/src/booking.rs`, `service_impl/src/billing_period_report.rs`, `service_impl/src/reporting.rs`.
- KEIN Bump von `CURRENT_SNAPSHOT_SCHEMA_VERSION` (CC-07).
- Bestehende Integration-Tests bleiben unverändert.
</additivity>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4.1: `shifty_bin/src/main.rs` PATCH — DI-Block, RestStateImpl-Erweiterung</name>
  <files>shifty_bin/src/main.rs</files>
  <read_first>
    - shifty_bin/src/main.rs (Zeilen 1–60 Imports + Type-Aliases; 220–250 ExtraHoursServiceDependencies; 440–540 RestStateImpl + RestStateDef-Impl; 580–700 RestStateImpl::new mit Service-Konstruktion)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer G Adaptation Notes Zeilen 1351–1361)
    - service_impl/src/absence.rs (Wave 2 — gen_service_impl!-Output)
    - dao_impl_sqlite/src/absence.rs (Wave 1)
  </read_first>
  <behavior>
    - `cargo build --workspace` kompiliert nach diesem Task.
    - `RestStateImpl` hat ein neues `absence_service: Arc<AbsenceService>`-Feld.
    - `RestStateDef::absence_service()` ist auf `RestStateImpl` implementiert.
    - DAO und Service werden in `RestStateImpl::new` konstruiert; Reihenfolge respektiert die existierenden Service-Konstruktions-Ketten (Permission/SalesPerson/Clock/Uuid/Transaction sind bereits vorher konstruiert).
  </behavior>
  <action>
    Wende **6 inkrementelle Patches** auf `shifty_bin/src/main.rs` an:

    1. **Patch 1 — Use-Block-Erweiterung (~ZZ. 10 Imports von dao_impl_sqlite):**
       Vorhanden:
       ```rust
       use dao_impl_sqlite::{
           /* … */
           extra_hours::ExtraHoursDaoImpl, sales_person::SalesPersonDaoImpl,
           /* … */
       };
       ```
       Hinzufügen `absence::AbsenceDaoImpl,` (alphabetisch davor):
       ```rust
       use dao_impl_sqlite::{
           /* … */
           absence::AbsenceDaoImpl,
           extra_hours::ExtraHoursDaoImpl, sales_person::SalesPersonDaoImpl,
           /* … */
       };
       ```

    2. **Patch 2 — DAO-Type-Alias (neben ZZ. 38 `type ExtraHoursDao = ExtraHoursDaoImpl;`):**
       Hinzufügen davor:
       ```rust
       type AbsenceDao = AbsenceDaoImpl;
       ```

    3. **Patch 3 — `AbsenceServiceDependencies`-Block (analog `ExtraHoursServiceDependencies`-Block ZZ. 223–236), an strukturell sinnvoller Stelle (idiomatisch alphabetisch oder neben `ExtraHoursServiceDependencies`):**
       ```rust
       pub struct AbsenceServiceDependencies;
       impl service_impl::absence::AbsenceServiceDeps for AbsenceServiceDependencies {
           type Context = Context;
           type Transaction = Transaction;
           type AbsenceDao = AbsenceDao;
           type PermissionService = PermissionService;
           type SalesPersonService = SalesPersonService;
           type ClockService = ClockService;
           type UuidService = UuidService;
           type TransactionDao = TransactionDao;
       }
       type AbsenceService =
           service_impl::absence::AbsenceServiceImpl<AbsenceServiceDependencies>;
       ```
       (Option A — KEIN `SalesPersonShiftplanService`, KEIN `CustomExtraHoursService`. Reihenfolge der Felder MUSS exakt mit dem `gen_service_impl!`-Output aus Plan 02 übereinstimmen.)

    4. **Patch 4 — `RestStateImpl`-Feld (im Struct-Definition-Block ~ZZ. 440–460, neben `extra_hours_service: Arc<ExtraHoursService>` ZZ. 447):**
       Hinzufügen davor:
       ```rust
       absence_service: Arc<AbsenceService>,
       ```

    5. **Patch 5 — `RestStateDef`-Impl auf `RestStateImpl` (im Block ~ZZ. 470–540, zwei Stellen):**

       a. Bei den `type Xxx = Xxx;`-Zeilen (~ZZ. 471–476), neben `type ExtraHoursService = ExtraHoursService;`, hinzufügen:
          ```rust
          type AbsenceService = AbsenceService;
          ```

       b. Bei den `fn xxx_service(&self) -> Arc<Self::Xxx>`-Methoden (~ZZ. 519–540), neben `fn extra_hours_service`, hinzufügen:
          ```rust
          fn absence_service(&self) -> Arc<Self::AbsenceService> {
              self.absence_service.clone()
          }
          ```

    6. **Patch 6 — `RestStateImpl::new`-Konstruktor (~ZZ. 587 für DAO + ~ZZ. 680–688 für Service):**

       a. Nach der DAO-Konstruktion (`let extra_hours_dao = Arc::new(ExtraHoursDao::new(pool.clone()));`), hinzufügen davor:
          ```rust
          let absence_dao = Arc::new(AbsenceDao::new(pool.clone()));
          ```

       b. Nach der Service-Konstruktion (analog zu `let extra_hours_service = Arc::new(...);` ZZ. 680–688), hinzufügen vorher:
          ```rust
          let absence_service = Arc::new(service_impl::absence::AbsenceServiceImpl {
              absence_dao,
              permission_service: permission_service.clone(),
              sales_person_service: sales_person_service.clone(),
              clock_service: clock_service.clone(),
              uuid_service: uuid_service.clone(),
              transaction_dao: transaction_dao.clone(),
          });
          ```

       c. In der finalen `RestStateImpl { … }`-Struct-Initialisierung (Datei-Ende, vermutlich ~ZZ. 850+) hinzufügen `absence_service,` neben `extra_hours_service,`.

    Anti-Patterns explizit verboten:
    - KEIN `custom_extra_hours_service: …` in der `AbsenceServiceImpl { … }`-Struct (Option A — Service hat diese Dependency nicht).
    - KEIN `sales_person_shiftplan_service: …` in der `AbsenceServiceImpl { … }`-Struct.
    - KEIN Bump von `CURRENT_SNAPSHOT_SCHEMA_VERSION`.
    - KEIN Editieren der `ExtraHoursServiceDependencies`/`ExtraHoursServiceImpl { … }`-Blöcke.
    - KEIN Reorder der bestehenden `RestStateImpl`-Felder.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "absence::AbsenceDaoImpl" shifty_bin/src/main.rs` ≥ 1.
    - `grep -c "type AbsenceDao = AbsenceDaoImpl" shifty_bin/src/main.rs` = 1.
    - `grep -c "pub struct AbsenceServiceDependencies" shifty_bin/src/main.rs` = 1.
    - `grep -c "service_impl::absence::AbsenceServiceDeps for AbsenceServiceDependencies" shifty_bin/src/main.rs` = 1.
    - `grep -c "type AbsenceService =\\s*service_impl::absence::AbsenceServiceImpl" shifty_bin/src/main.rs` = 1.
    - `grep -c "absence_service: Arc<AbsenceService>" shifty_bin/src/main.rs` = 1.
    - `grep -c "type AbsenceService = AbsenceService" shifty_bin/src/main.rs` = 1.
    - `grep -c "fn absence_service(&self) -> Arc<Self::AbsenceService>" shifty_bin/src/main.rs` = 1.
    - `grep -c "let absence_dao = Arc::new(AbsenceDao::new(pool.clone()))" shifty_bin/src/main.rs` = 1.
    - `grep -c "service_impl::absence::AbsenceServiceImpl {" shifty_bin/src/main.rs` = 1.
    - Innerhalb des `AbsenceServiceImpl { … }`-Blocks: `awk '/AbsenceServiceImpl \{/,/}\)/' shifty_bin/src/main.rs | grep -c "custom_extra_hours_service\\|sales_person_shiftplan_service"` = 0 (Option A).
    - `cargo build --workspace` exit 0 (FINAL!).
    - `git diff service_impl/src/billing_period_report.rs service_impl/src/extra_hours.rs service_impl/src/booking.rs service_impl/src/reporting.rs` ist leer (additivity, CC-07).
  </acceptance_criteria>
  <done>DI-Verdrahtung komplett; Workspace kompiliert; alle Phase-1-Layer (DAO, Service, REST, DI) sind nun lauffähig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4.2: `shifty_bin/src/integration_test/absence_period.rs` — End-to-End-Tests gegen In-Memory-SQLite</name>
  <files>shifty_bin/src/integration_test/absence_period.rs, shifty_bin/src/integration_test.rs</files>
  <read_first>
    - shifty_bin/src/integration_test/extra_hours_update.rs (komplette Vorlage 1–271)
    - shifty_bin/src/integration_test.rs (Modul-Decl-Block ~ZZ. 1430–1440; TestSetup::new() ~ZZ. 266–300)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§11.2 Mindest-Test-Set Zeilen 1481–1493)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer F integration_test Zeilen 1129–1294)
    - .planning/phases/01-absence-domain-foundation/01-VALIDATION.md (Per-Task-Verifikations-Map ABS-01..ABS-04)
  </read_first>
  <behavior>
    - 8 Integration-Tests laufen gegen In-Memory-SQLite mit lebender Migration (Phase-0-Migration aktiv).
    - DB-CHECK-Constraint blockt Direkt-INSERT mit `to_date < from_date`.
    - Partial Unique Index blockt zweite aktive Row mit gleicher `logical_id`.
    - logical_id-Update-Pfad legt Tombstone + neue Row an, verifiziert per direkter SQLx-Query.
    - Self-Overlap (Same-Category) wird vom Service als `ServiceError::ValidationError` zurückgewiesen.
    - Self-Overlap (Cross-Category, D-12) ist erlaubt.
    - D-15: Update mit verlängerter Range gegen sich selbst kollidiert NICHT.
    - Soft-Delete macht Row für `find_by_id` unsichtbar.
  </behavior>
  <action>
    1. **Neue Datei `shifty_bin/src/integration_test/absence_period.rs`** mit folgender Struktur (Vorlage: `extra_hours_update.rs`):

       a. **Imports** (analog Vorlage):
          ```rust
          use std::sync::Arc;

          use rest::RestStateDef;
          use service::absence::{AbsenceCategory, AbsencePeriod, AbsenceService};
          use service::permission::Authentication;
          use service::sales_person::{SalesPerson, SalesPersonService};
          use sqlx::Row;
          use time::macros::date;
          use uuid::Uuid;

          use crate::integration_test::TestSetup;
          ```

       b. **Helper `create_sales_person`** (1:1 aus `extra_hours_update.rs:14-29` adaptiert):
          ```rust
          async fn create_sales_person(test_setup: &TestSetup, name: &str) -> SalesPerson {
              test_setup.rest_state.sales_person_service().create(
                  &SalesPerson {
                      id: Uuid::nil(),
                      version: Uuid::nil(),
                      name: name.into(),
                      background_color: "#000000".into(),
                      inactive: false,
                      is_paid: Some(true),
                      deleted: None,
                  },
                  Authentication::Full,
                  None,
              ).await.unwrap()
          }
          ```

       c. **Helper `create_absence_period`**:
          ```rust
          async fn create_absence_period(test_setup: &TestSetup, sales_person_id: Uuid) -> AbsencePeriod {
              test_setup.rest_state.absence_service().create(
                  &AbsencePeriod {
                      id: Uuid::nil(),
                      sales_person_id,
                      category: AbsenceCategory::Vacation,
                      from_date: date!(2026-04-12),
                      to_date: date!(2026-04-15),
                      description: "initial".into(),
                      created: None,
                      deleted: None,
                      version: Uuid::nil(),
                  },
                  Authentication::Full,
                  None,
              ).await.unwrap()
          }
          ```

       d. **8 Tests** (alle als `#[tokio::test]`):

          1. **`test_create_assigns_id_equal_to_logical_id`** — analog `extra_hours_update.rs:58-75`. Verifiziert: `created.id != Uuid::nil()`, `created.version != Uuid::nil()`, direkte SQLx-Probe `SELECT id, logical_id FROM absence_period WHERE id = ?` zeigt `id == logical_id` (first version).

          2. **`test_update_creates_tombstone_and_new_active_row`** — analog `extra_hours_update.rs:79-128`. Erst `create_absence_period`, dann `update` mit verlängertem `to_date`. Direkte SQLx-Query: `SELECT id, deleted FROM absence_period WHERE logical_id = ? ORDER BY created ASC` muss 2 Rows liefern; erste hat `deleted IS NOT NULL`, zweite hat `deleted IS NULL`.

          3. **`test_partial_unique_index_enforces_one_active_per_logical_id`** — analog `extra_hours_update.rs:192-231`. Erst `create_absence_period`, dann **direkter** SQLx-INSERT mit gleichem `logical_id` und `deleted = NULL`:
             ```rust
             let result = sqlx::query(
                 "INSERT INTO absence_period (id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_process, update_version) VALUES (?, ?, ?, ?, ?, ?, ?, ?, NULL, ?, ?)"
             )
             .bind(/* anderes id */)
             .bind(/* gleiches logical_id */)
             ...
             .execute(pool).await;
             assert!(result.is_err(), "second active row with same logical_id should violate the partial unique index");
             ```

          4. **`test_check_constraint_rejects_inverted_range`** — Direkter SQLx-INSERT mit `to_date < from_date`:
             ```rust
             let result = sqlx::query("INSERT INTO absence_period (...) VALUES (..., '2026-05-20', '2026-05-15', ...)")
                 .execute(pool).await;
             assert!(result.is_err(), "DB CHECK should reject inverted range");
             let err_msg = result.unwrap_err().to_string();
             assert!(err_msg.to_lowercase().contains("check"), "expected CHECK violation, got: {}", err_msg);
             ```

          5. **`test_create_overlapping_same_category_returns_validation_error`**:
             ```rust
             let initial = create_absence_period(&test_setup, sp.id).await;
             let conflict = AbsencePeriod {
                 id: Uuid::nil(),
                 sales_person_id: sp.id,
                 category: AbsenceCategory::Vacation,
                 from_date: date!(2026-04-14),  // overlaps initial 2026-04-12..15
                 to_date: date!(2026-04-18),
                 description: "conflict".into(),
                 created: None, deleted: None, version: Uuid::nil(),
             };
             let result = test_setup.rest_state.absence_service().create(&conflict, Authentication::Full, None).await;
             assert!(matches!(result, Err(service::ServiceError::ValidationError(_))));
             ```

          6. **`test_create_overlapping_different_category_succeeds`** (D-12):
             ```rust
             let _initial = create_absence_period(&test_setup, sp.id).await;  // Vacation 12..15
             let sick = AbsencePeriod {
                 id: Uuid::nil(), sales_person_id: sp.id,
                 category: AbsenceCategory::SickLeave,  // different category
                 from_date: date!(2026-04-13), to_date: date!(2026-04-14),  // covered by Vacation
                 description: "sick".into(),
                 created: None, deleted: None, version: Uuid::nil(),
             };
             let result = test_setup.rest_state.absence_service().create(&sick, Authentication::Full, None).await;
             assert!(result.is_ok(), "different categories should be allowed to overlap (D-12), got: {:?}", result);
             ```

          7. **`test_update_can_extend_range_without_self_collision`** (D-15):
             ```rust
             let initial = create_absence_period(&test_setup, sp.id).await;  // 12..15
             let extended = AbsencePeriod {
                 id: initial.id,
                 sales_person_id: sp.id,
                 category: AbsenceCategory::Vacation,
                 from_date: date!(2026-04-10),  // earlier
                 to_date: date!(2026-04-20),    // later — fully encompasses old range
                 description: "extended".into(),
                 created: initial.created,
                 deleted: None,
                 version: initial.version,
             };
             let result = test_setup.rest_state.absence_service().update(&extended, Authentication::Full, None).await;
             assert!(result.is_ok(), "extending self range should not collide with self (D-15), got: {:?}", result);
             ```

          8. **`test_delete_softdeletes_row`**:
             ```rust
             let initial = create_absence_period(&test_setup, sp.id).await;
             test_setup.rest_state.absence_service().delete(initial.id, Authentication::Full, None).await.unwrap();
             let result = test_setup.rest_state.absence_service().find_by_id(initial.id, Authentication::Full, None).await;
             assert!(matches!(result, Err(service::ServiceError::EntityNotFound(_))));
             // Plus direct SQL probe:
             let pool = test_setup.pool.as_ref();
             let logical_id_bytes = initial.id.as_bytes().to_vec();
             let row = sqlx::query("SELECT deleted FROM absence_period WHERE logical_id = ? ORDER BY created DESC LIMIT 1")
                 .bind(&logical_id_bytes).fetch_one(pool).await.unwrap();
             let deleted: Option<String> = row.get("deleted");
             assert!(deleted.is_some(), "row should have deleted set after soft delete");
             ```

    2. **Patch `shifty_bin/src/integration_test.rs`** — neue Modul-Deklaration neben den bestehenden (~ZZ. 1437):
       Vorhanden:
       ```rust
       mod billing_period_custom_reports;
       mod billing_period_snapshot_versioning;
       mod extra_hours_update;
       mod dev_seed;
       ```
       Hinzufügen direkt davor (alphabetisch):
       ```rust
       mod absence_period;
       ```

    Anti-Patterns explizit verboten:
    - KEIN Bestand-Test in `extra_hours_update.rs` ändern (Pitfall 7).
    - KEIN Hand-rolled Mock — Integration-Tests nutzen die echte In-Memory-SQLite über `TestSetup::new()`.
    - KEIN Test, der eine Datei außerhalb `integration_test/absence_period.rs` schreibt.
    - KEIN Hardcoding von realen production-Datenbank-Pfaden — nur `sqlite::memory:`-Pool.
    - KEIN `Authentication::Anonymous` (Phase 1 setzt voll-eingeloggte User voraus; `forbid_unauthenticated`-Middleware ist unbeachtet hier).
  </action>
  <verify>
    <automated>cargo test -p shifty_bin --test integration_test absence_period 2>&amp;1 || cargo test -p shifty_bin integration_test::absence_period</automated>
  </verify>
  <acceptance_criteria>
    - `test -f shifty_bin/src/integration_test/absence_period.rs` exit 0.
    - `grep -c "mod absence_period;" shifty_bin/src/integration_test.rs` = 1.
    - `grep -E "#\\[tokio::test\\]" shifty_bin/src/integration_test/absence_period.rs | wc -l` ≥ 8.
    - Pflicht-Tests: `grep -E "fn (test_create_assigns_id_equal_to_logical_id|test_update_creates_tombstone_and_new_active_row|test_partial_unique_index_enforces_one_active_per_logical_id|test_check_constraint_rejects_inverted_range|test_create_overlapping_same_category_returns_validation_error|test_create_overlapping_different_category_succeeds|test_update_can_extend_range_without_self_collision|test_delete_softdeletes_row)" shifty_bin/src/integration_test/absence_period.rs | wc -l` ≥ 8.
    - `grep -c "TestSetup::new" shifty_bin/src/integration_test/absence_period.rs` ≥ 8 (jeder Test setzt frische DB auf).
    - `grep -c "Authentication::Full" shifty_bin/src/integration_test/absence_period.rs` ≥ 8.
    - `grep -c "absence_service()" shifty_bin/src/integration_test/absence_period.rs` ≥ 6.
    - `grep -c "sqlx::query" shifty_bin/src/integration_test/absence_period.rs` ≥ 3 (Direkt-SQL für Constraint-Tests).
    - `cargo test -p shifty_bin integration_test::absence_period 2>&1 | grep -c "FAILED"` = 0.
    - `cargo test -p shifty_bin integration_test::absence_period 2>&1 | grep -E "test result: ok\\.\\s+[0-9]+\\s+passed"` matcht (≥ 8 Tests grün).
  </acceptance_criteria>
  <done>8 Integration-Tests grün; CRUD + Schema-Constraints + Self-Overlap + D-12 + D-15 + Soft-Delete End-to-End verifiziert.</done>
</task>

<task type="auto">
  <name>Task 4.3: Phase-1-Final-Smoke-Gate (`cargo test --workspace` + Additivitäts-Check + Smoke `cargo run`)</name>
  <files>(no file modifications — final verification gate)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase-1 Success Criteria 1–5)
    - .planning/phases/01-absence-domain-foundation/01-VALIDATION.md (Phase-Gate)
    - .planning/phases/01-absence-domain-foundation/01-CONTEXT.md (Strikt-NICHT-in-Scope-Liste)
  </read_first>
  <action>
    Run sequentially (jeder Schritt MUSS exit 0 / no failures liefern):

    1. **Build-Gate:**
       ```
       cargo build --workspace
       ```

    2. **Test-Gate:**
       ```
       cargo test --workspace
       ```
       Erwartet: alle bestehenden Tests bleiben grün (Phase-1-Erfolgskriterium 5: "Bestehende Reporting-, Booking- und Snapshot-Pfade liefern bit-identische Ergebnisse"); neue Phase-1-Tests sind grün.

    3. **Additivitäts-Audit:**
       ```
       git diff service_impl/src/billing_period_report.rs \
                service_impl/src/reporting.rs \
                service_impl/src/extra_hours.rs \
                service_impl/src/booking.rs \
                dao_impl_sqlite/src/extra_hours.rs \
                dao_impl_sqlite/src/booking.rs \
                rest/src/extra_hours.rs \
                rest/src/booking.rs
       ```
       Erwartet: leerer Output. Falls eine Zeile auftaucht, ist Pitfall 7 verletzt — Fix vor Phase-Abschluss.

    4. **Snapshot-Versioning-Gate (CC-07):**
       ```
       grep -E "CURRENT_SNAPSHOT_SCHEMA_VERSION\\s*:\\s*u32\\s*=" service_impl/src/billing_period_report.rs
       ```
       Erwartet: Wert UNVERÄNDERT seit Phase-Start (vom User-Operator gegen `git log` zu validieren — Phase 2 ist die Bump-Phase).

    5. **REST-Smoke (cargo run, CC-08-Pflicht — KEIN "optional"):**
       Smoke-Run mit Timeout (CC-08-Pflicht):
         - Laufanweisung: `timeout 10s cargo run --bin shifty_bin`
         - Erwartet: Exit-Code 0 (sauber beendet) oder 124 (Timeout — Server lief und wurde nach 10s gekillt). Beides ist OK = sauber gestartet.
         - Fehler: Jeder andere Exit-Code = Panic während Boot → Final-Gate fehlschlagen lassen.
         - NixOS: ggf. `nix-shell -p sqlx-cli --run "timeout 10s cargo run --bin shifty_bin"` falls Migration zur Boot-Zeit läuft.
       Konkrete Shell-Form:
       ```
       timeout 10s cargo run --bin shifty_bin || ([ $? -eq 124 ] && echo "OK: started, timed out as expected" || (echo "PANIC during boot" && exit 1))
       ```

    Bei jedem Failure: NICHT Fix-up im Final-Gate; stattdessen Issue dokumentieren und zurück in den verursachenden Plan iterieren.
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exit 0.
    - `cargo test --workspace 2>&1 | grep -c "FAILED"` = 0.
    - `cargo test --workspace 2>&1 | grep -E "test result: ok"` matcht für jeden Crate (kein FAILED in einem Sub-Crate).
    - `cargo test -p service_impl test::absence 2>&1 | grep -c "FAILED"` = 0 und mindestens 13 Tests grün.
    - `cargo test -p shifty_bin integration_test::absence_period 2>&1 | grep -c "FAILED"` = 0 und mindestens 8 Tests grün.
    - `cargo test -p shifty-utils date_range 2>&1 | grep -c "FAILED"` = 0 und mindestens 8 Tests grün.
    - Additivitäts-Diff: `git diff service_impl/src/billing_period_report.rs service_impl/src/reporting.rs service_impl/src/extra_hours.rs service_impl/src/booking.rs dao_impl_sqlite/src/extra_hours.rs dao_impl_sqlite/src/booking.rs rest/src/extra_hours.rs rest/src/booking.rs | wc -l` = 0.
    - `git status --short` zeigt nur erwartete Files: neue Dateien (`migrations/sqlite/*_create-absence-period.sql`, `shifty-utils/src/date_range.rs`, `dao/src/absence.rs`, `dao_impl_sqlite/src/absence.rs`, `service/src/absence.rs`, `service_impl/src/absence.rs`, `service_impl/src/test/absence.rs`, `rest/src/absence.rs`, `shifty_bin/src/integration_test/absence_period.rs`, `.planning/phases/01-absence-domain-foundation/*-SUMMARY.md`) plus Patches (`shifty-utils/src/lib.rs`, `service/src/lib.rs`, `service_impl/src/lib.rs`, `dao/src/lib.rs`, `dao_impl_sqlite/src/lib.rs`, `service_impl/src/test/mod.rs`, `rest/src/lib.rs`, `rest-types/src/lib.rs`, `shifty_bin/src/main.rs`, `shifty_bin/src/integration_test.rs`, `.sqlx/`-Updates).
    - Snapshot-Schema-Versioning: `git diff service_impl/src/billing_period_report.rs | grep -i "CURRENT_SNAPSHOT_SCHEMA_VERSION" | wc -l` = 0 (CC-07).
    - `timeout 10s cargo run --bin shifty_bin` exits with code 0 or 124 (124 = clean startup that hit the timeout); any other exit code = boot panic, blocks Final-Gate (CC-08).
  </acceptance_criteria>
  <done>Phase 1 fertig. Alle 5 ROADMAP-Success-Criteria erfüllt: HR-CRUD per REST funktioniert, `_forbidden`-Tests pro Methode existieren, Self-Overlap-Detection arbeitet, Integration-Test-Roundtrip ist grün, bestehende Pfade unverändert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production-Deploy → DB | Migration läuft im Bestand-Boot (außerhalb dieses Plans); Phase 1 vertraut darauf. |
| Integration-Test → In-Memory-DB | sqlite::memory: ist isoliert pro `TestSetup::new()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | DI-Verdrahtung könnte Service mit falscher Dep instanziieren | mitigate | Compiler erzwingt Type-Match (`AbsenceServiceDeps`-Trait Bounds); `cargo build --workspace` ist Hard-Gate. |
| T-01-04-02 | Information Disclosure | Integration-Tests könnten Production-DB-Pfad benutzen | accept | Pattern verwendet `sqlite::memory:`-Pool über bestehenden `TestSetup::new()` — keine Production-Zugriffe. |
| T-01-04-03 | Repudiation | Audit-Trail (`update_process`) wird im Service-Layer korrekt geschrieben | mitigate | Service-Layer (Plan 02) übergibt Process-Strings an DAO. Integration-Test verifiziert, dass `update_process` für tombstone und neue Row gesetzt ist. |
| T-01-04-04 | Tampering | Phase-Boundary-Verletzung: Phase 1 ändert Reporting-Pfad | mitigate | Final-Gate-Task führt expliziten `git diff` gegen alle Bestand-Reporting/Booking/Snapshot-Files (CC-07, Pitfall 7). |
</threat_model>

<verification>
- `cargo build --workspace` und `cargo test --workspace` als Hard-Gates.
- `git diff` gegen die in `<additivity>` aufgelisteten Files MUSS leer sein.
- `cargo run` Smoke-Test (mit Timeout, optional) bestätigt Boot ohne Panic.
- Snapshot-Schema-Version unverändert (manuell vom Operator gegen `git log` zu validieren).
</verification>

<success_criteria>
Alle 5 Phase-1-ROADMAP-Success-Criteria erfüllt:
1. ✓ HR-CRUD-Endpoints arbeiten (Plan 03 + Plan 04 Integration-Test).
2. ✓ `_forbidden`-Test pro public service method (Plan 02 Test 2.3).
3. ✓ Self-Overlap-Detection (Plan 02 Service-Logic + Plan 04 Integration-Test).
4. ✓ `cargo test --workspace` grün; Integration-Test deckt CRUD + Soft-Delete + logical_id-Update.
5. ✓ Bestehende Reporting-/Booking-/Snapshot-Pfade unverändert (Final-Gate Diff-Check).
</success_criteria>

<output>
After completion, create `.planning/phases/01-absence-domain-foundation/01-04-SUMMARY.md` mit:
- Liste der DI-Patches in `main.rs` (6 Stellen mit Zeilenangabe).
- Liste der 8 Integration-Tests + Pass-Status.
- Confirmed: `cargo test --workspace` grün; alle 5 Phase-1-Success-Criteria erfüllt.
- Confirmed: Additivitäts-Diff ist leer (CC-07, Pitfall 7).
- Confirmed: keine `CURRENT_SNAPSHOT_SCHEMA_VERSION`-Bump.
- Open Items für Phase 2:
  * Reporting-Integration (`derive_hours_for_range`).
  * Snapshot-Schema-Bump 2 → 3 im selben Commit wie der Reporting-Switch.
  * Sick-overlapping-Vacation Policy (BUrlG §9) — Discuss-Phase 2.
- Open Items für Phase 3:
  * Booking-Forward-Warning + Wrapper-Type (BOOK-01).
  * `find_overlapping_for_booking` (cross-category).
  * D-10 Schichtplan-Kollege-Read-Sicht (deferred per A2 Option A).
</output>
