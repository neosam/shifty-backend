---
phase: 01-absence-domain-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - service/src/absence.rs
  - service/src/lib.rs
  - service_impl/src/absence.rs
  - service_impl/src/lib.rs
  - service_impl/src/test/absence.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [ABS-01, ABS-03, ABS-05]
must_haves:
  truths:
    - "`AbsenceService`-Trait existiert mit 6 Methoden (find_all, find_by_sales_person, find_by_id, create, update, delete)."
    - "Domain-Modell `AbsencePeriod` hat `id == logical_id`-Semantik und Helper `date_range()`."
    - "`AbsenceCategory`-Enum (3 Varianten) mit bidirektionalen `From`-Conversions zu `dao::absence::AbsenceCategoryEntity`."
    - "D-08, D-10: `AbsenceServiceImpl` ist via `gen_service_impl!` mit Deps `AbsenceDao`, `PermissionService`, `SalesPersonService`, `ClockService`, `UuidService`, `TransactionDao` instanziiert — KEIN `BookingService`-Dependency (D-08), KEIN `SalesPersonShiftplanService` in Phase 1 (D-10 Read-Sicht via A2 Option A: HR ∨ self only; Schichtplan-Kollegen-Erweiterung deferred to Phase 3)."
    - "D-09: Schreib-Methoden enforcen Permission per `tokio::join!(check_permission(HR_PRIVILEGE), verify_user_is_sales_person(sales_person_id))`."
    - "D-07, D-13, D-14: `create` validiert Range (`DateRange::new` → `DateOrderWrong` per D-14 Defense-in-Depth) und Self-Overlap (`find_overlapping(.., None)`); bei Konflikt `ValidationError([OverlappingPeriod(other_logical_id)])` per D-13 (A1)."
    - "D-07, D-15: `update` folgt 1:1 dem ExtraHours-logical_id-Pattern (find_by_logical_id → permission → sales_person match → version match → tombstone alt → insert neu mit gleichem logical_id, neuer physical id, neuer version → commit) und validiert Self-Overlap mit `exclude_logical_id: Some(logical_id)` (D-15)."
    - "`update` erzwingt `sales_person_id`-Immutability (D-06) und `version`-Optimistic-Lock."
    - "`delete` ist Soft-Delete via `update(entity { deleted: Some(now) })`."
    - "Mindestens 13 Service-Tests grün, davon je ein `_forbidden`-Test für jede public method (D-11, ABS-05)."
  artifacts:
    - path: "service/src/absence.rs"
      provides: "AbsenceService-Trait, AbsencePeriod, AbsenceCategory, MockAbsenceService"
      exports: ["AbsenceService", "AbsencePeriod", "AbsenceCategory", "MockAbsenceService"]
    - path: "service_impl/src/absence.rs"
      provides: "AbsenceServiceImpl mit gen_service_impl!-DI"
      contains: "AbsenceServiceImpl"
    - path: "service_impl/src/test/absence.rs"
      provides: "Mock-basierte Service-Tests"
      contains: "test_create_success"
  key_links:
    - from: "service_impl/src/absence.rs::create"
      to: "absence_dao.find_overlapping(.., None, ..)"
      via: "Self-Overlap-Check vor DAO::create"
      pattern: "find_overlapping"
    - from: "service_impl/src/absence.rs::update"
      to: "absence_dao.find_overlapping(.., Some(logical_id), ..)"
      via: "Self-Overlap-Check exkludiert eigene Row (D-15)"
      pattern: "Some\\(logical_id\\)"
    - from: "service_impl/src/absence.rs"
      to: "ServiceError::ValidationError(Arc::from([OverlappingPeriod(...)]))"
      via: "Self-Overlap → Validation-Channel"
      pattern: "OverlappingPeriod"
    - from: "service_impl/src/test/absence.rs"
      to: "test_forbidden(&result)"
      via: "Pro public method ein Forbidden-Test (ABS-05, D-11)"
      pattern: "test_forbidden"
---

<objective>
Wave 2 — Plan 02: Service-Layer für `absence`. Trait, Domain-Modell, Enum, `From`-Conversions in `service/`. `gen_service_impl!`-Block, Range-Validation, Self-Overlap-Detection (Create + Update mit `exclude_logical_id`), Permission-Gate (HR ∨ self), `logical_id`-Update-Pattern (Tombstone + neue Row), Soft-Delete in `service_impl/`. Mock-basierte Unit-Tests pro Methode mit `_success` und `_forbidden` (D-11, ABS-05).

Purpose: Plan 03 (REST) hängt vom `MockAbsenceService` und der konkreten Service-Surface ab. Plan 04 (DI in main.rs) braucht `AbsenceServiceImpl` mit korrekter Deps-Struktur. Plan 05 (Integration-Test) verifiziert die Service-Logik gegen echte DB.

Output: 4 neue Dateien, 2 `lib.rs`-Patches, 1 `test/mod.rs`-Patch, 13+ Tests grün.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-absence-domain-foundation/01-CONTEXT.md
@.planning/phases/01-absence-domain-foundation/01-RESEARCH.md
@.planning/phases/01-absence-domain-foundation/01-PATTERNS.md
@.planning/phases/01-absence-domain-foundation/01-VALIDATION.md
@.planning/phases/01-absence-domain-foundation/01-00-SUMMARY.md
@.planning/phases/01-absence-domain-foundation/01-01-SUMMARY.md
@service/src/extra_hours.rs
@service_impl/src/extra_hours.rs
@service_impl/src/test/extra_hours.rs
@service_impl/src/test/error_test.rs
@service_impl/src/macros.rs

<interfaces>
<!-- Konkrete Konturen — direkt zitierfähig, keine Erkundung nötig. -->

`service/src/extra_hours.rs:185-235` — Trait-Vorlage für `AbsenceService`:
```rust
#[automock(type Context=(); type Transaction=dao::MockTransaction;)]
#[async_trait]
pub trait ExtraHoursService {
    type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
    type Transaction: dao::Transaction;
    async fn create(&self, entity: &ExtraHours, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<ExtraHours, ServiceError>;
    // …
}
```

`service_impl/src/extra_hours.rs:22-32` — `gen_service_impl!`-Vorlage:
```rust
gen_service_impl! {
    struct ExtraHoursServiceImpl: ExtraHoursService = ExtraHoursServiceDeps {
        ExtraHoursDao: ExtraHoursDao<Transaction = Self::Transaction> = extra_hours_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,
        CustomExtraHoursService: CustomExtraHoursService<Context = Self::Context, Transaction = Self::Transaction> = custom_extra_hours_service,
        ClockService: ClockService = clock_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

`service_impl/src/extra_hours.rs:220-301` — Update-Flow mit logical_id (1:1 Vorlage).

`service_impl/src/extra_hours.rs:236-245` — Permission-Pattern:
```rust
let (hr_permission, sales_person_permission) = join!(
    self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
    self.sales_person_service.verify_user_is_sales_person(active.sales_person_id, context, tx.clone().into()),
);
hr_permission.or(sales_person_permission)?;
```

`service_impl/src/test/error_test.rs:5-138` — Test-Helpers:
```rust
pub fn test_forbidden<T>(result: &Result<T, service::ServiceError>);
pub fn test_not_found<T>(result: &Result<T, service::ServiceError>, target_id: &Uuid);
pub fn test_date_order_wrong<T>(result: &Result<T, service::ServiceError>);
pub fn test_conflicts<T>(...);
pub fn test_validation_error<T>(...);
```

`service/src/lib.rs:48-117` — `ValidationFailureItem` (mit `OverlappingPeriod(Uuid)` aus Wave 0) und `ServiceError`-Surface.
</interfaces>

<additivity>
- KEIN Diff an `service_impl/src/extra_hours.rs`, `service_impl/src/booking.rs`, `service_impl/src/billing_period_report.rs`, `service_impl/src/reporting.rs` (Pitfall 7, CC-07).
- KEIN Bump von `CURRENT_SNAPSHOT_SCHEMA_VERSION`.
- KEIN neuer Privilegien-Konstante in `service/src/permission.rs` (D-09: HR_PRIVILEGE reicht).
</additivity>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.1: `service/src/absence.rs` — Trait, Domain-Modell, Enum, From-Conversions</name>
  <files>service/src/absence.rs, service/src/lib.rs</files>
  <read_first>
    - service/src/extra_hours.rs (Vorlage komplett — 1–235)
    - service/src/lib.rs (Zeilen 17–22 für `pub mod`-Pattern; 40 für Re-Exports)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§5 + Code-Skizze Zeilen 670–814)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer D service-trait Zeilen 372–479)
    - .planning/phases/01-absence-domain-foundation/01-CONTEXT.md (D-01..D-08)
    - dao/src/absence.rs (Wave 1 — Entity-Surface verifizieren)
  </read_first>
  <behavior>
    - `AbsenceCategory` ↔ `dao::absence::AbsenceCategoryEntity` ist roundtrippable: `AbsenceCategory::from(&AbsenceCategoryEntity::from(&AbsenceCategory::Vacation)) == AbsenceCategory::Vacation`.
    - `AbsencePeriod::from(&dao::absence::AbsencePeriodEntity { logical_id: X, ..})` setzt `id = X` (Domain-id == logical_id).
    - `TryFrom<&AbsencePeriod> for AbsencePeriodEntity` wirft `ServiceError::InternalError` falls `created.is_none()`.
    - `AbsencePeriod::date_range()` gibt `Err(DateOrderWrong)` für `from > to`.
    - `MockAbsenceService` ist von Tests in Plan 03 (REST) und Wave 2 (Service-Tests) importierbar.
  </behavior>
  <action>
    1. **Neue Datei `service/src/absence.rs`** mit folgendem Inhalt (Vorlage: `service/src/extra_hours.rs`, RESEARCH §5):

       ```rust
       use std::fmt::Debug;
       use std::sync::Arc;

       use async_trait::async_trait;
       use mockall::automock;
       use shifty_utils::DateRange;
       use time::Date;
       use uuid::Uuid;

       use crate::{permission::Authentication, ServiceError};

       #[derive(Clone, Copy, Debug, PartialEq, Eq)]
       pub enum AbsenceCategory {
           Vacation,
           SickLeave,
           UnpaidLeave,
       }

       impl From<&dao::absence::AbsenceCategoryEntity> for AbsenceCategory {
           fn from(c: &dao::absence::AbsenceCategoryEntity) -> Self {
               match c {
                   dao::absence::AbsenceCategoryEntity::Vacation => Self::Vacation,
                   dao::absence::AbsenceCategoryEntity::SickLeave => Self::SickLeave,
                   dao::absence::AbsenceCategoryEntity::UnpaidLeave => Self::UnpaidLeave,
               }
           }
       }
       impl From<&AbsenceCategory> for dao::absence::AbsenceCategoryEntity {
           fn from(c: &AbsenceCategory) -> Self {
               match c {
                   AbsenceCategory::Vacation => Self::Vacation,
                   AbsenceCategory::SickLeave => Self::SickLeave,
                   AbsenceCategory::UnpaidLeave => Self::UnpaidLeave,
               }
           }
       }

       #[derive(Clone, Debug, PartialEq, Eq)]
       pub struct AbsencePeriod {
           /// Externally stable id == DAO logical_id. Equals the physical row id of the first version.
           pub id: Uuid,
           pub sales_person_id: Uuid,
           pub category: AbsenceCategory,
           pub from_date: Date,
           pub to_date: Date,
           pub description: Arc<str>,
           pub created: Option<time::PrimitiveDateTime>,
           pub deleted: Option<time::PrimitiveDateTime>,
           pub version: Uuid,
       }

       impl From<&dao::absence::AbsencePeriodEntity> for AbsencePeriod {
           fn from(e: &dao::absence::AbsencePeriodEntity) -> Self {
               Self {
                   id: e.logical_id,
                   sales_person_id: e.sales_person_id,
                   category: (&e.category).into(),
                   from_date: e.from_date,
                   to_date: e.to_date,
                   description: e.description.clone(),
                   created: Some(e.created),
                   deleted: e.deleted,
                   version: e.version,
               }
           }
       }
       impl TryFrom<&AbsencePeriod> for dao::absence::AbsencePeriodEntity {
           type Error = ServiceError;
           fn try_from(a: &AbsencePeriod) -> Result<Self, Self::Error> {
               Ok(Self {
                   id: a.id,
                   logical_id: a.id,
                   sales_person_id: a.sales_person_id,
                   category: (&a.category).into(),
                   from_date: a.from_date,
                   to_date: a.to_date,
                   description: a.description.clone(),
                   created: a.created.ok_or(ServiceError::InternalError)?,
                   deleted: a.deleted,
                   version: a.version,
               })
           }
       }

       impl AbsencePeriod {
           pub fn date_range(&self) -> Result<DateRange, ServiceError> {
               DateRange::new(self.from_date, self.to_date)
                   .map_err(|_| ServiceError::DateOrderWrong(self.from_date, self.to_date))
           }
       }

       #[automock(type Context=(); type Transaction=dao::MockTransaction;)]
       #[async_trait]
       pub trait AbsenceService {
           type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
           type Transaction: dao::Transaction;

           /// HR only — full visibility (D-10 Option A).
           async fn find_all(
               &self,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<Arc<[AbsencePeriod]>, ServiceError>;

           /// HR ∨ verify_user_is_sales_person(sales_person_id) (D-10 Option A; Schichtplan-Kollege deferred to Phase 3).
           async fn find_by_sales_person(
               &self,
               sales_person_id: Uuid,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<Arc<[AbsencePeriod]>, ServiceError>;

           /// HR ∨ self (Self ermittelt aus active row's sales_person_id).
           async fn find_by_id(
               &self,
               id: Uuid,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<AbsencePeriod, ServiceError>;

           async fn create(
               &self,
               entity: &AbsencePeriod,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<AbsencePeriod, ServiceError>;

           async fn update(
               &self,
               entity: &AbsencePeriod,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<AbsencePeriod, ServiceError>;

           async fn delete(
               &self,
               id: Uuid,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<(), ServiceError>;
       }
       ```

    2. **Patch `service/src/lib.rs`** — füge `pub mod absence;` neben `pub mod extra_hours;` ein (alphabetisch davor; Zeile 17 ist `pub mod custom_extra_hours;`):
       ```rust
       pub mod absence;
       ...
       pub mod extra_hours;
       ```

    Anti-Patterns explizit verboten:
    - KEINE Conversion `From<&service::extra_hours::ExtraHoursCategory> for AbsenceCategory` (D-03 saubere Domain-Trennung).
    - KEIN `Custom`/`Holiday`/`Unavailable`/`VolunteerWork`/`ExtraWork` in `AbsenceCategory` (D-02).
    - KEIN `pub mut from_date` etc. — Domain-Felder bleiben pub-Read aber strukturell stabil; der Update-Flow läuft NIE durch direktes Mutieren der Bestand-Row, sondern über `update(entity { ... })`-Aufrufe.
  </action>
  <verify>
    <automated>cargo build -p service</automated>
  </verify>
  <acceptance_criteria>
    - `test -f service/src/absence.rs` exit 0.
    - `grep -c "pub trait AbsenceService" service/src/absence.rs` = 1.
    - `grep -c "pub struct AbsencePeriod" service/src/absence.rs` = 1.
    - `grep -c "pub enum AbsenceCategory" service/src/absence.rs` = 1.
    - `grep -E "Vacation|SickLeave|UnpaidLeave" service/src/absence.rs | wc -l` ≥ 6 (3 in `AbsenceCategory`, 3 in zwei From-Match-Armen — mind. 6).
    - `grep -c "Custom\\|ExtraWork\\|Holiday\\|Unavailable\\|VolunteerWork" service/src/absence.rs` = 0 (D-02/03).
    - `grep -E "fn (find_all|find_by_sales_person|find_by_id|create|update|delete)" service/src/absence.rs | wc -l` ≥ 6.
    - `grep -c "id: e.logical_id" service/src/absence.rs` = 1 (Domain-id == logical_id).
    - `grep -c "DateOrderWrong" service/src/absence.rs` ≥ 1 (date_range Helper).
    - `grep -c "#\\[automock(type Context=(); type Transaction=dao::MockTransaction;)\\]" service/src/absence.rs` = 1.
    - `grep -c "pub mod absence" service/src/lib.rs` = 1.
    - `cargo build -p service` exit 0.
    - `cargo test -p service --no-run` exit 0.
  </acceptance_criteria>
  <done>Service-Trait + Domain-Modell + Conversions kompilieren; `MockAbsenceService` automatisch via automock vorhanden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: `service_impl/src/absence.rs` — `AbsenceServiceImpl` mit gen_service_impl!, CRUD, Permission, Self-Overlap</name>
  <files>service_impl/src/absence.rs, service_impl/src/lib.rs</files>
  <read_first>
    - service_impl/src/extra_hours.rs (komplette Vorlage 1–348)
    - service_impl/src/macros.rs (gen_service_impl!-Macro-Definition)
    - service_impl/src/booking.rs (Zeilen 76–98 — HR-only Pattern für find_all)
    - service_impl/src/lib.rs (Zeile 17 — `pub mod`-Pattern)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§5–§9 + Patterns S-1..S-6 in PATTERNS)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer D Service-Impl Zeilen 484–686)
    - .planning/phases/01-absence-domain-foundation/01-CONTEXT.md (D-06..D-15)
    - service/src/absence.rs (Wave 2 Task 2.1 — Trait verifizieren)
    - dao/src/absence.rs (Wave 1 — DAO-Surface verifizieren)
  </read_first>
  <behavior>
    - `find_all` lehnt non-HR-User mit `Forbidden` ab.
    - `find_by_sales_person` und `find_by_id` lehnen User ab, die weder HR sind noch die Sales-Person selbst (D-10 Option A).
    - `create` mit `from > to` → `ServiceError::DateOrderWrong`.
    - `create` mit Same-Category-Overlap → `ServiceError::ValidationError([OverlappingPeriod(other_logical_id)])`.
    - `create` mit Different-Category-Overlap → erfolgreich (D-12).
    - `create` mit `id != Uuid::nil()` → `ServiceError::IdSetOnCreate`.
    - `create` mit `version != Uuid::nil()` → `ServiceError::VersionSetOnCreate`.
    - `update` mit unbekannter `logical_id` → `ServiceError::EntityNotFound`.
    - `update` mit anderer `sales_person_id` als active row → `ServiceError::ValidationError([ModificationNotAllowed("sales_person_id")])`.
    - `update` mit stale `version` → `ServiceError::EntityConflicts(logical_id, expected, actual)`.
    - `update` Self-Overlap-Check exkludiert die alte Row via `exclude_logical_id: Some(logical_id)` (D-15).
    - `update` legt Tombstone (UPDATE deleted=now) und neue Row (INSERT mit gleichem logical_id, neuem id+version) an (D-07).
    - `delete` ist Soft-Delete via `update(tombstone)`.
    - Jede public method hat einen `_forbidden`-Test (ABS-05, D-11).
  </behavior>
  <action>
    1. **Neue Datei `service_impl/src/absence.rs`** mit:
       a. **Imports** (analog `extra_hours.rs:1-21`):
          ```rust
          use crate::gen_service_impl;
          use std::sync::Arc;

          use async_trait::async_trait;
          use dao::{
              absence::{self, AbsenceDao},
              TransactionDao,
          };
          use service::{
              absence::{AbsenceCategory, AbsencePeriod, AbsenceService},
              clock::ClockService,
              permission::{Authentication, HR_PRIVILEGE},
              sales_person::SalesPersonService,
              uuid_service::UuidService,
              PermissionService, ServiceError, ValidationFailureItem,
          };
          use shifty_utils::DateRange;
          use tokio::join;
          use uuid::Uuid;
          ```

       b. **`gen_service_impl!`-Block** (Option A — KEIN `SalesPersonShiftplanService`, KEIN `CustomExtraHoursService`):
          ```rust
          gen_service_impl! {
              struct AbsenceServiceImpl: AbsenceService = AbsenceServiceDeps {
                  AbsenceDao: AbsenceDao<Transaction = Self::Transaction> = absence_dao,
                  PermissionService: PermissionService<Context = Self::Context> = permission_service,
                  SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,
                  ClockService: ClockService = clock_service,
                  UuidService: UuidService = uuid_service,
                  TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
              }
          }
          ```

       c. **`#[async_trait] impl<Deps: AbsenceServiceDeps> AbsenceService for AbsenceServiceImpl<Deps>`** mit den 6 Methoden.

       d. **`find_all`** — HR-only (RESEARCH §9 Code-Skizze):
          ```rust
          async fn find_all(&self, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<Arc<[AbsencePeriod]>, ServiceError> {
              let tx = self.transaction_dao.use_transaction(tx).await?;
              self.permission_service.check_permission(HR_PRIVILEGE, context).await?;
              let entities = self.absence_dao.find_all(tx.clone()).await?;
              let result: Arc<[AbsencePeriod]> = entities.iter().map(AbsencePeriod::from).collect();
              self.transaction_dao.commit(tx).await?;
              Ok(result)
          }
          ```

       e. **`find_by_sales_person`** — HR ∨ self (RESEARCH §9 Option A):
          ```rust
          async fn find_by_sales_person(&self, sales_person_id: Uuid, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<Arc<[AbsencePeriod]>, ServiceError> {
              let tx = self.transaction_dao.use_transaction(tx).await?;
              let (hr, sp) = join!(
                  self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
                  self.sales_person_service.verify_user_is_sales_person(sales_person_id, context, tx.clone().into()),
              );
              hr.or(sp)?;
              let entities = self.absence_dao.find_by_sales_person(sales_person_id, tx.clone()).await?;
              let result: Arc<[AbsencePeriod]> = entities.iter().map(AbsencePeriod::from).collect();
              self.transaction_dao.commit(tx).await?;
              Ok(result)
          }
          ```

       f. **`find_by_id`** — HR ∨ self auf entity.sales_person_id:
          ```rust
          async fn find_by_id(&self, id: Uuid, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<AbsencePeriod, ServiceError> {
              let tx = self.transaction_dao.use_transaction(tx).await?;
              let active = self.absence_dao.find_by_logical_id(id, tx.clone()).await?
                  .ok_or(ServiceError::EntityNotFound(id))?;
              let (hr, sp) = join!(
                  self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
                  self.sales_person_service.verify_user_is_sales_person(active.sales_person_id, context, tx.clone().into()),
              );
              hr.or(sp)?;
              let result = AbsencePeriod::from(&active);
              self.transaction_dao.commit(tx).await?;
              Ok(result)
          }
          ```

       g. **`create`** — Permission + IdSetOnCreate + VersionSetOnCreate + Range + Self-Overlap + Insert:
          ```rust
          async fn create(&self, request: &AbsencePeriod, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<AbsencePeriod, ServiceError> {
              let tx = self.transaction_dao.use_transaction(tx).await?;
              let (hr, sp) = join!(
                  self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
                  self.sales_person_service.verify_user_is_sales_person(request.sales_person_id, context, tx.clone().into()),
              );
              hr.or(sp)?;

              let mut entity = request.to_owned();
              if !entity.id.is_nil() { return Err(ServiceError::IdSetOnCreate); }
              if !entity.version.is_nil() { return Err(ServiceError::VersionSetOnCreate); }
              if entity.deleted.is_some() { return Err(ServiceError::DeletedSetOnCreate); }
              if entity.created.is_some() { return Err(ServiceError::CreatedSetOnCreate); }

              let new_range = DateRange::new(entity.from_date, entity.to_date)
                  .map_err(|_| ServiceError::DateOrderWrong(entity.from_date, entity.to_date))?;

              let conflicts = self.absence_dao.find_overlapping(
                  entity.sales_person_id,
                  (&entity.category).into(),
                  new_range,
                  None,
                  tx.clone(),
              ).await?;
              if !conflicts.is_empty() {
                  return Err(ServiceError::ValidationError(Arc::from([
                      ValidationFailureItem::OverlappingPeriod(conflicts[0].logical_id),
                  ])));
              }

              entity.id = self.uuid_service.new_uuid("absence_service::create::id");
              entity.version = self.uuid_service.new_uuid("absence_service::create::version");
              entity.created = Some(self.clock_service.date_time_now());

              let dao_entity = absence::AbsencePeriodEntity::try_from(&entity)?;
              self.absence_dao.create(&dao_entity, "absence_service::create", tx.clone()).await?;

              self.transaction_dao.commit(tx).await?;
              Ok(entity)
          }
          ```

       h. **`update`** — wörtlich nach RESEARCH.md ZZ. 343–423 mit `exclude_logical_id: Some(logical_id)` (D-15) und `ModificationNotAllowed("sales_person_id")` + `EntityConflicts`:
          ```rust
          async fn update(&self, request: &AbsencePeriod, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<AbsencePeriod, ServiceError> {
              let tx = self.transaction_dao.use_transaction(tx).await?;
              let logical_id = request.id;

              let active = self.absence_dao.find_by_logical_id(logical_id, tx.clone()).await?
                  .ok_or(ServiceError::EntityNotFound(logical_id))?;

              let (hr, sp) = join!(
                  self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
                  self.sales_person_service.verify_user_is_sales_person(active.sales_person_id, context, tx.clone().into()),
              );
              hr.or(sp)?;

              if request.sales_person_id != active.sales_person_id {
                  return Err(ServiceError::ValidationError(Arc::from([
                      ValidationFailureItem::ModificationNotAllowed("sales_person_id".into()),
                  ])));
              }
              if request.version != active.version {
                  return Err(ServiceError::EntityConflicts(logical_id, request.version, active.version));
              }

              let new_range = DateRange::new(request.from_date, request.to_date)
                  .map_err(|_| ServiceError::DateOrderWrong(request.from_date, request.to_date))?;

              let conflicts = self.absence_dao.find_overlapping(
                  active.sales_person_id,
                  (&request.category).into(),
                  new_range,
                  Some(logical_id),
                  tx.clone(),
              ).await?;
              if !conflicts.is_empty() {
                  return Err(ServiceError::ValidationError(Arc::from([
                      ValidationFailureItem::OverlappingPeriod(conflicts[0].logical_id),
                  ])));
              }

              let mut tombstone = active.clone();
              tombstone.deleted = Some(self.clock_service.date_time_now());
              self.absence_dao.update(&tombstone, "absence_service::update::soft_delete", tx.clone()).await?;

              let new_id = self.uuid_service.new_uuid("absence_service::update::id");
              let new_version = self.uuid_service.new_uuid("absence_service::update::version");
              let now = self.clock_service.date_time_now();

              let new_entity = absence::AbsencePeriodEntity {
                  id: new_id,
                  logical_id: active.logical_id,
                  sales_person_id: active.sales_person_id,
                  category: (&request.category).into(),
                  from_date: request.from_date,
                  to_date: request.to_date,
                  description: request.description.clone(),
                  created: now,
                  deleted: None,
                  version: new_version,
              };
              self.absence_dao.create(&new_entity, "absence_service::update::insert", tx.clone()).await?;

              self.transaction_dao.commit(tx).await?;
              Ok(AbsencePeriod::from(&new_entity))
          }
          ```

       i. **`delete`** — Soft-Delete via `update(tombstone)`:
          ```rust
          async fn delete(&self, id: Uuid, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<(), ServiceError> {
              let tx = self.transaction_dao.use_transaction(tx).await?;
              let active = self.absence_dao.find_by_logical_id(id, tx.clone()).await?
                  .ok_or(ServiceError::EntityNotFound(id))?;
              let (hr, sp) = join!(
                  self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
                  self.sales_person_service.verify_user_is_sales_person(active.sales_person_id, context, tx.clone().into()),
              );
              hr.or(sp)?;

              let mut tombstone = active;
              tombstone.deleted = Some(self.clock_service.date_time_now());
              self.absence_dao.update(&tombstone, "absence_service::delete", tx.clone()).await?;

              self.transaction_dao.commit(tx).await?;
              Ok(())
          }
          ```

    2. **Patch `service_impl/src/lib.rs`** — füge `pub mod absence;` neben `pub mod extra_hours;` (Zeile 17) ein:
       ```rust
       pub mod absence;
       ```
       (alphabetisch davor in den existierenden Block).

    Anti-Patterns explizit verboten:
    - KEINE direkte Mutation der DAO-Entity ohne Tombstone+Insert (D-07; verwende immer `update(tombstone)` + `create(neu)`).
    - KEIN `delete`-DAO-Call (DAO hat keine `delete`-Methode; nutze `update(entity { deleted: Some(now) })`).
    - KEIN `exclude_logical_id: None` im `update`-Pfad (D-15: alte Row exkludieren!).
    - KEIN Dependency auf `BookingService` oder Booking-Lookups (D-08).
    - KEIN `SalesPersonShiftplanService` in `gen_service_impl!` (Option A; defer to Phase 3).
    - KEIN Editieren von `service_impl/src/extra_hours.rs` (Pitfall 7).
  </action>
  <verify>
    <automated>cargo build -p service_impl</automated>
  </verify>
  <acceptance_criteria>
    - `test -f service_impl/src/absence.rs` exit 0.
    - `grep -c "gen_service_impl!" service_impl/src/absence.rs` = 1.
    - `grep -c "struct AbsenceServiceImpl" service_impl/src/absence.rs` = 1.
    - `grep -c "AbsenceServiceDeps" service_impl/src/absence.rs` ≥ 1.
    - `grep -c "SalesPersonShiftplanService\\|CustomExtraHoursService" service_impl/src/absence.rs` = 0 (Option A).
    - `grep -c "BookingService\\|booking_service" service_impl/src/absence.rs` = 0 (D-08).
    - `grep -E "fn (find_all|find_by_sales_person|find_by_id|create|update|delete)" service_impl/src/absence.rs | wc -l` ≥ 6.
    - `grep -c "Some(logical_id)" service_impl/src/absence.rs` ≥ 1 (D-15 Update-Self-Overlap-Filter).
    - `grep -c "exclude_logical_id\\s*:\\s*None\\|None,\\s*tx" service_impl/src/absence.rs` ≥ 1 (Create-Pfad ohne Exclude).
    - `grep -c "DateRange::new" service_impl/src/absence.rs` ≥ 2 (create + update).
    - `grep -c "DateOrderWrong" service_impl/src/absence.rs` ≥ 2 (create + update).
    - `grep -c "OverlappingPeriod" service_impl/src/absence.rs` ≥ 2 (create + update).
    - `grep -c "ModificationNotAllowed" service_impl/src/absence.rs` ≥ 1 (update sales_person_id-immutable).
    - `grep -c "EntityConflicts" service_impl/src/absence.rs` ≥ 1 (version-Optimistic-Lock).
    - `grep -c "IdSetOnCreate\\|VersionSetOnCreate" service_impl/src/absence.rs` ≥ 2.
    - `grep -c "tokio::join\\|join!" service_impl/src/absence.rs` ≥ 4 (find_by_sales_person, find_by_id, create, update, delete = 5; Pflicht ≥ 4).
    - `grep -c "HR_PRIVILEGE" service_impl/src/absence.rs` ≥ 5 (alle 5 permissioned methods außer find_all+find_by_id verwenden join, find_all check_permission solo — also ≥ 5 inkl. find_all).
    - `grep -c "use_transaction\\|transaction_dao" service_impl/src/absence.rs` ≥ 6 (jede Methode).
    - `grep -c "transaction_dao.commit" service_impl/src/absence.rs` ≥ 6 (jede Methode).
    - `grep -c "pub mod absence" service_impl/src/lib.rs` = 1.
    - `cargo build -p service_impl` exit 0.
    - `git diff service_impl/src/extra_hours.rs service_impl/src/booking.rs service_impl/src/billing_period_report.rs service_impl/src/reporting.rs` ist leer (Pitfall 7).
  </acceptance_criteria>
  <done>Service-Impl kompiliert; alle 6 Methoden implementiert mit Permission-Gate, Range-Validation, Self-Overlap, logical_id-Update.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.3: `service_impl/src/test/absence.rs` — Mock-DI + 13+ Service-Tests inkl. `_forbidden` pro Methode</name>
  <files>service_impl/src/test/absence.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/extra_hours.rs (komplette Vorlage 1–450)
    - service_impl/src/test/error_test.rs (Helper test_forbidden, test_validation_error, test_date_order_wrong, test_conflicts, test_not_found)
    - service_impl/src/test/mod.rs (Pattern Zeilen 37–38)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§5 Test-Matrix Zeilen 1075–1102, §11.1)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer F service_impl test Zeilen 982–1126)
    - .planning/phases/01-absence-domain-foundation/01-VALIDATION.md (Per-Task-Verifikations-Map)
    - service_impl/src/absence.rs (Wave 2 Task 2.2)
    - Lese-Hilfe: Tests deklarieren `let mut deps = build_dependencies();` — alle Mock-Felder werden via `&mut self` über `deps.<dao>.expect_*(...)` konfiguriert. Vorlage: `service_impl/src/test/extra_hours.rs:160`.
  </read_first>
  <behavior>
    - Build-Time: `MockAbsenceDao`, `MockPermissionService`, `MockSalesPersonService`, `MockClockService`, `MockUuidService`, `MockTransactionDao` füllen alle Deps des `AbsenceServiceImpl`.
    - Tests laufen in &lt; 5 s (Sampling-Rate aus VALIDATION.md).
    - 13+ Tests grün; mindestens 1 `_forbidden`-Test pro public method (find_all, find_by_sales_person, find_by_id, create, update, delete = 6 forbidden tests minimum).
    - Self-Overlap-Test verifiziert konkret `OverlappingPeriod(other_logical_id)`-Kontext (nicht bloß `Forbidden`).
    - Update-Self-Overlap-Test verifiziert via Mock-Predicate, dass `find_overlapping` mit `Some(default_logical_id)` aufgerufen wird (D-15 enforcement).
  </behavior>
  <action>
    1. **Neue Datei `service_impl/src/test/absence.rs`** mit folgendem Aufbau (1:1 Vorlage `service_impl/src/test/extra_hours.rs`, adaptiert):

       a. **Imports + Test-Konstanten** (analog `extra_hours.rs:1-86`):
          ```rust
          use std::sync::Arc;

          use dao::absence::{AbsenceCategoryEntity, AbsencePeriodEntity, MockAbsenceDao};
          use dao::DaoError;
          use dao::MockTransaction;
          use dao::MockTransactionDao;
          use mockall::predicate::{always, eq};
          use service::absence::{AbsenceCategory, AbsencePeriod, AbsenceService};
          use service::clock::MockClockService;
          use service::permission::{Authentication, HR_PRIVILEGE};
          use service::sales_person::MockSalesPersonService;
          use service::uuid_service::MockUuidService;
          use service::{MockPermissionService, ServiceError, ValidationFailureItem};
          use time::macros::{date, datetime};
          use uuid::{uuid, Uuid};

          use crate::absence::{AbsenceServiceDeps, AbsenceServiceImpl};
          use crate::test::error_test::{test_conflicts, test_date_order_wrong, test_forbidden, test_not_found, test_validation_error};

          fn default_logical_id() -> Uuid { uuid!("AB000000-0000-0000-0000-000000000001") }
          fn default_physical_id() -> Uuid { default_logical_id() }
          fn alternate_physical_id() -> Uuid { uuid!("AB000000-0000-0000-0000-000000000002") }
          fn other_logical_id() -> Uuid { uuid!("AB000000-0000-0000-0000-000000000099") }
          fn unknown_logical_id() -> Uuid { uuid!("AB000000-0000-0000-0000-0000000000FF") }
          fn default_sales_person_id() -> Uuid { uuid!("BB000000-0000-0000-0000-000000000001") }
          fn other_sales_person_id() -> Uuid { uuid!("BB000000-0000-0000-0000-000000000002") }
          fn default_version() -> Uuid { uuid!("CC000000-0000-0000-0000-000000000001") }
          fn alternate_version() -> Uuid { uuid!("CC000000-0000-0000-0000-000000000002") }

          fn default_active_entity() -> AbsencePeriodEntity {
              AbsencePeriodEntity {
                  id: default_physical_id(),
                  logical_id: default_logical_id(),
                  sales_person_id: default_sales_person_id(),
                  category: AbsenceCategoryEntity::Vacation,
                  from_date: date!(2026-04-12),
                  to_date: date!(2026-04-15),
                  description: "initial".into(),
                  created: datetime!(2026-04-01 12:00:00),
                  deleted: None,
                  version: default_version(),
              }
          }
          fn default_create_request() -> AbsencePeriod {
              AbsencePeriod {
                  id: Uuid::nil(),
                  sales_person_id: default_sales_person_id(),
                  category: AbsenceCategory::Vacation,
                  from_date: date!(2026-04-12),
                  to_date: date!(2026-04-15),
                  description: "initial".into(),
                  created: None,
                  deleted: None,
                  version: Uuid::nil(),
              }
          }
          fn default_update_request() -> AbsencePeriod {
              AbsencePeriod {
                  id: default_logical_id(),
                  sales_person_id: default_sales_person_id(),
                  category: AbsenceCategory::Vacation,
                  from_date: date!(2026-04-12),
                  to_date: date!(2026-04-20), // verlängerter Range — D-15-Test
                  description: "updated".into(),
                  created: Some(datetime!(2026-04-01 12:00:00)),
                  deleted: None,
                  version: default_version(),
              }
          }
          ```

       b. **`AbsenceDependencies`-Struct + `AbsenceServiceDeps`-Impl + `build_dependencies()`-Helper** (analog `extra_hours.rs:88-156`, **OHNE** `custom_extra_hours_service` und **OHNE** `sales_person_shiftplan_service`):

          ```rust
          struct AbsenceDependencies {
              absence_dao: MockAbsenceDao,
              permission_service: MockPermissionService,
              sales_person_service: MockSalesPersonService,
              clock_service: MockClockService,
              uuid_service: MockUuidService,
              transaction_dao: MockTransactionDao,
          }

          impl AbsenceServiceDeps for AbsenceDependencies {
              type Context = ();
              type Transaction = MockTransaction;
              type AbsenceDao = MockAbsenceDao;
              type PermissionService = MockPermissionService;
              type SalesPersonService = MockSalesPersonService;
              type ClockService = MockClockService;
              type UuidService = MockUuidService;
              type TransactionDao = MockTransactionDao;
          }

          impl AbsenceDependencies {
              fn build_service(self) -> AbsenceServiceImpl<AbsenceDependencies> {
                  AbsenceServiceImpl {
                      absence_dao: self.absence_dao.into(),
                      permission_service: self.permission_service.into(),
                      sales_person_service: self.sales_person_service.into(),
                      clock_service: self.clock_service.into(),
                      uuid_service: self.uuid_service.into(),
                      transaction_dao: self.transaction_dao.into(),
                  }
              }
          }

          fn build_dependencies() -> AbsenceDependencies {
              let absence_dao = MockAbsenceDao::new();
              let mut permission_service = MockPermissionService::new();
              let mut sales_person_service = MockSalesPersonService::new();
              let mut clock_service = MockClockService::new();
              let uuid_service = MockUuidService::new();
              let mut transaction_dao = MockTransactionDao::new();

              permission_service.expect_check_permission().returning(|_, _| Ok(()));
              sales_person_service.expect_verify_user_is_sales_person().returning(|_, _, _| Ok(()));
              clock_service.expect_date_time_now().returning(|| datetime!(2026-04-28 12:00:00));
              transaction_dao.expect_use_transaction().returning(|_| Ok(MockTransaction));
              transaction_dao.expect_commit().returning(|_| Ok(()));

              AbsenceDependencies {
                  absence_dao,
                  permission_service,
                  sales_person_service,
                  clock_service,
                  uuid_service,
                  transaction_dao,
              }
          }
          ```

          (Die exakte Struktur des `gen_service_impl!`-Output für `AbsenceServiceDeps` und die Feld-Namen sind durch Task 2.2 festgelegt. Falls der Macro abweichende Field-Names erzeugt, dem Macro-Output folgen — siehe `service_impl/src/macros.rs`.)

       c. **Tests** — minimum 13 Tests mit folgenden Test-Funktionsnamen (ohne `#`-Präfix in den Bedingungen, alle als `#[tokio::test]`):

          1. `test_create_success` — Mock `find_overlapping().returning(|_, _, _, _, _| Ok(Arc::from([])))`, `uuid_service.expect_new_uuid().returning(...)` für id+version, `absence_dao.expect_create().returning(|_, _, _| Ok(()))`. Service.create returnt mit gesetzter id und version. Assert `result.is_ok()`.
          2. `test_create_inverted_range_returns_date_order_wrong` — Request mit `from_date > to_date` → `test_date_order_wrong(&result)`.
          3. `test_create_self_overlap_same_category_returns_validation` — `find_overlapping().returning(|_, _, _, _, _| Ok(Arc::from([{ entity mit logical_id = other_logical_id() }])))` → `test_validation_error(&result, &ValidationFailureItem::OverlappingPeriod(other_logical_id()), 1)`.
          4. `test_create_self_overlap_different_category_succeeds` — D-12: Mock `find_overlapping` für `Vacation` → empty → `result.is_ok()` obwohl ein SickLeave-Range im selben Zeitraum existieren würde (nicht eingerichtet, weil Service mit Filter category=Vacation lookt).
          5. `test_create_id_set_returns_error` — Request mit `id = some_uuid` → `assert!(matches!(result, Err(ServiceError::IdSetOnCreate)))`.
          6. `test_create_version_set_returns_error` — Request mit `version = some_uuid` → `assert!(matches!(result, Err(ServiceError::VersionSetOnCreate)))`.
          7. `test_create_other_sales_person_without_hr_is_forbidden` — Mocks `permission_service.checkpoint(); permission_service.expect_check_permission().returning(|_, _| Err(ServiceError::Forbidden))` und `sales_person_service.checkpoint(); sales_person_service.expect_verify_user_is_sales_person().returning(|_, _, _| Err(ServiceError::Forbidden))` → `test_forbidden(&result)`.
          8. `test_update_success_soft_deletes_old_inserts_new` — `find_by_logical_id().returning(|_, _| Ok(Some(default_active_entity())))`, `find_overlapping().returning(|_, _, _, _, _| Ok(Arc::from([])))`, `absence_dao.expect_update().with(predicate::function(|e: &AbsencePeriodEntity| e.deleted.is_some()), always(), always()).returning(|_, _, _| Ok(()))` (Tombstone), `absence_dao.expect_create().returning(|_, _, _| Ok(()))` (neue Row), uuid_service liefert alternate ids/versions. Assert OK + version != alt.
          9. `test_update_self_overlap_excludes_self` — Mock `absence_dao.expect_find_overlapping().with(eq(default_sales_person_id()), eq(AbsenceCategoryEntity::Vacation), always(), eq(Some(default_logical_id())), always()).returning(|_, _, _, _, _| Ok(Arc::from([])))`. Assert OK. Test erzwingt D-15 strukturell.
          10. `test_update_unknown_logical_id_returns_not_found` — `find_by_logical_id().returning(|_, _| Ok(None))` → `test_not_found(&result, &unknown_logical_id())`.
          11. `test_update_changing_sales_person_id_is_rejected` — Mock active mit `sales_person_id = default`, request mit `sales_person_id = other` → `test_validation_error(&result, &ValidationFailureItem::ModificationNotAllowed("sales_person_id".into()), 1)`.
          12. `test_update_stale_version_returns_conflict` — Active mit `version = default`, request mit `version = alternate` → `test_conflicts(&result, &default_logical_id(), &alternate_version(), &default_version())`.
          13. `test_update_other_sales_person_without_hr_is_forbidden` — analog test 7 für update; mocks für `find_by_logical_id` → active, dann beide permission-Mocks → Forbidden → `test_forbidden(&result)`.
          14. `test_delete_success_soft_deletes` — `find_by_logical_id` → active, `absence_dao.expect_update().with(predicate::function(|e: &AbsencePeriodEntity| e.deleted.is_some()), always(), always()).returning(|_, _, _| Ok(()))` → assert ok.
          15. `test_delete_other_sales_person_without_hr_is_forbidden` — analog 7 für delete.
          16. `test_find_by_id_returns_active` — `find_by_logical_id` → active → `test_find_by_id` → assert OK + correct id.
          17. `test_find_by_id_unknown_returns_not_found` — `find_by_logical_id` → None → `test_not_found(&result, &unknown_logical_id())`.
          18. `test_find_by_id_other_sales_person_without_hr_is_forbidden` — Mock active belongs to `other_sales_person_id()`, both perms fail → `test_forbidden(&result)`.
          19. `test_find_by_sales_person_self_succeeds` — `find_by_sales_person` → returnt empty array für `default_sales_person_id()` mit self-permission OK. Assert ok.
          20. `test_find_by_sales_person_other_without_permission_is_forbidden` — beide perms fail → `test_forbidden(&result)`.
          21. `test_find_all_hr_succeeds` — HR perm OK → `find_all` returnt empty → ok.
          22. `test_find_all_non_hr_is_forbidden` — HR perm fail → `test_forbidden(&result)` (find_all macht KEIN `or(sp)` — HR-only).

          Mindestens 13 dieser 22 Tests müssen implementiert sein, davon zwingend (Pflichtset für Phase 1):
          - test_create_success, test_create_inverted_range_returns_date_order_wrong, test_create_self_overlap_same_category_returns_validation, test_create_self_overlap_different_category_succeeds, test_create_other_sales_person_without_hr_is_forbidden,
          - test_update_success_soft_deletes_old_inserts_new, test_update_self_overlap_excludes_self, test_update_unknown_logical_id_returns_not_found, test_update_changing_sales_person_id_is_rejected, test_update_stale_version_returns_conflict, test_update_other_sales_person_without_hr_is_forbidden,
          - test_delete_other_sales_person_without_hr_is_forbidden,
          - test_find_by_id_other_sales_person_without_hr_is_forbidden,
          - test_find_by_sales_person_other_without_permission_is_forbidden,
          - test_find_all_non_hr_is_forbidden.

          Das deckt D-11/ABS-05 (`_forbidden` pro public method) plus die Self-Overlap- und logical_id-Update-Pflicht-Behaviors ab.

    2. **Patch `service_impl/src/test/mod.rs`** — neben den bestehenden `#[cfg(test)] pub mod xxx;`-Zeilen (laut PATTERNS Zeile 37–38):
       ```rust
       #[cfg(test)] pub mod absence;
       ```
       (alphabetisch davor in den existierenden Block).

    Anti-Patterns explizit verboten:
    - KEIN Hand-rolled Mock-Struct neben dem automock-generierten (verwende `MockAbsenceDao` etc.).
    - KEINE eigene Assertion-Helper (verwende `error_test::test_*`).
    - KEINE Tests, die `Authentication::Anonymous` oder ähnliches benutzen — Phase 1 verifiziert nur `Authentication::Full` mit gemockten Permission-Services.
    - KEIN `_forbidden`-Test, der nicht beide permission-Mocks abdeckt (HR fail + self fail).
  </action>
  <verify>
    <automated>cargo test -p service_impl test::absence</automated>
  </verify>
  <acceptance_criteria>
    - `test -f service_impl/src/test/absence.rs` exit 0.
    - `grep -c "struct AbsenceDependencies" service_impl/src/test/absence.rs` = 1.
    - `grep -c "fn build_dependencies" service_impl/src/test/absence.rs` ≥ 1.
    - `grep -c "MockAbsenceDao::new" service_impl/src/test/absence.rs` ≥ 1.
    - `grep -E "#\\[tokio::test\\]" service_impl/src/test/absence.rs | wc -l` ≥ 13.
    - Pflicht-Tests vorhanden: `grep -E "fn (test_create_success|test_create_inverted_range_returns_date_order_wrong|test_create_self_overlap_same_category_returns_validation|test_create_self_overlap_different_category_succeeds|test_create_other_sales_person_without_hr_is_forbidden|test_update_success_soft_deletes_old_inserts_new|test_update_self_overlap_excludes_self|test_update_unknown_logical_id_returns_not_found|test_update_changing_sales_person_id_is_rejected|test_update_stale_version_returns_conflict|test_update_other_sales_person_without_hr_is_forbidden|test_delete_other_sales_person_without_hr_is_forbidden|test_find_by_id_other_sales_person_without_hr_is_forbidden|test_find_by_sales_person_other_without_permission_is_forbidden|test_find_all_non_hr_is_forbidden)" service_impl/src/test/absence.rs | wc -l` ≥ 15.
    - Forbidden-Tests pro public method: `grep -E "test_(create|update|delete|find_by_id|find_by_sales_person|find_all).*forbidden" service_impl/src/test/absence.rs | wc -l` ≥ 6 (D-11, ABS-05).
    - `grep -c "test_validation_error.*OverlappingPeriod" service_impl/src/test/absence.rs` ≥ 1.
    - `grep -c "Some(default_logical_id" service_impl/src/test/absence.rs` ≥ 1 (D-15 Strukturtest).
    - `grep -c "pub mod absence" service_impl/src/test/mod.rs` = 1.
    - `cargo test -p service_impl test::absence -- --list 2>&1 | grep -c "test " | xargs -I{} test {} -ge 13` (≥ 13 Tests gelistet).
    - `cargo test -p service_impl test::absence` exit 0 (alle Tests grün).
    - `cargo test -p service_impl test::absence 2>&1 | grep -c "FAILED"` = 0.
    - `cargo test -p service_impl test::absence 2>&1 | grep -E "test result: ok\\."` matched.
  </acceptance_criteria>
  <done>13+ Service-Tests grün; jede public method hat einen `_forbidden`-Test; Self-Overlap und `exclude_logical_id`-Behavior verifiziert.</done>
</task>

<task type="auto">
  <name>Task 2.4: Wave-2-Service-Smoke-Gate (`cargo test -p service_impl` + Workspace-Build)</name>
  <files>(no file modifications — verification gate)</files>
  <read_first>
    - .planning/phases/01-absence-domain-foundation/01-VALIDATION.md (Sampling Rate)
  </read_first>
  <action>
    Run sequentially:
    ```
    cargo build --workspace
    cargo test -p service_impl
    cargo test -p shifty-utils -p dao -p dao_impl_sqlite
    ```
    Expected:
    - Workspace baut.
    - Alle service_impl-Tests inkl. `test::absence::*` grün.
    - Bestehende DAO/utils-Tests grün (keine Regression).
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test -p service_impl</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exit 0.
    - `cargo test -p service_impl 2>&1 | grep -c "FAILED"` = 0.
    - `cargo test -p service_impl test::absence 2>&1 | grep -E "test result: ok\\.\\s+[0-9]+\\s+passed"` matcht (kein FAILED).
    - `git diff service_impl/src/billing_period_report.rs service_impl/src/extra_hours.rs service_impl/src/booking.rs service_impl/src/reporting.rs` ist leer.
  </acceptance_criteria>
  <done>Wave 2 abgeschlossen; Service-Layer fertig; Wave 3 (REST + DI) freigegeben.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated Caller → Service | `Authentication<Context>` wird durch jede Methode gereicht; Permission-Service entscheidet. |
| Service → DAO (innerhalb Transaction) | Service vertraut DAO-Layer-Werten (z.B. `active.sales_person_id` ist authoritativ, nicht der Request-Body). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | Update mit `sales_person_id`-Spoofing | mitigate | `update`-Flow lädt `active`-Row aus DB und vergleicht `request.sales_person_id` gegen `active.sales_person_id`. Mismatch → `ValidationError(ModificationNotAllowed)`. |
| T-01-02-02 | Elevation | Mitarbeiter editiert AbsencePeriod fremder Sales-Person | mitigate | `tokio::join!(check_permission(HR), verify_user_is_sales_person(active.sales_person_id))` mit `or` — beide false → `Forbidden`. `_forbidden`-Tests pro Methode. |
| T-01-02-03 | Tampering | Self-Overlap beim Update kollidiert mit alter Row | mitigate | `find_overlapping(.., Some(logical_id), ..)` exkludiert die alte Row (D-15, Pitfall 2). Strukturtest `test_update_self_overlap_excludes_self`. |
| T-01-02-04 | Tampering | Optimistic-Lock verlieren bei concurrent updates | mitigate | `request.version != active.version` → `EntityConflicts(logical_id, expected, actual)`. |
| T-01-02-05 | Information Disclosure | SickLeave-Daten in Logs | accept | Phase 1 fügt KEIN neues `tracing::info!` mit Body-Inhalten hinzu. Bestehende `tracing::instrument` schickt nur Method-Args ohne Body. v2 kann Logging-Level-Policy nachziehen. |
| T-01-02-06 | DoS | unbounded `find_all`-Lookup | accept | Hidden hinter HR-Permission-Gate; v2 fügt Pagination zu. |
</threat_model>

<verification>
- `cargo build -p service` (Trait-Surface) und `cargo build -p service_impl` (Impl) als Compile-Gates.
- `cargo test -p service_impl test::absence` für alle Service-Unit-Tests.
- Pattern-Greps in acceptance_criteria erzwingen Permission-Pattern, Self-Overlap-Filter, logical_id-Update-Pattern, ValidationFailureItem-Verwendung.
- Additivitäts-Greps verifizieren, dass keine ExtraHours/Booking/Snapshot-Files berührt wurden.
</verification>

<success_criteria>
- `service::absence::AbsenceService` Trait + `AbsencePeriod` + `AbsenceCategory` exportiert.
- `service_impl::absence::AbsenceServiceImpl` mit `gen_service_impl!`-DI-Block existiert.
- 13+ Tests grün; jede public method hat `_forbidden`-Test (ABS-05, D-11).
- Self-Overlap-Detection mit `exclude_logical_id` strukturell verifiziert (D-15).
- `cargo build --workspace` ist grün.
- Kein Diff in additivity-protected Files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-absence-domain-foundation/01-02-SUMMARY.md` mit:
- AbsenceServiceImpl-Deps-Liste (6 Stück, ohne SalesPersonShiftplanService — Option A bestätigt).
- Test-Liste (Funktionsnamen, gepasst).
- Confirmed: D-15 Update-Self-Overlap-Filter strukturell verifiziert.
- Confirmed: D-11 / ABS-05 — `_forbidden` pro public method.
- Confirmed: kein Diff in `service_impl/src/{billing_period_report,reporting,extra_hours,booking}.rs`.
- A2-Pinning bestätigt: Phase 1 = HR ∨ self only; Schichtplan-Kollege deferred to Phase 3.
</output>
