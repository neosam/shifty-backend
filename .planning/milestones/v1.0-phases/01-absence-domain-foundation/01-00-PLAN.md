---
phase: 01-absence-domain-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - migrations/sqlite/<timestamp>_create-absence-period.sql
  - shifty-utils/src/date_range.rs
  - shifty-utils/src/lib.rs
  - service/src/lib.rs
autonomous: true
requirements: [ABS-01, ABS-03]
must_haves:
  truths:
    - "D-01: Tabelle `absence_period` existiert in jeder neu migrierten DB (Tabellenname per CONTEXT.md D-01)."
    - "D-04, D-14: Datenbank lehnt invertierte Ranges (`to_date < from_date`) per CHECK-Constraint ab (Schema-Constraints D-04 + Range-Validierung Defense-in-Depth D-14)."
    - "D-04: Maximal eine aktive Row pro `logical_id` ist möglich (partial unique index `WHERE deleted IS NULL`)."
    - "D-05: Date-Storage als TEXT im ISO-8601-Format mit inclusive bounds beidseitig."
    - "D-16, D-17: `shifty_utils::DateRange` bietet die volle Phase-1-API (`new`, `from`, `to`, `overlaps`, `contains`, `iter_days`, `day_count`); Phase 1 nutzt nur `overlaps`/`contains`, Rest steht für Phase 2 bereit (vermeidet Refactor)."
    - "D-13: `service::ValidationFailureItem::OverlappingPeriod(Uuid)` existiert als neue Variante (A1-Pinning) — nicht `Duplicate`-Reuse."
  artifacts:
    - path: "migrations/sqlite/<timestamp>_create-absence-period.sql"
      provides: "Schema mit CHECK + 3 partial indexes."
      contains: "CREATE TABLE absence_period"
    - path: "shifty-utils/src/date_range.rs"
      provides: "Inclusive DateRange-Wrapper mit overlaps/contains/iter_days/day_count."
      exports: ["DateRange", "RangeError"]
    - path: "service/src/lib.rs"
      provides: "Erweiterung um OverlappingPeriod(Uuid)-Variante."
      contains: "OverlappingPeriod(Uuid)"
  key_links:
    - from: "service::ValidationFailureItem"
      to: "OverlappingPeriod(Uuid)"
      via: "enum-Variante, in service/src/lib.rs angehängt"
      pattern: "OverlappingPeriod"
    - from: "shifty-utils/src/lib.rs"
      to: "shifty-utils/src/date_range.rs"
      via: "pub mod date_range; pub use date_range::*;"
      pattern: "pub use date_range"
---

<objective>
Wave 0 schafft die Grundlage: Schema-Migration für `absence_period`, die `DateRange`-Utility in `shifty-utils`, und die Erweiterung von `ValidationFailureItem` um die `OverlappingPeriod(Uuid)`-Variante (D-13/A1). Nach diesem Plan kompiliert das Workspace weiterhin (additiv), die Migration läuft, und SQLx-Offline-Cache ist auf dem neuen Schema.

Purpose: Alle nachfolgenden Wave-1-Pläne (DAO-Trait, DAO-Impl) hängen davon ab, dass die Tabelle existiert (Pitfall 5: SQLx-`query_as!` bricht Compile, wenn die Tabelle fehlt) und dass `DateRange` als gemeinsamer Type für `find_overlapping` und Service-Range-Validierung importierbar ist.

Output: Migration-File, `date_range.rs` mit Tests, `lib.rs`-Patches, regenerierter `.sqlx/`-Offline-Cache.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-absence-domain-foundation/01-CONTEXT.md
@.planning/phases/01-absence-domain-foundation/01-RESEARCH.md
@.planning/phases/01-absence-domain-foundation/01-PATTERNS.md
@.planning/phases/01-absence-domain-foundation/01-VALIDATION.md
@migrations/sqlite/20260428101456_add-logical-id-to-extra-hours.sql
@shifty-utils/src/lib.rs
@shifty-utils/src/date_utils.rs
@service/src/lib.rs

<interfaces>
<!-- Bestehende Konturen, die die Tasks NICHT brechen dürfen. -->

In `service/src/lib.rs` Zeilen 48–54 (verifiziert):
```rust
#[derive(Debug, PartialEq, Eq)]
pub enum ValidationFailureItem {
    ModificationNotAllowed(Arc<str>),
    InvalidValue(Arc<str>),
    IdDoesNotExist(Arc<str>, Uuid),
    Duplicate,
}
```
Der Patch fügt `OverlappingPeriod(Uuid)` als zusätzliche Variante an — keine bestehende Variante umbenennen oder entfernen.

In `shifty-utils/src/lib.rs` (verifiziert):
```rust
mod date_utils;
pub use date_utils::*;
```
Neuer Patch hängt `pub mod date_range; pub use date_range::*;` an.

In `migrations/sqlite/20260428101456_add-logical-id-to-extra-hours.sql` Zeilen 41–44 (verifiziert):
```sql
CREATE UNIQUE INDEX idx_extra_hours_logical_id_active
    ON extra_hours(logical_id)
    WHERE deleted IS NULL;
```
— exakt dieses Idiom als Vorlage für `idx_absence_period_logical_id_active`.
</interfaces>

<additivity>
- KEINE Datei in `service_impl/src/billing_period_report.rs`, `service_impl/src/reporting.rs`, `service_impl/src/extra_hours.rs`, `service_impl/src/booking.rs`, `dao/src/extra_hours.rs`, `dao_impl_sqlite/src/extra_hours.rs`, `rest/src/extra_hours.rs`, `rest/src/booking.rs` darf editiert werden.
- `CURRENT_SNAPSHOT_SCHEMA_VERSION` darf NICHT verändert werden (CC-07).
</additivity>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0.1: Migration-File `create-absence-period.sql` erzeugen</name>
  <files>migrations/sqlite/&lt;timestamp&gt;_create-absence-period.sql</files>
  <read_first>
    - migrations/sqlite/20260428101456_add-logical-id-to-extra-hours.sql (Partial-Unique-Idiom Zeilen 8–44)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§3.1 SQL-Vollwortlaut, Zeilen 948–999)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer A, Zeilen 47–95)
  </read_first>
  <behavior>
    - Existierende DB nimmt das `CREATE TABLE absence_period`-Statement ohne Fehler an.
    - Insert mit `to_date < from_date` schlägt mit CHECK-Constraint-Fehler fehl.
    - Zwei aktive Rows mit gleicher `logical_id` (beide `deleted IS NULL`) verletzen `idx_absence_period_logical_id_active`.
    - `find_overlapping(sales_person, category, range)` kann den `idx_absence_period_self_overlap`-Index nutzen (`EXPLAIN QUERY PLAN` zeigt SEARCH ... USING INDEX).
  </behavior>
  <action>
    1. **Migration erzeugen** (NixOS, CC-10):
       ```
       nix-shell -p sqlx-cli --run "sqlx migrate add create-absence-period --source migrations/sqlite"
       ```
       Das erzeugt eine leere Datei `migrations/sqlite/<TS>_create-absence-period.sql` mit aktuellem Timestamp.

    2. **Datei ersetzen** mit folgendem SQL **wörtlich** (per D-04, D-05; Pitfall 8: SQLite kann ADD CONSTRAINT nicht nachträglich, deshalb inline):
       ```sql
       -- Phase 1: range-based absence domain (additiv).
       -- Strikt additiv: keine Änderungen an extra_hours, billing_period o.ä.

       CREATE TABLE absence_period (
           id              BLOB(16) NOT NULL PRIMARY KEY,
           logical_id      BLOB(16) NOT NULL,
           sales_person_id BLOB(16) NOT NULL,
           category        TEXT NOT NULL,
           from_date       TEXT NOT NULL,
           to_date         TEXT NOT NULL,
           description     TEXT,
           created         TEXT NOT NULL,
           deleted         TEXT,
           update_timestamp TEXT,
           update_process  TEXT NOT NULL,
           update_version  BLOB(16) NOT NULL,

           CHECK (to_date >= from_date),

           FOREIGN KEY (sales_person_id) REFERENCES sales_person(id)
       );

       CREATE UNIQUE INDEX idx_absence_period_logical_id_active
           ON absence_period(logical_id)
           WHERE deleted IS NULL;

       CREATE INDEX idx_absence_period_sales_person_from
           ON absence_period(sales_person_id, from_date)
           WHERE deleted IS NULL;

       CREATE INDEX idx_absence_period_self_overlap
           ON absence_period(sales_person_id, category, from_date)
           WHERE deleted IS NULL;
       ```

    3. **Migration anwenden** gegen Dev-DB (aus `.env:DATABASE_URL`, CC-10):
       ```
       nix-shell -p sqlx-cli --run "sqlx migrate run --source migrations/sqlite"
       ```

    4. **Offline-Cache regenerieren** (A3 — `.sqlx/` existiert im Repo, also Offline-Build):
       ```
       nix-shell -p sqlx-cli --run "cargo sqlx prepare --workspace -- --tests"
       ```
       Falls `cargo sqlx prepare` ohne Subkommandos läuft (Repo-Konvention prüfen): plain `cargo sqlx prepare --workspace`. Generierte `.sqlx/*.json` belassen — der User commitet manuell per `jj` (CC-11). Nicht selber `git add`/`jj describe` aufrufen.

    Anti-Patterns explizit verboten (Pitfalls 8, 5):
    - KEIN `ALTER TABLE absence_period ADD CONSTRAINT …` — SQLite syntax error.
    - KEIN Migrations-File OHNE Timestamp-Präfix.
    - KEIN Bump von `CURRENT_SNAPSHOT_SCHEMA_VERSION` (CC-07, additivity).
  </action>
  <verify>
    <automated>nix-shell -p sqlx-cli --run "sqlx migrate run --source migrations/sqlite" &amp;&amp; sqlite3 "$(grep -oE 'sqlite:[^ ]+' .env | sed 's|sqlite:||')" ".schema absence_period" | grep -q "CHECK.*to_date.*from_date"</automated>
  </verify>
  <acceptance_criteria>
    - `find migrations/sqlite -name "*_create-absence-period.sql" -type f` liefert genau eine Datei mit Timestamp ≥ `20260501000000`.
    - `grep -c "CREATE TABLE absence_period" migrations/sqlite/*_create-absence-period.sql` = 1.
    - `grep -E "CHECK *\\(to_date *>= *from_date\\)" migrations/sqlite/*_create-absence-period.sql` matcht (defense-in-depth).
    - `grep -c "CREATE UNIQUE INDEX idx_absence_period_logical_id_active" migrations/sqlite/*_create-absence-period.sql` = 1.
    - `grep -c "CREATE INDEX idx_absence_period_sales_person_from" migrations/sqlite/*_create-absence-period.sql` = 1.
    - `grep -c "CREATE INDEX idx_absence_period_self_overlap" migrations/sqlite/*_create-absence-period.sql` = 1.
    - `grep -E "WHERE *deleted *IS *NULL" migrations/sqlite/*_create-absence-period.sql | wc -l` ≥ 3 (jeder partial index).
    - `grep -c "ALTER TABLE" migrations/sqlite/*_create-absence-period.sql` = 0 (Pitfall 8).
    - `nix-shell -p sqlx-cli --run "sqlx migrate run --source migrations/sqlite"` exit 0.
    - Schema-Probe: `sqlite3 <DEV_DB> ".schema absence_period"` enthält die Strings `CHECK`, `to_date >= from_date`, `idx_absence_period_logical_id_active`, `idx_absence_period_sales_person_from`, `idx_absence_period_self_overlap`.
    - Nach `cargo sqlx prepare --workspace`: `ls .sqlx/ | wc -l` &gt; 0 (Cache existiert weiterhin); kein Build-Fehler.
    - `git diff -- service_impl/src/billing_period_report.rs` ist leer (CC-07).
  </acceptance_criteria>
  <done>Migrations-File existiert mit korrektem SQL, Migration wurde gegen Dev-DB ausgeführt, Schema enthält CHECK + 3 partial indexes, SQLx-Offline-Cache regeneriert, kein Diff in Snapshot-Files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.2: `shifty-utils::DateRange` mit Tests</name>
  <files>shifty-utils/src/date_range.rs, shifty-utils/src/lib.rs</files>
  <read_first>
    - shifty-utils/src/date_utils.rs (Style: Error-Type, time-Crate-Imports)
    - shifty-utils/src/lib.rs (Re-Export-Idiom Zeilen 1–3)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§4 + Code-Skizze Zeilen 588–667)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer B, Zeilen 100–132)
  </read_first>
  <behavior>
    - `DateRange::new(date!(2026-05-20), date!(2026-05-15))` liefert `Err(RangeError::FromAfterTo {..})`.
    - `DateRange::new(date!(2026-05-15), date!(2026-05-15))` ist Ok (Single-Day-Range erlaubt).
    - `a.overlaps(&a)` für Single-Day-Range = true.
    - `[2026-05-15, 2026-05-20].overlaps([2026-05-20, 2026-05-25])` = true (Touching boundaries inclusive).
    - `[2026-05-15, 2026-05-17].iter_days().count()` = 3.
    - `[2026-05-15, 2026-05-17].day_count()` = 3.
    - `contains` ist inklusiv an beiden Endpunkten.
  </behavior>
  <action>
    1. **Neue Datei** `shifty-utils/src/date_range.rs` erstellen mit dem **wörtlichen** Inhalt aus RESEARCH.md §4 (Zeilen 588–667). Keine API-Änderung, keine zusätzlichen Methoden.

       Struktur:
       - `pub enum RangeError { FromAfterTo { from: Date, to: Date } }` mit `#[derive(Debug, Error, PartialEq, Eq)]`.
       - `pub struct DateRange { from: Date, to: Date }` mit `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]`. Felder PRIVAT (sonst Invariante umgehbar).
       - `impl DateRange`:
         * `pub fn new(from: Date, to: Date) -> Result<Self, RangeError>` — `Err(FromAfterTo { from, to })` falls `from > to`.
         * `pub fn from(&self) -> Date` (Getter).
         * `pub fn to(&self) -> Date` (Getter).
         * `pub fn overlaps(&self, other: &DateRange) -> bool` — Inclusive Allen: `self.from <= other.to && other.from <= self.to`.
         * `pub fn contains(&self, day: Date) -> bool` — `self.from <= day && day <= self.to`.
         * `pub fn iter_days(&self) -> impl Iterator<Item = Date>` — Inclusive (emittiert `from..=to`). Wörtlich aus RESEARCH.md.
         * `pub fn day_count(&self) -> u32` — `((self.to - self.from).whole_days() + 1).max(0) as u32`.
       - `#[cfg(test)] mod tests` mit MINDESTENS folgenden Tests (Namen wörtlich):
         * `new_rejects_inverted` — `DateRange::new(date!(2026-05-20), date!(2026-05-15)).is_err()`.
         * `new_accepts_single_day` — `DateRange::new(date!(2026-05-15), date!(2026-05-15)).is_ok()`.
         * `overlaps_single_day_with_self` — `a.overlaps(&a)` für Single-Day = true.
         * `overlaps_touching_boundary` — `[15..20].overlaps([20..25])` = true (D-05 inclusive).
         * `overlaps_disjoint_returns_false` — `[15..16].overlaps([18..20])` = false.
         * `contains_endpoints_inclusive` — `r.contains(from)` und `r.contains(to)` = true.
         * `iter_days_emits_inclusive_count` — Range über 3 Tage produziert `count()` = 3 und `day_count()` = 3.
         * `iter_days_year_boundary` — `[2026-12-30, 2027-01-02]` produziert 4 Items in Reihenfolge `[12-30, 12-31, 01-01, 01-02]`.

    2. **Patch `shifty-utils/src/lib.rs`** — bestehender Inhalt:
       ```rust
       mod date_utils;

       pub use date_utils::*;
       ```
       wird (additiv) zu:
       ```rust
       mod date_utils;
       pub mod date_range;

       pub use date_utils::*;
       pub use date_range::{DateRange, RangeError};
       ```

    Anti-Patterns explizit verboten:
    - KEIN `pub from: Date` (Felder müssen privat bleiben — sonst kann `from > to` umgangen werden, Pitfall 1).
    - KEINE Half-Open-Bounds in `overlaps` (`<` statt `<=`) — Pitfall 1: Single-Day-Range bricht.
    - KEIN `iter_days(&self) -> impl Iterator<Item = Date> + '_` mit Lifetime — die RESEARCH-Skizze gibt eine `'static`-fähige Form via `std::iter::from_fn(move || …)`.
  </action>
  <verify>
    <automated>cargo test -p shifty-utils date_range</automated>
  </verify>
  <acceptance_criteria>
    - `test -f shifty-utils/src/date_range.rs` exit 0.
    - `grep -c "pub struct DateRange" shifty-utils/src/date_range.rs` = 1.
    - `grep -c "pub enum RangeError" shifty-utils/src/date_range.rs` = 1.
    - `grep -E "fn (new|from|to|overlaps|contains|iter_days|day_count)" shifty-utils/src/date_range.rs | wc -l` ≥ 7 (alle 7 Methoden vorhanden).
    - `grep -c "self.from <= other.to" shifty-utils/src/date_range.rs` ≥ 1 (Inclusive Allen-Idiom verifiziert).
    - `grep -c "pub mod date_range" shifty-utils/src/lib.rs` = 1.
    - `grep -c "pub use date_range" shifty-utils/src/lib.rs` = 1.
    - `cargo build -p shifty-utils` exit 0.
    - `cargo test -p shifty-utils date_range -- --list 2>&1 | grep -E "new_rejects_inverted|new_accepts_single_day|overlaps_single_day_with_self|overlaps_touching_boundary|overlaps_disjoint_returns_false|contains_endpoints_inclusive|iter_days_emits_inclusive_count|iter_days_year_boundary" | wc -l` ≥ 8.
    - `cargo test -p shifty-utils date_range` exit 0 (alle 8 Tests grün).
  </acceptance_criteria>
  <done>`DateRange` ist als pub-Type in `shifty-utils` exportiert, Felder privat, alle 8 Tests grün, kein Bestand-Test in `shifty-utils` regrediert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 0.3: `ValidationFailureItem::OverlappingPeriod(Uuid)`-Variante hinzufügen</name>
  <files>service/src/lib.rs</files>
  <read_first>
    - service/src/lib.rs (Zeilen 48–54 — bestehender Enum-Body)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§8, Zeilen 1226–1259 — Recommendation + Patch-Skizze)
    - .planning/phases/01-absence-domain-foundation/01-CONTEXT.md (D-13)
  </read_first>
  <behavior>
    - `ValidationFailureItem::OverlappingPeriod(Uuid)` ist konstruierbar.
    - `ServiceError::ValidationError(Arc::from([ValidationFailureItem::OverlappingPeriod(some_uuid)]))` rendert in `Debug` mit der Uuid als Inhalt.
    - Bestehende Matches auf `ValidationFailureItem` brechen NICHT (alle Konsumenten benutzen entweder einzelne Varianten oder fehlen `_`-arms — der Patch ist additiv).
    - Bestehende `Duplicate`-Variante bleibt unverändert.
  </behavior>
  <action>
    1. **Patch `service/src/lib.rs`** an der Enum-Definition (verifiziert Zeilen 48–54):
       Vorher:
       ```rust
       #[derive(Debug, PartialEq, Eq)]
       pub enum ValidationFailureItem {
           ModificationNotAllowed(Arc<str>),
           InvalidValue(Arc<str>),
           IdDoesNotExist(Arc<str>, Uuid),
           Duplicate,
       }
       ```
       Nachher:
       ```rust
       #[derive(Debug, PartialEq, Eq)]
       pub enum ValidationFailureItem {
           ModificationNotAllowed(Arc<str>),
           InvalidValue(Arc<str>),
           IdDoesNotExist(Arc<str>, Uuid),
           Duplicate,
           /// Self-overlap detected during AbsencePeriod create/update.
           /// The wrapped `Uuid` is the `logical_id` of the conflicting AbsencePeriod (D-13).
           OverlappingPeriod(Uuid),
       }
       ```
       — KEINE bestehende Variante umbenennen, umordnen oder entfernen.

    2. **Workspace-Build prüfen.** Falls bestehender Code an einer Stelle `match item { … }` ohne `_`-Catch-All hat und dadurch nun `non-exhaustive patterns`-Warning/Error wirft: Im Rahmen dieser Task wird der `_ => …`-Arm an genau dieser Stelle ergänzt (kein Verhalten-Change, nur Exhaustiveness). Die explizite Suche dafür ist:
       ```
       grep -rn "match.*ValidationFailureItem" --include="*.rs" .
       ```
       Erwartet: alle Match-Sites benutzen ein anderes Pattern (Format-Display oder Vec-Iteration), aber falls eine `match`-Site Compile-Fehler wirft, in dieser Task gefixt — nur additive `_ => unreachable!()` oder analoges No-Op, keine semantische Änderung.

    Anti-Patterns explizit verboten:
    - KEIN Reuse von `Duplicate` mit Kontext-String (D-13/A1 Recommendation: dedizierte Variante; Plan-Phase pinnt Variante).
    - KEINE Umbenennung bestehender Varianten.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "OverlappingPeriod *\\( *Uuid *\\)" service/src/lib.rs | wc -l` = 1.
    - `grep -c "Duplicate," service/src/lib.rs` = 1 (alte Variante unverändert vorhanden).
    - `grep -c "ModificationNotAllowed(Arc<str>)" service/src/lib.rs` = 1.
    - `grep -c "InvalidValue(Arc<str>)" service/src/lib.rs` = 1.
    - `grep -c "IdDoesNotExist(Arc<str>, Uuid)" service/src/lib.rs` = 1.
    - `cargo build --workspace` exit 0.
    - `cargo test --workspace --no-run` exit 0 (alles compiliert weiterhin).
    - `git diff service_impl/src/billing_period_report.rs service_impl/src/reporting.rs service_impl/src/extra_hours.rs service_impl/src/booking.rs` ist leer (Additivität, CC-07 + Pitfall 7).
  </acceptance_criteria>
  <done>Variante existiert; Workspace baut; keine bestehende Pfade berührt.</done>
</task>

<task type="auto">
  <name>Task 0.4: Wave-0-Smoke-Gate (`cargo test --workspace`)</name>
  <files>(no file modifications — verification gate)</files>
  <read_first>
    - .planning/phases/01-absence-domain-foundation/01-VALIDATION.md (Sampling Rate)
  </read_first>
  <action>
    Run:
    ```
    cargo build --workspace
    cargo test --workspace
    ```
    Erwartet: alle bestehenden Tests grün; neu: 8 `date_range`-Tests grün. Keine Regressions.

    Bei Failures: NICHT die Migration zurücknehmen, sondern den Failure-Report im Wave-1-Plan adressieren oder in einer follow-up-task. Wave 1 darf erst starten, wenn dieser Gate grün ist.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exit 0.
    - `cargo test --workspace 2>&1 | grep -E "test result: ok"` matcht (mindestens 1 ok-Zeile, keine `FAILED`).
    - `cargo test --workspace 2>&1 | grep -c "FAILED"` = 0.
    - `cargo test -p shifty-utils date_range 2>&1 | grep -E "8 passed|test result: ok\\. 8 passed"` matcht (alle 8 neuen Tests grün).
  </acceptance_criteria>
  <done>Alles grün; Wave 0 abgeschlossen; freigabebereit für Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → Bestehende DB | DDL-Änderung; jeder neuer Build/Test-Run wendet sie auf In-Memory-DB an. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-00-01 | Tampering | `absence_period.from_date/to_date` | mitigate | DB-CHECK `to_date >= from_date` lehnt Direkt-Inserts mit invertierter Range ab (defense-in-depth zum Service-Check in Plan 02). |
| T-01-00-02 | Tampering | `absence_period.logical_id` | mitigate | Partial UNIQUE-Index `idx_absence_period_logical_id_active` erzwingt max. 1 aktive Row pro logical_id, blockt manuelle Duplikate. |
| T-01-00-03 | Information Disclosure | Snapshot-Schema | accept | CC-07: Phase 1 berührt nicht `billing_period_report.rs`; Versionsbump erfolgt in Phase 2. Risiko bleibt akzeptiert weil keine Reporting-Berührung. |
</threat_model>

<verification>
- Migration läuft im Build automatisch (Integration-Tests in `shifty_bin/src/integration_test.rs:266` rufen `sqlx::migrate!("./../migrations/sqlite").run(...)`).
- DateRange-Tests laufen via `cargo test -p shifty-utils date_range`.
- Workspace-Build via `cargo build --workspace` (Validierung des `ValidationFailureItem`-Patches).
- Additivitäts-Verifikation: `git diff service_impl/src/extra_hours.rs service_impl/src/booking.rs service_impl/src/billing_period_report.rs service_impl/src/reporting.rs` muss leer sein.
</verification>

<success_criteria>
- `migrations/sqlite/<TS>_create-absence-period.sql` existiert mit CHECK + 3 partial indexes.
- `shifty-utils/src/date_range.rs` exportiert `DateRange` und `RangeError` mit 8+ Tests grün.
- `service::ValidationFailureItem::OverlappingPeriod(Uuid)`-Variante ist gelistet.
- `cargo test --workspace` ist grün.
- Kein Diff in Snapshot- oder ExtraHours-Pfaden (Phase-1-Erfolgskriterium 5).
</success_criteria>

<output>
After completion, create `.planning/phases/01-absence-domain-foundation/01-00-SUMMARY.md` mit:
- Migrations-File-Pfad (mit Timestamp).
- DateRange-API-Surface (Liste der pub-Items).
- Patch-Stelle in `service/src/lib.rs` (line range).
- Confirmed: `cargo test --workspace` grün.
- A1-Pinning bestätigt: Variante `OverlappingPeriod(Uuid)` (NICHT `Duplicate`-Reuse).
- A3-Pinning bestätigt: `cargo sqlx prepare --workspace` ausgeführt; `.sqlx/`-Cache regeneriert.
</output>
