---
phase: 35-slot-einzelwoche-aenderung
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - service/src/shiftplan_edit.rs
  - service_impl/src/shiftplan_edit.rs
  - service_impl/src/test/shiftplan_edit.rs
  - rest/src/shiftplan_edit.rs
autonomous: true
requirements: [SWO-02, SWO-03, SWO-04]
must_haves:
  truths:
    - "[D-35-01] modify_slot_single_week erzeugt 3 Slot-Versionen: Segment 1 valid_to=Sonntag KW-1; Segment 2 Mon–Sun der KW mit neuen Werten; Segment 3 ab Montag KW+1 mit Original-Werten und ursprünglichem valid_to (None=unbegrenzt)"
    - "[D-35-03] Buchungen der Ausnahme-KW landen auf Segment 2, Buchungen ab KW+1 auf Segment 3 — jede genau einmal, keine verwaisten Rows"
    - "[D-35-04] Der gesamte Vorgang läuft in EINER Transaktion (ein use_transaction, ein commit); jeder Fehler → kein commit → Rollback"
    - "[D-35-05] Re-Point ist hart gegen Doppelzählung getestet; soft-gelöschte Vorgänger sind via deleted IS NULL unsichtbar"
    - "[D-35-06] modify_slot_single_week verlangt check_permission(shiftplan.edit) vor jeder Mutation; ohne Recht → Forbidden"
    - "PUT /shiftplan-edit/slot/{year}/{week}/single-week ruft modify_slot_single_week"
  artifacts:
    - "service/src/shiftplan_edit.rs — Trait-Methode modify_slot_single_week"
    - "service_impl/src/shiftplan_edit.rs — Implementierung (3-Segment-Split + Booking-Partition)"
    - "service_impl/src/test/shiftplan_edit.rs — D-35-05 Test-Modul"
    - "rest/src/shiftplan_edit.rs — Handler edit_slot_single_week + Route"
  key_links:
    - "REST-Route /single-week → ShiftplanEditService::modify_slot_single_week"
    - "modify_slot_single_week → SlotService.{create,update,delete}_slot + BookingService.{delete,create} innerhalb derselben tx"
---

<objective>
Erweitert `ShiftplanEditService` um eine neue Methode `modify_slot_single_week`, die die
Werte eines Slots als einmalige Ausnahme für **genau eine Kalenderwoche** ändert
(D-35-01, Ansatz B: Split + Re-Merge), inklusive der hart getesteten Booking-Re-Point-Aufteilung
(D-35-03/05), Atomarität in einer Transaktion (D-35-04), `shiftplan.edit`-Gate (D-35-06) und
einer neuen REST-Route, die die Mechanik exponiert.

Zugang: TDD. Erst RED (Trait-Signatur + Stub + vollständiges Test-Modul, das fehlschlägt),
dann GREEN (Implementierung bis alle Tests grün), dann REST-Glue.

Purpose: SWO-02 (Mechanik), SWO-03 (Atomarität), SWO-04 (harte Tests + Gate). Die bestehende
`modify_slot`-Methode bleibt **unverändert** (Option B aus RESEARCH — keine Bool-Flag-Erweiterung,
keine bestehenden Mock-Tests anfassen).
Output: neue Service-Methode + Tests + REST-Endpoint `PUT .../slot/{year}/{week}/single-week`.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-CONTEXT.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-RESEARCH.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-PATTERNS.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Neue Symbole / Endpunkte, die dieser Plan erzeugt (für nachgelagerte Pläne):
- Service-Trait-Methode `ShiftplanEditService::modify_slot_single_week`
- Impl `ShiftplanEditServiceImpl::modify_slot_single_week`
- REST-Handler `edit_slot_single_week` + Route `PUT /shiftplan-edit/slot/{year}/{week}/single-week`
- Auto-generierter Mock `MockShiftplanEditService::expect_modify_slot_single_week` (durch `#[automock]`)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Trait-Methode + Stub + vollständiges D-35-05 Test-Modul</name>
  <files>service/src/shiftplan_edit.rs, service_impl/src/shiftplan_edit.rs, service_impl/src/test/shiftplan_edit.rs</files>
  <read_first>
    - service/src/shiftplan_edit.rs:35-56 (#[automock]-Trait, modify_slot/remove_slot-Signaturen)
    - service_impl/src/shiftplan_edit.rs:51-143 (modify_slot — exaktes Vorbild)
    - service_impl/src/test/shiftplan_edit.rs:75-118 (monday_slot, default_booking, persisted_booking)
    - service_impl/src/test/shiftplan_edit.rs:207-295 (build_dependencies — Default-Mocks: get_slot→monday_slot, use_transaction/commit→Ok, booking.create→Ok)
    - service_impl/src/test/shiftplan_edit.rs:1157-1209 (test_modify_slot_carries_max_paid_employees — exaktes Mock-Muster)
    - service/src/booking.rs:13-24 (Booking: calendar_week ist i32, year ist u32)
  </read_first>
  <behavior>
    Test-Modul (alle als #[tokio::test], Basis build_dependencies(true, true)) — Szenario change_year=2026,
    change_week=26. Bekannte Daten: Montag KW26 = 2026-06-22; Segment-1 valid_to = 2026-06-21;
    Segment-2 valid_to = 2026-06-28; Segment-3 valid_from = 2026-06-29.
    - 3-Segment-Struktur: expect_update_slot prüft valid_to == Some(2026-06-21); expect_create_slot wird
      genau 2× gerufen — Segment 2 (valid_from=2026-06-22, valid_to=Some(2026-06-28), neue min_resources/max_paid_employees),
      Segment 3 (valid_from=2026-06-29, valid_to=None, Original-Werte). create_slot-Mock gibt je nach valid_from
      unterschiedliche ids zurück (SEG2_ID bzw. SEG3_ID).
    - Booking-Partition: get_for_slot_id_since liefert eine Buchung in KW26 und eine in KW27; Buchung KW26 →
      new_booking.slot_id == SEG2_ID, Buchung KW27 → new_booking.slot_id == SEG3_ID; expect_delete times(2),
      expect_create (booking) times(2); jede Buchung genau einmal re-pointet (keine Doppel-/Waisen-Row).
    - Rollback: ein Fehler mitten im Vorgang (z. B. zweiter create_slot oder ein booking.create liefert Err)
      → transaction_dao.expect_commit().times(0); Ergebnis ist Err; keine weiteren Mutationen.
    - Edge erste KW: get_slot liefert Slot mit valid_from == Montag der change_week (2026-06-22) → Segment 1
      valid_to (2026-06-21) < valid_from → expect_delete_slot statt update; Segmente 2 und 3 trotzdem erzeugt.
    - Edge unbegrenzt: stored valid_to == None → Segment 3 valid_to == None.
    - Edge ohne Buchungen: get_for_slot_id_since liefert leeren Vec → keine booking.delete/create-Aufrufe; 3 Segmente.
    - Permission: build_dependencies(false, _) → modify_slot_single_week liefert ServiceError::Forbidden; keine
      Slot-/Booking-Mutation (Muster aus test_forbidden / NoneTypeExt, service_impl/src/test/error_test).
  </behavior>
  <action>
    Füge in `service/src/shiftplan_edit.rs` innerhalb des `#[automock]`-Traits die Methode
    `modify_slot_single_week` mit identischer Signatur zu `modify_slot` hinzu (slot: &Slot, change_year: u32,
    change_week: u8, context: Authentication&lt;Self::Context&gt;, tx: Option&lt;Self::Transaction&gt;) ->
    Result&lt;Slot, ServiceError&gt;. `#[automock]` generiert den Mock automatisch — keine weiteren Mock-Dateien
    anfassen (Option B, RESEARCH).
    Füge in `service_impl/src/shiftplan_edit.rs` einen kompilierenden Stub-Impl ein: binde alle Parameter
    (`let _ = (slot, change_year, change_week);` und `let _ = (context, tx);`) um clippy-`unused_variables` zu
    vermeiden, dann `todo!("GREEN: Task 2")`. KEINE Logik im RED-Stub.
    Ergänze in `service_impl/src/test/shiftplan_edit.rs` ein Test-Modul gemäß `<behavior>`. Verwende
    `build_dependencies(...)`, `().auth()` (NoneTypeExt) und das Mock-Muster aus
    test_modify_slot_carries_max_paid_employees. Beachte den Typ-Hazard: `booking.calendar_week == change_week as i32`.
    Für die distinkten Segment-ids: create_slot-Mock matcht auf `slot.valid_from` und gibt SEG2_ID/SEG3_ID
    zurück; die slot_id-Zuordnung der Buchungen über ein geteiltes `Arc&lt;Mutex&lt;Vec&lt;(i32, Uuid)&gt;&gt;&gt;`
    (calendar_week → slot_id) einsammeln und nach dem Aufruf asserten.
    Tests sind hier ERWARTET ROT (Stub `todo!`), aber müssen kompilieren.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test -p service_impl modify_slot_single_week 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'modify_slot_single_week' service/src/shiftplan_edit.rs` ≥ 1 (Trait-Signatur vorhanden)
    - `SQLX_OFFLINE=true cargo build -p service_impl --tests` kompiliert fehlerfrei (Trait + Stub + Tests)
    - `cargo clippy -p service service_impl --tests -- -D warnings` ist clean (Stub bindet alle Parameter)
    - Die neuen `modify_slot_single_week`-Tests laufen und sind ROT (schlagen am `todo!` fehl) — kein anderer Test bricht
    - Mindestens diese Tests existieren: 3-Segment-Struktur, Booking-KW26→Seg2, Booking-KW27→Seg3, je-genau-einmal,
      Rollback (commit times 0), erste-KW-delete, unbegrenztes valid_to, ohne-Buchungen, forbidden
  </acceptance_criteria>
  <done>Trait-Methode + kompilierender Stub + vollständiges, klar fehlschlagendes Test-Modul (RED) committet.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): modify_slot_single_week implementieren (3-Segment-Split + Booking-Partition)</name>
  <files>service_impl/src/shiftplan_edit.rs</files>
  <read_first>
    - service_impl/src/shiftplan_edit.rs:51-143 (modify_slot — Zeilen 59 use_transaction, 60-62 Gate, 64-75 Version-Check, 77-79 Datums-Arithmetik, 92-102 Segment-1, 104-117 Segment-2, 119-139 Booking-Loop, 141 commit)
    - .planning/phases/35-slot-einzelwoche-aenderung/35-PATTERNS.md:28-167 (zeilengenaue Erweiterung)
    - .planning/phases/35-slot-einzelwoche-aenderung/35-RESEARCH.md:444-484 (Pitfalls 1-7)
  </read_first>
  <action>
    Ersetze den RED-Stub durch die volle Implementierung, modelliert verbatim auf `modify_slot`, mit diesen
    drei gezielten Abweichungen:
    1. Snapshot VOR jeder Mutation: `let original_snapshot = stored_slot.clone();` direkt nach dem Version-Check
       und VOR `stored_slot.valid_to = Some(old_slot_valid_to)` (Pitfall 2 — sonst erbt Segment 3 ein falsches valid_to).
    2. Zwei neue Datumsgrenzen: `seg2_valid_to = new_slot_valid_from + time::Duration::days(6)` (Sonntag KW),
       `seg3_valid_from = new_slot_valid_from + time::Duration::days(7)` (Montag KW+1). `new_slot_valid_from` und
       `old_slot_valid_to` wie in modify_slot.
    3. Segment 1 identisch zu modify_slot (valid_to=old_slot_valid_to; delete_slot falls valid_to &lt; valid_from,
       sonst update_slot — beides gefolgt unbedingt von Segment 2 + 3). Segment 2 = wie modify_slot, ABER
       `seg2.valid_to = Some(seg2_valid_to)` statt `original_valid_to` (Pitfall 1 — sonst läuft Segment 2 unbegrenzt
       und kollidiert mit Segment 3) und mit den Eingabe-Werten (slot.min_resources/max_paid_employees/from/to).
       Segment 3 NEU aus `original_snapshot`: valid_from=seg3_valid_from, valid_to=original_valid_to (None bleibt None),
       id=Uuid::nil(), version=Uuid::nil(), restliche Felder = Original.
    Booking-Loop: pro Buchung soft-delete (booking_service.delete) + create mit Ziel-slot_id =
    `if booking.year == change_year && booking.calendar_week == change_week as i32 { seg2_slot.id } else { seg3_slot.id }`
    (Typ-Cast beachten). Re-Point-Felder wie modify_slot (id/version=nil, created=None, created_by=None).
    EINE Transaktion: `use_transaction` am Anfang, alle Schritte `tx.clone()`, GENAU EIN `commit(tx)` am Ende
    (D-35-04). Rückgabe: `seg2_slot` (Ausnahme-Slot = Edit-Ziel). Gate `check_permission("shiftplan.edit", context)`
    als erster Aufruf nach use_transaction (D-35-06). KEINE neuen sqlx-Queries (nur bestehende Service-Aufrufe).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo test -p service_impl shiftplan_edit 2>&1 | tail -20 && cargo clippy -p service_impl --tests -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Alle `modify_slot_single_week`-Tests aus Task 1 sind GRÜN
    - `cargo clippy --workspace -- -D warnings` ist clean (Hard-Gate)
    - `grep -c 'self.transaction_dao.commit' service_impl/src/shiftplan_edit.rs` zeigt für die neue Methode genau EIN commit (kein Zwischen-commit)
    - `grep -v '^//' service_impl/src/shiftplan_edit.rs | grep -c 'sqlx::query' == 0` (keine neuen SQL-Queries → kein cargo sqlx prepare)
    - Bestehende modify_slot-Tests weiterhin grün (unverändert)
  </acceptance_criteria>
  <done>modify_slot_single_week voll implementiert, alle D-35-05-Tests grün, Workspace-Clippy clean.</done>
</task>

<task type="auto">
  <name>Task 3 (Glue): REST-Handler edit_slot_single_week + Route</name>
  <files>rest/src/shiftplan_edit.rs</files>
  <read_first>
    - rest/src/shiftplan_edit.rs:20-40 (generate_route)
    - rest/src/shiftplan_edit.rs:42-64 (edit_slot — exaktes Vorbild)
    - rest/src/shiftplan_edit.rs:203-229 (ShiftplanEditApiDoc — Hinweis: edit_slot/delete_slot tragen bewusst KEINE #[utoipa::path]-Annotation und sind NICHT in paths())
  </read_first>
  <action>
    Registriere in `generate_route` eine neue Route `.route("/slot/{year}/{week}/single-week",
    put(edit_slot_single_week::&lt;RestState&gt;))` (zusätzlich, bestehende Routen unverändert).
    Lege den Handler `edit_slot_single_week` an, indem du `edit_slot` (Zeilen 42-64) VERBATIM kopierst und nur
    den Service-Aufruf von `.modify_slot(...)` auf `.modify_slot_single_week(...)` änderst (gleiche Argumente:
    &amp;(&amp;slot).into(), year, week, context.into(), None). Signatur/Path-Extraktor/error_handler-Wrapper/Status 200
    identisch zu edit_slot.
    WICHTIG: Spiegle edit_slot exakt — KEINE `#[utoipa::path]`-Annotation hinzufügen und edit_slot_single_week
    NICHT in `ShiftplanEditApiDoc::paths(...)` aufnehmen. Grund: die Phase-1-Slot-Handler sind dort bewusst
    ausgespart (dokumentiert in rest/src/shiftplan_edit.rs:203-209); ein annotierter Handler außerhalb des
    paths()-Bündels bzw. ein nicht-annotierter im Bündel würde inkonsistent sein. Konsistenz mit dem direkten
    Analog edit_slot hat Vorrang.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build -p rest 2>&1 | tail -10 && cargo clippy -p rest -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'single-week' rest/src/shiftplan_edit.rs` ≥ 1 (Route registriert)
    - `grep -c 'fn edit_slot_single_week' rest/src/shiftplan_edit.rs` == 1
    - `grep -c 'modify_slot_single_week' rest/src/shiftplan_edit.rs` == 1
    - `cargo build -p rest` und `cargo clippy -p rest -- -D warnings` clean
    - edit_slot_single_week trägt KEINE #[utoipa::path]-Annotation (Konsistenz mit edit_slot)
  </acceptance_criteria>
  <done>Route + Handler vorhanden, rest-Crate baut clippy-clean, Mechanik via PUT .../single-week erreichbar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → REST | Untrusted Slot-Edit-Payload (SlotTO) + Auth-Context überqueren die Grenze zum neuen single-week-Endpoint |
| Service → DAO/SQLite | Slot-Schnitte + Booking-Re-Points mutieren persistente Daten innerhalb einer Transaktion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Elevation of Privilege | modify_slot_single_week | high | mitigate | `check_permission("shiftplan.edit", context)` als erster Aufruf nach use_transaction, vor jeder Mutation (D-35-06); Test `forbidden` deckt es ab |
| T-35-02 | Tampering | Transaktions-Klammer (torn write) | high | mitigate | EINE tx, GENAU EIN commit am Ende; jeder Fehler → kein commit → Rollback (D-35-04); Rollback-Test asserts commit.times(0) |
| T-35-03 | Tampering | Booking-Re-Point (Doppelzählung/Waisen) | high | mitigate | Partition `calendar_week == change_week as i32` → genau ein Zielsegment pro Buchung; soft-delete + `deleted IS NULL`-Filter im DAO macht Vorgänger unsichtbar (D-35-03/05); Tests je-genau-einmal + KW26→Seg2 / KW27→Seg3 |
| T-35-04 | Tampering | Versionskonflikt (concurrent edit) | medium | mitigate | `stored_slot.version != slot.version → EntityConflicts` (verbatim aus modify_slot übernommen) vor jeder Mutation |
| T-35-SC | Tampering | Package-Installs (cargo) | low | accept | Diese Phase installiert KEINE neuen Packages — nur bestehende Workspace-Deps; kein Supply-Chain-Risiko |
</threat_model>

<verification>
- `SQLX_OFFLINE=true cargo test --workspace` grün (inkl. neuem modify_slot_single_week-Modul)
- `cargo clippy --workspace -- -D warnings` clean (Hard-Gate, CLAUDE.md)
- Kein `cargo sqlx prepare` nötig (Grep == 0 neue sqlx::query) — explizit verifiziert
- Kein Snapshot-Schema-Bump (Slot-Versionierung berührt billing_period_report nicht; CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 12)
- modify_slot (2-Segment) bleibt unverändert; bestehende Mock-Tests kompilieren ohne Änderung (Option B)
</verification>

<success_criteria>
- modify_slot_single_week erzeugt 3 korrekte Slot-Versionen und re-pointet Buchungen je genau einmal (SWO-02)
- Vorgang atomar; Rollback-Test grün (SWO-03)
- Alle D-35-05-Tests grün, Gate shiftplan.edit durchgesetzt (SWO-04)
- PUT /shiftplan-edit/slot/{year}/{week}/single-week exponiert die Mechanik
</success_criteria>

<output>
Create `.planning/phases/35-slot-einzelwoche-aenderung/35-01-SUMMARY.md` when done
</output>
