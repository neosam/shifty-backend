---
phase: 33-special-days-ui-einstellungen
plan: 04
type: execute
wave: 2
depends_on: ["33-01", "33-02"]
files_modified:
  - shifty-dioxus/src/page/shiftplan.rs
autonomous: true
requirements: [SPD-01, SPD-03, SPD-04]

must_haves:
  truths:
    - "D-33-03: each weekday column (Mo-So) in the shiftplan week grid shows a dropdown with Feiertag / Kurzer Tag / Nichts; Feiertag/Kurzer Tag create a special day for that day's (year, calendar_week, day_of_week), Nichts deletes it."
    - "D-33-01 / SPD-04: the per-day dropdown renders only when the existing is_shiftplanner guard (shiftplan.rs:102-105) is true — deckungsgleich to the backend create/delete gate."
    - "D-33-06: choosing 'Kurzer Tag' opens an inline time-of-day prompt in that day column; the special day is only created after a valid time is entered (Holiday needs no time)."
    - "SPD-01/03: after create or delete the week's special-day state and the shift plan are reloaded so the dropdown trigger and slot filtering reflect the change; the trigger shows the current state (Holiday/ShortDay dot or 'Nichts')."
    - "D-33-07: this surface does not pre-check duplicates; a duplicate create surfaces the backend error in an inline error span under that day column."
  artifacts:
    - "shifty-dioxus/src/page/shiftplan.rs — per-weekday special-day DropdownTrigger, a use_resource over api::get_special_days_for_week, a shortday_prompt_day signal + inline time prompt, and an inline error signal"
  key_links:
    - "Per-day dropdown loads via api::get_special_days_for_week(config, year, week) (the page does not currently expose special days; loader::load_shift_plan only consumes them internally) and reloads that resource AND shift_plan_context after each mutation (slot filtering depends on holidays at loader.rs:126-131)."
    - "create_special_day / delete_special_day from Plan 02; (year, week) come from the existing week/year signals; day_of_week comes from the grid column (no date parsing needed)."
    - "DropdownEntry built as (ImStr, closure, disabled) — DropdownBase hides disabled==true entries, so 'Nichts' is encoded disabled when no entry exists for the day (RESEARCH Pitfall 3)."
---

<objective>
Add the interactive per-weekday Special-Day dropdown to the shiftplan week grid (Surface 1):
Feiertag / Kurzer Tag / Nichts per day, with an inline time prompt for ShortDay. Implements
SPD-01/03 on the shiftplan surface and decisions D-33-01/03/06/07.

Purpose: Let a shiftplanner set/clear holidays and short days directly in the week they are
looking at — no calendar-date mapping needed (the grid already knows year, week, weekday).

Output:
- shifty-dioxus/src/page/shiftplan.rs — per-day dropdown + ShortDay inline prompt + week reload.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-special-days-ui-einstellungen/33-CONTEXT.md
@.planning/phases/33-special-days-ui-einstellungen/33-RESEARCH.md
@.planning/phases/33-special-days-ui-einstellungen/33-PATTERNS.md
@.planning/phases/33-special-days-ui-einstellungen/33-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-weekday special-day dropdown (Holiday / Nichts) + week reload</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs (is_shiftplanner lines 102-105; week/year signals 115-116; existing field_dropdown_entries 695-738; DropdownTrigger usage ~838; shift_plan_context.restart() at 300/1067; per-weekday column rendering near 586/788/1183)
    - shifty-dioxus/src/state/dropdown.rs (DropdownEntry: text/action/disabled; From<(ImStr, F, bool)>)
    - shifty-dioxus/src/component/dropdown_base.rs (DropdownTrigger/DropdownBase; line 52 filters disabled==true)
    - shifty-dioxus/src/api.rs (get_special_days_for_week; create_special_day/delete_special_day from Plan 02)
    - rest-types/src/lib.rs (SpecialDayTO, SpecialDayTypeTO::{Holiday,ShortDay}, DayOfWeekTO)
    - 33-RESEARCH.md § "Pattern 8" + § "Pitfall 3"
    - 33-UI-SPEC.md § "Surface 1: Shiftplan Per-Weekday Dropdown"
  </read_first>
  <action>
    Add a use_resource over api::get_special_days_for_week(config, *year.read(), *week.read()) so the
    page can see the week's special days (it currently cannot — loader::load_shift_plan only consumes
    them internally). Add a component-level Signal<Option<Rc<str>>> for an inline error message.
    For each weekday column, only when is_shiftplanner (D-33-01): compute existing_sd = the loaded
    special day whose day_of_week matches the column, existing_id, and has_entry. Render a
    DropdownTrigger whose label/dot reflects state (no entry → 'Nichts' / em dash; Holiday → accent
    dot + ShiftplanDayTypeHoliday; ShortDay → warn dot + ShiftplanDayTypeShortDay) per 33-UI-SPEC.
    Build entries as DropdownEntry triples (ImStr, closure, disabled):
    (ShiftplanDayTypeHoliday, create Holiday with time_of_day=None for that (year, week, day_of_week)
    then reload, false); (ShiftplanDayTypeShortDay, open the inline prompt — wired in Task 2 — false);
    (ShiftplanDayTypeNone, delete existing_id then reload, !has_entry so it is hidden when no entry).
    Each mutating closure: capture cfg + (year, week, day_of_week) by move, spawn the api call, on Ok
    restart BOTH the special-days resource AND shift_plan_context (slot filtering depends on holidays,
    loader.rs:126-131), on Err set the inline error signal (rendered as span.text-small.text-bad under
    that column). Holiday/Kurzer-Tag are always enabled; a duplicate create simply surfaces the backend
    error inline (D-33-07 — no pre-check on this surface).
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5) && cargo clippy --workspace -- -D warnings</automated>
    <human-check>Browser as shiftplanner on the shiftplan week view: each weekday shows the dropdown. Pick Feiertag on a day → a holiday dot appears and that day's slots re-filter; the dropdown now offers Nichts. Pick Nichts → it clears and persists after week nav. Picking Feiertag twice surfaces an inline error. As a non-shiftplanner the dropdown is absent.</human-check>
  </verify>
  <acceptance_criteria>
    - Per-weekday dropdown renders only for shiftplanners; Feiertag creates, Nichts deletes (hidden when no entry).
    - After mutation both the special-days resource and shift_plan_context reload; trigger reflects state.
    - Backend errors (e.g. duplicate) show inline; WASM build green.
  </acceptance_criteria>
  <done>Per-day Holiday/Nichts dropdown working + week reload; WASM green; browser roundtrip confirmed at verify-work.</done>
</task>

<task type="auto">
  <name>Task 2: ShortDay inline time prompt</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs (Task 1 dropdown + day-column rendering; existing inline-form patterns e.g. week-message edit ~617-630)
    - shifty-dioxus/src/component/form/inputs.rs (TextInput type="time" via on_change/oninput)
    - shifty-dioxus/src/component/atoms/btn.rs (Btn Primary / Ghost)
    - 33-UI-SPEC.md § "ShortDay time-of-day interaction" + § "Copywriting Contract" (ShiftplanDayShortDayConfirm, reuse CancelLabel)
    - 33-RESEARCH.md § "Pitfall 4"
  </read_first>
  <action>
    Add a component-level Signal<Option<DayOfWeekTO>> shortday_prompt_day. The 'Kurzer Tag' entry from
    Task 1 sets shortday_prompt_day to that column's day. When shortday_prompt_day == Some(day), render
    in that day column (in place of / beside the trigger) an inline form: TextInput type="time"
    (max-w-[140px], bound via on_change), a Btn Primary labelled ShiftplanDayShortDayConfirm (disabled
    until the time string is non-empty, D-33-06), and a Btn Ghost labelled CancelLabel. On confirm:
    parse the time with format_description!("[hour]:[minute]") into time::Time, spawn
    create_special_day with SpecialDayTypeTO::ShortDay + time_of_day=Some(parsed) for that
    (year, week, day_of_week), then reset shortday_prompt_day to None and reload the special-days
    resource + shift_plan_context (as in Task 1); on Err set the inline error signal. On cancel: set
    shortday_prompt_day to None (no write). Build the SpecialDayTO with id/version nil (api also guards).
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5) && cargo clippy --workspace -- -D warnings</automated>
    <human-check>Browser as shiftplanner: pick 'Kurzer Tag' on a day → an inline time field + Save/Cancel appear; Save is disabled until a time is entered. Enter 13:00 → Save → a ShortDay (warn dot) appears for that day and persists; Cancel makes no change.</human-check>
  </verify>
  <acceptance_criteria>
    - 'Kurzer Tag' opens an inline time prompt scoped to the chosen day; Save disabled until a valid time.
    - Confirm creates a ShortDay with the parsed time and reloads; Cancel writes nothing.
    - WASM build green.
  </acceptance_criteria>
  <done>ShortDay inline prompt complete; create-with-time works; WASM green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user role → shiftplan dropdown | The dropdown must only act for shiftplanners; mutations cross to the gated backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Elevation of Privilege (UX) | per-day dropdown visibility | medium | mitigate | Rendered only under the existing is_shiftplanner guard (D-33-01); backend create/delete enforce the gate of record. |
| T-33-09 | Tampering (double submit) | create/delete spawns | low | mitigate | Each spawn resets the prompt/reloads on completion; the dropdown closes on selection, limiting re-entry. |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` green.
- `cargo clippy --workspace -- -D warnings` from backend root before commit.
- Manual (verify-work): shiftplan dropdown create/delete roundtrip + ShortDay time prompt + shiftplanner gating.
</verification>

<success_criteria>
- A shiftplanner can set Feiertag, set Kurzer Tag (with time), and clear (Nichts) per weekday directly in the grid, with the view reloading to reflect the change and a clean clippy/WASM gate.
</success_criteria>

<output>
Create `.planning/phases/33-special-days-ui-einstellungen/33-04-SUMMARY.md` when done.
</output>
