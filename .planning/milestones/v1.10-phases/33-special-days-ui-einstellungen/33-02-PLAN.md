---
phase: 33-special-days-ui-einstellungen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [SPD-01, SPD-02, SPD-03, SPD-04]

must_haves:
  truths:
    - "D-33-05: get_special_days_for_year(config, year) fetches GET /special-days/for-year/{year} and returns Rc<[SpecialDayTO]>, matching the backend route from Plan 01."
    - "SPD-01: create_special_day(config, body) POSTs to /special-days/ with id and version forced to Uuid::nil() before send, returning the created SpecialDayTO."
    - "SPD-03: delete_special_day(config, id) issues DELETE /special-days/{id} and errors on non-2xx."
    - "SPD-04: all 18 new i18n Key variants from 33-UI-SPEC § 'New i18n Keys' exist in the Key enum and have de + en + cs translations; the compile-time exhaustive Key match (each locale must cover every Key) builds green on wasm32 — that compile is the completeness gate (there is no named `cargo test i18n`)."
  artifacts:
    - "shifty-dioxus/src/api.rs — get_special_days_for_year, create_special_day, delete_special_day"
    - "shifty-dioxus/src/i18n/mod.rs — 18 new Key variants"
    - "shifty-dioxus/src/i18n/de.rs, en.rs, cs.rs — 18 translations each (54 entries)"
  key_links:
    - "create_special_day sets body.id = Uuid::nil() and body.version = Uuid::nil() before POST (else backend returns IdSetOnCreate/VersionSetOnCreate)."
    - "i18n keys are the exact contract consumed by Plan 03 (Settings Card-3) and Plan 04 (Shiftplan dropdown) — names must match 33-UI-SPEC verbatim."
---

<objective>
Lay the shared frontend foundation both Phase-33 surfaces depend on: the three FE API functions
(create / delete / get-for-year) and all new i18n keys (de/en/cs). Isolated here because the i18n
Key enum (mod.rs) is a single shared file — splitting keys across the two surface plans would
force a file conflict.

Purpose: SPD-01/02/03 wiring (api.rs) + SPD-04 localization, ready for Card-3 and the shiftplan
dropdown to consume without touching shared files.

Output:
- New symbols: `api::create_special_day`, `api::delete_special_day`, `api::get_special_days_for_year`.
- 18 new `Key` variants + 54 locale entries (canonical list in 33-UI-SPEC § "New i18n Keys").
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-special-days-ui-einstellungen/33-CONTEXT.md
@.planning/phases/33-special-days-ui-einstellungen/33-RESEARCH.md
@.planning/phases/33-special-days-ui-einstellungen/33-PATTERNS.md
@.planning/phases/33-special-days-ui-einstellungen/33-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three Special-Day FE API functions</name>
  <files>shifty-dioxus/src/api.rs</files>
  <read_first>
    - shifty-dioxus/src/api.rs (get_special_days_for_week lines 974-984; create_absence_period lines 608-631; delete_absence_period lines 659-667; SpecialDayTO already imported line ~12; Uuid + Config + Rc in scope)
    - 33-PATTERNS.md § "shifty-dioxus/src/api.rs — add 3 new functions"
    - 33-RESEARCH.md § "Pattern 4: FE API-Funktionen"
    - rest-types/src/lib.rs (SpecialDayTO fields: id, version (#[serde rename "$version"]), year, calendar_week, day_of_week, day_type, time_of_day, created, deleted)
  </read_first>
  <action>
    Add three async functions to api.rs cloning the cited analogs.
    get_special_days_for_year(config: Config, year: u32) -> Result<Rc<[SpecialDayTO]>, reqwest::Error>:
    GET format!("{}/special-days/for-year/{}", config.backend, year), error_for_status_ref, json
    (clone of get_special_days_for_week). create_special_day(config: Config, mut body: SpecialDayTO)
    -> Result<SpecialDayTO, reqwest::Error>: set body.id = Uuid::nil() and body.version = Uuid::nil()
    FIRST, then POST format!("{}/special-days/", config.backend) with .json(&body), error_for_status_ref,
    return parsed SpecialDayTO. delete_special_day(config: Config, id: Uuid) -> Result<(), reqwest::Error>:
    DELETE format!("{}/special-days/{}", config.backend, id), error_for_status_ref, return Ok(()).
    The nil guard is per SPD-01 / RESEARCH Pitfall 2; the for-year URL must equal Plan 01's route (D-33-05).
    No new imports needed (SpecialDayTO + Uuid already in scope — confirm before adding).
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5) && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - The three functions exist with the exact signatures above and compile to wasm32.
    - create_special_day forces id and version to Uuid::nil() before the POST.
    - get_special_days_for_year targets /special-days/for-year/{year}.
  </acceptance_criteria>
  <done>WASM build green with the three new api functions.</done>
</task>

<task type="auto">
  <name>Task 2: Add 18 i18n keys + de/en/cs translations</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (Key enum; existing SettingsHolidayAutoCredit* entries as insertion anchor; the completeness/exhaustive match that enforces all locales)
    - shifty-dioxus/src/i18n/de.rs, en.rs, cs.rs (existing Settings* entry shape)
    - 33-UI-SPEC.md § "New i18n Keys" (authoritative table: exact key names + de/en/cs strings) and § "Reused existing keys" (do NOT recreate SettingsSaved, SettingsSaveError, CancelLabel, Monday..Sunday)
    - 33-PATTERNS.md § "i18n/mod.rs — add 17 new Keys" + § "i18n/{de,en,cs}.rs"
  </read_first>
  <action>
    Append these 18 variants to the Key enum in mod.rs (exact names from 33-UI-SPEC):
    SettingsSpecialDaysSectionLabel, SettingsSpecialDaysSectionDescription, SettingsSpecialDaysYearLabel,
    SettingsSpecialDaysDateLabel, SettingsSpecialDaysTypeLabel, SettingsSpecialDaysTypeHoliday,
    SettingsSpecialDaysTypeShortDay, SettingsSpecialDaysTimeLabel, SettingsSpecialDaysAddBtn,
    SettingsSpecialDaysEmptyBody, SettingsSpecialDaysDuplicateHint, SettingsSpecialDaysDeleteBtn,
    SettingsSpecialDaysDeleteError, SettingsSpecialDaysCalendarWeekAbbr, ShiftplanDayTypeHoliday,
    ShiftplanDayTypeShortDay, ShiftplanDayTypeNone, ShiftplanDayShortDayConfirm.
    Add the matching arm in each of de.rs / en.rs / cs.rs using the strings from the 33-UI-SPEC
    table verbatim (SPD-04). For SettingsSpecialDaysEmptyBody keep the {year} placeholder consistent
    with how other parameterized Settings strings are rendered in this codebase. Do NOT add keys that
    33-UI-SPEC marks as reused. The exhaustive-match i18n test fails to compile if any locale is
    missing a key — that compile failure is the gate.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -8) && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - All 18 keys present in mod.rs and in all three locale files (54 translation arms).
    - The WASM build compiles green; the compile-time exhaustive Key match guarantees no locale is missing a key (there is no named `cargo test i18n` — completeness is enforced at compile time).
    - No duplication of reused keys (SettingsSaved / SettingsSaveError / CancelLabel / weekday keys).
  </acceptance_criteria>
  <done>i18n test green, WASM build green; 18 keys × 3 locales present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM client → REST | api.rs functions send create/delete/read requests; client-supplied body crosses into backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Tampering | create_special_day body | medium | mitigate | FE forces id/version to Uuid::nil() before POST; backend independently rejects non-nil (gate-of-record). |
| T-33-05 | Elevation of Privilege | api functions callable without shiftplanner | low | accept | api.rs is a thin transport; the shiftplanner gate is enforced server-side (create/delete) — FE gating in Plans 03/04 is UX only. |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` green — this is the i18n completeness gate (compile-time exhaustive Key match; no named `cargo test i18n` exists).
- `cargo clippy --workspace -- -D warnings` from backend root (run before commit; dioxus clippy must NOT run from the dioxus shell — E0514 — per project memory).
</verification>

<success_criteria>
- Three api functions compile to wasm and target the exact backend routes.
- 18 keys localized in de/en/cs; completeness test green.
- Foundation ready for Plans 03 and 04 to consume with no shared-file edits.
</success_criteria>

<output>
Create `.planning/phases/33-special-days-ui-einstellungen/33-02-SUMMARY.md` when done.
</output>
