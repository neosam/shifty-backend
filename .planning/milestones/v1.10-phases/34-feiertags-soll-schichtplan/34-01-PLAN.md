---
phase: 34-feiertags-soll-schichtplan
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - service_impl/src/reporting.rs
  - service_impl/src/test/reporting_holiday_auto_credit.rs
autonomous: true
requirements: [HSP-01, HSP-02, HSP-03, HSP-04]

must_haves:
  truths:
    - "D-34-01: In get_week reduziert ein auto-angerechneter Feiertag NUR das per-Mitarbeiter-expected_hours (40→32) ueber einen separaten holiday_derived_gated-Term; die per-Tag Mo–So-Aggregatzeile (booking_information.rs:517) bleibt unangetastet. (HSP-01)"
    - "D-34-02: holiday_hours (report.holiday_hours → WorkingHoursPerSalesPerson.holiday_hours) wird mit derived+manual gefuellt (0→8); BE-only, keine FE-/i18n-Aenderung. (HSP-02)"
    - "HSP-03 Band-Guard: dynamic_hours bleibt 40 (paid_hours-Band) vor und nach der Aenderung — holiday_derived_gated wird NICHT von dynamic_hours subtrahiert."
    - "D-34-03: test_holiday_auto_credit_no_year_view_impact wird in place umgebaut (Panic-Guard → positive Asserts 32/8/40) und um 2 HSP-04-Subtests ergaenzt (vor-Stichtag wirkungslos; manual-wins keine Doppelzaehlung). (HSP-04)"
    - "HSP-04: Stichtag-Gate + manual-wins werden aus build_derived_holiday_map wiederverwendet (keine Neu-Implementierung); Feiertag vor Stichtag → keine Wirkung; manueller ExtraHours(Holiday) gewinnt, holiday_hours==8 nicht 16."
    - "D-34-04: CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 12 — billing_period_report.rs liest get_report_for_employee_range, nicht get_week; grep-verifiziert."
  artifacts:
    - "service_impl/src/reporting.rs — get_week: neuer holiday_derived_gated-Term + geshadowtes holiday_hours + expected_hours-Subtraktion"
    - "service_impl/src/test/reporting_holiday_auto_credit.rs — umgebautes test_holiday_auto_credit_no_year_view_impact + test_hsp04_before_cutoff + test_hsp04_manual_wins"
  key_links:
    - "get_week → build_derived_holiday_map(Montag..Sonntag der KW, &working_hours, per-Mitarbeiter ExtraHours-Slice, context.clone())"
    - "report.expected_hours → booking_information.rs:322 available_hours; report.holiday_hours → :331 holiday_hours (automatische Propagation, kein Eingriff)"
    - "report.dynamic_hours → booking_information paid_hours-Band (muss konstant bleiben)"
---

<objective>
Verdrahtet die bestehende Phase-25-Feiertags-Automatik (`build_derived_holiday_map`) als **vierten Injektionspunkt** in den `get_week`-Report-Pfad (`service_impl/src/reporting.rs:884`), sodass ein automatisch angerechneter Feiertag das angezeigte Soll (`expected_hours` → `available_hours`) in der Schichtplan-Wochentabelle reduziert und als `holiday_hours` erscheint — konsistent zum uebrigen Stundenkonto. Die Kapazitaetsbaender (`dynamic_hours`/`paid_hours`/...) bleiben strikt unangetastet (D-25-08/HSP-03).

Reine Backend-Phase (D-34-02): keine FE-Aenderung, keine neue UI-Spalte, kein i18n. Kein Snapshot-Bump (D-34-04, im Plan grep-verifiziert).

Purpose: Konsistenz-Fix einer Auslassung — die Feiertags-Anrechnung ist im gesamten uebrigen Report bereits drin, nur in `get_week` fehlt sie versehentlich. Keine neue Mechanik, keine zweite Berechnung.

Output: `holiday_derived_gated`-Term in `get_week`; umgebauter HOL-03-Regressionstest + 2 neue HSP-04-Subtests; grep-verifizierter Snapshot-Status (bleibt 12).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-feiertags-soll-schichtplan/34-CONTEXT.md
@.planning/phases/34-feiertags-soll-schichtplan/34-RESEARCH.md
@.planning/phases/34-feiertags-soll-schichtplan/34-PATTERNS.md

# Implementierungspfad (read-first in den Tasks praezisiert):
@service_impl/src/reporting.rs
@service_impl/src/test/reporting_holiday_auto_credit.rs
</context>

<vcs_jj_only>
Dieses Repo wird mit **jj (Jujutsu, co-located mit git)** verwaltet. NIE `git commit`/`git add`/`git`-Fallbacks ausfuehren. Commits macht der User manuell mit jj. Falls ein Commit-Schritt verlangt wird: nur ueber die `jj-commit`-Skill bzw. `jj`-native Befehle, niemals git.
</vcs_jj_only>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): HOL-03 in place umbauen + 2 HSP-04-Subtests schreiben (test-first)</name>
  <files>service_impl/src/test/reporting_holiday_auto_credit.rs</files>
  <read_first>
    - service_impl/src/test/reporting_holiday_auto_credit.rs:540–620 (aktuelles `test_holiday_auto_credit_no_year_view_impact` mit Panic-Guard-Semantik — special_day_service OHNE Expectations).
    - service_impl/src/test/reporting_holiday_auto_credit.rs:43–149 (`ReportingMocks`-Struct + `build()`; Helper `make_holiday` und `make_holiday_extra_hours`; toggle_service-Default = Ok(None)).
    - service_impl/src/test/reporting_holiday_auto_credit.rs:226–305 (HOL-01/HOL-02 als Fixture-/Assert-Vorbild: active-cutoff-Toggle-Mock + special_day-Mock KW23).
    - 34-PATTERNS.md § "service_impl/src/test/reporting_holiday_auto_credit.rs" (vollstaendige Mock-/Assert-Skeletons fuer HOL-03-Rebuild + HSP-04a + HSP-04b).
    - 34-RESEARCH.md § "HOL-03 Test Rebuild Plan (D-34-03)".
  </read_first>
  <behavior>
    Rebuild `test_holiday_auto_credit_no_year_view_impact` (in place, ersetzt die bisherigen Panic-Guard-Asserts):
    - special_day_service-Mock: 1 Holiday auf KW23 Montag (2024) via `make_holiday(2024, 23, DayOfWeek::Monday)`; andere Wochen leer.
    - toggle_service-Mock ersetzen durch aktiven Stichtag VOR dem Feiertag: `Ok(Some(Arc::from("2024-01-01")))`.
    - Assert HSP-03: `report.dynamic_hours == 40.0` (Band invariant) — Failmeldung enthaelt woertlich `HSP-03: dynamic_hours must remain 40h`.
    - Assert HSP-01: `report.expected_hours == 32.0` (40 − 8) — Failmeldung enthaelt woertlich `HSP-01: expected_hours must be 32h`.
    - Assert HSP-02: `report.holiday_hours == 8.0` — Failmeldung enthaelt woertlich `HSP-02: holiday_hours must be 8h`.
    Neuer Test `test_hsp04_before_cutoff` (gleiche Fixtures): toggle-cutoff `"2024-12-31"` (NACH dem Feiertag 2024-06-03/KW23) → `expected_hours == 40.0`, `holiday_hours == 0.0`, `dynamic_hours == 40.0`.
    Neuer Test `test_hsp04_manual_wins`: aktiver Stichtag + special_day KW23 Montag UND manueller `make_holiday_extra_hours(8.0, date!(2024-06-03))` via `find_by_week` → `holiday_hours == 8.0` (genau einmal, NICHT 16.0).
  </behavior>
  <action>
    Baue `test_holiday_auto_credit_no_year_view_impact` (aktuell reporting_holiday_auto_credit.rs:542–620) gemaess `<behavior>` und den Skeletons in 34-PATTERNS.md um. Ersetze die bisherigen Negativ-/Panic-Guard-Asserts (dynamic==40 als "no auto-credit", holiday==0) durch die positiven HSP-01/02/03-Asserts. Setze die zuvor absichtlich fehlenden Mock-Expectations: `mocks.special_day_service.expect_get_by_week()` (KW23 → ein Holiday-Montag, sonst leer) und ersetze `mocks.toggle_service` durch ein frisches `MockToggleService::new()` mit `expect_get_toggle_value().returning(|_,_,_| Ok(Some(Arc::from("2024-01-01"))))`. Verwende die existierenden Helper `make_holiday`/`make_holiday_extra_hours` und die 6 get_week-Mock-Expectations aus dem bestehenden Test unveraendert (employee_work_details/shiftplan_report/extra_hours/sales_person/transaction_dao/absence_service).

    Fuege im selben Modul zwei neue `#[tokio::test]`-Funktionen hinzu: `test_hsp04_before_cutoff` und `test_hsp04_manual_wins`, jeweils mit dem gleichen Mock-Grundgeruest. Fuer `test_hsp04_manual_wins` ersetze die `extra_hours_service`-Expectation so, dass `find_by_week` `Arc::from(vec![make_holiday_extra_hours(8.0, date!(2024-06-03))])` liefert (verschiebe die Arc per `move`-Closure-Clone). Halte die Failmeldungen so, dass sie die in `<behavior>` genannten woertlichen Token enthalten (sie dienen als RED-Nachweis).

    Mockall-Hinweis: Expectations ohne `.times(...)` gelten als 0..n — die neu gesetzten special_day/toggle-Expectations panicken NICHT, solange `get_week` sie (Task 2) noch nicht aufruft; der Test laeuft durch und scheitert allein an der `expected_hours == 32`-Assertion. Genau das ist der gewuenschte RED-Zustand.

    Keine Produktionscode-Aenderung in diesem Task (get_week bleibt unveraendert). Achte auf clippy-Sauberkeit: keine ungenutzten Imports/Variablen (sonst `_`-Praefix).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop -c bash -c '! cargo test -p service_impl test_holiday_auto_credit_no_year_view_impact 2>&1 | grep -q "test result: ok" && cargo test -p service_impl test_hsp04 && cargo clippy --workspace -- -D warnings'</automated>
  </verify>
  <acceptance_criteria>
    - RED nachgewiesen: `test_holiday_auto_credit_no_year_view_impact` schlaegt fehl (Assertion `expected_hours == 32`, ist-Wert 40), weil get_week den derived-Holiday noch nicht anrechnet.
    - Die 2 neuen Tests `test_hsp04_before_cutoff` und `test_hsp04_manual_wins` kompilieren und laufen gruen (sie wirken als Regressions-Guards; manual-wins/vor-Stichtag liefern schon vor dem Fix das korrekte Ergebnis).
    - `cargo clippy --workspace -- -D warnings` ist sauber (Code kompiliert; Assertion-Fail ist Laufzeit, kein clippy-Problem).
    - Keine Aenderung an `service_impl/src/reporting.rs` in diesem Task.
  </acceptance_criteria>
  <done>HOL-03-Rebuild + 2 HSP-04-Subtests geschrieben; HOL-03 schlaegt fehl (RED, expected 32 ist 40), beide HSP-04-Tests gruen, clippy sauber, reporting.rs unveraendert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): 4. Injektionspunkt in get_week — holiday_derived_gated reduziert nur expected_hours</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:884–1112 (`get_week` komplett; per-Mitarbeiter `for (sales_person_id, working_hours)`-Loop).
    - service_impl/src/reporting.rs:955–962 (aktuelle `holiday_hours`-Summe — NUR manueller ExtraHours(Holiday)).
    - service_impl/src/reporting.rs:1021–1029 (`has_contract_row`, `planned_hours`, `dynamic_hours`).
    - service_impl/src/reporting.rs:1066–1072 (`abense_hours_for_balance`, `absence_derived_balance_total`, `expected_hours` — Guard-/Formel-Vorbild).
    - service_impl/src/reporting.rs:1081 (`dynamic_hours`-Formel — MUSS unveraendert bleiben, HSP-03).
    - service_impl/src/reporting.rs:151–242 (`build_derived_holiday_map`-Signatur: Param-Typ `ShiftyDate`, nicht time::Date; Stichtag-Gate + manual-wins + ISO-Wochen built-in).
    - service_impl/src/reporting.rs:1035–1044 (absence_service-Aufruf mit `.to_date()` — Kontrast: holiday-Map braucht KEIN `.to_date()`).
    - 34-RESEARCH.md § "Implementation Pattern — 4th Injection Point" und § "Common Pitfalls" (Pitfall 1/2/3/5).
    - 34-PATTERNS.md § "service_impl/src/reporting.rs — 4th injection point in get_week".
  </read_first>
  <behavior>
    Nach dem Fix erfuellt `get_week` (verifiziert durch die Tests aus Task 1):
    - HSP-01: 40h-Mon-Fri-Vertrag + 1 aktiver Feiertag → `expected_hours == 32.0`.
    - HSP-02: `holiday_hours == 8.0` (derived additiv zu manuellem Holiday).
    - HSP-03: `dynamic_hours == 40.0` UNVERAENDERT.
    - HSP-04: Feiertag vor Stichtag → keine Wirkung; manueller ExtraHours(Holiday) gewinnt → keine Doppelzaehlung (holiday_hours==8, nicht 16).
  </behavior>
  <action>
    Fuege im per-Mitarbeiter-Loop von `get_week` zwischen der `absence_derived_balance_total`-Bindung (aktuell Zeile 1067–1071) und der `expected_hours`-Bindung (aktuell Zeile 1072) den vierten Injektionspunkt ein — analog zu den bestehenden Aufrufstellen 1a/1b (reporting.rs:361/754):

    1. Per-Mitarbeiter ExtraHours-Slice typsicher binden: `let employee_extra_hours_slice: &[ExtraHours] = employee_extra_hours.map(|v| v.as_ref()).unwrap_or(&[]);`. Die explizite `&[ExtraHours]`-Annotation disambiguiert `as_ref` und stellt sicher, dass die manual-wins-Pruefung in build_derived_holiday_map die echten ExtraHours sieht (Pitfall 5 / D-25-03). NIE `&[]` direkt uebergeben.

    2. Die derived-Holiday-Map fuer genau diese eine Woche bauen: `self.build_derived_holiday_map(ShiftyWeek::new(year, week).as_date(DayOfWeek::Monday), ShiftyWeek::new(year, week).as_date(DayOfWeek::Sunday), &working_hours, employee_extra_hours_slice, context.clone()).await?`. WICHTIG: Die from_date/to_date-Parameter sind vom Typ `ShiftyDate` — KEIN `.to_date()` anhaengen (anders als der absence_service-Aufruf bei Zeile 1038, der time::Date braucht). `&working_hours` ist die im Loop bereits vorhandene per-Mitarbeiter-Bindung (gleiche Form wie `find_working_hours_for_calendar_week(&working_hours, year, week)` bei Zeile 1022). Kein `tx`-Param (build_derived_holiday_map managt seine eigenen, tx-freien Service-Calls).

    3. Map-Werte summieren: `let derived_holiday_for_week: f32 = derived_holiday_map.values().sum();`.

    4. Dynamic-Week-Guard identisch zu `absence_derived_balance_total` (Zeile 1067) anwenden: `let holiday_derived_gated = if !has_contract_row || planned_hours <= 0.0 { 0.0f32 } else { derived_holiday_for_week };` (D-34-01 (d) — kein negatives expected/aufgeblaehte balance bei dynamischen Wochen, Pitfall 2).

    5. `holiday_hours` additiv shadowen: `let holiday_hours = holiday_hours + holiday_derived_gated;` (HSP-02; manual-wins ist bereits in build_derived_holiday_map behandelt, daher keine Doppelzaehlung).

    Aendere die `expected_hours`-Bindung (Zeile 1072) so, dass der neue Term subtrahiert wird: `let expected_hours = planned_hours - abense_hours_for_balance - absence_derived_balance_total - holiday_derived_gated;` (HSP-01).

    KRITISCH (HSP-03 Band-Guard, Pitfall 1): Die `dynamic_hours`-Bindung (Zeile 1081) NICHT anfassen — sie bleibt `dynamic_hours - abense_hours_for_balance - absence_derived_balance_total`. `holiday_derived_gated` darf dort NICHT subtrahiert werden (sonst sinkt das paid_hours-Band in booking_information). Begruendung fuer den separaten Term statt Einbau in `abense_hours_for_balance`: letzteres reduziert BEIDE (expected + dynamic). Ausserdem: die per-Tag Mo–So-Aggregatzeile in booking_information.rs:517 NICHT anfassen (D-34-01) und booking_information.rs gar nicht aendern (Propagation `available_hours = report.expected_hours` / `holiday_hours = report.holiday_hours` ist automatisch).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop -c bash -c 'cargo test -p service_impl reporting_holiday && cargo clippy --workspace -- -D warnings'</automated>
  </verify>
  <acceptance_criteria>
    - GREEN: `test_holiday_auto_credit_no_year_view_impact`, `test_hsp04_before_cutoff`, `test_hsp04_manual_wins` laufen alle gruen.
    - `report.expected_hours == 32.0`, `report.holiday_hours == 8.0`, `report.dynamic_hours == 40.0` im aktiven-Feiertag-Fall.
    - `cargo clippy --workspace -- -D warnings` sauber.
    - `service_impl/src/booking_information.rs` und die per-Tag-Aggregatzeile bleiben unveraendert; `dynamic_hours`-Formel (reporting.rs:1081) unveraendert.
  </acceptance_criteria>
  <done>4. Injektionspunkt in get_week eingebaut; alle 3 Tests gruen (expected 32 / holiday 8 / dynamic 40 + HSP-04-Subtests), clippy sauber, dynamic_hours-Formel + booking_information.rs unangetastet.</done>
</task>

<task type="auto">
  <name>Task 3: Snapshot-Schema-Version verifizieren — bleibt 12 (D-34-04)</name>
  <files>service_impl/src/billing_period_report.rs</files>
  <read_first>
    - service_impl/src/billing_period_report.rs:117 (`pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12;`).
    - service_impl/src/billing_period_report.rs:140–185 (ReportingService-Aufrufe — alle `get_report_for_employee_range`, keiner liest aus dem in Phase 34 geaenderten get_week-Pfad).
    - 34-RESEARCH.md § "Snapshot Verification (D-34-04)" (Grep-Evidenz) und shifty-backend/CLAUDE.md § "Billing Period Snapshot Schema Versioning".
  </read_first>
  <action>
    Verifiziere (kein Code-Write), dass Phase 34 keinen persistierten `BillingPeriodValueType` in seiner Computation aendert und daher KEIN Snapshot-Bump noetig ist:
    1. Bestaetige per grep, dass der Snapshot-Writer ausschliesslich `get_report_for_employee_range` als ReportingService-Quelle nutzt (>= 1 Treffer, erwartet 4).
    2. Bestaetige durch Lesen, dass die einzige Erwaehnung des in Phase 34 geaenderten get_week-/booking_information-Pfads in billing_period_report.rs ein Kommentar ist (kein Aufruf-/Call-Site). <!-- planner-discipline-allow: get_week --> <!-- planner-discipline-allow: booking_information -->
    3. Bestaetige, dass `CURRENT_SNAPSHOT_SCHEMA_VERSION` unveraendert auf 12 steht.
    Erwartetes Ergebnis: kein Bump (bleibt 12). Falls Schritt 2 unerwartet ein echtes Call-Site findet, das aus dem in Phase 34 geaenderten Pfad liest → Bump 12→13 in billing_period_report.rs:117 vornehmen und die Begruendung im SUMMARY dokumentieren (Abweichung von D-34-04 explizit melden).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && test "$(grep -c 'get_report_for_employee_range' service_impl/src/billing_period_report.rs)" -ge 1 && grep -q 'CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12' service_impl/src/billing_period_report.rs && echo SNAPSHOT_OK_STAYS_12</automated>
  </verify>
  <acceptance_criteria>
    - `get_report_for_employee_range` ist nachweislich die einzige ReportingService-Quelle im Snapshot-Writer (kein Lesen aus dem in Phase 34 geaenderten Report-Pfad).
    - `CURRENT_SNAPSHOT_SCHEMA_VERSION` steht weiterhin auf 12 (kein Bump, D-34-04).
    - Falls unerwartet ein echtes Call-Site gefunden wird: Bump 12→13 + dokumentierte Begruendung im SUMMARY (sonst NICHT bumpen).
  </acceptance_criteria>
  <done>Snapshot-Status verifiziert: get_report_for_employee_range einzige Quelle, CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 12 (kein Bump, D-34-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (keine neue) | Reine interne Report-Berechnung in `get_week`. Keine neuen externen Eingaben, kein neuer Endpoint, kein neuer DAO-Call. Auth-Gate bleibt unveraendert (via `employee_work_details_service.all_for_week`). Keine neuen Dependency-Installs (npm/pip/cargo) in dieser Phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering (data integrity) | `get_week` holiday_derived_gated-Term | medium | mitigate | Separater Term nur fuer expected_hours (HSP-03 Band-Guard) + dynamic-week-Guard (`planned_hours<=0→0`) + manual-wins via build_derived_holiday_map; durch HOL-03-Rebuild + 2 HSP-04-Tests abgesichert. |
| T-34-02 | Information Disclosure | `holiday_hours`/`expected_hours` Propagation | low | accept | Nur bereits HR/shiftplanner-sichtbare Reportfelder; keine neue Exposition, booking_information unveraendert. |
| T-34-SC | Tampering | npm/pip/cargo installs | low | accept | Keine neuen Pakete in Phase 34 (kein Install-Task, reine Workspace-interne Code-Aenderung) → kein Legitimacy-Gate noetig. |
</threat_model>

<verification>
## Phase-Gesamtpruefung (vor /gsd-verify-work)

```
cd /home/neosam/programming/rust/projects/shifty/shifty-backend
nix develop -c bash -c 'cargo test --workspace && cargo clippy --workspace -- -D warnings'
```

- Alle 3 Tests gruen: `test_holiday_auto_credit_no_year_view_impact` (32/8/40), `test_hsp04_before_cutoff` (40/0/40), `test_hsp04_manual_wins` (8, nicht 16).
- `cargo clippy --workspace -- -D warnings` ohne Warnung (Pflicht-Gate, `cargo test` allein reicht NICHT — CLAUDE.md).
- Snapshot-Verifikation: `CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt 12.
- Manuelle Diff-Kontrolle: `service_impl/src/booking_information.rs` und reporting.rs:1081 (`dynamic_hours`) unveraendert.
</verification>

<success_criteria>
- HSP-01: Auto-Feiertag reduziert `expected_hours`/`available_hours` pro Mitarbeiter in `get_week` (40→32), konsistent zum Stundenkonto.
- HSP-02: `holiday_hours` (derived + manual, additiv) ist in der get_week-Ausgabe gefuellt (0→8) und propagiert automatisch nach `WorkingHoursPerSalesPerson.holiday_hours`.
- HSP-03: Kapazitaetsbaender (`dynamic_hours`/`paid_hours`/committed_voluntary/volunteer) in derselben Woche identisch vor/nach der Aenderung (`dynamic_hours == 40`).
- HSP-04: Stichtag-Gate + manual-wins wirken in get_week identisch zum Stundenkonto (Wiederverwendung von build_derived_holiday_map; keine Doppelzaehlung).
- D-34-04: Kein Snapshot-Bump (bleibt 12), grep-verifiziert.
- Gates gruen: `cargo test --workspace` + `cargo clippy --workspace -- -D warnings`.
</success_criteria>

<output>
Create `.planning/phases/34-feiertags-soll-schichtplan/34-01-SUMMARY.md` when done
</output>