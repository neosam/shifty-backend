---
phase: 05-slot-paid-capacity-warning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql
  - dao/src/slot.rs
  - dao_impl_sqlite/src/slot.rs
autonomous: true
requirements: [D-01, D-02, D-15]
must_haves:
  truths:
    - "DB schema has slot.max_paid_employees INTEGER NULL with no DEFAULT"
    - "SlotEntity carries max_paid_employees: Option<u8>"
    - "DAO read/insert/update round-trip preserves Some(u8) and None"
  artifacts:
    - path: "migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql"
      provides: "ALTER TABLE slot ADD COLUMN max_paid_employees INTEGER"
      contains: "max_paid_employees"
    - path: "dao/src/slot.rs"
      provides: "SlotEntity with max_paid_employees: Option<u8>"
      contains: "max_paid_employees"
    - path: "dao_impl_sqlite/src/slot.rs"
      provides: "All 4 read sites + create_slot INSERT + update_slot UPDATE handle max_paid_employees"
      contains: "max_paid_employees"
  key_links:
    - from: "dao_impl_sqlite/src/slot.rs (read sites)"
      to: "SQLite slot table"
      via: "SELECT id, day_of_week, time_from, time_to, min_resources, max_paid_employees, ..."
      pattern: "max_paid_employees"
    - from: "dao_impl_sqlite/src/slot.rs (create_slot)"
      to: "SQLite slot table"
      via: "INSERT INTO slot (..., max_paid_employees, ...)"
      pattern: "INSERT INTO slot"
    - from: "dao_impl_sqlite/src/slot.rs (update_slot)"
      to: "SQLite slot table"
      via: "UPDATE slot SET ..., max_paid_employees = ?, ..."
      pattern: "UPDATE slot SET"
---

<objective>
Foundation layer for Phase 5 — extend the SQLite schema, the `SlotEntity` model, and the SQLite-`SlotDao` impl so the new optional `max_paid_employees: Option<u8>` field is persisted and read end-to-end at the DAO tier.

Purpose: Every higher tier (slot service, shiftplan view, REST DTO, warning emission) needs the field to exist on `SlotEntity` and round-trip cleanly through SQLite before it can be wired further. This is the strict prerequisite for waves 2 and 3.

Output: New migration file applied locally; `SlotEntity` carrying the field; `dao_impl_sqlite::slot` reading/writing the column at all read/write sites.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-slot-paid-capacity-warning/05-CONTEXT.md
@.planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md
@shifty-backend/CLAUDE.md
@migrations/sqlite/20240813080347_add-column-min-resources.sql
@dao/src/slot.rs
@dao_impl_sqlite/src/slot.rs

<interfaces>
<!-- Existing DAO surface that the new field flows through unchanged. Executor uses these directly — no codebase exploration needed. -->

From `dao/src/slot.rs` (current `SlotEntity` shape — lines 10-22 per Pattern Map):
```rust
pub struct SlotEntity {
    pub id: Uuid,
    pub day_of_week: DayOfWeek,
    pub from: time::Time,
    pub to: time::Time,
    pub min_resources: u8,
    // INSERT max_paid_employees: Option<u8> HERE (Phase 5)
    pub valid_from: time::Date,
    pub valid_to: Option<time::Date>,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
    pub shiftplan_id: Option<Uuid>,
}
```

From `dao_impl_sqlite/src/slot.rs` (4 read sites at approx lines 36-56, 73-94, 122-144, 171-194; create_slot at lines 197-231; update_slot at lines 233-254 per Pattern Map). Each read site builds `SlotEntity { ... min_resources: row.min_resources as u8, ... }`.

`SlotDao` trait (no method-signature change needed — `create_slot`, `update_slot`, all `get_*` already take/return `SlotEntity`, so the new field flows through automatically once added to the struct).

Migration analog (`migrations/sqlite/20240813080347_add-column-min-resources.sql`):
```sql
ALTER TABLE slot
ADD COLUMN min_resources INTEGER DEFAULT 2 NOT NULL
```
**Phase 5 strips `DEFAULT` and `NOT NULL`** — see PATTERN-Map "Pitfalls Summary" #1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite migration for nullable max_paid_employees column</name>
  <read_first>
    - migrations/sqlite/20240813080347_add-column-min-resources.sql (analog — copy structure, strip DEFAULT and NOT NULL)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md § "migrations/sqlite/...add-max-paid-employees-to-slot.sql"
    - shifty-backend/CLAUDE.md § "Database Migrations"
  </read_first>
  <files>migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql</files>
  <action>
    Create the new migration file at exactly the path `migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql` with the following content (and ONLY this content — no additional statements):

    ```sql
    ALTER TABLE slot
    ADD COLUMN max_paid_employees INTEGER
    ```

    Per D-01 and D-15: the column is nullable (no `NOT NULL`), no `DEFAULT`. Existing rows get implicit `NULL` (= "no limit"). Do NOT add backfill, do NOT touch `min_resources`, do NOT add an index.

    After writing the file, apply the migration locally via `nix develop` (per CLAUDE.local.md and MEMORY.md `reference_local_dev_commands.md`):
    ```
    cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop --command sqlx migrate run --source migrations/sqlite
    ```
    (Use `migrate run` — additive, non-destructive. NEVER use `sqlx database reset` or `sqlx setup` without explicit user confirmation per MEMORY.md `feedback_destructive_db_ops.md`.)

    SQLx `query!`/`query_as!` macros read the live DB schema at compile time, so subsequent tasks in this plan will fail to compile until the migration is applied.
  </action>
  <verify>
    <automated>test -f /home/neosam/programming/rust/projects/shifty/shifty-backend/migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql &amp;&amp; grep -q "ADD COLUMN max_paid_employees INTEGER" /home/neosam/programming/rust/projects/shifty/shifty-backend/migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql &amp;&amp; ! grep -q "NOT NULL\|DEFAULT" /home/neosam/programming/rust/projects/shifty/shifty-backend/migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `migrations/sqlite/20260503221640_add-max-paid-employees-to-slot.sql` exists.
    - File contains the literal string `ADD COLUMN max_paid_employees INTEGER`.
    - File contains NO occurrence of `NOT NULL` or `DEFAULT`.
    - Local DB has the column applied: `nix develop --command sqlite3 $DATABASE_URL ".schema slot" | grep -q "max_paid_employees INTEGER"` returns exit 0 (executor must run the migration before the next task).
  </acceptance_criteria>
  <done>
    Migration file created with correct SQL, applied locally so SQLx compile-time checks see the new column.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add max_paid_employees field to SlotEntity (DAO trait)</name>
  <read_first>
    - dao/src/slot.rs (full file — currently ~62 lines, includes SlotEntity + SlotDao trait)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md § "dao/src/slot.rs"
  </read_first>
  <files>dao/src/slot.rs</files>
  <action>
    Add a new public field `pub max_paid_employees: Option&lt;u8&gt;,` to `SlotEntity`, positioned **immediately after** the existing `pub min_resources: u8,` field (preserves the conceptual grouping of slot-capacity knobs per D-02 and the Pattern-Map analog placement).

    Do NOT modify the `SlotDao` trait — all methods (`create_slot`, `update_slot`, `get_slot`, `get_slots`, `get_slots_for_week`, etc.) already take/return `SlotEntity` and pass the new field transparently.

    `#[automock]`-generated `MockSlotDao` updates automatically when the struct changes — no manual mock edit needed.

    No new derive needed. The existing `#[derive(Debug, PartialEq, Eq)]` on `SlotEntity` covers `Option&lt;u8&gt;`.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; grep -q "pub max_paid_employees: Option<u8>" dao/src/slot.rs &amp;&amp; cargo build -p dao 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub max_paid_employees: Option<u8>" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao/src/slot.rs` returns `1` (exactly one occurrence).
    - The field appears in source order after `pub min_resources: u8`: `grep -n "min_resources\|max_paid_employees" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao/src/slot.rs` shows `min_resources` line number &lt; `max_paid_employees` line number.
    - `cargo build -p dao` exits 0.
  </acceptance_criteria>
  <done>
    `SlotEntity` carries `max_paid_employees: Option&lt;u8&gt;`; `dao` crate compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire max_paid_employees through SQLite SlotDao impl (4 reads + INSERT + UPDATE)</name>
  <read_first>
    - dao_impl_sqlite/src/slot.rs (full file — ~260 lines)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md § "dao_impl_sqlite/src/slot.rs" (covers all 4 read sites + create_slot + update_slot patches)
    - shifty-backend/CLAUDE.md § "DAO Implementation" (soft-delete + sqlx-prepare conventions)
  </read_first>
  <behavior>
    - Test 1: After `create_slot(slot_with_max_paid_employees=Some(3))` then `get_slot(id)`, the returned `SlotEntity.max_paid_employees == Some(3)`.
    - Test 2: After `create_slot(slot_with_max_paid_employees=None)` then `get_slot(id)`, the returned `SlotEntity.max_paid_employees == None`.
    - Test 3: After `update_slot(slot_with_max_paid_employees=Some(5))` on an existing row, a subsequent `get_slot(id)` returns `Some(5)` (i.e. the UPDATE statement persists the field).
    - Test 4: `update_slot(slot_with_max_paid_employees=None)` on a row that previously had `Some(5)` clears the field back to `None`.

    These behaviors are exercised by the SQLx integration tests in this DAO crate (use `dao_impl_sqlite/tests/slot_integration_test.rs` if it exists; otherwise create one analog to existing patterns). If the DAO crate has no integration test infrastructure yet, the behavior is instead exercised at the service-tier mock layer in Plan 03 — in that case skip writing the integration test here and note in the summary.
  </behavior>
  <files>dao_impl_sqlite/src/slot.rs</files>
  <action>
    Apply six concrete edits in `dao_impl_sqlite/src/slot.rs`:

    **Edit A — extend each of the 4 SELECT lists** (per Pattern-Map line refs ~29 / ~67 / ~110-111 / ~158-159; locate via `grep -n "SELECT id, day_of_week, time_from, time_to, min_resources" dao_impl_sqlite/src/slot.rs`):
    ```text
    BEFORE: SELECT id, day_of_week, time_from, time_to, min_resources, valid_from, valid_to, deleted, update_version, shiftplan_id FROM slot ...
    AFTER:  SELECT id, day_of_week, time_from, time_to, min_resources, max_paid_employees, valid_from, valid_to, deleted, update_version, shiftplan_id FROM slot ...
    ```
    Insert `max_paid_employees` immediately after `min_resources` in every SELECT list (4 occurrences).

    **Edit B — extend each of the 4 read-site `SlotEntity { ... }` constructors:** add the line
    ```rust
    max_paid_employees: row.max_paid_employees.map(|n| n as u8),
    ```
    immediately after the existing `min_resources: row.min_resources as u8,` line at each read site. Per Pattern-Map "Pitfall Summary" #2: SQLx infers nullable INTEGER as `Option&lt;i64&gt;`; the `.map(|n| n as u8)` cast is the standard pattern (truncation safe — `u8` range fits in `i64`). Mirror the `as u8` cast precedent set by `min_resources`.

    **Edit C — extend `create_slot` INSERT** (Pattern-Map lines ~197-231):
    Add the local binding before the `query!` macro:
    ```rust
    let max_paid_employees = slot.max_paid_employees;
    ```
    Then in the SQL INSERT add `max_paid_employees` to the column list (positioned right after `min_resources`) and add a corresponding `?` placeholder, and add `max_paid_employees,` to the bind argument list (right after `min_resources,`):
    ```rust
    query!(
        "INSERT INTO slot (id, day_of_week, time_from, time_to, valid_from, valid_to, deleted, update_version, update_process, min_resources, max_paid_employees, shiftplan_id) \
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
        id_vec, day_of_week, from, to, valid_from, valid_to, deleted,
        version_vec, process, min_resources, max_paid_employees, shiftplan_id_vec,
    )
    ```
    `Option&lt;u8&gt;` binds natively to a nullable INTEGER column — no manual `Option::map` needed at the bind site.

    **Edit D — extend `update_slot` UPDATE** (Pattern-Map lines ~233-254):
    The current UPDATE only sets `valid_to, deleted, update_version, update_process` (the existing `min_resources` gap noted in CONTEXT.md "Strikt nicht in Scope" — leave `min_resources` alone). Add `max_paid_employees` to the SET list so the slot-service can mutate the limit in-place per D-11:
    ```rust
    let max_paid_employees = slot.max_paid_employees;
    query!(
        "UPDATE slot SET valid_to = ?, deleted = ?, max_paid_employees = ?, update_version = ?, update_process = ? WHERE id = ?",
        valid_to, deleted, max_paid_employees, version_vec, process, id_vec,
    )
    ```
    Per Pattern-Map Pitfall #4: this is the cheapest path that satisfies D-11 (in-place mutation; no temporal-replay implication for `max_paid_employees`). Do NOT extend the UPDATE to also cover `min_resources` — that gap is explicitly out of Phase-5 scope per CONTEXT.md "Strikt nicht in Scope".

    Do not run any git/jj commands. The user controls all commits manually.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p dao_impl_sqlite 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p dao_impl_sqlite` exits 0 (proves all SELECT lists, INSERT, UPDATE compile against the live SQLx schema).
    - `grep -c "max_paid_employees" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao_impl_sqlite/src/slot.rs | grep -v '^#'` returns ≥ 10 (4 SELECT-list mentions + 4 read-site assignments + 1 INSERT column + 1 UPDATE column + at least 2 local bindings — comments excluded).
    - `grep -c "row.max_paid_employees.map(|n| n as u8)" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao_impl_sqlite/src/slot.rs` returns `4` (one per read site).
    - `grep -q "INSERT INTO slot.*max_paid_employees" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao_impl_sqlite/src/slot.rs` exits 0.
    - `grep -q "UPDATE slot SET.*max_paid_employees" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao_impl_sqlite/src/slot.rs` exits 0.
    - `cargo build` (workspace-wide) exits 0.
  </acceptance_criteria>
  <done>
    All 4 read sites read `max_paid_employees` as `Option&lt;u8&gt;`; `create_slot` persists it; `update_slot` persists it. Workspace compiles.
  </done>
</task>

</tasks>

<verification>
After all tasks: `cargo build -p dao -p dao_impl_sqlite` exits 0. The `slot` table in the local SQLite DB has the new column. The Pattern-Map's "Pitfalls Summary" items 1, 2, 3 (no DEFAULT/NOT NULL; nullable INTEGER → `Option&lt;i64&gt;`; `update_slot` extended) are all satisfied. Item 10 (use `nix develop --command sqlx migrate run`, never `sqlx database reset` without confirmation) was respected.
</verification>

<success_criteria>
- Migration file exists with the exact column DDL and no NOT NULL / DEFAULT.
- `SlotEntity` carries `max_paid_employees: Option&lt;u8&gt;`.
- All 4 SQLite read sites + INSERT + UPDATE handle the field.
- `cargo build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/05-slot-paid-capacity-warning/05-01-SUMMARY.md` documenting:
- Migration filename
- All edited line ranges in `dao_impl_sqlite/src/slot.rs`
- Confirmation that `min_resources` UPDATE gap was explicitly NOT touched (out of scope per CONTEXT.md)
- Local DB state (column applied via `migrate run`)
</output>
</content>
</invoke>