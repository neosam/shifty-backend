---
phase: 05-slot-paid-capacity-warning
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - service/src/shiftplan.rs
  - service_impl/src/shiftplan.rs
  - service_impl/src/test/shiftplan.rs
autonomous: true
requirements: [D-04, D-05, D-09]
must_haves:
  truths:
    - "ShiftplanSlot carries current_paid_count: u8 (always populated, never Option)"
    - "build_shiftplan_day computes current_paid_count from already-resolved bookings via filter on sales_person.is_paid"
    - "Per-sales-person view (build_shiftplan_day_for_sales_person) inherits current_paid_count transitively (no separate edit needed)"
    - "All Slot { ... } literals in service_impl/src/test/shiftplan.rs carry max_paid_employees: None (Plan 04 owns this file's fixture migration; Plan 03 owns the other 5 test files)"
    - "Read tests verify current_paid_count counts only bookings whose sales_person.is_paid == true; soft-deleted SP excluded; absence-bookings still counted (D-05)"
  artifacts:
    - path: "service/src/shiftplan.rs"
      provides: "ShiftplanSlot struct with current_paid_count: u8"
      contains: "current_paid_count: u8"
    - path: "service_impl/src/shiftplan.rs"
      provides: "build_shiftplan_day populates current_paid_count from slot_bookings filter"
      contains: "current_paid_count"
    - path: "service_impl/src/test/shiftplan.rs"
      provides: "Tests asserting current_paid_count for mixed paid/unpaid bookings + Slot { ... } fixture migration (max_paid_employees: None) for default_slot, slot_with_day_and_time, and inline literals at lines 503/507"
      contains: "current_paid_count"
  key_links:
    - from: "service_impl/src/shiftplan.rs::build_shiftplan_day"
      to: "service::shiftplan::ShiftplanSlot.current_paid_count"
      via: "slot_bookings.iter().filter(|sb| sb.sales_person.is_paid).count() as u8"
      pattern: "is_paid"
    - from: "service::shiftplan::ShiftplanSlot"
      to: "rest-types ShiftplanSlotTO (Plan 05)"
      via: "From<&ShiftplanSlot> for ShiftplanSlotTO — passes current_paid_count through"
      pattern: "current_paid_count"
---

<objective>
Compute and surface `current_paid_count: u8` per slot in the Shiftplan-Week-View aggregator. Read-side aggregation lives in the existing business-logic-tier `ShiftplanViewService` (per CONTEXT.md `code_context` and Pattern-Map § "service_impl/src/shiftplan.rs").

Purpose: Half of D-09 (the read DTO surface). The frontend later consumes `current_paid_count` alongside `slot.max_paid_employees` to render visual indicators. Implements D-04 (count rule: bookings whose `sales_person.is_paid == true`), D-05 (absence status irrelevant — anyone booked counts), D-09 (always populated, no Option dance).

**Wave-2 placement + file ownership (revision):** Plan 04 lives in Wave 2 alongside Plan 03. Plan 03 explicitly does NOT touch `service_impl/src/test/shiftplan.rs` (per its revised `files_modified` list). Plan 04 owns:
1. The new `current_paid_count` field on `ShiftplanSlot` and its computation in `build_shiftplan_day`.
2. The `Slot { ... }` fixture migration (`max_paid_employees: None`) in `service_impl/src/test/shiftplan.rs` for `default_slot()` (line 35), `slot_with_day_and_time()` (line 321), and inline literals at lines 503/507.
3. The 4 new `current_paid_count` tests in the same file.

This split keeps Plan 03 and Plan 04 on disjoint files in Wave 2 (no parallel-write conflict). Plan 05 (Wave 3, depends on 05-04) consumes `ShiftplanSlot.current_paid_count` for the REST DTO mirror.

Output: `ShiftplanSlot` struct gets a new field; `build_shiftplan_day` computes it inline; `test/shiftplan.rs` fixtures migrated to include `max_paid_employees: None`; 4 read tests verify the count semantics.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-slot-paid-capacity-warning/05-CONTEXT.md
@.planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md
@shifty-backend/CLAUDE.md
@service/src/shiftplan.rs
@service_impl/src/shiftplan.rs
@service_impl/src/test/shiftplan.rs

<interfaces>
<!-- Existing aggregator surface — Plan 04 only adds one new field + one count operation + the test/shiftplan.rs fixture migration. -->

From `service/src/shiftplan.rs` (lines 47-50 per Pattern-Map):
```rust
#[derive(Debug, Clone)]
pub struct ShiftplanSlot {
    pub slot: crate::slot::Slot,
    pub bookings: Vec<ShiftplanBooking>,
    // INSERT current_paid_count: u8 here (Phase 5)
}

pub struct ShiftplanBooking {
    pub booking: crate::booking::Booking,
    pub sales_person: crate::sales_person::SalesPerson,  // sales_person.is_paid is in-hand
    pub self_added: Option<bool>,
}
```

`ShiftplanSlot` is reused by both `ShiftplanDay` (line 39) and `PlanDayView` (line 62) — adding a field flows through both. Plan 05's `ShiftplanSlotTO` mirrors the new field.

From `service_impl/src/shiftplan.rs` (lines 27-114 per Pattern-Map): `build_shiftplan_day` already iterates over bookings and resolves the per-booking `sales_person`. The count is derivable in the same loop with one extra `.filter().count()` after `slot_bookings` is built (lines 67-95).

`build_shiftplan_day_for_sales_person` (lines 130-178+) calls `build_shiftplan_day` first — `current_paid_count` populates transitively. **No separate edit there.**

**`Slot { ... }` literal sites in `service_impl/src/test/shiftplan.rs` (verified by grep during revision):**

| Function / Context | Line(s) |
|--------------------|---------|
| `default_slot()` | 34-35 |
| `slot_with_day_and_time()` | 320-321 |
| inline `let slot_a = Slot { ... }` | 503 |
| inline `let slot_b = Slot { ... }` | 507 |

(There are no `SlotEntity { ... }` literals in this file per the grep sweep during revision.)

After Plan 03 lands (same wave), `Slot` will carry `max_paid_employees: Option<u8>` — every literal above must include `max_paid_employees: None,` or this file will not compile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add current_paid_count: u8 to ShiftplanSlot struct</name>
  <read_first>
    - service/src/shiftplan.rs (full file — small, ~80 lines)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md § "service_impl/src/shiftplan.rs" (covers the recommended `Option<u8>` vs `u8` choice)
  </read_first>
  <files>service/src/shiftplan.rs</files>
  <action>
    Add a new public field `pub current_paid_count: u8,` to `ShiftplanSlot`, immediately after `pub bookings: Vec&lt;ShiftplanBooking&gt;,`.

    Per Pattern-Map "Discretion" recommendation: use `u8` (always populated), NOT `Option&lt;u8&gt;`. Rationale: cost is one `.filter().count()` per slot on already-loaded data; the simpler DTO contract avoids `Option` round-trips at the wire layer.

    Per D-09: always populated regardless of whether `slot.max_paid_employees.is_some()`. The frontend decides what to render based on whether `slot.max_paid_employees` is set; the count itself is always factual.

    Do NOT add new derives — `u8` is `Copy + Eq + Debug`, covered by the existing `#[derive(Debug, Clone)]`.

    Do not run any git/jj commands. The user controls all commits manually.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; grep -q "pub current_paid_count: u8" service/src/shiftplan.rs &amp;&amp; cargo build -p service 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub current_paid_count: u8" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/shiftplan.rs` returns 1.
    - The field appears in source order after `pub bookings`: `grep -nE "pub bookings|pub current_paid_count" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/shiftplan.rs` shows `bookings` line &lt; `current_paid_count` line.
    - `cargo build -p service` exits 0 (any consumer that constructs `ShiftplanSlot { slot, bookings }` literally without `..Default::default()` will fail to compile — Task 2 fixes this in `service_impl`).
  </acceptance_criteria>
  <done>
    `ShiftplanSlot.current_paid_count: u8` declared.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute current_paid_count in build_shiftplan_day + migrate test/shiftplan.rs Slot fixtures + add 4 read tests</name>
  <read_first>
    - service_impl/src/shiftplan.rs (full file — focus on `build_shiftplan_day` lines ~27-114; sweep for any `ShiftplanSlot { ... }` literals)
    - service_impl/src/test/shiftplan.rs (full file — locate existing `test_get_shiftplan_week*`-style tests as template; verify the 4 `Slot { ... }` literal sites at lines 35, 321, 503, 507 plus any `ShiftplanSlot { ... }` literals)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md § "service_impl/src/shiftplan.rs"
    - .planning/phases/05-slot-paid-capacity-warning/05-CONTEXT.md (D-04, D-05)
  </read_first>
  <behavior>
    - Test `test_shiftplan_week_emits_current_paid_count_zero_when_no_paid`: slot with 2 bookings, both sales_persons have `is_paid: false`. Returned `ShiftplanSlot.current_paid_count == 0`.
    - Test `test_shiftplan_week_emits_current_paid_count_mixed`: slot with 3 bookings — 2 paid, 1 unpaid. Returned `ShiftplanSlot.current_paid_count == 2`.
    - Test `test_shiftplan_week_emits_current_paid_count_with_no_limit`: slot with `max_paid_employees: None` and 1 paid booking. Returned `ShiftplanSlot.current_paid_count == 1` (D-09: always populated, even when no limit configured).
    - Test `test_shiftplan_week_paid_in_absence_still_counts`: slot has 1 booking whose sales_person `is_paid: true` and is also in an Absence (test fixture marks them). The booking is still counted (D-05: "Absence-Status der gebuchten Person ist irrelevant").
  </behavior>
  <files>service_impl/src/shiftplan.rs, service_impl/src/test/shiftplan.rs</files>
  <action>
    **Step A — `service_impl/src/shiftplan.rs::build_shiftplan_day`** (lines ~27-114):

    Locate the block where `slot_bookings: Vec&lt;ShiftplanBooking&gt;` is constructed (lines ~67-94 per Pattern-Map) and the subsequent `day_slots.push(ShiftplanSlot { ... })` (line ~95).

    Insert the count derivation **between** the `slot_bookings` collection and the `push`:

    ```rust
    // Phase 5 (D-04, D-05, D-09): count bookings whose sales_person.is_paid == true.
    // Soft-deleted bookings are already filtered upstream by booking_dao (WHERE deleted IS NULL);
    // soft-deleted sales_persons are already filtered upstream by SalesPersonService.
    // Absence status of the booked person is irrelevant (D-05) — anyone booked counts.
    let current_paid_count: u8 = slot_bookings
        .iter()
        .filter(|sb| sb.sales_person.is_paid)
        .count()
        .min(u8::MAX as usize) as u8;
    ```

    Then update the push to:
    ```rust
    day_slots.push(ShiftplanSlot {
        slot: slot.clone(),
        bookings: slot_bookings,
        current_paid_count,
    });
    ```

    Do NOT touch `build_shiftplan_day_for_sales_person` (lines ~130-178+) — it calls `build_shiftplan_day` and inherits the new field transitively per Pattern-Map.

    Also: locate any other `ShiftplanSlot { ... }` literal constructors in `service_impl/src/shiftplan.rs` (search via `grep -n "ShiftplanSlot {" service_impl/src/shiftplan.rs`). Each one needs `current_paid_count: …` populated. If a constructor exists outside the main aggregator (e.g. in `PlanDayView` building), set the count using the same per-bookings filter on `slot.bookings` (or `0` if there are no bookings in scope). All such sites must compile.

    **Step B — `service_impl/src/test/shiftplan.rs` fixture migration (Plan 04 owns this file's `Slot { ... }` literals):**

    1. Update `default_slot()` (line ~35): add `max_paid_employees: None,` immediately after the `min_resources:` field (or after the closest existing field if `min_resources` is absent — match the existing struct field order).
    2. Update `slot_with_day_and_time()` (line ~321): same edit.
    3. Update inline `let slot_a = Slot { ... }` (line ~503): same edit.
    4. Update inline `let slot_b = Slot { ... }` (line ~507): same edit.
    5. Search for any other `Slot { ... }` literals in the file: `grep -n "Slot {" service_impl/src/test/shiftplan.rs`. For each match not already covered above, add `max_paid_employees: None,`.
    6. Also: locate any `ShiftplanSlot { ... }` literals (`grep -n "ShiftplanSlot {" service_impl/src/test/shiftplan.rs`). For each, add `current_paid_count: 0,` (or whatever value the test logically requires).

    **Step C — `service_impl/src/test/shiftplan.rs` new tests:**

    Add the 4 tests listed in `<behavior>`, modeled on the existing `test_get_shiftplan_week*` patterns in this file. Use mock `SalesPersonService::get_all` to return SPs with the desired `is_paid` flags; mock `BookingService::get_for_week` to return the bookings.

    For the absence test: set up the SP normally with `is_paid: true`. The test does NOT need to mock the `AbsenceService` — `current_paid_count` is computed from booking + sales_person only (D-05 explicit: absence is orthogonal); the assertion proves we don't accidentally filter on absence.

    For all tests: assert `result.days[…].slots[0].current_paid_count == &lt;expected&gt;`. Use the smallest-slice fixtures possible to keep tests focused.

    Do not run any git/jj commands. The user controls all commits manually.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib shiftplan:: 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "current_paid_count" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/shiftplan.rs` returns ≥ 2 (the count derivation + the struct construction).
    - `grep -q "filter(|sb| sb.sales_person.is_paid)" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/shiftplan.rs` exits 0 (the exact filter predicate is used — proves D-04 semantics).
    - `grep -c "max_paid_employees: None" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan.rs` returns ≥ 4 (the 4 fixture sites — `default_slot`, `slot_with_day_and_time`, inline literals at 503/507).
    - `grep -cE "fn test_shiftplan_week_emits_current_paid_count_zero_when_no_paid|fn test_shiftplan_week_emits_current_paid_count_mixed|fn test_shiftplan_week_emits_current_paid_count_with_no_limit|fn test_shiftplan_week_paid_in_absence_still_counts" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/shiftplan.rs` returns 4.
    - `cargo test -p service_impl --lib shiftplan::` exits 0 (all 4 new tests pass + all pre-existing shiftplan tests still pass).
    - After both Plans 03 and 04 land in Wave 2: `cargo test -p service_impl --lib` exits 0 (workspace-wide test crate compiles and runs cleanly).
    - `cargo build` (workspace) exits 0.
  </acceptance_criteria>
  <done>
    `current_paid_count` populated on every `ShiftplanSlot` returned by `build_shiftplan_day` (and transitively by the per-sales-person view); `test/shiftplan.rs` fixtures migrated to include `max_paid_employees: None`; 4 read tests verify count semantics including the absence-irrelevant rule (D-05).
  </done>
</task>

</tasks>

<verification>
- `cargo build` (workspace) exits 0
- `cargo test -p service_impl --lib shiftplan::` exits 0
- After Plans 03 and 04 both land in Wave 2: `cargo test -p service_impl --lib` exits 0 (no E0063 anywhere in the test crate)
- All 4 new tests pass (zero / mixed / no-limit / absence-still-counts)
- `build_shiftplan_day_for_sales_person` was NOT edited (transitive inheritance works)
</verification>

<success_criteria>
- `ShiftplanSlot.current_paid_count: u8` always populated
- Count rule is `sales_person.is_paid` filter (D-04)
- Absence status does not affect count (D-05 — verified by test)
- Always populated even when `max_paid_employees` is None (D-09)
- All 4 `Slot { ... }` fixture sites in `test/shiftplan.rs` migrated (no E0063 in this file)
</success_criteria>

<output>
After completion, create `.planning/phases/05-slot-paid-capacity-warning/05-04-SUMMARY.md` documenting:
- Lines edited in `service_impl/src/shiftplan.rs` (the count derivation insert + struct push)
- Any other `ShiftplanSlot { ... }` literal sites that needed updating
- All `Slot { ... }` fixture sites in `test/shiftplan.rs` migrated (line numbers + helper-function context)
- Names of the 4 new tests + pass status
- Confirmation that `build_shiftplan_day_for_sales_person` was NOT touched (transitive inheritance)
- Confirmation that `cargo test -p service_impl --lib` exits 0 once both 05-03 and 05-04 are applied (workspace-wide test crate compile + pass)
</output>
</content>
