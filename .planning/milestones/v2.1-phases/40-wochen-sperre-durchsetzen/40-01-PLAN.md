---
phase: 40-wochen-sperre-durchsetzen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service/src/lib.rs
  - rest/src/lib.rs
  - service/src/shiftplan_edit.rs
  - service_impl/src/shiftplan_edit.rs
  - shifty_bin/src/main.rs
  - service_impl/src/test/shiftplan_edit.rs
autonomous: true
requirements: [WST-03, WST-04]

must_haves:
  truths:
    - "ServiceError::WeekLocked { year, week } existiert und mappt in error_handler auf HTTP 423 (D-40-01)."
    - "ShiftplanEditService bietet delete_booking; WeekStatusService ist eine Dep in ShiftplanEditServiceDeps; das Bypass-Privileg des Lock-Gates ist shiftplan.edit (D-40-02)."
    - "Nach Hinzufügen der Phase-40-Oberfläche bleiben cargo build --workspace und cargo test --workspace grün — die Sperre blockiert noch nicht (Enforcement folgt in 40-03)."
  artifacts:
    - service/src/lib.rs
    - rest/src/lib.rs
    - service/src/shiftplan_edit.rs
    - service_impl/src/shiftplan_edit.rs
    - shifty_bin/src/main.rs
    - service_impl/src/test/shiftplan_edit.rs
  key_links:
    - "ServiceError::WeekLocked -> exhaustiver Match in rest/src/lib.rs error_handler -> 423"
    - "ShiftplanEditServiceDeps.week_status_service -> assert_week_not_locked -> WeekStatusService::get_week_status"
    - "gen_service_impl! Deps-Feld -> main.rs ShiftplanEditServiceDependencies assoc type + Konstruktor-Feld -> Test-Harness build_service/build_dependencies"
---

<objective>
Errichtet die vollständige Phase-40-Backend-Oberfläche so, dass der Workspace grün
kompiliert und alle bestehenden Tests grün bleiben — jedoch OHNE aktive Sperre. Dieser
Plan ist reines Scaffolding/Plumbing: die eigentliche Blockier-Logik (RED-first Tests +
GREEN) folgt in Plan 40-03.

Konkret: neue Fehler-Variante `ServiceError::WeekLocked` + HTTP-423-Mapping (D-40-01);
`delete_booking` im `ShiftplanEditService`-Trait; `WeekStatusService` als neue Dependency
im Business-Logic-Tier-Service; ein zunächst NICHT-blockierender `assert_week_not_locked`-
Helper (liest den Status, gibt aber immer Ok zurück), aufgerufen an den 6 Schreibpfad-Köpfen;
und die DI-/Test-Harness-Nachführung, damit der Deps-Change kompiliert.

Purpose: Die enge Cross-Crate-Compile-Kopplung (exhaustives Error-Match, Trait-Methode,
neue Dep bricht Test-Harness) in einem grünen Schritt auflösen, damit Plan 40-03 die
Enforcement echt TDD-RED-first entwickeln kann.
Output: Kompilierende WeekLocked-Variante, 423-Arm, delete_booking-Methode (get→helper→delete),
WeekStatusService-Dep verdrahtet, pass-through Helper an 6 Köpfen, Test-Harness aktualisiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-CONTEXT.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Dieser Plan liefert (Enforcement inaktiv):
- `ServiceError::WeekLocked { year, week }` (service/src/lib.rs)
- 423-Match-Arm in `error_handler` (rest/src/lib.rs)
- `ShiftplanEditService::delete_booking`-Trait-Methode (service/src/shiftplan_edit.rs)
- `assert_week_not_locked`-Helper (pass-through) + `WeekStatusService` in `ShiftplanEditServiceDeps` (service_impl/src/shiftplan_edit.rs)
- DI-Wiring in main.rs (assoc type + Konstruktor-Feld)
- Test-Harness-Nachführung (service_impl/src/test/shiftplan_edit.rs)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: WeekLocked-Variante + HTTP-423-Mapping (D-40-01)</name>
  <files>service/src/lib.rs, rest/src/lib.rs</files>
  <read_first>
    - service/src/lib.rs (ServiceError-Enum, Zeilen 65-132; PaidLimitExceeded bei :129 als Nachbar-Variante)
    - rest/src/lib.rs (error_handler; PaidLimitExceeded-Arm bei :254-258 als 409-Template — das strukturelle Vorbild, hier mit Status 423)
  </read_first>
  <action>
    In service/src/lib.rs die neue Variante `WeekLocked { year: u32, week: u8 }` zum
    `ServiceError`-Enum hinzufügen (direkt nach `PaidLimitExceeded`), mit einer
    `#[error(...)]`-Message, die Jahr und Woche einbettet und ausdrückt, dass die Woche
    gesperrt ist und Änderungen nicht möglich sind (D-40-01). Feldtypen exakt u32/u8, damit
    Booking-Werte (`booking.year: u32`, `booking.calendar_week as u8`) und die Slot-Args
    (change_year: u32, change_week: u8) ohne weitere Casts passen.

    In rest/src/lib.rs im `error_handler` einen neuen Match-Arm ergänzen, strukturell
    identisch zum PaidLimitExceeded-Arm, aber mit `.status(423)` statt 409. Body via
    `Body::new(err.to_string())` (i18n-neutrale #[error]-Message; FE zeigt keinen Fehler-
    Banner, D-40-04, deshalb genügt der Response-Body). Der Match ist exhaustiv (kein
    `_`-Catch-all) — der Compiler erzwingt diesen Arm; ohne ihn kompiliert das rest-Crate
    nicht. Erster 423-Fall im Codebase (bewusst abweichend vom 409-Muster, D-40-01).
  </action>
  <verify>
    <automated>cargo build -p service && cargo build -p rest && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `ServiceError::WeekLocked { year: u32, week: u8 }` existiert mit passender #[error]-Message.
    - error_handler besitzt einen `WeekLocked`-Arm mit HTTP-Status 423 und Response-Body aus `err.to_string()`.
    - Workspace kompiliert und clippy ist grün (der exhaustive Match ist erfüllt).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: delete_booking-Trait + WeekStatusService-Dep + pass-through Helper + DI + Test-Harness</name>
  <files>service/src/shiftplan_edit.rs, service_impl/src/shiftplan_edit.rs, shifty_bin/src/main.rs, service_impl/src/test/shiftplan_edit.rs</files>
  <read_first>
    - service/src/shiftplan_edit.rs (Trait ShiftplanEditService; :118 book_slot, :41 modify_slot, :62 modify_slot_single_week, :71 remove_slot, :134 copy_week — Signaturmuster für die neue Methode)
    - service/src/booking.rs (:72-77 BookingService::get, :108-113 BookingService::delete — Semantik-Referenz; :12-24 Booking trägt year:u32/calendar_week:i32)
    - service/src/week_status.rs (WeekStatusService-Trait; get_week_status(year:u32, calendar_week:u8, context: Authentication<Self::Context>, tx: Option<Self::Transaction>))
    - service_impl/src/shiftplan_edit.rs (:11 import SHIFTPLANNER_PRIVILEGE/Authentication; :26-44 gen_service_impl! ShiftplanEditServiceDeps; Schreibmethoden-Köpfe modify_slot :51/:59-62, remove_slot :145/:153-156, modify_slot_single_week :199/:208-213, book_slot :551/:557/:574-575, copy_week :759/:768/:773-775; :826 Helper-impl-Block count_paid_bookings_in_slot_week)
    - shifty_bin/src/main.rs (:426-437 WeekStatusServiceDependencies + type WeekStatusService; :439-461 ShiftplanEditServiceDependencies impl; :1005-1021 ShiftplanEditServiceImpl-Konstruktion; :1064 week_status_service-Konstruktion)
    - service_impl/src/test/shiftplan_edit.rs (:150-202 ShiftplanEditDependencies struct + Deps-impl + build_service; :207-220 build_dependencies)
  </read_first>
  <action>
    1) service/src/shiftplan_edit.rs: Dem `ShiftplanEditService`-Trait die Methode
    `delete_booking(&self, booking_id: Uuid, context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>) -> Result<(), ServiceError>` hinzufügen. Der Trait trägt
    `#[automock]` → MockShiftplanEditService::delete_booking wird automatisch generiert.

    2) service_impl/src/shiftplan_edit.rs — Dependency: Im `gen_service_impl!`-Block
    (:26-44) `WeekStatusService: service::week_status::WeekStatusService<Context = Self::Context,
    Transaction = Self::Transaction> = week_status_service` ergänzen. WICHTIG (RESEARCH Open
    Question 1): zuerst `cargo build -p service_impl` ausführen; bei E0220/E0277 (kein
    Transaction-Assoc-Type) den Bound auf `<Context = Self::Context>` reduzieren, analog zum
    39-03-RestStateDef-Auto-Fix. Import `service::week_status::WeekStatus` ergänzen.

    3) service_impl/src/shiftplan_edit.rs — Helper (im impl-Block bei :826, neben
    count_paid_bookings_in_slot_week): private `async fn assert_week_not_locked(&self,
    year: u32, calendar_week: u8, context: Authentication<Deps::Context>, tx: Deps::Transaction)
    -> Result<(), ServiceError>`. In DIESEM Plan bewusst NICHT-blockierend: den Status in
    derselben Transaktion lesen (`self.week_status_service.get_week_status(year, calendar_week,
    Authentication::Full, Some(tx)).await?`) und danach immer `Ok(())` zurückgeben. Der Read
    „benutzt" die neue Dep (kein dead-field-clippy). Die Blockier-Logik + der shiftplan.edit-
    Bypass werden in Plan 40-03 (GREEN) ergänzt. KEINEN `todo!()` verwenden (würde bestehende
    Tests bei Aufruf paniken lassen).

    4) service_impl/src/shiftplan_edit.rs — Gate-Aufrufe an 6 Köpfen, jeweils direkt nach
    dem bestehenden `check_permission`, vor der ersten Business-Logik, mit `context.clone()`
    und `tx.clone()`:
      - modify_slot: nach :62, `assert_week_not_locked(change_year, change_week, ...)`
      - remove_slot: nach :156, `(change_year, change_week, ...)`
      - modify_slot_single_week: nach :213, `(change_year, change_week, ...)`
      - book_slot_with_conflict_check: nach :575 (nach `sp_perm.or(self_perm)?`),
        `(booking.year, booking.calendar_week as u8, ...)`
      - copy_week_with_conflict_check: nach :775, AUSSCHLIESSLICH die ZIEL-Woche
        `(to_year, to_calendar_week, ...)` (nicht from_* — Lesen der Quelle ist nie gesperrt)
    Den Helper unbedingt AUFRUFEN (sonst dead_code auf der Methode).

    5) service_impl/src/shiftplan_edit.rs — delete_booking-Impl: `use_transaction(tx)`;
    dann Booking via `self.booking_service.get(booking_id, Authentication::Full, Some(tx.clone()))`
    laden (VOR dem Delete — sonst kein year/week lesbar); dann `assert_week_not_locked(
    booking.year, booking.calendar_week as u8, context.clone(), tx.clone())` (6. Pfad); dann
    `self.booking_service.delete(booking_id, context, Some(tx.clone()))` (erhält die
    Basic-Tier-Permission Shiftplanner ∨ Self — kein Zugriffsregel-Change, nur Lock addiert);
    `commit(tx)`. Reihenfolge get → assert → delete ist zwingend und wird in 40-03 getestet.

    6) shifty_bin/src/main.rs: In `ShiftplanEditServiceDependencies` (:439-461)
    `type WeekStatusService = WeekStatusService;` ergänzen. In der ShiftplanEditServiceImpl-
    Konstruktion (:1005) das Feld `week_status_service: week_status_service.clone()` ergänzen.
    `week_status_service` wird bereits bei :1064 konstruiert und muss VOR der
    ShiftplanEdit-Konstruktion verfügbar sein — falls die Konstruktionsreihenfolge nicht passt
    (ShiftplanEdit bei :1005 vor week_status bei :1064), die `let week_status_service = ...`-
    Bindung vor die ShiftplanEdit-Konstruktion ziehen (Basic-Tier vor Business-Logic-Tier,
    Service-Tier-Konvention).

    7) service_impl/src/test/shiftplan_edit.rs: `week_status_service: MockWeekStatusService`
    zum `ShiftplanEditDependencies`-struct + `type WeekStatusService = MockWeekStatusService`
    zum Deps-impl + `week_status_service: self.week_status_service.into()` zu build_service
    hinzufügen. In `build_dependencies` einen Default setzen: `week_status_service.expect_get_week_status()
    .returning(|_, _, _, _| Ok(WeekStatus::Unset))` (ohne .times) — damit die bestehenden
    Nicht-Bypass-Tests (Self-Booker in book_slot) beim Helper-Read nicht paniken. MockWeekStatusService
    importieren (aus service::week_status).
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test --workspace && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `delete_booking` ist im Trait deklariert und im Impl mit Reihenfolge get→assert_week_not_locked→delete umgesetzt.
    - `WeekStatusService` ist Dep im gen_service_impl!-Block; main.rs assoc type + Konstruktor-Feld gesetzt; Konstruktionsreihenfolge Basic-vor-Business erhalten.
    - `assert_week_not_locked` liest den Status in-tx und wird an allen 6 Köpfen aufgerufen (copy_week: nur Ziel-Woche); in DIESEM Plan blockiert es nicht (immer Ok).
    - Test-Harness (struct/impl/build_service/build_dependencies) kompiliert; bestehende service_impl-Tests bleiben grün (Default get_week_status→Unset).
    - `cargo build --workspace`, `cargo test --workspace`, `cargo clippy --workspace -- -D warnings` grün. Keine neue query! → kein sqlx prepare nötig.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST API | Rolle/Context des Aufrufers ist untrusted; Autorisierung erst serverseitig |
| REST → Service | error_handler ist der einzige Ort für Error→HTTP-Mapping |
| Service → DB | Lock-Read + Write laufen in einer Transaktion |

## STRIDE Threat Register (Phase 40 gesamt — dieser Plan errichtet die Oberfläche)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Elevation of Privilege | 6 Schreibpfade in ShiftplanEditService | high | mitigate | assert_week_not_locked an allen 6 Köpfen (Logik in 40-03); Oberfläche hier |
| T-40-02 | Elevation of Privilege | DELETE /booking bypass (WST-04) | high | mitigate | neue delete_booking-Methode (get→gate→delete); Re-Routing in 40-04 |
| T-40-05 | Denial of Service | vergessenes 423-Mapping | low | mitigate | exhaustives match ohne `_` → Compiler erzwingt den Arm (Task 1) |
| T-40-SC | Tampering | Package-Installs | low | accept | Phase 40 installiert keine Pakete (RESEARCH: Audit entfällt) |
</threat_model>

<verification>
- `cargo build --workspace` grün (WeekLocked-Variante + 423-Arm + delete_booking + Dep kompilieren).
- `cargo test --workspace` grün (keine Regression durch Dep-Change + pass-through Gate).
- `cargo clippy --workspace -- -D warnings` grün (kein dead-field/dead-code).
- Enforcement ist bewusst NOCH NICHT aktiv — verifiziert in 40-03.
</verification>

<success_criteria>
Der Workspace kompiliert und testet grün mit der vollständigen Phase-40-Backend-Oberfläche;
die Sperre blockiert noch nicht (Scaffold). 40-03 kann RED-first darauf aufsetzen.
</success_criteria>

<output>
Create `.planning/phases/40-wochen-sperre-durchsetzen/40-01-SUMMARY.md` when done
</output>
