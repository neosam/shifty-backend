---
phase: 40-wochen-sperre-durchsetzen
plan: 04
type: execute
wave: 3
depends_on: [40-03]
files_modified:
  - rest/src/booking.rs
  - rest/src/shiftplan_edit.rs
autonomous: true
requirements: [WST-04, WST-03]

must_haves:
  truths:
    - "DELETE /booking/{id} routet über ShiftplanEditService::delete_booking und schließt damit den Basic-Tier BookingService::delete-Bypass (WST-04)."
    - "Die HTTP-423-Antwort ist in der OpenAPI-Spec auf den annotierten Schreib-Endpoints dokumentiert (D-40-01)."
  artifacts:
    - rest/src/booking.rs
    - rest/src/shiftplan_edit.rs
  key_links:
    - "rest/src/booking.rs delete_booking-Handler -> shiftplan_edit_service().delete_booking()"
    - "book_slot_with_conflict_check / copy_week_with_conflict_check #[utoipa::path] responses -> (status = 423)"
---

<objective>
Schließt den letzten echten Nicht-Schichtplaner-Bypass (WST-04): der `DELETE /booking/{id}`-
Handler ruft heute direkt das Basic-Tier `BookingService::delete` (ohne Lock-Gate) auf. Er
wird auf das Business-Logic-Tier `ShiftplanEditService::delete_booking` (aus 40-01/40-03,
mit Lock-Gate) umgeroutet. Zusätzlich wird die neue HTTP-423-Antwort in der OpenAPI-Spec
auf den bereits annotierten Schreib-Endpoints dokumentiert (D-40-01, CLAUDE.md-Gate).

Purpose: Ohne das Re-Routing bleibt der delete-Pfad trotz implementierter Enforcement
umgehbar. Dieser Plan macht die Enforcement für den delete-Pfad wirksam.
Output: umgerouteter delete-Handler; 423 in den utoipa-responses von book_slot + copy_week.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-CONTEXT.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-RESEARCH.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- Re-Routing des `DELETE /booking/{id}`-Handlers auf `shiftplan_edit_service().delete_booking(...)` (rest/src/booking.rs)
- `(status = 423, ...)` in den #[utoipa::path]-responses von book_slot + copy_week (rest/src/shiftplan_edit.rs)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: DELETE /booking/{id} auf ShiftplanEditService::delete_booking umrouten (WST-04)</name>
  <files>rest/src/booking.rs</files>
  <read_first>
    - rest/src/booking.rs (:156-172 delete_booking-Handler; ruft heute booking_service().delete(...); :25 Route DELETE /{id})
    - rest/src/lib.rs (:427 shiftplan_edit_service()-Accessor in RestStateDef — bereits vorhanden)
    - service/src/shiftplan_edit.rs (delete_booking-Signatur aus 40-01)
  </read_first>
  <action>
    Im `delete_booking`-Handler (:156-172) den Aufruf von `rest_state.booking_service()
    .delete(booking_id, context.into(), None)` auf `rest_state.shiftplan_edit_service()
    .delete_booking(booking_id, context.into(), None)` umstellen. `shiftplan_edit_service()`
    ist in RestStateDef bereits vorhanden (rest/src/lib.rs:427) — keine Änderung an Route
    oder RestStateDef nötig. Der Response-Statuscode (200 + leerer Body) bleibt unverändert;
    im Locked-Fall liefert error_handler jetzt 423 (via 40-01-Arm), sonst wie bisher.
    Semantik (Permission Shiftplanner ∨ Self) bleibt erhalten, da delete_booking intern an
    booking_service.delete delegiert — es wird nur der Lock-Gate addiert.
  </action>
  <verify>
    <automated>cargo build -p rest && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Der DELETE-Handler ruft `shiftplan_edit_service().delete_booking(...)` statt `booking_service().delete(...)`.
    - Route/RestStateDef unverändert; Erfolgs-Response 200 unverändert.
    - `cargo build -p rest` + clippy grün.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HTTP 423 in OpenAPI dokumentieren (D-40-01)</name>
  <files>rest/src/shiftplan_edit.rs</files>
  <read_first>
    - rest/src/shiftplan_edit.rs (:154-166 book_slot_with_conflict_check #[utoipa::path] mit responses inkl. :162 status=409; :195-205 copy_week_with_conflict_check #[utoipa::path]; :72 + :237 dokumentierte Konvention, dass edit_slot/edit_slot_single_week/delete_slot bewusst OHNE Annotation sind)
  </read_first>
  <action>
    In den `responses(...)`-Blöcken der beiden bereits annotierten Schreib-Handler
    `book_slot_with_conflict_check` (~:159-164) und `copy_week_with_conflict_check` (~:200-204)
    jeweils `(status = 423, description = "Week is locked — changes are not possible")`
    ergänzen. book_slot ist der reale 423-Pfad (Self-Booker in gesperrter Woche); copy_week
    ergänzt es der Konsistenz halber (Bypass-only, kann in der Praxis kein 423 liefern, aber
    dokumentiert die mögliche Antwort einheitlich).

    delete_booking (rest/src/booking.rs) trägt heute KEINE #[utoipa::path]-Annotation — genau
    wie die Geschwister-Handler edit_slot/delete_slot, deren fehlende Annotation in
    rest/src/shiftplan_edit.rs:72 und :237 als bewusste Codebase-Konvention dokumentiert ist.
    booking.rs besitzt zudem keinen eigenen ApiDoc/OpenApi-Rahmen. Daher wird für delete_booking
    KEINE neue utoipa-Infrastruktur erzwungen (konsistent mit der bestehenden Konvention);
    im SUMMARY vermerken, dass der delete_booking-423 der No-Annotation-Konvention folgt.
    Keine bestehende Annotation entfernen.
  </action>
  <verify>
    <automated>cargo build -p rest && cargo test --workspace && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - book_slot_with_conflict_check und copy_week_with_conflict_check listen `(status = 423, ...)` in ihren responses.
    - Keine bestehende utoipa-Annotation entfernt; delete_booking folgt der dokumentierten No-Annotation-Konvention (im SUMMARY vermerkt).
    - `cargo build -p rest` + `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` grün.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DELETE /booking/{id} | bisher direkter Basic-Tier-Pfad ohne Lock-Gate (Bypass) |
| REST → OpenAPI-Spec | Spec muss das tatsächliche 423-Verhalten widerspiegeln |

## STRIDE Threat Register (dieser Plan: Bypass-Schließung + Doku)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-02 | Elevation of Privilege | DELETE /booking/{id} bypass (WST-04) | high | mitigate | Handler auf shiftplan_edit_service().delete_booking() umgeroutet → Lock-Gate greift |
| T-40-06 | Repudiation/Doku | OpenAPI ohne 423 | low | mitigate | 423 in den annotierten Schreib-Endpoints dokumentiert (Task 2) |
| T-40-SC | Tampering | Package-Installs | low | accept | Keine neuen Pakete. |
</threat_model>

<verification>
- `cargo build -p rest` grün (Re-Routing kompiliert gegen delete_booking-Trait).
- `cargo test --workspace` grün.
- `cargo clippy --workspace -- -D warnings` grün.
- Der delete-Pfad ist jetzt gegated (end-to-end wirksam durch 40-03-Enforcement + dieses Re-Routing).
</verification>

<success_criteria>
DELETE /booking/{id} durchläuft das Lock-Gate; der letzte Nicht-Schichtplaner-Bypass ist
geschlossen; die 423-Antwort ist in der OpenAPI-Spec dokumentiert.
</success_criteria>

<output>
Create `.planning/phases/40-wochen-sperre-durchsetzen/40-04-SUMMARY.md` when done
</output>
