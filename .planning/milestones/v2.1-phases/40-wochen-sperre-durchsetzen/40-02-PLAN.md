---
phase: 40-wochen-sperre-durchsetzen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/page/shiftplan.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [WST-03]

must_haves:
  truths:
    - "In einer Locked-Woche sieht ein Nicht-shiftplan.edit-Nutzer keine +/- Buttons; die Controls verschwinden komplett aus dem DOM (D-40-03)."
    - "Es wird kein Inline-Banner gebaut; das rote Gesperrt-Badge aus Phase 39 plus die fehlenden Buttons sind das einzige UI-Signal (D-40-04)."
    - "Ein i18n-Key WeekLockedError existiert in de/en/cs für den 423-Response-Text (D-40-05)."
    - "shiftplan.edit-Halter (is_shift_editor) sehen in einer Locked-Woche weiterhin +/- Buttons (D-40-02-Bypass, D-40-03)."
  artifacts:
    - shifty-dioxus/src/page/shiftplan.rs
    - shifty-dioxus/src/i18n/mod.rs
    - shifty-dioxus/src/i18n/de.rs
    - shifty-dioxus/src/i18n/en.rs
    - shifty-dioxus/src/i18n/cs.rs
  key_links:
    - "WEEK_STATUS_STORE.status (Phase 39) + is_shift_editor -> button_mode -> WeekViewButtonTypes::None -> ColumnViewSlot show_add/show_remove"
    - "Key::WeekLockedError -> de.rs/en.rs/cs.rs Übersetzungen + Presence-Test"
---

<objective>
Rein subtraktive FE-Änderung: In einer gesperrten Woche werden für Nicht-shiftplan.edit-
Nutzer die +/- Buttons ausgeblendet (D-40-03). Kein neues visuelles Element, kein Banner
(D-40-04). Zusätzlich der lokalisierte Sperr-Text (de/en/cs) als i18n-Key für den
423-Response-Body (D-40-05).

Purpose: Damit niemand gegen eine Wand läuft — reine UX-Ergänzung zum Server-Gate. Die
eigentliche Durchsetzung bleibt serverseitig (D-40-03); dieses Ausblenden schützt NICHT
gegen direkte API-Calls.
Output: neuer else-if-Zweig in button_mode; WeekLockedError-Key in allen drei Locales +
Presence-Test.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-CONTEXT.md
@.planning/phases/40-wochen-sperre-durchsetzen/40-UI-SPEC.md
@shifty-dioxus/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- `else if`-Zweig in `button_mode` (shifty-dioxus/src/page/shiftplan.rs), Priorität 2
- `Key::WeekLockedError` + de/en/cs-Übersetzungen + Presence-Test (shifty-dioxus/src/i18n/*)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: +/- Buttons in Locked-Woche für Nicht-shiftplan.edit ausblenden (D-40-03)</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs (:107 `week_status = WEEK_STATUS_STORE.read().status.clone()`; :108-111 `is_shiftplanner`; :112-115 `is_shift_editor` = has_privilege("shiftplan.edit"); :259-266 button_mode-Kette)
    - shifty-dioxus/src/state/week_status.rs (FE-Enum WeekStatus mit Locked-Variante; derive Clone/PartialEq/Default)
    - .planning/phases/40-wochen-sperre-durchsetzen/40-UI-SPEC.md (Prioritätentabelle + DOM-Verhalten)
  </read_first>
  <action>
    In der `button_mode`-Berechnung (aktuell :259-266) einen neuen Zweig als Priorität 2
    einfügen — nach `change_structure_mode` (Priorität 1), VOR der 2-Wochen-Veraltungs-
    Heuristik (Priorität 3): wenn die Woche gesperrt ist UND der Nutzer KEIN shiftplan.edit-
    Halter ist, dann `WeekViewButtonTypes::None`.

    Bedingung: `week_status == crate::state::week_status::WeekStatus::Locked && !is_shift_editor`.
    `is_shift_editor` (has_privilege("shiftplan.edit"), bereits :112-115 vorhanden) ist der
    Bypass — NICHT `is_shiftplanner`. Grund: D-40-02 definiert das Bypass-Privileg als
    shiftplan.edit; das hält FE und BE-Helper (40-01/40-03) konsistent (RESEARCH/UI-SPEC
    nannten is_shiftplanner — die gelockte Entscheidung D-40-02 hat Vorrang). `week_status`
    ist ein geclonter Wert (kein Signal), also kein `*` nötig; ggf. `use crate::state::week_status::WeekStatus`.

    `WeekViewButtonTypes::None` setzt show_add:false/show_remove:false auf jedem ColumnViewItem
    → die bestehenden `if props.item_data.show_add { … }`-Zweige erzeugen keinen DOM-Knoten.
    Kein neues Tailwind-Class, keine neue Komponente. DayAggregateView erhält button_types aus
    demselben button_mode → automatisch abgedeckt, kein separater Fix. Rotes Gesperrt-Badge
    (Phase 39) bleibt unverändert.
  </action>
  <verify>
    <automated>cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - button_mode enthält einen Priorität-2-Zweig `week_status == Locked && !is_shift_editor → WeekViewButtonTypes::None`.
    - Priorität 1 (change_structure_mode) und Priorität 3 (2-Wochen-Heuristik) bleiben unverändert und in dieser Reihenfolge.
    - Kein neues Tailwind-Class, keine neue Komponente, kein Banner.
    - `cargo build --target wasm32-unknown-unknown` (in shifty-dioxus/) grün.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: i18n WeekLockedError de/en/cs + Presence-Test (D-40-05)</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (pub enum Key; tests-Modul mit bestehenden Presence-Tests als Muster)
    - shifty-dioxus/src/i18n/de.rs, en.rs, cs.rs (Match-Arme pro Key als Muster)
  </read_first>
  <action>
    Variante `WeekLockedError` zu `pub enum Key` in mod.rs hinzufügen (falls Phase 39 einen
    verwandten Key wie WeekStatusLocked eingeführt hat, Namensraum entsprechend prüfen; sonst
    WeekLockedError). Übersetzungen ergänzen — alle drei Pflicht:
      - de.rs: "Diese Woche ist gesperrt — Änderungen sind nicht möglich."
      - en.rs: "This week is locked — changes are not possible."
      - cs.rs: "Tento týden je uzamčen — změny nejsou možné."
    Presence-Test im tests-Modul von mod.rs nach bestehendem Muster ergänzen, sodass der
    Key in allen drei Locales einen nicht-leeren Text liefert (kein "??"-Fallback).
    Hinweis: Da kein FE-Banner gebaut wird (D-40-04), wird der Key aktuell nicht direkt
    gerendert — er dient dem 423-Response-Body und potenzieller späterer Verwendung.
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus i18n</automated>
  </verify>
  <acceptance_criteria>
    - `Key::WeekLockedError` (oder passend zum Phase-39-Namensraum) existiert und ist in de/en/cs übersetzt.
    - Presence-Test deckt alle drei Locales ab und ist grün.
    - `cargo test -p shifty-dioxus i18n` (in shifty-dioxus/) grün.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (FE) → REST API | FE-Sichtbarkeit ist KEINE Sicherheitsgrenze; nur UX |

## STRIDE Threat Register (dieser Plan: FE-Anteil)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-04 | Tampering / EoP | FE-Button-Ausblenden | high | mitigate | FE-Control ist UX-only (D-40-03); die eigentliche Durchsetzung ist das Server-Gate (40-01/40-03/40-04). Ausblenden schützt NICHT gegen direkte API-Calls. |
| T-40-SC | Tampering | Package-Installs | low | accept | Keine neuen FE-Pakete. |
</threat_model>

<verification>
- `cargo build --target wasm32-unknown-unknown` (shifty-dioxus/) grün.
- `cargo test -p shifty-dioxus` (shifty-dioxus/) grün.
- Struktureller Test (button_mode-Prädikat / Presence-Test) ist das harte Gate; der Live-
  Browser-Smoke (Buttons verschwinden für Nicht-Editor in Locked-Woche) ist optional
  (D-25-06-Klasse, siehe 40-VALIDATION.md Manual-Only).
</verification>

<success_criteria>
Nicht-shiftplan.edit-Nutzer sehen in einer Locked-Woche keine +/- Buttons; Editoren behalten
sie; kein Banner; der lokalisierte Sperr-Text existiert in de/en/cs. FE-Build + Tests grün.
</success_criteria>

<output>
Create `.planning/phases/40-wochen-sperre-durchsetzen/40-02-SUMMARY.md` when done
</output>
