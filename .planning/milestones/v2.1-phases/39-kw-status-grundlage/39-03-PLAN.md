---
phase: 39-kw-status-grundlage
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - rest-types/src/lib.rs
  - rest/src/week_status.rs
  - rest/src/lib.rs
  - shifty_bin/src/main.rs
autonomous: true
requirements: [WST-01]
must_haves:
  truths:
    - "GET /week-status/by-year-and-week/{year}/{week} liefert fuer alle Rollen den aktuellen Status als WeekStatusTO (status=Unset wenn keine Zeile); PUT auf demselben Pfad setzt den Status (Upsert) und ist Schichtplaner-gated (D-39-01, D-39-06)."
    - "Der neue WeekStatusService wird in main.rs in der Basic-Service-Schicht verdrahtet — direkt neben week_message_service, VOR der Business-Logic-Schicht — und haengt an keinem Domain-Service (D-39-12)."
    - "Jeder neue REST-Handler traegt #[utoipa::path]; WeekStatusApiDoc ist in die zentrale ApiDoc-Struct eingetragen, sonst fehlen die Endpunkte in Swagger (RESEARCH Pitfall P-3)."
    - "WeekStatusTO/WeekStatusKindTO haben ToSchema; die WeekStatusKindTO-Variante heisst Unset, nie None (D-39-03), und traegt die vierte Leer-Variante fuer die Wire-Ebene (D-39-04)."
  artifacts:
    - "rest-types/src/lib.rs (WeekStatusTO, WeekStatusKindTO + service-impl-From-Impls)"
    - "rest/src/week_status.rs (generate_route, get_/upsert-Handler, WeekStatusApiDoc)"
    - "rest/src/lib.rs (mod week_status, RestStateDef-Assoc-Type + Accessor, ApiDoc-Eintrag, Router-nest)"
    - "shifty_bin/src/main.rs (Typ-Alias, WeekStatusServiceDependencies, RestStateImpl-Feld+Accessor, Konstruktion)"
  key_links:
    - "rest/src/lib.rs: `.nest(\"/week-status\", week_status::generate_route())` + `(path = \"/week-status\", api = week_status::WeekStatusApiDoc)` — beide noetig."
    - "main.rs: week_status_service-Feld in RestStateImpl + Accessor + `type WeekStatusService = WeekStatusService;` in impl RestStateDef."
    - "rest-types From-Impls unter #[cfg(feature = \"service-impl\")] mappen WeekStatusKindTO ↔ service::week_status::WeekStatus (inkl. Unset)."
---

<objective>
Transport- und Transport-Verdrahtungs-Schicht: das DTO in rest-types, die REST-Handler mit OpenAPI-Annotationen und das DI-Wiring des Basic-Tier-Service in main.rs. Damit ist der KW-Status ueber HTTP erreichbar — Lese-Pfad fuer alle Rollen, Schreib-Pfad Schichtplaner-gated (das Gate liegt im Service aus Wave 2).

Purpose: WST-01 verlangt Status-CRUD-REST; ohne DTO + Router + DI ist der Service nicht ansprechbar und das Frontend (Wave 4/5) haette keinen Endpunkt.
Output: `WeekStatusTO`/`WeekStatusKindTO`, `rest/src/week_status.rs` (Router + Handler + ApiDoc), Registrierung in `rest/src/lib.rs`, komplettes DI-Wiring in `shifty_bin/src/main.rs`.

Warum eigene Welle: Die rest-types-From-Impls und die REST-Handler referenzieren `service::week_status::WeekStatus` aus Wave 2; das DI-Wiring instanziiert `WeekStatusServiceImpl`. Muss vor dem Frontend (Wave 4) fertig sein, weil das FE `WeekStatusTO` aus rest-types importiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-kw-status-grundlage/39-CONTEXT.md
@.planning/phases/39-kw-status-grundlage/39-RESEARCH.md
@.planning/phases/39-kw-status-grundlage/39-PATTERNS.md
@.planning/phases/39-kw-status-grundlage/39-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WeekStatusTO + WeekStatusKindTO in rest-types</name>
  <files>rest-types/src/lib.rs</files>
  <read_first>
    - rest-types/src/lib.rs (WeekMessageTO um Zeile ~1238 + dessen #[cfg(feature = "service-impl")]-From-Impls als Analog)
    - service/src/week_status.rs (WeekStatus-Enum aus Wave 2 — Ziel der From-Impls)
  </read_first>
  <action>
    Ergaenze in `rest-types/src/lib.rs` (Analog WeekMessageTO): Enum `WeekStatusKindTO { Unset, InPlanning, Planned, Locked }` mit `#[derive(Clone, Debug, Serialize, Deserialize, ToSchema, PartialEq)]` und `#[serde(rename_all = "snake_case")]`; Variante heisst Unset, nie None (D-39-03). Struct `WeekStatusTO { year: u32, calendar_week: u8, status: WeekStatusKindTO }` mit `#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]`. Bewusst schlank: KEIN id/created/deleted/version im TO — das FE sendet immer year+week+status, der Service haelt version intern (RESEARCH Abschnitt 1: kein id-basierter Endpunkt).

    Ergaenze unter `#[cfg(feature = "service-impl")]` die From-Impls analog WeekMessageTO: `From<&service::week_status::WeekStatus> for WeekStatusKindTO` und `From<&WeekStatusKindTO> for service::week_status::WeekStatus` (vollstaendiges 4-Varianten-Mapping inkl. Unset ↔ Unset). Optional ein `From<(u32,u8,service::week_status::WeekStatus)>`/Konstruktions-Helfer fuer WeekStatusTO, falls die Handler ihn brauchen — ansonsten Feld-Konstruktion im Handler.
  </action>
  <verify>
    <automated>cargo build -p rest-types --features service-impl</automated>
  </verify>
  <done>WeekStatusTO/WeekStatusKindTO (ToSchema) + From-Impls (inkl. Unset) kompilieren.</done>
  <acceptance_criteria>
    - `cargo build -p rest-types --features service-impl` gruen; WeekStatusTO + WeekStatusKindTO exportiert, beide mit ToSchema.
    - From-Impls mappen alle vier Varianten inkl. Unset ↔ Unset.
    - WeekStatusKindTO-Variante heisst Unset (kein None).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: REST-Handler + Router + ApiDoc-Registrierung</name>
  <files>rest/src/week_status.rs, rest/src/lib.rs</files>
  <read_first>
    - rest/src/week_message.rs (Router generate_route, error_handler, #[utoipa::path], WeekMessageApiDoc, by-year-and-week-Route)
    - rest/src/lib.rs (Zeile ~36 mod-Registrierung, ~350/~426 RestStateDef, ~539 ApiDoc-paths, ~624 Router-nest)
  </read_first>
  <action>
    Lege `rest/src/week_status.rs` an (Analog rest/src/week_message.rs). `generate_route::<RestState>()` mit zwei Routen auf DEMSELBEN Pfad `/by-year-and-week/{year}/{week}`: `get(get_week_status_by_year_and_week::<RestState>)` und `put(upsert_week_status::<RestState>)` (D-39-06: FE sendet immer year+week; kein id-Endpunkt). Handler `get_week_status_by_year_and_week`: liest `Path((year, week))`, ruft `rest_state.week_status_service().get_week_status(year, week, context.into(), None)`, verpackt in `WeekStatusTO { year, calendar_week: week, status: (&status).into() }`, via `error_handler`. Handler `upsert_week_status`: `Path((year, week))` + `Json(body): Json<WeekStatusTO>`, ruft `set_week_status(year, week, (&body.status).into(), context.into(), None)`, gibt das Resultat als WeekStatusTO zurueck. Beide mit `#[instrument(skip(rest_state))]` und `#[utoipa::path(...)]` (get bzw. put, tag "Week Status", request_body/responses inkl. 200 und 403 fuer put). `#[derive(OpenApi)] WeekStatusApiDoc` mit `paths(get_week_status_by_year_and_week, upsert_week_status)` und `components(schemas(WeekStatusTO, WeekStatusKindTO))`. Das Permission-Gate wird NICHT im Handler dupliziert — es liegt im Service (set_week_status), der bei Nicht-Schichtplaner Forbidden liefert und via error_handler zu 403 wird.

    In `rest/src/lib.rs`: `mod week_status;` ergaenzen; in `RestStateDef` den assoziierten Typ `type WeekStatusService: service::week_status::WeekStatusService<Context = Context, Transaction = Self::Transaction> + Send + Sync + 'static;` + Accessor `fn week_status_service(&self) -> Arc<Self::WeekStatusService>;`; in der zentralen ApiDoc-paths-Liste `(path = "/week-status", api = week_status::WeekStatusApiDoc)` ergaenzen (Pitfall P-3); im Router `.nest("/week-status", week_status::generate_route())` ergaenzen.
  </action>
  <verify>
    <automated>cargo build -p rest</automated>
  </verify>
  <done>rest baut; beide Handler mit #[utoipa::path]; WeekStatusApiDoc in ApiDoc; Router nestet /week-status.</done>
  <acceptance_criteria>
    - `cargo build -p rest` gruen; beide Handler tragen #[utoipa::path]; WeekStatusApiDoc ist in der zentralen ApiDoc-paths-Liste eingetragen.
    - Router nestet `/week-status`; GET+PUT liegen auf `/by-year-and-week/{year}/{week}`.
    - RestStateDef hat den WeekStatusService-Assoc-Type + Accessor.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: DI-Wiring in main.rs (Basic-Tier) + Workspace-Gates</name>
  <files>shifty_bin/src/main.rs</files>
  <read_first>
    - shifty_bin/src/main.rs (Zeile ~46 Typ-Alias WeekMessageDao; ~409-420 WeekMessageServiceDependencies; ~588 Feld in RestStateImpl; ~627 impl RestStateDef assoc type; ~705 Accessor; ~1034 Konstruktion)
    - shifty-backend/.planning/phases/39-kw-status-grundlage/39-RESEARCH.md (Abschnitt 5: exaktes DI-Wiring)
  </read_first>
  <action>
    Verdrahte den Service in `shifty_bin/src/main.rs` exakt neben week_message (Basic-Tier, vor Business-Logic — D-39-12): Typ-Alias `type WeekStatusDao = dao_impl_sqlite::week_status::WeekStatusDaoImpl;` (bei ~Zeile 46). `pub struct WeekStatusServiceDependencies;` + `impl service_impl::week_status::WeekStatusServiceDeps` (Context=Context, Transaction=Transaction, WeekStatusDao, PermissionService, ClockService, UuidService, TransactionDao) + `type WeekStatusService = service_impl::week_status::WeekStatusServiceImpl<WeekStatusServiceDependencies>;` (bei ~Zeile 420, direkt nach dem WeekMessage-Block). Feld `week_status_service: Arc<WeekStatusService>` in der RestStateImpl-Struct (bei ~588). In `impl rest::RestStateDef for RestStateImpl`: `type WeekStatusService = WeekStatusService;` (bei ~627) + Accessor `fn week_status_service(&self) -> Arc<Self::WeekStatusService> { self.week_status_service.clone() }` (bei ~705). Konstruktion in RestStateImpl::new(): `let week_status_service = Arc::new(WeekStatusService { week_status_dao: Arc::new(WeekStatusDao::new(pool.clone())), permission_service: permission_service.clone(), clock_service: clock_service.clone(), uuid_service: uuid_service.clone(), transaction_dao: transaction_dao.clone() });` direkt nach `week_message_service` (bei ~1034), und `week_status_service` in die Struct-Instanziierung (bei ~1136) aufnehmen.

    Fuehre danach die Workspace-Gates: `cargo build`, `cargo test --workspace`, `cargo clippy --workspace -- -D warnings`. Wenn seit Wave 1 weitere Queries kamen, erneut `cargo sqlx prepare --workspace` + `.sqlx` committen.
  </action>
  <verify>
    <automated>cargo build && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>DI-Wiring in Basic-Tier komplett; cargo build + cargo test --workspace + Clippy gruen.</done>
  <acceptance_criteria>
    - `cargo build` bindet den Server samt week_status-Route; `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` gruen.
    - week_status_service steht in der Basic-Service-Schicht (neben week_message_service), haengt an keinem Domain-Service.
    - RestStateImpl-Feld + Accessor + Assoc-Type sind konsistent verdrahtet.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → REST API | HTTP-Aufrufer sind ungetrusted; das Schreib-Gate muss server-seitig (Service) greifen, der Read-Pfad ist offen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | PUT /week-status (upsert_week_status) | high | mitigate | Handler delegiert an set_week_status; Service prueft SHIFTPLANNER_PRIVILEGE → error_handler mappt Forbidden auf HTTP 403. Kein separater ungegateter Schreibpfad. |
| T-39-02 | Tampering/Input Validation | Json(WeekStatusTO) | low | mitigate | WeekStatusKindTO ist geschlossene serde-Enum → unbekannte Werte werden als Deserialisierungsfehler abgewiesen. |
| T-39-03 | Information Disclosure | GET /week-status | low | accept | Status ist nicht sensitiv; alle Rollen lesen per Design. |
</threat_model>

<verification>
- `cargo build` + `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` gruen.
- WeekStatusApiDoc in zentraler ApiDoc; Router nestet `/week-status`.
- DI-Wiring in Basic-Tier verifiziert (neben week_message).
</verification>

<success_criteria>
- Der KW-Status ist ueber HTTP erreichbar (GET alle Rollen, PUT Schichtplaner-gated via Service). Wave 4 (FE-Foundation) kann `WeekStatusTO` importieren und die Endpunkte ansprechen.
</success_criteria>

<output>
Create `.planning/phases/39-kw-status-grundlage/39-03-SUMMARY.md` when done
</output>
