---
phase: 39-kw-status-grundlage
plan: 05
type: execute
wave: 5
depends_on: ["39-04"]
files_modified:
  - shifty-dioxus/src/component/atoms/week_status_badge.rs
  - shifty-dioxus/src/component/atoms/mod.rs
  - shifty-dioxus/src/component/week_status_dropdown.rs
  - shifty-dioxus/src/component/mod.rs
  - shifty-dioxus/src/page/shiftplan.rs
autonomous: true
requirements: [WST-02]
must_haves:
  truths:
    - "Alle Rollen sehen das Status-Badge im Wochen-Header, aber nur bei Status ungleich Unset; bei Unset wird fuer Nicht-Schichtplaner GAR NICHTS gerendert (kein leeres/graues Badge) — durch einen Pure-Fn-Sichtbarkeitstest belegt (D-39-05, WST-02)."
    - "Schichtplaner sehen an derselben Stelle ein Dropdown (kein controlled <select>) mit allen vier Optionen inkl. Unset zum Zuruecksetzen; nach Auswahl loest on_change eine Set-Mutation aus (D-39-06, D-39-07)."
    - "Farb-Semantik ist Locked=rot (bad), Planned=gruen (good), InPlanning=amber (warn), Unset=neutral (nur im Dropdown-Trigger, nie als Badge) — statische Tailwind-Token-Klassen pro match-Ast (D-39-08)."
    - "Nicht-Schichtplaner koennen den Status nicht aendern; das Dropdown erscheint nur unter is_shiftplanner (D-39-01, Anzeige-Seite)."
  artifacts:
    - "shifty-dioxus/src/component/atoms/week_status_badge.rs (WeekStatusBadge + week_status_badge_class-Helper + Pure-Fn-Test)"
    - "shifty-dioxus/src/component/week_status_dropdown.rs (WeekStatusDropdown, DropdownTrigger-basiert)"
    - "shifty-dioxus/src/page/shiftplan.rs (Status-Strip oberhalb WeekView, Load bei Init + Wochen-Navigation)"
  key_links:
    - "shiftplan.rs sendet WeekStatusAction::Load bei Init und in den NextWeek/PreviousWeek-Handlern (frischer Status pro KW)."
    - "on_change des Dropdowns sendet WeekStatusAction::Set { year, week, status } an die Store-Coroutine (Wave 4)."
    - "is_shiftplanner (shiftplan.rs ~103) gated das Dropdown; sonst nur Badge (bei != Unset)."
---

<objective>
Sichtbare Frontend-Schicht: das farbkodierte Status-Badge (alle Rollen), das Schichtplaner-Dropdown und die Integration in den Schichtplan-Wochen-Header. Schliesst die Phase-39-Vertikale ab — ab hier ist der KW-Status fuer alle Rollen sichtbar und fuer Schichtplaner setzbar.

Purpose: WST-02 verlangt Badge (alle Rollen) + Dropdown (nur Schichtplaner) mit korrekter Sichtbarkeits- und Farbsemantik; D-39-05/06/07/08 legen das genaue Verhalten fest.
Output: `WeekStatusBadge` (Atom + Pure-Fn-Klassenhelfer + Sichtbarkeitstest), `WeekStatusDropdown` (kein controlled select), Status-Strip in `shiftplan.rs` inkl. Laden pro KW.

Warum eigene Welle: Konsumiert `WEEK_STATUS_STORE`/`WeekStatusAction`/`WeekStatus`/i18n-Keys aus Wave 4 und rendert sie. Letzte Welle der Vertikale.

Scope-Guard: Das Locked-Badge ZEIGT nur — es sperrt in Phase 39 NICHTS. Die Sperr-Durchsetzung (assert_week_not_locked, HTTP 423, read-only) ist Phase 40 und darf hier NICHT gebaut werden.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-kw-status-grundlage/39-CONTEXT.md
@.planning/phases/39-kw-status-grundlage/39-UI-SPEC.md
@.planning/phases/39-kw-status-grundlage/39-PATTERNS.md
@.planning/phases/39-kw-status-grundlage/39-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WeekStatusBadge-Atom (Klassenhelfer + Sichtbarkeits-Pure-Fn-Test) + WeekStatusDropdown</name>
  <files>shifty-dioxus/src/component/atoms/week_status_badge.rs, shifty-dioxus/src/component/atoms/mod.rs, shifty-dioxus/src/component/week_status_dropdown.rs, shifty-dioxus/src/component/mod.rs</files>
  <read_first>
    - .planning/phases/39-kw-status-grundlage/39-UI-SPEC.md (Component Inventory: exakte Klassen-Strings pro Variante, Trigger-Klassen, Eintragsreihenfolge)
    - shifty-dioxus/src/component/atoms/person_chip.rs (Atom-Muster mit statischen match-Klassen)
    - shifty-dioxus/src/component/dropdown_base.rs (DropdownTrigger-Nutzung, DropdownEntry-Aufbau)
    - shifty-dioxus/src/page/employees.rs (Zeile ~18-43: no_legacy_classes_in_source-Testmuster)
  </read_first>
  <behavior>
    - week_status_badge_class(Locked) enthaelt bad-Token (bg-bad-soft/border-bad/text-bad); (Planned) good-Token; (InPlanning) warn-Token — je ein statischer Klassenstring.
    - Sichtbarkeit: should_show_badge(status=Unset) == false; should_show_badge(status=InPlanning|Planned|Locked) == true. (Ein Nicht-Schichtplaner sieht bei Unset nichts — D-39-05.)
    - no_legacy_classes: die Quelldatei enthaelt keine verbotenen Legacy-Tailwind-Klassen (Test analog employees.rs), nur Design-Token-Klassen.
  </behavior>
  <action>
    Schreibe ZUERST (RED) in `shifty-dioxus/src/component/atoms/week_status_badge.rs` einen `#[cfg(test)]`-Block mit: (a) einer Sichtbarkeits-Pure-Fn `pub(crate) fn should_show_badge(status: &WeekStatus) -> bool` (Unset → false, sonst true) und deren Test (WST-02, D-39-05); (b) dem Test fuer `week_status_badge_class`, der die korrekten Token pro Variante prueft; (c) dem `no_legacy_classes_in_source`-Test (Muster employees.rs). RED, solange die Funktionen fehlen.

    Implementiere DANN (GREEN) den Klassenhelfer `pub(crate) fn week_status_badge_class(status: &WeekStatus) -> &'static str` mit AUSSCHLIESSLICH statischen match-Armen und den exakten Klassenstrings aus der UI-SPEC (Locked=bg-bad-soft border border-bad text-bad, Planned=good, InPlanning=warn; Unset → `unreachable!()`, da das Badge nie fuer Unset gerendert wird). KEINE dynamischen Klassen via format!(). Die Komponente `WeekStatusBadge { status: WeekStatus }` rendert ein `span` mit `week_status_badge_class(status)` und dem i18n-Label (t(WeekStatusInPlanning/Planned/Locked)). Registriere `pub mod week_status_badge;` in `shifty-dioxus/src/component/atoms/mod.rs`.

    Lege `shifty-dioxus/src/component/week_status_dropdown.rs` an (UI-SPEC Component 2): `WeekStatusDropdown { current_status: WeekStatus, year: u32, week: u8, on_change: EventHandler<WeekStatus> }`, basierend auf dem bestehenden `DropdownTrigger` (KEIN controlled <select> — D-39-06). Trigger-Text/Klasse spiegeln den aktuellen Status (bei Unset neutraler Trigger `bg-surface-alt border-border text-ink-muted` mit Label "Kein"; sonst wie das Badge + `gap-1`). Vier Eintraege in Reihenfolge Kein → In Planung → Geplant → Gesperrt (aufsteigend nach Verbindlichkeit); jeder Eintrag ruft `on_change.call(WeekStatus::...)` — inkl. Unset zum Zuruecksetzen (D-39-07). `aria-label` = t(WeekStatusChangeAriaLabel). Registriere `pub mod week_status_dropdown;` in `shifty-dioxus/src/component/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus week_status_badge</automated>
  </verify>
  <done>Badge-Atom (Klassenhelfer + Sichtbarkeits-Pure-Fn + no_legacy) gruen; Dropdown ohne controlled select.</done>
  <acceptance_criteria>
    - `cargo test -p shifty-dioxus week_status_badge` gruen: should_show_badge(Unset)==false, sonst true; Klassen-Token korrekt; no_legacy_classes gruen.
    - WeekStatusDropdown nutzt DropdownTrigger (kein <select value=...>); vier Eintraege inkl. Unset; on_change verdrahtet.
    - Farbklassen sind statische match-Arme mit Design-Token (kein bg-gray/red/green/blue-Literal, kein format!()).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integration in shiftplan.rs (Status-Strip + Laden pro KW) + WASM-Gate</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <read_first>
    - .planning/phases/39-kw-status-grundlage/39-UI-SPEC.md (Placement Contract: Status-Strip div, Sichtbarkeitsmatrix, print:hidden)
    - shifty-dioxus/src/page/shiftplan.rs (Zeile ~103 is_shiftplanner; ~640 SaveWeekMessage-Handler; NextWeek/PreviousWeek-Handler; SlotEdit/WeekView-Platzierung)
    - shifty-dioxus/src/service/week_status.rs (WEEK_STATUS_STORE, WeekStatusAction aus Wave 4)
  </read_first>
  <action>
    Baue den Status-Strip in `shifty-dioxus/src/page/shiftplan.rs` gemaess UI-SPEC Placement Contract ein: ein `div { class: "mb-3 flex items-center gap-2 print:hidden" }` oberhalb der `WeekView`-Karte (im ViewMode::Week-Zweig, nach `SlotEdit`). Sichtbarkeitsmatrix (D-39-05): wenn `is_shiftplanner` → `WeekStatusDropdown { current_status, year: *year.read(), week: *week.read(), on_change: |s| week_status_service.send(WeekStatusAction::Set { year, week, status: s }) }`; sonst wenn `status != WeekStatus::Unset` → `WeekStatusBadge { status }`; sonst nichts. Der aktuelle Status kommt aus `WEEK_STATUS_STORE.read().status`.

    Verdrahte das Laden pro KW (D-39-06, frischer Status): sende `WeekStatusAction::Load { year, week }` (a) beim initialen Render mit year/week (im bestehenden use_effect/Refresh-Pfad) und (b) in den `ShiftPlanAction::NextWeek`/`PreviousWeek`-Handlern der Coroutine, nachdem year/week aktualisiert wurden. Nutze die bestehenden year/week-Signale des Schichtplans (dieselbe Quelle, die week_message verwendet) — keine eigene Datumsableitung im FE noetig; die (year, week)-Werte sind bereits ISO-korrekt (dieselbe Basis, auf der week_message funktioniert).

    Reiner Anzeige-/Setz-Scope: KEINE Sperr-Durchsetzung, kein read-only, kein 423 (Phase 40).

    Fuehre danach die FE-Gates: `cargo build --target wasm32-unknown-unknown` (aus shifty-dioxus) und `cargo test -p shifty-dioxus`.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo build --target wasm32-unknown-unknown && cargo test -p shifty-dioxus</automated>
  </verify>
  <done>Status-Strip integriert (Sichtbarkeitsmatrix, print:hidden, Load pro KW); WASM-Build + FE-Tests gruen; kein Sperr-Scope.</done>
  <acceptance_criteria>
    - `cargo build --target wasm32-unknown-unknown` + `cargo test -p shifty-dioxus` gruen.
    - Status-Strip steht oberhalb der WeekView (mb-3, print:hidden); Sichtbarkeitsmatrix erfuellt (Nicht-Schichtplaner bei Unset: kein Element; Schichtplaner: Dropdown; sonst Badge).
    - WeekStatusAction::Load wird bei Init und bei jedem Wochenwechsel gesendet; on_change des Dropdowns sendet Set.
    - Keine Sperr-/read-only-Logik eingefuehrt (Scope-Guard Phase 40 gewahrt).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-UI → Nutzer | Die Sichtbarkeits-Gates (Badge/Dropdown) sind UX, keine Sicherheitsgrenze; die Autorisierung liegt server-seitig (Wave 2/3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | Dropdown-Sichtbarkeit vs. Server-Gate | high | mitigate | Das Dropdown erscheint nur unter is_shiftplanner (UX); die echte Autorisierung erzwingt der Service (403 bei Nicht-Schichtplaner). FE-Gate ist Komfort, nicht Kontrolle. |
| T-39-05 | Spoofing/Consistency | Badge-Anzeige nach Mutation | low | mitigate | Badge liest aus WEEK_STATUS_STORE, der nach Mutation frisch nachgeladen wird (Wave 4, D-39-06) — keine driftende Statusanzeige. |
| T-39-03 | Information Disclosure | Badge fuer alle Rollen | low | accept | Status ist nicht sensitiv; Anzeige fuer alle Rollen ist beabsichtigt. |
</threat_model>

<verification>
- `cargo test -p shifty-dioxus week_status_badge` + `cargo test -p shifty-dioxus` gruen.
- `cargo build --target wasm32-unknown-unknown` (shifty-dioxus) gruen.
- Sichtbarkeitsmatrix + Farbsemantik gemaess UI-SPEC; kein Sperr-Scope.
</verification>

<success_criteria>
- Der KW-Status ist im Schichtplan fuer alle Rollen als farbkodiertes Badge sichtbar (nur bei != Unset) und fuer Schichtplaner ueber ein driftfreies Dropdown setzbar. Phase-39-Vertikale komplett; Phase 40 kann auf dem Locked-Status aufsetzen.
</success_criteria>

<output>
Create `.planning/phases/39-kw-status-grundlage/39-05-SUMMARY.md` when done
</output>
