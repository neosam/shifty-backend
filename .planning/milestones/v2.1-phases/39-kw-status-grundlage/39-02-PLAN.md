---
phase: 39-kw-status-grundlage
plan: 02
type: tdd
wave: 2
depends_on: ["39-01"]
files_modified:
  - service/src/week_status.rs
  - service/src/lib.rs
  - service_impl/src/week_status.rs
  - service_impl/src/lib.rs
  - service_impl/src/test/week_status.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [WST-01]
must_haves:
  truths:
    - "Nur Traeger von SHIFTPLANNER_PRIVILEGE koennen den Status setzen/aendern; alle anderen Rollen erhalten Forbidden, und bei Forbidden wird KEIN DAO-Schreibzugriff ausgeloest (D-39-01)."
    - "Alle Statusuebergaenge sind frei — jeder Status geht direkt in jeden anderen, inkl. Locked zurueck auf InPlanning oder Unset, ohne gesondertes Entsperr-Gate (D-39-02)."
    - "set_week_status(Unset) soft-deletet die vorhandene Zeile (Zeilen-Abwesenheit = Unset); get_week_status liefert Unset, wenn keine aktive Zeile existiert (D-39-04)."
    - "Das ISO-Jahr wird immer aus date.to_iso_week_date().0 abgeleitet, nie aus date.year(); KW-53-/Jahresgrenz-Tage landen in der richtigen (year, week)-Zeile — durch Pflicht-Unit-Tests belegt (D-39-11)."
    - "WeekStatusService ist Basic-Tier: Dependencies sind ausschliesslich WeekStatusDao, PermissionService, ClockService, UuidService, TransactionDao — kein Domain-Service (D-39-12, D-39-03 Enum Unset)."
  artifacts:
    - "service/src/week_status.rs (WeekStatus-Enum inkl. Unset, WeekStatusService-Trait)"
    - "service_impl/src/week_status.rs (WeekStatusServiceImpl via gen_service_impl!, WeekStatusServiceDeps)"
    - "service_impl/src/test/week_status.rs (mockall-Tests: iso_week, permission, upsert/soft-delete, transitions)"
  key_links:
    - "service/src/lib.rs registriert `pub mod week_status;` — sonst ist der Trait fuer rest/main.rs unsichtbar."
    - "gen_service_impl! erzeugt WeekStatusServiceImpl aus WeekStatusServiceDeps — Basic-Tier, keine Domain-Service-Dep (verhindert Zyklus mit Phase-40-Sperr-Gate)."
    - "check_permission(SHIFTPLANNER_PRIVILEGE) laeuft VOR jedem DAO-Schreibzugriff in set_week_status."
---

<objective>
Business-Logik-Schicht des KW-Status als Basic-Tier-Service (TDD). Der Service kapselt das Permission-Gate (nur Schichtplaner mutieren), die Upsert-/Soft-Delete-Semantik (Unset = Zeile loeschen) und die ISO-Wochen-Korrektheit. Er exponiert das Domain-Enum `WeekStatus` (inkl. Unset) statt der DAO-Entitaet.

Purpose: WST-01 verlangt, dass ein Schichtplaner Status setzt/aendert und der Wert korrekt pro ISO-(Jahr, Woche) landet. Diese Regeln sind pruefbare Business-Logik mit definiertem I/O → TDD (RED vor GREEN).
Output: `WeekStatus`-Enum, `WeekStatusService`-Trait, `WeekStatusServiceImpl` (Basic-Tier), vollstaendige mockall-Testsuite inkl. der 5 Pflicht-KW-53-Faelle.

Warum eigene Welle: Der Service konsumiert `WeekStatusDao`/`MockWeekStatusDao` aus Plan 39-01 (Wave 1) und muss vor REST/DI (Wave 3) fertig sein, weil die rest-types-From-Impls und die REST-Handler das `WeekStatus`-Enum referenzieren.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-kw-status-grundlage/39-CONTEXT.md
@.planning/phases/39-kw-status-grundlage/39-RESEARCH.md
@.planning/phases/39-kw-status-grundlage/39-PATTERNS.md
@.planning/phases/39-kw-status-grundlage/39-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Service-Trait/Enum + vollstaendige failing Testsuite</name>
  <files>service/src/week_status.rs, service/src/lib.rs, service_impl/src/week_status.rs, service_impl/src/test/week_status.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service/src/week_message.rs (Trait-Analog: Signaturen, Authentication<Context>, Result<_, ServiceError>)
    - service_impl/src/week_message.rs (gen_service_impl!-Block, SHIFTPLANNER_PRIVILEGE-Gate Zeile ~79, Transaktions-Wrapper Zeile ~37-40)
    - service_impl/src/test/sales_person_unavailable.rs (mockall-Test-Struct + build_service()-Helper, test_forbidden aus error_test)
    - service_impl/src/test/mod.rs (Test-Modul-Registrierung)
    - service_impl/src/test/error_test.rs (test_forbidden-Helper)
  </read_first>
  <behavior>
    - iso_week (5 Pflicht-Faelle, reine time-Tests): 2021-01-01 → (2020, 53); 2020-12-28 → (2020, 53); 2025-12-29 → (2026, 1); 2025-12-28 → (2025, 52); 2026-03-15 → (2026, 11). Beweis: to_iso_week_date().0 statt year().
    - permission_denied: MockPermissionService liefert Forbidden → set_week_status gibt Err(ServiceError::Forbidden) UND MockWeekStatusDao.create/update/delete werden NIE aufgerufen (kein expect → mockall failt bei Aufruf).
    - set_unset_soft_deletes: Vorhandene Zeile (find_by_year_and_week → Some) + status=Unset → dao.delete(existing.id) wird genau einmal aufgerufen, kein create/update.
    - set_unset_noop_when_absent: Keine Zeile (find → None) + status=Unset → weder delete noch create/update.
    - set_creates_when_absent: find → None + status=Locked → dao.create mit WeekStatusKind::Locked genau einmal.
    - set_updates_when_present: find → Some(InPlanning-Zeile) + status=Planned → dao.update mit WeekStatusKind::Planned genau einmal.
    - transitions_free: Sequenz InPlanning→Planned→Locked→Unset→InPlanning ist jeweils erlaubt (kein Uebergang wirft) (D-39-02).
    - get_returns_unset_when_absent: find → None → get_week_status liefert WeekStatus::Unset.
    - get_maps_kind: find → Some(Locked) → get_week_status liefert WeekStatus::Locked.
  </behavior>
  <action>
    Lege `service/src/week_status.rs` an (Analog service/src/week_message.rs): Domain-Enum `WeekStatus { Unset, InPlanning, Planned, Locked }` mit `#[derive(Clone, Debug, PartialEq)]` (Unset ist die vierte Variante, lebt nur im Service/FE, wird nie persistiert — D-39-04; Name Unset, nie None — D-39-03). `From<dao::week_status::WeekStatusKind> for WeekStatus` (InPlanning/Planned/Locked-Mapping; Unset entsteht nie aus dem DAO). Trait `WeekStatusService` mit `#[automock(type Context=(); type Transaction=MockTransaction;)]` + `#[async_trait]`, assoziierten Typen Context/Transaction (analog week_message) und zwei Methoden: `get_week_status(&self, year: u32, calendar_week: u8, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<WeekStatus, ServiceError>` (alle Rollen) und `set_week_status(&self, year: u32, calendar_week: u8, status: WeekStatus, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<WeekStatus, ServiceError>` (nur Schichtplaner). Registriere `pub mod week_status;` in `service/src/lib.rs`.

    Lege in `service_impl/src/week_status.rs` das gen_service_impl!-Skelett an (Analog week_message): `WeekStatusServiceImpl: WeekStatusService = WeekStatusServiceDeps` mit den Deps WeekStatusDao, PermissionService, ClockService, UuidService, TransactionDao (Basic-Tier, D-39-12) und der Prozess-Konstante `WEEK_STATUS_SERVICE_PROCESS`. In dieser RED-Phase duerfen die beiden Methodenrumpfe noch `todo!()` sein (nur damit der Trait/Struct kompiliert und die Tests referenzierbar sind).

    Lege `service_impl/src/test/week_status.rs` an (Analog sales_person_unavailable.rs): Test-Dependency-Struct mit den Mock-Typen (MockWeekStatusDao, MockPermissionService, MockClockService, MockUuidService, MockTransactionDao), `impl WeekStatusServiceDeps` (Context=()) und `build_service()`-Helper. Schreibe alle unter <behavior> gelisteten Tests. Die iso_week-Tests stehen in einem eigenen `mod iso_week`, das eine lokale Helper-Fn `iso_year_week(d: time::Date) -> (u32, u8)` definiert (= `let (y,w,_) = d.to_iso_week_date(); (y as u32, w)`) und die 5 Pflicht-Faelle via `time::macros::date!` prueft — rein, ohne Service/DAO. Registriere `pub mod week_status;` in `service_impl/src/test/mod.rs`.

    Am Ende dieser Task MUESSEN die Tests kompilieren und ROT sein (todo!()-Panics bzw. Assertion-Fehler).
  </action>
  <verify>
    <automated>cargo test -p service_impl week_status 2>&1 | grep -qE 'FAILED|panicked|test result: FAILED' && echo RED-OK</automated>
  </verify>
  <done>Trait/Enum/Testdatei kompilieren; Tests existieren und sind ROT; 5 KW-53-Faelle vorhanden.</done>
  <acceptance_criteria>
    - Trait/Enum/Testdatei kompilieren; `cargo test -p service_impl week_status` laeuft und ist ROT (Tests existieren, schlagen fehl).
    - Die 5 KW-53-Faelle sind exakt als Tests vorhanden (2021-01-01→(2020,53), 2020-12-28→(2020,53), 2025-12-29→(2026,1), 2025-12-28→(2025,52), 2026-03-15→(2026,11)).
    - `WeekStatus` hat vier Varianten inkl. Unset; die Deps-Liste enthaelt keinen Domain-Service.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Service-Logik implementieren bis gruen + Clippy</name>
  <files>service_impl/src/week_status.rs</files>
  <read_first>
    - service_impl/src/week_message.rs (Transaktions-Wrapper use_transaction/commit, check_permission, ClockService/UuidService-Nutzung)
    - service_impl/src/week_status.rs (das RED-Skelett aus Task 1)
  </read_first>
  <action>
    Implementiere `get_week_status`: `use_transaction(tx)` → `week_status_dao.find_by_year_and_week(year, calendar_week, tx.clone())` → `commit(tx)` → `Ok(existing.map(|e| e.status.into()).unwrap_or(WeekStatus::Unset))`. Kein Permission-Gate (alle Rollen lesen; Status ist nicht sensitiv).

    Implementiere `set_week_status`: ZUERST `permission_service.check_permission(SHIFTPLANNER_PRIVILEGE, context).await?` (VOR jedem DAO-Zugriff — D-39-01, T-39-01). Dann `use_transaction(tx)` und `find_by_year_and_week` in DERSELBEN Transaktion (kein TOCTOU — T-39-04). Fallunterscheidung: bei `status == WeekStatus::Unset` → wenn eine Zeile existiert, `dao.delete(existing.id, WEEK_STATUS_SERVICE_PROCESS, tx.clone())` (Soft-Delete, D-39-04), sonst No-Op. Bei InPlanning/Planned/Locked → konvertiere zu `WeekStatusKind` (explizite Hilfsfunktion oder match; Unset ist hier durch den ersten Zweig ausgeschlossen); wenn Zeile existiert → `dao.update(...)` mit neuer `version` (UuidService), sonst `dao.create(...)` mit neuer `id` + `version` (UuidService) und `created` = ClockService-now. Danach `commit(tx)` und `Ok(status)` zurueckgeben (Echo des gesetzten Status; alle Uebergaenge sind frei, D-39-02 — keine Uebergangs-Validierung).

    Ersetze die todo!()-Rumpfe, bis alle Tests aus Task 1 GRUEN sind. Achte auf Clippy: keine None-Variante im Scope, keine ungenutzten Imports/Deps.
  </action>
  <verify>
    <automated>cargo test -p service_impl week_status && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>Alle Service-Tests GRUEN; Permission-Gate vor DAO-Write; Transaktions-Klammer; Clippy gruen.</done>
  <acceptance_criteria>
    - Alle Tests aus Task 1 sind GRUEN (permission, soft-delete/upsert, transitions, get-mapping, iso_week).
    - `cargo clippy --workspace -- -D warnings` gruen.
    - `check_permission(SHIFTPLANNER_PRIVILEGE)` steht nachweislich vor jedem DAO-Schreibpfad in set_week_status; get_week_status hat kein Gate.
    - find + create/update laufen in einer Transaktion (use_transaction/commit-Klammer).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aufrufer → Service | `Authentication<Context>` traegt die Rolle; Schreibzugriff muss server-seitig gegated sein (Anzeige-Gating im FE ist keine Sicherheitsgrenze). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Elevation of Privilege | `set_week_status` | high | mitigate | `check_permission(SHIFTPLANNER_PRIVILEGE)` als erste Anweisung, vor jedem DAO-Write; durch `permission_denied`-Test belegt (Nicht-Schichtplaner → Forbidden, kein DAO-Aufruf). |
| T-39-04 | Tampering (TOCTOU) | Upsert find-then-write | low | mitigate | `find_by_year_and_week` + create/update in einer Transaktion (use_transaction/commit). |
| T-39-03 | Information Disclosure | `get_week_status` | low | accept | Status ist nicht sensitiv; alle Rollen lesen per Design (kein Gate). |
</threat_model>

<verification>
- `cargo test -p service_impl week_status` gruen inkl. der 5 KW-53-Faelle.
- `cargo clippy --workspace -- -D warnings` gruen.
- Permission-Gate + Transaktions-Klammer strukturell vorhanden.
</verification>

<success_criteria>
- Basic-Tier `WeekStatusService` liefert das Domain-Enum `WeekStatus` mit korrekter Upsert-/Soft-Delete-/Permission-Semantik und ISO-Wochen-Korrektheit. Wave 3 (REST/DI) kann Trait + Enum konsumieren.
</success_criteria>

<output>
Create `.planning/phases/39-kw-status-grundlage/39-02-SUMMARY.md` when done
</output>
