---
phase: 39-kw-status-grundlage
plan: 04
type: execute
wave: 4
depends_on: ["39-03"]
files_modified:
  - shifty-dioxus/src/state/week_status.rs
  - shifty-dioxus/src/state/mod.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/service/week_status.rs
  - shifty-dioxus/src/service/mod.rs
  - shifty-dioxus/src/app.rs
  - shifty-dioxus/Dioxus.toml
autonomous: true
requirements: [WST-02, WST-05]
must_haves:
  truths:
    - "Alle vier Status-Labels sind in de/en/cs vorhanden (WeekStatusUnset/InPlanning/Planned/Locked) plus Fehler- und Aria-Label-Keys; ein i18n-Vollstaendigkeitstest belegt 4 Varianten × 3 Locales (D-39-09, WST-05)."
    - "Der FE-Status-Store laedt nach jeder Mutation frisch vom Server nach (Set → serverseitiges PUT → anschliessendes GET), kein optimistisches Signal, das driften kann (D-39-06)."
    - "Die FE-Domain-Enum WeekStatus hat vier Varianten mit Default=Unset; keine Zeile/kein Server-Wert bedeutet Unset (D-39-04, D-39-03 Name Unset)."
  artifacts:
    - "shifty-dioxus/src/state/week_status.rs (WeekStatus-Enum + From<&WeekStatusTO>)"
    - "shifty-dioxus/src/service/week_status.rs (WEEK_STATUS_STORE, WeekStatusAction, week_status_service-Coroutine)"
    - "shifty-dioxus/src/api.rs (get_week_status, set_week_status)"
    - "shifty-dioxus/src/i18n/{mod,de,en,cs}.rs (WeekStatus*-Keys)"
    - "shifty-dioxus/Dioxus.toml (Proxy-Eintrag /week-status)"
  key_links:
    - "app.rs registriert `use_coroutine(service::week_status::week_status_service)` — sonst laeuft der Store nie."
    - "Dioxus.toml `[[web.proxy]] backend = \"http://localhost:3000/week-status\"` — sonst 405/404 im Dev-Proxy (Praezedenz Phase 28)."
    - "api::set_week_status ruft PUT /week-status/by-year-and-week/{year}/{week}; api::get_week_status ruft GET auf demselben Pfad."
---

<objective>
Frontend-Fundament fuer den KW-Status: die reine Zustands-/Daten-Schicht ohne UI-Rendering — Domain-Enum, i18n-Labels, API-Client-Funktionen und die Store-Coroutine mit Fresh-Fetch-Fluss. Trennt bewusst die zustandslose Plumbing-Schicht (dieser Plan) von den sichtbaren Komponenten + Schichtplan-Integration (Plan 39-05).

Purpose: WST-05 verlangt i18n de/en/cs aller vier Labels; WST-02 verlangt Anzeige/Bearbeitung — beides braucht zuerst Enum, Übersetzungen, API-Zugriff und einen Store, der nach Mutation frisch nachlaedt (D-39-06).
Output: `state::week_status::WeekStatus`, i18n-Keys, `api::get_week_status`/`set_week_status`, `service::week_status`-Coroutine + `WEEK_STATUS_STORE`, App-Registrierung, Dev-Proxy.

Warum eigene Welle: Konsumiert `WeekStatusTO`/`WeekStatusKindTO` aus rest-types (Wave 3) und die REST-Endpunkte. Die sichtbaren Komponenten (39-05) haengen wiederum an diesem Store + Enum.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-kw-status-grundlage/39-CONTEXT.md
@.planning/phases/39-kw-status-grundlage/39-UI-SPEC.md
@.planning/phases/39-kw-status-grundlage/39-PATTERNS.md
@.planning/phases/39-kw-status-grundlage/39-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FE-State-Enum + i18n-Keys (de/en/cs) + Vollstaendigkeitstest</name>
  <files>shifty-dioxus/src/state/week_status.rs, shifty-dioxus/src/state/mod.rs, shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (Key-Enum, WeekMessage-Key bei ~Zeile 84, fn t)
    - shifty-dioxus/src/i18n/de.rs (Translation-Match-Muster; en.rs/cs.rs analog)
    - shifty-dioxus/src/state/mod.rs (Re-Export-Muster)
    - rest-types WeekStatusTO/WeekStatusKindTO (aus Wave 3)
  </read_first>
  <action>
    Lege `shifty-dioxus/src/state/week_status.rs` an: `#[derive(Clone, PartialEq, Debug, Default)] pub enum WeekStatus { #[default] Unset, InPlanning, Planned, Locked }` (Default=Unset — D-39-04; Name Unset — D-39-03). `From<&rest_types::WeekStatusKindTO> for WeekStatus` und `From<&rest_types::WeekStatusTO> for WeekStatus` (Status-Feld mappen). Re-Export in `shifty-dioxus/src/state/mod.rs` (`pub mod week_status;`).

    Ergaenze die i18n-Keys im `Key`-Enum in `shifty-dioxus/src/i18n/mod.rs`: `WeekStatusUnset`, `WeekStatusInPlanning`, `WeekStatusPlanned`, `WeekStatusLocked`, `WeekStatusSetError`, `WeekStatusChangeAriaLabel`. Trage alle in de.rs, en.rs, cs.rs ein (D-39-09 / UI-SPEC Copywriting Contract): de = Kein / In Planung / Geplant / Gesperrt / "Status konnte nicht gespeichert werden." / "Wochenstatus aendern"; en = None / In planning / Planned / Locked / "Failed to save week status." / "Change week status"; cs = Zadny / V planovani / Naplanovano / Uzamceno / "Nepodarilo se ulozit stav tydne." / "Zmenit stav tydne". (Diakritik in cs wie im bestehenden cs.rs-Stil.)

    Schreibe/ergaenze einen i18n-Vollstaendigkeitstest (Analog vorhandener i18n-Tests, sonst neuer `#[cfg(test)]`-Block in i18n/mod.rs): fuer jede der vier WeekStatus-Varianten-Keys liefert `t(Locale::De/En/Cs, key)` einen nicht-leeren, nicht-"??"-String — beweist 4 Varianten × 3 Locales (WST-05).
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus i18n</automated>
  </verify>
  <done>WeekStatus-Enum (Default Unset) + i18n 4×3 vollstaendig; Vollstaendigkeitstest gruen.</done>
  <acceptance_criteria>
    - `cargo test -p shifty-dioxus i18n` gruen; der Vollstaendigkeitstest deckt alle vier Labels in de/en/cs ab.
    - `WeekStatus` (FE) hat vier Varianten, Default=Unset.
    - Kein i18n-Key liefert "??" fuer eine der drei Locales.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: API-Client + Store-Coroutine (Fresh-Fetch) + App/Proxy-Registrierung</name>
  <files>shifty-dioxus/src/api.rs, shifty-dioxus/src/service/week_status.rs, shifty-dioxus/src/service/mod.rs, shifty-dioxus/src/app.rs, shifty-dioxus/Dioxus.toml</files>
  <read_first>
    - shifty-dioxus/src/api.rs (Zeile ~1152-1202: get/post/put_week_message als API-Client-Analog)
    - shifty-dioxus/src/service/weekly_summary.rs (Coroutine-Struktur, ERROR_STORE/error-Handling, GlobalSignal-Store)
    - shifty-dioxus/src/app.rs (use_coroutine-Registrierungen Zeile ~14-23)
    - shifty-dioxus/Dioxus.toml (web.proxy-Eintraege Zeile ~45-54)
    - .planning/phases/39-kw-status-grundlage/39-UI-SPEC.md (Abschnitt 3: Store/Action/Fresh-Fetch-Fluss)
  </read_first>
  <action>
    Ergaenze in `shifty-dioxus/src/api.rs` (Analog week_message): `pub async fn get_week_status(config: Config, year: u32, week: u8) -> Result<Option<WeekStatusTO>, reqwest::Error>` (GET `/week-status/by-year-and-week/{year}/{week}`; 404 → Ok(None)) und `pub async fn set_week_status(config: Config, year: u32, week: u8, status: WeekStatus) -> Result<(), reqwest::Error>` (PUT auf denselben Pfad mit Body `WeekStatusTO { year, calendar_week: week, status: (&status).into() }`; error_for_status_ref).

    Lege `shifty-dioxus/src/service/week_status.rs` an (UI-SPEC Abschnitt 3): `pub static WEEK_STATUS_STORE: GlobalSignal<WeekStatusStore>` mit `WeekStatusStore { status: WeekStatus (Default Unset), loaded_year: Option<u32>, loaded_week: Option<u8> }`; `pub enum WeekStatusAction { Load { year, week }, Set { year, week, status: WeekStatus } }`; `pub async fn week_status_service(mut rx: UnboundedReceiver<WeekStatusAction>)`-Coroutine. Fluss (D-39-06, KEIN optimistisches Update): Bei `Set` → `api::set_week_status(...)`.await; bei Ok → selbst `Load { year, week }` ausloesen (Fresh-Fetch); bei Err → error_handler/ERROR_STORE mit Key `WeekStatusSetError`, Store bleibt unveraendert. Bei `Load` → `api::get_week_status(...)`.await; bei Ok → WEEK_STATUS_STORE.write() status/loaded_year/loaded_week setzen (None → WeekStatus::Unset); bei Err → error_handler, Store behaelt letzten Wert.

    Registriere `pub mod week_status;` in `shifty-dioxus/src/service/mod.rs` und `use_coroutine(service::week_status::week_status_service);` in `shifty-dioxus/src/app.rs`. Ergaenze in `shifty-dioxus/Dioxus.toml` einen `[[web.proxy]] backend = "http://localhost:3000/week-status"`-Eintrag (sonst Dev-Proxy-405, Praezedenz Phase 28).
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <done>API-Client + Fresh-Fetch-Store + Coroutine registriert + Dev-Proxy; WASM-Build gruen.</done>
  <acceptance_criteria>
    - `cargo build --target wasm32-unknown-unknown` (aus shifty-dioxus) gruen.
    - Der Coroutine-Set-Zweig loest nach erfolgreichem PUT ein GET aus (Fresh-Fetch); es gibt kein optimistisches Store-Update vor dem Server-Roundtrip.
    - `use_coroutine(service::week_status::week_status_service)` ist in app.rs registriert; Dioxus.toml hat den /week-status-Proxy.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (FE) → REST API | Das FE ist keine Sicherheitsgrenze; das Schreib-Gate ist server-seitig (Wave 2/3). Der Store/Proxy transportiert nur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-05 | Spoofing/Consistency | Optimistisches FE-Signal | low | mitigate | Kein optimistisches Update; Fresh-Fetch nach Mutation (D-39-06) verhindert driftende Anzeige, die einen falschen Status vortaeuscht. |
| T-39-01 | Elevation of Privilege | FE-Aufruf PUT /week-status | high | mitigate | FE gated die Sichtbarkeit spaeter (39-05), aber die Autorisierung liegt server-seitig — ein direkter PUT eines Nicht-Schichtplaners wird vom Service mit 403 abgewiesen. |
</threat_model>

<verification>
- `cargo test -p shifty-dioxus i18n` gruen (4×3 i18n-Vollstaendigkeit).
- `cargo build --target wasm32-unknown-unknown` (shifty-dioxus) gruen.
- Store/Coroutine registriert; Dev-Proxy gesetzt.
</verification>

<success_criteria>
- FE-Zustands-/Daten-Schicht steht: Enum, Übersetzungen, API-Client, Fresh-Fetch-Store. Plan 39-05 kann Badge + Dropdown darauf aufsetzen.
</success_criteria>

<output>
Create `.planning/phases/39-kw-status-grundlage/39-04-SUMMARY.md` when done
</output>
