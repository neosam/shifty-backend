---
phase: 39-kw-status-grundlage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260702000000_create-week-status.sql
  - dao/src/week_status.rs
  - dao/src/lib.rs
  - dao_impl_sqlite/src/week_status.rs
  - dao_impl_sqlite/src/lib.rs
  - .sqlx/
autonomous: true
requirements: [WST-01]
must_haves:
  truths:
    - "Die Migration legt eine week_status-Tabelle mit ISO-(year, calendar_week)-Composite + TEXT-Diskriminant + partial UNIQUE WHERE deleted IS NULL an (D-39-10)."
    - "Nur InPlanning/Planned/Locked bekommen je eine DB-Zeile; die Leer-Variante Unset wird NIE als Diskriminant persistiert — Zeilen-Abwesenheit bedeutet Unset (D-39-04)."
    - "Die persistierte Diskriminant-Variante heisst im DAO-Enum WeekStatusKind (InPlanning/Planned/Locked), nie None — kein Option-Shadowing (D-39-03)."
    - "Ein unbekannter DB-TEXT-Wert im status-Feld liefert DaoError::EnumValueNotFound statt Panic/stiller Fehlinterpretation."
  artifacts:
    - "migrations/sqlite/20260702000000_create-week-status.sql"
    - "dao/src/week_status.rs (WeekStatusEntity, WeekStatusKind, WeekStatusDao trait)"
    - "dao_impl_sqlite/src/week_status.rs (WeekStatusDaoImpl, WeekStatusDb, TryFrom)"
    - ".sqlx/ (offline query cache fuer die neuen week_status-Queries)"
  key_links:
    - "dao/src/lib.rs registriert `pub mod week_status;` — sonst ist der Trait workspace-weit unsichtbar."
    - "dao_impl_sqlite/src/lib.rs registriert `pub mod week_status;` — sonst kann main.rs den Impl-Typ nicht aliasen."
    - "partial UNIQUE idx_week_status_active garantiert genau eine aktive Zeile pro (year, calendar_week)."
---

<objective>
Persistenz-Fundament fuer den KW-Status: neue `week_status`-Tabelle (Migration) + DAO-Trait/Entity + SQLite-DAO-Impl mit TEXT-Enum-Diskriminant. Dies ist die unterste Schicht der Phase-39-Vertikale (Migration → DAO → Service → REST → FE); alle folgenden Wellen konsumieren die hier exportierten Symbole.

Purpose: Ohne persistierten Status pro ISO-(Jahr, Woche) gibt es keinen Zustand, den Service/REST/FE anzeigen oder aendern koennen (WST-01).
Output: Migration, `WeekStatusEntity` + `WeekStatusKind` (3 persistierte Varianten) + `WeekStatusDao`-Trait, `WeekStatusDaoImpl` mit `TryFrom`-Diskriminant, regenerierter `.sqlx`-Offline-Cache.

## Artifacts diese Phase produziert (Gesamt-Uebersicht, hier Schicht 1)
- Migration `week_status`-Tabelle (dieser Plan)
- DAO: `WeekStatusEntity`, `WeekStatusKind {InPlanning, Planned, Locked}`, `WeekStatusDao`, `WeekStatusDaoImpl`, `WeekStatusDb` (dieser Plan)
- Service (39-02): `WeekStatus {Unset, InPlanning, Planned, Locked}`, `WeekStatusService`, `WeekStatusServiceImpl`, `WeekStatusServiceDeps`
- rest-types (39-03): `WeekStatusTO`, `WeekStatusKindTO`
- REST (39-03): `week_status::generate_route`, `WeekStatusApiDoc`, Handler `get_week_status_by_year_and_week`/`upsert_week_status`
- FE (39-04/05): `state::week_status::WeekStatus`, `service::week_status` (Store/Action/Coroutine), `WeekStatusBadge`, `WeekStatusDropdown`, i18n-Keys `WeekStatus*`
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-kw-status-grundlage/39-CONTEXT.md
@.planning/phases/39-kw-status-grundlage/39-RESEARCH.md
@.planning/phases/39-kw-status-grundlage/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + DAO-Trait/Entity im dao-Crate</name>
  <files>migrations/sqlite/20260702000000_create-week-status.sql, dao/src/week_status.rs, dao/src/lib.rs</files>
  <read_first>
    - migrations/sqlite/20260629000000_create-vacation-entitlement-offset.sql (Migrations-Template: soft-delete + partial UNIQUE)
    - dao/src/week_message.rs (Trait/Entity-Analog, 1:1 Struktur)
    - dao/src/lib.rs (mod-Registrierung, alphabetische Einordnung)
  </read_first>
  <action>
    Lege die Migration `migrations/sqlite/20260702000000_create-week-status.sql` nach dem vacation-entitlement-offset-Muster an (D-39-10): Tabelle `week_status` mit Spalten `id BLOB NOT NULL PRIMARY KEY`, `year INTEGER NOT NULL`, `calendar_week INTEGER NOT NULL`, `status TEXT NOT NULL` (nur die Werte InPlanning/Planned/Locked werden je geschrieben), `created TEXT NOT NULL`, `deleted TEXT`, `update_process TEXT NOT NULL`, `update_version BLOB NOT NULL`. Plus partial UNIQUE Index `idx_week_status_active ON week_status (year, calendar_week) WHERE deleted IS NULL`. KEIN FOREIGN KEY, KEINE sales_person_id. Bewusst NICHT das week_message-Migrations-Muster kopieren (dort UNIQUE ohne WHERE — erlaubt keine Soft-Delete-History; siehe RESEARCH Pitfall P-6).

    Lege `dao/src/week_status.rs` analog `dao/src/week_message.rs` an: `WeekStatusEntity { id: Uuid, year: u32, calendar_week: u8, status: WeekStatusKind, created: time::PrimitiveDateTime, deleted: Option<time::PrimitiveDateTime>, version: Uuid }`; das Enum `WeekStatusKind { InPlanning, Planned, Locked }` mit `#[derive(Clone, Debug, PartialEq)]` — bewusst OHNE Unset-Variante, weil Unset auf DAO-Ebene = Zeilen-Abwesenheit ist (D-39-04) und die Variante NICHT None heisst (D-39-03). Trait `WeekStatusDao` mit `#[automock(type Transaction = crate::MockTransaction;)]` + `#[async_trait::async_trait]` und exakt diesen Methoden (analog week_message, aber nur der benoetigte Ausschnitt): `find_by_year_and_week(&self, year: u32, calendar_week: u8, tx) -> Result<Option<WeekStatusEntity>, DaoError>`, `create(&self, entity: &WeekStatusEntity, process: &str, tx) -> Result<(), DaoError>`, `update(&self, entity: &WeekStatusEntity, process: &str, tx) -> Result<(), DaoError>`, `delete(&self, id: Uuid, process: &str, tx) -> Result<(), DaoError>`.

    Registriere `pub mod week_status;` in `dao/src/lib.rs` an alphabetisch passender Stelle (nach `pub mod special_day;`/vor `week_message`-naher Position — halte die bestehende Ordnung ein).
  </action>
  <verify>
    <automated>cargo build -p dao</automated>
  </verify>
  <done>Migration + DAO-Trait/Entity kompilieren; WeekStatusKind hat genau InPlanning/Planned/Locked; mod registriert.</done>
  <acceptance_criteria>
    - `cargo build -p dao` kompiliert; `WeekStatusEntity`, `WeekStatusKind`, `WeekStatusDao`, `MockWeekStatusDao` sind aus dem dao-Crate importierbar.
    - Migrationsdatei existiert mit partial UNIQUE `WHERE deleted IS NULL`; das status-Feld ist TEXT NOT NULL; kein FOREIGN KEY.
    - `WeekStatusKind` hat genau die drei Varianten InPlanning/Planned/Locked (keine Unset-, keine None-Variante).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite-DAO-Impl mit TEXT-Diskriminant + sqlx-prepare</name>
  <files>dao_impl_sqlite/src/week_status.rs, dao_impl_sqlite/src/lib.rs, .sqlx/</files>
  <read_first>
    - dao_impl_sqlite/src/week_message.rs (Gesamtstruktur: WeekMessageDb, TryFrom, DaoImpl, CRUD-Queries, Soft-Delete)
    - dao_impl_sqlite/src/special_day.rs (Zeilen ~36-39: TEXT-Enum-Diskriminant via manuellem match im TryFrom)
    - dao_impl_sqlite/src/lib.rs (mod-Registrierung)
  </read_first>
  <behavior>
    - Test unknown_discriminant: Ein `WeekStatusDb` mit `status = "Bogus"` ergibt via `TryFrom<&WeekStatusDb> for WeekStatusEntity` einen `Err(DaoError::EnumValueNotFound("Bogus"))`.
    - Test roundtrip_discriminant: `status = "Locked"` mappt auf `WeekStatusKind::Locked`, `"Planned"` auf `Planned`, `"InPlanning"` auf `InPlanning`.
  </behavior>
  <action>
    Schreibe ZUERST (RED) im `#[cfg(test)]`-Modul von `dao_impl_sqlite/src/week_status.rs` die Unit-Tests `unknown_discriminant` und `roundtrip_discriminant`, die nur die `TryFrom`-Mapping-Logik pruefen (kein DB-Zugriff — konstruiere `WeekStatusDb`-Instanzen direkt). Diese Tests scheitern, solange `TryFrom` noch nicht existiert.

    Implementiere DANN (GREEN) `dao_impl_sqlite/src/week_status.rs` analog `dao_impl_sqlite/src/week_message.rs`: private Row-Struct `WeekStatusDb { id: Vec<u8>, year: i64, calendar_week: i64, status: String, created: String, deleted: Option<String>, update_version: Vec<u8> }`; `impl TryFrom<&WeekStatusDb> for WeekStatusEntity` mit dem manuellen match-Ast nach special_day-Muster: "InPlanning" => WeekStatusKind::InPlanning, "Planned" => Planned, "Locked" => Locked, sonst `return Err(DaoError::EnumValueNotFound(value.into()))`. `WeekStatusDaoImpl { pool }` mit `new(pool)`. CRUD via `query_as!`/`query!`: `find_by_year_and_week` selektiert die aktive Zeile (`WHERE year = ? AND calendar_week = ? AND deleted IS NULL`); `create` INSERTet id/year/calendar_week/status/created/update_process/update_version, wobei die Enum→&str-Serialisierung ein explizites match ist (InPlanning=>"InPlanning", Planned=>"Planned", Locked=>"Locked") — KEIN `.to_string()`; `update` setzt status + update_version + update_process fuer die id; `delete` ist Soft-Delete (`UPDATE week_status SET deleted = ?, update_process = ? WHERE id = ?` mit ISO8601-now, analog week_message.rs). Es wird NIE ein Unset-Diskriminant serialisiert (D-39-04) — `WeekStatusKind` hat gar keine Unset-Variante, daher ist das strukturell garantiert.

    Registriere `pub mod week_status;` in `dao_impl_sqlite/src/lib.rs`.

    Fuehre danach in `nix develop` (NICHT nix-shell) `cargo sqlx prepare --workspace` aus und committe das aktualisierte `.sqlx/`-Verzeichnis mit (Pflicht-Gate: CI laeuft SQLX_OFFLINE=true — siehe RESEARCH Pitfall P-4).
  </action>
  <verify>
    <automated>cargo test -p dao_impl_sqlite week_status</automated>
  </verify>
  <done>DAO-Impl gruen inkl. unknown_discriminant-Test; .sqlx regeneriert+committet; Clippy gruen.</done>
  <acceptance_criteria>
    - `cargo test -p dao_impl_sqlite week_status` gruen; `unknown_discriminant` beweist `DaoError::EnumValueNotFound` fuer unbekannte TEXT-Werte.
    - `cargo clippy --workspace -- -D warnings` gruen (kein None-Shadowing, keine ungenutzten Imports).
    - `.sqlx/` enthaelt die neuen week_status-Query-Caches und ist committet (kein `error: failed to find data for query` in einem Clean-Build).
    - Enum→&str-Serialisierung ist ein explizites match; kein Unset-Zweig existiert.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite ↔ DAO | Persistierte TEXT-Diskriminanten sind ungetrusteter Input beim Deserialisieren (koennten durch Fremd-Migrationen/Alt-Daten unbekannt sein). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-02 | Tampering/Input Validation | `TryFrom<&WeekStatusDb>` | low | mitigate | Geschlossenes match; unbekannter Diskriminant → `DaoError::EnumValueNotFound` (durch `unknown_discriminant`-Test belegt) statt stiller Fehlinterpretation. |
| T-39-SC | Tampering | cargo installs | low | accept | Keine neuen Dependencies in dieser Phase (RESEARCH: keine neuen Deps) — reiner interner Copy-Vorlage-Ansatz. |
</threat_model>

<verification>
- `cargo build -p dao` + `cargo test -p dao_impl_sqlite week_status` gruen.
- `cargo clippy --workspace -- -D warnings` gruen.
- `.sqlx/` regeneriert + committet.
</verification>

<success_criteria>
- Migration + DAO-Schicht existieren, workspace-weit importierbar, Diskriminant-Fehlerpfad getestet, Offline-Cache aktuell. Wave 2 (Service) kann `WeekStatusDao`/`MockWeekStatusDao` konsumieren.
</success_criteria>

<output>
Create `.planning/phases/39-kw-status-grundlage/39-01-SUMMARY.md` when done
</output>
