---
phase: 41-avg-anwesenheit-flexible
plan: 04
type: execute
wave: 4
depends_on: ["41-03"]
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/service/employee.rs
  - shifty-dioxus/src/component/employee_view.rs
autonomous: true
requirements: [AVG-02, AVG-03]
must_haves:
  truths:
    - "D-AVG-07: die neue Zahl erscheint im bestehenden HR-Statistik-Block (should_show_hr_stats) DIREKT nach 'Ø Std/Woche' und vor 'Einbezogene Wochen' in employee_view.rs"
    - "D-AVG-06: bei average_hours_per_attendance_day == None (< 2 Anwesenheitstage) wird EN-DASH '–' (text-ink-muted) mit title=AvgHoursPerAttendanceDayEmpty gezeigt statt einer Zahl"
    - "D-AVG-05: attendance_statistics ist zwei-stufig Option — bei None (nicht-flexibler MA oder Nicht-HR) wird die TupleRow gar nicht gerendert"
    - "D-AVG-09: Label, Beschreibung und Leerzustand der Kennzahl liegen in de/en/cs vor (i18n-Completeness-Test grün)"
  artifacts:
    - "shifty-dioxus/src/i18n: Key::AvgHoursPerAttendanceDay / *Description / *Empty in mod.rs + Übersetzungen in de/en/cs + Test i18n_attendance_keys_present_in_all_locales"
    - "shifty-dioxus/src/api.rs :: get_employee_attendance_statistics(config, id, year, until_week) -> Result<Option<Rc<EmployeeAttendanceStatisticsTO>>, reqwest::Error>"
    - "shifty-dioxus/src/service/employee.rs :: EmployeeStore.attendance_statistics + Laden in load_employee_data"
    - "shifty-dioxus/src/component/employee_view.rs :: attendance_statistics-Prop + TupleRow-Rendering im HR-Stats-Block"
  key_links:
    - "load_employee_data ruft get_employee_attendance_statistics(year, until_week) und setzt EmployeeStore.attendance_statistics IMMER neu (auch None), damit alter State geleert wird; employee_view rendert die Row nur bei Some"
---

<objective>
Frontend-Vertikal-Slice: i18n-Keys (de/en/cs) + FE-API-Loader + EmployeeStore-Feld + Rendering
der Ø-Anwesenheits-Kennzahl im bestehenden HR-Statistik-Block von `employee_view.rs`.

Purpose: AVG-02 (im Report sichtbar) + AVG-03 (i18n) abschließen — die neue Zahl steht neben
"Ø Std/Woche", zeigt Leerzustand "–" bei < 2 Anwesenheitstagen und erscheint gar nicht für
nicht-flexible MA (server-seitiger Filter liefert None).
Output: 3 i18n-Keys × 3 Locales + Completeness-Test, api-Loader, Store-Feld, TupleRow-Rendering.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-avg-anwesenheit-flexible/41-CONTEXT.md
@.planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md
@.planning/phases/41-avg-anwesenheit-flexible/41-UI-SPEC.md
@.planning/phases/41-avg-anwesenheit-flexible/41-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 4) delivers the FE-facing artifacts: i18n keys (de/en/cs), the api loader,
the `EmployeeStore.attendance_statistics` field + loader wiring, and the `TupleRow` rendering
in `employee_view.rs`. Consumes `EmployeeAttendanceStatisticsTO` from 41-03 (rest-types) and
the endpoint from 41-03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: i18n-Keys (de/en/cs) + Completeness-Test</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs:879 (i18n_week_status_keys_present_in_all_locales — Test-Vorlage) und die enum-Key-Deklarationen
    - .planning/phases/41-avg-anwesenheit-flexible/41-UI-SPEC.md §"Copywriting Contract" (exakte Texte de/en/cs)
  </read_first>
  <action>
    Ergänze `enum Key` in mod.rs um `AvgHoursPerAttendanceDay`, `AvgHoursPerAttendanceDayDescription`,
    `AvgHoursPerAttendanceDayEmpty`. Füge die Übersetzungen in de.rs/en.rs/cs.rs hinzu, exakt nach
    UI-SPEC §Copywriting Contract:
    de: "Ø Std/Anwesenheitstag" / "Durchschnittliche Arbeitsstunden pro Anwesenheitstag (nur
    flexible MA). Urlaub und Abwesenheiten sind nicht im Nenner." / "Nicht aussagekräftig
    (weniger als 2 Anwesenheitstage)".
    en: "Avg h/attendance day" / "Average working hours per day actually worked (flexible
    employees only). Absences excluded from the denominator." / "Not meaningful (fewer than 2
    attendance days)".
    cs: "Prům. hod./den přítomnosti" / "Průměrné pracovní hodiny za den skutečné přítomnosti
    (pouze flexibilní zaměstnanci). Absence nejsou ve jmenovateli." / "Nevýznamné (méně než 2
    dny přítomnosti)".
    Füge einen `#[test] fn i18n_attendance_keys_present_in_all_locales()` analog zu
    `i18n_week_status_keys_present_in_all_locales` hinzu, der die 3 neuen Keys über alle Locales
    (De/En/Cs) auf nicht-leere Übersetzung prüft.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo test i18n_attendance_keys_present_in_all_locales</automated>
  </verify>
  <acceptance_criteria>
    - 3 neue Keys im enum + Übersetzungen in de/en/cs exakt nach UI-SPEC
    - Test `i18n_attendance_keys_present_in_all_locales` grün
  </acceptance_criteria>
  <done>3 Keys × de/en/cs vorhanden (exakt UI-SPEC-Texte); Completeness-Test grün.</done>
</task>

<task type="auto">
  <name>Task 2: FE-API-Loader + EmployeeStore-Feld + load_employee_data</name>
  <files>shifty-dioxus/src/api.rs, shifty-dioxus/src/service/employee.rs</files>
  <read_first>
    - shifty-dioxus/src/api.rs:387-401 (get_employee_weekly_statistics — API-Vorlage)
    - shifty-dioxus/src/service/employee.rs:28-36 (EmployeeStore), :73 (Default-Init), :91-127 (load_employee_data mit year/until_week)
    - .planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md §"Pattern 6/7"
  </read_first>
  <action>
    (1) api.rs: `pub async fn get_employee_attendance_statistics(config: Config,
    sales_person_id: Uuid, year: u32, until_week: u8) -> Result<Option<Rc<EmployeeAttendanceStatisticsTO>>,
    reqwest::Error>`; URL `"{backend}/report/{id}/attendance-statistics?year={year}&until_week={until_week}"`;
    `reqwest::get(url).await?; response.error_for_status_ref()?;` dann
    `response.json::<Option<EmployeeAttendanceStatisticsTO>>().await` und in `Ok(opt.map(Rc::new))`
    verpacken — so werden None (nicht-flexibler MA) und HTTP-Fehler getrennt behandelt.
    (2) employee.rs: Feld `pub attendance_statistics: Option<Rc<EmployeeAttendanceStatisticsTO>>`
    in `EmployeeStore` + `attendance_statistics: None` im Default/Init. In `load_employee_data`
    nach dem weekly_statistics-Abruf: `let attendance_statistics =
    api::get_employee_attendance_statistics(CONFIG.read().clone(), sales_person_id, year,
    until_week).await.ok().flatten();` und ins Store-Objekt übernehmen (IMMER setzen, auch None,
    damit alter State bei nicht-flexiblem MA geleert wird — Pitfall 5).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `api::get_employee_attendance_statistics` gibt `Result<Option<Rc<..TO>>, reqwest::Error>` zurück
    - `EmployeeStore.attendance_statistics` existiert, wird in load_employee_data mit year/until_week geladen und immer (auch None) gesetzt
    - WASM-Build grün
  </acceptance_criteria>
  <done>api-Loader (Option-Return) + EmployeeStore-Feld + load_employee_data-Wiring; WASM baut.</done>
</task>

<task type="auto">
  <name>Task 3: Rendering im HR-Stats-Block von employee_view.rs</name>
  <files>shifty-dioxus/src/component/employee_view.rs</files>
  <read_first>
    - shifty-dioxus/src/component/employee_view.rs:42 (weekly_statistics-Prop), :89 (should_show_hr_stats), :519-541 (HR-Stats-Block mit AverageWorkedHoursPerWeek + StatisticsIncludedWeeks), :1017/1037 (Prop-Weitergabe aus employee_store)
    - .planning/phases/41-avg-anwesenheit-flexible/41-UI-SPEC.md §"Component Inventory" + §"Interaction Contract"
  </read_first>
  <action>
    (1) Neues Prop `attendance_statistics: Option<Rc<EmployeeAttendanceStatisticsTO>>` im
    Props-Struct von EmployeeViewPlain; beim Aufruf aus dem employee_store durchreichen
    (analog `weekly_statistics = employee_store.weekly_statistics.clone()` bei :1017/1037).
    (2) Im HR-Stats-`section` (innerhalb `should_show_hr_stats`), DIREKT nach der
    AverageWorkedHoursPerWeek-`TupleRow` und VOR StatisticsIncludedWeeks (D-AVG-07):
    `if let Some(att) = props.attendance_statistics.as_ref()` → rendere eine `TupleRow` mit
    `label: ImStr::from(i18n.t(Key::AvgHoursPerAttendanceDay).as_ref())`,
    `description: Some(rsx!{ "{i18n.t(Key::AvgHoursPerAttendanceDayDescription)}" })`, und value:
    bei `Some(avg)` → `span { class: "font-mono tabular-nums", {format_hours(avg, 2)} }`;
    bei `None` → `span { class: "font-mono tabular-nums text-ink-muted",
    title: "{i18n.t(Key::AvgHoursPerAttendanceDayEmpty)}", "–" }` (EN-DASH U+2013).
    Nur bereits im Projekt genutzte Tailwind-Token verwenden (keine verbotenen Klassen laut UI-SPEC).
    Row wird NUR bei `Some(att)` gerendert (nicht-flexibler MA → keine Row, D-AVG-05).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5 && cargo test -p shifty-dioxus 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Neue `TupleRow` erscheint nach "Ø Std/Woche", vor "Einbezogene Wochen", nur bei Some(attendance_statistics)
    - Leerzustand: EN-DASH "–" + text-ink-muted + title=Empty-Key bei average None
    - Keine verbotenen Tailwind-Klassen (UI-SPEC Farbliste) im neuen Code
    - `cargo build --target wasm32-unknown-unknown` + `cargo test -p shifty-dioxus` grün
  </acceptance_criteria>
  <done>TupleRow nach "Ø Std/Woche" (nur bei Some), Leerzustand "–", keine verbotenen Klassen; WASM-Build + FE-Tests grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE (WASM) ← REST | FE konsumiert die HR-Kennzahl; Autorisierung ist server-seitig (41-02/03), FE ist reine Anzeige |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-09 | Information Disclosure | FE rendert Kennzahl für nicht-flexiblen MA aus altem Store-State | medium | mitigate | load_employee_data setzt attendance_statistics IMMER neu (auch None); Row nur bei Some; Server bleibt Source of Truth (Pitfall 5) |
| T-41-10 | Information Disclosure | Nicht-HR sieht HR-Block | low | mitigate | Endpoint HR-gated (403 → FE None); HR-Block bereits hinter should_show_hr_stats |
| T-41-SC | Tampering | Supply-Chain (neue Pakete) | low | accept | Keine neuen FE-Dependencies |
</threat_model>

<verification>
- `cargo test i18n_attendance_keys_present_in_all_locales` (i18n de/en/cs vollständig)
- `cargo build --target wasm32-unknown-unknown` (WASM-Gate)
- `cargo test -p shifty-dioxus`
- Optional Smoke (get_page_text/find, keine Screenshots — Reference-Note): Zahl neben "Ø Std/Woche", "–" bei < 2 Tagen, keine Row für nicht-flexible MA
</verification>

<success_criteria>
Kennzahl im HR-Stats-Block sichtbar (nur flexible MA), Leerzustand "–" bei < 2 Tagen,
i18n de/en/cs vollständig, WASM-Build + FE-Tests grün.
</success_criteria>

<output>
Create `.planning/phases/41-avg-anwesenheit-flexible/41-04-SUMMARY.md` when done
</output>
