---
phase: 16-jahresansicht-display
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/page/weekly_overview.rs
  - shifty-dioxus/src/component/weekly_overview_chart.rs
  - .planning/ROADMAP.md
autonomous: true
requirements: [CVC-07, CVC-08]
tags: [D-02, D-03, D-04, CVC-07, CVC-08]

must_haves:
  truths:
    - "D-02: Tabelle zeigt drei getrennte Tokens 💰paid | 🎯committed | 🤝surplus (Desktop + Mobile), nie kombiniert"
    - "D-03: committed == 0 rendert 🎯0.00 (zwei Dezimalstellen), KEINE blank/Strich-Sonderlogik — ROADMAP SC#2-Wortlaut auf D-03-Form korrigiert"
    - "D-04: Chart bekommt drittes gestapeltes Segment (paid/committed/surplus) mit var(--good); bar_total = paid + committed + surplus; Tooltip nennt alle drei"
    - "D-04: bestehender Test chart_volunteer_uses_ink_muted_not_good (weekly_overview_chart.rs:269-283) wird von einer globalen `!html.contains(\"background: var(--good)\")`-Assertion auf eine segment-spezifische Volunteer-Assertion verengt, damit das D-04-committed-var(--good)-Segment ihn nicht bricht"
    - "CVC-07: committed-Term durch alle drei Render-Boundaries (Token, Chart, Diff via available_hours)"
    - "CVC-08: neue Strings Key::Committed + Key::PaidCommittedVolunteer in De/En/Cs vollständig; Per-Locale-Reference-Matcher; bestehende cs.rs-Lücken (Key::Volunteer, Key::PaidVolunteer) geschlossen"
  artifacts:
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "Key::Committed + Key::PaidCommittedVolunteer Enum-Varianten + Per-Locale-Matcher-Tests"
      contains: "PaidCommittedVolunteer"
    - path: "shifty-dioxus/src/page/weekly_overview.rs"
      provides: "dritter Token 🎯committed (Desktop + Mobile) + erweiterter sample_week"
      contains: "🎯"
    - path: "shifty-dioxus/src/component/weekly_overview_chart.rs"
      provides: "drittes committed-Segment var(--good) + bar_total mit committed + verengter chart_volunteer_uses_ink_muted_not_good-Test"
      contains: "committed_voluntary_hours"
    - path: ".planning/ROADMAP.md"
      provides: "Phase-16 SC#2 auf D-03-Form korrigiert (committed=0 → 🎯0.00, keine blank/Strich-Sonderlogik)"
      contains: "🎯0.00"
  key_links:
    - from: "state::WeeklySummary.committed_voluntary_hours"
      to: "page token cell (Desktop Z.103 + Mobile Z.108)"
      via: "format!(\"💰{} | 🎯{} | 🤝{}\", ...)"
      pattern: "🎯"
    - from: "state::WeeklySummary.committed_voluntary_hours"
      to: "chart bar_total + drittes Segment"
      via: "w.paid_hours + w.committed_voluntary_hours + w.volunteer_hours"
      pattern: "committed_voluntary_hours"
    - from: "Key::Committed / Key::PaidCommittedVolunteer"
      to: "de.rs/en.rs/cs.rs add_text"
      via: "i18n.add_text(Locale::*, Key::*, \"...\")"
      pattern: "Key::Committed"
---

<objective>
Frontend-Render-/i18n-Wave: Den committed-Term durch die dritte Boundary (Render) führen — dritter Tabellen-Token (D-02/D-03), drittes Chart-Segment (D-04), drei neue i18n-Strings in allen drei Locales (CVC-08) — plus Schließen zweier bestehender cs.rs-Lücken. Per-Locale-Reference-Matcher, SSR-Tests, Hex-Audit, WASM-Gate. Plus zwei Doc-/Test-Korrekturen aus dem Checker-Review: bestehenden Volunteer-`var(--good)`-Guard-Test verengen (D-04) und ROADMAP-SC#2-Wortlaut auf D-03 nachziehen.

Purpose: Dies ist die sichtbare Phase-Wirkung. committed muss durch ALLE drei Boundaries (TO→state→render), sonst Omission-Lücke (ROADMAP SC1). Token-Farben token-basiert (kein Roh-Hex, von Tests gepinnt). i18n-Pflicht: alle drei Locales gleichzeitig, gegen den `Locale::De`-statt-`Locale::En`-Bug.

Output: Drei sichtbare Tokens, drei Chart-Segmente, vollständige i18n + Matcher-Tests, grüner WASM-Build, verengter Volunteer-Guard-Test, korrigierte ROADMAP-SC#2.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located mit git). GSD-Auto-Commit ist deaktiviert. Der EXECUTOR committet NICHT selbst — der User committet manuell via jj. NIEMALS `git commit`/`git add`/`git checkout`.
</vcs_jj_only>
</execution_context>

<context>
@.planning/phases/16-jahresansicht-display/16-CONTEXT.md
@.planning/phases/16-jahresansicht-display/16-PATTERNS.md
@.planning/phases/16-jahresansicht-display/16-UI-SPEC.md

<critical_facts>
- Depends_on 16-02: `state::WeeklySummary.committed_voluntary_hours` MUSS existieren (Plan 02).
- ZWEI lokale `sample_week`-Definitionen: `page/weekly_overview.rs:218` UND `component/weekly_overview_chart.rs:153`. BEIDE identisch um `committed: f32` erweitern, sonst kompilieren Tests mit falschen Werten (Pitfall 6).
- Chart-Farb-HARD-CONSTRAINT (UI-SPEC §Color): committed-Segment = `var(--good)`. NIEMALS Roh-Hex. Verbotene Hexes (von Tests gepinnt): `#3B82F6`, `#10B981`, `#EF4444`, `#e5e7eb`, `#6b7280`, `#374151`.
- ⚠ VERSTECKTE TEST-REGRESSION (Checker-BLOCKER): Der bestehende Test `chart_volunteer_uses_ink_muted_not_good` (`weekly_overview_chart.rs:269-283`) hat eine GLOBALE Assertion `assert!(!html.contains("background: var(--good)"), ...)` über das gesamte gerenderte HTML. Sobald das neue D-04-committed-Segment `background: var(--good);` rendert (bei `committed_voluntary_hours > 0`), enthält `html` `background: var(--good)` und dieser Test SCHLÄGT FEHL — er ist Teil des `cargo test -p shifty-dioxus`-Hard-Gates. Dieser Test MUSS in Task 3 von der globalen auf eine segment-spezifische Volunteer-Assertion verengt werden (siehe Action-Schritt 8a). Rationale: D-04 erlaubt `var(--good)` explizit für das committed-Segment.
- Chart-Stapel bottom→top: paid (`var(--accent)`) · committed (`var(--good)`) · surplus (`var(--ink-muted)` @0.35). Im RSX-Block stehen die Segmente in Reihenfolge Volunteer(top), Paid(bottom) — committed wird ZWISCHEN volunteer und paid eingefügt.
- D-02: drei GETRENNTE Tokens, NIE kombiniert (`💰{paid} | 🎯{committed} | 🤝{surplus}`). KEIN inline „5 + 2"-Token.
- D-03: committed==0 → `🎯0.00`, keine blank/Strich-Sonderlogik. `format_hours(value, 2)` in Tabelle, `format_hours(value, 1)` im Chart-Tooltip. KEINE neue Format-Funktion. ROADMAP-Phase-16-SC#2 trägt noch den alten „blank/Strich, nicht 0"-Wortlaut — per D-03 in diesem Plan (Task 3, Schritt 10) auf die D-03-Form nachzuziehen.
- Header-Key-Entscheidung (UI-SPEC, gelöst): NEUER Key `Key::PaidCommittedVolunteer`; `Key::PaidVolunteer` bleibt definiert (nicht mutieren).
- Czech-Strings sind MEDIUM-confidence (A3) — als Manual-Verification dem User zur Review markiert; trotzdem ausliefern.
- WASM-Build-Gate auf NixOS via `nix develop`: `cargo build --target wasm32-unknown-unknown` aus `shifty-dioxus/`.
</critical_facts>

<interfaces>
i18n-Strings (UI-SPEC Copywriting Contract):
| Key | De | En | Cs |
|-----|----|----|-----|
| `Key::Committed` | `Zugesagt` | `Committed` | `Přislíbeno` |
| `Key::PaidCommittedVolunteer` | `Bezahlt / Zugesagt / Freiwillig` | `Paid / Committed / Volunteer` | `Placené / Přislíbeno / Dobrovolné` |

Bestehende cs.rs-Lücken (verifiziert ABwesend, MIT schließen):
| Key | Cs-Wert |
|-----|---------|
| `Key::Volunteer` | `Dobrovolné` |
| `Key::PaidVolunteer` | `Placené / Dobrovolné` |

IST page Token (Z.103 Desktop, Z.108 Mobile, byte-identisch):
```rust
{format!("💰{} | 🤝{}", format_hours(week.paid_hours, 2), format_hours(week.volunteer_hours, 2))}
```
IST page sample_week (Z.218): `fn sample_week(year, week, paid, volunteer, required) -> WeeklySummary { available_hours: paid + volunteer, ... }`
IST page Header (Z.58): `let paid_volunteer = i18n.t(Key::PaidVolunteer);` → th rendert `"{paid_volunteer}"` (Z.72).

IST chart bar_total (Z.16): `let bar_total = w.paid_hours + w.volunteer_hours;`
IST chart Props: `WeeklyOverviewChartView { ..., paid_label, volunteer_label, required_label, week_short }` (Z.47-51 Wrapper, Z.56-63 Props). Test-`ViewProps`/`render_view` (Z.201-246) reicht die gleichen Labels durch.
IST chart Segmente (RSX): `if week.volunteer_hours > 0.0 { div { style: "height: {vol_pct}%; background: var(--ink-muted); opacity: 0.35;" } }` dann `if week.paid_hours > 0.0 { div { style: "height: {paid_pct}%; background: {paid_bg};" } }`.
IST chart Legend (3 spans, var(--accent)/var(--ink-muted)/var(--bad)); Tooltip nennt paid/volunteer/required.
IST chart Test `chart_volunteer_uses_ink_muted_not_good` (Z.269-283): rendert eine Woche, asserted `html.contains("background: var(--ink-muted); opacity: 0.35")` UND (GLOBAL, zu verengen) `!html.contains("background: var(--good)")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n — Key::Committed + Key::PaidCommittedVolunteer in 3 Locales + cs.rs-Lücken + Per-Locale-Matcher</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs:118-372 (Key-Enum; `Key::PaidVolunteer` ~Z.120, `Key::Volunteer` ~Z.344) und :778-809 (`i18n_absence_keys_match_{german,english,czech}_reference` — Matcher-Vorlage)
    - shifty-dioxus/src/i18n/de.rs:133,572 (`Key::PaidVolunteer`, `Key::Volunteer` add_text-Muster)
    - shifty-dioxus/src/i18n/en.rs:105,517
    - shifty-dioxus/src/i18n/cs.rs:127-135,539,597-600 (Stelle, wo Key::Volunteer/PaidVolunteer FEHLEN)
    - .planning/phases/16-jahresansicht-display/16-PATTERNS.md § „Shared Patterns / i18n" (Z.301-352)
    - .planning/phases/16-jahresansicht-display/16-UI-SPEC.md § „Copywriting Contract"
  </read_first>
  <behavior>
    - Test `i18n_committed_keys_match_german_reference`: `generate(Locale::De).t(Key::Committed) == "Zugesagt"` und `t(Key::PaidCommittedVolunteer) == "Bezahlt / Zugesagt / Freiwillig"`.
    - Test `_english_reference`: `generate(Locale::En).t(Key::Committed) == "Committed"`, `PaidCommittedVolunteer == "Paid / Committed / Volunteer"`.
    - Test `_czech_reference`: `generate(Locale::Cs).t(Key::Committed) == "Přislíbeno"`, `PaidCommittedVolunteer == "Placené / Přislíbeno / Dobrovolné"`.
    - Test (cs.rs-Lücke): `generate(Locale::Cs).t(Key::Volunteer) == "Dobrovolné"` und `t(Key::PaidVolunteer) == "Placené / Dobrovolné"` (keine `??`-Fallbacks mehr).
  </behavior>
  <action>
    (1) `i18n/mod.rs` Key-Enum: zwei neue Varianten nahe `Key::PaidVolunteer` (~Z.120) ergänzen:
    ```rust
    PaidCommittedVolunteer,
    Committed,
    ```
    `Key::PaidVolunteer` NICHT entfernen/umbenennen.

    (2) `de.rs` (jede Zeile mit `Locale::De`):
    ```rust
    i18n.add_text(Locale::De, Key::Committed, "Zugesagt");
    i18n.add_text(Locale::De, Key::PaidCommittedVolunteer, "Bezahlt / Zugesagt / Freiwillig");
    ```

    (3) `en.rs` (jede Zeile mit `Locale::En`):
    ```rust
    i18n.add_text(Locale::En, Key::Committed, "Committed");
    i18n.add_text(Locale::En, Key::PaidCommittedVolunteer, "Paid / Committed / Volunteer");
    ```

    (4) `cs.rs` (jede Zeile mit `Locale::Cs`) — neue Keys UND die zwei bestehenden Lücken:
    ```rust
    i18n.add_text(Locale::Cs, Key::Committed, "Přislíbeno");
    i18n.add_text(Locale::Cs, Key::PaidCommittedVolunteer, "Placené / Přislíbeno / Dobrovolné");
    i18n.add_text(Locale::Cs, Key::Volunteer, "Dobrovolné");
    i18n.add_text(Locale::Cs, Key::PaidVolunteer, "Placené / Dobrovolné");
    ```
    ⚠ Pitfall 5: In jeder Datei AUSSCHLIESSLICH den passenden `Locale::*` verwenden (kein `Locale::En` in `de.rs` etc.).

    (5) Per-Locale-Reference-Matcher-Tests (analog `i18n/mod.rs:778-809`) im i18n-Test-Modul ergänzen: `i18n_committed_keys_match_german_reference` / `_english_reference` / `_czech_reference` mit den oben gepinnten Strings, plus Assertions dass `Key::Volunteer`/`Key::PaidVolunteer` in `Locale::Cs` die erwarteten Werte (nicht leer/`??`) liefern.
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus i18n</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PaidCommittedVolunteer" shifty-dioxus/src/i18n/mod.rs` ≥ 1 (Enum-Variante) und je 1 Treffer in de.rs/en.rs/cs.rs.
    - `grep -n "Key::Committed" shifty-dioxus/src/i18n/de.rs shifty-dioxus/src/i18n/en.rs shifty-dioxus/src/i18n/cs.rs` liefert genau drei add_text-Treffer (einer pro Datei).
    - `grep -c "Locale::En" shifty-dioxus/src/i18n/de.rs` == 0 und `grep -c "Locale::De" shifty-dioxus/src/i18n/en.rs` == 0 (Locale-Swap-Guard).
    - `grep -n "Key::Volunteer\|Key::PaidVolunteer" shifty-dioxus/src/i18n/cs.rs` liefert jetzt beide (Lücken geschlossen).
    - `cargo test -p shifty-dioxus i18n` grün; die drei `_reference`-Matcher-Tests enthalten und grün.
  </acceptance_criteria>
  <done>Zwei neue Keys in 3 Locales, zwei cs.rs-Lücken geschlossen, Per-Locale-Matcher grün, kein Locale-Swap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: page — dritter Token 🎯committed (Desktop + Mobile) + Header-Key + sample_week + SSR-Tests</name>
  <files>shifty-dioxus/src/page/weekly_overview.rs</files>
  <read_first>
    - shifty-dioxus/src/page/weekly_overview.rs:55-115 (Token-Rendering Z.103 Desktop / Z.108 Mobile; Header Z.58/72; Diff Z.87)
    - shifty-dioxus/src/page/weekly_overview.rs:218-235 (lokaler `sample_week`-Helper) + alle Aufrufer (Z.283, 373, 375, 377, 379, 414)
    - shifty-dioxus/src/page/weekly_overview.rs:554-595 (`page_source_does_not_use_legacy_classes` — NICHT brechen)
    - .planning/phases/16-jahresansicht-display/16-PATTERNS.md § „page/weekly_overview.rs" (Z.162-228)
    - .planning/phases/16-jahresansicht-display/16-UI-SPEC.md § „Token Display Semantics"
  </read_first>
  <behavior>
    - SSR-Test (CVC-07d): gerendertes HTML enthält den `🎯`-Token in der Desktop-Zelle UND der Mobile-Inline-Zeile.
    - SSR-Test (CVC-07e): `committed=5, actual(surplus)=2` → HTML enthält `🎯5.00` und `🤝2.00` als getrennte Tokens (nie `🎯5.00 | 🤝2.00` kombiniert in einem Token — drei separate `💰/🎯/🤝`-Segmente).
    - SSR-Test (CVC-07f / D-03): `committed=0` → HTML enthält `🎯0.00` (kein Strich/em-dash/blank).
  </behavior>
  <action>
    (1) Token-Rendering an BEIDEN Stellen (Z.103 Desktop UND Z.108 Mobile) ändern — D-02, drei getrennte Tokens:
    IST:
    ```rust
    {format!("💰{} | 🤝{}", format_hours(week.paid_hours, 2), format_hours(week.volunteer_hours, 2))}
    ```
    SOLL:
    ```rust
    {format!("💰{} | 🎯{} | 🤝{}",
        format_hours(week.paid_hours, 2),
        format_hours(week.committed_voluntary_hours, 2),
        format_hours(week.volunteer_hours, 2))}
    ```
    `format_hours` ist bereits importiert (Z.7). KEINE neue Format-Funktion. D-03: committed==0 rendert `🎯0.00` ganz normal.

    (2) Header-Key (Z.58) auf den neuen Key umstellen:
    IST: `let paid_volunteer = i18n.t(Key::PaidVolunteer);` und `th { ... "{paid_volunteer}" }` (Z.72).
    SOLL: `let paid_committed_volunteer = i18n.t(Key::PaidCommittedVolunteer);` und im th `"{paid_committed_volunteer}"`. (`Key::PaidCommittedVolunteer` existiert aus Task 1.)

    (3) `sample_week`-Helper (Z.218) um `committed: f32` erweitern:
    SOLL:
    ```rust
    fn sample_week(year: u32, week: u8, paid: f32, committed: f32, volunteer: f32, required: f32) -> WeeklySummary {
        WeeklySummary {
            // ...
            available_hours: paid + committed + volunteer,
            committed_voluntary_hours: committed,
            volunteer_hours: volunteer,
            // ... übrige Felder unverändert ...
        }
    }
    ```
    ALLE Aufrufer nachziehen (committed-Argument zwischen paid und volunteer einfügen): Z.283 `sample_week(year, w, 20.0, 0.0, 5.0, 30.0)`, Z.373 `sample_week(2026, 1, 33.0, 0.0, 0.0, 30.0)`, Z.375/377/379 analog (committed=0.0), Z.414 `sample_week(2026, 27, 25.0, 0.0, 5.0, 30.0)`. (committed-Default 0.0, außer wo ein Test committed gezielt prüft.)

    (4) Neue SSR-Tests im bestehenden Harness (`render_table` + `VirtualDom` + `dioxus_ssr::render`, Z.237ff) ergänzen:
    - Ein Test mit `committed=5.0, volunteer=2.0`, der `html.contains("🎯5.00")` und `html.contains("🤝2.00")` asserted (Desktop + Mobile).
    - Ein Test mit `committed=0.0`, der `html.contains("🎯0.00")` asserted (D-03, keine blank-Logik) und sicherstellt, dass kein em-dash/Strich-Zeichen für committed erscheint.

    Den `page_source_does_not_use_legacy_classes`-Test NICHT brechen (keine Legacy-Klassen einführen).
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus weekly_overview</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "🎯" shifty-dioxus/src/page/weekly_overview.rs` ≥ 2 (Desktop- + Mobile-Token-String im Prod-Source).
    - `grep -n "💰{} | 🤝{}" shifty-dioxus/src/page/weekly_overview.rs` liefert KEINEN Treffer mehr (alter Zwei-Token-String ersetzt).
    - `grep -n "Key::PaidCommittedVolunteer" shifty-dioxus/src/page/weekly_overview.rs` liefert ≥ 1 Treffer (Header umgestellt).
    - `grep -n "committed_voluntary_hours: committed" shifty-dioxus/src/page/weekly_overview.rs` liefert 1 Treffer (sample_week erweitert).
    - `cargo test -p shifty-dioxus weekly_overview` grün; die neuen SSR-Tests (🎯5.00/🤝2.00 und 🎯0.00) enthalten und grün; `page_source_does_not_use_legacy_classes` grün.
  </acceptance_criteria>
  <done>Drei getrennte Tokens an Desktop+Mobile, Header umgestellt, sample_week erweitert, SSR-Tests inkl. D-03 grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chart — drittes committed-Segment var(--good) + bar_total + Tooltip/Legend/Props + Volunteer-Guard-Test verengen + ROADMAP-SC#2 + Hex-Audit + WASM-Gate</name>
  <files>shifty-dioxus/src/component/weekly_overview_chart.rs, .planning/ROADMAP.md</files>
  <read_first>
    - shifty-dioxus/src/component/weekly_overview_chart.rs:12-30 (`compute_max_hours`/`bar_total`)
    - shifty-dioxus/src/component/weekly_overview_chart.rs:44-130 (Wrapper-Threading Z.47-51, ViewProps Z.56-63, Legend Z.77-90, Bars/Segmente Z.99-130, Tooltip Z.106-112)
    - shifty-dioxus/src/component/weekly_overview_chart.rs:153-267 (lokaler `sample_week` Z.153, `ViewProps`/`render_view` Z.201-246, `chart_uses_token_styles_not_legacy_hex` Z.247-267, `compute_max_hours`-Tests Z.173-191) + alle sample_week-Aufrufer
    - shifty-dioxus/src/component/weekly_overview_chart.rs:269-283 (`chart_volunteer_uses_ink_muted_not_good` — ⚠ enthält die GLOBALE `!html.contains("background: var(--good)")`-Assertion, die das neue committed-var(--good)-Segment brechen würde; in Schritt 8a zu verengen)
    - .planning/phases/16-jahresansicht-display/16-PATTERNS.md § „component/weekly_overview_chart.rs" (Z.232-296)
    - .planning/phases/16-jahresansicht-display/16-UI-SPEC.md § „Color" (HARD CONSTRAINT var(--good))
    - .planning/ROADMAP.md § „Phase 16: Jahresansicht display" SC#2 (~Z.36 + ~Z.40 — der „blank/Strich, nicht 0"-Wortlaut, per D-03 zu korrigieren)
  </read_first>
  <behavior>
    - Test (CVC-07h): `compute_max_hours` rechnet `bar_total = paid + committed + volunteer` (z.B. paid=20, committed=5, volunteer=10, required=30 → bar_total=35 ≥ required → max berücksichtigt 35).
    - Test (CVC-07g): `chart_uses_token_styles_not_legacy_hex` — gerendertes HTML enthält `var(--good)` (committed-Segment) UND `var(--accent)`/`var(--ink-muted)`/`var(--bad)`; KEIN verbotenes Roh-Hex.
    - Test (D-04, verengt): `chart_volunteer_uses_ink_muted_not_good` asserted weiterhin, dass das Volunteer-Segment `var(--ink-muted); opacity: 0.35` nutzt, aber NICHT mehr global `!html.contains("background: var(--good)")` — sondern segment-spezifisch, dass der Volunteer-Div selbst NICHT `var(--good)` verwendet. Mit einer Woche, die `committed > 0` hat (damit das committed-`var(--good)`-Segment tatsächlich gerendert wird), bleibt der Test grün.
    - Test: Tooltip-String nennt drei Werte (paid, committed, volunteer).
  </behavior>
  <action>
    (1) `bar_total` (Z.16) — D-04 / Pitfall 3:
    IST: `let bar_total = w.paid_hours + w.volunteer_hours;`
    SOLL: `let bar_total = w.paid_hours + w.committed_voluntary_hours + w.volunteer_hours;`

    (2) Prozent-Wert ergänzen (bei `paid_pct`/`vol_pct`, ~Z.99-100):
    ```rust
    let committed_pct = (week.committed_voluntary_hours / max_hours) * 100.0;
    ```

    (3) Drittes Segment ZWISCHEN volunteer (top) und paid (bottom) einfügen — HARD CONSTRAINT `var(--good)`:
    ```rust
    // Volunteer (top) — unverändert
    if week.volunteer_hours > 0.0 {
        div { style: "height: {vol_pct}%; background: var(--ink-muted); opacity: 0.35;" }
    }
    // Committed (NEU, Mitte) — var(--good), niemals Roh-Hex
    if week.committed_voluntary_hours > 0.0 {
        div { style: "height: {committed_pct}%; background: var(--good);" }
    }
    // Paid (bottom) — unverändert
    if week.paid_hours > 0.0 {
        div { style: "height: {paid_pct}%; background: {paid_bg};" }
    }
    ```

    (4) Props/Wrapper-Threading für `committed_label` (analog `volunteer_label`):
    - Wrapper (Z.47-51): `committed_label: i18n.t(Key::Committed).to_string(),`
    - `WeeklyOverviewChartView`-Props (Z.56-63): `committed_label: String,`
    - Test-`ViewProps`-Struct (Z.201-208) + `render_view`-Aufbau (Z.232-244): `committed_label` ebenfalls durchreichen (Default z.B. `"Committed"`), sonst kompilieren die Chart-Tests nicht.

    (5) Legend — dritten Eintrag (Analog volunteer-span) einfügen:
    ```rust
    span { class: "inline-flex items-center gap-1.5",
        span { style: "background: var(--good); width: 12px; height: 12px; border-radius: 2px; display: inline-block;" }
        "{committed_label}"
    }
    ```

    (6) Tooltip (Z.106-112) — alle drei Werte (D-04), committed zwischen paid und volunteer, `format_hours(week.committed_voluntary_hours, 1)`:
    ```rust
    let tooltip = format!(
        "{week_short} {}: {paid_label} {}h, {committed_label} {}h, {volunteer_label} {}h, {required_label} {}h",
        week.week,
        format_hours(week.paid_hours, 1),
        format_hours(week.committed_voluntary_hours, 1),
        format_hours(week.volunteer_hours, 1),
        format_hours(week.required_hours, 1)
    );
    ```

    (7) Lokalen `sample_week` (Z.153) IDENTISCH zu Plan-Task-2 erweitern (`committed: f32` zwischen paid und volunteer; `available_hours = paid + committed + volunteer`; `committed_voluntary_hours: committed`). ALLE Aufrufer (Z.175,176,184,191,249,250,272,287,299-301,319-320,337-338,354) um das committed-Argument (0.0, außer wo gezielt geprüft) nachziehen. Pitfall 6.
    ⚠ HINWEIS für Schritt 8a: Der `sample_week`-Aufruf in `chart_volunteer_uses_ink_muted_not_good` (Z.272) wird in 8a so angepasst, dass committed > 0 ist (z.B. `sample_week(2026, 1, 20.0, 8.0, 10.0, 35.0)`), damit der Test die D-04-konforme Koexistenz von committed-`var(--good)` und volunteer-`var(--ink-muted)` tatsächlich prüft.

    (8) Tests erweitern:
    - `chart_uses_token_styles_not_legacy_hex` (Z.247) um `assert!(html.contains("var(--good)"))` ergänzen (verbotene Hexes bleiben absent).
    - `compute_max_hours`-Test (Z.173) um committed im bar_total (z.B. `sample_week(2026, 1, 20.0, 5.0, 10.0, 35.0)`).
    - Optional ein Tooltip-Assertion-Test, dass `committed_label` im title erscheint.

    (8a) ⚠ CHECKER-BLOCKER FIX — `chart_volunteer_uses_ink_muted_not_good` (Z.269-283) VERENGEN (D-04-Rationale):
    Der bestehende Test hat eine GLOBALE Assertion `assert!(!html.contains("background: var(--good)"), ...)`. Da D-04 das committed-Segment explizit mit `background: var(--good);` rendert, schlägt diese globale Assertion fehl, sobald eine Bar `committed_voluntary_hours > 0` hat. Den Test umbauen:
    IST (Z.272-282):
    ```rust
    let weeks: Rc<[WeeklySummary]> = vec![sample_week(2026, 1, 20.0, 10.0, 35.0)].into();
    let html = render_view(weeks, 2026, 1, "W");
    // The volunteer bar div should reference var(--ink-muted) with opacity 0.35.
    assert!(
        html.contains("background: var(--ink-muted); opacity: 0.35"),
        "expected volunteer ink-muted with opacity 0.35: {html}"
    );
    assert!(
        !html.contains("background: var(--good)"),
        "should not use --good for volunteer: {html}"
    );
    ```
    SOLL (committed > 0, damit das committed-var(--good)-Segment koexistiert; Assertion segment-spezifisch auf den Volunteer-Div):
    ```rust
    // committed > 0, damit das D-04-committed-Segment (var(--good)) tatsächlich rendert
    // und wir prüfen können, dass NUR das committed-Segment --good nutzt, NICHT das Volunteer-Segment.
    let weeks: Rc<[WeeklySummary]> = vec![sample_week(2026, 1, 20.0, 8.0, 10.0, 35.0)].into();
    let html = render_view(weeks, 2026, 1, "W");
    // Das Volunteer-Segment muss var(--ink-muted) mit opacity 0.35 nutzen.
    assert!(
        html.contains("background: var(--ink-muted); opacity: 0.35"),
        "expected volunteer ink-muted with opacity 0.35: {html}"
    );
    // D-04: var(--good) ist NUR für das committed-Segment erlaubt, NICHT für volunteer.
    // Segment-spezifischer Guard: der Volunteer-Div-Style (ink-muted + opacity 0.35) darf
    // selbst kein var(--good) tragen.
    assert!(
        !html.contains("background: var(--good); opacity: 0.35"),
        "volunteer segment (ink-muted/opacity 0.35) must not use --good: {html}"
    );
    ```
    Rationale (D-04): D-04 ordnet ein drittes committed-Segment mit `var(--good)` an. Die alte globale `!contains("background: var(--good)")`-Assertion war ein Phase-15-Guard gegen ein FALSCHES Volunteer-Grün und kollidiert jetzt mit der D-04-korrekten committed-Farbe. Die verengte Assertion bewahrt die ursprüngliche Absicht (Volunteer ≠ grün), ohne das legitime committed-`var(--good)`-Segment zu verbieten.
    ⚠ Falls der reale Volunteer-Div-Style-String von `"background: var(--ink-muted); opacity: 0.35"` abweicht (Reihenfolge/Spacing), die negative Assertion an den im Prod-Source tatsächlich emittierten Volunteer-Style anpassen — Ziel bleibt: assert, dass der Volunteer-Style-Token NICHT `var(--good)` enthält, während das separate committed-Segment `var(--good)` tragen darf.

    (9) WASM-Build-Gate am Ende der Wave: aus `shifty-dioxus/` unter `nix develop` `cargo build --target wasm32-unknown-unknown`.

    (10) ⚠ CHECKER-WARNING FIX — ROADMAP.md Phase-16 SC#2 (D-03) korrigieren:
    `.planning/ROADMAP.md` § „Phase 16: Jahresansicht display" trägt im Goal-Absatz (~Z.36) und in SC#2 (~Z.40) noch den revidierten Wortlaut „Für nicht-gedeckelte Personen blank/Strich, nicht `0`" bzw. „für nicht-gedeckelte Personen (`cap = false`) wird die Spalte blank/Strich angezeigt, nicht `0`". D-03 verwirft diese Sonderlogik für die Jahresansicht. Beide Stellen auf die D-03-Form umschreiben:
    - Goal-Absatz (~Z.36): den Satzteil „Für nicht-gedeckelte Personen blank/Strich, nicht `0`." ersetzen durch: „committed=0 wird als `🎯0.00` gezeigt (keine blank/Strich-Sonderlogik, D-03)."
    - SC#2 (~Z.40): „für nicht-gedeckelte Personen (`cap = false`) wird die Spalte blank/Strich angezeigt, nicht `0`" ersetzen durch: „committed=0 wird als `🎯0.00` gezeigt — keine blank/Strich-Sonderlogik (D-03; die blank/Strich-Idee gehört, falls überhaupt, in die Mitarbeiteransicht/Phase 17)".
    Den Überschuss-Teil von SC#2 (committed=5, actual=7 → `5 + 2`; committed=5, actual=3 → gedeckte `5`) UNVERÄNDERT lassen — nur den blank/Strich-Satzteil korrigieren.
    KEIN Commit (jj-managed, User committet manuell).
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus weekly_overview_chart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "w.paid_hours + w.committed_voluntary_hours + w.volunteer_hours" shifty-dioxus/src/component/weekly_overview_chart.rs` liefert 1 Treffer (bar_total).
    - `grep -c "var(--good)" shifty-dioxus/src/component/weekly_overview_chart.rs` ≥ 2 (Segment + Legend-Swatch).
    - `chart_cut=$(grep -n '#\[cfg(test)\]' shifty-dioxus/src/component/weekly_overview_chart.rs | head -1 | cut -d: -f1); head -n "$chart_cut" shifty-dioxus/src/component/weekly_overview_chart.rs | grep -E "#3B82F6|#10B981|#EF4444|#e5e7eb|#6b7280|#374151"` liefert KEINEN Treffer (kein Roh-Hex im Prod-Source).
    - `grep -n "committed_label" shifty-dioxus/src/component/weekly_overview_chart.rs` zeigt Wrapper-Threading, Props, Legend, Tooltip und Test-ViewProps.
    - Volunteer-Guard verengt: `grep -n '!html.contains("background: var(--good)")' shifty-dioxus/src/component/weekly_overview_chart.rs` liefert KEINEN Treffer mehr (globale Assertion entfernt); der Test `chart_volunteer_uses_ink_muted_not_good` ist weiterhin vorhanden, nutzt committed > 0 und eine segment-spezifische negative Assertion.
    - `cargo test -p shifty-dioxus weekly_overview_chart` grün; `chart_uses_token_styles_not_legacy_hex` (mit var(--good)-Assertion), `chart_volunteer_uses_ink_muted_not_good` (verengt) + erweiterter `compute_max_hours`-Test grün.
    - ROADMAP: `grep -c "blank/Strich" .planning/ROADMAP.md` zählt im Phase-16-Block keinen „nicht 0"-Wortlaut mehr; `grep -n "🎯0.00" .planning/ROADMAP.md` liefert im Phase-16-Block ≥ 1 Treffer (D-03-Wortlaut). (Andere Phasen-Blöcke unberührt.)
    - WASM-Gate: `cargo build --target wasm32-unknown-unknown` (aus `shifty-dioxus/`, `nix develop`) exit 0.
    - `cargo test -p shifty-dioxus` (volle Frontend-Suite) exit 0, inkl. `page_source_does_not_use_legacy_classes`.
  </acceptance_criteria>
  <done>Drittes committed-Segment (var(--good)), bar_total/Tooltip/Legend/Props erweitert, sample_week beider Dateien synchron, Volunteer-Guard-Test verengt (D-04), ROADMAP-SC#2 auf D-03 korrigiert, Hex-Audit + WASM-Gate grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| State → DOM-Render (Token-Cell, Chart-SVG/Divs, Tooltip) | client-seitiges Rendern eines bereits gegateten Read-Aggregats; keine User-Eingabe, kein neuer Fetch |
| i18n-String-Tabelle → DOM | statische, übersetzte Labels; keine dynamische/user-kontrollierte Interpolation in den neuen Strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information Disclosure | committed-Token + Chart-Segment (Render) | accept | Read-only Aggregat (Σ pro Woche), kein PII; identische Vertraulichkeitsklasse wie die bereits sichtbaren paid/volunteer-Tokens. |
| T-16-05 | Tampering (XSS) | i18n-Strings + `format!`-Token im DOM | mitigate | Dioxus RSX escaped Text-Interpolation automatisch; die neuen Strings sind statische Literale, die Werte sind via `format_hours` formatierte `f32` (keine rohen Strings) — kein Injection-Vektor. Keine `dangerous_inner_html`-Nutzung eingeführt. |

Keine High-Severity-Threats: rein additives Display + statische i18n-Labels, kein neuer Auth-/Fetch-/Schreib-Pfad, kein Secret. ASVS L1 V5 (Output-Encoding) durch Dioxus-Default-Escaping abgedeckt.
</threat_model>

<verification>
- `cargo test -p shifty-dioxus i18n` grün (3 Locales, Per-Locale-Matcher, cs.rs-Lücken).
- `cargo test -p shifty-dioxus weekly_overview` grün (drei Token, D-03, SSR).
- `cargo test -p shifty-dioxus weekly_overview_chart` grün (drittes Segment, bar_total, Hex-Audit, verengter Volunteer-Guard).
- `cargo test -p shifty-dioxus` voll grün.
- WASM-Build-Gate exit 0.
- ROADMAP-SC#2 auf D-03-Form korrigiert (committed=0 → 🎯0.00).
</verification>

<success_criteria>
- Tabelle zeigt drei getrennte Tokens 💰/🎯/🤝 (Desktop + Mobile); committed==0 → 🎯0.00 (D-02/D-03).
- Chart zeigt drittes var(--good)-Segment; bar_total + Tooltip konsistent mit Diff-Spalte (D-04); Volunteer-Guard-Test verengt, bricht nicht am committed-var(--good)-Segment.
- Key::Committed + Key::PaidCommittedVolunteer in De/En/Cs vollständig; cs.rs-Lücken geschlossen; Per-Locale-Matcher grün (CVC-08).
- Kein Roh-Hex im Chart-Prod-Source; WASM-Build grün.
- ROADMAP-Phase-16-SC#2 trägt nicht mehr den „blank/Strich, nicht 0"-Wortlaut, sondern die D-03-Form.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-jahresansicht-display/16-03-SUMMARY.md` erstellen.

⚠ Manual-Verification an den User markieren (16-VALIDATION.md): (1) visuelle Drei-Farben-Stapelung des Charts im Browser; (2) Czech-Strings sprachlich prüfen (`Přislíbeno`, `Dobrovolné`, `Placené` — MEDIUM-confidence A3).
</output>
</content>
</invoke>
