---
phase: 16-jahresansicht-display
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - shifty-dioxus/src/state/weekly_overview.rs
autonomous: true
requirements: [CVC-07]
tags: [D-01, CVC-07]

must_haves:
  truths:
    - "CVC-07: Frontend-WeeklySummary trägt committed_voluntary_hours und mappt es aus WeeklySummaryTO (keine Omission-Lücke, Pitfall 1)"
    - "D-01: available_hours im Frontend-State trägt committed bereits (es kommt aus overall_available_hours des Backends)"
  artifacts:
    - path: "shifty-dioxus/src/state/weekly_overview.rs"
      provides: "committed_voluntary_hours-Feld + From-Mapping-Arm"
      contains: "committed_voluntary_hours: summary.committed_voluntary_hours"
  key_links:
    - from: "rest-types WeeklySummaryTO.committed_voluntary_hours"
      to: "shifty-dioxus state::weekly_overview::WeeklySummary.committed_voluntary_hours"
      via: "impl From<&WeeklySummaryTO> for WeeklySummary"
      pattern: "committed_voluntary_hours: summary.committed_voluntary_hours"
---

<objective>
Frontend-State-Wave: Das in Plan 01 ans TO gefädelte `committed_voluntary_hours` durch die zweite Boundary (TO → State) führen — Feld auf der Frontend-`WeeklySummary`-Struct + `From<&WeeklySummaryTO>`-Mapping-Arm + Roundtrip-Test + WASM-Build-Gate.

Purpose: Diese Boundary ist die klassische Omission-Lücke (Pitfall 1): rustc erzwingt nur, dass das Feld im Struct-Literal *vorkommt*, NICHT dass es korrekt befüllt wird. Ein `Default::default()` würde kompilieren und die UI zeigte stumm `0.00`. Der WASM-Build-Gate beweist hier, dass TO (Plan 01) und State synchron sind.

Output: Neues State-Feld + Mapping-Arm + Roundtrip-Test + grüner WASM-Build.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located mit git). GSD-Auto-Commit ist deaktiviert. Der EXECUTOR committet NICHT selbst — der User committet manuell via jj. NIEMALS `git commit`/`git add`/`git checkout`.
</vcs_jj_only>
</execution_context>

<context>
@.planning/phases/16-jahresansicht-display/16-CONTEXT.md
@.planning/phases/16-jahresansicht-display/16-PATTERNS.md

<critical_facts>
- Depends_on 16-01: das `WeeklySummaryTO.committed_voluntary_hours`-Feld MUSS in `rest-types` existieren (Plan 01 Task 2), sonst kompiliert der Mapping-Arm nicht.
- `available_hours` im Frontend-State wird aus `summary.overall_available_hours` gesetzt (Z.33) — nach D-01 (Plan 01) trägt es committed bereits AUTOMATISCH. KEINE Änderung an der `available_hours`-Zeile nötig.
- Hilfsmethoden `monday_date()`/`sunday_date()` (Z.65-72) bleiben unverändert.
- WASM-Build-Gate auf NixOS via `nix develop`: `cargo build --target wasm32-unknown-unknown` aus `shifty-dioxus/`.
</critical_facts>

<interfaces>
IST `shifty-dioxus/src/state/weekly_overview.rs:11-27` (Struct) + `:29-63` (From). Analog: das `volunteer_hours`-Feld (Z.18) + Mapping-Arm (Z.37).
```rust
#[derive(Debug, Clone, PartialEq)]
pub struct WeeklySummary {
    pub week: u8,
    pub year: u32,
    pub available_hours: f32,       // ← aus summary.overall_available_hours (Z.33) — trägt committed nach D-01 automatisch
    pub required_hours: f32,
    pub paid_hours: f32,
    pub volunteer_hours: f32,       // ← Analog für das neue Feld
    // ... monday_available_hours ... sales_person_absences
}

impl From<&WeeklySummaryTO> for WeeklySummary {
    fn from(summary: &WeeklySummaryTO) -> Self {
        Self {
            // ...
            volunteer_hours: summary.volunteer_hours,   // ← Z.37, Analog
            // ...
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: committed_voluntary_hours auf Frontend-WeeklySummary + From-Mapping + Roundtrip-Test</name>
  <files>shifty-dioxus/src/state/weekly_overview.rs</files>
  <read_first>
    - shifty-dioxus/src/state/weekly_overview.rs:11-72 (Struct + From-Impl + Hilfsmethoden; Analog `volunteer_hours` Z.18/Z.37; `available_hours`-Zuweisung Z.33)
    - .planning/phases/16-jahresansicht-display/16-PATTERNS.md § „shifty-dioxus/src/state/weekly_overview.rs" (Z.126-159; Pitfall-1-Warnung)
  </read_first>
  <behavior>
    - Test (From-Roundtrip, gegen Pitfall 1): Konstruiere eine `WeeklySummaryTO` mit `committed_voluntary_hours = 5.0`, mappe via `WeeklySummary::from(&to)`, asserte `(ws.committed_voluntary_hours - 5.0).abs() < f32::EPSILON`.
    - Test: `available_hours` des gemappten State == `to.overall_available_hours` (bestätigt, dass available committed via Backend-D-01 trägt, ohne Frontend-Zusatzlogik).
  </behavior>
  <action>
    In `shifty-dioxus/src/state/weekly_overview.rs`:

    (1) Struct-Feld nach Z.18 (`pub volunteer_hours: f32,`) hinzufügen:
    ```rust
    pub volunteer_hours: f32,
    pub committed_voluntary_hours: f32,
    ```

    (2) Mapping-Arm im `impl From<&WeeklySummaryTO> for WeeklySummary` nach Z.37 (`volunteer_hours: summary.volunteer_hours,`) hinzufügen — exakt analog, KEIN `Default::default()`:
    ```rust
    volunteer_hours: summary.volunteer_hours,
    committed_voluntary_hours: summary.committed_voluntary_hours,
    ```

    Die `available_hours: summary.overall_available_hours`-Zeile (Z.33) NICHT ändern — sie trägt committed nach D-01 (Plan 01) bereits. Hilfsmethoden `monday_date`/`sunday_date` (Z.65-72) NICHT ändern.

    (3) From-Roundtrip-Test im `#[cfg(test)]`-Modul dieser Datei ergänzen (falls noch keins existiert, ein minimales `mod tests` anlegen). Der Test baut ein `WeeklySummaryTO`-Literal mit `committed_voluntary_hours: 5.0` (alle anderen Felder mit Dummy-Werten), mappt via `WeeklySummary::from(&to)` und asserted das committed-Feld sowie `available_hours == to.overall_available_hours`.
  </action>
  <verify>
    <automated>cargo test -p shifty-dioxus weekly_overview</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "committed_voluntary_hours" shifty-dioxus/src/state/weekly_overview.rs` enthält genau (mindestens) den Struct-Feld-Eintrag, den Mapping-Arm `committed_voluntary_hours: summary.committed_voluntary_hours` UND den Roundtrip-Test.
    - `grep -c "Default::default()" shifty-dioxus/src/state/weekly_overview.rs` zeigt KEINEN neuen `Default::default()` für committed (Pitfall-1-Guard).
    - `cargo test -p shifty-dioxus weekly_overview` grün; From-Roundtrip-Test enthalten und grün.
  </acceptance_criteria>
  <done>Frontend-State trägt committed_voluntary_hours, From mappt es korrekt (kein Default), Roundtrip-Test grün.</done>
</task>

<task type="auto">
  <name>Task 2: WASM-Build-Gate — TO/State-Synchronität beweisen</name>
  <files>shifty-dioxus/src/state/weekly_overview.rs</files>
  <read_first>
    - .planning/phases/16-jahresansicht-display/16-VALIDATION.md § „WASM-Build-Gate"
    - CLAUDE.local.md (NixOS: `nix develop`, NICHT `nix-shell`)
  </read_first>
  <action>
    Hartes Wave-Gate: Aus `shifty-dioxus/` den WASM-Build ausführen. Auf NixOS via `nix develop`. Dieser Build kompiliert `rest-types` (default-features=false) gegen die neue State-Struct → beweist, dass das TO-Feld aus Plan 01 und das State-Feld synchron sind. Zusätzlich die Frontend-Suite grün halten.
  </action>
  <verify>
    <automated>cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --target wasm32-unknown-unknown` (aus `shifty-dioxus/`, unter `nix develop`) exit 0.
    - `cargo test -p shifty-dioxus` exit 0, 0 failures.
  </acceptance_criteria>
  <done>WASM-Build grün; Frontend-Suite grün; TO/State synchron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST-JSON → Frontend-Deserialisierung (`WeeklySummaryTO` → State) | bestehende client-seitige Deserialisierung eines bereits gegateten Read-Aggregats; kein neuer Input-Pfad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | `committed_voluntary_hours` im Frontend-State | accept | Read-only Aggregat (Σ pro Woche), kein PII; identische Vertraulichkeitsklasse wie das bereits angezeigte paid/volunteer-Aggregat. |

Keine High-Severity-Threats: rein additives Deserialisierungs-Feld, keine neue Auth-/Fetch-/Schreib-Logik. ASVS L1 nicht anwendbar (Client-Display).
</threat_model>

<verification>
- `cargo test -p shifty-dioxus weekly_overview` grün (From-Roundtrip, Pitfall 1 abgedeckt).
- WASM-Build-Gate exit 0 (TO/State synchron).
- `cargo test -p shifty-dioxus` grün.
</verification>

<success_criteria>
- Frontend-`WeeklySummary` trägt `committed_voluntary_hours` und mappt es korrekt aus dem TO (CVC-07c, keine Omission-Lücke).
- `available_hours` trägt committed automatisch via Backend-D-01 (keine Frontend-Zusatzlogik).
- WASM-Build grün.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-jahresansicht-display/16-02-SUMMARY.md` erstellen.
</output>
