---
phase: 17-contract-editor-unpaid-volunteer-path
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/state/employee_work_details.rs
  - shifty-dioxus/src/component/contract_modal.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [CVC-09]
tags: [D-01, D-02, D-04, D-07]

must_haves:
  truths:
    - "D-02: committed_voluntary ist ein Feld auf der Frontend-EmployeeWorkDetails-State-Struct und in BEIDEN TryFrom-Richtungen durchgezogen (TO→State Z.145-Block + State→TO Z.218-Hardcode ersetzt)"
    - "D-02: Open→Save-unverändert-Round-Trip bewahrt den Backend-committed_voluntary-Wert (EmployeeWorkDetails → TO → EmployeeWorkDetails ist wertgleich)"
    - "D-01: Das numerische committed_voluntary-Feld im contract_modal ist sichtbar/editierbar genau dann, wenn cap_planned_hours_to_expected == true ODER expected_hours == 0.0 (symmetrisch zur D-05-Gate-Bedingung)"
    - "D-04: KEIN neues is_paid-Control im contract_modal (is_paid bleibt allein in sales_person_details.rs)"
    - "CVC-08-Stil: neue i18n-Keys (CommittedVoluntaryLabel, EmployeesShowAll) in allen drei Locales De/En/Cs gepflegt, per Per-Locale-Reference-Matcher abgesichert"
  artifacts:
    - path: "shifty-dioxus/src/state/employee_work_details.rs"
      provides: "committed_voluntary-Feld auf State-Struct + beide TryFrom-Richtungen + blank_standard-Init + Round-Trip-Test"
      contains: "committed_voluntary"
    - path: "shifty-dioxus/src/component/contract_modal.rs"
      provides: "bedingt sichtbares numerisches committed_voluntary-TextInput + SSR-Sichtbarkeits-Tests"
      contains: "committed_voluntary"
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "Key::CommittedVoluntaryLabel + Key::EmployeesShowAll + Per-Locale-Matcher-Tests"
      contains: "CommittedVoluntaryLabel"
  key_links:
    - from: "shifty-dioxus/src/component/contract_modal.rs"
      to: "EmployeeWorkDetails.committed_voluntary"
      via: "show_committed-Bedingung + TextInput on_change dispatch"
      pattern: "committed_voluntary"
    - from: "shifty-dioxus/src/state/employee_work_details.rs (State→TO)"
      to: "EmployeeWorkDetailsTO.committed_voluntary"
      via: "details.committed_voluntary (ersetzt 0.0-Hardcode)"
      pattern: "committed_voluntary: details.committed_voluntary"
---

<objective>
Mache `committed_voluntary` im Vertrags-Editor (`contract_modal.rs`) als numerisches Feld editierbar und fädele es durch beide `TryFrom`-Richtungen der Frontend-State-Struct (`state/employee_work_details.rs`). Das Feld ist sichtbar genau dann, wenn `cap_planned_hours_to_expected == true || expected_hours == 0.0` (D-01, symmetrisch zur D-05-Reporting-Gate-Bedingung aus Plan 02). Lege außerdem die beiden für die Phase nötigen i18n-Keys in allen drei Locales an (`CommittedVoluntaryLabel` für dieses Plan, `EmployeesShowAll` für Plan 04 — beide hier, weil i18n-Dateien File-Ownership-exklusiv in diesem Plan liegen).

Purpose: CVC-09 — Open→Save-unverändert-Round-Trip muss den Backend-Wert bewahren. Heute steht in der State→TO-Richtung ein `committed_voluntary: 0.0`-Hardcode (Z.218, „Phase 17 scope"-Kommentar), und die TO→State-Richtung (Z.145-Block) mappt das Feld gar nicht — beide Lücken schließen. D-04: is_paid wird NICHT im contract_modal dupliziert.

Output: State-Struct-Feld + beide TryFrom-Patches + blank_standard-Init + bedingtes TextInput im Modal + 2 i18n-Keys × 3 Locales + SSR-Sichtbarkeitstests + Round-Trip-Test + Per-Locale-Matcher-Tests.

Scope-Grenzen:
- Der `<input type=date>`-Signal-Caveat gilt hier NICHT (numerisches Input).
- KEIN neues is_paid-Control (D-04).
- Der „alle"-Filter selbst (employees_list.rs + loader) ist Plan 04 — dieser Plan legt nur den `EmployeesShowAll`-i18n-Key an (i18n-File-Ownership).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-CONTEXT.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-RESEARCH.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-PATTERNS.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-UI-SPEC.md

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located mit git). Führe NIEMALS `git commit`/`git add` aus.
GSD-Auto-Commit ist deaktiviert — der User committed manuell via jj. Lass alle Änderungen
uncommitted im Working-Tree zurück; dokumentiere im SUMMARY, was committed werden muss.
</vcs_jj_only>

<build_commands>
Frontend-Tests + WASM-Gate aus dem shifty-dioxus/-Subordner (NixOS, nix develop des Workspace-Root):
- Frontend-Tests: `cd shifty-dioxus && nix develop ../ --command cargo test`
- WASM-Build-Gate (Pflicht-Abschluss): `cd shifty-dioxus && nix develop ../ --command cargo build --target wasm32-unknown-unknown`
</build_commands>

<interfaces>
<!-- State-Struct: committed_voluntary fehlt; nach cap_planned_hours_to_expected einfügen. -->

EmployeeWorkDetails-Struct (state/employee_work_details.rs Z.43-): trägt u.a.
  pub expected_hours: f32,
  pub dynamic: bool,
  pub cap_planned_hours_to_expected: bool,
  pub vacation_days: u8,
  pub created/deleted/version ...
HINWEIS: Struct nutzt `dynamic` (nicht is_dynamic); TO nutzt `is_dynamic`. NEU einfügen: `pub committed_voluntary: f32,` direkt nach `cap_planned_hours_to_expected`.

TryFrom<&EmployeeWorkDetailsTO> for EmployeeWorkDetails (Z.145-182): mappt heute NICHT committed_voluntary.
  Nach `cap_planned_hours_to_expected: details.cap_planned_hours_to_expected,` (Z.174) einfügen:
  `committed_voluntary: details.committed_voluntary,`

TryFrom<&EmployeeWorkDetails> for EmployeeWorkDetailsTO (Z.185-230): Hardcode Z.218.
  `committed_voluntary: 0.0,` → ersetzen durch `committed_voluntary: details.committed_voluntary,`
  (und den „Phase 17 scope"-Gap-Kommentar Z.213-217 entfernen/aktualisieren)

blank_standard(sales_person_id) (Z.72-99): EmployeeWorkDetails-Konstruktion — `committed_voluntary: 0.0,` ergänzen.

EmployeeWorkDetailsTO.committed_voluntary ist `f32` (rest-types lib.rs Z.613, Phase 14, bereits vorhanden).

contract_modal.rs Vorlage — expected_hours-TextInput (Z.296-319):
```rust
Field {
    label: expected_hours_label,
    TextInput {
        value: ImStr::from(details.expected_hours.to_string()),
        input_type: ImStr::from("number"),
        step: Some(ImStr::from("0.01")),
        disabled: read_only,
        on_change: {
            let details = details.clone();
            move |value: ImStr| {
                if read_only { return; }
                if let Ok(n) = value.as_str().parse::<f32>() {
                    let mut next = details.clone();
                    next.expected_hours = n;
                    dispatch(next);
                }
            }
        },
    }
}
```
Layout-Fluss: Wochentags-Pills → expected_hours+workdays (2-Spalten-Grid) → vacation_days (nur !read_only, Z.343-360) → Toggle-Felder dynamic + cap (Z.366-397). Einhängepunkt committed-Feld: NACH dem vacation_days-Block, VOR den Toggle-Feldern.
i18n-Label-Idiom (Z.138): `let committed_voluntary_label = ImStr::from(i18n.t(Key::CommittedVoluntaryLabel).as_ref());`

i18n bestehende Filter-Keys (mod.rs Z.264-267): ShowActive, ShowInactive, ShowPaid, ShowUnpaid (alle drei Locales gepflegt). NEU: CommittedVoluntaryLabel + EmployeesShowAll.
de.rs Z.415-418 / en.rs Z.368-371 / cs.rs Z.399-402 sind die add_text-Blöcke für die Show*-Keys.
Per-Locale-Matcher-Vorlage: i18n/mod.rs Z.813-843 (i18n_committed_keys_match_german_reference etc.).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: committed_voluntary durch State-Struct + beide TryFrom-Richtungen + Round-Trip-Test</name>
  <files>shifty-dioxus/src/state/employee_work_details.rs</files>
  <read_first>
    - shifty-dioxus/src/state/employee_work_details.rs (gesamte Datei: Struct Z.43-, blank_standard Z.72-99, TryFrom TO→State Z.145-182, TryFrom State→TO Z.185-230 inkl. Hardcode Z.218, bestehende Tests am Dateiende)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-PATTERNS.md § employee_work_details.rs (exakte Feld-Insertionspunkte)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-RESEARCH.md § Pitfall 1 + Pitfall 5
  </read_first>
  <behavior>
    - Test committed_voluntary_round_trip: EmployeeWorkDetails mit committed_voluntary=3.5 → TryInto EmployeeWorkDetailsTO → TryInto EmployeeWorkDetails ergibt committed_voluntary == 3.5 (beide Richtungen).
    - Test committed_voluntary_from_to_maps_field: EmployeeWorkDetailsTO mit committed_voluntary=7.25 → EmployeeWorkDetails hat committed_voluntary == 7.25 (TO→State-Richtung mappt das Feld).
  </behavior>
  <action>
    Drei Patches in state/employee_work_details.rs:
    1. Struct (Z.43-): `pub committed_voluntary: f32,` direkt nach `pub cap_planned_hours_to_expected: bool,` einfügen.
    2. blank_standard (Z.72-99): `committed_voluntary: 0.0,` in der EmployeeWorkDetails-Konstruktion ergänzen (Pitfall 5 — sonst Compile-Error).
    3. TryFrom<&EmployeeWorkDetailsTO> (Z.145-182): nach `cap_planned_hours_to_expected: details.cap_planned_hours_to_expected,` (Z.174) `committed_voluntary: details.committed_voluntary,` einfügen.
    4. TryFrom<&EmployeeWorkDetails> (Z.185-230): den `committed_voluntary: 0.0,`-Hardcode (Z.218) durch `committed_voluntary: details.committed_voluntary,` ersetzen; den „Phase 17 scope"-Gap-Kommentar (Z.213-217) entfernen oder durch einen knappen „D-02: threaded in Phase 17"-Kommentar ersetzen.

    Falls weitere EmployeeWorkDetails-Konstruktionen in der Datei existieren (z.B. in bestehenden Tests), die das neue Pflichtfeld brauchen: ergänze `committed_voluntary: 0.0` dort, damit der Build durchgeht.

    Füge zwei Tests am Dateiende hinzu (im bestehenden `#[cfg(test)] mod tests`-Block, falls vorhanden — sonst analog zu bestehenden TryFrom-Tests):
    - `committed_voluntary_round_trip` (Behavior 1)
    - `committed_voluntary_from_to_maps_field` (Behavior 2)
    Beide nutzen `TryInto`/`try_from` über eine vollständig konstruierte EmployeeWorkDetailsTO bzw. EmployeeWorkDetails (alle Pflichtfelder setzen). Verifiziere die Round-Trip-Wertgleichheit von committed_voluntary.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && nix develop ../ --command cargo test employee_work_details</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "committed_voluntary: details.committed_voluntary" shifty-dioxus/src/state/employee_work_details.rs` zeigt mindestens 2 (beide TryFrom-Richtungen)
    - `grep -c "committed_voluntary: 0.0" shifty-dioxus/src/state/employee_work_details.rs` zeigt KEINEN Treffer mehr in den TryFrom-Blöcken (Hardcode ersetzt; blank_standard-Init darf 0.0 sein, aber NICHT in TryFrom State→TO)
    - `grep -n "pub committed_voluntary: f32" shifty-dioxus/src/state/employee_work_details.rs` zeigt das neue Struct-Feld
    - `grep -n "committed_voluntary_round_trip\|committed_voluntary_from_to_maps_field" shifty-dioxus/src/state/employee_work_details.rs` zeigt beide Tests
    - `cd shifty-dioxus && nix develop ../ --command cargo test employee_work_details` läuft grün
  </acceptance_criteria>
  <done>committed_voluntary ist auf der State-Struct, in beiden TryFrom-Richtungen durchgezogen und per Round-Trip-Test wertbewahrend gepinnt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bedingtes committed_voluntary-TextInput im contract_modal + SSR-Sichtbarkeitstests</name>
  <files>shifty-dioxus/src/component/contract_modal.rs</files>
  <read_first>
    - shifty-dioxus/src/component/contract_modal.rs (gesamte ContractModalBody: i18n-Labels Z.135-145, expected_hours-TextInput Z.296-319, vacation_days-Block Z.343-360, cap-Toggle Z.366-397, dispatch Z.155-157, SSR-Test-Helper am Dateiende Z.409+)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-PATTERNS.md § contract_modal.rs (Pattern 2 + D-01-Sichtbarkeitsbedingung)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-UI-SPEC.md § Component Inventory + Interaction Contract + Verification Hooks
  </read_first>
  <behavior>
    - SSR-Test committed_visible_when_cap_true: ContractModalBody mit cap_planned_hours_to_expected=true rendert ein input[type=number][step=0.01] für committed_voluntary.
    - SSR-Test committed_visible_when_expected_hours_zero: cap=false, expected_hours=0.0 → committed-Feld sichtbar.
    - SSR-Test committed_hidden_when_no_cap_no_zero: cap=false, expected_hours=40.0 → committed-Feld NICHT gerendert.
  </behavior>
  <action>
    Füge in ContractModalBody NACH dem vacation_days-Block (~Z.360) und VOR den Toggle-Feldern (~Z.366) ein bedingtes `Field { TextInput }` für committed_voluntary ein. Vorlage: der expected_hours-TextInput (Z.296-319), 1:1 mit Feld-Name committed_voluntary.

    1. i18n-Label (bei den anderen Labels Z.135-145): `let committed_voluntary_label = ImStr::from(i18n.t(Key::CommittedVoluntaryLabel).as_ref());`
    2. Sichtbarkeits-Signal (D-01) direkt vor dem committed-Feld: `let show_committed = details.cap_planned_hours_to_expected || details.expected_hours == 0.0;`
    3. Bedingter Block:
    ```rust
    if show_committed {
        Field {
            label: committed_voluntary_label,
            TextInput {
                value: ImStr::from(details.committed_voluntary.to_string()),
                input_type: ImStr::from("number"),
                step: Some(ImStr::from("0.01")),
                disabled: read_only,
                on_change: {
                    let details = details.clone();
                    move |value: ImStr| {
                        if read_only { return; }
                        if let Ok(n) = value.as_str().parse::<f32>() {
                            let mut next = details.clone();
                            next.committed_voluntary = n;
                            dispatch(next);
                        }
                    }
                },
            }
        }
    }
    ```
    Passe die rsx!-Einbettung an die tatsächliche Struktur des Render-Baums an (das `if show_committed { ... }` muss als Element-Ausdruck im rsx!-Kontext stehen — orientiere dich am bestehenden `if !read_only { ... }`-Muster des vacation_days-Blocks Z.343).

    KEIN neues is_paid-Control (D-04). KEINE Änderung an der cap-Toggle-Logik selbst.

    Füge drei SSR-Snapshot-Tests am Dateiende hinzu (Test-Helper `render(comp)` mit `VirtualDom` + `dioxus_ssr::render`, Z.409+ als Vorlage). Konstruiere je eine ContractModalBody-Variante mit den drei Sichtbarkeits-Kombinationen und assert auf das Vorhandensein/Fehlen eines `type="number"`-committed-Inputs (z.B. via Substring-Match auf den gerenderten Label-Text `CommittedVoluntaryLabel` oder ein eindeutiges Markup-Fragment). Falls das Setup einen I18N-Locale-Pin braucht (RuntimeError außerhalb reactive scope), nutze das Hook-basierte pin-Pattern aus den Phase-8.5-Snapshot-Tests (use_hook).
    - `committed_visible_when_cap_true`
    - `committed_visible_when_expected_hours_zero`
    - `committed_hidden_when_no_cap_no_zero`
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && nix develop ../ --command cargo test committed</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "show_committed" shifty-dioxus/src/component/contract_modal.rs` zeigt die D-01-Bedingung `cap_planned_hours_to_expected || details.expected_hours == 0.0`
    - `grep -n "next.committed_voluntary = n" shifty-dioxus/src/component/contract_modal.rs` zeigt den Dispatch-Mutator
    - `grep -c "is_paid" shifty-dioxus/src/component/contract_modal.rs` zeigt KEINEN Treffer (D-04: kein is_paid-Control im Modal)
    - `grep -n "committed_visible_when_cap_true\|committed_visible_when_expected_hours_zero\|committed_hidden_when_no_cap_no_zero" shifty-dioxus/src/component/contract_modal.rs` zeigt alle drei Tests
    - `cd shifty-dioxus && nix develop ../ --command cargo test committed` läuft grün
  </acceptance_criteria>
  <done>committed_voluntary-TextInput erscheint genau bei cap || expected_hours==0; drei SSR-Tests pinnen alle Sichtbarkeits-Kombinationen; kein is_paid-Control im Modal.</done>
</task>

<task type="auto">
  <name>Task 3: i18n-Keys CommittedVoluntaryLabel + EmployeesShowAll in De/En/Cs + Per-Locale-Matcher</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs Z.260-270 (Key-Enum-Kontext: ShowActive/ShowInactive/ShowPaid/ShowUnpaid) + Z.813-843 (Per-Locale-Matcher-Test-Vorlage)
    - shifty-dioxus/src/i18n/de.rs Z.415-418, en.rs Z.368-371, cs.rs Z.399-402 (Show*-add_text-Blöcke als Einhängepunkt)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-UI-SPEC.md § Copywriting Contract (exakte Übersetzungen)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-RESEARCH.md § Pitfall 6 (Locale::En-statt-Locale::De-Bug)
  </read_first>
  <action>
    Lege genau zwei neue i18n-Keys an (UI-SPEC Copywriting Contract):
    1. mod.rs Key-Enum: `CommittedVoluntaryLabel,` und `EmployeesShowAll,` nach `ShowUnpaid` (Z.267) ergänzen.
    2. de.rs (mit `Locale::De` — NICHT En!): 
       `i18n.add_text(Locale::De, Key::CommittedVoluntaryLabel, "Freiwillige Zusage (h)");`
       `i18n.add_text(Locale::De, Key::EmployeesShowAll, "alle");`
    3. en.rs (mit `Locale::En`):
       `i18n.add_text(Locale::En, Key::CommittedVoluntaryLabel, "Voluntary Commitment (h)");`
       `i18n.add_text(Locale::En, Key::EmployeesShowAll, "all");`
    4. cs.rs (mit `Locale::Cs`):
       `i18n.add_text(Locale::Cs, Key::CommittedVoluntaryLabel, "Dobrovolný závazek (h)");`
       `i18n.add_text(Locale::Cs, Key::EmployeesShowAll, "vše");`

    KRITISCH (Pitfall 6): in de.rs IMMER `Locale::De`, in en.rs `Locale::En`, in cs.rs `Locale::Cs`. Nicht versehentlich das falsche Locale-Tag verwenden.

    Füge drei Per-Locale-Reference-Matcher-Tests in mod.rs hinzu (Vorlage Z.813-843), die beide neuen Keys gegen ihren Referenz-String pinnen:
    - `i18n_phase17_keys_match_german_reference` — assert CommittedVoluntaryLabel == "Freiwillige Zusage (h)" UND EmployeesShowAll == "alle"
    - `i18n_phase17_keys_match_english_reference` — "Voluntary Commitment (h)" / "all"
    - `i18n_phase17_keys_match_czech_reference` — "Dobrovolný závazek (h)" / "vše"

    Falls ein bestehender „all keys present in all locales"-Test existiert, der über den Key-Enum iteriert: stelle sicher, dass die neuen Keys ihn nicht fehlschlagen lassen (beide Keys in allen drei Locales gesetzt).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && nix develop ../ --command cargo test i18n_phase17</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CommittedVoluntaryLabel" shifty-dioxus/src/i18n/de.rs shifty-dioxus/src/i18n/en.rs shifty-dioxus/src/i18n/cs.rs` zeigt je 1 Treffer pro Datei
    - `grep -c "EmployeesShowAll" shifty-dioxus/src/i18n/de.rs shifty-dioxus/src/i18n/en.rs shifty-dioxus/src/i18n/cs.rs` zeigt je 1 Treffer pro Datei
    - `grep -n "Locale::De, Key::CommittedVoluntaryLabel" shifty-dioxus/src/i18n/de.rs` (korrektes Locale-Tag) — analog en/cs
    - `grep -n "i18n_phase17_keys_match_german_reference\|i18n_phase17_keys_match_english_reference\|i18n_phase17_keys_match_czech_reference" shifty-dioxus/src/i18n/mod.rs` zeigt alle drei Matcher-Tests
    - `cd shifty-dioxus && nix develop ../ --command cargo test i18n_phase17` läuft grün
  </acceptance_criteria>
  <done>Beide Keys in allen drei Locales mit korrektem Locale-Tag; Per-Locale-Matcher pinnen die Referenz-Strings (Pitfall-6-Guard).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser-Number-Input → EmployeeWorkDetails-State | User-getippter String wird zu f32 geparst und in den State dispatched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering (NaN/Infinity via Input) | contract_modal committed_voluntary TextInput | mitigate | `if let Ok(n) = value.as_str().parse::<f32>()` — ungültige/NaN-Strings werden nicht dispatched (stille Ignorierung, identisch expected_hours-Vorlage); kein NaN-Leak in den State |
| T-17-08 | Tampering (Round-Trip-Datenverlust) | TryFrom State↔TO | mitigate | Beide Richtungen explizit gepatcht; committed_voluntary_round_trip-Test pinnt Wertbewahrung; grep-Gate `committed_voluntary: 0.0` == 0 in TryFrom-Blöcken |
| T-17-09 | Spoofing (i18n-Locale-Swap zeigt falsche Sprache) | i18n de/en/cs | mitigate | Per-Locale-Reference-Matcher-Tests (i18n_phase17_keys_match_*) fangen Locale::En-statt-Locale::De-Bug |
</threat_model>

<verification>
- `cd shifty-dioxus && nix develop ../ --command cargo test` grün (Round-Trip + SSR-Sichtbarkeit + i18n-Matcher)
- `cd shifty-dioxus && nix develop ../ --command cargo build --target wasm32-unknown-unknown` Exit-Code 0 (WASM-Gate)
- grep bestätigt: beide TryFrom-Richtungen gepatcht, D-01-Sichtbarkeit korrekt, kein is_paid im Modal, beide i18n-Keys × 3 Locales
</verification>

<success_criteria>
- committed_voluntary ist auf State-Struct + in beiden TryFrom-Richtungen (Round-Trip wertbewahrend)
- Editor-Feld sichtbar genau bei cap || expected_hours==0 (3 SSR-Tests grün)
- Kein is_paid-Control im contract_modal (D-04)
- CommittedVoluntaryLabel + EmployeesShowAll in De/En/Cs (Per-Locale-Matcher grün)
- WASM-Build-Gate Exit-Code 0
</success_criteria>

<output>
After completion, create `.planning/phases/17-contract-editor-unpaid-volunteer-path/17-03-SUMMARY.md`.
Dokumentiere: exakte Insertionspunkte (Struct/TryFrom/Modal/i18n), gewähltes SSR-Test-Markup-Matching, Bestätigung WASM-Gate grün, dass EmployeesShowAll bereits hier angelegt (für Plan 04), jj-Commit-Hinweis.
</output>
