---
phase: 15-reporting-no-double-count-snapshot-bump-same-commit
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - service_impl/src/test/booking_information.rs
  - service_impl/src/test/mod.rs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CVC-04, CVC-05, CVC-06]
tags: [CVC-04, CVC-05, CVC-06, D-01, D-02, D-03, D-04, D-05]

must_haves:
  truths:
    - "D-02: maximal-coverage fixture suite for the two-band model — surplus over/under/boundary, multi_week_sum, multi_person (Formula B), cap_false, paid_capped, committed_zero_backward_compat — all with epsilon comparisons, never =="
    - "D-05: multi_person fixture pins committed_voluntary_hours=5.0 AND volunteer_hours=3.0 (total 8.0) via FORMULA B / two-band decomposition — supersedes the prior Formula-A=5.0 single-term pin"
    - "D-05: Band 2 per-person surplus fixtures — cvc04_band2_surplus (c=5,a=7 ⇒ committed 5, volunteer 2), cvc04_band2_pledge_covers (c=5,a=3 ⇒ committed 5, volunteer 0), cvc04_paid_capped_band2_zero (is_paid=true,cap,c=5 ⇒ committed 5, volunteer 0)"
    - "CVC-04: multi_week_sum fixture pins per-week-before-sum (W1 + W2 surplus summed), structurally excluding aggregate subtraction"
    - "CVC-06: cap_false fixture pins committed contribution 0.0 to both bands (cap=false person's pledge does not count; Band 2 == full actual)"
    - "CVC-06 / D-05: cvc06_committed_zero_backward_compat fixture proves committed=0 ⇒ volunteer_hours bit-identical to pre-v1.4 (Band 2 reduces to old computation)"
    - "D-01 / CVC-05: NO snapshot bump — CURRENT_SNAPSHOT_SCHEMA_VERSION stays 7, asserted by a regression test; no persisted value_type changes (committed_voluntary_hours is Achse-B year-view-only, never read by billing_period_report.rs)"
    - "CVC-05: ROADMAP Phase-15 SC#3 + REQUIREMENTS CVC-05 reconciled from 'bump 7→8' to an explicit no-bump justification (audit trail)"
  artifacts:
    - path: "service_impl/src/test/booking_information.rs"
      provides: "D-05 two-band fixture suite + version-unchanged regression test"
      min_lines: 90
      contains: "cvc04_multi_person"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CVC-05 reworded to no-bump justification"
      contains: "no-bump"
  key_links:
    - from: "service_impl/src/test/booking_information.rs"
      to: "service_impl/src/booking_information.rs::volunteer_surplus_above_committed"
      via: "direct pure-function unit tests of the per-person Band-2 surplus (FORMULA B)"
      pattern: "volunteer_surplus_above_committed"
    - from: "service_impl/src/test/booking_information.rs"
      to: "service_impl::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION"
      via: "regression assertion version == 7"
      pattern: "CURRENT_SNAPSHOT_SCHEMA_VERSION"
---

<objective>
Deliver maximal test coverage (D-02: "Alles was geht! Mehr Tests sind immer gut!") for the two-band `committed_voluntary_hours` / `volunteer_hours` computation under D-05 (FORMULA B), and reconcile the no-bump decision (D-01) across code + planning docs.

Two concerns:
1. **D-05 fixture suite** — fixtures pinning Band 1 (committed pledge sum) and Band 2 (per-person surplus `max(actual − committed, 0)`), the FORMULA-B multi-person aggregation (committed 5 / volunteer 3 / total 8), the cap=false gate, the paid-capped Band-2-zero case, and the committed=0 backward-compat lock proving `volunteer_hours` is bit-identical to pre-v1.4. Epsilon comparisons throughout.
2. **No-bump reconciliation (D-01 / CVC-05)** — assert `CURRENT_SNAPSHOT_SCHEMA_VERSION` stays 7 via a regression test, and reconcile the obsolete "bump 7→8" wording in ROADMAP Phase-15 SC#3 and REQUIREMENTS CVC-05 to an explicit no-bump justification (audit trail per D-01).

Purpose: The fixtures are the structural proof that the two bands never overlap (no double count, CVC-04) and that Band 2 reduces to today when committed=0. The no-bump reconciliation closes the documented contradiction between the source artifacts (ROADMAP/REQUIREMENTS still say "bump 7→8") and the locked decision D-01 (no bump).

Output: a new test module `service_impl/src/test/booking_information.rs` with the full D-05 fixture suite + version-unchanged test; ROADMAP + REQUIREMENTS reconciled.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-reporting-no-double-count-snapshot-bump-same-commit/15-CONTEXT.md
@.planning/phases/15-reporting-no-double-count-snapshot-bump-same-commit/15-RESEARCH.md
@.planning/phases/15-reporting-no-double-count-snapshot-bump-same-commit/15-VALIDATION.md
@.planning/phases/15-reporting-no-double-count-snapshot-bump-same-commit/15-01-PLAN.md

<vcs_jj_only>
This repo is jj-managed. GSD auto-commit is disabled. Do NOT run `git commit`/`git add`/`git stash` or any git mutation. The user commits manually via jj. There is NO commit task in this plan.
</vcs_jj_only>

<environment>
NixOS: run cargo inside `nix develop` if cargo is not on PATH. No DB migration. Pure in-memory tests.
</environment>

<interfaces>
From service_impl/src/booking_information.rs (created in Plan 15-01 — the pure helper under test):
```rust
// D-05 Band 2 per-person surplus = max(actual − committed, 0)
pub(crate) fn volunteer_surplus_above_committed(actual: f32, committed: f32) -> f32
```
(If Plan 15-01 also added an optional private per-person-committed helper, it is NOT `pub(crate)` — test the public `volunteer_surplus_above_committed` directly. Band 1 is a plain cap-gated sum; assert it by composing surplus + committed = max(committed, actual) where useful.)

From service_impl/src/billing_period_report.rs:74 (the version constant — MUST stay 7):
```rust
pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 7;
```

Test module registration: add `pub mod booking_information;` to service_impl/src/test/mod.rs (under `#[cfg(test)]`, alphabetical-ish with the existing modules).
</interfaces>

<d05_fixture_table>
## D-05 Fixtures (PINNED expected values) — two-band model

These test the PURE per-person surplus helper `volunteer_surplus_above_committed(actual, committed)` directly, and assert the two-band decomposition `committed_voluntary_hours` (Band 1) + `volunteer_hours` (Band 2) per the D-05 worked examples. Use epsilon `(a - b).abs() < 0.001`, never `==`.

Band invariant to encode in tests: per person/week `committed + surplus(actual, committed) = max(committed, actual)`.

| Test name | Person inputs (committed, actual; cap/is_paid) | Band 1 `committed_voluntary_hours` | Band 2 `volunteer_hours` | Pins |
|-----------|-----------------------------------------------|-----------------------------------|--------------------------|------|
| `cvc04_band2_surplus` | one is_paid=false person: c=5, a=7 (cap=true) | 5.0 | 2.0 | surplus = max(7−5,0) = 2; band sum = 7 |
| `cvc04_band2_pledge_covers` | c=5, a=3 (cap=true) | 5.0 | 0.0 | surplus floored at 0; band sum = max(5,3)=5 |
| `cvc04_boundary_equal` | c=5, a=5 | 5.0 | 0.0 | boundary committed==actual ⇒ surplus 0 |
| `cvc04_zero_actual` | c=5, a=0 (forward-looking pledge) | 5.0 | 0.0 | pledge with no actuals; Band 2 = 0 |
| `cvc06_cap_false_zero` | is_paid=false, cap=FALSE, c=5, a=7 (c pre-gated to 0.0) | 0.0 | 7.0 | cap=false ⇒ committed contributes 0 to both bands; Band 2 = full actual |
| `cvc04_multi_week_sum` | W1(c=5,a=7) + W2(c=5,a=3), same person | 10.0 | 2.0 | per-week-before-sum: B1 = 5+5=10, B2 = 2+0=2 |
| `cvc04_multi_person` | A(cap,c=5,a=0) + B(c=0,a=3) | 5.0 | 3.0 (total 8) | FORMULA B / two-band; supersedes Formula A=5 |
| `cvc04_paid_capped_band2_zero` | is_paid=true, cap, c=5 (actual_vol=0 in Achse B) | 5.0 | 0.0 | paid person: Band 1 only, Band 2 stays 0 |
| `cvc06_committed_zero_backward_compat` | c=0, several a (is_paid=false) | 0.0 | Σ a | committed=0 ⇒ Band 2 = Σ actual = pre-v1.4 |

Notes:
- `cvc06_cap_false_zero`: the cap gate happens at the call site (Plan 15-01 Task 2) for BOTH bands, so at the helper level a cap=false person presents `committed=0.0`. The fixture pins committed=0.0 contribution (Band 1) and `volunteer_surplus_above_committed(7.0, 0.0) = 7.0` (Band 2 = full actual). ADD a comment that the cap-gating-to-zero is enforced upstream by the per-row `.filter(cap_planned_hours_to_expected)`.
- `cvc04_multi_person`: encode FORMULA B explicitly in a comment: `// FORMULA B (per-person): A → B1+=5, B2+=surplus(0,5)=0; B(cap=false→c gated 0) → B1+=0, B2+=surplus(3,0)=3 ⇒ committed=5, volunteer=3, total=8`.
- `cvc04_multi_week_sum`: assert B1 totals 10.0 and B2 totals 2.0 by summing per-week values — the per-week `max`/surplus is taken BEFORE the year sum. (Replaces the prior single-term `12.0` Formula-A structural fixture, reinterpreted for two bands.)
- `cvc06_committed_zero_backward_compat`: e.g. actuals [2.0,4.0,1.0], each committed=0 → Band 2 = 7.0, and add an assertion that this equals the plain `Σ actual` (the pre-v1.4 `volunteer_hours`). Band 1 = 0.0.
- For each two-band row, also assert the no-double-count invariant where applicable: `committed_voluntary_hours + volunteer_hours == Σ_person max(committed_p, actual_p)`.
</d05_fixture_table>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 two-band fixture suite for committed_voluntary_hours + volunteer_hours surplus</name>
  <files>service_impl/src/test/booking_information.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/booking_information.rs (the pure helper volunteer_surplus_above_committed created in Plan 15-01)
    - service_impl/src/test/mod.rs (module registration list — you append one line)
    - service_impl/src/test/reporting_phase2_fixtures.rs (an existing fixture-style test module — match its style/imports for consistency)
    - 15-01-PLAN.md <resolved_semantic_decision> (FORMULA B; multi_person = committed 5 / volunteer 3 / total 8)
    - 15-VALIDATION.md "✅ Semantic decision RESOLVED — D-05" + its "Updated/added fixtures from D-05" table
  </read_first>
  <behavior>
    - All fixtures from <d05_fixture_table> pass with their pinned Band 1 + Band 2 values
    - Every assertion uses epsilon `(a - b).abs() < 0.001`, never `==` on f32
    - `cvc04_multi_person` proves committed=5.0 AND volunteer=3.0 (total 8.0) — FORMULA B
    - `cvc04_band2_surplus` proves surplus = max(7−5,0) = 2.0; `cvc04_band2_pledge_covers` proves floored 0.0
    - `cvc06_committed_zero_backward_compat` proves committed=0 ⇒ volunteer_hours == Σ actual (pre-v1.4)
    - no-double-count invariant `committed + surplus = max(committed, actual)` asserted per applicable fixture
  </behavior>
  <action>
    Step 1 — Create `service_impl/src/test/booking_information.rs`. Register it in `service_impl/src/test/mod.rs` by adding under the `#[cfg(test)]` list:
    ```rust
    #[cfg(test)]
    pub mod booking_information;
    ```

    Step 2 — Import the pure helper: `use crate::booking_information::volunteer_surplus_above_committed;`. Add an epsilon helper at the top of the module:
    ```rust
    fn approx(a: f32, b: f32) -> bool { (a - b).abs() < 0.001 }
    ```

    Step 3 — Implement ALL fixtures from <d05_fixture_table> as `#[test]` functions with the EXACT names and EXACT expected Band 1 / Band 2 values. For per-person cases compute Band 2 via `volunteer_surplus_above_committed(actual, committed)` and Band 1 as the (cap-gated) committed pledge; for multi-person/multi-week cases sum the per-person/per-week band values explicitly in the test body. Add the documented comments (FORMULA-B note on multi_person; upstream-cap-gating note on cap_false; per-week-before-sum note on multi_week_sum; backward-compat-equals-Σactual assertion).

    Step 4 — For each two-band fixture where both bands are present, ALSO assert the no-double-count invariant:
    ```rust
    // per person: committed + surplus(actual, committed) == max(committed, actual)
    assert!(approx(committed + volunteer_surplus_above_committed(actual, committed), committed.max(actual)));
    ```

    Step 5 — For `cvc06_committed_zero_backward_compat`, add the extra assertion:
    ```rust
    let actuals = [2.0_f32, 4.0, 1.0];
    let band2: f32 = actuals.iter().map(|&a| volunteer_surplus_above_committed(a, 0.0)).sum();
    let plain_sum: f32 = actuals.iter().sum();
    assert!(approx(band2, plain_sum)); // committed=0 ⇒ volunteer_hours bit-identical to pre-v1.4
    ```

    Do NOT add a TO mapping test (Phase 16). Do NOT modify reporting.rs / billing_period_report.rs.
  </action>
  <verify>
    <automated>cargo test -p service_impl cvc04_band2_surplus cvc04_band2_pledge_covers cvc04_boundary_equal cvc04_zero_actual cvc06_cap_false_zero cvc04_multi_week_sum cvc04_multi_person cvc04_paid_capped_band2_zero cvc06_committed_zero_backward_compat</automated>
    <automated>cargo test -p service_impl booking_information</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "#\[test\]" service_impl/src/test/booking_information.rs` shows >= 9
    - `grep -n "fn cvc04_multi_person" service_impl/src/test/booking_information.rs` exists AND the test asserts BOTH 5.0 (Band 1) and 3.0 (Band 2) — `grep -n "5.0\|5_f32" ...` and `grep -n "3.0\|3_f32" ...` present in that test
    - `grep -n "fn cvc04_band2_surplus" service_impl/src/test/booking_information.rs` exists and asserts 2.0 surplus
    - `grep -n "fn cvc06_committed_zero_backward_compat" service_impl/src/test/booking_information.rs` exists and asserts Band 2 == Σ actual
    - `grep -vn "^\s*//" service_impl/src/test/booking_information.rs | grep -c " == " ` shows 0 f32 `==` comparisons in non-comment lines (epsilon only)
    - `grep -n "pub mod booking_information" service_impl/src/test/mod.rs` shows the registration
    - all named tests pass: `cargo test -p service_impl cvc0` exits 0
  </acceptance_criteria>
  <done>All D-05 two-band fixtures pass with pinned values; multi_person = committed 5.0 / volunteer 3.0 / total 8.0 (FORMULA B), band2_surplus=2.0, pledge_covers=0.0, cap_false Band 2 = full actual, paid_capped Band 2 = 0, committed=0 backward-compat lock holds; no-double-count invariant asserted; epsilon comparisons only; module registered.</done>
</task>

<task type="auto">
  <name>Task 2: No-bump regression test + reconcile ROADMAP/REQUIREMENTS to no-bump justification (D-01/CVC-05)</name>
  <files>service_impl/src/test/booking_information.rs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - service_impl/src/billing_period_report.rs:55-74 (the version comment history block + `CURRENT_SNAPSHOT_SCHEMA_VERSION = 7`; note the existing precedent "Version BLEIBT 7 — v7 wurde nie deployed" for documenting a no-change rationale in this block)
    - .planning/ROADMAP.md (Phase 15 entry lines ~26-33 in the Phases list AND the detail block lines ~136-153 — both carry "snapshot bump 7→8" wording; SC#3 at line ~148)
    - .planning/REQUIREMENTS.md (CVC-05 at line 19 — "bump 7→8" wording; Traceability table line ~69)
    - 15-RESEARCH.md "No-Snapshot-Bump Justification (CVC-05 revised)" (the audit-trail justification to mirror)
    - 15-CONTEXT.md D-01 (the locked decision)
  </read_first>
  <action>
    Step 1 — Add a regression test in `service_impl/src/test/booking_information.rs` asserting the version is UNCHANGED (D-01 / CVC-05):
    ```rust
    #[test]
    fn snapshot_schema_version_unchanged_at_7() {
        // D-01 / CVC-05: Phase 15 touches NO persisted value_type — committed_voluntary_hours
        // (Band 1) and the reduced volunteer_hours (Band 2) are Achse-B (year-view) only and
        // are never read by billing_period_report.rs. Therefore the CLAUDE.md bump rule is
        // NOT triggered; version stays 7.
        assert_eq!(
            crate::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION,
            7
        );
    }
    ```
    (Confirm the import path resolves — `crate::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION`; it is `pub const`.)

    Step 2 — Reconcile ROADMAP.md (CVC-05 doc-reconciliation acceptance criterion — this is a DOC change, NOT a code change). In `.planning/ROADMAP.md`:
    - Phase-15 short entry (Phases list, ~line 26): change the title tail "+ snapshot bump (SAME commit)" and the "`CURRENT_SNAPSHOT_SCHEMA_VERSION` 7→8 atomar" clause to reflect NO bump. New tail: "(Achse B only, KEIN Snapshot-Bump)". Reword the bump clause to: "KEIN `CURRENT_SNAPSHOT_SCHEMA_VERSION`-Bump — `committed_voluntary_hours` (Band 1) + reduzierter `volunteer_hours` (Band 2) sind Achse-B-only (Jahresansicht), berühren keinen persistierten `BillingPeriodValueType`; Version bleibt 7 (D-01)."
    - Phase-15 detail block Goal (~line 138): replace the sentence "Atomar damit wird `CURRENT_SNAPSHOT_SCHEMA_VERSION` 7→8 gebumpt, weil sich Input/Computation des persistierten `BillingPeriodValueType::Volunteer` ändert." with a no-bump sentence: "KEIN Snapshot-Bump: die Zwei-Band-Dekomposition (D-05) fließt ausschließlich in Achse B (`get_weekly_summary`/Jahresansicht) und NICHT in den persistierten `BillingPeriodValueType::Volunteer` (der speist sich aus `reporting.rs`/Achse A). Version bleibt 7 (D-01; CLAUDE.md 'purely additive changes that do not touch the snapshot's value_types')."
    - Phase-15 Success Criterion #3 (~line 148): replace the "7→8 gebumpt … `// - v8:`-History-Eintrag" criterion with: "`CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt **7** (KEIN Bump) — begründet im Audit-Trail (Phase 15 berührt keinen persistierten `value_type`; `WeeklySummary` wird von `billing_period_report.rs` nie gelesen). Ein Regressionstest `snapshot_schema_version_unchanged_at_7` bestätigt die unveränderte Version (CVC-05)."

    Step 3 — Reconcile REQUIREMENTS.md. In `.planning/REQUIREMENTS.md`:
    - CVC-05 (line 19): replace the "wird 7→8 gebumpt …" text with: "**CVC-05** (revidiert per D-01 — KEIN Bump): `CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt **7**. Die Zwei-Band-Dekomposition (D-05: `committed_voluntary_hours` + reduzierter `volunteer_hours`) ist Achse-B-only (Jahresansicht-Verfügbarkeit) und speist keinen persistierten `BillingPeriodValueType`; `WeeklySummary` wird von `billing_period_report.rs` nie konsumiert → die CLAUDE.md-Bump-Regel ist NICHT ausgelöst (no-bump justification, Audit-Trail). Ein Regressionstest pinnt die unveränderte Version 7."
    - Keep the CVC-05 row in the Traceability table (still mapped to Phase 15, now Pending→satisfied-by-this-plan); no row deletion.

    Step 4 — Optionally (Discretion) add a one-line note to the `billing_period_report.rs` version comment block documenting WHY Phase 15 does NOT bump (mirrors the existing "Version BLEIBT 7 — v7 wurde nie deployed" precedent). This is a comment-only addition; if added, ensure `cargo build` still passes. Example line to append in the doc-comment block above the const: `/// - Phase 15 (committed_voluntary Zwei-Band): KEIN Bump — Achse-B-only, kein persistierter value_type berührt.`

    Run `cargo test --workspace` after the code change.
  </action>
  <verify>
    <automated>cargo test -p service_impl snapshot_schema_version_unchanged_at_7</automated>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 7" service_impl/src/billing_period_report.rs` STILL shows = 7 (no bump)
    - `grep -n "snapshot_schema_version_unchanged_at_7" service_impl/src/test/booking_information.rs` exists and the test passes
    - `grep -ni "7→8\|7->8" .planning/ROADMAP.md` returns NOTHING in the Phase-15 sections (bump wording removed; pre-v1.0/v1.3 history references elsewhere may legitimately remain — verify the Phase-15 short entry + detail block + SC#3 are reconciled)
    - `grep -ni "no-bump\|KEIN.*Bump\|bleibt.*7\|berührt keinen persistierten" .planning/REQUIREMENTS.md` shows the reconciled CVC-05 justification
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>Version stays 7 with a passing regression test; ROADMAP Phase-15 (short entry + detail Goal + SC#3) and REQUIREMENTS CVC-05 are reconciled from "bump 7→8" to an explicit no-bump justification with audit-trail rationale; full workspace tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only test code + planning-doc reconciliation. No production input surface, endpoint, auth path, or persistence is added or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering (data integrity) | two-band committed/surplus no-double-count invariant | mitigate | The D-05 fixture suite (this plan) is the structural mitigation: `cvc04_multi_person` (committed 5 / volunteer 3 / total 8, FORMULA B), `cvc04_band2_surplus`/`cvc04_band2_pledge_covers` (per-person surplus floor), and the per-fixture invariant `committed + surplus = max(committed, actual)` lock the no-double-count guarantee. |
| T-15-04 | Repudiation / audit integrity | snapshot schema version stamp | accept | The no-bump decision is documented (audit trail) and pinned by `snapshot_schema_version_unchanged_at_7`. No persisted value_type changes, so no snapshot-drift false-positive risk (Pitfall P2 is neutralized by D-01). |

**Honest assessment:** Pure test + documentation work on already-authorized internal calculation. No new threats introduced. The double-count correctness invariant under the two-band FORMULA-B model is the only integrity concern and is fully covered by the fixtures.
</threat_model>

<verification>
- `cargo test -p service_impl cvc0` exits 0 (all D-05 two-band fixtures).
- `cargo test -p service_impl snapshot_schema_version_unchanged_at_7` exits 0.
- `cargo test --workspace` exits 0.
- ROADMAP + REQUIREMENTS no longer claim "bump 7→8" for Phase 15.
- `CURRENT_SNAPSHOT_SCHEMA_VERSION = 7` unchanged.
</verification>

<success_criteria>
- All D-05 two-band fixtures pass with pinned values (CVC-04, CVC-06); multi_person = committed 5.0 / volunteer 3.0 / total 8.0 via FORMULA B.
- committed=0 backward-compat lock proves volunteer_hours bit-identical to pre-v1.4.
- Version stays 7, asserted by regression test (CVC-05 / D-01).
- ROADMAP Phase-15 + REQUIREMENTS CVC-05 reconciled to no-bump justification (CVC-05 doc-reconciliation criterion).
- Full workspace tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/15-reporting-no-double-count-snapshot-bump-same-commit/15-02-SUMMARY.md`. Document: the full D-05 fixture list with pinned Band 1 / Band 2 values, the no-bump justification as delivered, and the exact ROADMAP/REQUIREMENTS edits made.
</output>
