---
phase: 14-data-model-foundation-backend
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - service_impl/src/test/employee_work_details.rs
  - service_impl/src/reporting.rs
autonomous: true
requirements: [CVC-01, CVC-02, CVC-03]
tags: [CVC-01, CVC-02, CVC-03, D-OVERLAP-AGG, committed_voluntary]

must_haves:
  truths:
    - "CVC-01: ein erweiterter employee_work_details-Integrationstest verifiziert einen FRAKTIONALEN Open->Save->Reload-Round-Trip von committed_voluntary (z.B. 2.5 bleibt unveraendert durch Entity<->Service<->TO)"
    - "CVC-02: ein Test ueber den Update/Rotate-Struct-Update-Spread verifiziert, dass committed_voluntary mitgefuehrt und der NEUE (nicht der stale geladene) Wert an die DAO durchgereicht wird (Carry-Forward, kein silent Default-Reset)"
    - "CVC-03 / D-OVERLAP-AGG = SUM: die Aggregation bei zwei ueberlappenden aktiven EmployeeWorkDetails-Rows in derselben ISO-Woche ist als SUM definiert (find_working_hours_for_calendar_week(...).map(|wh| wh.committed_voluntary).sum()) und durch einen Test gepinnt (5h + 5h -> 10h); das Boolean-.any()-Pattern des Cap-Flags wird NICHT kopiert"
  artifacts:
    - path: "service_impl/src/test/employee_work_details.rs"
      provides: "Round-Trip-Test (CVC-01), Carry-Forward-Test (CVC-02), SUM-Aggregations-Test (CVC-03)"
      contains: "committed_voluntary"
    - path: "service_impl/src/reporting.rs"
      provides: "SUM-Aggregations-Semantik fuer committed_voluntary (getesteter Helper, Claude's Discretion)"
      contains: "committed_voluntary"
  key_links:
    - from: "service_impl/src/test/employee_work_details.rs Carry-Forward-Test"
      to: "dao.expect_update().with(function(|e| e.committed_voluntary == neuer Wert))"
      via: "mockall-Assertion auf den durchgereichten Entity-Wert"
      pattern: "committed_voluntary"
    - from: "SUM-Test"
      to: "find_working_hours_for_calendar_week + .map().sum()"
      via: "zwei ueberlappende Rows in derselben ISO-Woche"
      pattern: "\\.map\\(\\|wh\\| wh\\.committed_voluntary\\)\\.sum"
---

<objective>
Pinnt die drei testbaren Semantiken des `committed_voluntary`-Feldes, die Plan 14-01 end-to-end gefaedelt hat:
1. **CVC-01 Round-Trip:** ein FRAKTIONALER Wert (z.B. `2.5`) ueberlebt `Entity` -> Service -> `TryFrom`/`From` -> `EmployeeWorkDetailsTO` -> zurueck unveraendert (Float-Vergleich via Epsilon).
2. **CVC-02 Carry-Forward:** beim Update/Rotate-Pfad wird der NEUE `committed_voluntary` (nicht der per `find_by_id` stale geladene) an die DAO durchgereicht — Regression-Lock gegen silent Default-Reset.
3. **CVC-03 / D-OVERLAP-AGG = SUM:** zwei ueberlappende aktive Rows in derselben ISO-Woche aggregieren per **SUM** (`5h + 5h -> 10h`). Die Semantik wird via `find_working_hours_for_calendar_week(...).map(|wh| wh.committed_voluntary).sum()` definiert und per Test gepinnt — auch wenn in Phase 14 KEIN Produktions-Read-Site existiert (Feld inert). Das Boolean-`.any()`-Pattern des Cap-Flags generalisiert NICHT auf einen numerischen Wert und wird explizit NICHT kopiert.

Purpose: SC#1-4 von Phase 14 verlangen, dass diese Semantiken definiert + per Test gepinnt sind, bevor Phase 15 sie an einem Produktions-Read-Site liest.
Output: 3 neue/erweiterte Tests gruen; `cargo test --workspace` gruen.

**Claude's Discretion (per CONTEXT):** Ob die SUM-Aggregation als wiederverwendbarer Accessor (`committed_voluntary_for_calendar_week`) in `reporting.rs` eingefuehrt oder nur als getestete, dokumentierte Semantik gepinnt wird — Empfehlung: einen kleinen freistehenden Helper in `reporting.rs` analog `find_working_hours_for_calendar_week` definieren, damit Phase 15 ihn direkt konsumieren kann; SC#4 verlangt nur die definierte + getestete Semantik, KEINEN Produktions-Reader.

**NICHT in Scope:** Produktions-Read-Site fuer die Aggregation (Phase 15), Reporting-Berechnungs-Einrechnung, Snapshot-Bump, Frontend.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<vcs_jj_only>
Dieses Repo ist **jj-managed** (co-located git). NIEMALS `git commit`/`git add` aufrufen — auch nicht als Fallback. GSD-Auto-Commit ist AUS; der User committed manuell via jj. Erzeuge KEINE Commits.
</vcs_jj_only>

<build_environment>
**NixOS:** JEDER `cargo`-Befehl als `nix develop --command <cmd>`. Diese Plan-Tasks benoetigen KEINE DB-Operation (Tests laufen mockall-/in-memory-basiert). KEIN `sqlx database reset`.
</build_environment>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-data-model-foundation-backend/14-CONTEXT.md
@.planning/phases/14-data-model-foundation-backend/14-PATTERNS.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Vom Executor direkt zu nutzen — keine Codebase-Exploration noetig. -->

Test-Fixture (service_impl/src/test/employee_work_details.rs, nach 14-01):
```rust
// Bestehend (Z.54 ff.) — nach 14-01 traegt EmployeeWorkDetailsEntity zusaetzlich `committed_voluntary: f32`:
fn entity_with_cap(id: Uuid, version: Uuid, cap: bool) -> EmployeeWorkDetailsEntity { ... }
// committed_voluntary MUSS im Fixture-Init gesetzt sein (sonst Compile-Error: unvollstaendige Struct-Init).
// Empfehlung: Helper um Parameter erweitern, z.B. entity_with_cap_and_committed(id, version, cap, committed: f32).
```

Carry-Forward-Test-Analog (service_impl/src/test/employee_work_details.rs Z.86-143):
```rust
// update_propagates_cap_planned_hours_flag_to_dao — die line-fuer-line-Vorlage.
// mockall-Kern: dao.expect_update().with(function(|e: &EmployeeWorkDetailsEntity| <pred>), always(), always()).returning(...)
// find_by_id liefert den ALTEN Wert, Input flippt, Assertion prueft den NEUEN durchgereichten Wert.
```

Aggregations-Selektion (service_impl/src/reporting.rs Z.77-86):
```rust
pub fn find_working_hours_for_calendar_week(
    working_hours: &[EmployeeWorkDetails],
    year: u32,
    week: u8,
) -> impl Iterator<Item = &EmployeeWorkDetails> {
    working_hours.iter().filter(move |wh| {
        (year, week) >= (wh.from_year, wh.from_calendar_week)
            && (year, week) <= (wh.to_year, wh.to_calendar_week)
    })
}
```

SUM-Praezedenz (reporting.rs Z.240-254 — expected_hours .fold acc+a); ANTI-Pattern NICHT kopieren (Z.264-265 cap .any()).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SUM-Aggregations-Semantik (CVC-03 / D-OVERLAP-AGG) + Test</name>
  <read_first>
    - service_impl/src/reporting.rs Z.77-86 (find_working_hours_for_calendar_week), Z.240-254 (expected_hours .fold SUM-Praezedenz), Z.264-265 (cap .any() — NICHT kopieren)
    - .planning/phases/14-data-model-foundation-backend/14-PATTERNS.md Site 7 + 14-CONTEXT.md D-OVERLAP-AGG (Z.20-22)
  </read_first>
  <behavior>
    - SUM-Test: zwei EmployeeWorkDetails-Rows, beide aktiv in derselben ISO-Woche (z.B. KW 10 / 2026), committed_voluntary = 5.0 und 5.0 -> Aggregat = 10.0 (Epsilon-Vergleich)
    - Single-Row-Fall: eine aktive Row mit committed_voluntary = 5.0 -> Aggregat = 5.0
    - Keine aktive Row in der Woche -> Aggregat = 0.0 (leere .sum())
  </behavior>
  <action>
Definiere die SUM-Semantik (D-OVERLAP-AGG = SUM) als kleinen freistehenden Helper in `service_impl/src/reporting.rs`, analog `find_working_hours_for_calendar_week`. Verwende die GLEICHE Selektion + den SUM-Reduktions-Stil von `expected_hours` (`.fold` / `.sum`), NICHT das Boolean-`.any()`-Pattern des Cap-Flags:
```rust
pub fn committed_voluntary_for_calendar_week(
    working_hours: &[EmployeeWorkDetails],
    year: u32,
    week: u8,
) -> f32 {
    find_working_hours_for_calendar_week(working_hours, year, week)
        .map(|wh| wh.committed_voluntary)
        .sum()
}
```
(Dokumentiere am Helper, dass es in Phase 14 KEINEN Produktions-Read-Site gibt — Phase 15 konsumiert ihn.)

Schreibe den Test (RED zuerst, falls TDD-Reihenfolge eingehalten wird) im `#[cfg(test)]`-Modul von `reporting.rs` (oder im bestehenden reporting-Test-Modul): zwei ueberlappende Rows in derselben ISO-Woche (`5.0 + 5.0 -> 10.0`), plus die Single-Row- und Empty-Faelle aus `<behavior>`. Float-Vergleich via `(actual - expected).abs() < f32::EPSILON`, NICHT `==`.
  </action>
  <verify>nix develop --command cargo test -p service_impl committed_voluntary exit 0</verify>
  <acceptance_criteria>
    - `service_impl/src/reporting.rs` enthaelt `committed_voluntary_for_calendar_week` mit `.map(|wh| wh.committed_voluntary).sum()` (pruefbar: `grep -c 'map(|wh| wh.committed_voluntary).sum' service_impl/src/reporting.rs` >= 1)
    - KEIN `.any(|wh| ... committed_voluntary ...)` (das Bool-Anti-Pattern wurde NICHT auf das numerische Feld kopiert)
    - Ein Test pinnt `5.0 + 5.0 -> 10.0` fuer zwei ueberlappende Rows derselben ISO-Woche
    - `nix develop --command cargo test -p service_impl committed_voluntary` exit 0
  </acceptance_criteria>
  <done>SUM-Aggregations-Semantik als getesteter Helper definiert (kein Produktions-Reader); 5h+5h->10h gepinnt; .any()-Anti-Pattern vermieden.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Round-Trip-Test (CVC-01) + Carry-Forward-Test (CVC-02)</name>
  <read_first>
    - service_impl/src/test/employee_work_details.rs (Z.54-80 entity_with_cap Fixture; Z.86-143 update_propagates_cap_planned_hours_flag_to_dao — Carry-Forward-Vorlage)
    - service_impl/src/employee_work_details.rs (Z.228-248 Update/Rotate-Spread — der Pfad unter Test, inkl. der in 14-01 gesetzten committed_voluntary-Zeile)
    - .planning/phases/14-data-model-foundation-backend/14-PATTERNS.md Site 8 (Test-Anker)
  </read_first>
  <behavior>
    - Round-Trip (CVC-01): committed_voluntary = 2.5 ueberlebt Entity -> Service-Struct -> EmployeeWorkDetailsTO -> zurueck unveraendert (Epsilon)
    - Carry-Forward (CVC-02): find_by_id liefert alte Row (committed_voluntary = 0.0), Update-Input setzt 2.5; die an dao.expect_update durchgereichte Entity traegt 2.5 (NICHT 0.0)
  </behavior>
  <action>
**Fixture-Anpassung (Pflicht, sonst Compile-Error):** Ergaenze `entity_with_cap` (Z.54 ff.) um `committed_voluntary` im Struct-Init — entweder fester Wert (`committed_voluntary: 0.0`) oder, empfohlen, erweitere die Signatur zu `entity_with_cap_and_committed(id, version, cap, committed: f32)` (bzw. fuege ein zweites Fixture hinzu), damit der fraktionale Wert (`2.5`) gesetzt werden kann.

**(a) Round-Trip-Test (CVC-01):** Konstruiere ein `EmployeeWorkDetails` (Service-Struct) mit `committed_voluntary = 2.5`, konvertiere -> `EmployeeWorkDetailsTO` (`From<&EmployeeWorkDetails>`) -> zurueck `From<&EmployeeWorkDetailsTO> for EmployeeWorkDetails`, assertiere `(roundtrip.committed_voluntary - 2.5).abs() < f32::EPSILON`. (Falls das Test-Modul keinen Zugriff auf die cfg-gated rest-types-Konversionen hat, teste die Entity<->Service-Richtung analog und ergaenze die TO-Richtung im rest-types-Crate-Test — Hauptziel: ein fraktionaler Wert ueberlebt die Konversionskette unveraendert.)

**(b) Carry-Forward-Test (CVC-02)** — line-fuer-line nach `update_propagates_cap_planned_hours_flag_to_dao` (Z.86-143), nur mit dem numerischen Feld:
```rust
dao.expect_update()
    .with(
        function(|e: &EmployeeWorkDetailsEntity| (e.committed_voluntary - 2.5).abs() < f32::EPSILON),
        always(),
        always(),
    )
    .returning(|_, _, _| Ok(()));
```
Setup: `find_by_id` liefert die alte Row mit `committed_voluntary = 0.0`; der Update-Input traegt `2.5`; die Assertion beweist, dass der NEUE Wert (nicht der stale geladene `0.0`) an die DAO durchgereicht wird — das ist exakt die CVC-02-Carry-Forward-Garantie, die die Spread-Zeile aus 14-01 absichert. Epsilon-Vergleich, NICHT `==`.
  </action>
  <verify>nix develop --command cargo test -p service_impl employee_work_details exit 0</verify>
  <acceptance_criteria>
    - `entity_with_cap`/Fixture initialisiert `committed_voluntary` (kein Compile-Error durch unvollstaendige Struct-Init): `nix develop --command cargo test -p service_impl employee_work_details` kompiliert + exit 0
    - Round-Trip-Test pinnt `committed_voluntary = 2.5` als unveraendert durch die Konversionskette
    - Carry-Forward-Test enthaelt `dao.expect_update().with(function(|e: &EmployeeWorkDetailsEntity| (e.committed_voluntary - 2.5).abs() < f32::EPSILON)` (oder semantisch identische Epsilon-Assertion auf 2.5)
    - `nix develop --command cargo test --workspace` exit 0 (gesamte Suite gruen, kein Regress)
  </acceptance_criteria>
  <done>Fixture um committed_voluntary erweitert; Round-Trip-Test (2.5 unveraendert) + Carry-Forward-Test (NEUER Wert durchgereicht, nicht stale 0.0) gruen; Workspace-Suite gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Keine neue Trust-Boundary. Dieser Plan fuegt ausschliesslich Test-Code (`#[cfg(test)]`) und einen reinen, seiteneffektfreien Aggregations-Helper hinzu; kein neuer Endpoint, kein User-Input-Pfad, keine Persistenz-Aenderung.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | `committed_voluntary_for_calendar_week` Aggregations-Helper | accept | Reine Funktion ohne I/O/Persistenz; in Phase 14 kein Produktions-Reader (Feld inert); arithmetische SUM ueber bereits geladene, vertrauenswuerdige In-Memory-Rows. Kein Trust-Boundary-Crossing. |

Kein HIGH-Threat. Test- + Pure-Helper-Surface; keine Auth/Authz/Input-Aenderung.
</threat_model>

<verification>
- `nix develop --command cargo test --workspace` exit 0 (alle 3 neuen Semantiken gruen, keine Regression)
- SUM-Semantik gepinnt (5h+5h->10h); `.any()`-Anti-Pattern vermieden
- Round-Trip (2.5 unveraendert) + Carry-Forward (NEUER Wert durchgereicht) gepinnt
- Keine git-Commits, keine DB-Operation
</verification>

<success_criteria>
- CVC-01: fraktionaler Open->Save->Reload-Round-Trip per Test verifiziert
- CVC-02: Carry-Forward ueber den Update/Rotate-Spread per Test verifiziert (kein silent Reset)
- CVC-03 / D-OVERLAP-AGG: SUM-Semantik definiert + per Test gepinnt (5h+5h->10h)
- `cargo test --workspace` gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/14-data-model-foundation-backend/14-02-SUMMARY.md` erstellen (Template: @.claude/get-shit-done/templates/summary.md).
</output>
