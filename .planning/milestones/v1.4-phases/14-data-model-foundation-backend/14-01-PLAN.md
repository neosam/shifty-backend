---
phase: 14-data-model-foundation-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/<ts>_add-committed-voluntary-to-employee-work-details.sql
  - .sqlx/
  - dao/src/employee_work_details.rs
  - dao_impl_sqlite/src/employee_work_details.rs
  - service/src/employee_work_details.rs
  - service_impl/src/employee_work_details.rs
  - rest-types/src/lib.rs
autonomous: true
requirements: [CVC-01, CVC-02]
tags: [CVC-01, CVC-02, committed_voluntary, employee_work_details]

must_haves:
  truths:
    - "CVC-01: das zeit-versionierte Feld committed_voluntary: f32 existiert durchgaengig auf EmployeeWorkDetails ueber Migration -> DAO -> Service -> rest-types; .sqlx-Offline-Cache regeneriert; cargo check --workspace gruen; Bestandsdaten driftfrei (Default 0)"
    - "CVC-01: beide Konversionsrichtungen an jeder Boundary (DAO TryFrom, Service<->DAO, TO<->Service) sind durchgezogen; EmployeeWorkDetailsTO traegt committed_voluntary mit #[serde(default)]; KEIN REST/OpenAPI/ToSchema-Change"
    - "CVC-02: der Update/Rotate-Struct-Update-Spread in service_impl fuehrt committed_voluntary mit (entity.committed_voluntary = employee_work_details.committed_voluntary), kein stiller Reset auf Default"
  artifacts:
    - path: "migrations/sqlite/<ts>_add-committed-voluntary-to-employee-work-details.sql"
      provides: "additive ALTER TABLE ADD COLUMN committed_voluntary REAL NOT NULL DEFAULT 0"
      contains: "committed_voluntary REAL NOT NULL DEFAULT 0"
    - path: "dao/src/employee_work_details.rs"
      provides: "DAO-Trait-Entity-Feld committed_voluntary: f32"
      contains: "committed_voluntary: f32"
    - path: "dao_impl_sqlite/src/employee_work_details.rs"
      provides: "Row-Feld f64 + TryFrom as f32 + 4 SELECT + INSERT + UPDATE"
      contains: "committed_voluntary"
    - path: "service/src/employee_work_details.rs"
      provides: "Service-Struct-Feld + beide Konversionen (From<&Entity>, TryFrom<&EmployeeWorkDetails>)"
      contains: "committed_voluntary: f32"
    - path: "service_impl/src/employee_work_details.rs"
      provides: "Carry-Forward-Spread-Zeile (CVC-02)"
      contains: "entity.committed_voluntary = employee_work_details.committed_voluntary"
    - path: "rest-types/src/lib.rs"
      provides: "EmployeeWorkDetailsTO-Feld mit #[serde(default)] + beide From-Impls"
      contains: "committed_voluntary"
  key_links:
    - from: "dao_impl_sqlite/src/employee_work_details.rs (4 SELECT-Spaltenlisten + INSERT + UPDATE)"
      to: "Spalte committed_voluntary in employee_work_details"
      via: "SQLx compile-time query check gegen live DB + .sqlx-Cache"
      pattern: "committed_voluntary"
    - from: "service_impl/src/employee_work_details.rs update()"
      to: "DAO-Update mit dem neuen (nicht stale geladenen) Wert"
      via: "selektiver Feld-Copy nach find_by_id"
      pattern: "entity\\.committed_voluntary = employee_work_details\\.committed_voluntary"
---

<objective>
Faedelt das NEUE zeit-versionierte Feld `committed_voluntary: f32` (D-01 / Variante B — nur die freiwillige Zusage obendrauf, entkoppelt von `expected_hours`) durch JEDEN Backend-Layer des `EmployeeWorkDetails`-Aggregats: SQLite-Migration -> DAO-Trait + DAO-Impl (Row/Entity, `TryFrom`, 4 SELECT, INSERT, UPDATE) -> Service (Struct + beide Konversionen) -> `rest-types` (`EmployeeWorkDetailsTO` mit `#[serde(default)]`) -> Carry-Forward im Update/Rotate-Spread (CVC-02). Das Feld ist in dieser Phase **inert** — es persistiert und transportiert, wird aber **nirgends gelesen** (kein Reporting-/Display-Effekt).

**Leitprinzip:** `committed_voluntary` wird an JEDER Stelle dort eingefuegt, wo heute `cap_planned_hours_to_expected` steht (SAME position, SAME order). EINZIGE Divergenz: `cap...` ist Bool (`i64` in DB, `!= 0`-Coercion); `committed_voluntary` ist numerisch `REAL`/`f32` — kopiere die POSITION von `cap...`, aber den TYP/Cast von `expected_hours` (`f64` im Row-Struct, `as f32`/`as f64`-Casts, KEINE Bool-Coercion).

Purpose: Legt die Daten-Foundation, auf der Phase 15 (Reporting) liest und Phase 16/17 (Frontend) konsumiert/editiert.
Output: `committed_voluntary` end-to-end persistierbar + transportierbar; `cargo check --workspace` + `cargo test --workspace` gruen; `.sqlx`-Cache regeneriert.

**NICHT in Scope:** Frontend (Feld inert, Phasen 16/17). REST/OpenAPI/`utoipa`/`ToSchema` (serde-transparent). Snapshot-Bump (`CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt unveraendert — Feld inert, kein persistierter `value_type` aendert sich; Bump ist Phase 15). DI-Aenderung (`EmployeeWorkDetailsService` bleibt **Basic Service** — Feld ist Daten, keine Dependency). Tests (Plan 14-02).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<vcs_jj_only>
Dieses Repo ist **jj-managed** (co-located git). NIEMALS `git commit`/`git add` aufrufen — auch nicht als Fallback. GSD-Auto-Commit ist AUS (`commit_docs: false`); der User committed manuell via jj. Erzeuge KEINE Commits.
</vcs_jj_only>

<build_environment>
**NixOS:** `cargo`/`sqlx` sind NICHT auf dem nackten PATH. JEDER `cargo`/`sqlx`-Befehl MUSS als `nix develop --command <cmd>` laufen.
**DB-Migration ist additiv:** `nix develop --command sqlx migrate run` (oder die App laesst pending Migrationen laufen). **NIEMALS** `sqlx database reset`/`drop`/`truncate` — destruktiv, Dev-DB nicht recoverbar, braucht explizite User-Confirmation.
**Erster harter Compile-Gate:** Nach der Migration MUSS der `.sqlx`-Offline-Cache via `nix develop --command cargo sqlx prepare` regeneriert werden (SQLx compile-time query checking liest `.sqlx` + live DB-Schema).
</build_environment>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-data-model-foundation-backend/14-CONTEXT.md
@.planning/phases/14-data-model-foundation-backend/14-PATTERNS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive Migration + .sqlx-Regen (erster Compile-Gate)</name>
  <read_first>
    - migrations/sqlite/20260426120000_add-cap-flag-to-employee-work-details.sql (exakter Analog, NUR Typ-Divergenz INTEGER->REAL)
    - .planning/phases/14-data-model-foundation-backend/14-PATTERNS.md Site 1 (Migration)
    - CLAUDE.local.md + STATE.md "Constraints In Force" (jj + NixOS + additive Migration)
  </read_first>
  <action>
Erzeuge eine NEUE additive Migration. Lege die Datei via `nix develop --command sqlx migrate add committed-voluntary-to-employee-work-details` an (frischer Timestamp-Prefix) — falls `sqlx migrate add` im Environment nicht verfuegbar ist, erstelle die Datei direkt unter `migrations/sqlite/` mit einem Timestamp-Prefix, der NACH `20260426120000` sortiert (z.B. aktuelles Datum `YYYYMMDDHHMMSS`).

Exakter Datei-Inhalt (Typ-Divergenz REAL statt INTEGER, Default-Literal `0`):
```sql
ALTER TABLE employee_work_details
ADD COLUMN committed_voluntary REAL NOT NULL DEFAULT 0;
```

Wende die Migration additiv an:
```
nix develop --command sqlx migrate run --source migrations/sqlite
```
**NIEMALS** `sqlx database reset` — destruktiv. Falls die DB-URL/Setup fehlt, brich ab und melde es (NICHT mit reset "reparieren").

Regeneriere danach den `.sqlx`-Offline-Cache (erster harter Compile-Gate, da `query!`/`query_as!` compile-time-checked sind):
```
nix develop --command cargo sqlx prepare --workspace
```
(Falls `--workspace` nicht greift, fuehre `cargo sqlx prepare` im Crate `dao_impl_sqlite` aus.) Erwarte, dass dieser Schritt die DAO-Queries in Task 2 erst nach deren Aenderung vollstaendig validiert — fuehre `cargo sqlx prepare` daher am Ende erneut aus, nachdem Task 2 die SELECT/INSERT/UPDATE-Listen ergaenzt hat.
  </action>
  <verify>nix develop --command sqlx migrate info --source migrations/sqlite zeigt die neue Migration als applied</verify>
  <acceptance_criteria>
    - Genau eine neue Datei unter migrations/sqlite/ deren Name auf `_add-committed-voluntary-to-employee-work-details.sql` endet existiert
    - Diese Datei enthaelt die exakte Zeile `committed_voluntary REAL NOT NULL DEFAULT 0` (pruefbar: `grep -c 'committed_voluntary REAL NOT NULL DEFAULT 0' migrations/sqlite/*committed-voluntary*` >= 1)
    - KEIN Vorkommen von `database reset` / `drop` / `truncate` wurde ausgefuehrt
    - `.sqlx/`-Cache wurde regeneriert (nach Task 2) — Folge-Verifikation: `nix develop --command cargo check -p dao_impl_sqlite` exit 0
  </acceptance_criteria>
  <done>Additive Migration angelegt + via `sqlx migrate run` applied; `.sqlx`-Regen ausgefuehrt; keine destruktive DB-Operation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Field-Threading durch DAO-Trait + DAO-Impl + Service + rest-types + Carry-Forward</name>
  <read_first>
    - dao/src/employee_work_details.rs (Z.13 expected_hours: f32, Z.21-22 is_dynamic/cap_planned_hours_to_expected — Einfuege-Anker)
    - dao_impl_sqlite/src/employee_work_details.rs (Z.17 expected_hours: f64, Z.25-26 Row-Felder, Z.50 expected_hours-Cast, Z.62-63 TryFrom, Z.119/169/221/275 SELECT, Z.319-381 INSERT, Z.420-447 UPDATE — die cap-Praezedenz erscheint 12x)
    - service/src/employee_work_details.rs (Z.17 expected_hours: f32, Z.26 Struct-Feld, Z.56 From<&Entity>, Z.207 TryFrom<&EmployeeWorkDetails>)
    - service_impl/src/employee_work_details.rs (Z.228-248 selektiver Update-Spread — Carry-Forward-Anker Z.248)
    - rest-types/src/lib.rs (Z.609-611 TO-Struct, Z.650 From<&EmployeeWorkDetails>, Z.688 From<&EmployeeWorkDetailsTO>, Z.635/673 cfg-Gates)
    - .planning/phases/14-data-model-foundation-backend/14-PATTERNS.md Sites 2-6 + "Shared Patterns: Numeric-Field-Threading"
  </read_first>
  <action>
Fuege `committed_voluntary` an JEDER cap-Position ein. TYP-Regel: Row-Struct in `dao_impl_sqlite` nutzt `f64` (wie `expected_hours`), alle anderen Layer `f32`. Casts wie `expected_hours` (`as f32` beim Lesen, `as f64` beim Schreiben), NIEMALS Bool-Coercion (`!= 0` / `as i64`).

**(A) dao/src/employee_work_details.rs** — im `EmployeeWorkDetailsEntity`-Struct direkt nach `pub cap_planned_hours_to_expected: bool,`:
```rust
    pub committed_voluntary: f32,
```

**(B) dao_impl_sqlite/src/employee_work_details.rs** (mirror cap an allen 12 Positionen):
- Row-Struct `EmployeeWorkDetailsDb` nach `pub cap_planned_hours_to_expected: i64,`:
```rust
    pub committed_voluntary: f64,
```
- `TryFrom<&EmployeeWorkDetailsDb>` nach der `cap_planned_hours_to_expected: ... != 0,`-Zeile (Cast wie `expected_hours`, NICHT `!= 0`):
```rust
            committed_voluntary: working_hours.committed_voluntary as f32,
```
- 4x SELECT-Spaltenliste (`all`, `find_by_id`, `find_by_sales_person_id`, `find_for_week`) — `committed_voluntary,` direkt NACH `cap_planned_hours_to_expected,` in jeder Liste.
- INSERT: lokale Variable nach `let expected_hours = ...`-Stil: `let committed_voluntary = entity.committed_voluntary as f64;` + Spaltenliste `committed_voluntary,` nach `cap_planned_hours_to_expected,` + EIN weiterer `?`-Platzhalter im VALUES-Tupel + Binding `committed_voluntary,` nach `cap_planned_hours_to_expected,`.
- UPDATE: lokale Variable `let committed_voluntary = entity.committed_voluntary as f64;` + SET-Klausel `committed_voluntary = ?` (nach `cap_planned_hours_to_expected = ?`, mit Komma) + Binding `committed_voluntary,` (nach `cap_planned_hours_to_expected,`, VOR `id`).

**(C) service/src/employee_work_details.rs** (1:1 `f32`-Kopie, kein Cast):
- Struct `EmployeeWorkDetails` nach `pub cap_planned_hours_to_expected: bool,`: `pub committed_voluntary: f32,`
- `From<&Entity> for EmployeeWorkDetails` nach `cap_planned_hours_to_expected: working_hours.cap_planned_hours_to_expected,`: `committed_voluntary: working_hours.committed_voluntary,`
- `TryFrom<&EmployeeWorkDetails> for Entity` nach derselben cap-Zeile: `committed_voluntary: working_hours.committed_voluntary,`

**(D) rest-types/src/lib.rs** (KEIN ToSchema/utoipa hinzufuegen — Struct ist serde-transparent):
- `EmployeeWorkDetailsTO`-Struct nach dem `#[serde(default)] pub cap_planned_hours_to_expected: bool,` (mit eigenem `#[serde(default)]` fuer Wire-Backward-Compat):
```rust
    #[serde(default)]
    pub committed_voluntary: f32,
```
- `From<&EmployeeWorkDetails> for EmployeeWorkDetailsTO` (cfg-gated, NICHT entfernen) nach cap-Zeile: `committed_voluntary: working_hours.committed_voluntary,`
- `From<&EmployeeWorkDetailsTO> for EmployeeWorkDetails` (cfg-gated) nach cap-Zeile: `committed_voluntary: working_hours.committed_voluntary,`

**(E) CVC-02 Carry-Forward — service_impl/src/employee_work_details.rs** im selektiven Update-Spread (nach `entity.cap_planned_hours_to_expected = employee_work_details.cap_planned_hours_to_expected;`):
```rust
        entity.committed_voluntary = employee_work_details.committed_voluntary;
```
Das ist die EINZIGE compiler-ungeschuetzte Omission-Stelle (selektiver Spread) — Auslassung = silent Reset auf den stale geladenen Wert. PFLICHT, nicht optional.

Nach allen Edits: `.sqlx`-Cache erneut regenerieren (Task 1 Befehl) und `cargo check --workspace` gruen ziehen.
  </action>
  <verify>nix develop --command cargo check --workspace exit 0</verify>
  <acceptance_criteria>
    - `grep -c 'committed_voluntary' dao/src/employee_work_details.rs` >= 1 und enthaelt `committed_voluntary: f32`
    - `dao_impl_sqlite/src/employee_work_details.rs` enthaelt `committed_voluntary: f64` (Row), `committed_voluntary: working_hours.committed_voluntary as f32` (TryFrom) und mind. 6 weitere `committed_voluntary`-Vorkommen (4 SELECT + INSERT + UPDATE); KEIN `committed_voluntary` mit `!= 0` oder `as i64`
    - `service/src/employee_work_details.rs` enthaelt `committed_voluntary: f32` (Struct) und `committed_voluntary: working_hours.committed_voluntary` (>= 2x, beide Konversionen)
    - `rest-types/src/lib.rs` enthaelt `pub committed_voluntary: f32` mit vorangehendem `#[serde(default)]`; KEIN neues `ToSchema` an EmployeeWorkDetailsTO; `committed_voluntary: working_hours.committed_voluntary` (>= 2x)
    - `service_impl/src/employee_work_details.rs` enthaelt exakt die Zeile `entity.committed_voluntary = employee_work_details.committed_voluntary;`
    - `nix develop --command cargo check --workspace` exit 0
    - `nix develop --command cargo test --workspace` exit 0 (Bestandstests gruen — kein Regress)
    - Binary-Smoke (CLAUDE.md-Regel): `nix develop --command bash -c 'timeout 15 cargo run; rc=$?; test "$rc" -eq 124 -o "$rc" -eq 0'` — Backend linkt + startet sauber (Exit 124 = vom timeout beendet = Startup ok; Exit 0 falls sofort beendet)
    - `CURRENT_SNAPSHOT_SCHEMA_VERSION` ist UNVERAENDERT (`git diff` zeigt keine Aenderung an `service_impl/src/billing_period_report.rs` CURRENT_SNAPSHOT_SCHEMA_VERSION)
  </acceptance_criteria>
  <done>committed_voluntary durchgehend auf DAO-Trait, DAO-Impl (Row f64 + Cast f32 + 4 SELECT + INSERT + UPDATE), Service-Struct + beide Konversionen, EmployeeWorkDetailsTO (mit #[serde(default)], kein ToSchema) + beide From-Impls, sowie die Carry-Forward-Spread-Zeile. Workspace baut + Bestandstests gruen. Kein Snapshot-Bump, kein OpenAPI/DI-Change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> EmployeeWorkDetails-REST | Untrusted JSON ueber bestehenden, unveraenderten Endpoint; serde-Deserialisierung von `EmployeeWorkDetailsTO` |
| service -> SQLite | Persistenz via parametrisierte SQLx-Queries (compile-time-checked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | additive Migration `ADD COLUMN committed_voluntary REAL NOT NULL DEFAULT 0` | accept | Non-destruktiv, additiv via `sqlx migrate run`; `NOT NULL DEFAULT 0` haelt Bestandsdaten driftfrei; keine vorhandene Spalte/Row wird mutiert; keine neue Trust-Boundary. |
| T-14-02 | Tampering | `EmployeeWorkDetailsTO.committed_voluntary` (`#[serde(default)]`) | accept | `#[serde(default)]` bewahrt Wire-Backward-Compat (alte Clients -> `0.0`) ohne Input-Validierung zu verbreitern; Feld ist in Phase 14 inert (nirgends gelesen), daher kein Wert-abhaengiger Kontrollfluss; numerischer `f32`, kein String/Injection-Vektor. |
| T-14-03 | Information Disclosure | SQLx INSERT/UPDATE-Bindings | mitigate | Ausschliesslich parametrisierte Bindings (`?` + bind), compile-time-checked gegen das live Schema via `.sqlx`-Cache; keine String-Interpolation von `committed_voluntary` in SQL. |

Kein HIGH-Threat. Surface minimal: additive nullable-default-Spalte, kein neuer Endpoint, keine Auth/Authz-Aenderung, keine neue User-Input-Parsing-Logik ueber bestehendes serde hinaus.
</threat_model>

<verification>
- `nix develop --command cargo check --workspace` exit 0 (SQLx compile-time gate inkl. .sqlx-Cache)
- `nix develop --command cargo test --workspace` exit 0 (keine Regression bestehender Tests)
- Migration applied: `nix develop --command sqlx migrate info --source migrations/sqlite` listet die neue Migration
- Kein `ToSchema`/`#[utoipa::path]` an `EmployeeWorkDetailsTO` hinzugefuegt
- `CURRENT_SNAPSHOT_SCHEMA_VERSION` unveraendert
- Keine destruktive DB-Operation, keine git-Commits
</verification>

<success_criteria>
- CVC-01: `committed_voluntary: f32` existiert durchgaengig (Migration -> DAO -> Service -> rest-types); `.sqlx` regeneriert; Bestandsdaten driftfrei (Default 0)
- CVC-01: beide Konversionsrichtungen an jeder Boundary durchgezogen; `EmployeeWorkDetailsTO` mit `#[serde(default)]`; kein REST/OpenAPI-Change
- CVC-02: Carry-Forward-Spread-Zeile gesetzt — kein stiller Reset
- Workspace baut + Bestandstests gruen
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/14-data-model-foundation-backend/14-01-SUMMARY.md` erstellen (Template: @.claude/get-shit-done/templates/summary.md).
</output>
