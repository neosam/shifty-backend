---
phase: 49-pdf-download-button
plan: 04
type: execute
wave: 3
depends_on:
  - 49-02
files_modified:
  - shifty-dioxus/src/page/shiftplan.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements:
  - PDF-03
  - PDF-04
user_setup: []

must_haves:
  truths:
    - "D-49-10 + D-49-11: FE-Button ist ein `<a>`-Element (kein `reqwest`/`Blob`), direkt NEBEN dem iCal-Button in der Toolbar-Row von `shifty-dioxus/src/page/shiftplan.rs` (nach Zeile 1140, VOR dem `is_shiftplanner`-Booking-Log-Block auf Zeile 1141)."
    - "D-49-11: Styling 1:1 vom iCal-Precedent kopiert (`px-3 py-1.5 rounded-md text-body font-medium border bg-surface text-ink border-border-strong inline-flex items-center gap-1 hover:bg-surface-alt`), Icon-Prefix `↓` in `font-mono` — bewusst kein `target=\"_blank\"` (Anti-Pattern per RESEARCH.md Pitfall)."
    - "D-49-11: `download`-Attribut gesetzt: `format!(\"schichtplan-{y}-KW{w:02}.pdf\", ...)` — Filename-Konvention konsistent mit v2.2 WebDAV und Plan 02 Content-Disposition."
    - "D-49-12 (implements D-49-04): `href` nutzt `format!(\"{}/shiftplan/{}/{}/{}/pdf\", backend_url, shiftplan_id, *year.read(), *week.read())` — konsumiert die Signals `selected_shiftplan_id: Signal<Option<Uuid>>`, `year: Signal<u32>`, `week: Signal<u8>` — d.h. die im UI selektierte KW, nicht die heute-KW."
    - "D-49-13 + PDF-04: Sichtbarkeit ueber pure fn `should_show_pdf_button(status: WeekStatus, shiftplan_id: Option<Uuid>) -> bool` — Rueckgabe true genau dann, wenn `shiftplan_id.is_some()` UND `matches!(status, WeekStatus::Planned | WeekStatus::Locked)`. Button-Rendering ist ausschliesslich hinter diesem Praedikat gegated — kein disabled-Zustand, kein Tooltip mit Begruendung, kein Fehler-Toast."
    - "D-49-14 + Three-Locale-Rule: Neuer `Key::PdfDownload` in `shifty-dioxus/src/i18n/mod.rs`; Uebersetzungen `PDF` in allen drei Locales `de.rs`/`en.rs`/`cs.rs` (Symmetrie zum iCal-Kuerzel)."
    - "Nyquist: Unit-Test-Suite fuer `should_show_pdf_button` deckt alle 8 Kombinationen ab (4 WeekStatus × 2 shiftplan_id-Zustaende); `cd shifty-dioxus && cargo test should_show_pdf_button` gruen."
    - "WASM-Build-Gate: `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` warnungsfrei."
    - "FE-Clippy-Gate: `cd shifty-dioxus && cargo clippy -p shifty-dioxus -- -D warnings` gruen (0 Warnings, MEMORY-Anker v2.2 Phase 45)."
  artifacts:
    - shifty-dioxus/src/page/shiftplan.rs
    - shifty-dioxus/src/i18n/mod.rs
    - shifty-dioxus/src/i18n/de.rs
    - shifty-dioxus/src/i18n/en.rs
    - shifty-dioxus/src/i18n/cs.rs
  key_links:
    - "Der Button ist Kind der Toolbar-Row, nicht des Wochenrasters — Parent-Flex-Layout uebernimmt Gap-Verhalten."
    - "`should_show_pdf_button` ist eine PURE FN im selben File wie der RSX-Block (`shiftplan.rs`) — direkt testbar per `cargo test`; kein Browser-Test-Bedarf (MEMORY-Anker Phase 39 `should_show_badge`)."
    - "Der `week_status`-Signal-Read muss auf DEM `WeekStatus`-State liegen, der bei KW-Navigation aktualisiert wird (`WEEK_STATUS_STORE` mit `WeekStatusAction::Load` bei `year`/`week`-Change — pre-existing per RESEARCH.md Pitfall 4, grep-verifiziert)."
---

<objective>
Frontend-Komponente fuer den PDF-Download-Button implementieren: `<a>`-Element im Toolbar-Row neben dem iCal-Button, gegated durch pure fn `should_show_pdf_button`, mit i18n-Label `PdfDownload` in de/en/cs. Zusaetzlich Unit-Tests fuer das Praedikat.

Purpose: PDF-03 (Download-Button auf Schichtplan-Seite) und PDF-04 (Sichtbarkeits-Gate) im FE erfuellt. Der Klick loest per Cookie-Auth einen HTTP-GET auf den Wave-2-Endpoint aus — Browser speichert das PDF mit dem `download`-Attribut-Namen.
Output: 4 modifizierte Files im shifty-dioxus-Workspace + 1 neue pure fn (im gleichen File wie der RSX-Block).
</objective>

<execution_context>
@.planning/phases/49-pdf-download-button/49-CONTEXT.md
@.planning/phases/49-pdf-download-button/49-RESEARCH.md
@.planning/phases/49-pdf-download-button/49-PATTERNS.md
@.planning/phases/49-pdf-download-button/49-UI-SPEC.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-pdf-download-button/49-02-SUMMARY.md

@shifty-dioxus/src/page/shiftplan.rs
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/i18n/de.rs
@shifty-dioxus/src/i18n/en.rs
@shifty-dioxus/src/i18n/cs.rs
@shifty-dioxus/src/state/week_status.rs
@shifty-dioxus/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: i18n-Key + 3 Uebersetzungen</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs Zeile 55-95 (Key-Enum, insb. Zeile 84 `PersonalCalendarExport` als analog)
    - shifty-dioxus/src/i18n/de.rs (Muster fuer `i18n.add_text(Locale::De, Key::PersonalCalendarExport, "Persoenlichen Kalender exportieren (iCal)")` — der PDF-Key nutzt DAS EXAKTE Muster)
    - shifty-dioxus/src/i18n/en.rs + shifty-dioxus/src/i18n/cs.rs (analog)
    - shifty-dioxus/CLAUDE.md §"i18n System" (Locale::De statt Locale::En sicherstellen)
  </read_first>
  <behavior>
    - Neuer Enum-Variant `PdfDownload` in `Key`-Enum.
    - Drei `i18n.add_text(...)`-Aufrufe fuer de/en/cs mit dem string `"PDF"`.
    - Compile-time Vollstaendigkeit: alle drei Locales tragen den Key, sonst `cargo build --target wasm32-unknown-unknown` failt bei Enum-Exhaustiveness.
  </behavior>
  <action>
    Rein textuelle Additive-Aenderung; keine Refactor-Konflikte:
    - `shifty-dioxus/src/i18n/mod.rs`: `PdfDownload,` als neue Variant DIREKT NACH `PersonalCalendarExport,` einfuegen (Zeile ~85, alphabetisch/thematisch-nah — der Enum ist nach Screens gruppiert, dieser Key gehoert in die „Shiftplan"-Section).
    - `shifty-dioxus/src/i18n/de.rs`: `i18n.add_text(Locale::De, Key::PdfDownload, "PDF");` nach dem `PersonalCalendarExport`-Block einfuegen. Verifiziere per Read-Tool, dass der Locale-Constructor `Locale::De` verwendet (NICHT `Locale::En`, MEMORY-Anker).
    - `shifty-dioxus/src/i18n/en.rs`: `i18n.add_text(Locale::En, Key::PdfDownload, "PDF");` analog.
    - `shifty-dioxus/src/i18n/cs.rs`: `i18n.add_text(Locale::Cs, Key::PdfDownload, "PDF");` analog.

    Nach der Aenderung:
    - `cd shifty-dioxus && cargo build` gruen.
    - `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` gruen (WASM-Build-Gate).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo build 2>&1 | tail -10 ; grep -c 'PdfDownload' src/i18n/mod.rs ; grep -c 'PdfDownload' src/i18n/de.rs ; grep -c 'PdfDownload' src/i18n/en.rs ; grep -c 'PdfDownload' src/i18n/cs.rs</automated>
  </verify>
  <acceptance_criteria>
    - `Key::PdfDownload` als Variant in `shifty-dioxus/src/i18n/mod.rs` (grep-verifiziert, genau 1 Vorkommen).
    - `add_text(Locale::De, Key::PdfDownload, "PDF")` in `de.rs`.
    - `add_text(Locale::En, Key::PdfDownload, "PDF")` in `en.rs`.
    - `add_text(Locale::Cs, Key::PdfDownload, "PDF")` in `cs.rs`.
    - `cd shifty-dioxus && cargo build` gruen.
    - `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` gruen.
  </acceptance_criteria>
  <done>
    Neuer i18n-Key in allen drei Locales verfuegbar. Committed als `feat(49-04): add PdfDownload i18n key (de/en/cs)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure Sichtbarkeits-Predikat + 8 Unit-Tests (RED → GREEN)</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs Zeile 1-50 (Imports, insb. `WeekStatus`-Import + `uuid::Uuid`)
    - shifty-dioxus/src/page/shiftplan.rs Zeile 1120-1160 (iCal-Anchor-Block als Analog — Sichtbarkeits-Guard-Muster `if let Some(sp_id) = ...`)
    - shifty-dioxus/src/state/week_status.rs (Enum-Varianten `Unset`/`InPlanning`/`Planned`/`Locked` sowie `#[derive(Default)]`)
    - Analog `should_show_badge` aus Phase 39 (falls im repo grep-findbar in shifty-dioxus/src/state/week_status.rs oder analog)
  </read_first>
  <behavior>
    Pure fn definition:
    ```
    pub fn should_show_pdf_button(status: WeekStatus, shiftplan_id: Option<Uuid>) -> bool {
        shiftplan_id.is_some() && matches!(status, WeekStatus::Planned | WeekStatus::Locked)
    }
    ```

    Test-Matrix (8 Kombinationen):
    | # | shiftplan_id | status | expected |
    |---|--------------|--------|----------|
    | 1 | Some(uuid) | Planned | true |
    | 2 | Some(uuid) | Locked | true |
    | 3 | Some(uuid) | Unset | false |
    | 4 | Some(uuid) | InPlanning | false |
    | 5 | None | Planned | false |
    | 6 | None | Locked | false |
    | 7 | None | Unset | false |
    | 8 | None | InPlanning | false |
  </behavior>
  <action>
    RED → GREEN in einem atomaren Commit (Pure-Fn ist trivial):
    1. In `shifty-dioxus/src/page/shiftplan.rs` (oben nach den `use`-Statements, in File-Scope):
       ```
       pub fn should_show_pdf_button(status: crate::state::week_status::WeekStatus, shiftplan_id: Option<uuid::Uuid>) -> bool {
           shiftplan_id.is_some() && matches!(status, crate::state::week_status::WeekStatus::Planned | crate::state::week_status::WeekStatus::Locked)
       }
       ```
       (Alternativ mit `use`-Statements bereits gemacht — dann kurze Namen.)
    2. Am File-Ende (nach dem `pub fn Shiftplan()`-Component-Block) ein `#[cfg(test)] mod pdf_button_tests { ... }` mit 8 Test-Funktionen (klare Namen: `some_id_planned`, `some_id_locked`, `some_id_unset`, `some_id_in_planning`, `none_id_planned`, `none_id_locked`, `none_id_unset`, `none_id_in_planning`), jeweils Assertions `assert!(should_show_pdf_button(...))` bzw. `assert!(!should_show_pdf_button(...))`.

    Nach der Impl:
    - `cd shifty-dioxus && cargo test should_show_pdf_button` — alle 8 Tests gruen.
    - `cd shifty-dioxus && cargo build` gruen.
    - `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` gruen.
    - `cd shifty-dioxus && cargo clippy -p shifty-dioxus -- -D warnings` gruen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo test should_show_pdf_button 2>&1 | tail -15 ; cargo clippy -p shifty-dioxus -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub fn should_show_pdf_button' shifty-dioxus/src/page/shiftplan.rs` == 1.
    - Test-Modul `pdf_button_tests` mit 8 Test-Funktionen im gleichen File.
    - `cargo test should_show_pdf_button` alle 8 gruen.
    - `cargo build --target wasm32-unknown-unknown` gruen.
    - `cargo clippy -p shifty-dioxus -- -D warnings` gruen (kein neuer Warning).
  </acceptance_criteria>
  <done>
    Pure predicate + 8/8 Tests committed als `test(49-04): add should_show_pdf_button predicate with 8-case matrix`.
  </done>
</task>

<task type="auto">
  <name>Task 3: RSX-Block fuer PDF-Anchor neben iCal-Button</name>
  <files>shifty-dioxus/src/page/shiftplan.rs</files>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs Zeile 1120-1160 (iCal-Anchor-Block, Ziel-Insertion-Punkt zwischen Zeile 1140 und 1141)
    - shifty-dioxus/src/page/shiftplan.rs Zeile 100-200 (Signal-Deklarationen: `year: Signal<u32>`, `week: Signal<u8>`, `selected_shiftplan_id: Signal<Option<Uuid>>`, `week_status`-Read-Signal — verifizieren, dass alle vorhanden sind)
    - UI-SPEC.md §"Component Contract — PDF-Download-Button" (locked Styling + Sichtbarkeits-Vertrag)
    - RESEARCH.md §"Pattern 3" (RSX-Blueprint, insb. Umgang mit Signal-Reads + `matches!`-Guard)
  </read_first>
  <behavior>
    - Der neue Block sitzt DIREKT nach dem iCal-`<a>`-Anchor-Block (nach Zeile 1140) und VOR dem `if is_shiftplanner { Btn { ... } }`-Booking-Log-Block auf Zeile 1141.
    - Er nutzt die pure fn `should_show_pdf_button` als Guard.
    - Der href kombiniert `backend_url` + `sp_id` + `year.read()` + `week.read()`.
    - Das `download`-Attribut ist gesetzt; kein `target="_blank"`.
    - Icon `↓` in `<span class="font-mono">` als Prefix; Label ist `i18n.t(Key::PdfDownload)`.
  </behavior>
  <action>
    Einfuegen zwischen Zeile 1140 (Ende iCal-`{ ... }` block) und Zeile 1141 (`if is_shiftplanner {`) im Toolbar-Row-Container. Structural analog zum iCal-Block, aber:
    - Klasse-String 1:1 vom iCal-Block kopieren.
    - Statt `sales_person_id` verwendet der Guard `selected_shiftplan_id: Signal<Option<Uuid>>` + `week_status`.
    - Statt `format!("{}/sales-person/{}/ical", backend_url, sp_id)` verwendet der Block `format!("{}/shiftplan/{}/{}/{}/pdf", backend_url, sp_id, y, w)`.
    - `download`-Attribut mit `format!("schichtplan-{y}-KW{w:02}.pdf")` setzen.
    - `title`-Attribut mit dem Label.
    - Icon-Span + Label ausgeben.

    Konkrete RSX-Skelett (im vorhandenen Toolbar-`{ let ... rsx! { ... } }`-Wrapper-Muster):
    ```rust
    {
        let backend_url_pdf = backend_url.clone();
        let pdf_label = i18n.t(Key::PdfDownload);
        let sp_id_opt = *selected_shiftplan_id.read();
        let y = *year.read();
        let w = *week.read();
        let ws = week_status.read().clone();
        rsx! {
            if should_show_pdf_button(ws, sp_id_opt) {
                a {
                    class: "px-3 py-1.5 rounded-md text-body font-medium border bg-surface text-ink border-border-strong inline-flex items-center gap-1 hover:bg-surface-alt",
                    href: format!("{}/shiftplan/{}/{}/{}/pdf", backend_url_pdf, sp_id_opt.unwrap(), y, w),
                    download: format!("schichtplan-{y}-KW{w:02}.pdf"),
                    title: "{pdf_label}",
                    span { class: "font-mono", "↓" }
                    "{pdf_label}"
                }
            }
        }
    }
    ```

    `sp_id_opt.unwrap()` ist safe, weil `should_show_pdf_button` `is_some()` verlangt. Alternativ: `if let Some(sp_id) = sp_id_opt` verschachteln und `sp_id` verwenden — cleaner. Empfehlung: die verschachtelte Form.

    **Signal-Read-Semantik pruefen:** `week_status` ist ein `Signal<WeekStatus>` (oder `Signal<Arc<WeekStatus>>`) — `week_status.read()` liefert `&WeekStatus`; `.clone()` erzeugt owned `WeekStatus`; die Predikat-Fn nimmt `WeekStatus` by value → OK. Wenn `week_status` als `Rc`/`Arc` liegt, dann `.read().as_ref().clone()` oder analog.

    Nach der Impl:
    - `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` gruen.
    - `cd shifty-dioxus && cargo test` gruen (alle bestehenden + Wave-4 Task-2-Tests).
    - `cd shifty-dioxus && cargo clippy -p shifty-dioxus -- -D warnings` gruen.

    **Manuelle Browser-UAT (Nyquist manual-only per VALIDATION.md — nicht Blocker fuer Commit, aber im Summary vermerken):** `nix develop -c cargo run` (Backend, Port 3000) + `cd shifty-dioxus && dx serve` (FE, Port 8080). Login als Employee. Shiftplan-Seite. KW mit Status=Planned waehlen. Button muss sichtbar sein. Klick loest Download aus. Datei-Name `schichtplan-2026-KW{ww}.pdf`. Zu InPlanning-KW navigieren — Button verschwindet.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -10 ; cargo test 2>&1 | tail -10 ; cargo clippy -p shifty-dioxus -- -D warnings 2>&1 | tail -10 ; grep -c 'should_show_pdf_button' src/page/shiftplan.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'should_show_pdf_button' shifty-dioxus/src/page/shiftplan.rs` >= 2 (Definition + Nutzung im RSX-Block; Tests koennen zusaetzlich zaehlen).
    - `grep -c 'schichtplan-{y}-KW{w:02}.pdf' shifty-dioxus/src/page/shiftplan.rs` >= 1 (download-Attribut in korrektem Format).
    - `grep -c '/shiftplan/{}/{}/{}/pdf' shifty-dioxus/src/page/shiftplan.rs` >= 1 (URL-Pattern konsistent mit Wave-2-Endpoint).
    - Der PDF-Anchor-Block sitzt zwischen iCal-Block-Ende und `is_shiftplanner`-Booking-Log-Block (Zeilennummer via grep-verifiziert: iCal-Ende + 1 < PDF-Block < Booking-Log-Anfang).
    - Kein `target="_blank"` im PDF-Block (`grep -A 5 'download: format!("schichtplan' shifty-dioxus/src/page/shiftplan.rs | grep -c 'target'` == 0).
    - `cargo build --target wasm32-unknown-unknown` gruen.
    - `cargo test` gruen (alle bisherigen inkl. Task-2 8/8 Praedikat-Tests).
    - `cargo clippy -p shifty-dioxus -- -D warnings` gruen.
  </acceptance_criteria>
  <done>
    Button sitzt neben iCal, korrekt gegated, download-file benannt konsistent. WASM-Build + Clippy gruen. Committed als `feat(49-04): add PDF download anchor next to iCal button, gated on WeekStatus`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → REST (Cookie) | `<a href>` schickt Cookie; kein WASM-Fetch/Blob — Anti-CSRF via GET-only-Endpoint. |
| Signal → Rendering | `week_status.read().clone()` liefert owned value fuer pure fn — kein Aliasing-Panic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Spoofing / Authentication | FE-Anchor | low | mitigate | Browser reicht Cookie automatisch durch; Backend-Middleware `forbid_unauthenticated` gate — nicht angemeldeter User erhaelt 401 (kein FE-Fehler-Toast, Browser zeigt Standard-Error). |
| T-49-INFO | Information Disclosure | Signal-Guard | low | accept | `should_show_pdf_button` ist pure — kein Race zwischen Read und Rendering-Frame; Race-Case (WeekStatus-Wechsel zwischen Klick-Init und Server-Response) wird per 409-Response auf Backend-Seite behandelt (Plan 02). |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test should_show_pdf_button` — 8/8 gruen.
- `cd shifty-dioxus && cargo test` — alle bestehenden Tests + neue gruen.
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` — WASM-Build ohne Warnings.
- `cd shifty-dioxus && cargo clippy -p shifty-dioxus -- -D warnings` — 0 Warnings.
- Manueller Browser-Smoke (optional; im Summary dokumentieren): Klick + WeekStatus-Navigation.
</verification>

<success_criteria>
- FE-Button ist unter den 4 spezifizierten Bedingungen sichtbar (Some(sp_id) + Planned/Locked); versteckt in den 4 anderen Kombis.
- Download-Filename konsistent mit Backend-Content-Disposition (`schichtplan-{JJJJ}-KW{NN:02}.pdf`).
- i18n-Label in allen drei Locales „PDF".
- Pure fn ist Wahrheit fuer das Sichtbarkeits-Praedikat; nicht browser-testabhaengig.
</success_criteria>

## Artifacts this phase produces

**New symbols:**
- `shifty_dioxus::i18n::mod::Key::PdfDownload` (enum variant)
- `shifty_dioxus::page::shiftplan::should_show_pdf_button(status, shiftplan_id) -> bool` (pure fn)
- 8 new `#[test]` functions in `shifty-dioxus/src/page/shiftplan.rs::pdf_button_tests`

**New RSX block:**
- `<a>`-Anchor mit Klasse-String vom iCal-Precedent, gegated durch `should_show_pdf_button`

**New i18n entries:**
- `Locale::De, Key::PdfDownload → "PDF"`
- `Locale::En, Key::PdfDownload → "PDF"`
- `Locale::Cs, Key::PdfDownload → "PDF"`

<output>
Create `.planning/phases/49-pdf-download-button/49-04-SUMMARY.md` when done.
</output>
