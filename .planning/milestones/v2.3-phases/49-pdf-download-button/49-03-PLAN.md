---
phase: 49-pdf-download-button
plan: 03
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - service_impl/src/pdf_export_scheduler.rs
  - service_impl/src/test/pdf_export_scheduler.rs
  - shifty_bin/src/main.rs
autonomous: true
requirements:
  - PDF-03
  - PDF-04
user_setup: []

must_haves:
  truths:
    - "D-49-08: `PdfExportScheduler::run_once_now` (bisher `service_impl/src/pdf_export_scheduler.rs:365-400`) ruft ausschliesslich `PdfShiftplanService::render_week_pdf(shiftplan_id, y, w, Authentication::Full, None)` — kein inline `shiftplan_view_service.get_shiftplan_week` + `sales_person_service.get_all` + `pdf_render::render_shiftplan_week_pdf` mehr im Scheduler."
    - "D-49-08 (Q2): `ShiftplanViewService` und `SalesPersonService` sind aus `PdfExportSchedulerDeps` entfernt — nicht `#[allow(dead_code)]`, sondern KOMPLETT weg (DRY-Kern der Phase)."
    - "D-49-08 (Q1): Scheduler exportiert nach dem Refactor NUR noch Weeks mit Status in {Planned, Locked}. WeekStatus-Gate im Service (Wave 1) filtert im Scheduler-Kontext automatisch mit; ValidationError-Returns werden pro Woche geloggt + geskipt (bestehendes `record_error` + `return Ok(())`-Muster)."
    - "D-49-09: DI-Wiring in `shifty_bin/src/main.rs` — `PdfShiftplanServiceImpl` wird nach den Basic Services (`shiftplan_view_service`, `sales_person_service`, `week_status_service`) und VOR dem `PdfExportSchedulerImpl::new`-Aufruf konstruiert. `RestStateDef`-Impl-Block bekommt `type PdfShiftplanService = ...` + `fn pdf_shiftplan_service()`."
    - "Scheduler-Konstruktor-Signatur: Argumente `shiftplan_view_service` und `sales_person_service` sind ENTFERNT, `pdf_shiftplan_service: Arc<Deps::PdfShiftplanService>` ist NEU. `shiftplan_service` (Catalog fuer 'erster aktiver Shiftplan') bleibt — er wird upstream vor dem Wochen-Loop gerufen."
    - "Test-Anpassung `service_impl/src/test/pdf_export_scheduler.rs::TestDeps` — `MockShiftplanViewService`- und `MockSalesPersonService`-Felder + Expectations RAUS; `MockPdfShiftplanService`-Feld + Expectations REIN. `boot_trigger_reload_flow` weiterhin gruen (Cron + WebDAV + Retry-Persistenz-Kern unangefasst)."
    - "Kein Kollateralschaden: `cargo test --workspace` gruen, `cargo clippy --workspace -- -D warnings` gruen, `cargo build -p shifty_bin` gruen."
  artifacts:
    - service_impl/src/pdf_export_scheduler.rs
    - shifty_bin/src/main.rs
  key_links:
    - "Konstruktions-Reihenfolge in `main.rs`: `shiftplan_view_service` + `sales_person_service` + `week_status_service` → `pdf_shiftplan_service` → `pdf_export_scheduler`. Ohne diese Reihenfolge bricht die DI (Basic → Business-Logic gemaess CLAUDE.md Service-Tier-Konvention)."
    - "Der `PdfShiftplanService` im Scheduler-Kontext wird mit `Authentication::Full` gerufen (D-49-07 sagt: „context des Aufrufers"; fuer den Scheduler ist der Aufrufer `Full`)."
    - "WeekStatus-Semantik-Aenderung im Scheduler-Verhalten: Q1-Auflösung — Scheduler exportiert nur `Planned/Locked`, ValidationError-Returns werden per `record_error` geloggt + `return Ok(())` geskipt (bestehendes Muster fuer per-Week-Fehler)."
    - "`shifty_bin/src/main.rs` bekommt `type PdfShiftplanService = ...` in ExactlyEinem Impl-Block der `RestStateDef` (jenem, der auch `PdfExportScheduler` liefert)."
---

<objective>
Bestehende Assemble-Duplizierung im Scheduler (§347-400 in `pdf_export_scheduler.rs`) abraeumen, indem der Scheduler den in Plan 01 gebauten `PdfShiftplanService` konsumiert. Deps aus `PdfExportSchedulerDeps` streichen, Tests umbauen, DI-Wiring in `shifty_bin/src/main.rs` einreihen. Pure Refactor — keine neuen Behaviors, keine neuen Files.

Purpose: DRY-Auflösung der Phase 49. Nach diesem Plan gibt es GENAU EINEN Ort fuer die PDF-Assemble-Logik (`PdfShiftplanService`), konsumiert vom REST-Handler (Plan 02) UND vom Scheduler.
Output: 3 modifizierte Files, kein neuer File.
</objective>

<execution_context>
@.planning/phases/49-pdf-download-button/49-CONTEXT.md
@.planning/phases/49-pdf-download-button/49-RESEARCH.md
@.planning/phases/49-pdf-download-button/49-PATTERNS.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/49-pdf-download-button/49-01-SUMMARY.md

@service/src/pdf_shiftplan.rs
@service/src/pdf_export.rs
@service_impl/src/pdf_shiftplan.rs
@service_impl/src/pdf_export_scheduler.rs
@service_impl/src/test/pdf_export_scheduler.rs
@shifty_bin/src/main.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Scheduler-Impl + Deps + Konstruktor</name>
  <files>service_impl/src/pdf_export_scheduler.rs</files>
  <read_first>
    - service_impl/src/pdf_export_scheduler.rs (voller File, insb. Zeile 53-80 `gen_service_impl!`, Zeile 110-138 `new()`-Konstruktor, Zeile 300-445 `run_once_now`-Body)
    - service/src/pdf_shiftplan.rs (Trait-Signatur des neuen Service — `render_week_pdf`)
    - service_impl/src/pdf_shiftplan.rs (`filename_for`-Helper aus Plan 02 — kann jetzt hier statt inline im Scheduler benutzt werden)
  </read_first>
  <action>
    Refactor in einer atomaren Edit-Serie. Kein neuer Behavior, nur Umbau.

    **A) `gen_service_impl!`-Block (Zeile 53-74):**
    - ENTFERNEN: `ShiftplanViewService: ShiftplanViewService<Context = Self::Context, Transaction = Self::Transaction> = shiftplan_view_service,` (Zeile 59-62).
    - ENTFERNEN: `SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,` (Zeile 67-70).
    - EINFUEGEN (nach `PdfExportConfigService`-Block): `PdfShiftplanService: service::pdf_shiftplan::PdfShiftplanService<Context = Self::Context, Transaction = Self::Transaction> = pdf_shiftplan_service,`.
    - BLEIBEN: `ShiftplanService` (Catalog-Lookup fuer den ersten aktiven Shiftplan — wird noch in §330 gebraucht), `PermissionService`, `ClockService`, `TransactionDao`, `custom_fields`-Block.

    **B) Konstruktor `new()` (Zeile 114-138):**
    - ENTFERNEN: `shiftplan_view_service: Arc<Deps::ShiftplanViewService>,` und `sales_person_service: Arc<Deps::SalesPersonService>,` aus der Signatur.
    - EINFUEGEN: `pdf_shiftplan_service: Arc<Deps::PdfShiftplanService>,` an derselben Stelle wie im `gen_service_impl!`-Block.
    - Struct-Initialisierung analog anpassen (die 2 Feld-Zuweisungen ersetzen).

    **C) Imports (oben im File):**
    - `use service::shiftplan::ShiftplanViewService;` und `use service::sales_person::SalesPersonService;` ENTFERNEN, WENN sie nach dem Refactor ungenutzt sind (grep sagt sie werden nur in `gen_service_impl!` gebraucht; check nach Edit mit `cargo build`).
    - `use service::pdf_shiftplan::PdfShiftplanService;` (falls fuer die `gen_service_impl!`-Macro-Expansion sichtbar noetig; `service::pdf_shiftplan::` full-path im Macro-Body reicht in der Regel — nach Edit `cargo build` sagt).
    - `use crate::pdf_render;` — kann WEG, weil der Scheduler `pdf_render` nach dem Refactor nicht mehr direkt ruft. Verify per grep.

    **D) `run_once_now`-Body (Zeile 300-445):**
    - Zeile 347-355 (`all_sales_persons` + `active_sales_persons`-Filter) KOMPLETT ENTFERNEN — der `PdfShiftplanService` macht das jetzt intern.
    - Zeile 365-381 (`shiftplan_view_service.get_shiftplan_week`-Match) ERSETZEN durch:
      ```
      let bytes = match self
          .pdf_shiftplan_service
          .render_week_pdf(shiftplan_id, y, w, Authentication::Full, None)
          .await
      {
          Ok(b) => b,
          Err(e) => {
              let at = self.clock_service.date_time_now();
              let msg: Arc<str> = Arc::from(format!(
                  "PDF-Assemble/Render fuer KW{w:02}/{y} fehlgeschlagen: {e}"
              ));
              self.pdf_export_config_service
                  .record_error(at, msg, Authentication::Full, None)
                  .await?;
              return Ok(());
          }
      };
      ```
    - Zeile 383-400 (`pdf_render::render_shiftplan_week_pdf`-Match) KOMPLETT ENTFERNEN — bytes kommen jetzt direkt aus dem Service-Call oben.
    - Zeile 402 (`let filename = format!("schichtplan-{y}-KW{w:02}.pdf");`) ersetzen durch `let filename = service_impl::pdf_shiftplan::filename_for(y, w);` (nutzt den Helper aus Plan 02) — ODER die Zeile so lassen. Empfehlung: den Helper nutzen, damit die Format-Konvention eine Wahrheit hat. Wenn der Import-Weg `crate::pdf_shiftplan::filename_for` einfacher ist, dann so.
    - WebDAV-Upload-Block (Zeile 403-429) UNVERAENDERT lassen.

    **E) `PdfExportSchedulerHandle`-Struct (Zeile 449-459):** Muss NICHT geaendert werden — der Handle klont nur `Arc<PdfExportSchedulerImpl<Deps>>`.

    **Wichtig — Q1-Semantik-Aenderung dokumentieren:** Nach dem Refactor exportiert der Scheduler nur noch Weeks mit `WeekStatus in {Planned, Locked}`. Fuer alle anderen Weeks liefert `PdfShiftplanService::render_week_pdf` einen `ServiceError::ValidationError`, der im obigen Match-Arm als per-Week-Fehler behandelt wird (record_error + return Ok(())). Das ist eine bewusste Verhaltensaenderung (orchestrator-directive Q1(b)) — im inline-Kommentar VOR dem for-Loop ODER am oberen File-Header dokumentieren mit Verweis auf D-49-08. Kurzer doc-comment reicht.

    Nach der Impl:
    - `cargo build -p service_impl` gruen.
    - `cargo test -p service_impl pdf_export_scheduler` — die Tests werden hier noch failen (das ist Task 2's Job). Aber der Compile MUSS gruen sein.
    - `cargo clippy --workspace -- -D warnings 2>&1 | tail -20` gruen im Scheduler-File (dead-code-warnings duerfen nicht auftauchen — die 2 entfernten Deps sind komplett weg, nicht `#[allow(dead_code)]`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build -p service_impl 2>&1 | tail -15 ; grep -c 'shiftplan_view_service\|sales_person_service' service_impl/src/pdf_export_scheduler.rs || true ; grep -c 'pdf_shiftplan_service' service_impl/src/pdf_export_scheduler.rs || true ; grep -c 'pdf_render::' service_impl/src/pdf_export_scheduler.rs || true</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p service_impl` gruen.
    - `grep -v '^\s*//' service_impl/src/pdf_export_scheduler.rs | grep -c 'shiftplan_view_service'` == 0 (Dep + Field + Konstruktor-Arg alle weg).
    - `grep -v '^\s*//' service_impl/src/pdf_export_scheduler.rs | grep -c 'sales_person_service'` == 0.
    - `grep -c 'pdf_shiftplan_service' service_impl/src/pdf_export_scheduler.rs` >= 3 (gen_service_impl-Feld + Konstruktor-Arg + Aufruf in run_once_now).
    - `grep -v '^\s*//' service_impl/src/pdf_export_scheduler.rs | grep -c 'pdf_render::'` == 0 (Scheduler ruft `pdf_render` nicht mehr direkt).
    - `grep -c 'render_week_pdf' service_impl/src/pdf_export_scheduler.rs` >= 1 (Delegation an Service).
    - `cargo clippy -p service_impl -- -D warnings` gruen (0 Warnungen im refactored File).
  </acceptance_criteria>
  <done>
    Scheduler-Impl-File clean refactored. Tests failen — das ist erwartet (Task 2 macht sie wieder gruen).
  </done>
</task>

<task type="auto">
  <name>Task 2: Test-Anpassung fuer PdfExportScheduler mit MockPdfShiftplanService</name>
  <files>service_impl/src/test/pdf_export_scheduler.rs</files>
  <read_first>
    - service_impl/src/test/pdf_export_scheduler.rs (voller File — `TestDeps`-Struct, `arrange`-Funktion, alle Test-Cases inkl. `boot_trigger_reload_flow`)
    - service_impl/src/pdf_export_scheduler.rs (die refactored Version aus Task 1 — Konstruktor-Signatur)
    - service_impl/src/test/pdf_shiftplan.rs (Muster fuer `MockPdfShiftplanService`-Expectations aus Plan 01)
  </read_first>
  <action>
    Rein test-side Anpassung, kein Behavior-Change:

    **A) `TestDeps`-Struct:**
    - Felder `MockShiftplanViewService` + `MockSalesPersonService` (oder deren `Arc<>`-Wrapper) ENTFERNEN.
    - Feld `MockPdfShiftplanService` (oder `Arc<MockPdfShiftplanService>`) HINZUFUEGEN.
    - Trait-Impl `PdfExportSchedulerDeps for TestDeps` — die 2 assoc-types `ShiftplanViewService` + `SalesPersonService` ENTFERNEN; `PdfShiftplanService = MockPdfShiftplanService` HINZUFUEGEN.

    **B) `arrange`-/Setup-Funktion(en):**
    - Konstruktor-Args ohne die 2 Deps rufen, dafuer `pdf_shiftplan_service`-Arg reinreichen.
    - Alle Test-Cases, die `MockShiftplanViewService::expect_get_shiftplan_week` ODER `MockSalesPersonService::expect_get_all` gemockt haben, MUESSEN umgestellt werden auf `MockPdfShiftplanService::expect_render_week_pdf`. Erwartungs-Semantik: `.expect_render_week_pdf().returning(|_, _, _, _, _| Ok(vec![0u8; 100]))` fuer happy-path; `.returning(|_, _, _, _, _| Err(ServiceError::ValidationError(...)))` fuer skip-week-Faelle.
    - `boot_trigger_reload_flow` (der zentrale E2E-Test) MUSS umgestellt werden: statt View + get_all + printpdf-Roundtrip mockt er jetzt `render_week_pdf` und assertet WebDAV-Upload + record_success.

    **C) Zusaetzliche Assertions:**
    - `scheduler_calls_pdf_shiftplan_service_with_full_auth` — Test verifiziert, dass `render_week_pdf` mit `Authentication::Full` gerufen wird (per `.withf(|_, _, _, auth, _| matches!(auth, Authentication::Full))`) — D-49-07 fuer Scheduler-Aufrufer.
    - `scheduler_skips_week_on_validation_error` — MockPdfShiftplanService liefert `ValidationError` fuer eine spezifische KW; Test verifiziert, dass `record_error` gerufen wird und der Loop `return Ok(())` macht (per D-49-08 Q1-Semantik).

    Nach der Impl:
    - `cargo test -p service_impl pdf_export_scheduler` — ALLE Tests gruen (inkl. `boot_trigger_reload_flow`).
    - `cargo test --workspace` — 0 Regressions in anderen Crates.
    - `cargo clippy --workspace -- -D warnings` — 0 Warnungen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl pdf_export_scheduler 2>&1 | tail -20 ; cargo clippy --workspace -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p service_impl pdf_export_scheduler` alle Tests gruen (inkl. `boot_trigger_reload_flow`).
    - `service_impl/src/test/pdf_export_scheduler.rs` — grep `MockShiftplanViewService` == 0, grep `MockSalesPersonService` == 0, grep `MockPdfShiftplanService` >= 1.
    - Neue Tests `scheduler_calls_pdf_shiftplan_service_with_full_auth` + `scheduler_skips_week_on_validation_error` gruen.
    - `cargo test --workspace` gruen (0 Regressions).
    - `cargo clippy --workspace -- -D warnings` gruen.
  </acceptance_criteria>
  <done>
    Scheduler-Tests umgestellt auf `MockPdfShiftplanService`. Committed als `refactor(49-03): scheduler consumes PdfShiftplanService — DRY assemble path`.
  </done>
</task>

<task type="auto">
  <name>Task 3: DI-Wiring in shifty_bin/src/main.rs</name>
  <files>shifty_bin/src/main.rs</files>
  <read_first>
    - shifty_bin/src/main.rs (voller File; insb. die Konstruktionsstellen von `shiftplan_view_service`, `sales_person_service`, `week_status_service`, `pdf_export_scheduler` — Grep nach diesen Namen liefert die Zeilen)
    - RESEARCH.md §"DI-Wiring in shifty_bin/src/main.rs" (Blueprint fuer Konstruktion + RestStateDef-Impl-Block)
    - service_impl/src/pdf_shiftplan.rs (der Struct-Name `PdfShiftplanServiceImpl` + Deps-Struct-Name `PdfShiftplanServiceDeps`)
    - rest/src/lib.rs (die Wave-2-Erweiterung von `RestStateDef` — neuer assoc-type + accessor)
  </read_first>
  <action>
    Rein DI-Aenderung in `shifty_bin/src/main.rs`:

    **A) Konstruktion `pdf_shiftplan_service`:**
    - Nach `shiftplan_view_service`, `sales_person_service`, `week_status_service`, `permission_service` und `transaction_dao` (alle Basic) — VOR dem `pdf_export_scheduler`-Konstruktor:
      ```
      let pdf_shiftplan_service = Arc::new(
          service_impl::pdf_shiftplan::PdfShiftplanServiceImpl::<PdfShiftplanServiceDependencies> {
              shiftplan_view_service: shiftplan_view_service.clone(),
              sales_person_service: sales_person_service.clone(),
              week_status_service: week_status_service.clone(),
              permission_service: permission_service.clone(),
              transaction_dao: transaction_dao.clone(),
          },
      );
      ```
    - Konkreter Typ-Alias `PdfShiftplanServiceDependencies` in dem Deps-Type-Alias-Block anlegen (analog `PdfExportSchedulerDependencies`); enthaelt die 5 Deps-Typen (jeweils die konkreten `...Impl`-Namen).

    **B) `PdfExportSchedulerImpl::new(...)` Aufruf:**
    - Args `shiftplan_view_service.clone()` und `sales_person_service.clone()` ENTFERNEN.
    - Arg `pdf_shiftplan_service.clone()` an derselben Stelle EINFUEGEN.

    **C) `RestStateDef`-Impl-Block:** Der Impl-Block, der `RestStateDef` fuer die konkrete `ProdRestState`-Struct implementiert, bekommt:
    ```
    type PdfShiftplanService = service_impl::pdf_shiftplan::PdfShiftplanServiceImpl<PdfShiftplanServiceDependencies>;

    fn pdf_shiftplan_service(&self) -> Arc<Self::PdfShiftplanService> {
        self.pdf_shiftplan_service.clone()
    }
    ```
    - Der `ProdRestState`-Struct (oder wie er heisst) bekommt ein neues Feld `pdf_shiftplan_service: Arc<...>`.
    - Der Struct-Initialisierer fuer `ProdRestState { ... }` bekommt `pdf_shiftplan_service: pdf_shiftplan_service.clone(),`.

    Nach der Impl:
    - `cargo build -p shifty_bin` gruen.
    - `cargo build --workspace` gruen.
    - `cargo clippy --workspace -- -D warnings 2>&1 | tail -10` gruen.

    Optional Smoke-Test (nur wenn schnell zu bauen): `cargo run --bin shifty_bin` bootet ohne Panic bei DI-Aufloesung (kein `expect`/`unwrap`-Panic in der Konstruktionskette). Nicht Blocker.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build -p shifty_bin 2>&1 | tail -15 ; cargo build --workspace 2>&1 | tail -10 ; cargo clippy --workspace -- -D warnings 2>&1 | tail -10 ; grep -c 'pdf_shiftplan_service' shifty_bin/src/main.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p shifty_bin` gruen.
    - `cargo build --workspace` gruen.
    - `cargo test --workspace` gruen (kein Regression durch DI-Neuverdrahtung).
    - `cargo clippy --workspace -- -D warnings` gruen.
    - `grep -c 'pdf_shiftplan_service' shifty_bin/src/main.rs` >= 4 (Konstruktion + Scheduler-Arg + State-Feld + Accessor).
    - `grep -c 'PdfShiftplanServiceDependencies' shifty_bin/src/main.rs` >= 2 (Deps-Type-Alias + Nutzung in State-Impl).
    - Konstruktionsreihenfolge in main.rs verifizierbar: Zeile von `let pdf_shiftplan_service = ...` LIEGT NACH `week_status_service`- und `shiftplan_view_service`-Konstruktion (grep-basierte Line-Nummern-Assertion).
  </acceptance_criteria>
  <done>
    DI-Wiring in main.rs voll integriert. Full workspace test + clippy gruen. Phase-49-Backend-Kette (Plan 01 → 02 → 03) ist konsistent. Committed als `chore(49-03): wire PdfShiftplanService into DI + RestStateDef impl`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron-Callback → Service | Scheduler ruft `PdfShiftplanService` mit `Authentication::Full` — Full-Kontext ist Scheduler-inherent (kein User-Anmelde-Flow), Aufrufer-Kontext-Semantik konform mit D-49-07. |
| Service-internal Gate | Nach Refactor prueft der Service den WeekStatus im Scheduler-Kontext — ValidationError signalisiert per-Week-Skip, kein Fehler-Boot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03 | Tampering / Input Validation | Scheduler-Loop | low | mitigate | WeekStatus-Gate im Service filtert Unset/InPlanning-Weeks — kein leaky Export, keine unfertige Wochenplaene im WebDAV-Storage. Verifiziert per Scheduler-Test `scheduler_skips_week_on_validation_error`. |
| T-49-INFO | Information Disclosure | Deps-Streichung | low | accept | Entfernte `ShiftplanViewService`/`SalesPersonService`-Deps sind rein interne Refactor-Aenderung, keine externe Surface. |
</threat_model>

<verification>
- `cargo test --workspace` gruen — 0 Regressions.
- `cargo build --workspace` gruen (main.rs kompiliert mit neuer Konstruktions-Kette).
- `cargo clippy --workspace -- -D warnings` gruen.
- Scheduler-File hat keine `shiftplan_view_service`/`sales_person_service`-Bezuege (Wave-1-Aufrufe delegieren an Service).
- Frontend-Grep: `shifty-dioxus` referenziert `PdfShiftplanService` nicht (kein FE-Import), aber wenn `dx build` scheitert waer das ein anderer Fehler.
</verification>

<success_criteria>
- Ein einziger PDF-Assemble-Pfad: `PdfShiftplanService::render_week_pdf` konsumiert von REST-Handler (Plan 02) UND Scheduler (Plan 03).
- Scheduler-Deps sind minimiert; DRY-Anspruch der Phase erfuellt.
- DI-Reihenfolge in main.rs konform mit CLAUDE.md Service-Tier-Konvention (Basic → Business-Logic).
- Verhaltensaenderung Q1 (Scheduler exportiert nur Planned/Locked) dokumentiert im Scheduler-Header-Kommentar mit Verweis auf D-49-08.
</success_criteria>

## Artifacts this phase produces

**Removed symbols (Scheduler-only):**
- `PdfExportSchedulerDeps::ShiftplanViewService` assoc-type + `shiftplan_view_service` field
- `PdfExportSchedulerDeps::SalesPersonService` assoc-type + `sales_person_service` field
- 2 Argumente in `PdfExportSchedulerImpl::new(...)`

**Added symbols (Scheduler-only):**
- `PdfExportSchedulerDeps::PdfShiftplanService` assoc-type + `pdf_shiftplan_service` field
- 1 Argument in `PdfExportSchedulerImpl::new(...)`

**Added symbols (main.rs):**
- `PdfShiftplanServiceDependencies` type-alias (concrete Deps-Impls)
- `let pdf_shiftplan_service = ...` construction line
- `type PdfShiftplanService = ...` + `fn pdf_shiftplan_service(&self)` in `RestStateDef`-Impl-Block
- `pdf_shiftplan_service: Arc<...>` field in `ProdRestState` (oder aequivalent)

**Behavior change:**
- Scheduler exportiert nur `WeekStatus in {Planned, Locked}`-Weeks. Andere Weeks: ValidationError → `record_error` + per-Week-Skip.

<output>
Create `.planning/phases/49-pdf-download-button/49-03-SUMMARY.md` when done.
</output>