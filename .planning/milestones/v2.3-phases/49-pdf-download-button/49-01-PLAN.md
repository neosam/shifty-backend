---
phase: 49-pdf-download-button
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - service/src/lib.rs
  - service/src/pdf_shiftplan.rs
  - service_impl/src/lib.rs
  - service_impl/src/pdf_shiftplan.rs
  - service_impl/src/test/mod.rs
  - service_impl/src/test/pdf_shiftplan.rs
autonomous: true
requirements:
  - PDF-03
  - PDF-04
user_setup: []

must_haves:
  truths:
    - "D-49-05: Neuer Business-Logic-Service `PdfShiftplanService` in `service/src/pdf_shiftplan.rs` existiert und ist per CLAUDE.md Service-Tier-Konvention klassifiziert (konsumiert View + SalesPerson + WeekStatus + Permission + Transaction)."
    - "D-49-06: Trait-Signatur `render_week_pdf(shiftplan_id: Uuid, year: u32, calendar_week: u8, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<Vec<u8>, ServiceError>` genau so vorhanden."
    - "D-49-06 + PDF-04: Service-internes Gate prueft `WeekStatusService::get_week_status` und returned `ServiceError::ValidationError` (Defense-in-Depth) wenn Status not in {Planned, Locked}."
    - "D-49-06: Service ruft `ShiftplanViewService::get_shiftplan_week`, filtert `SalesPersonService::get_all` per `sp.deleted.is_none()` und delegiert an `pdf_render::render_shiftplan_week_pdf`."
    - "D-49-07: Service reicht `context` (der aufrufer-liefert) an alle konsumierten Services weiter; interne Aufrufe verwenden NIE `Authentication::Full`."
    - "PDF-03 happy-path Unit-Test: WeekStatus=Planned + View + get_all gemockt liefert Ok(Vec<u8>)."
    - "PDF-03 filter-Test: `deleted.is_some()` SalesPersons erreichen `pdf_render` nicht (Aufruf-Assertion via Mock-Injektion des Renderers ODER via Byte-Text-Grep)."
    - "PDF-04 Defense-in-Depth-Tests: WeekStatus=Unset, WeekStatus=InPlanning liefern `ServiceError::ValidationError`; WeekStatus=Planned + WeekStatus=Locked liefern Ok."
    - "Wave-0-Nyquist: `cargo test -p service_impl pdf_shiftplan -- --nocapture` >=8 Tests, alle gruen; `cargo clippy --workspace -- -D warnings` gruen."
  artifacts:
    - service/src/pdf_shiftplan.rs
    - service_impl/src/pdf_shiftplan.rs
    - service_impl/src/test/pdf_shiftplan.rs
  key_links:
    - "service/src/lib.rs muss `pub mod pdf_shiftplan;` bekommen — sonst kein Downstream-Import."
    - "service_impl/src/lib.rs muss `pub mod pdf_shiftplan;` bekommen — sonst kein Impl-Export fuer Plan 02+03."
    - "service_impl/src/test/mod.rs muss `mod pdf_shiftplan;` bekommen — sonst laufen die Unit-Tests nicht."
    - "Trait hat `#[automock(type Context=(); type Transaction=dao::MockTransaction;)]` — sonst kann Plan 02+03 keinen `MockPdfShiftplanService` konstruieren."
---

<objective>
Neuen Business-Logic-Service `PdfShiftplanService` einfuehren (Trait + Impl + Unit-Tests) — genau ein Ort fuer die Assemble-Logik `WeekStatus-Gate` + `View` + `SalesPerson-Filter` + `pdf_render`. Alle 8+ Unit-Tests decken PDF-03 + PDF-04 (Defense-in-Depth) ab. Blueprint fuer den REST-Handler (Plan 02) und den Scheduler-Refactor (Plan 03).

Purpose: DRY-Kern der Phase 49 — beide zukuenftigen Aufrufer (REST-Handler + Scheduler) gehen durch diesen Service. TDD-first fuer die Kern-Buisness-Logik.
Output: 3 neue Files (Trait, Impl, Test) + 3 module-registrations.
</objective>

<execution_context>
@.planning/phases/49-pdf-download-button/49-CONTEXT.md
@.planning/phases/49-pdf-download-button/49-RESEARCH.md
@.planning/phases/49-pdf-download-button/49-PATTERNS.md
@.planning/phases/49-pdf-download-button/49-VALIDATION.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@service/src/pdf_export.rs
@service/src/week_status.rs
@service/src/shiftplan.rs
@service/src/sales_person.rs
@service/src/lib.rs
@service_impl/src/pdf_export_scheduler.rs
@service_impl/src/test/pdf_export_scheduler.rs
@service_impl/src/pdf_render.rs
@service_impl/src/lib.rs
@service_impl/src/test/mod.rs
@CLAUDE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — Trait skeleton + failing unit test suite</name>
  <files>service/src/pdf_shiftplan.rs, service/src/lib.rs, service_impl/src/pdf_shiftplan.rs, service_impl/src/lib.rs, service_impl/src/test/pdf_shiftplan.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service/src/pdf_export.rs (Trait-Vorlage: `#[automock]`, `type Context`, `type Transaction`, `async fn` — 1:1 kopieren)
    - service/src/week_status.rs (Enum-Varianten `Unset`/`InPlanning`/`Planned`/`Locked` + `WeekStatusService::get_week_status`-Signatur)
    - service/src/shiftplan.rs (`ShiftplanViewService::get_shiftplan_week`-Signatur — nimmt `Uuid`, `u32`, `u8`, `Authentication`, `Option<Transaction>`)
    - service/src/sales_person.rs (`SalesPersonService::get_all(auth, tx)` und `SalesPerson.deleted`-Feld)
    - service/src/lib.rs (`ServiceError`-Enum inkl. `ValidationError(Arc<[ValidationFailureItem]>)` + `ValidationFailureItem { field, reason }`)
    - service_impl/src/test/pdf_export_scheduler.rs (Test-Harness-Muster — `TestDeps`-Struct, Mock-Setup, Assertion-Style)
    - service_impl/src/pdf_render.rs (`render_shiftplan_week_pdf(&week, &sales_persons, year, week) -> Result<Vec<u8>, ServiceError>`-Signatur — pure fn, wird direkt gerufen)
  </read_first>
  <behavior>
    Die 8+ Unit-Tests beschreiben die Verhaltens-Kontrakte VOR der Implementation:
    - `happy_path_returns_bytes` — WeekStatus=Planned; Mock-View + Mock-get_all liefern non-empty; erwartet `Ok(Vec<u8>)` mit len > 0.
    - `week_status_locked_returns_bytes` — analog fuer `Locked`.
    - `week_status_unset_returns_validation_error` — WeekStatus=Unset; erwartet `Err(ServiceError::ValidationError(_))`; kein Aufruf an View / get_all / pdf_render (via `.times(0)`).
    - `week_status_in_planning_returns_validation_error` — analog fuer `InPlanning`.
    - `filters_deleted_sales_persons` — get_all liefert Mix aus `deleted=None` + `deleted=Some(_)`; verifiziert dass die geloeschten nicht in der Liste sind, die an `pdf_render` geht (Assertion via injizierten Test-Renderer ODER via Text-Suche im PDF-Byte-Stream fuer den Namen).
    - `service_forwards_caller_context_to_view` — Mock-View `.expect().withf(|ctx| ...)` prueft, dass der Service den `context`-Parameter an `get_shiftplan_week` durchreicht (D-49-07); analog fuer `get_all` + `get_week_status`.
    - `render_error_bubbles_up_as_internal_error` — pdf_render liefert `Err`; Service returned diesen Fehler unveraendert.
    - `view_error_bubbles_up` — `get_shiftplan_week` liefert Err; Service returned diesen Fehler unveraendert (kein 500-Rewrite im Service).
  </behavior>
  <action>
    RED-Phase. Erst die Test-Datei `service_impl/src/test/pdf_shiftplan.rs` schreiben (alle 8+ Tests, aber `.unwrap()`-freundlich formuliert) — die Test-Funktion `arrange_service` baut einen `PdfShiftplanServiceImpl<TestDeps>` mit `MockShiftplanViewService`, `MockSalesPersonService`, `MockWeekStatusService`, `MockPermissionService`, `MockTransactionDao`. Muster 1:1 aus `service_impl/src/test/pdf_export_scheduler.rs` (Test-Deps-Struct + `mockall`-Expectations).

    Danach die Trait-Datei `service/src/pdf_shiftplan.rs` schreiben — Header + doc-comment (Business-Logic-Tier Note aus PATTERNS.md), dann Trait wie in D-49-06: `#[automock(type Context=(); type Transaction=dao::MockTransaction;)]` + `type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;` + `type Transaction: dao::Transaction;` + `render_week_pdf`-Method. Verwende die Import-Muster aus `service/src/pdf_export.rs`. In `service/src/lib.rs` `pub mod pdf_shiftplan;` einfuegen (alphabetisch neben `pdf_export`).

    Impl-Skelett in `service_impl/src/pdf_shiftplan.rs` schreiben, das noch NICHT die WeekStatus-Gate-Logik enthaelt (nur `unimplemented!()` im Body oder ein direktes `pdf_render`-Delegate). `gen_service_impl!` mit den 5 Deps (ShiftplanViewService, SalesPersonService, WeekStatusService, PermissionService, TransactionDao) — Muster 1:1 aus `service_impl/src/pdf_export_scheduler.rs:53–74` (ohne `custom_fields`-Block). In `service_impl/src/lib.rs` `pub mod pdf_shiftplan;` + in `service_impl/src/test/mod.rs` `mod pdf_shiftplan;`.

    Ziel: `cargo test -p service_impl pdf_shiftplan -- --nocapture` MUSS jetzt failen (Tests sind da, Impl unvollstaendig).

    Keine i18n-Aenderungen, kein REST, kein FE. Ausschliesslich service/ + service_impl/ + service_impl/test/.

    Referenz-Impl-Bausteine sichtbar in PATTERNS.md §"service_impl/src/pdf_shiftplan.rs" und RESEARCH.md §"Pattern 1".
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl pdf_shiftplan -- --nocapture 2>&1 | tail -30 ; echo "EXPECT: at least one test FAILS (RED phase)"</automated>
  </verify>
  <acceptance_criteria>
    - Datei `service/src/pdf_shiftplan.rs` existiert mit `pub trait PdfShiftplanService` + `#[automock]` + `render_week_pdf`-Signatur.
    - Datei `service_impl/src/pdf_shiftplan.rs` existiert mit `PdfShiftplanServiceImpl` + `gen_service_impl!` fuer 5 Deps.
    - Datei `service_impl/src/test/pdf_shiftplan.rs` existiert mit >=8 `#[tokio::test]`-Funktionen inkl. `happy_path_returns_bytes`, `week_status_unset_returns_validation_error`, `week_status_in_planning_returns_validation_error`, `filters_deleted_sales_persons`, `service_forwards_caller_context_to_view`.
    - `pub mod pdf_shiftplan;` in `service/src/lib.rs` (grep-verifiziert).
    - `pub mod pdf_shiftplan;` in `service_impl/src/lib.rs`.
    - `mod pdf_shiftplan;` in `service_impl/src/test/mod.rs`.
    - `cargo build -p service && cargo build -p service_impl` liefert 0 Errors (Kompiliert; Tests duerfen failen).
    - `cargo test -p service_impl pdf_shiftplan` executed >=1 Test, MINDESTENS 1 failure (RED phase-Nachweis).
  </acceptance_criteria>
  <done>
    Trait + Impl-Skelett + 8+ failing tests committed als atomarer RED-commit. `cargo build` gruen, `cargo test` liefert RED (mindestens ein Test ist bewusst rot).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — WeekStatus-Gate + View + Filter + Renderer wiring</name>
  <files>service_impl/src/pdf_shiftplan.rs</files>
  <read_first>
    - service_impl/src/pdf_shiftplan.rs (die RED-Skelett-Impl aus Task 1)
    - service_impl/src/test/pdf_shiftplan.rs (Assertions, damit die Impl exakt trifft)
    - service_impl/src/pdf_export_scheduler.rs Zeile 347–400 (Assemble-Sequenz — WORTGLEICH kopieren: `get_all` → `filter deleted.is_none()` → `get_shiftplan_week` → `pdf_render::render_shiftplan_week_pdf`)
    - service/src/lib.rs (`ServiceError::ValidationError(Arc<[ValidationFailureItem]>)`-Konstruktor-Muster)
  </read_first>
  <behavior>
    - Bei WeekStatus in {Planned, Locked}: full assemble-path, Ok(bytes).
    - Bei WeekStatus in {Unset, InPlanning}: sofortiger Err(ValidationError) ohne View/SalesPerson/Renderer-Aufruf.
    - Bei allen Aufrufen an konsumierte Services wird der uebergebene `context` durchgereicht (nie hardcoded `Full`).
    - Bei `get_all` wird `sp.deleted.is_none()`-Filter vor dem Renderer-Call angewandt.
    - Fehler aus `get_shiftplan_week`/`get_all`/`pdf_render` bubblen unveraendert per `?` hoch.
  </behavior>
  <action>
    GREEN-Phase. In `service_impl/src/pdf_shiftplan.rs` den `render_week_pdf`-Body implementieren:

    1. Zuerst `self.week_status_service.get_week_status(year, calendar_week, context.clone(), tx.clone()).await?` — matchen gegen `WeekStatus::Planned | WeekStatus::Locked` mit `matches!`. Wenn NOT in dieser Menge: `return Err(ServiceError::ValidationError(Arc::from([ValidationFailureItem { field: Arc::from("week_status"), reason: Arc::from(format!("Woche KW{calendar_week:02}/{year} ist im Status {status:?} — kein Download")) }])))`.
    2. Dann `self.shiftplan_view_service.get_shiftplan_week(shiftplan_id, year, calendar_week, context.clone(), tx.clone()).await?`.
    3. Dann `self.sales_person_service.get_all(context, tx).await?` + `.iter().filter(|sp| sp.deleted.is_none()).cloned().collect::<Vec<_>>()`.
    4. Zuletzt `pdf_render::render_shiftplan_week_pdf(&week_view, &active_sales_persons, year, calendar_week)`.

    Die Reihenfolge ist locked: Gate zuerst (nix teures vor Auth-Gate), dann View, dann Filter, dann Render. Kein Log-Statement noetig (Handler entscheidet ueber Logging spaeter).

    Nutze `context.clone()` fuer die ersten beiden Aufrufe; im dritten Aufruf `context` per move — der `Authentication<...>`-Owner-Wechsel ist okay, weil nach dem 3. Aufruf nichts mehr den Context braucht. `tx.clone()` analog. Prueft der Rust-Compiler — falls `Authentication` nicht `Clone` ist, dann alle 3 vorherigen Aufrufe mit `.clone()` und der dritte Aufruf zieht `context` per move.

    Nach der Impl `cargo test -p service_impl pdf_shiftplan -- --nocapture` — alle 8 Tests MUESSEN gruen sein. Falls filter-Test failed, muss die Test-Assertion so umgebaut werden, dass sie messbar prueft, ob die geloeschten SalesPersons an `pdf_render` durchgereicht werden. Weil `pdf_render` eine pure fn ist (keine Mock-Injektion), ist die Assertion am besten: `pdf_render` liefert Bytes, die (nach printpdf's `use_text`-Escape) den Namen als hex-encoded String enthalten. Der Test entpackt die Bytes in einen String via `String::from_utf8_lossy(&bytes)` und grept nach Fragmenten des Sales-Person-Namens — der geloeschte Name darf NICHT vorkommen, die aktiven MUESSEN vorkommen. Alternativ: den `filters_deleted_sales_persons`-Test durch eine sichtbarere Zwischen-Abstraktion ersetzen (z.B. den Filter in eine separate pure fn `filter_active(&[SalesPerson]) -> Vec<SalesPerson>` auslagern und die separat unit-testen — schlanker + robuster).

    Kein neuer clippy warning tolerieren — nach der Impl `cargo clippy --workspace -- -D warnings 2>&1 | tail -20` muss gruen sein.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl pdf_shiftplan -- --nocapture 2>&1 | tail -20 ; cargo clippy --workspace -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p service_impl pdf_shiftplan` ALLE Tests gruen (>=8).
    - `cargo clippy --workspace -- -D warnings` gruen (0 Warnungen im gesamten Workspace).
    - Grep `matches!(status, WeekStatus::Planned | WeekStatus::Locked)` findet mindestens 1 Treffer in `service_impl/src/pdf_shiftplan.rs`.
    - Grep `sp.deleted.is_none()` findet mindestens 1 Treffer in `service_impl/src/pdf_shiftplan.rs`.
    - Grep `pdf_render::render_shiftplan_week_pdf` findet genau 1 aktiven Aufruf in `service_impl/src/pdf_shiftplan.rs`.
    - Kein `Authentication::Full` in `service_impl/src/pdf_shiftplan.rs` (`grep -v '^\s*//' service_impl/src/pdf_shiftplan.rs | grep -c 'Authentication::Full'` == 0).
  </acceptance_criteria>
  <done>
    Alle Unit-Tests gruen, Clippy gruen, D-49-05/D-49-06/D-49-07 sind im Code manifest. Commit-Message `feat(49-01): implement PdfShiftplanService with WeekStatus gate + active filter`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aufrufer → Service | Service akzeptiert `Authentication<Context>` — `Full` (Scheduler) und `Authenticated(User)` (Handler); beide durchgereicht an konsumierte Services (D-49-07). |
| Service → WeekStatusService | Race-Window: Handler-Pre-Check + Service-Gate koexistieren; Service-Gate ist Defense-in-Depth. |
| Service → pdf_render | Renderer ist pure fn; kein State-Change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-03 | Tampering / Input Validation | `PdfShiftplanServiceImpl::render_week_pdf` | medium | mitigate | Path-Params sind bereits durch Axum-Typing (Uuid/u32/u8) gefiltert (Plan 02); Service prueft zusaetzlich `WeekStatus` und returned `ValidationError` fuer non-releasable Zustaende. Test-abgedeckt in `week_status_unset_returns_validation_error` + `week_status_in_planning_returns_validation_error`. |
| T-49-INFO | Information Disclosure | `SalesPerson`-Filter | low | accept | `sp.deleted.is_some()` SalesPersons duerfen keine Namen im PDF haben — durch `filter(|sp| sp.deleted.is_none())` erzwungen; Test `filters_deleted_sales_persons` verifiziert. Non-Deleted-Namen sind fuer alle authentifizierten User sichtbar (PDF-05, bewusst). |
</threat_model>

<verification>
- `cargo test -p service_impl pdf_shiftplan -- --nocapture` alle Tests gruen (>=8).
- `cargo clippy --workspace -- -D warnings` gruen.
- Grep-Assertions aus Task 2 alle erfuellt.
- `service`- und `service_impl`-Module registration verifizierbar via grep in `lib.rs`/`test/mod.rs`.
</verification>

<success_criteria>
- Neuer Service-Trait + Impl liefert die Assemble-Logik an einer einzigen Stelle.
- 8+ Unit-Tests decken PDF-03 (happy-path + filter) und PDF-04 (Defense-in-Depth gate) ab.
- Trait ist mit `#[automock]` versehen → Plan 02 (REST-Handler) und Plan 03 (Scheduler-Refactor) koennen `MockPdfShiftplanService` konsumieren.
- Alle Locked Decisions D-49-05/D-49-06/D-49-07 sind im Code manifest.
</success_criteria>

## Artifacts this phase produces

**New symbols:**
- `service::pdf_shiftplan::PdfShiftplanService` (trait, `#[automock]`)
- `service::pdf_shiftplan::MockPdfShiftplanService` (via automock)
- `service_impl::pdf_shiftplan::PdfShiftplanServiceImpl<Deps>`
- `service_impl::pdf_shiftplan::PdfShiftplanServiceDeps` (via `gen_service_impl!`)

**New method:**
- `PdfShiftplanService::render_week_pdf(shiftplan_id, year, calendar_week, context, tx) -> Result<Vec<u8>, ServiceError>`

**Module registrations:**
- `pub mod pdf_shiftplan;` in `service/src/lib.rs`
- `pub mod pdf_shiftplan;` in `service_impl/src/lib.rs`
- `mod pdf_shiftplan;` in `service_impl/src/test/mod.rs`

**Behavioral contract (as expressed by tests):**
- WeekStatus in {Planned, Locked} → assemble + render + Ok(bytes)
- WeekStatus in {Unset, InPlanning} → ValidationError, no side effects
- SalesPersons with `deleted.is_some()` are excluded from renderer input
- Context is forwarded (never hardcoded to `Full`)

<output>
Create `.planning/phases/49-pdf-download-button/49-01-SUMMARY.md` when done.
</output>
