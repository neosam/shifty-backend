---
phase: 49-pdf-download-button
plan: 05
type: execute
wave: 3
depends_on:
  - 49-04
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PDF-03
  - PDF-04
user_setup: []

must_haves:
  truths:
    - "D-49-04 (Deviation, dokumentiert via D-49-15/D-49-16): PDF-03 zielt auf die im UI selektierte KW, nicht auf die heute-KW; das Nicht-Ziel „Wochenwahl über die UI-Navigation" ist umgekehrt und der Backend-URL bleibt strukturell wie D-49-01 (nimmt einfach die FE-Werte)."
    - "D-49-15: `.planning/REQUIREMENTS.md` §PDF-03 formuliert die Ziel-KW als „aktuell im UI selektierte Kalenderwoche" statt „aktuelle Kalenderwoche (basierend auf heute)". Verifikations-Passus (manueller UAT-Klick) bleibt inhaltlich unveraendert."
    - "D-49-15: `.planning/REQUIREMENTS.md` §Nicht-Ziele — das Bullet „Wochenwahl über die UI-Navigation" (oder die aequivalente Formulierung im aktuellen File-Stand) ist ENTFERNT (bewusst umgekehrt: die UI-Selektion IST jetzt der Steuerungspunkt)."
    - "D-49-15: PDF-04 in REQUIREMENTS.md bleibt inhaltlich unangetastet (das Sichtbarkeits-Gate gilt jetzt fuer die vom FE gelieferte KW — semantisch identisch)."
    - "D-49-16: `.planning/ROADMAP.md` Phase 49 Goal — „aktuelle Kalenderwoche (basierend auf heute)" ist umformuliert zu „aktuell im UI selektierte Kalenderwoche"."
    - "D-49-16: `.planning/ROADMAP.md` Phase 49 SC 3 — die frueher gewuenschte Formulierung „laedt IMMER die KW von heute, nicht die im UI navigierte Woche" ist ERSETZT durch: Sichtbarkeit + Ziel-KW werden vom selektierten `week`/`year` gesteuert; Button versteckt (statt disabled) ausserhalb von {Planned, Locked}."
    - "Die Edits sind rein textuell — kein Requirement-ID-Wechsel, kein Milestone-Wechsel, kein Struktur-Umbau. Alle uebrigen Requirements (PDF-01, PDF-02, PDF-05) sind unangetastet."
    - "Grep-Assertion: nach Edit `grep -c 'aktuell im UI selektierte' .planning/REQUIREMENTS.md` >= 1, `grep -c 'aktuell im UI selektierte' .planning/ROADMAP.md` >= 1."
    - "Grep-Negativ-Assertion: `grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/REQUIREMENTS.md` == 0 in dem PDF-03-Absatz, `grep -c 'IMMER die KW von heute' .planning/ROADMAP.md` == 0 im Phase-49-Block. (Aufschluesselung: exakte Formulierungen koennen woanders im File auftauchen; das ist okay, nur PDF-03 und Phase-49-SC-3 sind das Ziel.)"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "Diese Aenderung ist Doku-Only — kein Code, keine Tests, keine Migration. Der zeitliche Punkt (Wave 3) ist bewusst spaet gewaehlt, damit der Deviation-Nachweis mit dem tatsaechlich implementierten Verhalten aus Plan 04 zusammenfaellt."
    - "Wenn diese Doku-Deviation NICHT in derselben Milestone-Auslieferung landet wie der Code (Wave 3), erzeugt sie einen Requirement-Deviation-Drift; deshalb ist der Commit-Zusammenhalt mit Plan 04 wichtig (gleiche Wave, gleicher Milestone-Cutover)."
---

<objective>
**Audit-/Verifikations-Plan (keine Content-Edits erwartet).** Nach Grep-Verifikation zur Planungszeit (2026-07-03) sind `.planning/REQUIREMENTS.md` und `.planning/ROADMAP.md` bereits D-49-15/D-49-16-konform (der Doku-Text wurde im selben Commit wie `49-CONTEXT.md` gefahren). Dieser Plan sichert die Konformitaet grep-basiert und dokumentiert im SUMMARY, dass beide Files zum Wave-3-Cutover unveraendert sind — falls die Assertions failen (d.h. jemand hat inzwischen die alte Formulierung wieder eingespielt), MUSS der Executor die zwei scoped Edits nachziehen.

Purpose: REQ ↔ ROADMAP ↔ Implementation konsistent halten — Audit-Trail fuer die Milestone-Verifikation und v2.3-Milestone-Archiv; explizit als Verifikations-Gate im Wave-3-Commit mit Plan 04 verankert.
Output: SUMMARY.md dokumentiert Verifikations-Stand. Content-Edits nur wenn Grep-Assertions failen (Notfallpfad).
</objective>

<execution_context>
@.planning/phases/49-pdf-download-button/49-CONTEXT.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUDIT — REQUIREMENTS.md PDF-03 + Nicht-Ziel-Bullet auf D-49-15-Konformitaet grep-verifizieren</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md komplett (kompakter File, siehe Zeilen 33-110 fuer Nicht-Ziele + Requirements)
    - .planning/phases/49-pdf-download-button/49-CONTEXT.md §D-49-15 (locked Wording)
  </read_first>
  <behavior>
    Reine Grep-Verifikation, KEIN Content-Edit erwartet.

    **Ist-Stand zum Planungszeitpunkt (Grep-Nachweis 2026-07-03):**
    - `grep -c 'aktuell im UI selektierte' .planning/REQUIREMENTS.md` == 1 (PDF-03-Absatz nutzt bereits die neue Formulierung).
    - `grep -c 'Wochenwahl über die UI-Navigation' .planning/REQUIREMENTS.md` == 0 (Nicht-Ziel-Bullet bereits gestrichen).
    - `grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/REQUIREMENTS.md` == 0 (alte Formulierung nirgends mehr).

    Aufgabe des Executors: die drei Grep-Werte bei Wave-3-Commit erneut messen und im SUMMARY dokumentieren.

    **Notfallpfad (nur falls Assertions failen):** Wenn zwischen Planungs-Read und Executor-Run jemand die alte Formulierung wieder eingespielt hat, macht der Executor zwei scoped `Edit`-Calls:
    - PDF-03-Satz zurueckdrehen auf „aktuell im UI selektierte Kalenderwoche".
    - Nicht-Ziel-Bullet „Wochenwahl über die UI-Navigation" streichen.

    In beiden Faellen (no-op ODER Notfall-Edit) bleibt PDF-01/02/04/05 + Constraints-Block UNVERAENDERT.
  </behavior>
  <action>
    Reine Verifikations-Task, no-op im Regelfall. Ablauf:

    1. `Read` `.planning/REQUIREMENTS.md`.
    2. Fuehre die drei greps aus dem `<verify>`-Block aus.
    3. Falls alle Assertions erfuellt (Erwartungswerte >= 1 / == 0 / == 0): **keine File-Aenderung**. Dokumentiere im SUMMARY den Grep-Beleg und den Verweis auf D-49-15.
    4. Falls Assertions failen: fuehre die zwei chirurgischen `Edit`-Calls aus dem `<behavior>`-Notfallpfad aus, dann Grep-Wiederholung bis alle Assertions gruen.

    Explizit KEIN `Write`-Call. Explizit KEIN Umbau des Requirements-Blocks — nur PDF-03-Satz und Nicht-Ziel-Bullet duerfen bei einem Notfall-Edit angefasst werden.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && grep -c 'aktuell im UI selektierte' .planning/REQUIREMENTS.md ; grep -c 'Wochenwahl über die UI-Navigation' .planning/REQUIREMENTS.md ; grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aktuell im UI selektierte' .planning/REQUIREMENTS.md` >= 1.
    - `grep -c 'Wochenwahl über die UI-Navigation' .planning/REQUIREMENTS.md` == 0.
    - `grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/REQUIREMENTS.md` == 0.
    - Alle anderen Requirements (PDF-01, PDF-02, PDF-04, PDF-05) unveraendert (per `jj diff` bzw. `git diff` nachgewiesen — im no-op-Regelfall: leerer Diff fuer diese Datei).
    - SUMMARY dokumentiert Grep-Werte + Konformitaets-Beleg.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md ist konform zu D-49-15 (grep-verifiziert). Regelfall: no-op, kein Commit-Delta an dieser Datei. Notfallpfad: Committed als `docs(49): restore PDF-03 UI-selected-week wording (D-49-15)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: AUDIT — ROADMAP.md Phase 49 Goal + SC 3 auf D-49-16-Konformitaet grep-verifizieren</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md Zeile 39-65 (Phase 49 Block: Goal, Requirements, Plans, Success Criteria)
    - .planning/phases/49-pdf-download-button/49-CONTEXT.md §D-49-16 (locked Wording)
  </read_first>
  <behavior>
    Reine Grep-Verifikation, KEIN Content-Edit erwartet.

    **Ist-Stand zum Planungszeitpunkt (Grep-Nachweis 2026-07-03):**
    - `grep -c 'aktuell im UI selektierte' .planning/ROADMAP.md` == 1 (Phase-49-Goal + SC 3 nutzen bereits die neue Formulierung).
    - `grep -c 'IMMER die KW von heute' .planning/ROADMAP.md` == 0.
    - `grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/ROADMAP.md` == 0.

    Aufgabe des Executors: die drei Grep-Werte bei Wave-3-Commit erneut messen und im SUMMARY dokumentieren.

    **Notfallpfad (nur falls Assertions failen):** Wenn die alten Formulierungen wieder im Phase-49-Block auftauchen, macht der Executor zwei scoped `Edit`-Calls im Phase-49-Block (Goal-Zeile und SC 3), keinen `Write`, keinen Umbau anderer Phasen, keinen Umbau des Milestone-Headers.

    Phase 50, Milestone-Block v2.3, Backlog: in beiden Faellen UNANGETASTET.
  </behavior>
  <action>
    Reine Verifikations-Task, no-op im Regelfall. Ablauf:

    1. `Read` `.planning/ROADMAP.md` (Phase-49-Block Zeile 39-65).
    2. Fuehre die drei greps aus dem `<verify>`-Block aus.
    3. Falls alle Assertions erfuellt: **keine File-Aenderung**. Dokumentiere im SUMMARY den Grep-Beleg und den Verweis auf D-49-16.
    4. Falls Assertions failen: fuehre die zwei chirurgischen `Edit`-Calls im Phase-49-Block aus, dann Grep-Wiederholung bis alle Assertions gruen.

    Explizit KEIN `Write`-Call. Explizit KEIN Umbau von Phase 50 oder des Milestone-Headers.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && grep -c 'aktuell im UI selektierte' .planning/ROADMAP.md ; grep -c 'IMMER die KW von heute' .planning/ROADMAP.md ; grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aktuell im UI selektierte' .planning/ROADMAP.md` >= 1.
    - `grep -c 'IMMER die KW von heute' .planning/ROADMAP.md` == 0.
    - `grep -c 'aktuelle Kalenderwoche (basierend auf heute)' .planning/ROADMAP.md` == 0.
    - Phase 50 Goal + SC unangetastet (diff-verifiziert; im no-op-Regelfall: leerer Diff fuer diese Datei).
    - Milestone-Header „v2.3" + Reihenfolge-Rationale unveraendert.
    - SUMMARY dokumentiert Grep-Werte + Konformitaets-Beleg.
  </acceptance_criteria>
  <done>
    ROADMAP.md ist konform zu D-49-16 (grep-verifiziert). Regelfall: no-op, kein Commit-Delta an dieser Datei. Notfallpfad: Committed als `docs(49): restore Phase 49 Goal + SC 3 UI-selected-week wording (D-49-16)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Doku-Only — keine trust-boundaries. Der Bezug zu Sicherheit besteht nur indirekt: falsch dokumentierte Zielsemantik koennte Auditoren beim Verifikations-Klick verwirren. Non-Issue.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-DOC | Repudiation | Doku-Drift zwischen REQ ↔ Code | low | mitigate | Grep-Assertions in Task 1/2 verhindern Drift; SUMMARY dokumentiert den Verifikations-Stand fuer den Milestone-Audit. |
</threat_model>

<verification>
- 6 grep-Assertions (3 per Task) alle erfuellt.
- Kein Impact auf andere Files (Doku-Only).
- `cargo build --workspace` und `cargo test --workspace` gruen (Sanity-Check, dass keine Doku-Datei versehentlich Code enthaelt) — ist trivialerweise der Fall, aber im Wave-3-Zusammenzug mit Plan 04 automatisch mitgetestet.
</verification>

<success_criteria>
- REQUIREMENTS.md PDF-03 nutzt die „aktuell im UI selektierte KW"-Formulierung.
- Nicht-Ziel „Wochenwahl über die UI-Navigation" ist entfernt.
- ROADMAP.md Phase 49 Goal + SC 3 spiegeln die gleiche Semantik.
- Kein Kollateralschaden an anderen Requirements/Phasen.
</success_criteria>

## Artifacts this phase produces

**Modified doc-strings:**
- `.planning/REQUIREMENTS.md` §PDF-03 (Formulierung „aktuell im UI selektierte Kalenderwoche")
- `.planning/REQUIREMENTS.md` §Nicht-Ziele (Bullet „Wochenwahl über die UI-Navigation" entfernt, falls praesent)
- `.planning/ROADMAP.md` §Phase 49 Goal + SC 3

**Rationale:**
- Traceability: der Milestone-Audit v2.3 kann bei jedem `git log`-Eintrag zuordnen, wo die semantische Deviation gemacht wurde.

<output>
Create `.planning/phases/49-pdf-download-button/49-05-SUMMARY.md` when done.
</output>
