---
phase: 24-paid-capacity-enforcement-config
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [D-24-06, D-24-03, D-24-05]
must_haves:
  truths:
    - "D-24-06/D-24-03/D-24-05: All Phase-24 UI strings exist as i18n Key variants translated in all three locales (En, De, Cs)"
    - "A present-in-all-locales test guards against missing/wrong-locale translations"
  artifacts:
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "9 new Key variants + i18n_phase24_keys_present_in_all_locales test"
      contains: "BookingBlockedPaidLimit"
    - path: "shifty-dioxus/src/i18n/de.rs"
      provides: "German translations (Locale::De) for all 9 keys"
      contains: "Bezahlt-Limit erreicht"
    - path: "shifty-dioxus/src/i18n/cs.rs"
      provides: "Czech translations (Locale::Cs) for all 9 keys"
      contains: "Vynucení limitu"
    - path: "shifty-dioxus/src/i18n/en.rs"
      provides: "English translations (Locale::En) for all 9 keys"
      contains: "Paid employee limit reached"
  key_links:
    - from: "Key enum (mod.rs)"
      to: "add_text in en/de/cs.rs"
      via: "present-in-all-locales test"
      pattern: "i18n_phase24_keys_present_in_all_locales"
---

<objective>
Add the nine new i18n keys Phase 24 needs, translated in all three locales, with a guard test. This is the foundation both frontend plans (24-04 Settings, 24-05 overage + inline block) depend on.

The exact En/De/Cs strings are pinned in 24-UI-SPEC.md §Copywriting Contract and reproduced verbatim below. Use the per-locale reference-matcher test pattern to guard against the classic `Locale::En`-in-`de.rs` bug.

Purpose: Centralize all phase strings so the Settings and shiftplan plans only reference `Key::*`, never literals.
Output: 9 new `Key` variants, 27 translations (9 × 3 locales), 1 present-in-all-locales test.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md
@.planning/phases/24-paid-capacity-enforcement-config/24-PATTERNS.md

<interfaces>
<!-- Analogs verified 2026-06-27. -->
Existing Key enum entries (mod.rs): `Settings:319`, `BookingWarningPaidLimitExceeded:565`.
Per-locale add_text pattern: `de.rs:506` (`add_text(Locale::De, Key::Settings, "Einstellungen")`), `cs.rs:476`.
Existing present-in-all-locales test pattern: `mod.rs:604,683,708` (`i18n_*_present_in_all_locales`).
Key::Settings is REUSED for nav/heading — do NOT add a new Settings key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 9 Key variants + En/De/Cs translations + guard test</name>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (Key enum near :319 and :565; existing i18n_*_present_in_all_locales tests at :604/:683/:708)
    - shifty-dioxus/src/i18n/en.rs, de.rs, cs.rs (add_text blocks; de.rs:506, cs.rs:476 as shape reference)
    - .planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md §Copywriting Contract (verbatim strings)
  </read_first>
  <action>
    1. In `shifty-dioxus/src/i18n/mod.rs`, add these 9 variants to the `Key` enum:
    `SettingsPaidLimitToggleLabel`, `SettingsPaidLimitToggleDescription`, `SettingsPaidLimitToggleOn`, `SettingsPaidLimitToggleOff`, `SettingsSaved`, `SettingsSaveError`, `ShiftplanPaidOverageSectionHeader`, `ShiftplanPaidOverageRow`, `BookingBlockedPaidLimit`.

    2. In `shifty-dioxus/src/i18n/en.rs` (Locale::En), `de.rs` (Locale::De), `cs.rs` (Locale::Cs) add `add_text(...)` calls with these EXACT strings (from UI-SPEC):

    | Key | En | De | Cs |
    |-----|----|----|-----|
    | SettingsPaidLimitToggleLabel | Paid employee limit enforcement | Bezahlt-Limit Durchsetzung | Vynucení limitu placených zaměstnanců |
    | SettingsPaidLimitToggleDescription | When enabled, booking over the paid limit is blocked for non-shift-planners. | Wenn aktiviert, wird das Buchen über das Bezahlt-Limit für Nicht-Schichtplaner blockiert. | Pokud je aktivováno, překročení limitu placených zaměstnanců je blokováno pro uživatele bez oprávnění schichtplannera. |
    | SettingsPaidLimitToggleOn | Hard (enforced) | Hart (blockierend) | Tvrdé (blokující) |
    | SettingsPaidLimitToggleOff | Soft (warnings only) | Weich (nur Warnungen) | Měkké (pouze upozornění) |
    | SettingsSaved | Saved. | Gespeichert. | Uloženo. |
    | SettingsSaveError | Could not save setting. | Einstellung konnte nicht gespeichert werden. | Nastavení se nepodařilo uložit. |
    | ShiftplanPaidOverageSectionHeader | Paid employee limit exceeded this week | Bezahlt-Limit diese Woche überschritten | Limit placených zaměstnanců tento týden překročen |
    | ShiftplanPaidOverageRow | {slot}: {current}/{max} paid | {slot}: {current}/{max} bezahlt | {slot}: {current}/{max} placených |
    | BookingBlockedPaidLimit | Paid employee limit reached — only shift planners may book beyond the limit. | Bezahlt-Limit erreicht — nur Schichtplaner können über das Limit buchen. | Limit placených zaměstnanců dosažen — překročit ho mohou pouze schichtplanneři. |

    CRITICAL: in `en.rs` use `Locale::En`, in `de.rs` use `Locale::De`, in `cs.rs` use `Locale::Cs` — never mismatch.

    3. In `mod.rs`, add `#[test] fn i18n_phase24_keys_present_in_all_locales()` mirroring the existing present-in-all-locales tests (:604/:683/:708): assert each of the 9 keys resolves to a non-empty, locale-appropriate string in En, De, and Cs (use the same reference-matcher assertion shape the existing tests use).
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "BookingBlockedPaidLimit" src/i18n/mod.rs && grep -q "Bezahlt-Limit erreicht" src/i18n/de.rs && grep -q "Vynucení limitu" src/i18n/cs.rs && grep -q "Paid employee limit reached" src/i18n/en.rs && grep -q "i18n_phase24_keys_present_in_all_locales" src/i18n/mod.rs && cargo test --lib i18n_phase24 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `shifty-dioxus/src/i18n/mod.rs` contains all 9 new Key variants and `i18n_phase24_keys_present_in_all_locales`
    - `de.rs` contains `Bezahlt-Limit erreicht` under `Locale::De`
    - `cs.rs` contains `Vynucení limitu placených zaměstnanců` under `Locale::Cs`
    - `en.rs` contains `Paid employee limit reached` under `Locale::En`
    - `cargo test --lib i18n_phase24` passes
  </acceptance_criteria>
  <done>All 9 keys translated in 3 locales; guard test green.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend WASM build gate</name>
  <read_first>
    - shifty-dioxus/CLAUDE.md (WASM build gate from shifty-dioxus/)
  </read_first>
  <action>Run the WASM build gate to confirm the i18n additions compile to the WASM target.</action>
  <verify>
    <automated>cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --target wasm32-unknown-unknown` (run from `shifty-dioxus/`) exits 0
  </acceptance_criteria>
  <done>i18n changes compile for WASM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds only static localized strings; no input crosses a trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Information Disclosure | localized error/strings | accept | Strings are static UI copy with no PII or system internals; `{slot}/{current}/{max}` are user-visible scheduling values already shown elsewhere. |
</threat_model>

<verification>
- 9 keys present in all three locales with the exact UI-SPEC strings and correct `Locale::*` per file.
- Present-in-all-locales test green; WASM build green.
</verification>

<success_criteria>
- All Phase-24 strings centralized as `Key::*` and translated En/De/Cs.
- Guard test prevents missing/wrong-locale regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/24-paid-capacity-enforcement-config/24-03-SUMMARY.md`
</output>
