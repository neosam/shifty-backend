---
phase: 24-paid-capacity-enforcement-config
plan: 05
type: execute
wave: 2
depends_on: [24-03]
files_modified:
  - shifty-dioxus/src/page/shiftplan.rs
autonomous: true
requirements: [D-24-03, D-24-05]
must_haves:
  truths:
    - "D-24-03: A persistent overage warning section renders above the shiftplan (below booking_warnings banner, above ShiftplanTabBar) for ALL roles, listing every loaded-week slot where current_paid_count > max_paid_employees; client-side only, no backend/DTO change; hidden when no overage"
    - "D-24-05: When the booking POST returns the new non-403 block status (409 CONFLICT), an inline localized text-bad message renders at the slot (not a dialog, not a dismissible banner), instead of silently ignoring"
  artifacts:
    - path: "shifty-dioxus/src/page/shiftplan.rs"
      provides: "Persistent overage section + inline 409 hard-block error in AddUserToSlot handler"
      contains: "ShiftplanPaidOverageSectionHeader"
  key_links:
    - from: "AddUserToSlot Err(Reqwest) status == CONFLICT"
      to: "inline BookingBlockedPaidLimit signal at slot"
      via: "new match arm before generic Err(e)"
      pattern: "StatusCode::CONFLICT"
    - from: "state::shiftplan::Slot (current_paid_count > max_paid_employees)"
      to: "overage section list"
      via: "client-side iteration over loaded-week slots"
      pattern: "current_paid_count"
---

<objective>
Two shiftplan-page additions (both touch only `shiftplan.rs`), depending on 24-03 for i18n keys:
1. **Inline hard-block error (D-24-05):** add a new match arm in the `AddUserToSlot` handler that detects the new 409 CONFLICT status (produced by 24-01/24-02) and surfaces a localized inline `text-bad` message at the slot — instead of the silent-403 path. NOT a dialog, NOT a dismissible banner.
2. **Persistent overage section (D-24-03):** a custom section mirroring the conflict block, inserted below the `booking_warnings` banner and above the `ShiftplanTabBar` div, listing every loaded-week slot where `current_paid_count > max_paid_employees`. Visible to ALL roles, client-side only (both fields already in week-view state), hidden when there is no overage.

This plan is independent of plan 24-04 (different files) but depends on the backend status from 24-01/24-02 for runtime correctness of (1). The frontend code change for (1) only keys off the HTTP status, so it can be written/compiled independently; it is functionally exercised once the backend (24-02) ships.
Output: edits to `shifty-dioxus/src/page/shiftplan.rs` only.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md
@.planning/phases/24-paid-capacity-enforcement-config/24-PATTERNS.md

<interfaces>
<!-- Verified 2026-06-27. -->
AddUserToSlot handler (shiftplan.rs:403-441): Ok path :421-429, silent-403 arm :431-436, generic Err :437-440. Insert the new CONFLICT arm BEFORE the generic Err.
slot_edit error <p> analog: `p { class: "text-bad text-small font-normal", "{error_str}" }`.

Conflict section analog (shiftplan.rs:869-903): `div { class: "mx-4 my-3 px-4 py-3 bg-bad-soft border border-bad rounded-md print:hidden" h2 {...} ul { list-disc... } }`. Note it IS role-gated (`is_shiftplanner`); the new overage section must NOT be role-gated.
booking_warnings dismissible banner: :906-934. ShiftplanTabBar div: :941-942. Insert overage section between :934 and :936.

state::shiftplan::Slot (state/shiftplan.rs:167-181): fields `id`, `day_of_week`, `from`, `to`, `max_paid_employees: Option<u8>`, `current_paid_count: u8`. Loaded-week slots are reachable via the shiftplan state used to render the week view (same source the WeekView renders from).

New i18n keys from 24-03: `ShiftplanPaidOverageSectionHeader`, `ShiftplanPaidOverageRow` ("{slot}: {current}/{max} paid"), `BookingBlockedPaidLimit`.

reqwest::StatusCode::CONFLICT == 409 (matches the rest/src/lib.rs mapping chosen in 24-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline 409 hard-block error in AddUserToSlot (D-24-05)</name>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs:403-441 (AddUserToSlot match: Ok, silent-403, generic Err)
    - shifty-dioxus/src/component/slot_edit.rs:264-266 (inline error <p> analog)
    - .planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md §Surface 2
  </read_first>
  <action>
    1. Add a slot-scoped signal to hold the blocked slot id, e.g. `let mut block_error: Signal<Option<Uuid>> = use_signal(|| None);` near the other page signals.
    2. In the `AddUserToSlot` match (:403-441), add a NEW arm BEFORE the generic `Err(e)` arm (:437) that matches the 409 status and sets the signal instead of silently ignoring:
    ```rust
    Err(crate::error::ShiftyError::Reqwest(ref e))
        if e.status() == Some(reqwest::StatusCode::CONFLICT) =>
    {
        // D-24-05: surface inline block message at the slot (NOT silent like 403)
        block_error.set(Some(slot_id));
        update_shiftplan();
    }
    ```
    Keep the existing silent-403 arm and generic Err arm. On the Ok path (:421-429) and on RemoveUserFromSlot, clear the block: `block_error.set(None);` so the message clears on the next successful booking action (UI-SPEC §Surface 2).
    3. Render the inline message at the slot where the booking UI lives: a non-dismissible div using EXACTLY `class: "text-bad text-small font-normal mt-1"` with text `i18n.t(Key::BookingBlockedPaidLimit)`, shown only when `block_error.read().as_ref() == Some(&that_slot_id)`. This is a separate element from the dismissible `booking_warnings` banner. (If the per-slot booking control is rendered in a child component, thread the `block_error` slot-id down or render the message adjacent to the slot's add control in the same RSX scope — keep it scoped to the slot that was blocked.)
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "StatusCode::CONFLICT" src/page/shiftplan.rs && grep -q "BookingBlockedPaidLimit" src/page/shiftplan.rs && grep -q "text-bad text-small font-normal mt-1" src/page/shiftplan.rs && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `shiftplan.rs` has a new `AddUserToSlot` arm matching `reqwest::StatusCode::CONFLICT` that sets a slot-scoped signal (does NOT silently ignore)
    - The inline message uses class `text-bad text-small font-normal mt-1` and `Key::BookingBlockedPaidLimit`
    - The block signal clears on the next successful booking action
    - No dialog/modal is introduced
    - WASM build exits 0
  </acceptance_criteria>
  <done>A 409 hard-block surfaces as a localized inline text-bad message at the slot; cleared on next success.</done>
</task>

<task type="auto">
  <name>Task 2: Persistent overage warning section (D-24-03) + WASM gate</name>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs:869-903 (conflict section analog), :905-934 (booking_warnings banner), :936-942 (ShiftplanTabBar)
    - shifty-dioxus/src/state/shiftplan.rs:167-181 (Slot fields)
    - .planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md §Surface 3 + "Key Tailwind class strings"
  </read_first>
  <action>
    Insert a NEW custom section between the `booking_warnings` banner (ends ~:934) and the `ShiftplanTabBar` block (~:936), mirroring the conflict section but with WARN tokens and NO role gate (all roles, D-24-03 / D-23-05).
    - Compute the overage list client-side: iterate the loaded-week slots from the shiftplan state (same source the WeekView renders) and select each slot where `slot.max_paid_employees == Some(max)` AND `slot.current_paid_count > max`. Preserve natural slot order (day/time). Collect `(slot_label, current_paid_count, max)` per overage slot, where `slot_label` is a human label (slot day-of-week + from–to time, using the existing weekday i18n + time formatting already used on this page).
    - If the list is empty → render NOTHING (zero-state invisible, like the conflict section).
    - Otherwise render with EXACTLY these UI-SPEC class strings:
      - container: `class: "mx-4 my-3 px-4 py-3 bg-warn-soft border border-warn rounded-md print:hidden"`
      - heading: `h2 { class: "text-h2 font-semibold pb-2 text-warn", "⚠️ {i18n.t(Key::ShiftplanPaidOverageSectionHeader)}" }`
      - list: `ul { class: "list-disc list-inside text-body text-ink", ... }`, one non-interactive `li` per overage slot, text built from `Key::ShiftplanPaidOverageRow` ("{slot}: {current}/{max} paid") with `{slot}`/`{current}`/`{max}` substituted (mirror how other rows on this page substitute placeholders in i18n strings).
    Do NOT reuse `WarningList` here (UI-SPEC recommends the custom section to keep the localized heading). Do NOT couple visibility to the enforcement mode — show overage whenever it exists.
    Then run the WASM build gate.
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "ShiftplanPaidOverageSectionHeader" src/page/shiftplan.rs && grep -q "bg-warn-soft border border-warn rounded-md" src/page/shiftplan.rs && grep -q "current_paid_count" src/page/shiftplan.rs && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `shiftplan.rs` renders a section with container class `mx-4 my-3 px-4 py-3 bg-warn-soft border border-warn rounded-md print:hidden` and heading `text-h2 font-semibold pb-2 text-warn` using `Key::ShiftplanPaidOverageSectionHeader`
    - The list is computed from `current_paid_count > max` over loaded-week slots and is NOT gated by `is_shiftplanner` (all roles)
    - The section is positioned after the `booking_warnings` banner and before `ShiftplanTabBar`
    - When no slot is over its limit, the section is not rendered
    - WASM build exits 0
  </acceptance_criteria>
  <done>A persistent, all-roles overage section lists every over-limit slot for the loaded week; hidden when none; client-side only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend response → frontend handler | HTTP status drives UI branch; must not mis-handle the block status |
| client state → overage render | Read-only display of already-loaded counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-12 | Information Disclosure | overage section visible to all roles | accept | D-24-03/D-23-05 deliberately make overage visible to all roles; the data (slot label + paid count vs max) is non-sensitive scheduling info already rendered in the week view. No names/PII added. |
| T-24-13 | Tampering | client-only block message | mitigate | The inline message is purely advisory UX; the actual block is enforced server-side (24-02). A user cannot bypass enforcement by manipulating the frontend — the backend rejects the booking with 409 regardless of what the client renders. |
| T-24-14 | Spoofing | wrong status handled as block | accept | Only the exact 409 CONFLICT status (matching the 24-01 mapping) triggers the inline block; other errors fall through to existing handling, so a generic failure is not mislabeled as a paid-limit block. |
</threat_model>

<verification>
- 409 block → inline localized text-bad message at the slot (not silent, not a dialog); clears on next success.
- Overage section persistent, all roles, client-side, hidden when empty, correct WARN tokens + heading.
- WASM build green.
</verification>

<success_criteria>
- D-24-05 inline block UX and D-24-03 persistent overage section delivered without backend/DTO changes.
</success_criteria>

<output>
After completion, create `.planning/phases/24-paid-capacity-enforcement-config/24-05-SUMMARY.md`
</output>
