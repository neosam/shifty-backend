---
phase: 24-paid-capacity-enforcement-config
plan: 04
type: execute
wave: 2
depends_on: [24-03]
files_modified:
  - shifty-dioxus/src/page/settings.rs
  - shifty-dioxus/src/page/mod.rs
  - shifty-dioxus/src/router.rs
  - shifty-dioxus/src/component/top_bar.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
autonomous: true
requirements: [D-24-06]
must_haves:
  truths:
    - "D-24-06: A new admin-gated /settings/ route renders a SettingsPage with exactly ONE toggle (paid-limit hard/soft) that flips via the existing Toggle REST API"
    - "The Settings nav entry is visible only to admins (is_admin_target) and reuses Key::Settings"
    - "Toggle initial state loads from GET /toggle/paid_limit_hard_enforcement/enabled; click PUTs enable/disable"
  artifacts:
    - path: "shifty-dioxus/src/page/settings.rs"
      provides: "SettingsPage component with single paid-limit toggle, inline saved/error feedback"
      contains: "paid_limit_hard_enforcement"
    - path: "shifty-dioxus/src/router.rs"
      provides: "Route::Settings {} at /settings/ + Settings alias"
      contains: "/settings/"
    - path: "shifty-dioxus/src/component/top_bar.rs"
      provides: "NavTarget::Settings wired across all 6 nav sites, admin-gated"
      contains: "NavTarget::Settings"
    - path: "shifty-dioxus/src/api.rs"
      provides: "set_toggle + get_toggle_enabled REST client fns"
      contains: "/toggle/"
  key_links:
    - from: "SettingsPage toggle click"
      to: "PUT /toggle/paid_limit_hard_enforcement/{enable|disable}"
      via: "api::set_toggle through loader"
      pattern: "enable|disable"
    - from: "NavVisibility.settings"
      to: "is_admin_target(NavTarget::Settings)"
      via: "top_bar nav wiring"
      pattern: "NavTarget::Settings"
---

<objective>
Build the admin-gated Settings surface (D-24-06): a new `/settings/` route + nav entry + `SettingsPage` containing exactly ONE toggle (paid-limit hard/soft) that flips via the existing Toggle REST API. Depends on 24-03 for i18n keys. Independent of the backend enforcement and of plan 24-05.

Deliberately NOT a generic toggle list — only this one switch (scope per D-24-06; deferred generic UI is out of scope).
Output: SettingsPage, route, barrel export, six-site nav wiring, toggle REST client.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md
@.planning/phases/24-paid-capacity-enforcement-config/24-PATTERNS.md

<interfaces>
<!-- Analogs verified 2026-06-27. -->
UserManagementPage (src/page/user_management.rs:38-45, imports :10-23) — page skeleton analog.
api::update_shiftplan PUT (src/api.rs:119-131) — reqwest PUT + status-check analog.
loader delegation shape (src/loader.rs:286-296, register_user_to_slot) — Result<_, ShiftyError> wrapper.
router enum + pub use aliases (src/router.rs:23-59); UserManagementPage route arm at :44-45.
page/mod.rs barrel (:1-34): `pub mod` + `pub use`.
top_bar.rs NavTarget::UserManagement chain: NavVisibility struct :21-31, nav_visibility() :41-56 (`user_management: has("admin")` :54), NavTarget enum :60-70, is_active_for() :72-95, is_admin_target() :120-128, TopBarRouted items builder :422-428; test helpers nav_items_for_visibility :1123+, nav_visibility_admin_shows_user_management_and_templates :776.

Backend toggle REST routes (rest/src/toggle.rs:23-25):
  GET  /toggle/{name}/enabled
  PUT  /toggle/{name}/enable
  PUT  /toggle/{name}/disable

Key::Settings exists in all 3 locales (En "Settings" / De "Einstellungen" / Cs "Nastavení") — REUSE, no new key.
New i18n keys from 24-03: SettingsPaidLimitToggleLabel, SettingsPaidLimitToggleDescription, SettingsPaidLimitToggleOn, SettingsPaidLimitToggleOff, SettingsSaved, SettingsSaveError.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle REST client (api.rs + loader.rs)</name>
  <read_first>
    - shifty-dioxus/src/api.rs:119-131 (update_shiftplan PUT analog)
    - shifty-dioxus/src/loader.rs:286-296 (register_user_to_slot delegation shape)
    - rest/src/toggle.rs:23-25 (route shapes)
  </read_first>
  <action>
    1. In `shifty-dioxus/src/api.rs` add two functions (enable/disable have NO body):
    ```rust
    pub async fn set_toggle(config: Config, name: &str, enabled: bool) -> Result<(), reqwest::Error> {
        let verb = if enabled { "enable" } else { "disable" };
        let url = format!("{}/toggle/{}/{}", config.backend, name, verb);
        let client = reqwest::Client::new();
        client.put(url).send().await?.error_for_status()?;
        Ok(())
    }
    pub async fn get_toggle_enabled(config: Config, name: &str) -> Result<bool, reqwest::Error> {
        let url = format!("{}/toggle/{}/enabled", config.backend, name);
        Ok(reqwest::get(url).await?.json().await?)
    }
    ```
    Match the existing `Config`/`config.backend` access pattern used by `update_shiftplan`.
    2. In `shifty-dioxus/src/loader.rs` add thin wrappers returning `Result<_, ShiftyError>` (mirror `register_user_to_slot` delegation): e.g. `set_toggle(config, name, enabled)` and `get_toggle_enabled(config, name) -> Result<bool, ShiftyError>` delegating to the api fns and mapping the error into `ShiftyError`.
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -q "/toggle/" src/api.rs && grep -q "set_toggle" src/loader.rs && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/api.rs` contains `set_toggle` (PUT enable/disable) and `get_toggle_enabled` (GET /enabled)
    - `src/loader.rs` exposes `ShiftyError`-returning wrappers for both
    - WASM build exits 0
  </acceptance_criteria>
  <done>Toggle REST client available to the page layer.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsPage + route + barrel export</name>
  <read_first>
    - shifty-dioxus/src/page/user_management.rs:10-23,38-45 (page skeleton + imports)
    - shifty-dioxus/src/router.rs:23-59 (route enum + pub use aliases, UserManagementPage arm :44-45)
    - shifty-dioxus/src/page/mod.rs:1-34 (barrel)
    - .planning/phases/24-paid-capacity-enforcement-config/24-UI-SPEC.md §Surface 1 + "Key Tailwind class strings"
  </read_first>
  <action>
    1. Create `shifty-dioxus/src/page/settings.rs` with `#[component] pub fn SettingsPage() -> Element`, mirroring `UserManagementPage`. Layout per UI-SPEC §Surface 1 with these EXACT class strings:
       - Page heading: `class: "text-h2 font-semibold pb-4"`, text `i18n.t(Key::Settings)`.
       - Card: `class: "bg-surface border border-border rounded-md p-4 flex flex-col gap-3"`.
       - Toggle row: label `class: "text-body text-ink font-semibold"` (`Key::SettingsPaidLimitToggleLabel`) + description `class: "text-small text-ink-soft"` (`Key::SettingsPaidLimitToggleDescription`).
       - Toggle `<button>` (NOT checkbox) with `aria-pressed` reflecting state:
         - Off (soft): `class: "px-3 py-2 rounded-md border border-border text-ink text-body bg-surface hover:bg-surface-alt"`, text `Key::SettingsPaidLimitToggleOff`.
         - On (hard): `class: "px-3 py-2 rounded-md border border-bad text-bad text-body font-semibold bg-bad-soft"`, text `Key::SettingsPaidLimitToggleOn`.
       - Inline success flash: `class: "text-small text-ink-muted"` (`Key::SettingsSaved`); inline error: `class: "text-bad text-small font-normal"` (`Key::SettingsSaveError`).
       Behavior (UI-SPEC §Interaction): load initial state via `loader::get_toggle_enabled(config, "paid_limit_hard_enforcement")` (use a `use_resource` or signal-init); on click → disable the button during the in-flight request, call `loader::set_toggle(config, "paid_limit_hard_enforcement", !current)`; on success flip state + show `SettingsSaved` (clears on next interaction); on error show `SettingsSaveError` and revert. No confirm dialog (non-blocking inline feedback).
       Include the `TopBar` at the top consistent with other pages.
    2. In `shifty-dioxus/src/page/mod.rs` add `pub mod settings;` and `pub use settings::SettingsPage;`.
    3. In `shifty-dioxus/src/router.rs` add `pub use crate::page::SettingsPage as Settings;` and the route arm:
    ```rust
    #[route("/settings/")]
    Settings {},
    ```
  </action>
  <verify>
    <automated>cd shifty-dioxus && test -f src/page/settings.rs && grep -q "paid_limit_hard_enforcement" src/page/settings.rs && grep -q "SettingsPage" src/page/mod.rs && grep -q "/settings/" src/router.rs && grep -q "aria-pressed\|aria_pressed" src/page/settings.rs && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/page/settings.rs` exists, references `paid_limit_hard_enforcement`, uses the exact UI-SPEC class strings, has an `aria-pressed` toggle button
    - `src/page/mod.rs` exports `SettingsPage`
    - `src/router.rs` has `#[route("/settings/")] Settings {}` and the `Settings` alias
    - WASM build exits 0
  </acceptance_criteria>
  <done>SettingsPage renders the single paid-limit toggle wired to the Toggle REST API; route reachable at /settings/.</done>
</task>

<task type="auto">
  <name>Task 3: Admin-gated Settings nav wiring (top_bar.rs, 6 sites) + WASM gate</name>
  <read_first>
    - shifty-dioxus/src/component/top_bar.rs:21-31,41-56,60-70,72-95,120-128,422-428 (the 6 NavTarget::UserManagement sites) + test helpers :776,:1123+
    - shifty-dioxus/CLAUDE.md (WASM build gate)
  </read_first>
  <action>
    Wire `NavTarget::Settings` across ALL of these sites in lockstep (mirror `NavTarget::UserManagement`), so the build/tests don't break:
    1. `NavVisibility` struct (:21-31): add `pub settings: bool,`.
    2. `nav_visibility()` (:41-56): add `settings: has("admin"),` (mirror `user_management: has("admin")` :54).
    3. `NavTarget` enum (:60-70): add variant `Settings,`.
    4. `is_active_for()` (:72-95): add `NavTarget::Settings => matches!(route, Route::Settings {}),`.
    5. `is_admin_target()` (:120-128): add `NavTarget::Settings` to the matches! set (puts it in the admin dropdown, D-24-06).
    6. `TopBarRouted` items builder (:422-428): add
    ```rust
    if visibility.settings {
        items.push((
            NavTarget::Settings,
            Route::Settings {},
            i18n.t(Key::Settings).to_string(),
        ));
    }
    ```
    Reuse `Key::Settings` (no new key). Update the test helpers at the bottom of `top_bar.rs` (`nav_items_for_visibility` :1123+, `nav_visibility_admin_shows_user_management_and_templates` :776) with a parallel assertion for the new `settings` field (admin → settings present; non-admin → absent).
    Then run the frontend test + WASM gate.
  </action>
  <verify>
    <automated>cd shifty-dioxus && grep -c "NavTarget::Settings" src/component/top_bar.rs && grep -q "settings: has(\"admin\")\|settings:has(\"admin\")" src/component/top_bar.rs && cargo test --lib top_bar 2>&1 | tail -10 && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/component/top_bar.rs` references `NavTarget::Settings` in at least 4 distinct sites (enum, is_active_for, is_admin_target, items builder)
    - `nav_visibility()` sets `settings` from `has("admin")`
    - top_bar tests assert Settings nav visible for admin and hidden for non-admin
    - `cargo test --lib top_bar` passes
    - `cargo build --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>Settings nav entry is admin-gated and routes to /settings/; nav tests pass; WASM build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Toggle REST | Enable/disable PUT must remain server-side authorized |
| nav visibility (client) | Client-side admin gate is convenience only; server enforces auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Elevation of Privilege | client-side nav gate (`has("admin")`) | mitigate | The client nav gate is cosmetic; the actual authorization is enforced server-side by the existing `toggle_admin`-gated Toggle REST endpoints (rest/src/toggle.rs). A non-admin who manually navigates to /settings/ or crafts the PUT is rejected by the backend (403/permission error). No new backend endpoint is added. |
| T-24-10 | Tampering | toggle PUT from browser | mitigate | enable/disable target only the fixed name `paid_limit_hard_enforcement`; no user-supplied toggle name reaches the backend from this page, and the backend privilege check is unchanged. |
| T-24-11 | Information Disclosure | GET /enabled exposes mode | accept | The boolean enforcement mode is non-sensitive operational config; reading it client-side is acceptable. |
</threat_model>

<verification>
- /settings/ route exists and is admin-gated (nav + is_admin_target).
- Single toggle flips via existing Toggle REST API; inline saved/error feedback; aria-pressed.
- Reuses Key::Settings; uses 24-03 keys for the toggle copy.
- top_bar tests + WASM build green.
</verification>

<success_criteria>
- D-24-06 delivered: minimal one-switch admin Settings page wired to the Toggle REST API.
</success_criteria>

<output>
After completion, create `.planning/phases/24-paid-capacity-enforcement-config/24-04-SUMMARY.md`
</output>
