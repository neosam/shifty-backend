---
phase: 32-admin-impersonation-frontend-audit-schicht-fe-be
plan: 03
type: execute
wave: 2
depends_on: ["32-02"]
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/component/impersonation_banner.rs
  - shifty-dioxus/src/component/mod.rs
  - shifty-dioxus/src/app.rs
  - shifty-dioxus/src/page/user_management.rs
autonomous: true
requirements: [IMP-01, IMP-02]
must_haves:
  truths:
    - "SC1 / D-32-04: while impersonating, a persistent NON-closable amber banner appears on EVERY page (mounted in app.rs above the router) showing the impersonated user_id and a one-click Stop button; static Tailwind classes only (Pitfall 5)."
    - "D-32-03: the banner shows ImpersonateTO.user_id directly (username), with no name lookup and no ImpersonateTO change."
    - "SC2 / D-32-05 (IMP-02): GET /admin/impersonate (LoadStatus) is fired as the first init call on app mount, so the banner re-appears automatically after a hard page reload — not only from the start callback."
    - "SC1 / D-32-07 (IMP-01): impersonation starts from the HR/admin person list — the User-Management 'Users' tab gains a per-row 'Act as this person' action that calls Start with the row's username (the auth identity the POST endpoint needs)."
    - "D-32-02a (P10): the banner text notes the known limitation that admin-only functions are disabled while impersonating a non-admin and that stop is always available."
    - "D-32-08: all new user-visible strings (banner text, Stop button, 'Act as this person' action, P10 hint) exist in de, en and cs."
  artifacts:
    - shifty-dioxus/src/i18n/{mod.rs,en.rs,de.rs,cs.rs} (new Key variants + de/en/cs translations)
    - shifty-dioxus/src/component/impersonation_banner.rs (the banner)
    - shifty-dioxus/src/component/mod.rs (registration)
    - shifty-dioxus/src/app.rs (banner mount above router + LoadStatus init on mount)
    - shifty-dioxus/src/page/user_management.rs ('Act as this person' per-user action)
  key_links:
    - "IMPERSONATE_STORE (Plan 02) → ImpersonationBanner visibility/label in app.rs."
    - "Users-tab row username → ImpersonateAction::Start(username) → POST /admin/impersonate/{user_id}."
    - "App mount → ImpersonateAction::LoadStatus → GET /admin/impersonate → banner survives reload."
---

<objective>
Build the impersonation UI on top of Plan 02's runtime: the i18n strings (de/en/cs), a
persistent non-closable amber banner mounted on every page in app.rs (showing the impersonated
username + a one-click Stop + the P10 hint), the first-init GET-status call on app mount so the
banner survives a hard reload (IMP-02), and the entry point — an "Act as this person" action in
the User-Management Users tab whose row username is exactly the auth identity the POST endpoint
needs (IMP-01 / D-32-07).

Purpose: Deliver SC1 (start from person list → amber banner on every page), SC2 (banner survives
reload), and the i18n coverage (D-32-08).
Output: i18n keys, the banner component, app.rs mount + init, and the person-list entry.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-admin-impersonation-frontend-audit-schicht-fe-be/32-CONTEXT.md
@shifty-dioxus/src/app.rs
@shifty-dioxus/src/page/user_management.rs
@shifty-dioxus/src/service/impersonate.rs
@shifty-dioxus/src/i18n/mod.rs
@shifty-dioxus/src/i18n/en.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys for impersonation (de/en/cs)</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (enum Key :55 — add new variants near related top-bar/admin keys)
    - shifty-dioxus/src/i18n/en.rs (add_text(Locale::En, Key::..., "...") pattern; supports {placeholder} interpolation as in Key::ShiftplanCalendarWeek)
    - shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs (mirror every key in all three locales)
  </read_first>
  <action>
    Per D-32-08, add Key variants and translations in ALL THREE locales (en, de, cs) for:
    ImpersonateActAs (person-list action label, e.g. EN "Act as this person"), ImpersonateBanner
    (banner body with a {user} placeholder, e.g. EN "You are acting as {user}."), ImpersonateStop
    (Stop button, e.g. EN "Stop impersonation"), and ImpersonateP10Hint (P10 note, e.g. EN
    "Admin-only functions are disabled while acting as a non-admin; you can stop at any time.").
    Provide natural German and Czech translations (not English placeholders). Keep the {user}
    placeholder identical across locales so the banner can string-replace it. Add each Key to the
    enum in mod.rs and an add_text line in each of en.rs/de.rs/cs.rs.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown)</automated>
  </verify>
  <done>Four new impersonation Key variants exist and are translated in en, de and cs; wasm build green (no missing-arm/locale gaps).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImpersonationBanner component + mount in app.rs + first-init LoadStatus</name>
  <files>shifty-dioxus/src/component/impersonation_banner.rs, shifty-dioxus/src/component/mod.rs, shifty-dioxus/src/app.rs</files>
  <read_first>
    - shifty-dioxus/src/app.rs (App() coroutine registrations :13-28; the authenticated Auth branch rendering Router::<Route> :49-52 — mount point above the router; web_sys::window already imported)
    - shifty-dioxus/src/service/impersonate.rs (IMPERSONATE_STORE, ImpersonateAction::{LoadStatus,Stop}, impersonate_service — from Plan 02)
    - shifty-dioxus/src/component/mod.rs (pub mod + pub use pattern)
    - shifty-dioxus/src/page/user_management.rs (#[cfg(test)] dioxus_ssr::render harness — mirror for the banner SSR test)
    - shifty-dioxus/src/i18n/mod.rs (I18N read + t(Key) + {user} string replace like DeleteUserConfirmBody in page/user_management.rs)
  </read_first>
  <behavior>
    - When IMPERSONATE_STORE.impersonating is false, the banner renders nothing (no amber bar, no Stop button).
    - When impersonating with user_id Some("alex"), the banner renders the amber bar, interpolates "alex"
      into the banner text, shows a Stop button, and shows the P10 hint.
    - The banner has no close/dismiss control (non-closable, D-32-04).
  </behavior>
  <action>
    Create shifty-dioxus/src/component/impersonation_banner.rs exposing `#[component] pub fn
    ImpersonationBanner() -> Element`. Read IMPERSONATE_STORE and I18N. If not impersonating, return
    an empty rsx!{}. Otherwise render a persistent amber banner using STATIC Tailwind utility classes
    only (Pitfall 5 — no interpolated class strings; reuse the project's warn token classes, e.g. the
    bg-warn-soft / text-warn family already used elsewhere — grep the codebase for the established amber/
    warn classes and reuse them). Show the banner text with the impersonated user_id substituted into the
    {user} placeholder (D-32-03: raw user_id, no lookup), the P10 hint (Key::ImpersonateP10Hint), and a
    Stop button (Key::ImpersonateStop) whose on_click dispatches ImpersonateAction::Stop via
    use_coroutine_handle::<ImpersonateAction>(). Provide NO close button (D-32-04 non-closable). Register
    in component/mod.rs (pub mod + pub use ImpersonationBanner).
    In app.rs: register the service coroutine `use_coroutine(service::impersonate::impersonate_service)`
    alongside the others, and add a `use_effect` that sends ImpersonateAction::LoadStatus once on mount
    (D-32-05 / SC2 — the first init call so the banner survives a hard reload). Mount `ImpersonationBanner {}`
    in the authenticated branch ABOVE `Router::<Route> {}` so it shows on every route (D-32-04). Keep it
    inside the authenticated path (impersonation implies a logged-in admin).
    Add a `#[cfg(test)] mod tests` using the dioxus_ssr::render harness (mirror page/user_management.rs):
    one test asserting an empty/not-impersonating store renders no Stop button / no amber bar; one test
    seeding IMPERSONATE_STORE (or a prop-driven inner render) asserting the amber class, the interpolated
    username, the Stop button, and the absence of any close/dismiss control.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo test impersonation_banner) && (cd shifty-dioxus && cargo build --target wasm32-unknown-unknown)</automated>
  </verify>
  <done>ImpersonationBanner renders nothing when not impersonating and an amber, non-closable, on-every-page banner (interpolated username + P10 hint + Stop) when impersonating; app.rs fires LoadStatus on mount and mounts the banner above the router; SSR tests pass; wasm build green.</done>
</task>

<task type="auto">
  <name>Task 3: 'Act as this person' entry in the User-Management Users tab (IMP-01 / D-32-07)</name>
  <files>shifty-dioxus/src/page/user_management.rs</files>
  <read_first>
    - shifty-dioxus/src/page/user_management.rs (UsersTabContent :354 — per-user row renders `user.username`; the actions cell :447-469 with the Edit link + Danger delete Btn; Btn/BtnVariant usage; existing SSR tests :514+)
    - shifty-dioxus/src/service/impersonate.rs (ImpersonateAction::Start(ImStr) — from Plan 02)
    - shifty-dioxus/src/i18n/mod.rs (Key::ImpersonateActAs — from Task 1)
  </read_first>
  <action>
    Per D-32-07 / IMP-01: in UsersTabContent's per-row actions cell, add an "Act as this person"
    action (a Btn, Secondary variant) next to Edit/Delete. Its on_click dispatches
    ImpersonateAction::Start(username) using use_coroutine_handle::<ImpersonateAction>(); the row's
    `user.username` IS the auth identity the POST /admin/impersonate/{user_id} endpoint expects — pass
    it straight through (no SalesPerson→user mapping needed; the Users tab already holds the auth username).
    Label via Key::ImpersonateActAs. The service performs the API call + full reload (Plan 02), so no
    extra navigation is needed here. Keep the existing Edit/Delete actions intact. If a small SSR test is
    cheap, assert the new action label renders for a user row (mirror the existing render_users_with tests);
    otherwise rely on the wasm build + Plan 02 service tests.
    Note for verification (Backend-Roundtrip): start uses the username path segment — confirm the live
    POST path is /admin/impersonate/{username} (matches rest/src/impersonate.rs route), since create vs
    edit-style paths have bitten this project before.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo test user_management) && (cd shifty-dioxus && cargo build --target wasm32-unknown-unknown)</automated>
  </verify>
  <done>The Users tab shows a localized 'Act as this person' action per user row that dispatches Start with the row username (the auth user_id); Edit/Delete unchanged; user_management tests pass; wasm build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI 'Act as' → backend | The action sends a username as the impersonation target; the server re-verifies admin + target existence (Plan 01) — the UI cannot bypass the gate. |
| impersonation state → every rendered page | The non-closable banner guarantees the acting admin is always aware they are impersonating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-07 | Spoofing | 'Act as this person' action | medium | mitigate | Server re-verifies admin (raw session.user_id) + target existence on POST; FE cannot forge privilege; non-admins never see a working action (status/gate are server-side). |
| T-32-08 | Repudiation / awareness | impersonation visibility | low | mitigate | Persistent non-closable banner on every page + P10 hint; reload-persistent via GET-status init (D-32-04/D-32-05). |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies; UI built from existing dioxus/web_sys/i18n. |
</threat_model>

<verification>
Frontend gate from shifty-dioxus/ (use `nix develop --command` / `nix-shell -p openssl pkg-config lld` if the wasm build needs the env):
- `cargo test` (banner + user_management SSR tests pass)
- `cargo build --target wasm32-unknown-unknown`
Do NOT run `clippy -- -D warnings` on the dioxus workspace.
Optional NON-BLOCKING manual smoke (not pixel-automatable, note in SUMMARY as deferred UAT):
start impersonation from the Users tab → amber banner appears on every page with the username + Stop;
hard-reload → banner returns; click Stop → page reloads, banner gone, current view is the real admin's.
</verification>

<success_criteria>
- SC1: start from the person list → non-closable amber banner with username + Stop on every page (D-32-04, D-32-07).
- SC2: banner survives a hard reload via the first-init GET-status call (D-32-05).
- D-32-03: banner shows the raw username; D-32-02a P10 hint present.
- D-32-08: banner/Stop/Act-as/P10 strings in en, de and cs.
- wasm build green; SSR tests pass.
</success_criteria>

<constraints>
- VCS = jj, commits MANUAL (commit_docs:false). Do NOT git/jj commit — leave the working tree dirty.
- Do NOT run clippy -D warnings on shifty-dioxus. No new dependencies. No ImpersonateTO change. Static Tailwind classes only (Pitfall 5). Banner is NON-closable (no dismiss control).
</constraints>

<output>
Create `.planning/phases/32-admin-impersonation-frontend-audit-schicht-fe-be/32-03-SUMMARY.md` when done.
Do NOT commit — leave changes uncommitted for the user to review and commit with jj.
</output>
