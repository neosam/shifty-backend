---
phase: 32-admin-impersonation-frontend-audit-schicht-fe-be
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/service/impersonate.rs
  - shifty-dioxus/src/service/mod.rs
autonomous: true
requirements: [IMP-02, IMP-04, IMP-01]
must_haves:
  truths:
    - "D-32-05 (IMP-02): the impersonation status is loaded via GET /admin/impersonate and mapped into a global store (impersonating flag + user_id); a pure status-mapping helper turns ImpersonateTO into the store and is unit-tested."
    - "D-32-05: the service exposes a LoadStatus action so the app can fire GET /admin/impersonate as its first init call on mount, making the banner survive a hard reload."
    - "D-32-06 (IMP-04 / SC4): on STOP (DELETE /admin/impersonate) the service performs a full client reload (window.location.reload) so every user-bound store — including the component-local current_sales_person — is re-initialized for the real admin, leaving no stale impersonated state."
    - "D-32-06 (SC1): on START (POST /admin/impersonate/{user_id}) the service symmetrically performs a full client reload so the impersonated view is immediately correct and the banner re-appears via the GET-status init."
    - "D-32-03: the store carries the raw user_id/username from ImpersonateTO unchanged (no name lookup, no ImpersonateTO change)."
  artifacts:
    - shifty-dioxus/src/api.rs (start_impersonate, stop_impersonate, get_impersonate_status)
    - shifty-dioxus/src/service/impersonate.rs (IMPERSONATE_STORE, ImpersonateAction, impersonate_service, pure status-mapping helper)
    - shifty-dioxus/src/service/mod.rs (module registration)
  key_links:
    - "GET /admin/impersonate → ImpersonateTO → pure status-mapping helper → IMPERSONATE_STORE → banner (Plan 03)."
    - "Start/Stop actions call api.rs then window.location.reload() — the reload re-fires LoadStatus on remount, the single source of truth for banner visibility and store freshness."
---

<objective>
Build the frontend impersonation runtime (no UI yet): three api.rs client calls against the
already-existing /admin/impersonate endpoints, and a service module mirroring the established
GlobalSignal + coroutine pattern that loads impersonation status (survives reload, IMP-02),
starts/stops impersonation, and performs the store teardown via a full client reload on both
start and stop (IMP-04 / SC4). Status mapping is a pure, unit-tested helper.

Purpose: Provide the data + actions the banner and the person-list entry (Plan 03) consume,
with IMP-02 reload-persistence and IMP-04 teardown wired here at the service layer.
Output: api.rs calls + service/impersonate.rs (store, actions, coroutine, tested helper).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-admin-impersonation-frontend-audit-schicht-fe-be/32-CONTEXT.md
@shifty-dioxus/src/api.rs
@shifty-dioxus/src/service/weekly_summary.rs
@shifty-dioxus/src/error.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: three api.rs client calls (start / stop / status)</name>
  <files>shifty-dioxus/src/api.rs</files>
  <read_first>
    - shifty-dioxus/src/api.rs (imports incl. ImStr + rest_types; reqwest patterns — get_current_sales_person GET parsing :298, post_user_to_sales_person POST :367, delete_user_from_sales_person DELETE :381; `format!("{}/...", config.backend)` URL join)
    - rest-types ImpersonateTO { impersonating: bool, user_id: Option<Arc<str>> }
    - rest/src/impersonate.rs (route shapes: POST /{user_id}, DELETE "", GET "" under /admin/impersonate; responses are ImpersonateTO JSON)
  </read_first>
  <action>
    Add three functions to shifty-dioxus/src/api.rs, mirroring the existing reqwest conventions
    (reqwest::Client::new() for POST/DELETE, reqwest::get for GET; error_for_status_ref(); ImStr
    import already present). Import ImpersonateTO from rest_types.
    - get_impersonate_status(config: Config) -> Result<ImpersonateTO, reqwest::Error>: GET
      `{backend}/admin/impersonate`, parse JSON into ImpersonateTO.
    - start_impersonate(config: Config, user_id: ImStr) -> Result<ImpersonateTO, reqwest::Error>:
      POST `{backend}/admin/impersonate/{user_id}` (user_id is the auth username/identity — it is
      the path segment, NOT a body), parse JSON into ImpersonateTO.
    - stop_impersonate(config: Config) -> Result<ImpersonateTO, reqwest::Error>: DELETE
      `{backend}/admin/impersonate`, parse JSON into ImpersonateTO.
    Per D-32-03 do NOT alter ImpersonateTO or do any name lookup. Keep the existing info! logging style.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown)</automated>
  </verify>
  <done>Three impersonation api.rs calls compile to wasm and use the correct method/URL shapes against the existing /admin/impersonate endpoints, returning ImpersonateTO.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: service/impersonate.rs — store, pure status mapping, coroutine, start/stop teardown reload</name>
  <files>shifty-dioxus/src/service/impersonate.rs, shifty-dioxus/src/service/mod.rs</files>
  <read_first>
    - shifty-dioxus/src/service/weekly_summary.rs (GlobalSignal store + action enum + `async fn *_service(mut rx: UnboundedReceiver<Action>)` coroutine + ERROR_STORE error routing — the exact pattern to mirror)
    - shifty-dioxus/src/service/mod.rs (pub mod list — add `pub mod impersonate;` alphabetically)
    - shifty-dioxus/src/error.rs (:66 — the proven `web_sys::window().expect("no window").location().reload()` teardown pattern)
    - shifty-dioxus/src/base_types.rs (ImStr)
    - shifty-dioxus/src/api.rs (the three calls from Task 1)
  </read_first>
  <behavior>
    - status_from_to(ImpersonateTO): produces an ImpersonateStore where impersonating mirrors the TO
      flag and user_id mirrors the TO user_id (mapped to Option<ImStr>); D-32-03 keeps the raw id.
    - Test: impersonating=true + Some("alex") → store.impersonating==true, store.user_id==Some("alex").
    - Test: impersonating=false + None → store.impersonating==false, store.user_id==None.
    - Test: defensive — impersonating=true + None maps to impersonating==true with user_id None (or
      define impersonating to require Some; pick one and pin it in a test).
  </behavior>
  <action>
    Create shifty-dioxus/src/service/impersonate.rs mirroring weekly_summary.rs. Define
    `pub struct ImpersonateStore { pub impersonating: bool, pub user_id: Option<ImStr>, pub loaded: bool }`
    (Clone, Default/PartialEq as the pattern requires) and
    `pub static IMPERSONATE_STORE: GlobalSignal<ImpersonateStore> = GlobalSignal::new(ImpersonateStore::default)`.
    Add a pure free function `status_from_to(to: ImpersonateTO) -> ImpersonateStore` per the behavior
    block (D-32-03: copy user_id straight through, no lookup) with a `#[cfg(test)] mod` unit-testing it.
    Define `pub enum ImpersonateAction { LoadStatus, Start(ImStr), Stop }` and
    `pub async fn impersonate_service(mut rx: UnboundedReceiver<ImpersonateAction>)` that, per action:
    - LoadStatus (D-32-05): call api::get_impersonate_status(CONFIG.read().clone()); on Ok write
      status_from_to(to) (with loaded=true) into IMPERSONATE_STORE; on Err route to ERROR_STORE.
      Note: a 403 (caller is not an admin) is the normal non-admin case — treat a non-2xx/permission
      error as "not impersonating" rather than surfacing a scary error to non-admins (map it to a
      cleared, loaded store); only surface genuine transport errors. Keep this branch simple and
      documented.
    - Start(user_id) (D-32-06 / SC1): call api::start_impersonate(config, user_id); on Ok perform a
      full client reload via web_sys::window().expect("no window").location().reload() (ignore the
      Result, exactly as error.rs:66) so the impersonated view + banner re-initialize from the
      GET-status init on remount; on Err route to ERROR_STORE (no reload).
    - Stop (D-32-06 / IMP-04 / SC4): call api::stop_impersonate(config); on Ok perform the same full
      client reload so every user-bound store — including the component-local current_sales_person in
      shiftplan.rs, which has no global handle — is re-initialized for the real admin with no stale
      impersonated state; on Err route to ERROR_STORE.
    Add a module-level doc comment explaining WHY a full reload is the chosen teardown (current_sales_person
    is a component-local signal; a targeted reload would require a broad, out-of-scope refactor; the
    full reload is the strongest no-stale-state guarantee and re-fires the single GET-status init).
    Register the module in service/mod.rs (`pub mod impersonate;`).
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo test impersonate) && (cd shifty-dioxus && cargo build --target wasm32-unknown-unknown)</automated>
  </verify>
  <done>service/impersonate.rs exists with IMPERSONATE_STORE, ImpersonateAction (LoadStatus/Start/Stop), impersonate_service coroutine, and a unit-tested status_from_to; Start and Stop both reload the page after a successful API call (IMP-04 teardown + SC1); module registered; wasm build green and the status-mapping tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend /admin/impersonate | FE calls are admin-gated server-side; the FE renders only what the server returns and cannot bypass the gate. |
| impersonation stop → user-bound FE stores | Stale impersonated state must not survive a stop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Information Disclosure | get_impersonate_status for non-admins | low | accept | Endpoint is admin-gated server-side (Plan 01); a 403 is mapped to a cleared "not impersonating" store so non-admins see nothing and no scary error. |
| T-32-06 | Tampering | stale current_sales_person / user-bound stores after stop | medium | mitigate | Full client reload on stop (and start) re-initializes all stores from the post-change session (D-32-06 / IMP-04). |
| T-32-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies (reqwest/web_sys/dioxus already present); no install tasks. |
</threat_model>

<verification>
Frontend gate from shifty-dioxus/ (use `nix develop --command` / `nix-shell -p openssl pkg-config lld` if the wasm build needs the env):
- `cargo test` (status-mapping unit tests pass)
- `cargo build --target wasm32-unknown-unknown`
Do NOT run `clippy -- -D warnings` on the dioxus workspace (pre-existing lints; not gated).
</verification>

<success_criteria>
- IMP-02: status loadable via GET /admin/impersonate into a global store with a tested pure mapping (D-32-05).
- IMP-04 / SC4: stop triggers a full client reload that clears all user-bound stale state (D-32-06).
- SC1 support: start triggers a reload so the impersonated view + banner are immediately correct.
- D-32-03: user_id carried through unchanged; no ImpersonateTO change.
- wasm build green; service unit tests pass.
</success_criteria>

<constraints>
- VCS = jj, commits MANUAL (commit_docs:false). Do NOT git/jj commit — leave the working tree dirty.
- Do NOT run clippy -D warnings on shifty-dioxus. No new dependencies. No ImpersonateTO change.
</constraints>

<output>
Create `.planning/phases/32-admin-impersonation-frontend-audit-schicht-fe-be/32-02-SUMMARY.md` when done.
Do NOT commit — leave changes uncommitted for the user to review and commit with jj.
</output>
