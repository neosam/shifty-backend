---
phase: quick-260612-o7t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/page/absences.rs
autonomous: true
requirements:
  - QUICK-260612-o7t
must_haves:
  truths:
    - "D1: When HR selects a single Sales Person, StatsGrid counts sick/unpaid/active only for that person"
    - "D1: Selecting a person scopes StatsGrid by person ONLY — category/status filters do NOT affect StatsGrid"
    - "D2: When HR selects a single Sales Person, VacationEntitlementCard shows that person's self-view (hero + 5 StatBoxes), not the team aggregate"
    - "D2: When no person is selected (None), VacationEntitlementCard keeps the existing HR team aggregate + per-person list"
    - "D3: Non-HR (employee) path renders unchanged"
    - "D4: No backend / rest-types changes — pure frontend filtering over already-loaded stores"
  artifacts:
    - path: "shifty-dioxus/src/page/absences.rs"
      provides: "stats_for_person pure helper + person-scoped StatsGrid + self-view VacationEntitlementCard on selection"
      contains: "fn stats_for_person"
  key_links:
    - from: "AbsencesPage render (StatsGrid call site)"
      to: "stats_for_person via person_filter_val"
      via: "person_filter prop / pre-filtered absences"
      pattern: "stats_for_person|person_filter"
    - from: "AbsencesPage render (VacationEntitlementCard call site)"
      to: "matching VacationBalance from vacation_team"
      via: "selected_person prop"
      pattern: "selected_person"
---

<objective>
HR's AbsencesPage person-filter dropdown already scopes the AbsenceList, but the two
aggregate widgets at the top (StatsGrid + VacationEntitlementCard) ignore it. When HR
selects a single Sales Person, those widgets must show exactly that person's view —
"the view the Sales Person sees" — as a pure frontend filter (no impersonation, no
permission change).

Purpose: Fulfill locked decisions D1–D4 from FINDINGS.md. D1 person-scopes StatsGrid
(person dimension ONLY). D2 renders the self-view layout for the selected person's
VacationEntitlementCard. D3/D4: employee path and backend untouched.

Output: A pure, unit-testable `stats_for_person` helper; a person-scoped StatsGrid; a
VacationEntitlementCard that renders the selected person's self-body on selection. New
tests (RED-first for the helper, snapshot for the card self-view).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260612-o7t-absencespage-sales-person-filter/FINDINGS.md
@shifty-dioxus/CLAUDE.md
@shifty-dioxus/src/page/absences.rs

<interfaces>
<!-- Contracts the executor needs — already in the codebase, do NOT explore further. -->

State types (src/state/):
```rust
// src/state/vacation_balance.rs
pub struct VacationBalance {
    pub sales_person_id: Uuid,
    pub year: u32,
    pub entitled_days: f32,
    pub carryover_days: i32,
    pub used_days: f32,
    pub planned_days: f32,
    pub remaining_days: f32,
}

// src/state/absence_period.rs (relevant fields)
pub struct AbsencePeriod {
    pub sales_person_id: Uuid,
    pub category: AbsenceCategory,   // Vacation | SickLeave | UnpaidLeave
    pub from_date: time::Date,
    pub to_date: time::Date,
    // ...
}
```

StatsGrid today (absences.rs ~1285) — Props: `absences: Rc<[AbsencePeriod]>`, `year: u32`,
`today: time::Date`. Loop computes `sick_days`/`unpaid_days` (inclusive-day sums for the
year, by category) and `active_count` (via `compute_status(from,to,today) == Active`).

VacationEntitlementCard today (absences.rs ~221) — Props: `is_hr: bool`, `year: u32`,
`vacation_self: Option<VacationBalance>` (default None), `vacation_team: Rc<[VacationBalance]>`,
`sales_persons: Rc<[SalesPerson]>`. Branches: `is_hr` → `VacationEntitlementHrBody` (team
aggregate + `VacationPerPersonList`), else → `VacationEntitlementSelfBody` (hero + 5 StatBoxes,
self-title `Key::VacationCardSelfTitle` = "Dein Urlaubskonto").

Render call sites (absences.rs ~1873–1884): `person_filter_val: Option<Uuid>` is already
computed at line ~1706. `absences` is the unfiltered `Rc<[AbsencePeriod]>` from
`ABSENCE_STORE`. `vacation_team` is `Rc<[VacationBalance]>` from `VACATION_TEAM_STORE`.

Pure helper already present (`compute_status`) and a TDD precedent `marker_matches_filters`
(predecessor task 260612-nlv) — mirror that style.

Test harness (absences.rs ~1957, `mod tests`): `render(comp)` =
`VirtualDom::new + rebuild_in_place + dioxus_ssr::render`; `pin_de_locale()` installs
`Locale::De` via `use_hook`. Use `date!(yyyy-mm-dd)` for dates.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure stats_for_person helper (RED → GREEN) and person-scope StatsGrid</name>
  <files>shifty-dioxus/src/page/absences.rs</files>
  <behavior>
    New pure free function (no rendering, no signals):
    `fn stats_for_person(absences: &[AbsencePeriod], person_filter: Option<Uuid>, year: u32, today: time::Date) -> (i64, i64, usize)`
    returning `(sick_days, unpaid_days, active_count)` with the SAME math currently inlined
    in StatsGrid (inclusive day sum per category for rows touching `year`; active_count via
    `compute_status == Active`), BUT when `person_filter` is `Some(uuid)` only rows with
    `sales_person_id == uuid` are counted.

    RED tests to write first (in `mod tests`):
    - `stats_for_person_none_counts_all_persons`: two persons, mixed categories → totals span both.
    - `stats_for_person_some_scopes_to_that_person`: same fixture, `Some(person_a)` → only A's
      sick/unpaid/active counted; B's excluded.
    - `stats_for_person_sick_and_unpaid_inclusive_day_count`: a 5-day SickLeave (from..to
      inclusive) for the year → sick_days == 5; an UnpaidLeave counted in unpaid_days.
    - `stats_for_person_active_count_uses_compute_status`: one Active range (today inside),
      one Finished range → active_count == 1.
    - `stats_for_person_excludes_out_of_year`: a range entirely in a different year → not
      counted in sick/unpaid.
  </behavior>
  <action>
    Implement D1. Per FINDINGS recommendation (keep child dumb, smallest diff), add the pure
    helper AND pass person scoping into StatsGrid:
    1. Add `fn stats_for_person(...)` near `compute_status`. Lift the existing StatsGrid loop
       body verbatim into it; add the `person_filter` guard (`if let Some(p) = person_filter
       { if absence.sales_person_id != p { continue; } }`). Keep `in_year` and inclusive-day
       logic identical to the current StatsGrid.
    2. Add `person_filter: Option<Uuid>` to `StatsGridProps` (`#[props(!optional, default = None)]`).
    3. Replace the inline loop in `StatsGrid` with `let (sick_days, unpaid_days, active_count)
       = stats_for_person(&props.absences, props.person_filter, props.year, props.today);`.
       Leave all label/rsx rendering untouched.
    4. At the render call site (~line 1880) pass `person_filter: person_filter_val,` to
       StatsGrid. Keep passing the unfiltered `absences.clone()` (the helper does the scoping).
       Do NOT pass category/status — D1: person dimension ONLY.
    Write the RED tests FIRST and confirm they fail against a stub (or a not-yet-scoped helper)
    before completing the implementation, mirroring `marker_matches_filters` from task 260612-nlv.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test stats_for_person</automated>
    <automated>cd shifty-dioxus && cargo test page::absences</automated>
    <automated>cd shifty-dioxus && nix develop -c cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <done>
    `stats_for_person` exists as a pure function; all 5 new unit tests pass; StatsGrid uses
    the helper and accepts `person_filter`; render passes `person_filter_val`; full
    `shifty-dioxus` test suite green; WASM build exits 0. D1 satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VacationEntitlementCard renders selected person's self-view on selection (D2)</name>
  <files>shifty-dioxus/src/page/absences.rs</files>
  <behavior>
    Snapshot tests (using the existing `render` + `pin_de_locale` harness):
    - `vacation_card_hr_with_selected_person_renders_self_body`: `is_hr: true`,
      `selected_person: Some(person_a)`, a `vacation_team` containing A (remaining e.g. 17)
      and B (remaining e.g. 3). Assert the rendered HTML contains A's remaining number
      (e.g. "17") and the self-hero label (`Key::VacationEntitlementHero` → German text),
      and does NOT contain the per-person-list header (`Key::VacationPerPersonHeader`
      German text) nor B's distinct number — i.e. team aggregate is gone.
    - `vacation_card_hr_without_selection_renders_team_aggregate`: `is_hr: true`,
      `selected_person: None`, same team → HTML still contains the per-person-list header
      (team view unchanged).
    - `vacation_card_hr_selected_person_missing_from_team_falls_back`: `selected_person:
      Some(unknown_uuid)` not in team → renders self-body with the zero/empty
      VacationBalance default (no panic).
  </behavior>
  <action>
    Implement D2 + D3 (employee path unchanged). Smallest-diff approach — add one prop and a
    branch inside the existing component:
    1. Add `selected_person: Option<Uuid>` to `VacationEntitlementCardProps`
       (`#[props(!optional, default = None)]`).
    2. In `VacationEntitlementCard`, compute
       `let forced_self = props.selected_person.and_then(|id|
        props.vacation_team.iter().find(|b| b.sales_person_id == id).cloned());`
    3. Branch logic: render the SELF body when either `!props.is_hr` OR `forced_self.is_some()`.
       Concretely: `let show_self = !props.is_hr || forced_self.is_some();`
       - When `show_self`: use title/subtitle from `Key::VacationCardSelfTitle` /
         `Key::VacationCardSelfSubtitle` and render `VacationEntitlementSelfBody` with
         `vacation_self: forced_self.clone().or(props.vacation_self.clone())`. (For HR-with-
         selection `forced_self` is the chosen person; for employee it stays `vacation_self`.)
       - Else (HR, no selection): keep the existing team title/subtitle + `VacationEntitlementHrBody`.
       Reuse the EXISTING self-title/self-body — no new visible strings, so no i18n changes
       (constraint: new keys only if strictly needed; D2's "exactly the view the sales
       person sees" is the existing self-layout).
    4. At the render call site (~line 1873) add `selected_person: person_filter_val,` to the
       VacationEntitlementCard invocation. Leave `vacation_self`, `vacation_team`, `is_hr`,
       `sales_persons`, `year` as-is.
    Confirm D3: with `selected_person: None` and `is_hr: false`, behaviour is byte-identical
    to before (existing self path). Confirm no `src/i18n/*` edits are needed; if a reviewer
    insists on a distinct HR-viewing-person title later, that is out of scope for this task.
    Write the snapshot tests FIRST (RED) before wiring the branch.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test vacation_card</automated>
    <automated>cd shifty-dioxus && cargo test page::absences</automated>
    <automated>cd shifty-dioxus && nix develop -c cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <done>
    `selected_person` prop added; HR-with-selection renders the self-body for that person and
    drops the team per-person list; HR-without-selection unchanged; employee path unchanged;
    missing-from-team falls back without panic; all 3 new snapshot tests pass; full
    `shifty-dioxus` suite green; WASM build exits 0; no `src/i18n/*` changes. D2/D3 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side filtering of data already loaded into HR's stores (D4). No new input crosses a trust boundary; no new network call; no permission/impersonation change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-o7t-01 | Information Disclosure | VacationEntitlementCard self-view for selected person | accept | HR already has the full `vacation_team` aggregate loaded and authorized (existing HR privilege gate at `is_hr`). Showing one team member's already-loaded balance in a different layout discloses nothing new. No backend call, no broadened authz (D4). |
| T-o7t-02 | Tampering | `selected_person` prop / person_filter | accept | Filter is a local UI signal scoping a read-only view; cannot mutate data. Backend remains source of truth and is unchanged. |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test` — full suite green (565 prior + new helper/snapshot tests).
- `cd shifty-dioxus && nix develop -c cargo build --target wasm32-unknown-unknown` — exit 0.
- Manual logic check: D1 person dimension ONLY on StatsGrid (category/status NOT applied);
  D2 self-view on HR selection + team aggregate when None; D3 employee path byte-identical;
  D4 no `src/state`, `rest-types`, `src/api.rs`, or backend file touched.
- `git diff --stat` (read-only inspection) shows only `shifty-dioxus/src/page/absences.rs`.
</verification>

<success_criteria>
- HR selecting a single Sales Person scopes BOTH top widgets to that person (D1 + D2).
- StatsGrid is scoped by person only; category/status do not bleed into it (D1).
- VacationEntitlementCard shows the rich self-hero + 5 StatBoxes for the selected person and
  the existing team aggregate when no person is selected (D2).
- Employee (non-HR) path and backend unchanged (D3, D4).
- New pure `stats_for_person` helper unit-tested RED-first; card self-view snapshot-tested.
- `shifty-dioxus` test suite + WASM build both green.
</success_criteria>

<output>
After completion, create `.planning/quick/260612-o7t-absencespage-sales-person-filter/260612-o7t-SUMMARY.md`.

VCS: this repo is jj-co-located. Commit jj-natively (the executor receives separate jj
instructions). Do NOT run any `git commit` / `git add`. No worktrees.
</output>
