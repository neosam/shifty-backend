---
phase: 54-data-model-voluntary-stats
plan: 09
type: execute
wave: 3
depends_on:
  - 54-07
  - 54-08
files_modified:
  - .planning/phases/54-data-model-voluntary-stats/54-VERIFICATION.md
  - .planning/phases/54-data-model-voluntary-stats/54-UAT.md
autonomous: false
gap_closure: true
requirements:
  - VOL-STAT-01
  - VOL-STAT-02  # inherited from Plan 54-03, preserved (not re-implemented) by this gap plan — verified via Non-HR-User check in Task 2 (D-Roundtrip)
  - VOL-ACCT-01
  - VOL-ACCT-02
tags: [gap-closure, verification, uat-rerun, manual-verify, human-checkpoint]

must_haves:
  truths:
    - "Backend + FE laufen lokal; Employee-Detail-Report des betroffenen 5h/Woche-Freiwilligen zeigt einen Ehrenamt-Soll-Wert, der zur Range (Jan 1 bis aktueller Sonntag der KW) passt — NICHT die 177h aus dem UAT-Report."
    - "Ehrenamt-Wording auf HR-View ist konsistent (kein 'Freiwillig'/'Volunteer' im DE- bzw. EN-User-Interface mehr sichtbar)."
    - "Non-HR-User sieht weiterhin keine Ehrenamt-Zeile (Backend-Redaktion + Component-Guard bleiben)."
    - "54-VERIFICATION.md und 54-UAT.md sind mit Gap-Closure-Ergebnissen aktualisiert (Gaps G1 + G2 als resolved markiert oder mit User-Feedback dokumentiert)."
  artifacts:
    - "Screenshot ODER Get-Page-Text-Log als Anlage im 54-09-SUMMARY.md (Referenz-Wert fuer Ehrenamt Soll)."
    - "54-VERIFICATION.md updated (Gap G1 + G2 status auf resolved oder mit follow-up)."
    - "54-UAT.md optional updated (round 2)."
  key_links:
    - "Manual UAT ist der einzige Weg, den 177 vs ~53.6 Fix zu verifizieren — cargo test kann Range-Semantik in Isolation prueft, aber der Roundtrip (FE-URL-Konstruktion → BE-Range-Parse → Aggregation → DTO → FE-Render → i18n-String) ist nur end-to-end sichtbar."
    - "MEMORY reference_dioxus_browser_verify_reports: fuer Report-Zahlen `get_page_text` + `find` verwenden; Screenshots timeouten bei WASM-Report-Seiten."
    - "MEMORY reference_dioxus_screenshots_html2canvas: falls Screenshot noetig, html2canvas via lokalen Upload-Server."
    - "Rechnung fuer erwarteten Wert: 5.0 * (Tage von 2026-04-27 bis Sunday_der_KW) / 7 — bei KW 28 (2026-07-12) = 5.0 * 77 / 7 = 55.0h. Exakter Wert haengt vom KW-Rollover-Tag ab."
---

<objective>
Manual-UAT-Re-Run zur Verifikation, dass Gap G1 (Range-Cutoff) + Gap G2
(Ehrenamt/voluntary-Vereinheitlichung) im live-Roundtrip funktionieren.

Purpose:
- cargo test verifiziert Range-Semantik in-isolation (Plan 54-07 Tests decken
  Regression, Edge-Weeks, 5h-Mai-Szenario ab).
- SSR-Component-Tests verifizieren i18n-Rendering in-isolation (bestehende
  Tests aus Plan 54-05).
- ABER: der eigentliche 177-vs-~55h-Bug hat sich im End-to-End-Roundtrip
  gezeigt (FE ruft BE, BE aggregiert, DTO transportiert, FE rendert Zahl).
  Dieser Plan schliesst mit einem live-Roundtrip als Beweis.

Output:
- Bericht ueber den live-UAT (Backend + FE laufen, HR-Login, betroffener
  SP-Report, gezeigter Ehrenamt-Soll-Wert).
- 54-VERIFICATION.md und/oder 54-UAT.md aktualisiert.

Dieser Plan ist NICHT autonom — der User (bzw. Executor mit User-approval)
fuehrt den live-Roundtrip aus. Der Checkpoint ist ein `checkpoint:human-verify`.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-data-model-voluntary-stats/54-UAT.md
@.planning/phases/54-data-model-voluntary-stats/54-VERIFICATION.md
@.planning/phases/54-data-model-voluntary-stats/54-07-PLAN.md
@.planning/phases/54-data-model-voluntary-stats/54-08-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Regression-Gate — Full-Workspace Build + Test + Clippy</name>
  <files>(no files modified — verify-only)</files>
  <action>
Vor dem manuellen Checkpoint werden alle automatisierten Gates final durchgezogen
— diese sollten seit Plan 54-07/08 gruen sein, dies ist der finale Regression-
Check:

```bash
# Backend
SQLX_OFFLINE=true cargo build --workspace
SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test --workspace
SQLX_OFFLINE=true cargo clippy --workspace -- -D warnings

# Frontend
cd shifty-dioxus
cargo build --target wasm32-unknown-unknown
cargo test -p shifty-dioxus
cargo clippy -p shifty-dioxus -- -D warnings
cd ..
```

Alle 6 Gates MUESSEN gruen sein. Bei rot: Fehler analysieren, ggf. Follow-up-
Plan (54-10) anlegen oder in-plan fixen. Bei einem clippy-Regressionswarning,
das durch Plan 54-08 verursacht wurde: fixen bevor Manual-UAT.

**grep-Sanity:**
```bash
# Backend: keine Full-Year fn-Namen mehr im Rust-Code
grep -rn "voluntary_ist_total_for_year\|committed_voluntary_target_for_year" \
  service/ service_impl/ rest/ shifty_bin/ rest-types/ docs/features/ 2>/dev/null
# Erwartung: 0 Treffer

# FE i18n: keine alten Terminologie-Reste in String-Werten
grep -c "\"[^\"]*Freiwillig" shifty-dioxus/src/i18n/de.rs
# Erwartung: 0
grep -c "\"[^\"]*Volunteer\b\|\"[^\"]*Volunteer Work" shifty-dioxus/src/i18n/en.rs
# Erwartung: 0

# FE-URL: kein alter ?year=-Aufruf in get_voluntary_stats
grep -B 2 -A 6 "fn get_voluntary_stats" shifty-dioxus/src/api.rs | grep -c "\?year="
# Erwartung: 0
```

Ergebnisse loggen fuer das SUMMARY.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test --workspace 2>&amp;1 | tail -5; SQLX_OFFLINE=true cargo clippy --workspace -- -D warnings 2>&amp;1 | tail -3; cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown 2>&amp;1 | tail -3 &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <done>Alle 6 Gates gruen (BE build/test/clippy + FE build/test/clippy); alle 4 grep-Guards liefern 0 Treffer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
Range-basierte Aggregation im VoluntaryStatsService (Plan 54-07) + Frontend-
Range-Call + i18n-Terminologie-Vereinheitlichung (Plan 54-08).

Konkret verifiziert der User:
- Der 177-Freiwillig-Sollstunden-Bug ist weg — der Wert entspricht jetzt
  der Range vom 01.01. bis zur aktuellen KW (fuer 5h/Woche seit Mai: ~55h,
  nicht 177h).
- Die Zeilen sind mit "Ehrenamt Ø/Woche", "Ehrenamt Soll", "Ehrenamt Delta"
  (DE) bzw. "Voluntary avg / week", "Voluntary target", "Voluntary delta" (EN)
  beschriftet — kein "Freiwillig" oder "Volunteer" mehr sichtbar.
- Non-HR-User sieht die Zeilen weiter NICHT.
- Der bestehende OVERALL-Wert "Ehrenamt" (Kategorie-Label, unveraendert
  seit vor Phase 54) bleibt korrekt.
  </what-built>
  <how-to-verify>

**Setup (drei Terminals):**

1. Backend starten:
   ```bash
   cd shifty-backend
   nix develop -c cargo run
   # oder ohne nix: cargo run
   # Erwartung: Server auf http://localhost:3000
   ```

2. Tailwind-Watcher (in shifty-dioxus):
   ```bash
   cd shifty-backend/shifty-dioxus
   npx tailwindcss -i ./input.css -o ./assets/tailwind.css --watch
   ```

3. dx serve mit hot-reload:
   ```bash
   cd shifty-backend/shifty-dioxus
   dx serve --hot-reload
   # Erwartung: FE auf http://localhost:8080
   ```

**Verifikations-Schritte:**

**A) HR-Login + betroffener SP:**

1. Browser auf http://localhost:8080 oeffnen.
2. Als DEVUSER einloggen (default Mock-Auth admin, hat HR-Privileg).
3. Zu "Employees / Mitarbeiter" navigieren, dann den Employee mit
   5h committed_voluntary/Woche seit Mai auswaehlen (der Freiwillige, der
   in der Original-UAT den 177h-Wert zeigte).
4. **Erwartung im OVERALL-Block:**
   - Unter der Zeile "Ehrenamt" (Kategorie-Label, alter Wert = geleistete
     Freiwilligen-Stunden im Jahr) erscheinen 3 neue Zeilen:
     - "Ehrenamt Ø / Woche" mit einem Wert, der zur Anzahl-Wochen-mit-Vertrag
       im Range passt (5.0h/Woche wenn im Report-Range genau die Vertrags-
       wochen liegen).
     - **"Ehrenamt Soll" mit einem Wert um ~50-56h** (5.0 × Tage-im-Vertrag ÷ 7).
       Der genaue Wert haengt vom Report-Range-Endpunkt (until_week) und dem
       Vertragsbeginn-Datum ab.
     - "Ehrenamt Delta" mit `ist_total - soll_total`.
   - **KRITISCHER REGRESSION-CHECK:** Der Wert bei "Ehrenamt Soll" ist
     **NICHT 177** und **NICHT irgendein Full-Year-Aggregat**.

**B) EN-Locale Wortlaut:**

1. Locale auf Englisch umstellen (falls im UI verfuegbar; sonst Browser-Locale
   auf en-US und Reload).
2. Die 3 Zeilen zeigen jetzt "Voluntary avg / week", "Voluntary target",
   "Voluntary delta".
3. **KRITISCHER REGRESSION-CHECK:** Keine Zeile enthaelt mehr das Wort
   "Volunteer" oder "Volunteer Work" — nur "Voluntary".
4. Andere Referenzen (OVERALL-Block, Absence-Gruppe etc.) zeigen ebenfalls
   "Voluntary" — insbesondere:
   - `Key::CategoryVolunteerWork` = "Voluntary work" (vorher "Volunteer Work").
   - `Key::PaidVolunteer` = "Paid / Voluntary" (vorher "Paid / Volunteer").
   - `Key::Volunteer` = "Voluntary" (vorher "Volunteer").
   - `Key::AbsenceGroupVolunteers` = "Voluntary staff" (vorher "Volunteers").

**C) DE-Locale Wortlaut:**

1. Locale auf Deutsch umstellen.
2. Alle User-facing Strings zeigen "Ehrenamt …" — konsistent mit dem bereits
   bestehenden "Ehrenamt"-Kategorie-Label.
3. **KRITISCHER REGRESSION-CHECK:** Kein "Freiwillig" mehr in Labels — insbesondere:
   - `Key::PaidVolunteer` = "Bezahlt / Ehrenamt"
   - `Key::Committed` = "Ehrenamt zugesagt"
   - `Key::CommittedVoluntaryLabel` = "Ehrenamt-Zusage (h)" im Arbeitsvertrag-Modal
   - `Key::AbsenceGroupVolunteers` = "Ehrenamtliche"

**D) Non-HR-User:**

1. Logout, Login als Non-HR-User (siehe integration test fixture: `some-non-hr-user`
   oder ein User ohne HR-Privileg im aktuellen Setup).
2. Zum eigenen Employee-Detail-Report navigieren.
3. **Erwartung:** Die 3 "Ehrenamt Ø/Soll/Delta"-Zeilen erscheinen NICHT
   (Backend-Redaktion liefert alle Felder als null; Component-Guard rendert
   `rsx! {}`).
4. Der urspruengliche "Ehrenamt"-Kategorie-Wert (alter Wert seit Phase 14)
   bleibt fuer alle Rollen sichtbar (nicht redaktiert).

**Get-Page-Text-Verifikation (falls Screenshot-Verify unsicher):**

Nach memory `reference_dioxus_browser_verify_reports`:

```javascript
// Playwright/CDP-Session, HR-Login, Employee-Report offen:
const text = await page.content();
console.assert(text.includes('Ehrenamt Soll'), 'DE label missing');
// Extrahiere den Zahlenwert neben "Ehrenamt Soll":
const match = text.match(/Ehrenamt Soll[\s\S]{0,200}?(\d+[.,]\d+)\s*Stunden/);
if (match) {
  const soll = parseFloat(match[1].replace(',', '.'));
  console.assert(soll < 100, `Ehrenamt Soll must be < 100h (range-cutoff worked), got ${soll}`);
  console.log(`Ehrenamt Soll = ${soll}h`);
}
```

**Ergebnis dokumentieren:**

- ✅ HR-Roundtrip DE — Wert bei "Ehrenamt Soll" = ___h (erwartet ~50-56, nicht 177)
- ✅ HR-Roundtrip EN — Labels als "Voluntary …" konsistent
- ✅ DE-Terminologie überall "Ehrenamt", kein "Freiwillig"
- ✅ EN-Terminologie überall "Voluntary", kein "Volunteer"
- ✅ Non-HR — Ehrenamt-Zeilen nicht sichtbar
- ✅ Kategorie-Label "Ehrenamt" (OVERALL-Block) unveraendert

**Bei Abweichung**: Screenshot + get_page_text im SUMMARY.md dokumentieren,
Follow-up-Plan (54-10) anlegen bei Executor-Advice oder User-approved.
  </how-to-verify>
  <resume-signal>
Antworte mit "approved" wenn alle 6 Checks passen. Sonst beschreibe, welcher
Check gefailt hat (Wert-Anomalie / Label-Anomalie / etc.) — dann wird ein
Follow-up-Plan (54-10) erstellt.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update 54-VERIFICATION.md + 54-UAT.md nach Roundtrip</name>
  <files>.planning/phases/54-data-model-voluntary-stats/54-VERIFICATION.md, .planning/phases/54-data-model-voluntary-stats/54-UAT.md</files>
  <action>
Nach dem Manual-Checkpoint (Task 2):

**54-VERIFICATION.md**:
- Neue Sektion "## Gap-Closure Round 2 (Phase 54-07 + 54-08)" ans Ende ergaenzen.
- Datum + Ergebnis der 6 automatisierten Gates dokumentieren.
- Ergebnis des Manual-Roundtrip dokumentieren (Wert fuer "Ehrenamt Soll",
  User-Feedback zu Terminologie, Non-HR-Guard).
- Status der 2 Gaps setzen:
  - G1 (Range-Cutoff): `resolved` mit Wert-Nachweis.
  - G2 (Terminologie): `resolved` mit User-Approval.

**54-UAT.md**:
- Neuer Abschnitt "## Round 2 — Post-Gap-Closure" mit den drei
  Original-Tests re-durchgefuehrt:
  - Test 1 (HR-Roundtrip): result `pass` mit dem neuen Wert-Log.
  - Test 2 (Non-HR-Roundtrip): result `pass` (unveraendert).
  - Test 3 (cs-Locale): result `skipped` (User: nicht pruefen).
- Frontmatter `status: passed` (oder `resolved`) setzen.

Bei Abweichung im Checkpoint (Task 2 nicht approved): Status `blocked` oder
`re-diagnosed` mit neuer Root-Cause-Analyse.

**Commit-Konvention:**
- `docs(54-09): record post-gap-closure UAT round 2 verification`

Danach ist die Gap-Closure-Sequenz fuer Phase 54 abgeschlossen. Naechster
Schritt (nicht Teil dieses Plans): `/gsd-verify-work 54` oder `/gsd-phase 54 complete`
je nach GSD-Flow.
  </action>
  <verify>
    <automated>grep -q "Round 2" .planning/phases/54-data-model-voluntary-stats/54-UAT.md &amp;&amp; grep -q "Gap-Closure Round 2" .planning/phases/54-data-model-voluntary-stats/54-VERIFICATION.md</automated>
  </verify>
  <done>Beide Dokumente enthalten Round-2-Sektion mit Ergebnis-Nachweis; Frontmatter status auf resolved/passed gesetzt oder mit neuer Root-Cause bei Abweichung.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Keine neuen Trust Boundaries — dieser Plan berührt keinen Code, nur Dokumentation + manuellen Roundtrip.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-09-01 | Repudiation | UAT-Nachweis | medium | mitigate | 54-VERIFICATION.md + 54-UAT.md Round-2-Sektionen mit konkretem Wert-Nachweis (Ehrenamt-Soll = X.Xh) — reproduzierbar. |
| T-54-09-SC | Tampering | npm/pip/cargo installs | high | accept | Kein Cargo-Dep-Install in diesem Plan. |
</threat_model>

<verification>

- Alle 6 automatisierten Gates aus Task 1 grün.
- Manual-Roundtrip (Task 2) approved.
- 54-VERIFICATION.md + 54-UAT.md aktualisiert (Task 3).

</verification>

<success_criteria>

- 5h-Woche-Freiwilligen-Report zeigt "Ehrenamt Soll" < 100h (nicht 177).
- DE-Locale komplett "Ehrenamt", EN-Locale komplett "Voluntary".
- Non-HR sieht Zeilen nicht.
- Beide Gap-Reports (G1 + G2) in 54-VERIFICATION.md als `resolved` markiert.
- 54-UAT.md hat "Round 2 — Post-Gap-Closure"-Abschnitt mit `pass`-Ergebnissen für Tests 1 + 2.

</success_criteria>

<output>
Create `.planning/phases/54-data-model-voluntary-stats/54-09-SUMMARY.md` when done.
Include:
- Ergebnis Task 1 (automated regression gates — mit Log-Zeilen)
- Ergebnis Task 2 (Manual-Roundtrip Checkpoint) — konkret der Ehrenamt-Soll-Wert
  fuer den betroffenen SP und User-Feedback zu Terminologie
- Ergebnis Task 3 (Doc-Updates)
- Bei Abweichung: neue Root-Cause-Analyse + Follow-up-Plan-Empfehlung (54-10)
- Auth-Gates (falls beim Login-als-Non-HR auftraten)
- Commits (git log --oneline --grep="54-09")
</output>
