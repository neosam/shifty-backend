---
phase: 54-data-model-voluntary-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/sqlite/20260707000000_create-rebooking-batch.sql
  - migrations/sqlite/20260707000001_add-source-column-to-extra-hours.sql
  - migrations/sqlite/20260707000002_seed-voluntary-rebooking-toggle.sql
  - dao/src/rebooking_batch.rs
  - dao/src/lib.rs
  - dao/src/extra_hours.rs
  - dao_impl_sqlite/src/rebooking_batch.rs
  - dao_impl_sqlite/src/lib.rs
  - dao_impl_sqlite/src/extra_hours.rs
  - service/src/extra_hours.rs
  - .sqlx/
autonomous: true
requirements:
  - VOL-ACCT-03
user_setup: []

must_haves:
  truths:
    - "[D-54-DM-01] Ein aktiver rebooking_batch pro (sales_person_id, iso_year, iso_week) — WHERE deleted IS NULL — global über alle kinds; INSERT eines zweiten aktiven Rebooking-Batches für dieselbe (sp, y, w) muss fehlschlagen."
    - "[D-54-DM-02] Jede extra_hours-Row trägt eine source-Spalte mit Werten 'manual' | 'rebooking'; bestehende Rows bekommen per DEFAULT 'manual'; das Marker-Filter-Feld ist im Service-Struct ExtraHours als ExtraHoursSource verfügbar."
    - "Die Toggle-Seed-Zeile voluntary_rebooking_auto_active_from (enabled=0, value NULL) existiert nach Migration und ist idempotent gegenüber Wiederanwendung (INSERT OR IGNORE)."
  artifacts:
    - "migrations/sqlite/20260707000000_create-rebooking-batch.sql: Tabellen rebooking_batch + rebooking_batch_entry + UNIQUE-Partial-Index rebooking_batch_week_unique_idx + Performance-Indices."
    - "migrations/sqlite/20260707000001_add-source-column-to-extra-hours.sql: ALTER TABLE extra_hours ADD COLUMN source TEXT NOT NULL DEFAULT 'manual'."
    - "migrations/sqlit/20260707000002_seed-voluntary-rebooking-toggle.sql: INSERT OR IGNORE INTO toggle für voluntary_rebooking_auto_active_from."
    - "dao/src/rebooking_batch.rs: trait RebookingBatchDao + Entities RebookingBatchEntity + RebookingBatchEntryEntity + Kind-Enum + State-Enum."
    - "dao_impl_sqlite/src/rebooking_batch.rs: RebookingBatchDaoImpl mit find_by_id / find_by_sales_person_year_week / create_batch_with_entries."
    - "dao/src/extra_hours.rs: ExtraHoursEntity.source: String (neues Feld) + Konvention 'manual' | 'rebooking'."
    - "dao_impl_sqlite/src/extra_hours.rs: ExtraHoursDb.source Feld + SELECT-/INSERT-Spalte angepasst + .sqlx-Cache aktualisiert."
    - "service/src/extra_hours.rs: enum ExtraHoursSource { Manual, Rebooking } + Feld source auf struct ExtraHours + From/TryFrom-Mapper auf Entity."
  key_links:
    - "Der UNIQUE-Partial-Index (D-54-DM-01) muss WHERE deleted IS NULL enthalten, damit soft-deletete Zeilen den Slot freigeben (Präzedenz Phase 39 week_status)."
    - "Der ALTER-TABLE-Backfill via DEFAULT ist die einzige Migration; kein separater UPDATE-Sweep — Bestandsrows landen automatisch auf source='manual'."
    - "Nach Migration + DAO-Anpassung MUSS cargo sqlx prepare --workspace laufen und .sqlx/-Diff im gleichen Commit committet werden (MEMORY reference_sqlx_prepare_after_new_query)."
---

<objective>
Additive Datenmodell-Basis für die gesamte v2.6-Rebooking-Domäne: zwei neue
SQLite-Tabellen (rebooking_batch, rebooking_batch_entry) mit globalem
Week-Slot-Constraint, eine additive source-Spalte auf extra_hours als
Rebooking-Marker, ein idempotenter Toggle-Seed voluntary_rebooking_auto_active_from
(Wirkung erst Phase 56), plus DAO-Skelett für rebooking_batch und die Feld-
Ergänzung source auf ExtraHoursEntity/ExtraHoursDb.

Purpose: Ohne diese Migrationen + DAO-Marker greifen die späteren F1/F2-
Read-Aggregate (Plan 03) sowie F3/F4/F5 (Phasen 55+56) ins Leere; der
Marker-Filter muss von Anfang an im ExtraHours-Struct hängen, sonst muss
Phase 55 den Filter retroaktiv nachziehen (Pitfall 1).

Output: 3 Migrations-Dateien, 2 neue DAO-Traits/Impls, extra_hours-Struct +
Entity + Db-Row je um ein source-Feld erweitert, .sqlx-Offline-Cache aktualisiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-data-model-voluntary-stats/54-CONTEXT.md
@.planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md

# Präzedenzen zum Kopieren
@migrations/sqlite/20260702000000_create-week-status.sql
@migrations/sqlite/20260704000001_seed-shortday-slot-clipping-toggle.sql
@dao/src/week_status.rs
@dao_impl_sqlite/src/week_status.rs
@dao/src/extra_hours.rs
@dao_impl_sqlite/src/extra_hours.rs
@service/src/extra_hours.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 1 — rebooking_batch + rebooking_batch_entry Tabellen + UNIQUE-Partial-Index</name>
  <files>migrations/sqlite/20260707000000_create-rebooking-batch.sql</files>
  <read_first>
    - migrations/sqlite/20260702000000_create-week-status.sql (Präzedenz: BLOB PRIMARY KEY, Soft-Delete-Konvention, Partial-Unique-Index-Muster)
    - migrations/sqlite/20260703000000_create-pdf-export-config.sql (weiteres Beispiel für Multi-Column-Tabelle mit deleted TIMESTAMP)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §A.1 (exakte SQL-Blaupause + FK-Semantik ohne CASCADE)
  </read_first>
  <action>
    Lege die Datei mit exakt zwei CREATE TABLE + drei CREATE INDEX-Statements an.

    Tabelle 1: rebooking_batch mit Spalten in dieser Reihenfolge:
      id BLOB NOT NULL PRIMARY KEY,
      sales_person_id BLOB NOT NULL,
      iso_year INTEGER NOT NULL,
      iso_week INTEGER NOT NULL,
      kind TEXT NOT NULL,           -- 'manual' | 'hr_suggestion' | 'auto_cron' | 'auto_cron_backfill'
      state TEXT NOT NULL,          -- 'pending' | 'approved' | 'rejected' | 'skipped_locked'
      created TEXT NOT NULL,        -- ISO-Timestamp (Präzedenz week_status: created TEXT)
      approved TEXT,                -- optional, gesetzt bei state='approved'
      approved_by TEXT,             -- optional, username bei approve
      deleted TEXT,                 -- Soft-Delete (Präzedenz)
      update_process TEXT NOT NULL,
      update_version BLOB NOT NULL  -- Optimistic-Lock, Präzedenz week_status

    Tabelle 2: rebooking_batch_entry mit Spalten:
      id BLOB NOT NULL PRIMARY KEY,
      batch_id BLOB NOT NULL REFERENCES rebooking_batch(id),
      sales_person_id BLOB NOT NULL,
      hours REAL NOT NULL,                 -- positiv, Betrag der Umbuchung
      balance_before REAL NOT NULL,        -- Snapshot Stundenkonto vor Rebooking
      voluntary_actual REAL NOT NULL,      -- F1-Ist zum Zeitpunkt (Audit)
      voluntary_committed REAL NOT NULL,   -- F2-Soll zum Zeitpunkt (Audit)
      extra_hours_out_id BLOB,             -- FK auf extra_hours (−N), NULL bis approved
      extra_hours_in_id  BLOB,             -- FK auf extra_hours (+N), NULL bis approved
      created TEXT NOT NULL,
      deleted TEXT,
      update_process TEXT NOT NULL,
      update_version BLOB NOT NULL

    Indices (nach den Tabellen):
      CREATE UNIQUE INDEX rebooking_batch_week_unique_idx
        ON rebooking_batch (sales_person_id, iso_year, iso_week)
        WHERE deleted IS NULL;
      CREATE INDEX rebooking_batch_state_idx
        ON rebooking_batch (state) WHERE deleted IS NULL;
      CREATE INDEX rebooking_batch_entry_sp_idx
        ON rebooking_batch_entry (sales_person_id) WHERE deleted IS NULL;

    Führe die Migration per `nix develop --command sqlx migrate run --source migrations/sqlite`
    lokal aus (dev.sqlite), damit `cargo sqlx prepare` in späteren Tasks funktioniert.
    Kein FK ON DELETE CASCADE (Soft-Delete-Muster; Präzedenz Repo-weit).
    Kein separater `iso_week`-CHECK-Constraint (bleibt semantisch dem Rust-Layer überlassen).

    Nutze das exakte Datei-Muster aus 20260702000000_create-week-status.sql
    (Kommentar-Header „Phase 54 (VOL-STAT / VOL-ACCT — Data-Model-Basis)", CREATE TABLE IF NOT EXISTS,
    CREATE UNIQUE INDEX IF NOT EXISTS). Implementiert per D-54-DM-01.
  </action>
  <verify>
    <automated>nix develop --command sqlx migrate run --source migrations/sqlite &amp;&amp; nix develop --command sqlite3 dev.sqlite ".schema rebooking_batch" | grep -q "sales_person_id BLOB NOT NULL"</automated>
  </verify>
  <done>
    Datei migrations/sqlite/20260707000000_create-rebooking-batch.sql existiert; sqlx migrate run erfolgreich; Schema-Dump zeigt rebooking_batch, rebooking_batch_entry, den Partial-UNIQUE-Index und beide Performance-Indices; sqlite3 rebooking_batch_week_unique_idx-INSERT-Konflikttest schlägt bei zweitem aktivem Row mit identischer (sp, y, w) fehl.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration 2 — source-Spalte auf extra_hours + Migration 3 — Toggle-Seed voluntary_rebooking_auto_active_from</name>
  <files>migrations/sqlite/20260707000001_add-source-column-to-extra-hours.sql, migrations/sqlite/20260707000002_seed-voluntary-rebooking-toggle.sql</files>
  <read_first>
    - migrations/sqlite/20260704000001_seed-shortday-slot-clipping-toggle.sql (exaktes Toggle-Seed-Muster + INSERT OR IGNORE-Konvention)
    - migrations/sqlite/20260628000001_seed-holiday-auto-credit-toggle.sql (zweites Beispiel für Seed-Migration)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §A.2 + §A.3
  </read_first>
  <action>
    Datei 1 (20260707000001_add-source-column-to-extra-hours.sql):
      -- Phase 54 (D-54-DM-02): Rebooking-Marker auf extra_hours.
      -- Bestehende Rows bekommen per DEFAULT 'manual' (kein Backfill-Sweep noetig).
      -- Werte-Konvention: 'manual' (Standard) | 'rebooking' (Marker fuer F1/F2-Filter).
      ALTER TABLE extra_hours
          ADD COLUMN source TEXT NOT NULL DEFAULT 'manual';

    Datei 2 (20260707000002_seed-voluntary-rebooking-toggle.sql), exakt nach dem SHC-04-Muster:
      INSERT OR IGNORE INTO toggle (name, enabled, description, update_process)
      VALUES (
          'voluntary_rebooking_auto_active_from',
          0,
          'When a cutoff date is set in `value` (ISO YYYY-MM-DD), the voluntary rebooking auto-cron (Phase 56 F4) runs only for ISO weeks >= that date. Leave value NULL to disable (default, no rebooking).',
          'phase-54-migration'
      );

    Fuehre beide Migrationen mit `nix develop --command sqlx migrate run --source migrations/sqlite` aus.
    Der Seed muss idempotent sein: ein zweiter Migrate-Lauf fuegt keine Duplikate hinzu
    (INSERT OR IGNORE). Kein Umlaut in Beschreibungstext (Ascii-only) — MEMORY feedback_no_umlauts_in_paths
    gilt nur fuer Pfade, aber wir vermeiden auch Umlaute im description-Text zur Sicherheit.
  </action>
  <verify>
    <automated>nix develop --command sqlx migrate run --source migrations/sqlite &amp;&amp; nix develop --command sqlite3 dev.sqlite "SELECT COUNT(*) FROM toggle WHERE name='voluntary_rebooking_auto_active_from';" | grep -q '^1$' &amp;&amp; nix develop --command sqlite3 dev.sqlite "SELECT source FROM extra_hours LIMIT 1;" | grep -Eq '^(manual|)$'</automated>
  </verify>
  <done>
    Beide Migrations-Dateien existieren und laufen idempotent (zweiter `sqlx migrate run` ohne DB-Reset ist ohne Fehler); extra_hours-Schema zeigt source TEXT NOT NULL DEFAULT 'manual'; toggle-Zeile voluntary_rebooking_auto_active_from mit enabled=0, value NULL existiert genau einmal.
  </done>
</task>

<task type="auto">
  <name>Task 3: DAO-Trait + Entity fuer rebooking_batch (crate dao)</name>
  <files>dao/src/rebooking_batch.rs, dao/src/lib.rs</files>
  <read_first>
    - dao/src/week_status.rs (Praezedenz: Basic-Entity + Trait mit find/create/update/delete, mockall::automock, WeekStatusKind-Enum)
    - dao/src/lib.rs (Modul-Export-Konvention `pub mod week_status;`)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §B.1
  </read_first>
  <action>
    Neue Datei dao/src/rebooking_batch.rs mit:

    - Enum `RebookingBatchKind` mit Varianten Manual, HrSuggestion, AutoCron, AutoCronBackfill
      (String-Konversion in dao_impl_sqlite, hier reines Domain-Enum).
    - Enum `RebookingBatchState` mit Varianten Pending, Approved, Rejected, SkippedLocked.
    - `RebookingBatchEntity` struct mit Feldern id (Uuid), sales_person_id (Uuid),
      iso_year (u32), iso_week (u8), kind (RebookingBatchKind), state (RebookingBatchState),
      created (time::PrimitiveDateTime), approved (Option&lt;PrimitiveDateTime&gt;),
      approved_by (Option&lt;Arc&lt;str&gt;&gt;), deleted (Option&lt;PrimitiveDateTime&gt;), version (Uuid).
    - `RebookingBatchEntryEntity` struct mit Feldern id, batch_id, sales_person_id,
      hours (f32), balance_before (f32), voluntary_actual (f32), voluntary_committed (f32),
      extra_hours_out_id (Option&lt;Uuid&gt;), extra_hours_in_id (Option&lt;Uuid&gt;),
      created (PrimitiveDateTime), deleted (Option&lt;PrimitiveDateTime&gt;), version (Uuid).
    - `#[automock(type Transaction = crate::MockTransaction;)]`-annotiertes async-Trait
      `RebookingBatchDao` mit type Transaction: crate::Transaction; und Methoden:

        async fn find_by_id(&amp;self, id: Uuid, tx: Self::Transaction)
            -&gt; Result&lt;Option&lt;RebookingBatchEntity&gt;, DaoError&gt;;

        async fn find_by_sales_person_year_week(
            &amp;self, sales_person_id: Uuid, iso_year: u32, iso_week: u8,
            tx: Self::Transaction,
        ) -&gt; Result&lt;Option&lt;RebookingBatchEntity&gt;, DaoError&gt;;

        async fn create_batch_with_entries(
            &amp;self,
            batch: &amp;RebookingBatchEntity,
            entries: &amp;[RebookingBatchEntryEntity],
            process: &amp;str,
            tx: Self::Transaction,
        ) -&gt; Result&lt;(), DaoError&gt;;

        async fn list_entries_for_batch(
            &amp;self, batch_id: Uuid, tx: Self::Transaction,
        ) -&gt; Result&lt;Arc&lt;[RebookingBatchEntryEntity]&gt;, DaoError&gt;;

    Kommentar an der Trait-Deklaration: "Phase 54 (VOL-ACCT / D-54-DM-01): Basic-Tier
    Rebooking-Batch DAO. Konsumenten (F3 manuell / F4 Cron / F5 Alert) folgen ab
    Phase 55/56. Der UNIQUE-Partial-Index rebooking_batch_week_unique_idx enforced
    (sales_person_id, iso_year, iso_week) global ueber alle kinds."

    dao/src/lib.rs: `pub mod rebooking_batch;` in alphabetischer Reihenfolge nach
    `pub mod permission;` einfuegen. Keine anderen Exports aendern.
  </action>
  <verify>
    <automated>cargo build -p dao --quiet</automated>
  </verify>
  <done>
    dao/src/rebooking_batch.rs compiliert; alle vier Trait-Methoden mit korrekten Signaturen vorhanden; RebookingBatchEntity + RebookingBatchEntryEntity + Kind-Enum + State-Enum exportiert; MockRebookingBatchDao verfuegbar via `#[automock]`.
  </done>
</task>

<task type="auto">
  <name>Task 4: DAO-Impl (sqlite) fuer rebooking_batch + ExtraHoursDb.source-Feld</name>
  <files>dao_impl_sqlite/src/rebooking_batch.rs, dao_impl_sqlite/src/lib.rs, dao_impl_sqlite/src/extra_hours.rs, .sqlx/</files>
  <read_first>
    - dao_impl_sqlite/src/week_status.rs (Praezedenz: WeekStatusDb-Struct + TryFrom + query_as!-Muster + fn find_by_year_and_week + create-Muster mit deleted=NULL)
    - dao_impl_sqlite/src/extra_hours.rs (Ziel-Datei fuer source-Feld-Ergaenzung; Zeilen ~20-70 Struct + TryFrom + Query-Strings)
    - dao_impl_sqlite/src/lib.rs (Modul-Export-Konvention)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §B (DAO-Layer-Empfehlung)
  </read_first>
  <action>
    (a) Neue Datei dao_impl_sqlite/src/rebooking_batch.rs implementiert das trait
        `dao::rebooking_batch::RebookingBatchDao` genau nach dem week_status-Muster:

        - Interne Db-Structs `RebookingBatchDb` (Row-Layout wie Migration) und
          `RebookingBatchEntryDb`; jeweils `TryFrom&lt;&amp;…Db&gt; for …Entity` implementieren
          (Uuid::from_slice, PrimitiveDateTime::parse mit Iso8601::DATE_TIME).
        - Helper `kind_to_str(&amp;RebookingBatchKind) -&gt; &amp;'static str` und `state_to_str`;
          Reverse-Match in TryFrom mit DaoError::Deserialisation bei unbekannten Werten.
        - Struct `RebookingBatchDaoImpl { pool: Arc&lt;SqlitePool&gt; }` mit `pub fn new(pool)`.
        - `#[async_trait::async_trait] impl dao::rebooking_batch::RebookingBatchDao`:
            * find_by_id: SELECT ... FROM rebooking_batch WHERE id = ? AND deleted IS NULL
            * find_by_sales_person_year_week: WHERE sales_person_id = ? AND iso_year = ?
              AND iso_week = ? AND deleted IS NULL
            * create_batch_with_entries: INSERT INTO rebooking_batch (...) VALUES (...);
              danach Loop ueber entries: INSERT INTO rebooking_batch_entry (...).
              Setze `created` auf `entity.created` (nicht CURRENT_TIMESTAMP — Clock-Service
              Praezedenz). Nutze `tx.executor()` konsistent mit week_status.
            * list_entries_for_batch: SELECT ... WHERE batch_id = ? AND deleted IS NULL.

    (b) dao_impl_sqlite/src/lib.rs: `pub mod rebooking_batch;` in alphabetischer Reihenfolge einfuegen.

    (c) dao_impl_sqlite/src/extra_hours.rs anpassen:
        - Struct `ExtraHoursDb` (etwa Zeile 20-30) um Feld `source: String` erweitern.
        - `TryFrom&lt;&amp;ExtraHoursDb&gt; for ExtraHoursEntity`: source in Entity mappen (String -&gt; String).
        - Alle 5 SELECT-Query-Strings (Zeilen 91, 110, 130, 157, 201) um `, source` in der
          Spaltenliste erweitern — nach `update_version`.
        - INSERT-Query (Zeile 244) um `source` erweitern: `INSERT INTO extra_hours (..., update_version, source) VALUES (..., ?, ?)` und `.bind(&amp;entity.source)` in der Reihenfolge.
        - Verify dass alle bestehenden Aufrufer im DAO das source-Feld nicht setzen — hier
          reicht `entity.source.clone()` beim create; wenn Aufrufer noch nicht befuellen,
          Default `"manual".to_string()` in ExtraHoursEntity Default-Impl (falls existiert)
          bzw. beim Konvertieren aus service::ExtraHours (siehe Task 5).

    (d) Nach den Anpassungen: `nix develop --command bash -c 'DATABASE_URL=sqlite:./dev.sqlite cargo sqlx prepare --workspace'`
        laufen lassen und die aktualisierten .sqlx/*.json in den Commit aufnehmen.
        Ohne diesen Schritt failt die CI (SQLX_OFFLINE=true) — MEMORY reference_sqlx_prepare_after_new_query.

    Nutze das gleiche RebookingBatchDb-TryFrom-Muster wie WeekStatusDb::try_from
    (DaoError-Verkabelung, deleted-Parse).
  </action>
  <verify>
    <automated>cargo build -p dao_impl_sqlite --quiet &amp;&amp; nix develop --command bash -c 'DATABASE_URL=sqlite:./dev.sqlite cargo sqlx prepare --workspace --check'</automated>
  </verify>
  <done>
    dao_impl_sqlite/src/rebooking_batch.rs compiliert; alle vier Trait-Methoden implementiert; ExtraHoursDb enthaelt source-Feld und alle SELECT/INSERT-Queries lesen/schreiben source; `cargo sqlx prepare --workspace --check` ist zufrieden (`.sqlx/`-Cache aktuell); `cargo build -p dao_impl_sqlite` clean.
  </done>
</task>

<task type="auto">
  <name>Task 5: ExtraHours-Struct + ExtraHoursSource-Enum im Service-Layer</name>
  <files>service/src/extra_hours.rs</files>
  <read_first>
    - service/src/extra_hours.rs (Ziel-Datei; ganze Datei fuer Enum-Konvention + ExtraHours-Struct-Ergaenzung)
    - dao/src/extra_hours.rs (ExtraHoursEntity-Layout, insbesondere logical_id-Muster + neues source-Feld)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §B.4 (Empfehlung: Enum ExtraHoursSource statt String im Service-Layer)
  </read_first>
  <action>
    Fuege in service/src/extra_hours.rs nach dem `ExtraHoursCategory`-Enum-Block
    (vor dem `impl ExtraHoursCategory`-Block) das folgende ein:

      #[derive(Clone, Copy, Debug, PartialEq, Eq)]
      pub enum ExtraHoursSource {
          /// Row wurde manuell (UI-Add-Extra-Hours, HR-CRUD, Absence-Convert)
          /// erzeugt und zaehlt normal in Balance / F1-Ist / F2-Nutz.
          Manual,
          /// Row wurde durch Rebooking (F3/F4/F5, ab Phase 55) erzeugt und
          /// MUSS von F1-Ist- + F2-Soll-Aggregatoren gefiltert werden
          /// (D-54-DM-02, Pitfall 1).
          Rebooking,
      }

      impl ExtraHoursSource {
          pub fn as_str(&amp;self) -&gt; &amp;'static str {
              match self {
                  Self::Manual =&gt; "manual",
                  Self::Rebooking =&gt; "rebooking",
              }
          }
      }

      impl TryFrom&lt;&amp;str&gt; for ExtraHoursSource {
          type Error = ServiceError;
          fn try_from(s: &amp;str) -&gt; Result&lt;Self, Self::Error&gt; {
              match s {
                  "manual" =&gt; Ok(Self::Manual),
                  "rebooking" =&gt; Ok(Self::Rebooking),
                  other =&gt; Err(ServiceError::InternalError),
              }
          }
      }

      impl Default for ExtraHoursSource {
          fn default() -&gt; Self { Self::Manual }
      }

    Erweitere die `ExtraHours`-Struct um ein Feld `pub source: ExtraHoursSource,`
    (nach `version: Uuid,`). Passe die From-Impl `From&lt;&amp;dao::extra_hours::ExtraHoursEntity&gt; for ExtraHours` an:
      source: ExtraHoursSource::try_from(extra_hours.source.as_str())
          .unwrap_or(ExtraHoursSource::Manual),

    Passe TryFrom&lt;&amp;ExtraHours&gt; for ExtraHoursEntity an: `source: extra_hours.source.as_str().to_string(),`.

    Ergaenze `dao::extra_hours::ExtraHoursEntity` (in dao/src/extra_hours.rs) um ein
    entsprechendes Feld `pub source: String,`. Bestehende Aufrufer, die ExtraHoursEntity
    instanziieren, bekommen `source: "manual".to_string()` als expliziten Default —
    grep der Nutzung im Workspace und Anpassung aller (bookings-Ausschluss).

    KEINE oeffentlichen API-Aenderungen an bestehenden Trait-Methoden — nur additive
    Feld-Ergaenzung. Bestehender Test `test_unpaid_leave_report_type_is_absence_hours`
    bleibt unberuehrt.
  </action>
  <verify>
    <automated>cargo build -p service --quiet &amp;&amp; cargo build -p dao --quiet &amp;&amp; cargo build -p dao_impl_sqlite --quiet &amp;&amp; cargo test -p service extra_hours::tests --quiet</automated>
  </verify>
  <done>
    ExtraHoursSource-Enum + Feld source auf ExtraHours-Struct + ExtraHoursEntity vorhanden; From/TryFrom-Mapper konvertieren String &lt;-&gt; Enum; bestehende Tests weiter gruen; `cargo build -p service` und `cargo build -p dao_impl_sqlite` sauber.
  </done>
</task>

<task type="auto">
  <name>Task 6: Wave-1-Gate — Workspace-Build + Clippy + sqlx-prepare-Check</name>
  <files>.sqlx/</files>
  <read_first>
    - CLAUDE.md §"Clippy ist ein hartes Gate" (feedback_clippy_gate)
    - MEMORY reference_sqlx_prepare_after_new_query
  </read_first>
  <action>
    Fuehre folgende Gates seriell aus, im Fehlerfall abbrechen und diagnostizieren:

      1. nix develop --command bash -c 'DATABASE_URL=sqlite:./dev.sqlite cargo sqlx prepare --workspace --check'
         (wirft Fehler, falls neue Queries nicht im .sqlx-Cache stehen)
      2. cargo build --workspace
      3. cargo test --workspace --lib -- --quiet
      4. cargo clippy --workspace -- -D warnings

    Wenn ein Gate fehlschlaegt: keine dummy-Anpassung — Ursache in Task 3/4/5 fixen
    und den ganzen Gate-Zyklus neu laufen lassen.
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo clippy --workspace -- -D warnings &amp;&amp; cargo test --workspace --lib -- --quiet</automated>
  </verify>
  <done>
    Alle vier Gates gruen; kein Warning; `.sqlx/`-Cache aktuell und committet; keine sqlx-Query bricht offline.
  </done>
</task>

</tasks>

<verification>
- 3 neue Migrations-Dateien vorhanden und laufen idempotent
- Tabelle rebooking_batch + rebooking_batch_entry haben Partial-Unique-Index + Performance-Indices
- extra_hours.source-Spalte existiert und ist NOT NULL DEFAULT 'manual'
- Toggle voluntary_rebooking_auto_active_from ist gesaet (enabled=0, value NULL)
- dao::rebooking_batch::RebookingBatchDao Trait + Impl kompilieren; MockRebookingBatchDao verfuegbar
- service::extra_hours::ExtraHoursSource + Feld auf ExtraHours + Konvertierungen vorhanden
- .sqlx-Cache aktuell (Offline-CI-fähig)
- cargo clippy --workspace -- -D warnings gruen
</verification>

<success_criteria>
- Migrationen laufen sauber auf einer leeren dev.sqlite (`sqlx migrate run`).
- Zweiter `sqlx migrate run` in derselben DB tut nichts (idempotent).
- UNIQUE-Constraint-Test: Manuelles INSERT von zwei aktiven rebooking_batch-Zeilen fuer dieselbe (sp, y, w) schlaegt fehl (`UNIQUE constraint failed`).
- ExtraHours-Struct und -Entity tragen source; F1/F2-Konsumenten (Plan 03) koennen diesen filtern.
- Workspace-Build + Clippy + Test gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/54-data-model-voluntary-stats/54-01-SUMMARY.md` am Ende
mit: migrierte Files-Liste, DAO-Trait-Signaturen (Rust-Snippet), ExtraHoursSource-Enum
(Rust-Snippet), Grep-Ergebnis fuer den UNIQUE-Index in der Schema-Dump, gate-Ergebnisse
(clippy/sqlx-prepare), aufgetauchte Randfaelle. Aktualisiere 54-VALIDATION.md
Task-IDs 54-01-01 .. 54-01-04 auf ✅ green mit dem tatsaechlichen automated command.
</output>
