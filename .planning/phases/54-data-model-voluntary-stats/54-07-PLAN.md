---
phase: 54-data-model-voluntary-stats
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - service_impl/src/reporting.rs
  - service/src/voluntary_stats.rs
  - service_impl/src/voluntary_stats.rs
  - service_impl/src/test/voluntary_stats.rs
  - rest/src/report.rs
  - docs/features/F14-rebooking.md
  - docs/features/F14-rebooking_de.md
autonomous: true
gap_closure: true
requirements:
  - VOL-STAT-01
  - VOL-STAT-02  # inherited from Plan 54-03, preserved (not re-implemented) by this gap plan — see must_haves.truths #2 (Non-HR-None-Redaktion)
  - VOL-ACCT-01
  - VOL-ACCT-02
tags: [gap-closure, backend, reporting, voluntary-stats, range-semantics, docs-freshness]

must_haves:
  truths:
    - "F1-Ist und F2-Soll akzeptieren eine Date-Range (from_date, to_date) statt eines ISO-Jahres; Edge-Weeks tragen nur die im Range liegenden Tage bei (Pro-Rata bleibt tages-basiert)."
    - "VoluntaryStatsService::get_voluntary_stats-Signatur ist (sales_person_id, from_date: ShiftyDate, to_date: ShiftyDate, ctx, tx) — analog ReportingService::get_report_for_employee_range; HR-Gate an erster Stelle bleibt erhalten; Non-HR-None-Redaktion bleibt erhalten (per D-F1-01 / D-F2-01 aus 54-CONTEXT.md)."
    - "REST-Query-Vertrag `GET /report/{id}/voluntary-stats?from_date=YYYY-MM-DD&to_date=YYYY-MM-DD` verwirft `year`; ISO-Wochen-Semantik der Kongruenz-Regel (D-F1-01) bleibt: Nenner und Zaehler nutzen die gleichen ISO-Wochen."
    - "Regression-Guard: Range = ganzes Jahr (from_date=1.1., to_date=31.12.) liefert das gleiche Ergebnis wie die alte weeks_in_year-Full-Year-Summe (fuer 2025 = 52 Wochen, fuer 2026 = 53 Wochen)."
    - "Edge-Week-Test: Range startet mid-week (z.B. from_date=Mi der KW 20) => committed_voluntary trägt nur fuer Mi/Do/Fr/Sa/So dieser Woche bei (5/7 der Wochen-Zusage), nicht 7/7."
    - "Edge-Week-Test: Range endet mid-week (z.B. to_date=Do der KW 30) => committed_voluntary trägt nur fuer Mo/Di/Mi/Do dieser Woche bei (4/7)."
    - "5h/Woche-seit-Mai-Szenario: 5h committed_voluntary + Vertrag ab 2026-05-04 (KW 18 Mo) + Range 2026-01-01..=2026-07-10 => soll_total = 5.0 * (Anzahl-Range-Wochen-mit-Vertrag)-analog, mit tages-genauer Edge-Week-Abrechnung; das per 07/2026 realistische Ergebnis liegt bei ~46-49h, NICHT 177h."
    - "Docs-Freshness (D-54-DM-02 / MEMORY feedback_docs_always_current_no_followup): docs/features/F14-rebooking.md UND F14-rebooking_de.md sind synchron zur Range-Semantik aktualisiert (Signaturen + REST-Query-Vertrag)."
  artifacts:
    - "service_impl/src/reporting.rs — 3 neue pure fns: voluntary_ist_total_in_range, contract_weeks_count_in_range, committed_voluntary_target_in_range; committed_voluntary_prorata_for_week bleibt als per-week Baustein"
    - "service/src/voluntary_stats.rs — Trait get_voluntary_stats(from_date, to_date) statt (year)"
    - "service_impl/src/voluntary_stats.rs — Impl gegen die 3 in-range-fns; ist_per_contract_week = ist_total / contract_weeks_in_range (contract_weeks==0 => 0.0)"
    - "service_impl/src/test/voluntary_stats.rs — bestehende Tests auf Range umgestellt + neue Range-Tests (Regression, Edge-Weeks, 5h-Mai-Szenario)"
    - "rest/src/report.rs — VoluntaryStatsRequest { from_date: String, to_date: String }; Handler parst ISO-Datum via time::Date::from_calendar_date + ShiftyDate::from_date; Doc-Kommentar erneuert; #[utoipa::path] aktualisiert"
    - "docs/features/F14-rebooking.md (EN) — Pure-fn-Signaturen + REST-Query-Vertrag reflektieren from_date/to_date"
    - "docs/features/F14-rebooking_de.md (DE) — analog synchronisiert"
  key_links:
    - "Range-Iteration in in-range-fns: ISO-Wochen zwischen from_date.as_shifty_week() und to_date.as_shifty_week() (inkl.); pro Woche werden nur die Tage in [from_date.to_date() ..= to_date.to_date()] gezählt"
    - "contract_weeks_count_in_range zaehlt eine Woche, wenn mindestens 1 Tag der ISO-Woche (i) im Range liegt UND (ii) durch eine aktive EmployeeWorkDetails gedeckt ist — D-F1-01 (expected_hours=0 zaehlt MIT)"
    - "committed_voluntary_target_in_range summiert je Woche nur die Range-Tage: Σ (committed_voluntary_of_active_contract_on_day / 7.0) über alle Tage der Woche, die (i) im Range liegen und (ii) durch aktiven Vertrag gedeckt sind — bewahrt tages-basierte D-F2-01-Semantik"
    - "voluntary_ist_total_in_range filtert ExtraHours mit source=Manual, category=VolunteerWork, deleted.is_none(), UND date_time in [from_date.to_date() ..= to_date.to_date()]"
    - "REST-Handler: bei Parse-Fehler (kein YYYY-MM-DD) → HTTP 400 (Präzedenz rest/src/toggle.rs Zeilen 350-377)"
---

<objective>
Gap G1 (Blocker aus 54-UAT.md) schließen: VoluntaryStatsService rechnet ab jetzt
über eine echte Date-Range (from_date, to_date) statt über ein volles ISO-Jahr.
Grund: der User hat 177 „Freiwillige Sollstunden" gesehen fuer einen Mitarbeiter,
der 5h/Woche freiwillig seit Mai leistet — die alte Semantik summiert stur ueber
das ganze Jahr, waehrend der Rest der Employee-Report-Chain bis zur aktuellen
KW rechnet. User-Zitat: „Das muss immer so sein beim Report. Alleine schon wegen
dem Abrechnungszeitraum."

Purpose: Range-Semantik analog `ReportingService::get_report_for_employee_range`;
Edge-Weeks tragen tages-genau bei (Pro-Rata bleibt Tages-Baustein); Regression-
Guard fuer Volles-Jahr-Case; Docs-Freshness fuer F14 (EN+DE) im gleichen Commit
(memory `feedback_docs_always_current_no_followup`).

Output:
- 3 neue in-range pure fns in reporting.rs; alte Full-Year-fns entfernt.
- Service-Trait + Impl auf Range-Signatur umgestellt.
- REST-Query { from_date, to_date } (String, ISO YYYY-MM-DD).
- Unit-Tests: Regression (Full-Year vs alte Semantik), Edge-Week-Start,
  Edge-Week-End, 5h-Mai-Szenario, Kongruenz.
- Property-Test VOL-ACCT-03 (Rebooking-Neutralität) angepasst.
- Backend-Gates: cargo build, cargo test --workspace, cargo clippy --workspace -- -D warnings.
- F14 EN + DE aktualisiert.

Der Frontend-Aufrufer (`shifty-dioxus`) wird von Plan 54-08 nachgezogen; dieser
Plan bricht den `?year=` FE-Aufruf temporaer (400 Bad Request) — akzeptabel im
Wave-1-Zwischenzustand, weil Plan 54-08 in Wave 2 sofort folgt und der User in
Prod erst nach beiden Plans testet.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-data-model-voluntary-stats/54-CONTEXT.md
@.planning/phases/54-data-model-voluntary-stats/54-UAT.md
@.planning/phases/54-data-model-voluntary-stats/54-03-SUMMARY.md
@.planning/phases/54-data-model-voluntary-stats/54-04-SUMMARY.md
@service/src/voluntary_stats.rs
@service_impl/src/voluntary_stats.rs
@service_impl/src/test/voluntary_stats.rs
@service/src/reporting.rs
@rest/src/report.rs
@docs/features/F14-rebooking.md
@docs/features/F14-rebooking_de.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Range-fähige pure fns in service_impl/src/reporting.rs</name>
  <files>service_impl/src/reporting.rs, service_impl/src/test/voluntary_stats.rs</files>
  <action>
Ziel: 3 neue pure fns hinzufuegen, die Range-basiert arbeiten. Alte Full-Year-fns
(voluntary_ist_total_for_year / contract_weeks_count / committed_voluntary_target_for_year)
ENTFERNEN — Plan 08 verifiziert danach, dass keine andere Kette diese fns benutzt
(grep-Check ist in Task 4 dieses Plans mit drin).

`committed_voluntary_prorata_for_week` (Zeilen 168-203) BLEIBT unverändert — es ist
der per-week Baustein und wird von der neuen `_in_range`-fn intern genutzt.

Neue fns direkt unter `committed_voluntary_prorata_for_week` platzieren:

```rust
/// Phase 54 Gap-Closure G1 (VOL-STAT-01 / VOL-ACCT-01-Ist, D-54-DM-02):
/// Summe der `VolunteerWork`-`ExtraHours` im Range `[from_date ..= to_date]`
/// (kalendarisch, Tag-Vergleich), gefiltert auf source==Manual und
/// deleted.is_none(). Rebooking-Marker-Rows sind ausgeschlossen (VOL-ACCT-03).
///
/// Argumentation Range-Cutoff: die Employee-Report-Chain rechnet bis zur
/// aktuellen KW / Range-Ende — nicht ueber das volle ISO-Jahr. Ohne Cutoff
/// zeigte 5h/Woche committed seit KW 18 einen Soll-Wert von ~177h (=~35
/// Rest-Jahres-Wochen); mit Cutoff nur die tatsaechlichen Range-Wochen.
pub fn voluntary_ist_total_in_range(
    extra_hours: &[ExtraHours],
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> f32 {
    let from = from_date.to_date();
    let to = to_date.to_date();
    extra_hours
        .iter()
        .filter(|eh| eh.deleted.is_none())
        .filter(|eh| matches!(eh.category, ExtraHoursCategory::VolunteerWork))
        .filter(|eh| eh.source == ExtraHoursSource::Manual)
        .filter(|eh| {
            let d = eh.date_time.date();
            from <= d && d <= to
        })
        .map(|eh| eh.amount)
        .sum()
}

/// Phase 54 Gap-Closure G1 (VOL-STAT-01-Nenner, D-F1-01):
/// Anzahl ISO-Wochen im Range `[from_date ..= to_date]`, in denen mindestens
/// eine aktive `EmployeeWorkDetails`-Row an mindestens einem Tag der Woche,
/// der im Range liegt, den Kalender-Wochen-Slot abdeckt. Eine Row mit
/// `expected_hours == 0` zaehlt MIT (D-F1-01).
///
/// Edge-Weeks (Range startet oder endet mitten in einer ISO-Woche) zaehlen
/// als 1 Woche, sofern mindestens 1 Range-Tag durch aktiven Vertrag gedeckt
/// ist — die tages-basierte Verduennung findet in `committed_voluntary_target_in_range`
/// (Zaehler) statt, nicht im Wochen-Zaehler.
pub fn contract_weeks_count_in_range(
    working_hours: &[EmployeeWorkDetails],
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> u32 {
    if from_date > to_date {
        return 0;
    }
    let from = from_date.to_date();
    let to = to_date.to_date();
    // Iteriere ISO-Wochen zwischen from_date.week und to_date.week (inklusive
    // Jahresuebergang). Verwende ShiftyWeek-Iteration analog assemble_weeks-
    // Vorbild oder ISO-Iteration Tag-basiert (siehe untenstehende Impl).
    let mut count: u32 = 0;
    let mut day = from;
    while day <= to {
        // Bestimme ISO-Woche des aktuellen Tages.
        let (iso_year, iso_week, _) = day.to_iso_week_date();
        // Ermittle Monday der ISO-Woche.
        let monday = time::Date::from_iso_week_date(iso_year, iso_week, time::Weekday::Monday)
            .expect("valid ISO week");
        let sunday = time::Date::from_iso_week_date(iso_year, iso_week, time::Weekday::Sunday)
            .expect("valid ISO week");
        // Range-Overlap dieser Woche mit [from ..= to].
        let week_start = if monday > from { monday } else { from };
        let week_end = if sunday < to { sunday } else { to };
        // Pruefe, ob irgendein Tag in [week_start ..= week_end] durch aktiven
        // Vertrag gedeckt ist (D-F1-01: expected_hours=0 zaehlt MIT).
        let has_contract = (0i64..=((week_end - week_start).whole_days())).any(|off| {
            let d = week_start + time::Duration::days(off);
            working_hours.iter().any(|wh| {
                if wh.deleted.is_some() {
                    return false;
                }
                let from_d = match wh.from_date() { Ok(x) => x.to_date(), Err(_) => return false };
                let to_d = match wh.to_date() { Ok(x) => x.to_date(), Err(_) => return false };
                from_d <= d && d <= to_d
            })
        });
        if has_contract {
            count += 1;
        }
        // Naechster Monday oder to+1 — nimm sunday+1, falls sunday < to; sonst break.
        if sunday >= to {
            break;
        }
        day = sunday + time::Duration::days(1);
    }
    count
}

/// Phase 54 Gap-Closure G1 (VOL-ACCT-01-Soll, D-F2-01):
/// Summe der pro-rata-Wochen im Range `[from_date ..= to_date]`. Fuer jede
/// ISO-Woche im Range wird `committed_voluntary_prorata_for_week` aufgerufen,
/// aber nur die im Range liegenden Tage der Woche gezaehlt. Der bestehende
/// per-week-Baustein bleibt semantisch unveraendert; Edge-Weeks tragen 
/// tages-genau bei (D-F2-01 pro-rata bleibt).
pub fn committed_voluntary_target_in_range(
    working_hours: &[EmployeeWorkDetails],
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> f32 {
    if from_date > to_date {
        return 0.0;
    }
    let from = from_date.to_date();
    let to = to_date.to_date();
    let mut total: f32 = 0.0;
    let mut day = from;
    while day <= to {
        // Fuer jeden Tag im Range: waehle aktiven Vertrag (tages-basiert),
        // summiere committed_voluntary / 7.0 (Wochen-Zusage auf Tage
        // verteilt — D-F2-01).
        let active = working_hours.iter().find(|wh| {
            if wh.deleted.is_some() {
                return false;
            }
            let from_d = match wh.from_date() { Ok(x) => x.to_date(), Err(_) => return false };
            let to_d = match wh.to_date() { Ok(x) => x.to_date(), Err(_) => return false };
            from_d <= day && day <= to_d
        });
        if let Some(wh) = active {
            total += wh.committed_voluntary / 7.0;
        }
        day = day + time::Duration::days(1);
    }
    total
}
```

Anschliessend die ALTEN drei fns loeschen:
- `voluntary_ist_total_for_year` (Zeilen 131-140)
- `contract_weeks_count` (Zeilen 150-155)
- `committed_voluntary_target_for_year` (Zeilen 209-217)

`committed_voluntary_prorata_for_week` (Zeilen 168-203) BLEIBT (per-week
Baustein, weiterhin gebraucht ggf. fuer Debug-Tests).

WICHTIG: Nach dem Delete `cargo build` mit workspace laufen lassen, um alle
Call-Sites zu identifizieren. Erwarteter Compile-Fehler: `service_impl/src/voluntary_stats.rs`
und `service_impl/src/test/voluntary_stats.rs` (Task 2 bzw. Task 3 fixen).

Grep-Verify NACH Task 2/3 (Task 4):
```
grep -rn "voluntary_ist_total_for_year\|committed_voluntary_target_for_year\|contract_weeks_count\b" service_impl/ service/ rest/ shifty_bin/ 2>/dev/null
```
darf keine Treffer mehr geben (ausser in altem Grep-Kommando-Text; ignorieren).

Wenn andere Konsumenten existieren (unerwartet — Grep vor Refactor zeigte nur
voluntary_stats.rs Impl + Tests), sind sie in Task 5 (Regression-Fix) mit
Range-Signatur zu aktualisieren.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build --workspace 2>&amp;1 | tee /tmp/build-54-07-task1.log; test $(grep -c "^error\[" /tmp/build-54-07-task1.log) -le 20</automated>
  </verify>
  <done>3 neue in-range fns compilieren; alte 3 Full-Year-fns entfernt; Compile-Fehler beschraenken sich auf service_impl/src/voluntary_stats.rs (Task 2) und service_impl/src/test/voluntary_stats.rs (Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: VoluntaryStatsService — Trait + Impl auf Range umstellen</name>
  <files>service/src/voluntary_stats.rs, service_impl/src/voluntary_stats.rs</files>
  <action>
In `service/src/voluntary_stats.rs`:
- Trait-Methode ersetzen. Alt: `year: u32`. Neu: `from_date: shifty_utils::ShiftyDate, to_date: shifty_utils::ShiftyDate`.
- `use shifty_utils::ShiftyDate;` hinzufuegen.
- Doc-Kommentar erneuern: "Analog `ReportingService::get_report_for_employee_range`
  akzeptiert diese Methode eine echte Date-Range; die Aggregation berücksichtigt
  nur Tage im Range (Pro-Rata tages-basiert)."
- `VoluntaryStats`-Struct bleibt unveraendert (5 Option-Felder).

In `service_impl/src/voluntary_stats.rs`:
- Imports anpassen: `use crate::reporting::{voluntary_ist_total_in_range, contract_weeks_count_in_range, committed_voluntary_target_in_range};`.
- `use shifty_utils::ShiftyDate;` hinzufuegen.
- Impl-Signatur auf `(sales_person_id, from_date: ShiftyDate, to_date: ShiftyDate, context, tx)` umstellen.
- HR-Gate-First-Guard bleibt unveraendert (per D-AVG-05 aus Phase 41).
- Non-HR-None-Redaktion bleibt unveraendert.
- Data-Fetch (ExtraHours + EmployeeWorkDetails) unveraendert:
  - `ExtraHoursService::find_by_iso_year(from_date.as_shifty_week().year, ...)` — REICHT DAS?
    Nein: wenn to_date im Folgejahr liegt (Range-Straddle), muss der Loader zwei
    Jahre laden. Vereinfachung: mit `for year in from_year..=to_year` iterieren
    und je Jahr find_by_iso_year callen; alle Rows in ein Vec konkatenieren.
    from_year = from_date.as_shifty_week().year; to_year = to_date.as_shifty_week().year.
  - `EmployeeWorkDetailsService::find_by_sales_person_id(sales_person_id, ...)` bleibt
    unveraendert (liefert alle Contract-Rows des Mitarbeiters, unabhaengig von Jahr).
- Aggregation:
  ```rust
  let ist_total = voluntary_ist_total_in_range(&extra_hours_for_sp, from_date, to_date);
  let soll_total = committed_voluntary_target_in_range(&working_hours_vec, from_date, to_date);
  let contract_weeks = contract_weeks_count_in_range(&working_hours_vec, from_date, to_date);
  let ist_per_contract_week = if contract_weeks == 0 {
      0.0
  } else {
      ist_total / contract_weeks as f32
  };
  let delta = ist_total - soll_total;
  ```

`Authentication::Full`-Bypass fuer Cross-Service-Calls bleibt unveraendert
(MEMORY `reference_toggle_service_full_context_bypass`).

Ergebnis-Struct: `VoluntaryStats { ist_per_contract_week: Some, ist_total: Some,
soll_total: Some, delta: Some, contract_weeks: Some }` (HR); `None` je Feld (Non-HR).

WICHTIG: MockVoluntaryStatsService in service/src/voluntary_stats.rs (via
#[automock]-Attribut) wird automatisch mit-gerefactored; wenn Konsumenten
(z.B. rest-types integration tests) Mock-Expectations setzen, muessen die
Signaturen matchen. Task 3 macht das.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true cargo build -p service -p service_impl 2>&amp;1 | tee /tmp/build-54-07-task2.log; ! grep -q "^error\[" /tmp/build-54-07-task2.log</automated>
  </verify>
  <done>service + service_impl kompilieren ohne Fehler; Trait `VoluntaryStatsService::get_voluntary_stats` erwartet nun `from_date, to_date: ShiftyDate` statt `year: u32`; Impl ruft die 3 neuen in-range-fns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests umstellen + neue Range-Tests hinzufuegen</name>
  <files>service_impl/src/test/voluntary_stats.rs</files>
  <behavior>
    - Test A (Regression-Guard, Full-Year): Range from_date=1.1.2025, to_date=31.12.2025 mit committed_voluntary=1.0 seit KW1 → soll_total = 52.0 (alte Semantik bewahrt).
    - Test B (Regression-Guard, 53-Wochen-Jahr): Range from_date=1.1.2026, to_date=31.12.2026 mit committed_voluntary=1.0 → soll_total = 53.0.
    - Test C (Edge-Week-Start, mid-week): Range from_date=2026-05-20 (Mi der KW 21), to_date=2026-05-31 (So der KW 22) mit committed_voluntary=7.0/Woche → KW 21 = 5 Tage × 7/7 = 5.0; KW 22 = 7 Tage × 7/7 = 7.0; total = 12.0.
    - Test D (Edge-Week-End, mid-week): Range from_date=2026-05-18 (Mo der KW 21), to_date=2026-05-21 (Do der KW 21) mit committed_voluntary=7.0/Woche → 4 Tage × 7/7 = 4.0.
    - Test E (5h-Mai-Szenario): Vertrag mit committed_voluntary=5.0 ab KW 18/2026 (from 2026-04-27 Mo), bis KW 52/2026. Range from_date=2026-01-01, to_date=2026-07-10 (KW 28 Fr). Erwartung: ~46-49h; SICHER < 60h. Genauer: 5.0/7.0 * (Tage von 2026-04-27 bis 2026-07-10, die Mo-Fr sind ODER So? — hier: TAGE, Pro-Rata Tages-basiert, ALLE Tage in [from ..= to] × 5/7). Tage von 27.4. bis 10.7. = 75. Erwartung = 5.0 * 75 / 7 = 53.57. Test-Assertion: 53.0 < soll_total < 54.5 UND soll_total < 60.0 (Regression-Gate gegen 177).
    - Test F (Edge-Week-Contract-Miss): Vertrag ab KW 18. Range from_date=2026-01-01, to_date=2026-01-07 (vor Vertragsbeginn) → soll_total=0.0, contract_weeks=0.
    - Test G (Range-Vollstaendigkeit + Kongruenz): 5h/Woche committed seit KW 18 + Range from_date=1.1., to_date=31.12.2026 → soll_total = 5.0 * (365-116)/7 (Tage KW 18-53 inkl.). 5.0 * ~249 / 7 ≈ 177.86 — DAS ist der 177er-Wert; im Range bis zur aktuellen KW soll er kleiner sein. Diese Erwartung als expliziter Beweis, dass die Range-Cutoff-Semantik greift.
    - Test H (Regression VOL-ACCT-03 Rebooking-Neutralität) — bestehender Property-Test angepasst auf Range-Signatur, gleiche Assertion (Rebooking-Pair neutral).
    - Test I (Service-Test HR): Range 2026-01-01..=2026-01-31 mit committed_voluntary=1.0 im Vertragszeitraum → soll_total = 31/7 ≈ 4.43; ist_total = manuelle Rows × Hours; ist_per_contract_week = ist_total / contract_weeks_in_range.
    - Test J (Service-Test Non-HR): alle Felder None; kein DAO-Call.
  </behavior>
  <action>
Bestehende Tests in `service_impl/src/test/voluntary_stats.rs` refactoren:

1. Bestehende Tests, die `voluntary_ist_total_for_year(&hours, 2026)` aufrufen,
   auf `voluntary_ist_total_in_range(&hours, ShiftyDate::first_day_in_year(2026), ShiftyDate::last_day_in_year(2026))` umstellen.
   Erwarteter Wert bleibt gleich (Volles-Jahr-Range = alte Semantik).

2. Tests, die `contract_weeks_count(&wh, 2026)` aufrufen, auf
   `contract_weeks_count_in_range(&wh, first_day_in_year, last_day_in_year)` umstellen.
   Erwartete Werte gleich fuer volles-Jahr-Range (z.B. `contract_weeks_zero_expected_counts_d_f1_01`
   mit KW 10..=15 im Vertrag → count = 6 bleibt).

3. Tests, die `committed_voluntary_target_for_year(&wh, year)` aufrufen, auf
   `committed_voluntary_target_in_range(&wh, first_day_in_year, last_day_in_year)`
   umstellen. `f2_soll_iso_week_53_year_boundary_d_f2_01`: Erwartung fuer 2026
   bleibt 53.0 (weil 53 Wochen × 7 Tage × 1/7 = 53.0), fuer 2025 bleibt 52.0.

4. Test `f1_ist_and_f2_soll_share_iso_week_semantics_d_f1_01_kongruenz`:
   Full-Year-Range 2026 verwenden.

5. Neue Range-Tests (A-G aus behavior-Block) hinzufuegen. Naming:
   - `range_regression_full_year_2025_52_weeks_matches_old_semantics`
   - `range_regression_full_year_2026_53_weeks_matches_old_semantics`
   - `range_edge_week_start_midweek_wednesday_kw21_2026`
   - `range_edge_week_end_midweek_thursday_kw21_2026`
   - `range_five_h_per_week_since_may_scenario_2026_until_kw28`
   - `range_before_contract_start_returns_zero`
   - `range_full_year_shows_full_annual_target_regression_lock_177`

6. Service-Tests (`service_tests`-Modul):
   - `service_non_hr_returns_all_none_vol_stat_02`: Signatur des Aufrufs
     `get_voluntary_stats(sp_id, from_date, to_date, Authentication::Context(()), None)`
     mit beliebiger Range (z.B. Full-Year 2026).
   - `service_hr_returns_some_and_delegates_to_pure_fns`: analog auf Range umstellen.
   - `service_zero_contract_weeks_yields_zero_per_week`: analog.
   - Zusatz-Assertion: `contract_weeks` matcht die Anzahl der Range-Wochen mit Vertrag.

Alle Assertions mit `assert!((x - expected).abs() < 1e-3, "…")` fuer f32-Vergleiche
(Praezedenz existierende Tests). Fuer den 5h-Mai-Test 53.57 erwarten mit Toleranz 
0.1: `assert!((soll_total - 53.57).abs() < 0.5, "expected ~53.57, got {soll_total}");`
UND `assert!(soll_total < 60.0, "regression-gate against 177h bug: got {soll_total}");`.

Fuer den 177-Regression-Lock-Test (Test G): der Test dokumentiert die alte
Full-Year-Semantik als Referenz (weil sie explizit reproduzierbar ist), aber
nicht als Fehlerfall — er beweist einfach, dass die Range-fn bei Full-Year-
Range dasselbe liefert. Der eigentliche Regression-Guard gegen 177 ist Test E
mit `to_date=2026-07-10`.

Fixture-Helpers (`make_working_hours`, `make_manual_volunteer_hours`) bleiben
unveraendert.

Zusaetzliches `use shifty_utils::ShiftyDate;` am Modulkopf.

Beim Aufruf `ShiftyDate::first_day_in_year(2026)` bzw. `ShiftyDate::last_day_in_year(2026)`
bzw. `ShiftyDate::from_ymd(2026, 7, 10).unwrap()` verwenden.

WICHTIG: Ausfuehren `cargo test -p service_impl voluntary_stats::` MUSS gruen sein.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test -p service_impl --lib voluntary_stats:: 2>&amp;1 | tee /tmp/test-54-07-task3.log; grep -E "test result: ok\." /tmp/test-54-07-task3.log</automated>
  </verify>
  <done>Alle Range-Tests gruen; keine bestehenden Tests broken; Test E (5h-Mai bis 07-10) liefert soll_total &lt; 60 (Regression-Lock gegen 177); Full-Year-Range-Tests reproduzieren alte Full-Year-Werte byte-genau (52.0 / 53.0).</done>
</task>

<task type="auto">
  <name>Task 4: REST-Query-Struct + Handler auf from_date/to_date umstellen</name>
  <files>rest/src/report.rs</files>
  <action>
In `rest/src/report.rs`:

1. `VoluntaryStatsRequest` neu definieren (Zeilen 42-54):
```rust
/// Phase 54 Gap-Closure G1 (VOL-STAT-01 / VOL-ACCT-01/02) — Query-Parameter fuer
/// `GET /report/{id}/voluntary-stats`. Analog `ReportingService::get_report_for_employee_range`
/// eine echte Date-Range: `from_date` und `to_date` als ISO YYYY-MM-DD.
///
/// Die Aggregation läuft ueber alle Tage in `[from_date ..= to_date]`;
/// Edge-Weeks tragen tages-genau bei (Pro-Rata bleibt Tages-basiert, D-F2-01).
#[derive(Clone, Debug, Deserialize, utoipa::ToSchema)]
pub struct VoluntaryStatsRequest {
    /// ISO date YYYY-MM-DD (inclusive lower bound).
    pub from_date: String,
    /// ISO date YYYY-MM-DD (inclusive upper bound).
    pub to_date: String,
}
```

2. `get_voluntary_stats` Handler (Zeilen 257-292) anpassen:
```rust
#[instrument(skip(rest_state))]
#[utoipa::path(
    get,
    path = "/{id}/voluntary-stats",
    tags = ["Report"],
    params(
        ("id" = Uuid, Path, description = "Sales person ID"),
        ("from_date" = String, Query, description = "ISO date YYYY-MM-DD, inclusive lower bound"),
        ("to_date" = String, Query, description = "ISO date YYYY-MM-DD, inclusive upper bound")
    ),
    responses(
        (status = 200, description = "HR-only voluntary hours statistics for the given date range; Non-HR receives all fields as null (API-level redaction)", body = VoluntaryStatsTO, content_type = "application/json"),
        (status = 400, description = "Invalid date format (expected YYYY-MM-DD) or from_date > to_date"),
        (status = 500, description = "Internal server error")
    )
)]
pub async fn get_voluntary_stats<RestState: RestStateDef>(
    rest_state: State<RestState>,
    query: Query<VoluntaryStatsRequest>,
    Path(sales_person_id): Path<Uuid>,
    Extension(context): Extension<Context>,
) -> Response {
    // Parse ISO YYYY-MM-DD (Praezedenz rest/src/toggle.rs Zeilen 350-377).
    let parse_iso = |s: &str| -> Option<time::Date> {
        if s.len() != 10 {
            return None;
        }
        let b = s.as_bytes();
        if b[4] != b'-' || b[7] != b'-' {
            return None;
        }
        let year: i32 = s[0..4].parse().ok()?;
        let month: u8 = s[5..7].parse().ok()?;
        let day: u8 = s[8..10].parse().ok()?;
        time::Date::from_calendar_date(year, time::Month::try_from(month).ok()?, day).ok()
    };
    let (Some(from_naive), Some(to_naive)) = (parse_iso(&query.from_date), parse_iso(&query.to_date)) else {
        return Response::builder()
            .status(400)
            .header("Content-Type", "text/plain")
            .body(Body::new("Invalid ISO date format. Expected YYYY-MM-DD.".to_string()))
            .unwrap();
    };
    if from_naive > to_naive {
        return Response::builder()
            .status(400)
            .header("Content-Type", "text/plain")
            .body(Body::new("from_date must be <= to_date.".to_string()))
            .unwrap();
    }
    let from_date = shifty_utils::ShiftyDate::from_date(from_naive);
    let to_date = shifty_utils::ShiftyDate::from_date(to_naive);
    error_handler(
        (async {
            let stats = rest_state
                .voluntary_stats_service()
                .get_voluntary_stats(sales_person_id, from_date, to_date, context.into(), None)
                .await?;
            let to_out = VoluntaryStatsTO::from(&stats);
            Ok(Response::builder()
                .status(200)
                .header("Content-Type", "application/json")
                .body(Body::new(serde_json::to_string(&to_out).unwrap()))
                .unwrap())
        })
        .await,
    )
}
```

3. `ReportApiDoc` (Zeilen 294-318) bleibt unveraendert (VoluntaryStatsTO + VoluntaryStatsRequest sind bereits registriert).

4. Wichtig: `use shifty_utils;` bereits vorhanden ODER neu adden. Falls nicht,
   Import ergaenzen (grep zeigt vermutlich schon `shifty_utils` in report.rs).

Danach Integration-Test-Datei `shifty_bin/src/integration_test/voluntary_stats.rs`
(aus Plan 54-04) muss angepasst werden:
- HTTP-URL: `/report/{id}/voluntary-stats?from_date=2026-01-01&to_date=2026-12-31`
  statt `?year=2026`.
- Alle 2 Testfaelle (HR + Non-HR) haben `year=2026` in der URL; auf Full-Year-
  Range mappen (identisches Verhalten fuer die Assertions).
- HR-Assertion: das aktuelle Seed (KW10 8h Manual VolunteerWork + committed_voluntary=2.0
  in KW10..=13) muss gegen `voluntary_ist_total_in_range` + `committed_voluntary_target_in_range`
  gleiche Werte liefern (ist_total=8.0, soll_total=8.0, delta=0.0, contract_weeks=4,
  ist_per_contract_week=2.0). Erwartung: Ergebnisse muessen bleiben, wenn Range
  = Full-Year 2026. Kleine Anpassung: 8.0 muss noch stimmen — 4 Vertragswochen × 
  (2.0/7 × 7) = 8.0 — passt.
- Test-Datei-Anpassung auch dort committen.

Verifikation: `cargo test -p shifty_bin voluntary_stats` gruen.
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test -p shifty_bin voluntary_stats 2>&amp;1 | tee /tmp/test-54-07-task4.log; grep -E "test result: ok\. 2 passed" /tmp/test-54-07-task4.log</automated>
  </verify>
  <done>REST-Query akzeptiert `?from_date=YYYY-MM-DD&to_date=YYYY-MM-DD`; ungueltiges Datum → 400; from_date &gt; to_date → 400; Integration-Test 2/2 passed mit Full-Year-Range.</done>
</task>

<task type="auto">
  <name>Task 5: Docs-Freshness F14-rebooking.md (EN + DE) + Full-Suite Gate</name>
  <files>docs/features/F14-rebooking.md, docs/features/F14-rebooking_de.md</files>
  <action>
Docs-Freshness-Gate ist ein hartes Requirement (MEMORY
`feedback_docs_always_current_no_followup`). F14 dokumentiert die
voluntary-stats-Chain und muss synchron.

In `docs/features/F14-rebooking.md` (EN):

1. Zeilen ~171-183 (Pure-fn-Signaturen) ersetzen auf Range-Semantik:
```
pub fn voluntary_ist_total_in_range(
    extra_hours: &[ExtraHours],
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> f32;

pub fn contract_weeks_count_in_range(
    working_hours: &[EmployeeWorkDetails],
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> u32;

pub fn committed_voluntary_prorata_for_week(
    working_hours: &[EmployeeWorkDetails], year: u32, week: u8) -> f32;   // internal per-week building block

pub fn committed_voluntary_target_in_range(
    working_hours: &[EmployeeWorkDetails],
    from_date: ShiftyDate,
    to_date: ShiftyDate,
) -> f32;
```

2. REST-Endpoint-Sektion (Zeile ~201): `GET /report/{id}/voluntary-stats?from_date=YYYY-MM-DD&to_date=YYYY-MM-DD`
   statt `?year=YYYY`.

3. Kurzer Rationale-Absatz einfuegen: "Range-based aggregation (Phase 54 Gap G1):
   consistent with `ReportingService::get_report_for_employee_range`; edge weeks
   contribute pro-rata for the days that fall inside the range. Without the
   cutoff, a 5h/week voluntary commitment starting in May yielded a full-year
   target that overshot the actual reporting range by ~4x (177h vs realistic
   ~50h for Jan-July window). See 54-UAT.md gap G1."

In `docs/features/F14-rebooking_de.md` (DE):

1. Zeilen ~181-193 analog auf Range-Signaturen umstellen.
2. Zeile ~201 REST-Endpoint auf `?from_date=YYYY-MM-DD&to_date=YYYY-MM-DD`.
3. Rationale-Absatz (deutsch): "Range-basierte Aggregation (Phase 54 Gap G1):
   konsistent mit `ReportingService::get_report_for_employee_range`;
   Edge-Weeks tragen pro-rata fuer die Tage im Range bei. Ohne Cutoff lieferte
   eine 5h/Woche-Zusage ab Mai ein Full-Year-Ziel, das den tatsaechlichen
   Report-Zeitraum um ~4x ueberschoss (177h vs realistisch ~50h fuer Jan-Juli).
   Siehe 54-UAT.md Gap G1."

**Grep-Verify Doku:**
```
grep -c "voluntary_ist_total_for_year\|committed_voluntary_target_for_year\|voluntary-stats?year=" docs/features/F14-rebooking.md docs/features/F14-rebooking_de.md
```
Erwartung: 0 (alle alten References sind entfernt).

**Full-Suite-Gate am Ende dieses Plans:**
```
SQLX_OFFLINE=true cargo build --workspace
SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test --workspace
SQLX_OFFLINE=true cargo clippy --workspace -- -D warnings
```

Alle drei MUESSEN gruen sein. Wenn nicht: Fehler analysieren, fixen. Kein Commit
mit rotem Gate. Clippy-Gate ist Pflicht (MEMORY `feedback_clippy_gate`).

**Grep-Verify** alte fn-Namen sind nirgends mehr im Rust-Code:
```
grep -rn "voluntary_ist_total_for_year\|contract_weeks_count\b\|committed_voluntary_target_for_year" service/ service_impl/ rest/ shifty_bin/ rest-types/ 2>/dev/null
```
Erwartung: 0 Treffer.

Wichtig: `contract_weeks_count_in_range` matcht NICHT das grep-Pattern `contract_weeks_count\b`
(word boundary), deshalb wird nur die alte fn gefunden — wenn sie noch existiert.

WARNUNG: Wenn `shifty-dioxus/src/api.rs` den alten `?year=YYYY`-URL-Aufruf
enthaelt, ist das ERWARTBAR — dieser Plan bricht bewusst den FE-URL-Aufruf
(Wave 1), und Plan 54-08 zieht ihn in Wave 2 nach. Der FE-Aufruf faellt auf 
400 Bad Request zurueck und `unwrap_or_default()` in service/employee.rs 
Zeile 154 fuellt Store mit `VoluntaryStats::default()`. Row rendert leer
(Non-HR-aequivalent). Kein UI-Crash, keine Panic.

Commit-Konvention: `feat(54-07): voluntary-stats — accept date range instead of ISO year (Gap G1)`
und dann docs commit: `docs(54-07): sync F14 to voluntary-stats range semantics`.
Oder zusammen. Executor entscheidet nach Praezedenz (typischerweise ein commit
pro TDD-Zyklus + docs im gleichen wrap-commit).
  </action>
  <verify>
    <automated>SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test --workspace 2>&amp;1 | tail -20 | tee /tmp/test-54-07-final.log; SQLX_OFFLINE=true cargo clippy --workspace -- -D warnings 2>&amp;1 | tail -10 | tee /tmp/clippy-54-07.log; test $(grep -c "voluntary_ist_total_for_year\|contract_weeks_count[^_]\|committed_voluntary_target_for_year" service/ service_impl/ rest/ shifty_bin/ rest-types/ -r 2>/dev/null | grep -vc ":0" || true) -eq 0</automated>
  </verify>
  <done>F14 EN + DE synchron zu Range-Semantik; cargo test --workspace gruen; cargo clippy --workspace -- -D warnings gruen; keine grep-Treffer der alten Full-Year-fn-Namen im Rust-Workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (FE / API-Client) → REST /report/{id}/voluntary-stats | User-supplied `from_date`, `to_date` als ISO-Strings; muessen validiert werden. Ungueltiges Format oder from>to → 400. |
| REST → VoluntaryStatsService | Context/Authentication transportiert; HR-Gate im Service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-07-01 | Tampering | REST /voluntary-stats Query | medium | mitigate | ISO-Date-Parse mit expliziter Length- + Delimiter-Check (Praezedenz toggle.rs); Fallback → HTTP 400 statt Panic; `from_date > to_date` → HTTP 400. |
| T-54-07-02 | Information Disclosure | VoluntaryStatsService — HR-Only Daten | high | mitigate | HR-Gate-First-Check (unveraendert aus Plan 03); Non-HR → all-None DTO (Praezedenz VAC-OFFSET-01 v1.8, kein 403). |
| T-54-07-03 | Denial of Service | contract_weeks_count_in_range mit `to_date - from_date` Range | low | accept | Range-Iteration ist O(Tage im Range); realistisch <= 366 Tage im typischen Employee-Report; kein DoS-Vektor. Kein Rate-Limit noetig. |
| T-54-07-04 | Repudiation | Berechnungs-Fehler nach Refactor | high | mitigate | 10 Range-Tests (Regression + Edge-Weeks + 5h-Mai-Szenario + Property-Test VOL-ACCT-03) verhindern Semantik-Drift; docs/features/F14 dokumentiert Rationale + Beispiel. |
| T-54-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Kein neues Cargo-Dep in diesem Plan (nur bestehende `time`-Features genutzt); RESEARCH Package Legitimacy Audit nicht getriggert. |
</threat_model>

<verification>

Ende dieses Plans muessen ALLE Gates gruen sein:

1. `SQLX_OFFLINE=true cargo build --workspace` — kein Compile-Fehler
2. `SQLX_OFFLINE=true DATABASE_URL="sqlite::memory:" cargo test --workspace` — 0 failed
3. `SQLX_OFFLINE=true cargo clippy --workspace -- -D warnings` — 0 warnings (Pflicht-Gate)
4. `cargo test -p service_impl voluntary_stats::` alle Range-Tests gruen inkl. 5h-Mai-Szenario
5. `cargo test -p shifty_bin voluntary_stats` 2/2 passed mit Full-Year-Range in URL
6. `grep -rn "voluntary_ist_total_for_year\|committed_voluntary_target_for_year" docs/features/ service/ service_impl/ rest/ shifty_bin/ rest-types/` — 0 Treffer im Rust + Docs
7. `grep -c "?from_date=" docs/features/F14-rebooking.md` >= 1
8. `grep -c "?from_date=" docs/features/F14-rebooking_de.md` >= 1

</verification>

<success_criteria>

- VoluntaryStatsService konsumiert `(from_date: ShiftyDate, to_date: ShiftyDate)` statt `(year: u32)`.
- 3 neue in-range pure fns in service_impl/src/reporting.rs; alte 3 Full-Year-fns entfernt.
- REST `?from_date=YYYY-MM-DD&to_date=YYYY-MM-DD`; ungueltiges Datum → 400.
- Tests: Full-Year-Regression, Edge-Week-Start, Edge-Week-End, 5h-Mai (soll_total < 60), Kongruenz, Non-HR-None.
- Property-Test VOL-ACCT-03 (Rebooking-Neutralität) angepasst und gruen.
- Docs F14 EN + DE synchron zu Range-Semantik + Rationale-Absatz.
- Alle Backend-Gates gruen (build + test + clippy -D warnings).
- Grep-Sanity: keine alten fn-Namen mehr im Rust-Code oder in F14.

</success_criteria>

<output>
Create `.planning/phases/54-data-model-voluntary-stats/54-07-SUMMARY.md` when done.
Include:
- Pure-fn Signaturen (Range-fähig)
- Trait- + Impl-Signatur diff
- Test-Uebersicht (mit Werten fuer 5h-Mai-Szenario, Full-Year-Regression, Edge-Weeks)
- Verification-Log (build, test, clippy, integration)
- Docs-Freshness-Diff (F14 EN + DE)
- Deviations from Plan (falls vorhanden)
- Auth-Gates (falls auftreten)
- Commits (git log --oneline --grep="54-07")
</output>