---
phase: 54-data-model-voluntary-stats
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - service/src/rebooking_batch.rs
  - service/src/lib.rs
  - service_impl/src/rebooking_batch.rs
  - service_impl/src/lib.rs
  - service_impl/src/test/mod.rs
  - service_impl/src/test/rebooking_batch.rs
  - shifty_bin/src/main.rs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "[D-54-DM-01] RebookingBatchService::create respektiert den DB-UNIQUE-Index — zwei aktive Batches fuer (sp, y, w) fuehren zu ServiceError::EntityAlreadyExists, ohne Panic; ein Test fangt das ab."
    - "RebookingBatchService ist Basic-Tier: seine Dep-Liste ist strikt {RebookingBatchDao, PermissionService, TransactionDao, UuidService, ClockService} — kein anderer Domain-Service (Service-Tier-Konvention CLAUDE.md)."
  artifacts:
    - "service/src/rebooking_batch.rs: pub trait RebookingBatchService (find_by_id, find_by_sales_person_year_week, create) mit type Context/Transaction, mockall::automock."
    - "service_impl/src/rebooking_batch.rs: RebookingBatchServiceImpl per gen_service_impl!-Makro; Permission-Gate uebernimmt shiftplan.edit-Aequivalent (siehe Task 1 fuer Gate-Wahl)."
    - "shifty_bin/src/main.rs: Konstruktion von RebookingBatchDaoImpl + RebookingBatchServiceImpl in der Basic-Services-Wave; RebookingBatchService via RestStateDef exponiert."
    - "service_impl/src/test/rebooking_batch.rs: mockall-basierte Unit-Tests fuer find_by_id, find_by_sales_person_year_week, create (Happy Path + UNIQUE-Konflikt-Path)."
  key_links:
    - "Der neue Service ist Basic — er darf nur DAO/Permission/Transaction konsumieren; ein Consumer (RebookingReconciliationService in Phase 55) waere BL und wuerde diesen Basic-Service verwenden."
    - "Kein neuer REST-Endpoint fuer rebooking_batch in Phase 54 — der Trait ist bewusst noch nicht extern erreichbar; erst Phase 55 exponiert Endpoints."
---

<objective>
Basic-Tier-Service `RebookingBatchService` als Entity-Manager fuer die neuen
Tabellen aus Plan 01. Der Service hat CRUD-Skelett (find_by_id, find_by_sales_person_year_week,
create) mit Permission-Gate, gen_service_impl!-DI, und wird in shifty_bin
verdrahtet, um in Phase 55 vom `RebookingReconciliationService` (BL) konsumiert zu werden.

Purpose: Trennung von Datenmodell und Business-Logik gemaess Service-Tier-Konvention
(CLAUDE.md). Ohne diesen Basic-Service muesste die BL-Reconciliation-Logik in Phase 55
direkt in den DAO greifen — das wuerde die DI-Konstruktion in shifty_bin und
Permission-Gating verwaschen.

Output: Trait + Impl + DI-Wiring + Mock-basierte Unit-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/54-data-model-voluntary-stats/54-CONTEXT.md
@.planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md
@.planning/phases/54-data-model-voluntary-stats/54-01-PLAN.md

# Praezedenzen zum Kopieren (Basic-Tier-Service)
@service/src/week_status.rs
@service_impl/src/week_status.rs
@service/src/vacation_entitlement_offset.rs
@service_impl/src/vacation_entitlement_offset.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trait RebookingBatchService (crate service)</name>
  <files>service/src/rebooking_batch.rs, service/src/lib.rs</files>
  <read_first>
    - service/src/week_status.rs (exakter Trait-Aufbau: type Context/Transaction, mockall::automock, HR-Gate-lose Reads-vs-Writes-Unterscheidung)
    - service/src/vacation_entitlement_offset.rs (Praezedenz Basic-Tier mit HR-Gate)
    - dao/src/rebooking_batch.rs (aus Plan 01, zur Signatur-Konsistenz)
  </read_first>
  <action>
    Neue Datei service/src/rebooking_batch.rs mit:

      use crate::permission::Authentication;
      use crate::ServiceError;
      use async_trait::async_trait;
      use dao::MockTransaction;
      use dao::rebooking_batch::{
          RebookingBatchEntity, RebookingBatchEntryEntity, RebookingBatchKind, RebookingBatchState,
      };
      use mockall::automock;
      use std::fmt::Debug;
      use std::sync::Arc;
      use uuid::Uuid;

      /// Phase 54 Basic-Tier: Entity-Manager fuer rebooking_batch + rebooking_batch_entry.
      /// Consumer (RebookingReconciliationService = BL) ab Phase 55.
      /// Permission-Gate: HR fuer Reads UND Writes in Phase 54 (Skelett — der
      /// eigentliche Write-Pfad kommt via BL in Phase 55; direktes Anlegen von
      /// Batches umgeht die BL und ist auf HR beschraenkt).
      #[automock(type Context=(); type Transaction=MockTransaction;)]
      #[async_trait]
      pub trait RebookingBatchService {
          type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
          type Transaction: dao::Transaction;

          async fn find_by_id(
              &amp;self,
              id: Uuid,
              context: Authentication&lt;Self::Context&gt;,
              tx: Option&lt;Self::Transaction&gt;,
          ) -&gt; Result&lt;Option&lt;RebookingBatchEntity&gt;, ServiceError&gt;;

          async fn find_by_sales_person_year_week(
              &amp;self,
              sales_person_id: Uuid,
              iso_year: u32,
              iso_week: u8,
              context: Authentication&lt;Self::Context&gt;,
              tx: Option&lt;Self::Transaction&gt;,
          ) -&gt; Result&lt;Option&lt;RebookingBatchEntity&gt;, ServiceError&gt;;

          /// Wave-1-Contract: batch + entries werden in einer Transaktion
          /// gespeichert; die id/version werden vom Service via UuidService gesetzt,
          /// created via ClockService; kind und state uebernimmt der Aufrufer.
          /// UNIQUE-Konflikt (D-54-DM-01) mapped auf ServiceError::EntityAlreadyExists.
          async fn create(
              &amp;self,
              batch: &amp;RebookingBatchEntity,
              entries: &amp;[RebookingBatchEntryEntity],
              context: Authentication&lt;Self::Context&gt;,
              tx: Option&lt;Self::Transaction&gt;,
          ) -&gt; Result&lt;RebookingBatchEntity, ServiceError&gt;;
      }

    service/src/lib.rs: `pub mod rebooking_batch;` alphabetisch nach `pub mod reporting;`.

    Doku-Kommentar am Trait: "Phase 55 wird zusaetzliche Methoden hinzufuegen
    (update_state approve/reject, list_pending). In Phase 54 halten wir das Trait
    minimal, damit die Basic-Tier-Konvention (Basic-Service ohne Domain-Service-Dep)
    klar sichtbar bleibt."
  </action>
  <verify>
    <automated>cargo build -p service --quiet</automated>
  </verify>
  <done>
    Trait RebookingBatchService kompiliert; MockRebookingBatchService verfuegbar; alle drei Methoden mit Authentication + Option&lt;Transaction&gt; nach Repo-Konvention.
  </done>
</task>

<task type="auto">
  <name>Task 2: Impl RebookingBatchServiceImpl (crate service_impl)</name>
  <files>service_impl/src/rebooking_batch.rs, service_impl/src/lib.rs</files>
  <read_first>
    - service_impl/src/week_status.rs (WeekStatusServiceImpl-Muster mit gen_service_impl!, Auth-Gate, DAO-Konvertierung)
    - service_impl/src/vacation_entitlement_offset.rs (Basic-Service mit HR-Gate via check_permission(HR_PRIVILEGE, ctx))
    - service_impl/src/lib.rs (Modul-Export-Konvention)
    - service_impl/src/macros.rs (gen_service_impl!-Makro)
    - service_impl/src/reporting.rs Zeilen 15, 865 (HR_PRIVILEGE-Import + check_permission-Muster)
  </read_first>
  <action>
    Neue Datei service_impl/src/rebooking_batch.rs mit:

    - use-Statements: `use crate::gen_service_impl;`, `async_trait::async_trait`,
      `dao::rebooking_batch::{RebookingBatchDao, RebookingBatchEntity, RebookingBatchEntryEntity}`,
      `dao::TransactionDao`, `service::clock::ClockService`, `service::uuid_service::UuidService`,
      `service::permission::{Authentication, HR_PRIVILEGE}`, `service::rebooking_batch::RebookingBatchService`,
      `service::{PermissionService, ServiceError}`, `uuid::Uuid`.

    - gen_service_impl!-Makro:

        gen_service_impl! {
            struct RebookingBatchServiceImpl: RebookingBatchService = RebookingBatchServiceDeps {
                RebookingBatchDao: dao::rebooking_batch::RebookingBatchDao&lt;Transaction = Self::Transaction&gt; = rebooking_batch_dao,
                PermissionService: PermissionService&lt;Context = Self::Context&gt; = permission_service,
                TransactionDao: dao::TransactionDao&lt;Transaction = Self::Transaction&gt; = transaction_dao,
                UuidService: UuidService = uuid_service,
                ClockService: ClockService = clock_service,
            }
        }

    - `#[async_trait] impl&lt;Deps: RebookingBatchServiceDeps&gt; RebookingBatchService for RebookingBatchServiceImpl&lt;Deps&gt;`:

        find_by_id / find_by_sales_person_year_week:
          - `self.permission_service.check_permission(HR_PRIVILEGE, context.clone()).await?;`
          - `let tx = self.transaction_dao.use_transaction(tx).await?;`
          - Delegation an DAO, Ergebnis zurueckgeben, `self.transaction_dao.commit(tx).await?;`.

        create:
          - HR_PRIVILEGE check.
          - Transaktion oeffnen.
          - Batch: id und version via UuidService::new_uuid setzen falls Uuid::nil (defensiv),
            created via ClockService::now falls PrimitiveDateTime::MIN (defensiv);
            Entries: pro Entry gleicher Umgang mit id/version/created.
          - `self.rebooking_batch_dao.create_batch_with_entries(&amp;batch, entries, "phase-54-rebooking-batch-create", tx.clone()).await`
            konvertieren:
              - Ok(()) =&gt; Ok(batch mit neu vergebenen Feldern)
              - Err(DaoError::UniqueConstraint(_)) =&gt; ServiceError::EntityAlreadyExists(batch.id)
              - Err(other) =&gt; other.into() (via impl From&lt;DaoError&gt; for ServiceError)
          - commit + Ok(batch).

    service_impl/src/lib.rs: `pub mod rebooking_batch;` alphabetisch einfuegen.

    Referenziere D-54-DM-01 im Impl-Doc-Kommentar von `create`:
      "UNIQUE-Konflikt (rebooking_batch_week_unique_idx aus Migration 20260707000000)
       wird per DaoError::UniqueConstraint auf ServiceError::EntityAlreadyExists gemapped."
  </action>
  <verify>
    <automated>cargo build -p service_impl --quiet</automated>
  </verify>
  <done>
    service_impl/src/rebooking_batch.rs compiliert; RebookingBatchServiceImpl bindet an das Trait; alle drei Methoden implementiert; HR-Gate an erster Stelle jeder Methode (T-54-01 Access Control).
  </done>
</task>

<task type="auto">
  <name>Task 3: DI-Wiring in shifty_bin/src/main.rs</name>
  <files>shifty_bin/src/main.rs</files>
  <read_first>
    - shifty_bin/src/main.rs (Konstruktion week_status-Service als Vorlage — grep 'week_status_dao\|week_status_service')
    - rest/src/lib.rs Zeilen 400-480 (RestStateDef-Trait, welche Services exponiert werden muessen — Praezedenz WeekStatusService)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §F (DI-Wiring)
  </read_first>
  <action>
    Aenderungen in shifty_bin/src/main.rs:

    (a) Neuer type-alias (nach `type WorkingHoursService = ...` bzw. dem entsprechenden Alias-Block):

        type RebookingBatchService = service_impl::rebooking_batch::RebookingBatchServiceImpl&lt;
            service_impl::rebooking_batch::RebookingBatchServiceDeps&lt;
                ClockServiceImpl,
                RebookingBatchDaoImpl,          // aus dao_impl_sqlite
                PermissionService,
                Transaction,                    // Repo-Alias
                TransactionDao,
                UuidServiceImpl,
                Context,
            &gt;,
        &gt;;

        (Beobachte den exakten Type-Parameter-Order per week_status als Praezedenz.)

    (b) Innerhalb der Service-Konstruktion (Basic-Services-Wave, nach week_status_service):

        let rebooking_batch_dao = Arc::new(dao_impl_sqlite::rebooking_batch::RebookingBatchDaoImpl::new(pool.clone()));
        let rebooking_batch_service = Arc::new(
            service_impl::rebooking_batch::RebookingBatchServiceImpl {
                rebooking_batch_dao: rebooking_batch_dao.clone(),
                permission_service: permission_service.clone(),
                transaction_dao: transaction_dao.clone(),
                uuid_service: uuid_service.clone(),
                clock_service: clock_service.clone(),
            },
        );

    (c) Struct-Feld `rebooking_batch_service: Arc&lt;RebookingBatchService&gt;` an RestStateImpl anfuegen
        (Sortierung analog week_status_service).

    (d) In `RestStateImpl::new(...)` das neue Feld initialisieren.

    (e) In `impl rest::RestStateDef for RestStateImpl` einen Getter analog anderen Basic-Services anfuegen:
        `fn rebooking_batch_service(&amp;self) -&gt; Arc&lt;Self::RebookingBatchService&gt; { self.rebooking_batch_service.clone() }`
        UND in dem Trait (siehe Task 4).

    Rueckwirkende Anforderung an rest/src/lib.rs (RestStateDef-Trait): siehe Task 4.
    Task 3 bleibt auf shifty_bin fokussiert; Task 4 macht den Trait-Delta in rest/src/lib.rs.
  </action>
  <verify>
    <automated>cargo build -p shifty_bin --quiet</automated>
  </verify>
  <done>
    shifty_bin compiliert; RebookingBatchServiceImpl in der Basic-Services-Wave konstruiert (VOR allen BL-Services die es spaeter konsumieren wuerden); RestStateImpl trage rebooking_batch_service; kein Zyklus.
  </done>
</task>

<task type="auto">
  <name>Task 4: RestStateDef-Trait um RebookingBatchService erweitern (crate rest)</name>
  <files>rest/src/lib.rs</files>
  <read_first>
    - rest/src/lib.rs Zeilen 380-480 (assoziierter Type + Getter fuer WeekStatusService als 1:1-Vorlage)
  </read_first>
  <action>
    Ergaenze rest/src/lib.rs zwei Stellen:

    (a) Assoziierter Type-Block (nach WeekStatusService-Block):

        type RebookingBatchService: service::rebooking_batch::RebookingBatchService&lt;Context = Context&gt;
            + Send
            + Sync
            + 'static;

    (b) Getter (nach week_status_service):

        fn rebooking_batch_service(&amp;self) -&gt; Arc&lt;Self::RebookingBatchService&gt;;

    Keine anderen Aenderungen (kein neuer Endpoint, keine OpenAPI-Erweiterung — Phase 54
    exponiert den Service NOCH NICHT via REST; erst Phase 55 fuegt Endpoints hinzu).
  </action>
  <verify>
    <automated>cargo build -p rest --quiet</automated>
  </verify>
  <done>
    RestStateDef enthaelt RebookingBatchService als assoziierter Type + Getter; rest-Crate compiliert (RestStateImpl in shifty_bin liefert bereits).
  </done>
</task>

<task type="auto">
  <name>Task 5: Unit-Tests fuer RebookingBatchService (mockall)</name>
  <files>service_impl/src/test/mod.rs, service_impl/src/test/rebooking_batch.rs</files>
  <read_first>
    - service_impl/src/test/mod.rs (Modul-Registrierung)
    - service_impl/src/test/booking_information_vaa.rs (Mock-basiertes Service-Test-Muster mit MockPermissionService)
    - service_impl/src/test/permission_test.rs (Auth-Setup-Muster)
    - dao/src/rebooking_batch.rs (MockRebookingBatchDao — aus Plan 01)
  </read_first>
  <action>
    service_impl/src/test/mod.rs: `mod rebooking_batch;` einfuegen.

    Neue Datei service_impl/src/test/rebooking_batch.rs mit mindestens 4 #[tokio::test]:

      (1) find_by_id_success:
          - MockPermissionService::expect_check_permission mit HR_PRIVILEGE + Ok(()).
          - MockRebookingBatchDao::expect_find_by_id mit Ok(Some(RebookingBatchEntity{...})).
          - MockTransactionDao::expect_use_transaction + expect_commit.
          - Assert: Service.find_by_id gibt Some(entity) zurueck.

      (2) find_by_sales_person_year_week_none:
          - Analog, aber DAO gibt None zurueck.
          - Assert Ok(None).

      (3) create_success:
          - MockPermissionService HR-Gate ok.
          - MockUuidService::expect_new_uuid mehrfach (fuer batch + je entry).
          - MockClockService::expect_now.
          - MockRebookingBatchDao::expect_create_batch_with_entries mit Ok(()).
          - Assert Ok(entity mit belegter id).

      (4) create_unique_conflict_maps_to_already_exists:
          - HR-Gate ok, UuidService/Clock ok.
          - MockRebookingBatchDao::expect_create_batch_with_entries returns
            Err(DaoError::UniqueConstraint("rebooking_batch_week_unique_idx".into()))
            (falls DaoError::UniqueConstraint nicht existiert: DaoError::DatabaseError mit dem UniqueViolation-String; passe Task 2-Mapping entsprechend an).
          - Assert Err(ServiceError::EntityAlreadyExists(_)).

      (5) find_by_id_non_hr_forbidden:
          - MockPermissionService::expect_check_permission returns Err(ServiceError::Forbidden).
          - Assert Err(ServiceError::Forbidden), DAO wird NICHT aufgerufen (expect_find_by_id NICHT gesetzt).

    Nutze die `NoneTypeExt`-Konvention aus CLAUDE.md fuer Authentication-Setup falls im Repo-Muster verwendet; sonst manuell Authentication::Full oder Auth-mit-Context-Struct wie in booking_information_vaa.

    Keine In-Memory-SQLite-Fixture in diesem Test — MockRebookingBatchDao reicht.
  </action>
  <verify>
    <automated>cargo test -p service_impl rebooking_batch:: --lib -- --quiet</automated>
  </verify>
  <done>
    5 Tests gruen; kein flaky (mockall-strict); Coverage fuer HR-Gate + UNIQUE-Konflikt-Mapping.
  </done>
</task>

<task type="auto">
  <name>Task 6: Wave-2-Gate — Full-Workspace-Test + Clippy</name>
  <files>.sqlx/</files>
  <read_first>
    - CLAUDE.md §"Clippy ist ein hartes Gate"
  </read_first>
  <action>
    Sequenz:
      cargo build --workspace
      cargo test --workspace
      cargo clippy --workspace -- -D warnings

    Bei Fehler: fix in Task 1-5 statt Workaround. Kein `#[allow(...)]` ohne Reason-Kommentar
    (Praezedenz Phase 45 D-45-01).
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
    Full-Suite gruen; Wave 2 bereit fuer Wave 3.
  </done>
</task>

</tasks>

<verification>
- RebookingBatchService als Basic-Tier-Trait registriert (Nur DAO + Permission + Transaction + Uuid + Clock als Deps)
- MockRebookingBatchService verfuegbar
- Impl mit HR_PRIVILEGE-Gate an erster Stelle jeder Methode
- UNIQUE-Konflikt sauber auf ServiceError::EntityAlreadyExists gemappt
- DI-Wiring in shifty_bin verdrahtet; kein Zyklus (grep bestaetigt: rebooking_batch_service konsumiert keinen Domain-Service)
- 5 mockall-Tests gruen
- cargo clippy --workspace -- -D warnings gruen
</verification>

<success_criteria>
- Kein Konsument in Phase 54 — Basic-Service steht zum Konsum durch Phase 55/56 bereit.
- `grep 'rebooking_batch_service' shifty_bin/src/main.rs` findet Konstruktion + RestStateImpl-Feld + RestStateDef-Getter.
- Test create_unique_conflict_maps_to_already_exists dokumentiert D-54-DM-01 als CI-Guard.
- Workspace-Build + Clippy gruen.
</success_criteria>

<output>
Erstelle `.planning/phases/54-data-model-voluntary-stats/54-02-SUMMARY.md` mit:
Trait-Signatur (Rust-Snippet), DI-Konstruktions-Diff-Snippet aus shifty_bin/src/main.rs
(nur der neue Block), Test-Uebersicht (5 tests, alle green), Clippy-Ergebnis.
Aktualisiere 54-VALIDATION.md Task-IDs 54-02-01 + 54-02-02 auf ✅ green.
</output>
