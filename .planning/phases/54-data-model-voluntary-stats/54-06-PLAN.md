---
phase: 54-data-model-voluntary-stats
plan: 06
type: execute
wave: 4
depends_on:
  - 54-01
  - 54-02
  - 54-03
  - 54-04
  - 54-05
files_modified:
  - docs/features/F14-rebooking.md
  - docs/features/F14-rebooking_de.md
  - docs/features/README.md
  - docs/features/README_de.md
  - docs/features/F07-reporting-balance.md
  - docs/features/F07-reporting-balance_de.md
  - docs/features/F08-billing-period.md
  - docs/features/F08-billing-period_de.md
  - docs/architecture/02-service-tiers.md
  - docs/architecture/02-service-tiers_de.md
  - docs/architecture/03-data-model.md
  - docs/architecture/03-data-model_de.md
  - docs/architecture/diagrams/service-graph-runtime.mmd
  - docs/architecture/diagrams/db-schema-er.mmd
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "docs/features/F14-rebooking.md und F14-rebooking_de.md existieren mit identischer Struktur (H1/H2 gleich), F1..F5-Uebersicht, Batch-Struktur, und referenzieren [D-54-DM-02] als Marker-Filter-Regel."
    - "[D-54-DM-01] docs/architecture/03-data-model.md und _de.md dokumentieren beide neuen Tabellen rebooking_batch, rebooking_batch_entry und den globalen UNIQUE-Partial-Index."
    - "docs/architecture/02-service-tiers.md + _de.md listen RebookingBatchService als Basic-Tier und VoluntaryStatsService als Business-Logic-Tier mit korrekten Deps."
    - "Diagramme db-schema-er.mmd + service-graph-runtime.mmd zeigen die neuen Nodes + Kanten."
  artifacts:
    - "docs/features/F14-rebooking.md (NEU) — Feature-Ueberblick F1..F5, Batch-Struktur, Marker-Filter-Regel."
    - "docs/features/F14-rebooking_de.md (NEU) — strukturell identische deutsche Version."
    - "docs/features/README.md + README_de.md: Index-Zeile fuer F14 ergaenzt."
    - "F07 + F08 (beide Sprachen): narrative Note zu source-Filter und Snapshot-12-Bestaetigung."
    - "02-service-tiers + 03-data-model + Diagramme: neue Nodes/Kanten."
  key_links:
    - "Same-Commit-Regel: Docs-Update landet gemaess CLAUDE.md-Docs-Freshness-Gate + MEMORY feedback_docs_always_current_no_followup im gleichen Commit wie Code-Diffs; Wave 4 fasst 14 Docs-Files in einem Commit zusammen."
    - "Kein Snapshot-Version-Bump in Phase 54 — die Version 12 bleibt (Bump 12->13 ist REB-AUTO-05 in Phase 56); wir dokumentieren das explizit in F08."
    - "Keine Umlaute in Dateinamen (MEMORY feedback_no_umlauts_in_paths) — F14-rebooking.md + F14-rebooking_de.md sind Ascii-safe."
---

<objective>
Docs-Freshness-Gate: Alle triggernden Docs-Files gemaess CLAUDE.md-Trigger-Tabelle
werden EN + DE synchron aktualisiert. Neu: F14-rebooking (beide Sprachen). Updates:
F07, F08 (narrative Notes), 02-service-tiers, 03-data-model, beide Mermaid-Diagramme,
Feature-Index.

Purpose: Kein deferred_item — Docs muessen mit Code-Diff synchron laufen
(MEMORY feedback_docs_always_current_no_followup). Der Milestone-Close-Audit
blockiert sonst.

Output: 14 Docs-Files: 2 neu, 12 Updates (inkl. Index).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-data-model-voluntary-stats/54-CONTEXT.md
@.planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md

# Vorlagen (Struktur + Sprach-Parallelitaet)
@docs/features/F13-system-infrastructure.md
@docs/features/F13-system-infrastructure_de.md
@docs/features/F07-reporting-balance.md
@docs/features/F08-billing-period.md
@docs/architecture/02-service-tiers.md
@docs/architecture/03-data-model.md
@docs/architecture/diagrams/service-graph-runtime.mmd
@docs/architecture/diagrams/db-schema-er.mmd
</context>

<tasks>

<task type="auto">
  <name>Task 1: NEU docs/features/F14-rebooking.md + _de.md + Index-Ergaenzung</name>
  <files>docs/features/F14-rebooking.md, docs/features/F14-rebooking_de.md, docs/features/README.md, docs/features/README_de.md</files>
  <read_first>
    - docs/features/F13-system-infrastructure.md (Struktur-Vorlage: H1 Title, H2 Purpose, H2 Domain Model, H2 Services, H2 REST, H2 Data Model, H2 Related Features)
    - docs/features/F13-system-infrastructure_de.md (Sprach-Parallelitaet)
    - docs/features/README.md (Feature-Index — F14 nach F13 verlinken)
    - docs/features/F07-reporting-balance.md (Marker-Filter-Kontext)
    - .planning/phases/54-data-model-voluntary-stats/54-RESEARCH.md §A + §B (Data-Model + Services)
  </read_first>
  <action>
    Datei 1 — docs/features/F14-rebooking.md (englisch, Ascii-safe Filename):

    Kapitel-Struktur:
      H1 F14 — Voluntary Rebooking
      H2 Purpose — kurz: gedeckelte Mitarbeiter mit voluntary hours sollen sich
         gegen negatives Konto ausgleichen; Milestone v2.6 liefert die End-to-End-
         Pipeline (F1..F5); Phase 54 die Datenmodell-Basis + HR-only F1/F2.
      H2 Feature Slices — nummerierte Liste F1 (Phase 54, shipped), F2 (Phase 54,
         shipped), F3 (Phase 55, planned), F4 (Phase 56, planned), F5 (Phase 55,
         planned). Jede Zeile 1-Satz Purpose + Referenz aufs verantwortliche Milestone.
      H2 Marker-Filter Rule — verweis auf D-54-DM-02: alle Aggregat-Reader filtern
         source='manual'; source='rebooking'-Rows sind audit-visible, aber nicht
         user-facing. Erklaerung, warum das die Balance-Neutralitaet (VOL-ACCT-03)
         auch nach F3/F4/F5-Rebooking-Pair-Rows sichert.
      H2 Batch Structure — Tabelle rebooking_batch (Spalten kind, state,
         UNIQUE-Constraint D-54-DM-01) und rebooking_batch_entry (Spalten hours,
         balance_before, voluntary_actual, voluntary_committed, extra_hours_out_id,
         extra_hours_in_id). extra_hours_out_id + extra_hours_in_id sind NULL bis
         state='approved'.
      H2 Services (Phase 54 baseline) — Zwei-Zeilen-Tabelle:
         RebookingBatchService (Basic, HR-gated CRUD, Consumer ab Phase 55).
         VoluntaryStatsService (BL, Read-only F1/F2, API-Level None-Redact fuer Non-HR).
      H2 REST (Phase 54) — GET /report/{id}/voluntary-stats?year=YYYY liefert
         VoluntaryStatsTO, HR-only via Option-Felder=None fuer Non-HR.
      H2 Related Features — Verweise auf F04 (extra_hours.source), F07 (Balance-
         Chain filter ab Phase 55), F08 (kein Snapshot-Bump in Phase 54; REB-AUTO-05
         pinnt Bump-Entscheidung in Phase 56).

    Datei 2 — docs/features/F14-rebooking_de.md: strukturgleich, deutsche
    Uebersetzung. Gleiche H1/H2/Tabellen-Zeilen; Diagramm-Referenzen identisch
    (wenn welche eingebettet — hier keine, alle Diagramme leben in architecture/).

    Datei 3 + 4 — Index: docs/features/README.md + README_de.md um Zeile
    "F14 Voluntary Rebooking (Link auf F14-rebooking.md)" nach F13-Zeile ergaenzen.

    Filenames sind Ascii-only (MEMORY feedback_no_umlauts_in_paths). Textinhalt
    darf Umlaute enthalten (analog zu anderen _de.md-Files).
  </action>
  <verify>
    <automated>test -f docs/features/F14-rebooking.md; test -f docs/features/F14-rebooking_de.md; grep -q '^# F14' docs/features/F14-rebooking.md; grep -q 'F14' docs/features/README.md; grep -q 'voluntary-stats' docs/features/F14-rebooking.md</automated>
  </verify>
  <done>
    Beide neuen F14-Files existieren; H1 zeigt "# F14"; Feature-Index verweist auf F14; Marker-Filter-Regel-Kapitel + REST-Kapitel + Batch-Struktur-Kapitel jeweils praesent.
  </done>
</task>

<task type="auto">
  <name>Task 2: UPDATE docs/features/F07-reporting-balance + F08-billing-period (beide Sprachen)</name>
  <files>docs/features/F07-reporting-balance.md, docs/features/F07-reporting-balance_de.md, docs/features/F08-billing-period.md, docs/features/F08-billing-period_de.md</files>
  <read_first>
    - docs/features/F07-reporting-balance.md (grep-Suche: gibt es bereits einen Abschnitt fuer Extra-Hours-Filter oder Balance-Chain-Konsumenten? Neue Note dort einreihen)
    - docs/features/F07-reporting-balance_de.md (Sprach-Parallelitaet)
    - docs/features/F08-billing-period.md (grep 'CURRENT_SNAPSHOT_SCHEMA_VERSION' und 'Bump-Historie' — die Note wird dort angebracht)
    - service_impl/src/billing_period_report.rs (CURRENT_SNAPSHOT_SCHEMA_VERSION Wert 12 — bestaetigen)
  </read_first>
  <action>
    F07 (EN + DE): am Ende eines geeigneten Abschnitts (z. B. "Balance chain
    inputs" / "Eingaben der Balance-Kette") einen narrative Note-Absatz ergaenzen:

      "Phase 54 (Milestone v2.6) introduces the extra_hours.source marker column
       (values: manual, rebooking). Read-aggregates in the Balance chain filter
       source='manual' from Phase 55 onwards; in Phase 54 no writer exists yet,
       so all existing rows continue to enter Balance identically (backfill via
       DEFAULT 'manual'). See feature F14 for the full rule set."

    Deutsche Fassung: strukturell gleich, wortgetreu uebersetzt.

    F08 (EN + DE): am Ende der Snapshot-Version-Bump-Historie einen Punkt ergaenzen:

      "Milestone v2.6 Phase 54: CURRENT_SNAPSHOT_SCHEMA_VERSION remains 12.
       Rationale: Phase 54 adds only the extra_hours.source marker column and
       two new rebooking_batch tables; neither introduces a new persisted
       BillingPeriodValueType, nor changes an existing computation.
       The Snapshot-Bump decision 12->13 is deferred to Phase 56 REB-AUTO-05
       (Milestone v2.6 F4-Cron) — see feature F14 and REQUIREMENTS.md."

    Deutsche Fassung: analog. Kein Bump in Version-Tabellen — nur die
    Bestaetigungs-Note.

    Fuer die spaetere Sichtbarkeit fuegt jede der 4 Files zusaetzlich einen
    "Related feature" Verweis auf F14 hinzu (Cross-Ref).
  </action>
  <verify>
    <automated>grep -q 'F14' docs/features/F07-reporting-balance.md; grep -q 'F14' docs/features/F07-reporting-balance_de.md; grep -q 'F14' docs/features/F08-billing-period.md; grep -q 'F14' docs/features/F08-billing-period_de.md; grep -q 'Phase 54' docs/features/F08-billing-period.md</automated>
  </verify>
  <done>
    F07 + F08 (beide Sprachen) tragen die Note zum Marker-Filter bzw. Snapshot-12-Bestaetigung; grep bestaetigt F14-Cross-Ref in allen 4 Files.
  </done>
</task>

<task type="auto">
  <name>Task 3: UPDATE docs/architecture/02-service-tiers + service-graph-runtime.mmd</name>
  <files>docs/architecture/02-service-tiers.md, docs/architecture/02-service-tiers_de.md, docs/architecture/diagrams/service-graph-runtime.mmd</files>
  <read_first>
    - docs/architecture/02-service-tiers.md (Service-Tier-Tabellen — Basic vs. BL, alphabetische Reihenfolge)
    - docs/architecture/02-service-tiers_de.md
    - docs/architecture/diagrams/service-graph-runtime.mmd (aktuelle Nodes + Edges — Format Mermaid)
  </read_first>
  <action>
    (a) 02-service-tiers.md (EN):
        - In der Basic-Services-Liste alphabetisch RebookingBatchService einfuegen
          mit Beschreibung "Entity-Manager for rebooking_batch (v2.6 Phase 54);
          consumers arrive in Phase 55 (RebookingReconciliationService)."
        - In der Business-Logic-Services-Liste alphabetisch VoluntaryStatsService
          einfuegen mit Beschreibung "Read-only F1/F2 aggregate on top of
          ExtraHoursService + EmployeeWorkDetailsService + SalesPersonService;
          HR-only via API-level None-redaction."
        - Referenz auf F14 als "Related feature".

    (b) 02-service-tiers_de.md: gleiche Struktur, deutsche Formulierung.

    (c) service-graph-runtime.mmd (Mermaid): Ergaenze zwei neue Nodes und ihre
        Edges:

          RebookingBatchService: Basic-Cluster, mit Dep-Edges zu RebookingBatchDao,
            PermissionService, TransactionDao, UuidService, ClockService.

          VoluntaryStatsService: BL-Cluster, mit Dep-Edges zu ExtraHoursService,
            EmployeeWorkDetailsService, SalesPersonService, PermissionService,
            TransactionDao.

        Node-Konvention nach dem bestehenden Muster (grep bestehende Nodes,
        z. B. WeekStatusService, uebernehmen).

        Kein neuer Konsument fuer die beiden Services in Phase 54 — daher keine
        eingehenden Edges (nur DI-Wiring in shifty_bin).
  </action>
  <verify>
    <automated>grep -q 'RebookingBatchService' docs/architecture/02-service-tiers.md; grep -q 'VoluntaryStatsService' docs/architecture/02-service-tiers.md; grep -q 'RebookingBatchService' docs/architecture/diagrams/service-graph-runtime.mmd; grep -q 'VoluntaryStatsService' docs/architecture/diagrams/service-graph-runtime.mmd</automated>
  </verify>
  <done>
    Beide neue Services + korrekte Deps im Runtime-Graph; Tabellen in 02-service-tiers.md + _de.md um beide Services erweitert; Basic vs. BL richtig zugeordnet.
  </done>
</task>

<task type="auto">
  <name>Task 4: UPDATE docs/architecture/03-data-model + db-schema-er.mmd</name>
  <files>docs/architecture/03-data-model.md, docs/architecture/03-data-model_de.md, docs/architecture/diagrams/db-schema-er.mmd</files>
  <read_first>
    - docs/architecture/03-data-model.md (Tabellen-Sektion — grep 'week_status' fuer Vorlage einer neu-hinzugefuegten Tabelle inkl. Column-Liste + Constraint-Beschreibung)
    - docs/architecture/03-data-model_de.md
    - docs/architecture/diagrams/db-schema-er.mmd (Mermaid ER-Diagramm — grep bestehende Nodes, z. B. week_status)
    - migrations/sqlite/20260707000000_create-rebooking-batch.sql (aus Plan 01)
    - migrations/sqlite/20260707000001_add-source-column-to-extra-hours.sql (aus Plan 01)
    - migrations/sqlite/20260707000002_seed-voluntary-rebooking-toggle.sql (aus Plan 01)
  </read_first>
  <action>
    (a) 03-data-model.md (EN):
        - Neue Subsection "rebooking_batch (Phase 54, v2.6)": Column-Liste (id BLOB PK,
          sales_person_id BLOB, iso_year INT, iso_week INT, kind TEXT, state TEXT,
          created TEXT, approved TEXT?, approved_by TEXT?, deleted TEXT?, update_process,
          update_version). Beschreibe D-54-DM-01 (globaler UNIQUE-Partial-Index
          WHERE deleted IS NULL) explizit.
        - Neue Subsection "rebooking_batch_entry (Phase 54, v2.6)": Column-Liste
          (id, batch_id FK, sales_person_id, hours, balance_before, voluntary_actual,
          voluntary_committed, extra_hours_out_id?, extra_hours_in_id?, created,
          deleted?, update_process, update_version).
        - Bestehende extra_hours-Sektion um Zeile "source TEXT NOT NULL DEFAULT 'manual'
          (Phase 54, v2.6 — values: manual | rebooking; see D-54-DM-02 / feature F14)".
        - toggle-Seed voluntary_rebooking_auto_active_from (Phase 54 gesaet, wirkt ab Phase 56).

    (b) 03-data-model_de.md: gleiche Struktur, deutsche Formulierung.

    (c) db-schema-er.mmd (Mermaid ER):
        - Zwei neue Entities: rebooking_batch, rebooking_batch_entry.
        - Relation rebooking_batch_entry.batch_id --> rebooking_batch.id (many-to-one).
        - Bestehende extra_hours-Node um ein "+source TEXT" Feld erweitern (falls die
          .mmd Attribute pro Entity zeigt).
  </action>
  <verify>
    <automated>grep -q 'rebooking_batch' docs/architecture/03-data-model.md; grep -q 'rebooking_batch_entry' docs/architecture/03-data-model.md; grep -q 'source' docs/architecture/03-data-model.md; grep -q 'rebooking_batch' docs/architecture/diagrams/db-schema-er.mmd</automated>
  </verify>
  <done>
    Beide Tabellen + source-Spalte + Toggle-Seed dokumentiert; ER-Diagramm zeigt neue Nodes + Relation; deutsche Fassung strukturell identisch.
  </done>
</task>

<task type="auto">
  <name>Task 5: Docs-Freshness-Trigger-Sanity + Phase-Wrap-Commit</name>
  <files>.planning/phases/54-data-model-voluntary-stats/</files>
  <read_first>
    - CLAUDE.md §"Docs-Freshness-Gate" (Trigger-Tabelle)
    - .planning/phases/54-data-model-voluntary-stats/54-VALIDATION.md (Row 54-06-01)
  </read_first>
  <action>
    Verifiziere per grep, dass jeder Trigger aus der CLAUDE.md-Trigger-Tabelle behandelt ist:

      - Trigger "migrations/sqlite/*.sql" -> 03-data-model + db-schema-er.mmd (Task 4).
      - Trigger "service/**/*.rs (neues Trait)" -> 02-service-tiers + service-graph-runtime.mmd (Task 3).
      - Trigger "dao/**/*.rs (neues Trait)" -> 03-data-model (Task 4, implizit ueber Tabellen-Doku).
      - Trigger "service_impl/billing_period_report.rs" -> KEINE Aenderung in Phase 54 (Version bleibt 12), aber die Note in F08 dokumentiert das Non-Bump-Rationale (Task 2).

    Sanity-Command-Sequenz:
      grep -l "F14" docs/features/       (mindestens 4 Files: F07 EN+DE, F08 EN+DE, F14 EN+DE, README EN+DE = 8)
      grep -l "RebookingBatchService" docs/architecture/   (mind. 2)
      grep -l "voluntary-stats" docs/                       (mind. F14 EN + DE)

    Wenn Sanity fehlschlaegt: fix in Task 1-4, nicht per Silent-Skip.

    Aktualisiere 54-VALIDATION.md Row 54-06-01 auf "✅ green" mit dem
    grep-Command als "automated" Feld.
  </action>
  <verify>
    <automated>test $(grep -rl 'F14' docs/features/ | wc -l) -ge 8; test $(grep -rl 'RebookingBatchService' docs/architecture/ | wc -l) -ge 2; grep -rq 'voluntary-stats' docs/features/</automated>
  </verify>
  <done>
    Alle Trigger aus CLAUDE.md-Docs-Freshness-Tabelle sind behandelt; Sanity-grep-Command bestaetigt Cross-Ref; 54-VALIDATION.md Row 54-06-01 auf green gesetzt.
  </done>
</task>

</tasks>

<verification>
- 2 neue Docs-Files (F14 EN + DE) mit strukturgleicher H1/H2-Anordnung
- Feature-Index in README + README_de.md um F14 ergaenzt
- F07 + F08 (beide Sprachen) tragen Marker-Filter- + Snapshot-12-Bestaetigungs-Note plus F14-Cross-Ref
- 02-service-tiers + service-graph-runtime.mmd enthalten RebookingBatchService (Basic) + VoluntaryStatsService (BL)
- 03-data-model + db-schema-er.mmd enthalten rebooking_batch + rebooking_batch_entry + extra_hours.source
- Sanity-grep-Kommandos gruen
- Docs-Freshness-Gate erfuellt (MEMORY feedback_docs_always_current_no_followup)
</verification>

<success_criteria>
- Milestone-Close-Audit (siehe .planning/PROJECT.md) findet keine Docs-Drift fuer Phase 54.
- Sprach-Parallelitaet EN + DE gewahrt: gleiche H1/H2, gleiche Diagramm-Referenzen.
- Kein Snapshot-Version-Bump dokumentiert; Version 12 bleibt CURRENT.
</success_criteria>

<output>
Erstelle `.planning/phases/54-data-model-voluntary-stats/54-06-SUMMARY.md` mit:
- Liste aller 14 geaenderten Files.
- Auszug der neuen F14-H1/H2-Struktur (EN).
- Diff-Snippet des Runtime-Graph-Updates (Mermaid).
- Sanity-grep-Ergebnisse.
Aktualisiere 54-VALIDATION.md Row 54-06-01 auf ✅ green.
</output>
