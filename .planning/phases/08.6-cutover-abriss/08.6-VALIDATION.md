---
phase: 8.6
slug: cutover-abriss
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-11
---

# Phase 8.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> **Subtraktive Phase:** Die Validierung ist primär das Green-Build / No-Dead-Code-Gate (D-09)
> plus Negative-Space-Asserts (entfernte Symbole verschwunden, Überlebende noch verdrahtet).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in (`#[tokio::test]`, `mockall`) + dioxus-ssr Snapshot-Tests |
| **Config file** | none — bestehende Cargo-Workspaces (Backend-Root + `shifty-dioxus/`) |
| **Quick run command** | `cargo check --workspace` |
| **Full suite command** | `cargo test --workspace` (Backend) + `cargo test` in `shifty-dioxus/` + `cargo build --target wasm32-unknown-unknown` (in `shifty-dioxus/`, via `nix develop`) |
| **Estimated runtime** | ~120–240 seconds (inkl. WASM-Build) |

---

## Sampling Rate

- **After every task commit:** Run `cargo check --workspace` (Dead-Code-Detektor — autoritativ, nicht rust-analyzer)
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite + WASM-Build müssen grün sein, `cargo check --workspace` ohne unused/dead-code-Warnungen auf entfernte Symbole
- **Max feedback latency:** ~240 seconds

---

## Per-Task Verification Map

> Subtraktive Phase — Verifikation ist Build/Test-grün + Negative-Space-Grep statt neuer Unit-Tests.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| backend-abriss | — | — | SC2 | — | Cutover-Endpoints mit `cutover_admin`-Gate entfernt (V4) | grep + build | `grep -r "CutoverService\|CutoverDao" --include=*.rs` → 0 (außer Kommentar); `cargo check --workspace` | ✅ | ⬜ pending |
| flag-removal | — | — | SC4 | — | N/A | grep + build | `grep -r "absence_range_source_active" --include=*.rs` → 0; `cargo check --workspace` | ✅ | ⬜ pending |
| migration-drops | — | — | SC3 | — | N/A | migration | DB-Migrate grün; `absence_period_migration_source` bleibt | ✅ | ⬜ pending |
| frontend-abriss | — | — | SC1 | — | N/A | grep + build | `grep -r "cutover\|AdminCutover" shifty-dioxus/src` → nur erlaubte Survivor; `cargo build --target wasm32-unknown-unknown`; `cargo test -p shifty-dioxus` | ✅ | ⬜ pending |
| openapi-surface | — | — | SC5 | — | N/A | unit | `cargo test -p rest -- openapi` grün; `cargo check --workspace` ohne dead-code-Warnung | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Plan/Wave-Spalten werden vom Planner befüllt; Task-IDs hier sind die SC-Validierungs-Anker.*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase validations.* Keine neuen Test-Dateien — alle Verifikationen
führen auf bestehende `cargo check` / `cargo test` / WASM-Build-Kommandos zurück.

Hinweis (aus RESEARCH §Runtime State Inventory): Nach DAO-Deletion ggf. `cargo sqlx prepare --workspace`
ausführen, damit der `.sqlx/`-Offline-Cache keine verwaisten Query-Fingerprints für gedroppte Tabellen behält.

---

## Negative-Space Verifications (Deletion-Phase-spezifisch)

| Assert | Erwartung | Kommando |
|--------|-----------|----------|
| Kein Cutover-Backend-Code | 0 Treffer | `grep -r "CutoverService\|CutoverDao\|detect_weekly_lump_sum" --include=*.rs .` |
| Kein Cutover-Frontend-Modul | 0 Treffer | `grep -r "page::cutover\|service::cutover\|state::cutover\|NavTarget::AdminCutover" shifty-dioxus/src` |
| Flag vollständig weg | 0 Treffer (außer gelöschten SQL-Kommentaren) | `grep -r "absence_range_source_active" --include=*.rs` |
| `CarryoverRebuildService` weg (RESEARCH-Fund) | 0 Treffer | `grep -r "CarryoverRebuildService" --include=*.rs` |
| Absences-Gate auf `logged_in` | `absences: logged_in` (kein `cutover_active`) | `grep -n "absences:" shifty-dioxus/src/component/top_bar.rs` |
| **Survivor:** `add_vacation` lebt | Treffer in `extra_hours_modal.rs` | `grep -rn "add_vacation" shifty-dioxus/src/` |
| **Survivor:** `/feature-flag/{key}` registriert | Treffer | `grep -rn "feature_flag" shifty_bin/src/main.rs rest/src/lib.rs` |
| **Survivor:** `absence_period_migration_source` bleibt | Treffer in DAO + AbsenceConversionService | `grep -r "MigrationSourceDao\|absence_period_migration_source" dao/src/ service_impl/src/absence_conversion.rs` |

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tabellen-Drops auf laufender DB | SC3 | Migration-Apply ist Laufzeit-Effekt, nicht compile-time prüfbar | `nix develop` → `sqlx migrate run` gegen Dev-DB; `sqlite3 <db> ".tables"` zeigt `absence_migration_quarantine` + `employee_yearly_carryover_pre_cutover_backup` NICHT mehr, `absence_period_migration_source` weiterhin vorhanden |

*Alle übrigen Phasen-Behaviors haben automatisierte (grep/build/test) Verifikation.*

---

## Validation Sign-Off

- [ ] Alle Tasks haben automatisierte Verify (grep/build/test) oder eine Manual-Only-Begründung
- [ ] Sampling-Kontinuität: kein Wave ohne `cargo check --workspace` + `cargo test --workspace`
- [ ] Wave 0 deckt alle fehlenden Referenzen (hier: keine — bestehende Infra reicht)
- [ ] Kein watch-mode-Flag in Verify-Kommandos
- [ ] Feedback-Latenz < 240s
- [ ] `nyquist_compliant: true` im Frontmatter gesetzt (nach Planner-Review)

**Approval:** pending
