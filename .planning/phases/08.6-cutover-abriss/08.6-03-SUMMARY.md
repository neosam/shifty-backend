---
phase: 08.6-cutover-abriss
plan: "03"
subsystem: database-migrations
tags: [migrations, sqlx, schema-cleanup, forward-only, cutover-abriss]
dependency_graph:
  requires: ["08.6-01"]
  provides: ["SC3-drop-migrations", "SC4-seed-delete", "clean-sqlx-cache"]
  affects: [".sqlx/", "localdb.sqlite3"]
tech_stack:
  added: []
  patterns: ["forward-only DROP TABLE IF EXISTS", "cargo sqlx prepare --workspace", "DELETE seed migration"]
key_files:
  created:
    - migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql
    - migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql
    - migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql
  modified:
    - .sqlx/ (9 orphaned cutover fingerprints removed, 2 new hashes added)
decisions:
  - "forward-only migrations (no .down.sql) per D-05; INT DB is wipeable, Prod never populated"
  - "absence_period_migration_source NOT dropped — living 8.5 AbsenceConversionService backlink"
  - "feature_flag table preserved as generic mechanism; only absence_range_source_active seed row removed"
  - "cargo sqlx prepare --workspace with DATABASE_URL pointing to post-migration localdb.sqlite3"
metrics:
  duration: "~5 minutes"
  completed_date: "2026-06-11"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 13
---

# Phase 08.6 Plan 03: Forward-only Drop-Migrations + .sqlx Regeneration Summary

Forward-only SQL migrations drop two dead cutover tables and remove the `absence_range_source_active` seed row; `.sqlx` offline cache regenerated with zero orphaned fingerprints for dropped tables.

## Tasks Completed

| Task | Name | Commit | Key Files |
|------|------|--------|-----------|
| 1 | Drei forward-only Drop-/Delete-Migrations anlegen | `79b15dc5` | 3 new `.sql` migration files |
| 2 | Migrationen anwenden + .sqlx-Cache regenerieren | `53380793` | `.sqlx/` (9 deleted, 2 added) |

## What Was Built

### Task 1: Three Forward-only Migrations

Three migration files created with correct `20260611` timestamps, no `.down.sql` counterparts (D-05):

- `20260611000000_drop-absence-migration-quarantine.sql` — `DROP TABLE IF EXISTS absence_migration_quarantine`
- `20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql` — `DROP TABLE IF EXISTS employee_yearly_carryover_pre_cutover_backup`
- `20260611000002_delete-absence-range-source-active-seed.sql` — `DELETE FROM feature_flag WHERE key = 'absence_range_source_active'`

Comments follow the Phase 8.5 analog pattern (Phase reference, rationale, Prod/INT fact). `absence_period_migration_source` was explicitly NOT dropped (living 8.5 `AbsenceConversionService` backlink).

### Task 2: Migration Application + .sqlx Cache Regeneration

Applied via `nix develop --command sqlx migrate run --source migrations/sqlite` (additive — no `database reset`):
- `20260611000000/migrate` applied in 4.22ms
- `20260611000001/migrate` applied in 1.97ms
- `20260611000002/migrate` applied in 1.23ms

Regenerated `.sqlx/` via `DATABASE_URL=sqlite:./localdb.sqlite3 nix develop --command cargo sqlx prepare --workspace`:
- 9 old query files removed (included the 8 cutover DAO sqlx queries from `dao_impl_sqlite/src/cutover.rs` deleted in Plan 08.6-01, plus 1 query that got a new hash)
- 2 new query files added

`grep -rl "absence_migration_quarantine\|employee_yearly_carryover_pre_cutover_backup" .sqlx/` → 0 matches.

`SQLX_OFFLINE=true cargo check --workspace` → `Finished dev profile` (clean, no warnings on removed symbols).

## Verification Results

| Check | Result |
|-------|--------|
| `absence_migration_quarantine` in `.tables` | NOT present (dropped) |
| `employee_yearly_carryover_pre_cutover_backup` in `.tables` | NOT present (dropped) |
| `absence_period_migration_source` in `.tables` | PRESENT (survivor) |
| `feature_flag` in `.tables` | PRESENT (survivor) |
| Orphaned fingerprints in `.sqlx/` | 0 matches |
| `cargo check --workspace` (SQLX_OFFLINE=true) | green |
| No `.down.sql` files for new migrations | confirmed |
| `absence_period_migration_source` not in new drop migrations | confirmed (0 grep hits) |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None. This plan is purely subtractive (migrations + cache regeneration). No UI stubs introduced.

## Threat Flags

None. This plan only removes dead schema (`DROP TABLE IF EXISTS` on never-populated tables). No new attack surface introduced. The `DELETE FROM feature_flag` only removes the `absence_range_source_active` key; the `feature_flag` table and mechanism remain intact.

## Self-Check: PASSED

- `migrations/sqlite/20260611000000_drop-absence-migration-quarantine.sql` — FOUND
- `migrations/sqlite/20260611000001_drop-employee-yearly-carryover-pre-cutover-backup.sql` — FOUND
- `migrations/sqlite/20260611000002_delete-absence-range-source-active-seed.sql` — FOUND
- Task 1 commit `79b15dc5` — FOUND
- Task 2 commit `53380793` — FOUND
- `.sqlx/` has 0 orphaned fingerprints for dropped tables — VERIFIED
- `cargo check --workspace` (SQLX_OFFLINE=true) — PASSED
