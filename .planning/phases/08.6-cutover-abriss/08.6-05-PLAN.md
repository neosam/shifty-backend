---
phase: 08.6-cutover-abriss
plan: 05
type: execute
wave: 3
depends_on: ["08.6-01", "08.6-02", "08.6-03", "08.6-04"]
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC5: cargo test --workspace + WASM-Build + cargo check --workspace grün ohne Dead-Code-Warnungen auf entfernte Symbole"
    - "Alle Negative-Space-Asserts (kein Cutover-Code) erfüllt"
    - "Alle Survivor-Asserts (add_vacation, /feature-flag, MigrationSourceDao, absence_period_migration_source) erfüllt"
    - "MUST-NOT: CURRENT_SNAPSHOT_SCHEMA_VERSION unverändert (D-10)"
  artifacts: []
  key_links:
    - from: "Verifikations-Gate"
      to: "Gesamt-Phase 8.6"
      via: "cargo test/check workspace + WASM-Build nach kompletter Subtraktion"
      pattern: "cargo check --workspace"
---

<objective>
End-of-Phase-Verifikations-Gate (D-09): nach Abschluss aller Subtraktions-Plans (01–04) den gesamten Workspace + Frontend + WASM-Build prüfen und alle Negative-Space-/Survivor-Asserts aus VALIDATION.md ausführen. Dieser Plan baut KEINEN Code — er ist das finale Green-Build-/No-Dead-Code-Tor.

Purpose: SC5 + D-09 — beweisen, dass der gesamte Cutover-Abriss sauber ist (grüner Build, kein toter Code, alle Survivor intakt).
Output: Bestätigung, dass alle 5 Success Criteria erfüllt sind; SUMMARY mit Assert-Tabelle.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.6-cutover-abriss/08.6-CONTEXT.md
@.planning/phases/08.6-cutover-abriss/08.6-VALIDATION.md
@.planning/phases/08.6-cutover-abriss/08.6-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Green-Build-Gate (Backend + Frontend + WASM)</name>
  <files>(reine Verifikation — keine Datei-Änderung)</files>
  <read_first>
    - 08.6-VALIDATION.md § "Per-Task Verification Map" + "Negative-Space Verifications"
    - CLAUDE.local.md (NixOS: nix develop für WASM; cargo check --workspace autoritativ, nicht rust-analyzer)
  </read_first>
  <action>
    Folgende Gates in dieser Reihenfolge ausführen und Ergebnisse sammeln:

    1. `cargo check --workspace` — MUSS grün sein UND keine `unused`/`dead_code`-Warnung auf entfernte
       Cutover/CarryoverRebuild-Symbole zeigen (autoritativer Dead-Code-Detektor, NICHT rust-analyzer).
    2. `cargo test --workspace` — alle Backend-Tests grün (Cutover-Tests sind entfernt; feature_flag-Tests
       re-keyed; absence_conversion-Tests unangetastet grün).
    3. `cd shifty-dioxus && cargo test` — Frontend-Tests grün (top_bar-Tests angepasst, i18n-Cutover-Tests entfernt).
    4. `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` — WASM-Gate exit 0.

    Falls eine dieser Stufen rot ist: in der SUMMARY präzise dokumentieren (welche Datei, welcher Fehler) und
    NICHT als grün markieren. Bei Dead-Code-Warnung auf entfernte Symbole: zurück an den jeweiligen Plan
    (meist verbleibende mod-Decl oder Import).
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -Ei "warning: unused|dead_code|^error" | grep -iE "cutover|carryover_rebuild" | head; echo "check-done"; cargo test --workspace 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --workspace` exit 0, keine warning/error-Zeile mit "cutover" oder "carryover_rebuild"
    - `cargo test --workspace` → "test result: ok" für alle Suites, 0 failures
    - `cd shifty-dioxus && cargo test` → 0 failures
    - `cd shifty-dioxus && nix develop --command cargo build --target wasm32-unknown-unknown` exit 0
  </acceptance_criteria>
  <done>Backend + Frontend Tests grün; WASM-Build exit 0; cargo check --workspace ohne Dead-Code-Warnung auf entfernte Symbole.</done>
</task>

<task type="auto">
  <name>Task 2: Negative-Space- + Survivor-Asserts (VALIDATION.md)</name>
  <files>(reine Verifikation — keine Datei-Änderung)</files>
  <read_first>
    - 08.6-VALIDATION.md § "Negative-Space Verifications" (vollständige Assert-Tabelle)
    - 08.6-RESEARCH.md § "Validation Architecture" (SC-zu-Validierungsmatrix)
  </read_first>
  <action>
    Alle Negative-Space- und Survivor-Asserts aus VALIDATION.md ausführen und in einer Tabelle in der
    SUMMARY festhalten. Grep-Hygiene: gelöschte/auskommentierte SQL-Kommentare oder erlaubte Kommentar-Header
    (z.B. in absence_conversion.rs) sind keine Verstöße — mit `grep -v '//'` Kommentare ausfiltern, wo nötig.

    Negative-Space (Erwartung: 0 echte Code-Treffer):
    - `grep -rn "CutoverService\|CutoverDao\|detect_weekly_lump_sum\|CarryoverRebuildService" --include=*.rs . | grep -v target/` → 0 (außer erlaubter Kommentar)
    - `grep -rn "page::cutover\|service::cutover\|state::cutover\|NavTarget::AdminCutover" shifty-dioxus/src/` → 0
    - `grep -rn "absence_range_source_active" --include=*.rs .` → 0 (gelöschte SQL-Kommentare ausgenommen)
    - `grep -n "absences:" shifty-dioxus/src/component/top_bar.rs` → "absences: logged_in"

    Survivor (Erwartung: Treffer an bekannten Stellen):
    - `grep -rn "add_vacation" shifty-dioxus/src/` → Treffer in api.rs + extra_hours_modal.rs
    - `grep -rn "feature_flag" shifty_bin/src/main.rs rest/src/lib.rs` → Treffer
    - `grep -r "MigrationSourceDao\|absence_period_migration_source" dao/src/ service_impl/src/absence_conversion.rs` → Treffer

    MUST-NOT (D-10):
    - `grep -n "CURRENT_SNAPSHOT_SCHEMA_VERSION" service_impl/src/billing_period_report.rs` zeigt den
      unveränderten Wert; in der SUMMARY explizit bestätigen, dass D-10 (kein Bump) eingehalten ist.
  </action>
  <verify>
    <automated>grep -rn "CutoverService\|CutoverDao\|CarryoverRebuildService\|detect_weekly_lump_sum" --include=*.rs service/ service_impl/ dao/ dao_impl_sqlite/ rest/ rest-types/ shifty_bin/ 2>/dev/null | grep -v "//"; echo "neg-done"; grep -rn "add_vacation" shifty-dioxus/src/ | head -2</automated>
  </verify>
  <acceptance_criteria>
    - Negative-Space-Greps liefern 0 echte Code-Treffer (Kommentare ausgefiltert)
    - `grep -n "absences:" shifty-dioxus/src/component/top_bar.rs` → "absences: logged_in"
    - Survivor-Greps (add_vacation, feature_flag, MigrationSourceDao, absence_period_migration_source) liefern Treffer
    - CURRENT_SNAPSHOT_SCHEMA_VERSION unverändert (D-10 bestätigt)
    - SUMMARY enthält die vollständige Assert-Tabelle mit Pass/Fail pro Zeile
  </acceptance_criteria>
  <done>Alle Negative-Space- + Survivor-Asserts grün; D-10 bestätigt; Phase 8.6 SC1–SC5 erfüllt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verifikations-Gate | Read-only — führt nur Build/Test/Grep aus, ändert keinen Code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-08 | I (Disclosure) | Gesamt-Surface nach Abriss | accept | Verifikation bestätigt, dass alle Cutover-Endpoints (cutover_admin-gated) entfernt sind und der autoritative Daten-Boundary (rest/src/absence.rs hr ∨ self) unverändert bleibt. Subtraktiv → Angriffsfläche gesunken, keine neue eingeführt. |

**Subtraktive Posture:** Read-only-Gate. Keine neue Angriffsfläche. Keine HIGH-severity-Threats.
</threat_model>

<verification>
- `cargo check --workspace` + `cargo test --workspace` grün, kein Dead-Code-Warning auf entfernte Symbole
- `cd shifty-dioxus && cargo test` grün; WASM-Build exit 0
- Alle Negative-Space- + Survivor-Asserts aus VALIDATION.md grün
- D-10: CURRENT_SNAPSHOT_SCHEMA_VERSION unverändert
</verification>

<success_criteria>
- SC5: vollständiges Green-Build-/No-Dead-Code-Gate grün
- SC1–SC4 indirekt bestätigt durch Negative-Space-/Survivor-Asserts
- D-09 + D-10 eingehalten
</success_criteria>

<output>
After completion, create `.planning/phases/08.6-cutover-abriss/08.6-05-SUMMARY.md`
</output>
