---
phase: 08.6-cutover-abriss
plan: 04
type: execute
wave: 2
depends_on: ["08.6-01"]
files_modified:
  - rest/tests/openapi_surface.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC5: OpenAPI-Surface-Test enthält keine Cutover-Pfade/-Schemas mehr"
    - "D-08: /feature-flag/{key} + FeatureFlagTO bleiben in der erwarteten Surface"
    - "cargo test -p rest -- openapi grün (kein Drift zwischen ApiDoc und Expected-Listen)"
  artifacts:
    - path: "rest/tests/openapi_surface.rs"
      provides: "EXPECTED_PATHS/EXPECTED_SCHEMAS ohne Cutover-Einträge"
      contains: "/feature-flag/{key}"
  key_links:
    - from: "rest/tests/openapi_surface.rs (EXPECTED_PATHS/EXPECTED_SCHEMAS)"
      to: "ApiDoc::openapi() Surface (nach Cutover-Removal in Plan 01)"
      via: "assert! auf paths.keys()/schema-Namen"
      pattern: "EXPECTED_PATHS"
---

<objective>
Den OpenAPI-Surface-Test (`rest/tests/openapi_surface.rs`) mit der nach Plan 01 verkleinerten API-Surface synchronisieren: die 4 Cutover-Pfade + 8 Cutover-Schemas aus `EXPECTED_PATHS`/`EXPECTED_SCHEMAS` entfernen. Der generische `/feature-flag/{key}`-Pfad + `FeatureFlagTO` bleiben (D-08).

Purpose: SC5 — Surface-Test grün halten, nachdem die Cutover-Endpoints aus dem ApiDoc verschwunden sind.
Output: `cargo test -p rest -- openapi` grün; kein Drift zwischen tatsächlicher und erwarteter Surface.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08.6-cutover-abriss/08.6-CONTEXT.md
@.planning/phases/08.6-cutover-abriss/08.6-RESEARCH.md
@.planning/phases/08.6-cutover-abriss/08.6-PATTERNS.md

<interfaces>
<!-- Exakte zu entfernende Einträge (RESEARCH § 5.5, PATTERNS § 3). -->

EXPECTED_PATHS — entfernen:
  "/admin/cutover/profile", "/admin/cutover/commit",
  "/admin/cutover/convert-quarantine-entry", "/admin/cutover/bulk-convert-quarantine-rows"
EXPECTED_SCHEMAS — entfernen:
  "CutoverProfileTO", "CutoverQuarantineEntryTO", "CutoverConvertQuarantineEntryRequest",
  "CutoverConvertQuarantineEntryResponse", "CutoverBulkConvertQuarantineRowsRequest",
  "CutoverBulkConvertQuarantineRowsResponse", "CutoverConvertErrorTO", "ManualRangeTO"
BLEIBEN (D-08): "/feature-flag/{key}", "FeatureFlagTO"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cutover-Pfade + -Schemas aus dem Surface-Test entfernen</name>
  <files>rest/tests/openapi_surface.rs</files>
  <read_first>
    - rest/tests/openapi_surface.rs (vollständig — EXPECTED_PATHS ~Z. 48–92, EXPECTED_SCHEMAS ~Z. 100–147, Cutover-Kommentar-Blöcke)
    - 08.6-RESEARCH.md § 5.5 (exakte Einträge)
    - 08.6-PATTERNS.md § 3 (Muster + Survivor-Einträge)
  </read_first>
  <action>
    In rest/tests/openapi_surface.rs:

    Aus EXPECTED_PATHS diese 4 &str-Literale ENTFERNEN:
      "/admin/cutover/profile", "/admin/cutover/commit",
      "/admin/cutover/convert-quarantine-entry", "/admin/cutover/bulk-convert-quarantine-rows"
    Falls weitere Cutover-Pfade (z.B. "/admin/cutover/gate-dry-run") in der Liste stehen, diese ebenfalls
    entfernen — Maxime: alle "/admin/cutover/*"-Pfade raus.

    Aus EXPECTED_SCHEMAS diese 8 Schema-Namen ENTFERNEN:
      "CutoverProfileTO", "CutoverQuarantineEntryTO", "CutoverConvertQuarantineEntryRequest",
      "CutoverConvertQuarantineEntryResponse", "CutoverBulkConvertQuarantineRowsRequest",
      "CutoverBulkConvertQuarantineRowsResponse", "CutoverConvertErrorTO", "ManualRangeTO"
    Falls weitere Cutover*TO-Schemas (z.B. CutoverRunResultTO, CutoverGateDriftReportTO,
    CutoverGateDriftRowTO, CutoverProfileBucketTO) in der Liste stehen, ebenfalls entfernen.

    Die zugehörigen Kommentar-Blöcke (`// Cutover admin...`, `// Cutover convert (Phase 8.1)`,
    `// Cutover.`, `// Cutover (Plan 08-08)...`, `// Cutover convert DTOs...`, `// Cutover manual-range...`)
    ebenfalls entfernen.

    BEHALTEN (D-08): "/feature-flag/{key}" in EXPECTED_PATHS und "FeatureFlagTO" in EXPECTED_SCHEMAS.
    Falls es in der Liste einen Tag-Namen-Block gibt, der einen Cutover-Tag enthält, diesen ebenfalls entfernen.
  </action>
  <verify>
    <automated>cargo test -p rest -- openapi 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cutover\|Cutover\|ManualRangeTO" rest/tests/openapi_surface.rs` → 0
    - `grep -c "/feature-flag/{key}" rest/tests/openapi_surface.rs` → ≥1 (Survivor)
    - `grep -c "FeatureFlagTO" rest/tests/openapi_surface.rs` → ≥1 (Survivor)
    - `cargo test -p rest -- openapi` grün (kein Surface-Drift-Failure)
  </acceptance_criteria>
  <done>Surface-Test enthält keine Cutover-Einträge mehr; /feature-flag/{key} + FeatureFlagTO bleiben; Test grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenAPI-Surface-Test → ApiDoc | Drift-Detection; rein Test-seitig, kein Laufzeit-Boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-07 | I (Disclosure) | OpenAPI-Surface | accept | Subtraktiv: Cutover-Schemas/-Pfade aus der dokumentierten Surface entfernt → kein Info-Leak über entfernte Admin-Endpoints. /feature-flag/{key} bleibt bewusst (auth-gated Survivor). |

**Subtraktive Posture:** Nur Test-Erwartungslisten werden verkleinert. Keine neue Angriffsfläche.
</threat_model>

<verification>
- `cargo test -p rest -- openapi` grün
- Negative-Space: `grep -c "cutover\|Cutover\|ManualRangeTO" rest/tests/openapi_surface.rs` → 0
- Survivor: `/feature-flag/{key}` + `FeatureFlagTO` weiterhin in den Expected-Listen
</verification>

<success_criteria>
- SC5: OpenAPI-Surface-Test ohne Cutover-Einträge, grün
- D-08: /feature-flag/{key} + FeatureFlagTO erhalten
</success_criteria>

<output>
After completion, create `.planning/phases/08.6-cutover-abriss/08.6-04-SUMMARY.md`
</output>
