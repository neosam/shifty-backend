---
phase: 08.6-cutover-abriss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service_impl/src/extra_hours.rs
  - service_impl/src/test/extra_hours.rs
  - service/src/cutover.rs
  - service_impl/src/cutover.rs
  - service_impl/src/test/cutover.rs
  - dao/src/cutover.rs
  - dao_impl_sqlite/src/cutover.rs
  - rest/src/cutover.rs
  - service/src/carryover_rebuild.rs
  - service_impl/src/carryover_rebuild.rs
  - service_impl/src/test/carryover_rebuild.rs
  - service/src/lib.rs
  - service_impl/src/lib.rs
  - service_impl/src/test/mod.rs
  - dao/src/lib.rs
  - dao_impl_sqlite/src/lib.rs
  - rest/src/lib.rs
  - rest-types/src/lib.rs
  - shifty_bin/src/main.rs
  - shifty_bin/src/integration_test.rs
  - shifty_bin/src/integration_test/cutover.rs
  - shifty_bin/src/integration_test/feature_flag.rs
  - service_impl/src/test/feature_flag.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "SC2: Backend hat keine Cutover-Handler/Service/DAO mehr"
    - "SC4: kein Reader/Writer des absence_range_source_active-Flags im Backend-Code"
    - "D-09: cargo check --workspace ohne Dead-Code-Warnungen auf entfernte Symbole"
    - "CUTOVER_ADMIN_PRIVILEGE lebt weiter als lokale Konstante in extra_hours.rs (soft_delete_bulk-Gate intakt)"
    - "AbsenceConversionService + MigrationSourceDao + generisches feature_flag-Subsystem bleiben verdrahtet"
    - "MUST-NOT: CURRENT_SNAPSHOT_SCHEMA_VERSION NICHT erhöht (D-10)"
  artifacts:
    - path: "service_impl/src/extra_hours.rs"
      provides: "lokale const CUTOVER_ADMIN_PRIVILEGE statt service::cutover-Import"
      contains: "const CUTOVER_ADMIN_PRIVILEGE"
    - path: "shifty_bin/src/main.rs"
      provides: "DI ohne CutoverService/CutoverDao/CarryoverRebuildService; migration_source_dao bleibt"
      contains: "let migration_source_dao"
  key_links:
    - from: "service_impl/src/absence_conversion.rs"
      to: "service_impl/src/extra_hours.rs (soft_delete_bulk)"
      via: "Authentication::Full bypass des CUTOVER_ADMIN_PRIVILEGE-Gates"
      pattern: "soft_delete_bulk"
    - from: "shifty_bin/src/main.rs"
      to: "AbsenceConversionServiceImpl"
      via: "migration_source_dao.clone()"
      pattern: "migration_source_dao"
---

<objective>
Backend-Abriss der Batch-Cutover-Maschinerie inklusive der inline-before-delete-Entkopplung und der CarryoverRebuildService-Mitentfernung. Alle 5 Cutover-Backend-Crates (rest/service/service_impl/dao/dao_impl_sqlite) werden gelöscht, die rest-types-Cutover-TOs entfernt, die DI in main.rs entkoppelt und die Backend-Cutover-Tests gelöscht.

Purpose: SC2 + SC4 (Backend) — tote Cutover-Wege ersatzlos entfernen, generische Survivor-Infra (feature_flag, AbsenceConversionService, MigrationSourceDao) intakt halten.
Output: Backend kompiliert ohne Cutover-Symbole; `cargo check --workspace` warnungsfrei auf entfernte Symbole.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08.6-cutover-abriss/08.6-CONTEXT.md
@.planning/phases/08.6-cutover-abriss/08.6-RESEARCH.md
@.planning/phases/08.6-cutover-abriss/08.6-PATTERNS.md
@.planning/phases/08.6-cutover-abriss/08.6-VALIDATION.md

<interfaces>
<!-- Survivor-Kontrakte, die NACH dem Abriss intakt bleiben müssen. Aus dem Live-Code extrahiert. -->

CUTOVER_ADMIN_PRIVILEGE — aktuell in service/src/cutover.rs:21:
  pub const CUTOVER_ADMIN_PRIVILEGE: &str = "cutover_admin";
  Konsumenten die NACH 8.6 leben: service_impl/src/extra_hours.rs:11+378, service_impl/src/test/extra_hours.rs:31+652+712.

main.rs Survivor-Block (Z. 1090–1106, UNVERÄNDERT lassen):
  let absence_conversion_service = Arc::new(
    AbsenceConversionServiceImpl { ..., migration_source_dao: migration_source_dao.clone(), ... });

Survivor (NICHT anfassen): rest/src/feature_flag.rs (generate_route bleibt im Router),
  service/src/feature_flag.rs, service_impl/src/feature_flag.rs, dao/src/feature_flag.rs,
  dao_impl_sqlite/src/feature_flag.rs, dao/src/migration_source.rs,
  dao_impl_sqlite/src/migration_source.rs, service_impl/src/absence_conversion.rs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CUTOVER_ADMIN_PRIVILEGE inlinen (inline-before-delete)</name>
  <files>service_impl/src/extra_hours.rs, service_impl/src/test/extra_hours.rs</files>
  <read_first>
    - service_impl/src/extra_hours.rs (Zeilen 1–20 use-Block, Zeile 358–396 soft_delete_bulk)
    - service_impl/src/test/extra_hours.rs (Zeile 31 Import, Zeile 640–720 Test-Verwendung)
    - service/src/cutover.rs Zeile 21 (Original-Definition — zur Bestätigung des String-Werts "cutover_admin")
    - 08.6-PATTERNS.md § 4 (exakte Inline-Vorlage)
  </read_first>
  <action>
    In service_impl/src/extra_hours.rs: Den Import `cutover::CUTOVER_ADMIN_PRIVILEGE,` aus dem
    `use service::{ ... }`-Block (Zeile 11) ENTFERNEN. Direkt nach den use-Blocks (vor dem ersten
    Struct/Impl) eine lokale Konstante einfügen:
    ```rust
    /// Privileg-Gate für `soft_delete_bulk`. Konstante aus `service::cutover`
    /// hierher umgezogen (Phase 8.6, D-01). String-Wert identisch — kein DB-Schema-Change.
    const CUTOVER_ADMIN_PRIVILEGE: &str = "cutover_admin";
    ```
    Die Verwendung in Zeile 378 (`.check_permission(CUTOVER_ADMIN_PRIVILEGE, context)`) bleibt unverändert.

    In service_impl/src/test/extra_hours.rs: Import `use service::cutover::CUTOVER_ADMIN_PRIVILEGE;`
    (Zeile 31) ENTFERNEN und durch lokale Konstante im Test-Modul ersetzen:
    ```rust
    const CUTOVER_ADMIN_PRIVILEGE: &str = "cutover_admin";
    ```
    Verwendungen in Zeile 652 + 712 (`.with(eq(CUTOVER_ADMIN_PRIVILEGE), always())`) bleiben unverändert.

    WICHTIG: Diese Task MUSS vor Task 2 (Löschen von service/src/cutover.rs) laufen, sonst bricht der Build.
  </action>
  <verify>
    <automated>cargo check -p service_impl 2>&1 | grep -qi "error" && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use service::cutover" service_impl/src/extra_hours.rs service_impl/src/test/extra_hours.rs` → 0 in beiden
    - `grep -c 'const CUTOVER_ADMIN_PRIVILEGE: &str = "cutover_admin"' service_impl/src/extra_hours.rs` → 1
    - `grep -c 'const CUTOVER_ADMIN_PRIVILEGE: &str = "cutover_admin"' service_impl/src/test/extra_hours.rs` → 1
    - `cargo check -p service_impl` zeigt keinen "unresolved import"-Fehler
  </acceptance_criteria>
  <done>extra_hours.rs (prod + test) definiert CUTOVER_ADMIN_PRIVILEGE lokal; kein service::cutover-Import mehr.</done>
</task>

<task type="auto">
  <name>Task 2: Cutover- + CarryoverRebuild-Backend-Dateien löschen + Modul-Decls entfernen</name>
  <files>service/src/cutover.rs, service_impl/src/cutover.rs, service_impl/src/test/cutover.rs, dao/src/cutover.rs, dao_impl_sqlite/src/cutover.rs, rest/src/cutover.rs, service/src/carryover_rebuild.rs, service_impl/src/carryover_rebuild.rs, service_impl/src/test/carryover_rebuild.rs, service/src/lib.rs, service_impl/src/lib.rs, service_impl/src/test/mod.rs, dao/src/lib.rs, dao_impl_sqlite/src/lib.rs, rest/src/lib.rs, rest-types/src/lib.rs, shifty_bin/src/integration_test.rs, shifty_bin/src/integration_test/cutover.rs</files>
  <read_first>
    - service/src/lib.rs, service_impl/src/lib.rs, dao/src/lib.rs, dao_impl_sqlite/src/lib.rs (mod-Deklarationen)
    - service_impl/src/test/mod.rs
    - rest/src/lib.rs (Z. 9, 378–381, 422, 536, 621 — RestStateDef + Router + ApiDoc)
    - rest-types/src/lib.rs (Cutover-TOs + From-Impls — per grep "Cutover\|ManualRangeTO" lokalisieren)
    - 08.6-RESEARCH.md § 1.1, 1.2, 1.4 (Blast Radius)
    - 08.6-PATTERNS.md § "Modul-Declarations entfernen"
  </read_first>
  <action>
    VOLLSTÄNDIG LÖSCHEN (Dateien entfernen):
    - service/src/cutover.rs, service_impl/src/cutover.rs, service_impl/src/test/cutover.rs
    - dao/src/cutover.rs, dao_impl_sqlite/src/cutover.rs, rest/src/cutover.rs
    - service/src/carryover_rebuild.rs, service_impl/src/carryover_rebuild.rs, service_impl/src/test/carryover_rebuild.rs
    - shifty_bin/src/integration_test/cutover.rs (2422 Zeilen, 18 Tests)

    MODUL-DEKLARATIONEN ENTFERNEN:
    - service/src/lib.rs: `pub mod cutover;` UND `pub mod carryover_rebuild;`
    - service_impl/src/lib.rs: `pub mod cutover;` UND `pub mod carryover_rebuild;`
    - service_impl/src/test/mod.rs: `pub mod cutover;` UND `pub mod carryover_rebuild;`
    - dao/src/lib.rs: `pub mod cutover;` (Kommentar Z. 93–100 zur rollback-Methode "Required by CutoverService::run" optional bereinigen)
    - dao_impl_sqlite/src/lib.rs: `pub mod cutover;`
    - shifty_bin/src/integration_test.rs: `mod cutover;`

    rest/src/lib.rs an 5 Stellen editieren:
    - Z. 9: `pub mod cutover;` entfernen
    - Z. 378–381: `type CutoverService: ...` aus RestStateDef entfernen
    - Z. 422: `fn cutover_service(...)` aus RestStateDef-Trait entfernen
    - Z. 536: `(path = "/admin/cutover", api = cutover::CutoverApiDoc)` aus `#[openapi]` entfernen
    - Z. 621: `.nest("/admin/cutover", cutover::generate_route())` aus Router entfernen
    Das `.nest("/feature-flag", feature_flag::generate_route())` (Z. 615) BLEIBT.

    rest-types/src/lib.rs bereinigen — alle Cutover-TOs + zugehörige `impl From<&service::cutover::...>`:
    CutoverQuarantineEntryTO, CutoverGateDriftRowTO, CutoverGateDriftReportTO, CutoverRunResultTO,
    CutoverProfileBucketTO, CutoverProfileTO, ManualRangeTO, CutoverConvertQuarantineEntryRequest,
    CutoverConvertQuarantineEntryResponse, CutoverBulkConvertQuarantineRowsRequest,
    CutoverBulkConvertQuarantineRowsResponse, CutoverConvertErrorTO + alle `impl From<&service::cutover::...>`
    (~8 From-Impls). Falls diese TOs in einer utoipa-`components(schemas(...))`-Liste oder ApiDoc-Struct
    in rest-types referenziert sind, dort ebenfalls entfernen. FeatureFlagTO BLEIBT.

    MUST-NOT: CURRENT_SNAPSHOT_SCHEMA_VERSION in service_impl/src/billing_period_report.rs NICHT anfassen (D-10).
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | grep -Ei "error\[|error:" | head; cargo check --workspace 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f service/src/cutover.rs && test ! -f service_impl/src/cutover.rs && test ! -f dao/src/cutover.rs && test ! -f dao_impl_sqlite/src/cutover.rs && test ! -f rest/src/cutover.rs && echo GONE` → GONE
    - `test ! -f service/src/carryover_rebuild.rs && test ! -f service_impl/src/carryover_rebuild.rs && echo GONE` → GONE
    - `grep -rn "page::cutover\|::cutover::generate_route\|CutoverApiDoc" rest/src/lib.rs` → 0 Treffer
    - `grep -rn "Cutover" rest-types/src/lib.rs` → 0 Treffer (ManualRangeTO ebenfalls weg)
    - `grep -rn "feature_flag::generate_route" rest/src/lib.rs` → 1 Treffer (Survivor)
    - Hinweis: main.rs DI ist noch nicht entkoppelt → `cargo check --workspace` darf in DIESER Task noch main.rs-Fehler zeigen; Task 3 schließt sie.
  </acceptance_criteria>
  <done>Alle Cutover/CarryoverRebuild-Backend-Dateien gelöscht, Modul-Decls + rest/lib.rs + rest-types bereinigt; nur main.rs-DI-Fehler verbleiben für Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: main.rs DI entkoppeln + Backend-Tests re-keyen</name>
  <files>shifty_bin/src/main.rs, shifty_bin/src/integration_test/feature_flag.rs, service_impl/src/test/feature_flag.rs</files>
  <read_first>
    - shifty_bin/src/main.rs (Z. 43, 500–531, 596, 635, 729–731, 1063–1106, 1137)
    - 08.6-PATTERNS.md § 2 (exakte main.rs-Block-Vorlagen, inkl. migration_source_dao-BLEIBT-Markierung)
    - 08.6-RESEARCH.md § 2 + Pitfall 2 (migration_source_dao darf NICHT mitgelöscht werden)
    - shifty_bin/src/integration_test/feature_flag.rs (5 Tests, alle nutzen "absence_range_source_active")
    - service_impl/src/test/feature_flag.rs (Mock-basiert, Key beliebig)
  </read_first>
  <action>
    shifty_bin/src/main.rs — folgende Blöcke ENTFERNEN:
    - Z. 43: `type CutoverDao = dao_impl_sqlite::cutover::CutoverDaoImpl;`
    - Z. 500–512: `CarryoverRebuildServiceDependencies` + `impl ...CarryoverRebuildServiceDeps` + `type CarryoverRebuildService = ...`
    - Z. 516–531: `CutoverServiceDependencies` + `impl ...CutoverServiceDeps` + `type CutoverService = ...`
    - Z. 596: `cutover_service: Arc<CutoverService>,` (RestStateImpl-Struct-Feld)
    - Z. 635: `type CutoverService = CutoverService;` (impl RestStateDef)
    - Z. 729–731: `fn cutover_service(&self) -> Arc<Self::CutoverService> { self.cutover_service.clone() }`
    - Z. 1063–1088: DI-Block — den Kommentar ("Phase 4 (Plan 04-06) — Cutover orchestration DI...")
      sowie `let cutover_dao = ...`, den kompletten `let carryover_rebuild_service = Arc::new(...)`-Block
      und den kompletten `let cutover_service = Arc::new(...)`-Block ENTFERNEN.
    - Z. 1137: `cutover_service,` aus dem `Self { ... }`-Konstruktor.

    KRITISCH (Pitfall 2): `let migration_source_dao = Arc::new(MigrationSourceDao::new(pool.clone()));`
    (Z. 1068) BLEIBT — wird von absence_conversion_service (Z. 1095–1106) gebraucht. NUR cutover_dao +
    carryover_rebuild_service + cutover_service aus dem Block schneiden, migration_source_dao stehen lassen.
    Der absence_conversion_service-Block (Z. 1090–1106) bleibt komplett unverändert.

    integration_test/feature_flag.rs re-keyen (D-06): Tests, die "absence_range_source_active" als
    bekannten Seed-Wert assertieren, auf einen neutralen, nicht existierenden Key umstellen, der den
    fail-safe-false-Pfad testet:
    - service_layer_known_flag_returns_seeded_value: nutzt jetzt "totally_unknown_flag_xyz", asserted
      `!value` (fail-safe false). Diesen Test ggf. mit service_layer_unknown_flag_returns_false_failsafe
      zusammenführen oder als generischen unknown-key-Test belassen.
    - rest_get_known_flag_returns_200_with_body / rest_get_without_user_returns_401: URI auf
      "/feature-flag/totally_unknown_flag_xyz" umstellen; Body-Assert `to.key == "totally_unknown_flag_xyz"`,
      `to.enabled == false`. Die generische Mechanismus-Coverage (200-Body-Shape, 401-ohne-User,
      fail-safe-false, auth-rejection) bleibt erhalten.

    service_impl/src/test/feature_flag.rs: Mock-Key von "absence_range_source_active" auf generischen
    "test_flag" umbenennen (Mock-Key ist beliebig, nicht seed-abhängig).
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | tail -1; cargo test -p shifty_bin --no-run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let migration_source_dao" shifty_bin/src/main.rs` → 1 (BLEIBT)
    - `grep -rn "CutoverService\|CutoverDao\|cutover_service\|carryover_rebuild_service\|CarryoverRebuildService" shifty_bin/src/main.rs` → 0 Treffer
    - `grep -c "absence_conversion_service" shifty_bin/src/main.rs` → ≥1 (Survivor intakt)
    - `grep -rn "absence_range_source_active" shifty_bin/src/integration_test/feature_flag.rs service_impl/src/test/feature_flag.rs` → 0 Treffer
    - `cargo check --workspace` → keine error[/error: Zeilen (Build grün)
    - `grep -rn "CarryoverRebuildService\|CutoverService\|CutoverDao" --include=*.rs service/ service_impl/ dao/ dao_impl_sqlite/ rest/ rest-types/ shifty_bin/ | grep -v '^Binary'` → 0 Treffer (außer evtl. erlaubter Kommentar in absence_conversion.rs)
  </acceptance_criteria>
  <done>main.rs DI entkoppelt (migration_source_dao + absence_conversion_service intakt); feature_flag-Tests re-keyed; `cargo check --workspace` grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→REST /admin/cutover/* | ENTFERNT — diese Endpoints (cutover_admin-gated) existieren nach 8.6 nicht mehr; Angriffsfläche reduziert |
| service::extra_hours::soft_delete_bulk | CUTOVER_ADMIN_PRIVILEGE-Gate bleibt unverändert (nur Konstanten-Heimat wechselt); AbsenceConversionService passiert es legitim via Authentication::Full |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-086-01 | E (Elevation) | soft_delete_bulk-Gate | mitigate | CUTOVER_ADMIN_PRIVILEGE-Gate inhaltlich unverändert; nur Definitions-Ort wechselt von service::cutover nach extra_hours.rs. String-Wert "cutover_admin" identisch → keine Gate-Lockerung. |
| T-086-02 | S/T/I/D | entfernte /admin/cutover-Endpoints | accept | Subtraktiv: Endpoints entfernt → Attack-Surface sinkt, keine neue eingeführt. cutover_admin-Privileg-Row in DB bleibt harmlos (kein Code-Konsument). |

**Subtraktive Posture:** Diese Phase entfernt ausschließlich Code/Endpoints und führt keine neue Angriffsfläche ein. Keine HIGH-severity-Threats.
</threat_model>

<verification>
- `cargo check --workspace` ohne error/dead-code-Warnungen auf entfernte Symbole
- Negative-Space: `grep -rn "CutoverService\|CutoverDao\|detect_weekly_lump_sum\|CarryoverRebuildService" --include=*.rs service/ service_impl/ dao/ dao_impl_sqlite/ rest/ rest-types/ shifty_bin/` → 0
- Survivor: `grep -rn "MigrationSourceDao\|absence_period_migration_source" dao/src/ service_impl/src/absence_conversion.rs` → Treffer
- MUST-NOT: `git diff service_impl/src/billing_period_report.rs` zeigt KEINE Änderung an CURRENT_SNAPSHOT_SCHEMA_VERSION (D-10)
</verification>

<success_criteria>
- SC2 (Backend): keine Cutover-Handler/Service/DAO mehr; Backend-Tests grün
- SC4 (Backend): kein Reader/Writer von absence_range_source_active im Rust-Code
- D-09: `cargo check --workspace` grün ohne Dead-Code-Warnungen
- Survivor-Infra (feature_flag-Subsystem, AbsenceConversionService, MigrationSourceDao) verdrahtet
</success_criteria>

<output>
After completion, create `.planning/phases/08.6-cutover-abriss/08.6-01-SUMMARY.md`
</output>
