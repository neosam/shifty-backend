---
phase: 35-slot-einzelwoche-aenderung
plan: 03
type: execute
wave: 2
depends_on: [35-02]
files_modified:
  - shifty-dioxus/src/component/slot_edit.rs
autonomous: true
requirements: [SWO-01]
must_haves:
  truths:
    - "[D-35-02][SWO-01] Im Edit-Modus rendert der Slot-Editor eine Modus-Radiogruppe ('Ab dieser Woche' default-checked / 'Nur diese Woche')"
    - "[SWO-01] Im New-Modus ist die Radiogruppe nicht vorhanden"
    - "[D-35-02] Auswahl 'Nur diese Woche' zeigt den Hinweis-Absatz und setzt single_week=true via SetSingleWeek"
  artifacts:
    - "shifty-dioxus/src/component/slot_edit.rs — Radiogruppe + Hinweis + SlotEditProps-Felder + SSR-Tests"
  key_links:
    - "Radio onchange → on_set_single_week → SlotEditAction::SetSingleWeek(bool) → SLOT_EDIT_STORE.single_week"
---

<objective>
Sichtbare Modus-Radiogruppe im Slot-Editor (D-35-02, SWO-01): zwei Radio-Buttons „Ab dieser Woche"
(Default) / „Nur diese Woche", nur im Edit-Modus, plus konditionaler Hinweis-Absatz, gemäß der
verifizierten 35-UI-SPEC. Verdrahtet die Auswahl über `on_set_single_week` an die in Plan 02
angelegte `SetSingleWeek`-Action.

Purpose: SWO-01 (explizite Modus-Wahl im Editor). Strikt additiv; bestehende Felder/Verhalten und
der Legacy-Class-Guard bleiben intakt.
Output: Radiogruppe + Hinweis in SlotEditInner, SlotEditProps-Erweiterung, SlotEdit-Wrapper-Verdrahtung,
neue SSR-Tests.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-CONTEXT.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-UI-SPEC.md
@shifty-dioxus/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SlotEditProps erweitern + Radiogruppe + Hinweis + Wrapper-Verdrahtung</name>
  <files>shifty-dioxus/src/component/slot_edit.rs</files>
  <read_first>
    - shifty-dioxus/src/component/slot_edit.rs:23-38 (SlotEditProps)
    - shifty-dioxus/src/component/slot_edit.rs:48-152 (SlotEditInner Kopf: i18n-Bindings, Dialog, flex-col gap-3 Spalte, explanation ul)
    - shifty-dioxus/src/component/slot_edit.rs:20-21 (FORM_INPUT_CLASSES, Token-Klassen-Konvention)
    - shifty-dioxus/src/component/slot_edit.rs:272-290 (SlotEdit-Wrapper, use_coroutine_handle, SlotEditAction)
    - .planning/phases/35-slot-einzelwoche-aenderung/35-UI-SPEC.md:85-198 (Component-Spec, Styling, Position, Default, Copy, Hint mit {week}/{year})
  </read_first>
  <action>
    SlotEditProps: ergänze `pub single_week: bool` und `pub on_set_single_week: EventHandler&lt;bool&gt;`.
    i18n-Bindings (nach den bestehenden Label-Bindings): SlotEditModeScopeLabel, SlotEditModeFromThisWeek,
    SlotEditModeThisWeekOnly über `i18n.t(...)`; den Hinweis über `i18n.t_m_rc(Key::SlotEditModeThisWeekOnlyHint,
    [("week", props.week.to_string().into()), ("year", props.year.to_string().into())].into())` (gleiche
    t_m_rc-Interpolation wie SlotEditExplanation).
    RSX (gemäß UI-SPEC „Component Specification"): füge als ZWEITES Item in der `flex flex-col gap-3`-Spalte —
    unmittelbar nach der info-`ul` und VOR dem Wochentag-Field — eine `if props.slot_edit_type == SlotEditType::Edit`-
    Sektion ein:
      - Wrapper `div.flex.flex-col.gap-2`
      - Gruppenlabel `span.text-small.font-medium.text-ink-muted` mit SlotEditModeScopeLabel
      - Options-Reihe `div.flex.gap-4` mit zwei `label.inline-flex.items-center.gap-2.cursor-pointer.text-body.text-ink`,
        jede mit `input` r#type "radio", name "slot_edit_mode", h-4 w-4 border border-border-strong accent-accent form-input:
        Option A checked=!props.single_week, onchange ruft props.on_set_single_week.call(false), Label SlotEditModeFromThisWeek;
        Option B checked=props.single_week, onchange ruft props.on_set_single_week.call(true), Label SlotEditModeThisWeekOnly.
      - konditional `if props.single_week`: `p.text-small.font-normal.text-ink-muted.mt-1` mit dem Hint-Text
        (KEIN Warn-Banner, kein border-l/bg-warn-soft — rein informativ).
    Verwende ausschließlich Design-Token-Klassen (accent-accent, border-border-strong, text-ink, text-ink-muted,
    text-small, text-body) — KEINE Legacy-Palette-Klassen (gray/white/blue/red/green-Literale), damit der
    bestehende Guard slot_edit_no_legacy_classes_in_source grün bleibt.
    SlotEdit-Wrapper (Zeilen 272-290): reiche `single_week: slot_edit.single_week` und
    `on_set_single_week: move |val| slot_service.send(SlotEditAction::SetSingleWeek(val))` an SlotEditInner durch.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'on_set_single_week' shifty-dioxus/src/component/slot_edit.rs` ≥ 3 (Prop-Deklaration + 2 onchange-Aufrufe + Wrapper)
    - `grep -c 'slot_edit_mode' shifty-dioxus/src/component/slot_edit.rs` ≥ 1 (Radio name)
    - `grep -c 'SlotEditModeScopeLabel\|SlotEditModeThisWeekOnlyHint' shifty-dioxus/src/component/slot_edit.rs` ≥ 2
    - Radiogruppe steht in einem `if props.slot_edit_type == SlotEditType::Edit`-Block (nicht im New-Modus gerendert)
    - `cargo build` (dioxus) clean
  </acceptance_criteria>
  <done>Radiogruppe + Hinweis im Editor, Props erweitert, Wrapper verdrahtet, dioxus baut.</done>
</task>

<task type="auto">
  <name>Task 2: SSR-Tests + Test-Helper-Update + Legacy-Guard + WASM-Gate</name>
  <files>shifty-dioxus/src/component/slot_edit.rs</files>
  <read_first>
    - shifty-dioxus/src/component/slot_edit.rs:292-329 (Test-Modul: render, pin_de_locale, props_with)
    - shifty-dioxus/src/component/slot_edit.rs:331-403 (bestehende SSR-Assert-Tests als Muster)
    - shifty-dioxus/src/component/slot_edit.rs:424-447 (slot_edit_no_legacy_classes_in_source — Guard)
  </read_first>
  <action>
    Erweitere den Test-Helper `props_with` um die neuen Pflichtfelder: `single_week: false` und
    `on_set_single_week: EventHandler::new(|_| {})` (sonst kompilieren die bestehenden Tests nicht mehr).
    Füge SSR-Tests hinzu (Muster: render(app) + pin_de_locale, wie slot_edit_renders_max_paid_employees_field_with_value):
    - Edit-Modus mit single_week=false: HTML enthält „Ab dieser Woche" (default-checked) und „Nur diese Woche";
      Hint-Text („Ab der Folgewoche"/Kalenderwoche-Hinweis) NICHT vorhanden.
    - Edit-Modus mit single_week=true: Hint-Absatz vorhanden (z. B. assert auf einen Teilstring des
      SlotEditModeThisWeekOnlyHint-De-Texts mit interpoliertem week/year).
    - New-Modus (slot_edit_type=SlotEditType::New): Radiogruppe NICHT vorhanden (assert !html.contains("Ab dieser Woche")
      bzw. !html.contains(Radio-name "slot_edit_mode")). Dafür eine props-Variante mit SlotEditType::New bauen
      (visible=true erzwingen, da SlotEditInner bei !visible früh leer zurückkehrt).
    Stelle sicher, dass der bestehende Test slot_edit_no_legacy_classes_in_source weiterhin grün ist (Produktionsquelle
    ohne Legacy-Klassen).
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test -p shifty-dioxus slot_edit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `props_with` enthält `single_week` und `on_set_single_week` (bestehende Tests kompilieren wieder)
    - Mindestens 3 neue SSR-Tests: Edit-default (beide Labels, kein Hint), Edit-single-week (Hint sichtbar), New (keine Radiogruppe)
    - `cargo test -p shifty-dioxus slot_edit` grün inkl. slot_edit_no_legacy_classes_in_source
    - WASM-Gate: `cargo build --target wasm32-unknown-unknown` (aus Backend-Shell, im shifty-dioxus/-Verzeichnis) baut
  </acceptance_criteria>
  <done>SSR-Tests grün, Legacy-Guard grün, WASM-Build grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benutzer → Editor-UI | Modus-Auswahl ist reine UI-State-Mutation; keine Datenpersistenz an dieser Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-UI-01 | Elevation of Privilege | Radiogruppe sichtbar für Nicht-Berechtigte | low | accept | Der Slot-Editor wird nur im shiftplan.edit/shiftplanner-Kontext geöffnet; die Mechanik wird backend-seitig (Plan 01) erzwungen — die sichtbare Wahl ändert nichts an der Durchsetzung |
| T-35-UI-02 | Information Disclosure | Hinweis-Text | low | accept | Rein informativ (Kalenderwoche/Jahr), keine sensiblen Daten |
</threat_model>

<verification>
- `cargo test -p shifty-dioxus slot_edit` grün (neue SSR-Tests + Legacy-Guard)
- `cargo build --target wasm32-unknown-unknown` (aus Backend-Shell) grün
- Manuelle Browser-Verifikation (nicht-blockierend, D-25-06-Caveat — siehe 35-VALIDATION „Manual-Only"):
  Editor öffnen, „Nur diese Woche" wählen, speichern, prüfen dass nur die gewählte KW geändert ist
</verification>

<success_criteria>
- Modus-Radiogruppe sichtbar (Edit) / abwesend (New), Default „Ab dieser Woche" (SWO-01)
- Auswahl „Nur diese Woche" zeigt Hinweis + setzt single_week über SetSingleWeek (SWO-01)
- SSR-Tests + Legacy-Guard + WASM-Build grün
</success_criteria>

<output>
Create `.planning/phases/35-slot-einzelwoche-aenderung/35-03-SUMMARY.md` when done
</output>
