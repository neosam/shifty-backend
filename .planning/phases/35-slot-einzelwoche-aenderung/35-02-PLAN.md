---
phase: 35-slot-einzelwoche-aenderung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/cs.rs
  - shifty-dioxus/src/state/slot_edit.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/service/slot_edit.rs
autonomous: true
requirements: [SWO-01, SWO-04]
must_haves:
  truths:
    - "[D-35-02] SlotEdit-State trägt single_week: bool (Default false = 'ab dieser Woche')"
    - "[D-35-02] save_slot_edit routet bei single_week==true auf save_slot_single_week → update_slot_single_week → PUT .../single-week; sonst bestehender save_slot-Pfad"
    - "[D-35-02][SWO-04] Die 4 i18n-Keys SlotEditModeScopeLabel/SlotEditModeFromThisWeek/SlotEditModeThisWeekOnly/SlotEditModeThisWeekOnlyHint existieren in de/en/cs"
    - "[D-35-02] SlotEditAction::SetSingleWeek(bool) setzt SLOT_EDIT_STORE.single_week; new/load setzen single_week zurück auf false"
  artifacts:
    - "shifty-dioxus/src/i18n/mod.rs — 4 Key-Varianten + Locale-Presence-Test"
    - "shifty-dioxus/src/i18n/{de,en,cs}.rs — Übersetzungen"
    - "shifty-dioxus/src/state/slot_edit.rs — single_week-Feld + Default"
    - "shifty-dioxus/src/api.rs — update_slot_single_week"
    - "shifty-dioxus/src/loader.rs — save_slot_single_week"
    - "shifty-dioxus/src/service/slot_edit.rs — SetSingleWeek + Routing"
  key_links:
    - "save_slot_edit → loader::save_slot_single_week → api::update_slot_single_week → PUT /shiftplan-edit/slot/{year}/{week}/single-week"
    - "SlotEditAction::SetSingleWeek → SLOT_EDIT_STORE.single_week"
---

<objective>
Frontend-Plumbing (nicht-visuell) für die Modus-Wahl „nur diese Woche" / „ab dieser Woche"
(D-35-02): State-Feld, API-Call auf die neue single-week-Route, Loader, Service-Routing und die
4 i18n-Keys in allen drei Locales. Die sichtbare Radiogruppe selbst kommt in Plan 03.

Purpose: SWO-01 (Modus-Wahl, Datenpfad) + SWO-04 (i18n de/en/cs). Strikt additive Änderungen —
der bestehende „ab dieser Woche"-Pfad (save_slot) bleibt unverändert; Default ist single_week=false
(100% Backward-Compat).
Output: single_week-State, update_slot_single_week/save_slot_single_week, SetSingleWeek-Action +
Routing, 4 i18n-Keys.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-CONTEXT.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-UI-SPEC.md
@.planning/phases/35-slot-einzelwoche-aenderung/35-PATTERNS.md
@shifty-dioxus/CLAUDE.md
</context>

<artifacts_this_phase_produces>
- i18n Key-Varianten: SlotEditModeScopeLabel, SlotEditModeFromThisWeek, SlotEditModeThisWeekOnly, SlotEditModeThisWeekOnlyHint
- State-Feld `SlotEdit.single_week: bool`
- `api::update_slot_single_week`, `loader::save_slot_single_week`
- `SlotEditAction::SetSingleWeek(bool)` + Routing in save_slot_edit
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 4 i18n-Keys (de/en/cs) + Locale-Presence-Test</name>
  <files>shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs:218-227 (Slot-edit Key-Block)
    - shifty-dioxus/src/i18n/mod.rs:1282-1303 (i18n_slot_paid_capacity_keys_present_in_all_locales — Test-Vorbild)
    - shifty-dioxus/src/i18n/de.rs (bestehender Slot-edit-Übersetzungsblock; t_m_rc-Interpolation für {week}/{year})
    - .planning/phases/35-slot-einzelwoche-aenderung/35-UI-SPEC.md:173-184 (autoritative Copy-Tabelle)
  </read_first>
  <action>
    Ergänze im Key-Enum (mod.rs, im Slot-edit-Abschnitt nach SlotEditSaveError) die 4 Varianten exakt aus der
    UI-SPEC: SlotEditModeScopeLabel, SlotEditModeFromThisWeek, SlotEditModeThisWeekOnly, SlotEditModeThisWeekOnlyHint.
    Füge in de.rs/en.rs/cs.rs je einen Match-Arm pro Key mit den Strings aus der UI-SPEC-Copy-Tabelle hinzu:
    - SlotEditModeScopeLabel: de "Geltungsbereich" / en "Scope" / cs "Rozsah platnosti"
    - SlotEditModeFromThisWeek: de "Ab dieser Woche (Standard)" / en "From this week on (default)" / cs "Od tohoto týdne (výchozí)"
    - SlotEditModeThisWeekOnly: de "Nur diese Woche" / en "This week only" / cs "Pouze tento týden"
    - SlotEditModeThisWeekOnlyHint (mit {week}/{year}-Platzhaltern für t_m_rc): de/en/cs-Texte verbatim aus der UI-SPEC-Tabelle.
    Ergänze in mod.rs einen Test analog i18n_slot_paid_capacity_keys_present_in_all_locales, der über
    [Locale::En, Locale::De, Locale::Cs] die 4 neuen Keys prüft (nicht leer, != "??").
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test -p shifty-dioxus i18n 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SlotEditModeScopeLabel\|SlotEditModeFromThisWeek\|SlotEditModeThisWeekOnly\|SlotEditModeThisWeekOnlyHint' shifty-dioxus/src/i18n/mod.rs` ≥ 4 (Enum-Deklarationen + Test)
    - Jede der 3 Locale-Dateien (de/en/cs) hat 4 neue Match-Arme (`grep -c 'SlotEditMode' shifty-dioxus/src/i18n/de.rs` ≥ 4 etc.)
    - Hint-Text enthält `{week}` und `{year}` Platzhalter in allen 3 Locales
    - Neuer Locale-Presence-Test grün; `cargo test -p shifty-dioxus i18n` grün
  </acceptance_criteria>
  <done>4 Keys + 12 Übersetzungen + Presence-Test vorhanden und grün.</done>
</task>

<task type="auto">
  <name>Task 2: single_week-State-Feld + update_slot_single_week (api) + save_slot_single_week (loader)</name>
  <files>shifty-dioxus/src/state/slot_edit.rs, shifty-dioxus/src/api.rs, shifty-dioxus/src/loader.rs</files>
  <read_first>
    - shifty-dioxus/src/state/slot_edit.rs:88-114 (SlotEdit-Struct + new_edit)
    - shifty-dioxus/src/api.rs:153-165 (update_slot — Vorbild, URL-Muster /shiftplan-edit/slot/{year}/{week})
    - shifty-dioxus/src/loader.rs:705-713 (save_slot — Vorbild)
  </read_first>
  <action>
    state/slot_edit.rs: Ergänze in `SlotEdit` das Feld `pub single_week: bool` (nach current_paid_count) und
    setze in `new_edit()` `single_week: false` (Default = bestehendes „ab dieser Woche"-Verhalten).
    api.rs: Lege `update_slot_single_week(config, slot: SlotTO, year, week)` an — exakt wie `update_slot`, nur
    die URL auf `format!("{}/shiftplan-edit/slot/{}/{}/single-week", config.backend, year, week)` geändert
    (PUT, json(&amp;slot), error_for_status_ref). Diese Route wird von Plan 01 bereitgestellt.
    loader.rs: Lege `save_slot_single_week(config, slot: Rc&lt;SlotEditItem&gt;, year, week)` an — exakt wie
    `save_slot`, nur Aufruf von `api::update_slot_single_week(...)` statt `api::update_slot(...)`.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub single_week: bool' shifty-dioxus/src/state/slot_edit.rs` == 1
    - `grep -c 'single_week: false' shifty-dioxus/src/state/slot_edit.rs` ≥ 1 (Default in new_edit)
    - `grep -c 'fn update_slot_single_week' shifty-dioxus/src/api.rs` == 1 und URL endet auf `/single-week`
    - `grep -c 'fn save_slot_single_week' shifty-dioxus/src/loader.rs` == 1
    - `cargo build` (dioxus) clean
  </acceptance_criteria>
  <done>State-Feld + api- + loader-Funktion vorhanden, dioxus baut.</done>
</task>

<task type="auto">
  <name>Task 3: SetSingleWeek-Action + save_slot_edit-Routing + Resets</name>
  <files>shifty-dioxus/src/service/slot_edit.rs</files>
  <read_first>
    - shifty-dioxus/src/service/slot_edit.rs:19-26 (SlotEditAction enum)
    - shifty-dioxus/src/service/slot_edit.rs:32-74 (new_slot_edit, save_slot_edit)
    - shifty-dioxus/src/service/slot_edit.rs:88-128 (load_slot_edit, Dispatcher slot_edit_service)
  </read_first>
  <action>
    Ergänze in `SlotEditAction` die Variante `SetSingleWeek(bool)`.
    In `save_slot_edit` den `SlotEditType::Edit`-Zweig verzweigen: bei `store.single_week == true` rufe
    `loader::save_slot_single_week(CONFIG.read().clone(), store.slot.clone(), store.year, store.week)`, sonst
    den bestehenden `loader::save_slot(...)`-Aufruf. `SlotEditType::New`-Zweig unverändert.
    Im Dispatcher `slot_edit_service` einen Arm `SlotEditAction::SetSingleWeek(val) => { SLOT_EDIT_STORE.write().single_week = val; Ok(()) }`
    ergänzen.
    In `new_slot_edit` und `load_slot_edit` `store.single_week = false;` setzen (sauberer Reset beim Öffnen des
    Editors — New ist immer „ab dieser Woche", Edit startet im Default).
    Hinweis Borrow: in save_slot_edit hält `store` ein write-borrow; `store.single_week` vor dem await-Aufruf
    auslesen, um Borrow-Konflikte zu vermeiden (Muster wie bestehender Code).
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo build 2>&1 | tail -10 && cargo clippy -- -D warnings 2>&1 | tail -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SetSingleWeek' shifty-dioxus/src/service/slot_edit.rs` ≥ 2 (Variante + Dispatcher-Arm)
    - `grep -c 'save_slot_single_week' shifty-dioxus/src/service/slot_edit.rs` == 1 (Routing im Edit-Zweig)
    - `grep -c 'single_week = false' shifty-dioxus/src/service/slot_edit.rs` ≥ 2 (Reset in new + load)
    - `cargo build` (dioxus) clean; bestehende save_slot-Pfad (single_week=false) unverändert erreichbar
  </acceptance_criteria>
  <done>SetSingleWeek-Action, Save-Routing und Resets vorhanden; dioxus baut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → REST | save_slot_single_week sendet den Slot-Payload an die single-week-Route; echte Autorisierung erfolgt backend-seitig |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-FE-01 | Elevation of Privilege | FE-Modus-Routing | low | accept | FE ist nur Soft-Gate; der Slot-Editor ist bereits shiftplan.edit/shiftplanner-Kontext (D-35-06), und der Backend-Endpoint (Plan 01) erzwingt check_permission unabhängig vom FE — FE-Manipulation ändert nichts an der Durchsetzung |
| T-35-FE-02 | Tampering | i18n-Keys | low | accept | Rein darstellende Strings; kein Sicherheitsbezug; Presence-Test verhindert fehlende Übersetzungen |
</threat_model>

<verification>
- `cargo build` (shifty-dioxus) clean; `cargo build --target wasm32-unknown-unknown` (aus Backend-Shell) grün
- `cargo test -p shifty-dioxus i18n` grün (neuer Locale-Presence-Test)
- Default single_week=false → bestehender save_slot-Pfad unverändert (Backward-Compat)
</verification>

<success_criteria>
- single_week im State, Default false (SWO-01)
- save_slot_edit routet korrekt auf single-week- vs bestehenden Pfad (SWO-01)
- 4 i18n-Keys in de/en/cs vorhanden + getestet (SWO-04)
</success_criteria>

<output>
Create `.planning/phases/35-slot-einzelwoche-aenderung/35-02-SUMMARY.md` when done
</output>
