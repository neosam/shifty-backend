---
phase: 08.1
plan: 11
type: execute
wave: 5
depends_on: [09]
files_modified:
  - .planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md
autonomous: false
requirements: [SC-8.1-09]
tags: [uat, manual, cutover-ui, wizard, type-to-confirm, idempotency]

must_haves:
  truths:
    - "8.1-eigener UAT-Plan (D-21) covers Wizard-Stages, all four row-actions, Bulk-Convert, Type-to-confirm, Idempotency-State."
    - "UAT runs on the int environment by the user (developer/operator) — Claude executes the planning artifact only, not the manual steps."
    - "UAT outcome (pass/fail-per-step) is recorded in `08.1-11-HUMAN-UAT.md`."
  artifacts:
    - path: ".planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md"
      provides: "Numbered UAT checklist with Pre-conditions / Steps / Expected / Recorded-result per Stage and Action."
      contains: "8.1 UAT"
  key_links:
    - from: ".planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md"
      to: ".planning/phases/08.1-cutover-migration-ui/08.1-09-SUMMARY.md (Plan 09 output)"
      via: "UAT exercises the rendered page"
      pattern: "8.1-09-SUMMARY"
---

<objective>
Author the 8.1-eigener UAT-Plan as a markdown checklist file. The user runs this manually against the int environment after Plan 09 ships. Per D-21 the UAT is independent of the Phase-8-HUMAN-UAT-Subsumption (Plan 12).

Purpose: SC-8.1-09 — independent verification that the Cutover-UI itself behaves correctly (wizard flow, all four row actions, bulk-convert, type-to-confirm, idempotency).

Output: `08.1-11-HUMAN-UAT.md` checklist file.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-cutover-migration-ui/08.1-CONTEXT.md
@.planning/phases/08.1-cutover-migration-ui/08.1-UI-SPEC.md
@.planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create the 8.1-UAT-Plan checklist (Claude authors; user runs the steps)</name>
  <files>.planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md (full file) — structural template (Pre-conditions, numbered steps, expected outcomes, Recorded-result column).
    - .planning/phases/08.1-cutover-migration-ui/08.1-UI-SPEC.md § Interaction Contracts + § States (lines 219-340).
  </read_first>
  <what-built>
A markdown checklist file `08.1-11-HUMAN-UAT.md` with:
- **Setup section**: int env URL, two test users (`cutover_admin` + `hr-only`), database state pre-condition (must have at least one quarantined row to be useful — if not, run `gate-dry-run` and seed test data via existing UI/REST).
- **Sections**:
  1. Wizard navigation (Stage 1→2→3 transitions; Back button; Stage 3 disabled until quarantined_rows == 0).
  2. Profile stage: stat-boxes correct, per-person tiles render correctly.
  3. Dry-Run stage: drift list grouped per (person, category, year); each entry shows ISO-Date + weekday-code + Hours + Reason + Suggested-Action.
  4. Per-row actions: Convert (single), Edit (open modal, change amount, save → row reflects update), Delete (row removed), Skip (row hidden until reload).
  5. Bulk-Convert: per-group "Alle in Gruppe konvertieren" button → all rows in group converted in one Tx; group section disappears.
  6. Type-to-confirm: Stage 3 input, button stays disabled until "CUTOVER" exact (test "cutover" / "CUTOVERX" / empty all stay disabled), enables on exact match.
  7. Privilege gate: HR-only user sees Stage 1 + 2 fully, Stage 3 disabled with tooltip; cutover_admin user can commit.
  8. Idempotency: after successful commit, reload page → IdempotenzBanner visible; Stage 3 Commit-Btn permanently disabled with tooltip.
  9. Error path: trigger a 422 by manually invoking convert on a known fractional row → toast renders the backend reason.
- Each step has a checkbox + a "Recorded result" placeholder.
  </what-built>
  <how-to-verify>
1. Run all backend + frontend builds + tests green: `cargo test --workspace && (cd shifty-dioxus && nix develop -c cargo test && nix-shell -p lld --command "cargo build --target wasm32-unknown-unknown")`. Plans 1-10 complete.
2. Deploy backend + frontend to int environment with admin-auto-grant trigger active so DEVUSER has `cutover_admin`. Verify via `/feature-flag/absence_range_source_active` — must be `enabled: false` (cutover not yet committed).
3. Seed at least 1 quarantined row that requires manual resolution (e.g. fractional 13.33h Vacation entry on a known sales person).
4. Open `/admin/cutover` in browser as `cutover_admin` user.
5. Walk through every numbered step in `08.1-11-HUMAN-UAT.md`; check each box; record observations.
6. Switch user to `hr`-only and re-walk steps 1-3 + 7 (privilege gate verification).
7. After all steps pass, fill in "UAT Sign-off" footer with date + tester.
8. **Resume the plan execution** by replying to the orchestrator with the UAT outcome (pass / fail per step / gap-2 candidates if any).
  </how-to-verify>
  <action>
Author the file `.planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md` with the following template structure (Claude writes the content; user runs the steps):

```markdown
# Phase 8.1 — 8.1-Eigener Cutover-UI UAT

**Plan:** 8.1-11 (D-21)
**Status:** pending
**Tester:** _____
**Date:** _____
**Environment:** int

> Independent UAT for the Cutover-Migration-UI itself. Phase-8-HUMAN-UAT
> (35 steps, post-Cutover) runs separately as Plan 8.1-12 (Subsumption).

## Setup

- [ ] Backend running on int with admin-auto-grant trigger (Plan 08-07).
- [ ] Frontend deployed.
- [ ] Two test users available:
  - **`cutover_admin_user`** with `cutover_admin` privilege (DEVUSER on int via auto-grant trigger).
  - **`hr_only_user`** with `hr` privilege only (no `cutover_admin`).
- [ ] `GET /feature-flag/absence_range_source_active` returns `{"enabled":false}` (cutover not yet committed).
- [ ] At least 1 quarantined extra_hours row exists (seed fractional 13.33h Vacation entry if needed for manual-resolution path).

## 1. Wizard Navigation (D-07)

- [ ] 1.1 Open `/admin/cutover/` as `cutover_admin_user`. Page loads with Stage 1 (Profile) active.
- [ ] 1.2 "Weiter" button is enabled after profile loads. Click → Stage 2 (Dry-Run) becomes active. Stage 1 marked complete (✓).
- [ ] 1.3 "Zurück" button takes user back to Stage 1.
- [ ] 1.4 Stage 3 ("Durchführen") is disabled while `quarantined_rows > 0`. Verify by hovering "Weiter" on Stage 2 → button disabled with tooltip OR Stage 3 click is blocked.

## 2. Profile Stage (D-10)

- [ ] 2.1 4 stat boxes render: "Zu migrierende Zeilen", "Betroffene Mitarbeiter", "Quarantäne-Einträge", "Übertrags-Differenz". Numbers > 0.
- [ ] 2.2 Per-person tiles render with PersonChip + row count + hours sum.

## 3. Dry-Run Stage (D-08, D-11..D-14)

- [ ] 3.1 Drift list groups rows by `(person_name, category, year)`. Each group has a header showing person + category + year + entry count.
- [ ] 3.2 Each entry row shows: ISO date + weekday code (e.g. "2026-05-08 (Fri)"), Amount, Reason badge, Suggested-Action hint, 4 inline action buttons.
- [ ] 3.3 Hover the Reason badge → tooltip shows the full `reason_text` (English; per D-27).

## 4. Per-Row Actions (D-12)

- [ ] 4.1 **Convert**: Click "Eintrag konvertieren" on a row that matches the heuristic. Row disappears within 1-2s; `quarantined_rows` count decreases by 1.
- [ ] 4.2 **Edit**: Click "Bearbeiten" → modal opens with Amount + Date inputs (NO Kategorie / Beschreibung — D-04). Change Amount (e.g. 13.33 → 16.0); save. Modal closes; the drift list reflects the new value.
- [ ] 4.3 **Delete**: Click "Löschen" on a row → row disappears immediately; `quarantined_rows` count decreases by 1.
- [ ] 4.4 **Skip**: Click "Eintrag überspringen" → row disappears immediately; `quarantined_rows` count UNCHANGED. Reload page (F5) → row reappears (D-05 frontend-only).

## 5. Bulk-Convert (D-13)

- [ ] 5.1 Pick a group with >= 2 rows. Click "Alle in Gruppe konvertieren". All rows in the group convert atomically; group section disappears; `quarantined_rows` count decreases by the group size.
- [ ] 5.2 Pick a group where at least one row will fail the heuristic (e.g. a fractional row + a heuristic-matching row). Click bulk-convert → toast renders the validation error; both rows REMAIN in the list (strict-atomic 422).

## 6. Type-to-Confirm (D-15)

- [ ] 6.1 Resolve all quarantined rows (combination of Convert / Edit / Delete) until count == 0.
- [ ] 6.2 "Weiter" to Stage 3. Migration summary stats render (Rows / AbsencePeriods / Carryover-Diff).
- [ ] 6.3 Type "cutover" (lowercase) → Commit button stays disabled.
- [ ] 6.4 Type "CUTOVERX" → button stays disabled.
- [ ] 6.5 Clear and type "CUTOVER" exactly → button enables (Danger variant — red).

## 7. Privilege Gate (D-23)

- [ ] 7.1 Log out and log in as `hr_only_user`.
- [ ] 7.2 Open `/admin/cutover/`. Stage 1 + 2 fully functional.
- [ ] 7.3 At Stage 3, the Commit button shows as disabled (Danger variant) with tooltip "Commit erfordert das cutover_admin-Privileg".
- [ ] 7.4 Switch user back to `cutover_admin_user`.

## 8. Commit + Idempotency (D-16, D-17)

- [ ] 8.1 With "CUTOVER" typed and admin privilege, click Commit. Loading-state appears (spinner / busy overlay).
- [ ] 8.2 Page transitions to Success stage. Summary stats render. Idempotenz-hint message visible. Backup-path hint visible.
- [ ] 8.3 Reload the page. IdempotenzBanner appears at the top: "Cutover bereits abgeschlossen". Stage 3 Commit button permanently disabled with tooltip "Commit deaktiviert: Quarantäne-Zeilen vorhanden oder Cutover bereits durchgeführt".

## 9. Error Toast Path

- [ ] 9.1 Open browser dev tools → manually fire a `POST /admin/cutover/convert-quarantine-entry` for a row that's already converted (use the existing dev `fetch(...)`). Verify the page renders an error toast with a meaningful message.

## Outcome

- [ ] All steps passed → mark `08.1-11-HUMAN-UAT.md` status: `passed`; record date + tester.
- [ ] OR: list failing steps + observed behaviour. Open issues / gap-2 candidates per D-22.

## UAT Sign-off

- **Status:** _____ (pending / passed / failed-with-gaps)
- **Tester:** _____
- **Date:** _____
- **Notes:** _____
```

After authoring the file, the executor's role is COMPLETE. The user (developer/operator) executes the steps manually. The plan's `<verify>` is the user's reply to the orchestrator with the UAT outcome.
  </action>
  <resume-signal>Reply to the orchestrator with the UAT outcome: "8.1-11 UAT passed" / "8.1-11 UAT failed at step X.Y — recorded gap-2 in 08.1-11-HUMAN-UAT.md".</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → int browser session | Manual UAT against deployed system; production-equivalent threat model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.1-AUTHZ-02 | Elevation of Privilege | Stage 3 Commit by HR user | mitigate | Step 7.3 verifies the disabled state + tooltip rendering. Backend gate is the authoritative defense — verified during commit attempt. |
| T-8.1-IDEM-01 | Tampering / Replay | Re-Open after Commit | mitigate | Step 8.3 verifies IdempotenzBanner + permanent button-disabled state. |

</threat_model>

<verification>

- [ ] `08.1-11-HUMAN-UAT.md` file exists in the phase directory.
- [ ] User completes all numbered steps and reports the outcome via the orchestrator.

</verification>

<success_criteria>

- UAT checklist file authored with all 9 sections.
- User runs the UAT and records the result.
- SC-8.1-09 closed (Cutover-UI verified by an independent UAT).

</success_criteria>

<output>
After the user replies with the UAT outcome, create `.planning/phases/08.1-cutover-migration-ui/08.1-11-SUMMARY.md` with:
- UAT date + tester
- Per-section pass/fail summary
- Any gap-2 candidates from step failures
- Files committed list (the UAT file itself)
</output>
