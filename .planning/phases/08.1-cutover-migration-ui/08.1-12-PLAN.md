---
phase: 08.1
plan: 12
type: execute
wave: 6
depends_on: [11]
files_modified:
  - .planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md
autonomous: false
requirements: [SC-8.1-10, SC-8.1-11]
tags: [uat, manual, phase-8-subsumption, closure]

must_haves:
  truths:
    - "Phase-8-HUMAN-UAT (35 steps) executed on int after Cutover, using migrated data."
    - "`08-HUMAN-UAT.md` gap-1 status updated to `resolved`."
    - "Backend `cargo test --workspace` + Frontend `cargo test` + WASM-Build green as final phase-closing gate."
    - "Phase 8 + Phase 8.1 closed jointly per D-20."
  artifacts:
    - path: ".planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md"
      provides: "gap-1 status flipped to `resolved` + Plan 8.1-12 sign-off section"
      contains: "resolved"
  key_links:
    - from: ".planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md"
      to: ".planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md (Plan 11 must be passed first)"
      via: "Plan 12 depends on Plan 11"
      pattern: "depends_on: \\[11\\]"
---

<objective>
Phase-8-HUMAN-UAT-Subsumption (D-20): the user runs the existing 35-step HUMAN-UAT in `08-HUMAN-UAT.md` against the int environment AFTER the Cutover-Migration-UI (Plan 09) successfully completed a real cutover. Updates gap-1 to `resolved` and closes Phases 8 + 8.1 jointly.

Final regression gates: `cargo test --workspace` + `cd shifty-dioxus && cargo test` + WASM build.

Purpose: SC-8.1-10 + SC-8.1-11 — Phase-8-block lifted; Phase 8.1 reaches closure.

Output: updated `08-HUMAN-UAT.md` + final regression-gate evidence.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-cutover-migration-ui/08.1-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md
@.planning/phases/08.1-cutover-migration-ui/08.1-11-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User runs Phase-8-HUMAN-UAT (35 steps) on int post-cutover</name>
  <files>.planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/08-absence-crud-page-foundation/08-HUMAN-UAT.md (full file) — the existing 35-step checklist + gap-1 detail + Setup pre-conditions.
    - .planning/phases/08.1-cutover-migration-ui/08.1-11-HUMAN-UAT.md (just authored) — must be passed BEFORE running the Phase-8-UAT (D-20: Phase-8-UAT runs post-Cutover).
  </read_first>
  <what-built>
The existing `08-HUMAN-UAT.md` is the artifact; this plan UPDATES it after the user runs it on int. Specifically:
1. The user runs the 35 steps documented in `08-HUMAN-UAT.md` against the int environment, with the database now containing migrated data (post-Cutover from Plan 09).
2. For each step, mark pass / fail / observation directly in the file's checkboxes.
3. Locate the gap-1 section and flip status from `open` to `resolved` (with a one-line rationale referencing Plan 8.1-09 + 8.1-11).
4. Append a "Phase 8.1-12 Sign-off" footer section: tester, date, summary outcome, link to 8.1-11 UAT outcome.
5. If any new drift patterns appear that the 8.1-UI cannot resolve → record as gap-2 (D-22) in the same file; Phase 8.1 closes regardless (per D-22).
  </what-built>
  <how-to-verify>
1. Plan 8.1-11 must be in `passed` state — confirmed by reading `08.1-11-HUMAN-UAT.md` "UAT Sign-off" footer.
2. After Cutover (Plan 09 successfully ran a real `commit` on int), the int database has migrated data: `extra_hours` rows soft-deleted, `absence_period` rows inserted, `absence_range_source_active` flag flipped to `true`.
3. Open `08-HUMAN-UAT.md` and walk through all 35 steps in order. Steps cover the absence-CRUD page from Phase 8 (now reading from migrated `absence_period` rows).
4. Record outcome per step. Flip gap-1 status to `resolved` once steps 1-35 all pass.
5. Run final regression gates:
   ```bash
   cargo test --workspace                            # backend
   (cd shifty-dioxus && nix develop -c cargo test)   # frontend tests
   (cd shifty-dioxus && nix-shell -p lld --command "cargo build --target wasm32-unknown-unknown")  # WASM
   ```
   All three exit 0.
6. Reply to the orchestrator with: "8.1-12 Phase-8 UAT passed; 35/35 steps; gap-1 resolved; final regression gates green".

If any step fails:
- Investigate; if it's a Phase 8 bug (not Phase 8.1) — out of scope here, file as a Phase 9.x backlog item.
- If it's a Phase 8.1 regression — that's a Plan 09/10 issue; reopen and fix.
- D-22: Phase 8.1 still closes if new drift-patterns appear that the 8.1-UI couldn't handle (gap-2 documented for Phase 8.2 / 9.x).
  </how-to-verify>
  <action>
The executor (Claude) prepares the UAT artifact for editing:

1. Read `08-HUMAN-UAT.md` and locate the gap-1 section header (search for `gap-1` or `## Gap`).
2. Append a new section at the END of the file titled "Phase 8.1-12 Sign-off":

```markdown
## Phase 8.1-12 Sign-off (Subsumption)

**Plan:** 8.1-12 (D-20 Subsumption-Closure)
**Tester:** _____
**Date:** _____
**Pre-conditions met:**
- [ ] Plan 8.1-09 deployed; Cutover-Migration-UI live on int.
- [ ] Plan 8.1-11 UAT passed (independent Cutover-UI verification).
- [ ] Real Cutover commit run via the 8.1-UI on int; `absence_range_source_active = true`.
- [ ] Migrated data visible in `absence_period` table.

**35-Step UAT Result:** _____ / 35 passed

**Gap-1 Status:** _____ (resolved | partial — see gap-2 below)

**Gap-2 (new patterns for Phase 8.2 / 9.x, if any):**
_____

**Final Regression Gates:**
- [ ] `cargo test --workspace` exit 0
- [ ] `cd shifty-dioxus && nix develop -c cargo test` exit 0
- [ ] `cd shifty-dioxus && nix-shell -p lld --command "cargo build --target wasm32-unknown-unknown"` exit 0

**Phase 8.1 Closure:** Phase 8 + Phase 8.1 jointly closed. v1.3 milestone progress unblocked.
```

3. Locate the `gap-1` mention earlier in the file. Add a one-line note immediately under it:

```markdown
> **Status update (2026-05-XX, Plan 8.1-12):** resolved. Operator-driven
> resolution via 8.1-Cutover-UI (Plan 09) handled all int drift patterns.
> See "Phase 8.1-12 Sign-off" section below for full audit trail.
```

(The user replaces the date placeholder with the actual date when running the UAT.)

The user then runs the 35 steps and the regression gates; replies to the orchestrator with the outcome. Claude does NOT execute the steps — only authors the artifact updates.
  </action>
  <resume-signal>Reply to the orchestrator with: "8.1-12 Phase-8 UAT passed: 35/35; gap-1 → resolved; cargo test --workspace green; cd shifty-dioxus && cargo test green; WASM build green; Phase 8.1 ready to close" — OR — "8.1-12 Phase-8 UAT failed at step N: [observation]; Phase 8.1 closes per D-22 with gap-2 [...] documented".</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → int browser session | Same as Plan 11; manual UAT against deployed system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (data-integrity) | Tampering | post-cutover migrated data inconsistency | mitigate | The 35-step Phase-8-UAT exercises the absence-CRUD page against migrated data; any inconsistency surfaces as a step failure. Reporting calculations (`derive_hours_for_range`) are validated implicitly as the user confirms balance/expected/used values match expectations. |

</threat_model>

<verification>

- [ ] `08-HUMAN-UAT.md` updated with Plan 8.1-12 Sign-off section + gap-1 status note.
- [ ] User reports UAT outcome via orchestrator.
- [ ] Final regression gates green (per user's report).

</verification>

<success_criteria>

- 35-step Phase-8-UAT runs on migrated int data.
- gap-1 status flipped to `resolved` (or `partial` with gap-2 documentation per D-22).
- Final regression gates green.
- Phase 8 + Phase 8.1 closed jointly.
- v1.3 milestone progress unblocked.

</success_criteria>

<output>
After the user replies with the UAT + regression-gate outcome, create `.planning/phases/08.1-cutover-migration-ui/08.1-12-SUMMARY.md` with:
- UAT date + tester
- 35-step pass/fail summary
- gap-1 resolution rationale (with link to Plan 09 + Plan 11 evidence)
- Gap-2 list (if any)
- Final regression gate evidence (3 commands × exit 0)
- Phase 8 + 8.1 joint closure note
- Files committed list (UAT updates + SUMMARYs)
</output>
