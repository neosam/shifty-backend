---
phase: 26-freiwilligen-abwesenheit-cross-navigation
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - service_impl/src/test/booking_information_vfa.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [VFA-01, VFA-02]
must_haves:
  truths:
    - "D-26-04: in get_weekly_summary, a Holiday special_day in a volunteer's week leaves committed_voluntary_hours UNCHANGED, while an absence in that week reduces it to 0 — the asymmetry is asserted in one shared test context (regression guard)."
    - "D-26-01: the full-service test exercises the reduction for an absence period of one of the three categories (Vacation/SickLeave/UnpaidLeave) and proves the year-view drop."
    - "D-26-03: the absence-overlap week shows the volunteer's committed pledge fully removed (whole week → 0), not pro-rated."
    - "D-26-02: a regression assertion proves CURRENT_SNAPSHOT_SCHEMA_VERSION is unchanged (still 11) — VFA adds no persisted value_type, no bump."
  artifacts:
    - "service_impl/src/test/booking_information_vfa.rs — full-service get_weekly_summary asymmetry test + no-bump assertion"
    - "service_impl/src/test/mod.rs — registers the new test module"
  key_links:
    - "MockAbsenceService.find_all returns one overlapping period in the absence-week and none in the holiday-week"
    - "MockSpecialDayService.get_by_week returns a Holiday in the holiday-week and none in the absence-week"
    - "assertion compares committed_voluntary_hours of the holiday-week vs the absence-week for the same volunteer"
---

<objective>
Prove VFA-02 (D-26-04): in `get_weekly_summary`, a holiday in a volunteer's week does NOT
reduce committed_voluntary 🎯, but an absence does — the deliberate asymmetry. Add a
full-service regression test that exercises both a holiday-week and an absence-week for the
same volunteer in one context, plus a guard that no snapshot-schema bump occurred (D-26-02).

Purpose: Lock the asymmetry so a future change that accidentally couples holiday handling to
the committed band (or that bumps the snapshot schema) fails CI.
Output: A new `booking_information_vfa` test module.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-freiwilligen-abwesenheit-cross-navigation/26-CONTEXT.md

# Test templates + the code under test
@service_impl/src/booking_information.rs
@service_impl/src/test/reporting_cap_overflow.rs
@service_impl/src/test/booking_information.rs
</context>

<artifacts_this_phase_produces>
- `service_impl/src/test/booking_information_vfa.rs` (new): full-service `get_weekly_summary`
  asymmetry test (holiday-week vs absence-week) + a `phase26_no_snapshot_bump` assertion.
- `service_impl/src/test/mod.rs`: `pub mod booking_information_vfa;` registration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full-service get_weekly_summary holiday-vs-absence asymmetry test</name>
  <read_first>
    - service_impl/src/booking_information.rs:69-84 (BookingInformationServiceImpl field list — the exact set of mocks to construct, now incl. absence_service)
    - service_impl/src/booking_information.rs:136-295 (get_weekly_summary control flow: per-week reporting.get_week, special_day.get_by_week, shiftplan_report.extract_shiftplan_report_for_week, slot.get_slots_for_week_all_plans; committed_voluntary_hours at 219-226)
    - service_impl/src/test/reporting_cap_overflow.rs:20-160 (full mock-deps construction template: TestDeps impl, per-service Mock*::new() + expect_*().returning(...), Arc wrapping)
    - service_impl/src/test/reporting_additive_merge.rs:100-235 (alternative TestDeps + builder template, incl. MockSpecialDayService / MockToggleService setup)
    - service/src/sales_person.rs:17 (SalesPerson.is_paid: Option<bool>) and the SalesPerson fixture shape used by sales_person_service.get_all
  </read_first>
  <files>service_impl/src/test/booking_information_vfa.rs, service_impl/src/test/mod.rs</files>
  <action>
    Create service_impl/src/test/booking_information_vfa.rs and register it in
    service_impl/src/test/mod.rs (`pub mod booking_information_vfa;`).

    Build a `BookingInformationServiceImpl` over a local `TestDeps` with all dependency mocks
    (mirror the field list in booking_information.rs:69-84): ShiftplanReportService,
    SlotService, BookingService, SalesPersonService, SalesPersonUnavailableService,
    ReportingService, SpecialDayService, EmployeeWorkDetailsService (alias of
    MockEmployeeWorkDetailsService), PermissionService, ClockService, UuidService,
    TransactionDao, and the NEW AbsenceService (MockAbsenceService). Use the
    reporting_cap_overflow.rs construction style.

    Fixture (one volunteer, is_paid=false, committed_voluntary>0, cap_planned_hours_to_expected
    so the Band-1 gate includes them):
    - permission_service.expect_check_permission → Ok (Shiftplanner path).
    - sales_person_service.expect_get_all → one SalesPerson with is_paid=Some(false).
    - employee_work_details_service.expect_all → one EmployeeWorkDetails for that volunteer with
      committed_voluntary = 5.0 and cap_planned_hours_to_expected=true, valid across the test weeks.
    - shiftplan_report_service.expect_extract_shiftplan_report_for_week → empty (no actuals;
      isolates the committed pledge band).
    - slot_service.expect_get_slots_for_week_all_plans → empty.
    - reporting_service.expect_get_week → empty week report (paid_hours irrelevant here).
    - clock/uuid/transaction → standard no-op mocks (transaction_dao returns MockTransaction).

    Asymmetry arrangement — pick two concrete weeks of the same year (e.g. an ABSENCE_WEEK and a
    HOLIDAY_WEEK that are NOT adjacent so a single absence period cannot bleed into the holiday
    week):
    - special_day_service.expect_get_by_week → returns a `SpecialDayType::Holiday` ONLY for
      HOLIDAY_WEEK, empty otherwise (match on the week arg).
    - absence_service.expect_find_all → returns ONE AbsencePeriod for the volunteer whose
      [from_date, to_date] falls inside ABSENCE_WEEK (use time::Date::from_iso_week_date to build
      the dates), and nothing overlapping HOLIDAY_WEEK. Category = Vacation (D-26-01 — category is
      not consulted by the reduction).

    Call `get_weekly_summary(year, Authentication::Full, None)`. From the returned
    `Arc<[WeeklySummary]>` locate the entry for HOLIDAY_WEEK and the entry for ABSENCE_WEEK and
    assert (epsilon compare):
    - HOLIDAY_WEEK.committed_voluntary_hours ≈ 5.0 (holiday does NOT reduce — D-26-04 / VFA-02).
    - ABSENCE_WEEK.committed_voluntary_hours ≈ 0.0 (absence reduces whole week — VFA-01 / D-26-03).
    - Assert the two differ, with a message naming the asymmetry, so the intent is explicit.

    Keep the test `#[tokio::test]`. Do not assert on unrelated weeks.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl booking_information_vfa 2>&1 | tail -30</automated>
  </verify>
  <done>All <acceptance_criteria> items are satisfied and the <verify> automated command passes.</done>
  <acceptance_criteria>
    - The new `#[tokio::test]` builds `BookingInformationServiceImpl` with all mocks (incl. AbsenceService) and passes.
    - It asserts HOLIDAY_WEEK committed_voluntary_hours ≈ 5.0 and ABSENCE_WEEK ≈ 0.0 for the same volunteer (asymmetry).
    - `cargo test -p service_impl booking_information_vfa` is green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: No-snapshot-bump regression assertion + full backend gate</name>
  <read_first>
    - service_impl/src/test/booking_information.rs:439-474 (existing snapshot_schema_version assertion asserting CURRENT_SNAPSHOT_SCHEMA_VERSION == 11)
    - shifty-backend/CLAUDE.md "Billing Period Snapshot Schema Versioning" (when a bump is/ isn't required)
  </read_first>
  <files>service_impl/src/test/booking_information_vfa.rs</files>
  <action>
    Add a second test `phase26_vfa_no_snapshot_bump()` to booking_information_vfa.rs asserting
    `crate::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION == 11`, with a comment stating
    the Phase-26 rationale (D-26-02): VFA-01 changes only the live, non-persisted year-view
    (get_weekly_summary); it adds no persisted BillingPeriodValueType and does not change any
    snapshot computation/input-set, so the version MUST stay 11. Do NOT modify the existing
    assertion in test/booking_information.rs — this is an independent Phase-26 guard.

    Then run the full backend gate to confirm no regression across the workspace.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl 2>&1 | tail -15 && cargo clippy --workspace -- -D warnings 2>&1 | tail -15</automated>
  </verify>
  <done>All <acceptance_criteria> items are satisfied and the <verify> automated command passes.</done>
  <acceptance_criteria>
    - `phase26_vfa_no_snapshot_bump` passes (version still 11, D-26-02).
    - `cargo test -p service_impl` passes (all existing + new tests).
    - `cargo clippy --workspace -- -D warnings` is clean (hard gate).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → BookingInformationServiceImpl | mock-only; no real DB or network; no untrusted runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-T1 | Tampering | snapshot schema drift hidden by missing guard | low | mitigate | phase26_vfa_no_snapshot_bump pins version 11 so an accidental future bump (or persisted value_type change) fails CI. |
| T-26-SC | Tampering | npm/pip/cargo installs | n/a | accept | Tests only; no new dependencies installed. |
</threat_model>

<verification>
- `cargo test -p service_impl booking_information_vfa` passes (asymmetry + no-bump).
- `cargo test -p service_impl` and `cargo clippy --workspace -- -D warnings` are clean.
</verification>

<success_criteria>
- The holiday-vs-absence asymmetry is locked by an executable regression test (VFA-02 / D-26-04).
- The snapshot schema version is asserted unchanged at 11 (D-26-02).
</success_criteria>

<output>
Create `.planning/phases/26-freiwilligen-abwesenheit-cross-navigation/26-02-SUMMARY.md` when done.
</output>
