---
phase: 22-mitarbeiter-statistik-hr
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/state/employee.rs
  - shifty-dioxus/src/service/employee.rs
  - shifty-dioxus/src/component/employee_view.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [STAT-01, STAT-02]

must_haves:
  truths:
    - "STAT-01 D-22-04 D-22-05: An HR-only statistics block is rendered on /employees/:id (EmployeeView) — visible with the HR role, hidden without it (is_hr gating like absences.rs)."
    - "STAT-02 D-22-02: The block shows the average worked hours per week fetched from the HR-gated backend endpoint GET /report/{id}/weekly-statistics."
    - "D-22-08: A frontend SSR test proves the block is present when is_hr=true and absent when is_hr=false; i18n keys exist in De/En/Cs."
  artifacts:
    - path: "shifty-dioxus/src/api.rs"
      provides: "get_employee_weekly_statistics fetch fn hitting /report/{id}/weekly-statistics"
      contains: "weekly-statistics"
    - path: "shifty-dioxus/src/component/employee_view.rs"
      provides: "HR-only stats block + should_show_hr_stats helper + SSR visibility test"
      contains: "should_show_hr_stats"
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "New i18n Key variants for the stats block"
      contains: "AverageWorkedHoursPerWeek"
  key_links:
    - from: "shifty-dioxus/src/component/employee_view.rs"
      to: "AUTH.has_privilege(\"hr\")"
      via: "is_hr gate controls block rendering"
      pattern: "has_privilege"
    - from: "shifty-dioxus/src/service/employee.rs"
      to: "api::get_employee_weekly_statistics"
      via: "loads stats into EMPLOYEE_STORE when HR"
      pattern: "get_employee_weekly_statistics"
    - from: "shifty-dioxus/src/component/employee_view.rs"
      to: "EmployeeWeeklyStatisticsTO"
      via: "renders average_worked_hours_per_week"
      pattern: "average_worked_hours_per_week"
---

<objective>
Add the HR-only statistics block to the employee detail page (/employees/:id), showing
the average worked hours per week fetched from the Phase-22 backend endpoint.

Purpose: STAT-01 (HR-only visibility, no new route — D-22-04) and STAT-02 (display the
vacation-adjusted average) on the FE. Gating uses the established is_hr pattern; the
value comes from the HR-gated backend so it never reaches non-HR users.
Output: API fetch fn, store wiring, an HR-gated block in EmployeeView, i18n in De/En/Cs,
and an SSR visibility test.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-mitarbeiter-statistik-hr/22-CONTEXT.md
@.planning/phases/22-mitarbeiter-statistik-hr/22-01-SUMMARY.md

# i18n: every new Key MUST get a translation in en.rs, de.rs AND cs.rs.
# WASM gate: `cargo build --target wasm32-unknown-unknown` must exit 0.
# Pitfall: gate on AUTH.loading_done before branching on is_hr (two-cycle auth resolve).
@shifty-dioxus/CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

Backend endpoint (from 22-01): GET `/report/{id}/weekly-statistics` (HR-gated, 403 for non-HR), returns:
```json
{ "average_worked_hours_per_week": 23.4, "included_weeks": 12, "total_worked_hours": 280.8 }
```
DTO: `rest_types::EmployeeWeeklyStatisticsTO { average_worked_hours_per_week: f32, included_weeks: u32, total_worked_hours: f32 }`.

From shifty-dioxus/src/api.rs (fetch idiom — mirror get_employee_reports):
```rust
pub async fn get_employee_reports(
    config: Config, sales_person_id: Uuid, year: u32, calendar_week: u8,
) -> Result<Rc<EmployeeReportTO>, reqwest::Error> {
    let url = format!("{}/report/{}?year={}&until_week={}", config.backend, sales_person_id, year, calendar_week);
    let response = reqwest::get(url).await?;
    response.error_for_status_ref()?;          // 403 → Err, handled by caller
    let res = response.json().await?;
    Ok(res)
}
```

From shifty-dioxus/src/service/auth.rs (auth global signal):
```rust
pub struct AuthStore { pub auth_info: Option<AuthInfo>, pub loading_done: bool }
pub static AUTH: GlobalSignal<AuthStore> = Signal::global(|| AuthStore::default());
```
From shifty-dioxus/src/state/auth_info.rs: `pub fn has_privilege(&self, privilege: &str) -> bool`.
is_hr idiom (page/absences.rs:1885): `AUTH.read().auth_info.as_ref().map(|a| a.has_privilege("hr")).unwrap_or(false)`.

From shifty-dioxus/src/component/employee_view.rs (where the block goes — inside EmployeeViewPlain rsx, near the summary boxes ~line 250-350; EmployeeView wrapper reads EMPLOYEE_STORE and passes props to EmployeeViewPlain). The wrapper component (EmployeeView, ~line 929) is the natural place to read AUTH + the store stat and pass into EmployeeViewPlain as a new prop.

From shifty-dioxus/src/service/employee.rs (EMPLOYEE_STORE — add the stat here):
```rust
pub struct EmployeeStore { pub year: u32, pub until_week: u8, pub employee: Employee, /* working_hours_by_week ... */ }
// Loaded via EmployeeAction (LoadEmployeeDataUntilNow { sales_person_id }) in employee_details.rs.
```

From shifty-dioxus/src/base_types.rs: `format_hours(f32) -> String` (used across employee_view for hour display).

SSR test idiom (component/employee_short.rs:99):
```rust
fn render(comp: fn() -> Element) -> String {
    let mut vdom = VirtualDom::new(comp);
    vdom.rebuild_in_place();
    dioxus_ssr::render(&vdom)
}
```

i18n key/translation idiom (i18n/de.rs): `i18n.add_text(Locale::De, Key::SomeKey, "Text");`. Key enum lives in i18n/mod.rs. Lookup: `i18n.t(Key::SomeKey)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API fetch fn + store wiring + i18n keys (De/En/Cs)</name>
  <files>shifty-dioxus/src/api.rs, shifty-dioxus/src/state/employee.rs, shifty-dioxus/src/service/employee.rs, shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <action>
    A. api.rs:
       1. Import `EmployeeWeeklyStatisticsTO` from rest_types (add to the existing `use rest_types::{...}` list).
       2. Add `pub async fn get_employee_weekly_statistics(config: Config, sales_person_id: Uuid) -> Result<Rc<EmployeeWeeklyStatisticsTO>, reqwest::Error>` mirroring get_employee_reports: url = `format!("{}/report/{}/weekly-statistics", config.backend, sales_person_id)`, `reqwest::get(url).await?`, `error_for_status_ref()?` (403 for non-HR → Err, which the caller swallows), `response.json().await?`.
    B. state/employee.rs: add a field to the Employee state OR a sibling — simplest: store the raw TO. Add `pub weekly_statistics: Option<Rc<EmployeeWeeklyStatisticsTO>>` to the `EmployeeStore` (NOT Employee, to avoid touching the report→Employee From impl). If EmployeeStore lives in service/employee.rs, do it there instead — put the field where EmployeeStore is defined (grep `pub struct EmployeeStore`). Default it to None in the Default/initializer.
    C. service/employee.rs: in the EmployeeAction handler that loads employee data (LoadEmployeeDataUntilNow / wherever get_employee_reports is awaited), after the report loads, additionally fetch the stat: call `api::get_employee_weekly_statistics(config, sales_person_id).await` and set `store.weekly_statistics = result.ok();` (Ok → Some, Err/403 → None, so non-HR simply gets None and the block stays hidden — defence in depth alongside the is_hr gate). Keep the existing report load behaviour unchanged.
    D. i18n: add Key variants to i18n/mod.rs enum: `StatisticsHeading`, `AverageWorkedHoursPerWeek`, `StatisticsIncludedWeeks`. Add translations to all three locales:
       - En: "Statistics" / "Average worked hours per week" / "Included weeks"
       - De: "Statistik" / "Durchschnittlich gearbeitete Stunden pro Woche" / "Einbezogene Wochen"
       - Cs: "Statistika" / "Průměrně odpracované hodiny za týden" / "Započítané týdny"
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo build 2>&1 | tail -20</automated>
  </verify>
  <done>Frontend compiles; api::get_employee_weekly_statistics exists; EmployeeStore has weekly_statistics; service fetches it on load; three new i18n keys exist in En/De/Cs.</done>
</task>

<task type="auto">
  <name>Task 2: HR-only stats block in EmployeeView + should_show_hr_stats helper + SSR visibility test</name>
  <files>shifty-dioxus/src/component/employee_view.rs</files>
  <behavior>
    - should_show_hr_stats(is_hr: bool, stats: Option<&EmployeeWeeklyStatisticsTO>) -> bool returns true ONLY when is_hr && stats.is_some().
    - SSR: when the block is rendered with is_hr=true + Some(stats), output contains the heading text and the formatted average; with is_hr=false the block markup is absent.
  </behavior>
  <action>
    1. Add `pub(crate) fn should_show_hr_stats(is_hr: bool, stats: Option<&EmployeeWeeklyStatisticsTO>) -> bool { is_hr && stats.is_some() }` (pure, testable; D-22-05 belt-and-braces with the backend gate).
    2. In the `EmployeeView` wrapper component (~line 929, which reads EMPLOYEE_STORE), compute `let is_hr = AUTH.read().auth_info.as_ref().map(|a| a.has_privilege("hr")).unwrap_or(false);` and read `let stats = EMPLOYEE_STORE.read().weekly_statistics.clone();`. Pass both into EmployeeViewPlain as new props (`is_hr: bool`, `weekly_statistics: Option<Rc<EmployeeWeeklyStatisticsTO>>`). Add these to EmployeeViewPlainProps.
    3. In EmployeeViewPlain rsx, after the summary boxes block, add an HR-only block gated by `if should_show_hr_stats(props.is_hr, props.weekly_statistics.as_deref()) { ... }`. Render a heading `i18n.t(Key::StatisticsHeading)` and a TupleRow (existing atom) with label `i18n.t(Key::AverageWorkedHoursPerWeek)` and value `format_hours(stats.average_worked_hours_per_week)` (font-mono tabular-nums to match siblings). Optionally a second row with Key::StatisticsIncludedWeeks → `included_weeks`. D-22-04: this lives inside EmployeeView, no new route.
    4. SSR test (mod tests, mirror employee_short render helper): write `hr_block_visible_when_hr_with_stats` rendering EmployeeViewPlain with is_hr=true + Some(stats with average_worked_hours_per_week=23.5) and assert the SSR html contains the heading and "23" (or the formatted value); write `hr_block_hidden_when_not_hr` with is_hr=false and assert the heading text is NOT present. Also add pure-fn tests for should_show_hr_stats covering all four (is_hr × stats) combinations. Build minimal Employee/props fixtures (reuse the existing make_week/test helpers; for stats use Rc::new(EmployeeWeeklyStatisticsTO { average_worked_hours_per_week: 23.5, included_weeks: 10, total_worked_hours: 235.0 })). Ensure the I18N global is initialized for the SSR render (follow how other render tests in this file/crate set up i18n; if i18n is uninitialized the key returns the key name — assert on a stable substring accordingly).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo test should_show_hr_stats hr_block 2>&1 | tail -25</automated>
  </verify>
  <done>should_show_hr_stats + SSR visibility tests pass; block renders for HR with stats, hidden otherwise; no new route added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→backend | WASM client fetches /report/{id}/weekly-statistics |
| auth-signal→UI | AUTH global signal drives is_hr gating of the block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Information Disclosure | HR-only block client-side gating | mitigate | Defence in depth: the backend endpoint is HR-gated (403 for non-HR → fetch returns None → block hidden). The is_hr client gate is the UX layer, not the security boundary; the data itself never reaches a non-HR client. |
| T-22-05 | Spoofing | client claims HR via tampered AUTH signal | accept | A tampered client signal only flips UI; the backend still 403s the fetch, so no real data is exposed. Trust boundary is server-side (22-01 T-22-01). |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo build` exits 0.
- `cd shifty-dioxus && cargo test` passes (incl. should_show_hr_stats + hr_block SSR tests).
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` exits 0 (WASM gate).
- `grep -c "AverageWorkedHoursPerWeek" shifty-dioxus/src/i18n/en.rs shifty-dioxus/src/i18n/de.rs shifty-dioxus/src/i18n/cs.rs` each ≥ 1.
- `grep -n "should_show_hr_stats\|has_privilege(\"hr\")" shifty-dioxus/src/component/employee_view.rs` shows the gate.
</verification>

<success_criteria>
- HR-only stats block on /employees/:id, visible with HR role, hidden without (STAT-01, D-22-04/D-22-05).
- Block shows average worked hours per week from the HR-gated backend endpoint (STAT-02, D-22-02).
- i18n present in De/En/Cs (D-22-08).
- Frontend SSR test proves visible-with-HR / hidden-without (D-22-08).
- `cd shifty-dioxus && cargo test` green + WASM build exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/22-mitarbeiter-statistik-hr/22-02-SUMMARY.md`.
</output>
