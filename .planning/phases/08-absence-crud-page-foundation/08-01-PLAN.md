---
phase: 08-absence-crud-page-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service/src/lib.rs
  - service/src/vacation_balance.rs
  - rest-types/src/lib.rs
autonomous: true
requirements: [FUI-A-04]
requirements_addressed: [FUI-A-04]
tags: [shifty-backend, vacation-balance, dto-foundation, service-trait, bl-tier]
must_haves:
  truths:
    - "Phase 8 ist Misch-Phase Backend + Frontend (D-02): dieser Plan schafft die Backend-Foundation für den Resturlaubs-Endpoint, der den `Backend bleibt unangetastet`-Hinweis aus ROADMAP.md überschreibt"
    - "Service-Trait `VacationBalanceService` existiert mit `get(sales_person_id, year, ctx, tx)` und `get_team(year, ctx, tx)` plus Domain-Struct `VacationBalance` (per D-04)"
    - "DTO `VacationBalanceTO` existiert in rest-types mit Feldern `sales_person_id, year, entitled_days, carryover_days, used_days, planned_days, remaining_days` und `ToSchema`-Derive (per D-03)"
    - "Frontend kann rest-types weiterhin mit `default-features = false` ziehen — service-impl-Feature-Pull-In bleibt geschlossen (per STATE.md Constraint, Pitfall 1)"
    - "automock-Annotation auf dem Trait erzeugt `MockVacationBalanceService` für späteren Plan-02 Tests (per D-04)"
  artifacts:
    - path: service/src/vacation_balance.rs
      provides: "VacationBalanceService Trait + VacationBalance Domain-Struct + automock"
      min_lines: 50
      contains: "trait VacationBalanceService"
    - path: service/src/lib.rs
      provides: "pub mod vacation_balance Eintrag"
      contains: "pub mod vacation_balance"
    - path: rest-types/src/lib.rs
      provides: "VacationBalanceTO DTO mit ToSchema + From-Impl gefeatured auf service-impl"
      contains: "struct VacationBalanceTO"
  key_links:
    - from: service/src/lib.rs
      to: service/src/vacation_balance.rs
      via: "pub mod vacation_balance;"
      pattern: "pub mod vacation_balance"
    - from: rest-types/src/lib.rs
      to: service::vacation_balance::VacationBalance
      via: "#[cfg(feature = \"service-impl\")] From-Impl"
      pattern: "impl From<&service::vacation_balance::VacationBalance>"
---

<objective>
Wave-1-Foundation: Definiere die Service-Trait-Schnittstelle (`VacationBalanceService`) inkl. Domain-Struct (`VacationBalance`) und das Wire-DTO (`VacationBalanceTO`) für den neuen Resturlaubs-Endpoint.

Purpose: Diese Plan setzt die Contracts, gegen die Plan 02 (Service-Impl + REST + DI) implementiert. Interface-First-Order — Plan 02 darf erst starten, wenn Trait + DTO stehen, weil sowohl `service_impl/vacation_balance.rs` als auch `rest/vacation_balance.rs` als auch alle Tests gegen diese Symbole bauen.

Output:
- `service/src/vacation_balance.rs` mit `VacationBalanceService`-Trait + `VacationBalance`-Domain-Struct + `automock`-Setup
- `service/src/lib.rs` Modul-Eintrag
- `rest-types/src/lib.rs` `VacationBalanceTO` Struct + `From`-Impls (gefeatured `service-impl`)
- `cargo check --workspace` grün
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

## Project Constraints (verbindlich)

- **VCS:** jj-only. Niemals `git commit`/`git add` aus dem Agent. GSD Auto-Commit ist `commit_docs: false`. Nutze die `jj-commit`-Skill oder `jj` direkt:
  ```fish
  jj describe -m "feat(08): introduce VacationBalanceService trait + VacationBalanceTO"
  jj new
  ```
  GSD ruft KEIN `git commit`. User entscheidet, ob er Commit + `jj new` oder nur `jj describe` will.
- **NixOS:** `nix develop` (NICHT `nix-shell` — `shell.nix` ist kaputt). Cargo-Befehle laufen innerhalb der nix-shell.
- **Service-Tier:** VacationBalanceService ist BL-Tier (D-04). Trait selbst hat keine Dep-Constraints — Constraints kommen erst in Plan 02 mit `gen_service_impl!`.
- **rest-types Cross-Crate:** Frontend zieht rest-types mit `default-features = false`. Alle `service`-Bridge-Code in rest-types MUSS `#[cfg(feature = "service-impl")]` haben (Pitfall 1, sonst WASM-Build kaputt).
- **OpenAPI:** `ToSchema`-Derive auf `VacationBalanceTO` Pflicht. Schema-Registrierung im REST-Layer (Plan 02).

## D-Mapping

- D-03: Backend liefert Resturlaubs-Endpoint — DTO-Foundation hier; REST-Endpoint in Plan 02.
- D-04: Service ist BL-Tier — Trait-Form folgt `service/src/absence.rs` Struktur (`Authentication<Self::Context>`, `Option<Self::Transaction>`, automock).
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-absence-crud-page-foundation/08-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md
@.planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md
@CLAUDE.md
@CLAUDE.local.md
@shifty-dioxus/CLAUDE.md

<interfaces>
<!-- Reference Trait — service/src/absence.rs (vollständig analog) -->

From service/src/absence.rs (Auszug — Trait-Form):
```rust
use std::sync::Arc;
use async_trait::async_trait;
use mockall::automock;
use uuid::Uuid;
use crate::{permission::Authentication, ServiceError};

#[derive(Clone, Debug, PartialEq)]
pub struct AbsencePeriod {
    pub id: Uuid,
    pub sales_person_id: Uuid,
    pub category: AbsenceCategory,
    pub from_date: time::Date,
    pub to_date: time::Date,
    pub description: Arc<str>,
    pub created: Option<time::PrimitiveDateTime>,
    pub deleted: Option<time::PrimitiveDateTime>,
    pub version: Uuid,
}

#[automock(type Context=(); type Transaction=dao::MockTransaction;)]
#[async_trait]
pub trait AbsenceService {
    type Context: Clone + std::fmt::Debug + PartialEq + Eq + Send + Sync + 'static;
    type Transaction: dao::Transaction;

    async fn find_all(
        &self,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<Arc<[AbsencePeriod]>, ServiceError>;
    // ... weitere Methoden
}
```

From rest-types/src/lib.rs (Auszug — DTO-Form mit Feature-Gating):
```rust
#[derive(Clone, Debug, Serialize, Deserialize, ToSchema)]
pub struct AbsencePeriodTO {
    #[serde(default)]
    pub id: Uuid,
    pub sales_person_id: Uuid,
    pub category: AbsenceCategoryTO,
    #[schema(value_type = String, format = "date")]
    pub from_date: time::Date,
    // ...
}

#[cfg(feature = "service-impl")]
impl From<&service::absence::AbsencePeriod> for AbsencePeriodTO {
    fn from(a: &service::absence::AbsencePeriod) -> Self { /* ... */ }
}

#[cfg(feature = "service-impl")]
impl From<&AbsencePeriodTO> for service::absence::AbsencePeriod { /* ... */ }
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: VacationBalance-Domain-Struct + Trait in service/src/vacation_balance.rs</name>

  <read_first>
    - service/src/absence.rs (gesamte Datei, ist die exakte Vorlage — Trait-Form, automock-Annotation, AbsencePeriod-Struct, Authentication/Transaction-Generics; Pflicht-Read damit das neue File 1:1 dem Pattern folgt)
    - service/src/lib.rs (um zu sehen, wie die existierenden `pub mod`-Einträge sortiert sind und wo der neue Eintrag hinkommt — die Datei lädt ALLE Module)
    - service/src/permission.rs (für `Authentication<T>`, `HR_PRIVILEGE`-Konstante)
    - service/src/lib.rs `ServiceError` (existing) damit klar ist, welche Error-Varianten zur Verfügung stehen
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Code Example 3" und § "Pattern 5" (Trait-Form + erwartete Domain-Struct-Felder)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "service/src/vacation_balance.rs" (exakter Trait-Excerpt)
    - shifty-backend/CLAUDE.md § "Service-Tier-Konventionen" (D-04: BL-Tier-Bestätigung; im Trait-Layer nur strukturell relevant)
  </read_first>

  <files>service/src/vacation_balance.rs (NEW), service/src/lib.rs (modify)</files>

  <action>
    Erstelle `service/src/vacation_balance.rs` analog `service/src/absence.rs`:

    1. **Imports** (kopiert von absence.rs Header — `std::sync::Arc`, `async_trait::async_trait`, `mockall::automock`, `time::Date`, `uuid::Uuid`, `crate::{permission::Authentication, ServiceError}`).

    2. **Domain-Struct `VacationBalance`:**
       ```rust
       #[derive(Clone, Debug, PartialEq)]
       pub struct VacationBalance {
           pub sales_person_id: Uuid,
           pub year: u32,
           pub entitled_days: f32,    // from EmployeeWorkDetailsService.vacation_days_for_year
           pub carryover_days: i32,   // from CarryoverService.get_carryover().vacation
           pub used_days: f32,        // sum of past Vacation AbsencePeriods (filter to_date < today)
           pub planned_days: f32,     // sum of future Vacation AbsencePeriods (filter from_date >= today)
           pub remaining_days: f32,   // entitled + carryover − (used + planned)
       }
       ```
       Begründung der Felder: UI-SPEC § "VacationEntitlementCard" zeigt fünf Stats (Vertrag/Übertrag/Genommen/Beantragt/Verbleibend), korrespondiert 1:1 mit i18n-Keys `VacationStatContract`, `VacationStatCarryover`, `VacationStatUsed`, `VacationStatPending`, `VacationStatRemaining` (UI-SPEC Copywriting Tabelle Z. 256-260). Dokumentations-Kommentare an JEDEM Feld die Quelle nennen wie oben.

    3. **Trait `VacationBalanceService`:**
       ```rust
       #[automock(type Context=(); type Transaction=dao::MockTransaction;)]
       #[async_trait]
       pub trait VacationBalanceService {
           type Context: Clone + std::fmt::Debug + PartialEq + Eq + Send + Sync + 'static;
           type Transaction: dao::Transaction;

           /// HR ∨ self — liefert Resturlaubs-Aggregat für genau einen Mitarbeiter.
           async fn get(
               &self,
               sales_person_id: Uuid,
               year: u32,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<VacationBalance, ServiceError>;

           /// HR-only — Aggregat für alle bezahlten Mitarbeiter (für VacationPerPersonList).
           async fn get_team(
               &self,
               year: u32,
               context: Authentication<Self::Context>,
               tx: Option<Self::Transaction>,
           ) -> Result<Arc<[VacationBalance]>, ServiceError>;
       }
       ```
       Doc-Kommentare an `get` und `get_team` müssen die Permission-Anforderung explizit nennen (`/// HR ∨ self`, `/// HR-only`) per D-04.

    4. **Modul-Registrierung:** In `service/src/lib.rs` füge `pub mod vacation_balance;` ein. Position: alphabetisch nach `pub mod sales_person_unavailable;` (oder analog zur existierenden Sortierung — schau dir die Datei an und füge sie an der passenden Stelle ein, sortiert oder nach existierender Konvention).

    5. **KEINE Service-Impl, KEIN gen_service_impl!, KEINE DI-Konstruktion** — das ist Plan 02. Hier nur Trait + Domain.
  </action>

  <verify>
    <automated>nix develop --command cargo check -p service 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `service/src/vacation_balance.rs` existiert
    - `grep -c "pub trait VacationBalanceService" service/src/vacation_balance.rs` ergibt 1
    - `grep -c "pub struct VacationBalance " service/src/vacation_balance.rs` ergibt mindestens 1
    - `grep -E "entitled_days|carryover_days|used_days|planned_days|remaining_days" service/src/vacation_balance.rs | grep -vc '^#'` ergibt mindestens 5 (alle fünf Felder vorhanden in struct-Body)
    - `grep -c "automock" service/src/vacation_balance.rs` ergibt mindestens 1
    - `grep -E "async fn get\b|async fn get_team\b" service/src/vacation_balance.rs` zeigt beide Methoden
    - `grep -c "pub mod vacation_balance" service/src/lib.rs` ergibt 1
    - `nix develop --command cargo check -p service` Exit-Code 0
  </acceptance_criteria>

  <done>service/src/vacation_balance.rs enthält Trait + Domain-Struct, lib.rs registriert das Modul, `cargo check -p service` ist grün.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: VacationBalanceTO + From-Impls (gefeatured) in rest-types/src/lib.rs</name>

  <read_first>
    - rest-types/src/lib.rs Z. 1565..2040 (gesamter AbsencePeriodTO-Block — DTO-Form, Feature-Gating, From-Impls — exakte Vorlage)
    - rest-types/Cargo.toml (`service-impl`-Feature-Definition, default-features-Logik — Plan darf das nicht verändern)
    - shifty-backend/shifty-dioxus/Cargo.toml Z. 28-30 (`rest-types = { path = "../rest-types", default-features = false }` — Plan darf das nicht verändern)
    - service/src/vacation_balance.rs (das Output von Task 1 — die From-Impls referenzieren VacationBalance aus diesem Modul)
    - .planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md § "Pitfall 1" (Cross-Crate-Feature-Pull-In)
    - .planning/phases/08-absence-crud-page-foundation/08-PATTERNS.md § "rest-types/src/lib.rs (dto)" (Apply-Pattern)
  </read_first>

  <files>rest-types/src/lib.rs (modify)</files>

  <action>
    Ergänze `rest-types/src/lib.rs` mit `VacationBalanceTO` direkt nach dem AbsencePeriod-Block (also nach den existierenden absence-DTOs, ca. Z. 2040):

    1. **DTO-Struct:**
       ```rust
       #[derive(Clone, Debug, Serialize, Deserialize, ToSchema, PartialEq)]
       pub struct VacationBalanceTO {
           pub sales_person_id: Uuid,
           pub year: u32,
           pub entitled_days: f32,
           pub carryover_days: i32,
           pub used_days: f32,
           pub planned_days: f32,
           pub remaining_days: f32,
       }
       ```
       KEIN `$version`-Field (read-only Aggregat, kein Optimistic-Locking — siehe PATTERNS.md "Apply to VacationBalanceTO"). Doc-Kommentar pro Feld optional, aber Struct-Header-Kommentar zwingend mit Verweis auf D-03 + UI-SPEC.

    2. **From-Impls gefeatured:**
       ```rust
       #[cfg(feature = "service-impl")]
       impl From<&service::vacation_balance::VacationBalance> for VacationBalanceTO {
           fn from(v: &service::vacation_balance::VacationBalance) -> Self {
               Self {
                   sales_person_id: v.sales_person_id,
                   year: v.year,
                   entitled_days: v.entitled_days,
                   carryover_days: v.carryover_days,
                   used_days: v.used_days,
                   planned_days: v.planned_days,
                   remaining_days: v.remaining_days,
               }
           }
       }
       ```
       Reverse-Impl `From<&VacationBalanceTO> for service::vacation_balance::VacationBalance` analog (für Tests + Konsistenz mit AbsencePeriodTO-Pattern). Beide MÜSSEN `#[cfg(feature = "service-impl")]` tragen — sonst pull-in im Frontend-WASM-Build (Pitfall 1).

    3. **KEINE Änderung** an `rest-types/Cargo.toml` (kein neues Feature, das `service-impl` schon existiert), KEINE Änderung an `shifty-dioxus/Cargo.toml`.

    4. **OpenAPI-Schema-Registrierung in `rest/src/lib.rs`** ist NICHT hier — kommt in Plan 02.
  </action>

  <verify>
    <automated>nix develop --command cargo check -p rest-types --features service-impl 2>&1 | tail -20 &amp;&amp; nix develop --command cargo check -p rest-types --no-default-features 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pub struct VacationBalanceTO" rest-types/src/lib.rs` ergibt 1
    - `grep -c "ToSchema" rest-types/src/lib.rs | xargs -I{} test {} -ge 2` (mindestens das absence-DTO + neues + ggf. mehr) — alternativ direkt: Zeile mit `pub struct VacationBalanceTO` ist in einem Block der `derive(...)` mit `ToSchema` enthält (`grep -B1 "pub struct VacationBalanceTO" rest-types/src/lib.rs | grep -c ToSchema` ≥ 1)
    - `grep -c "feature = \"service-impl\"" rest-types/src/lib.rs | xargs test 0 -lt` (Feature-Gates existieren); spezifisch: mindestens 2 neue `#[cfg(feature = "service-impl")]`-Blocks für die From-Impls (vorher-nachher diff)
    - `grep -c "impl From<&service::vacation_balance::VacationBalance>" rest-types/src/lib.rs` ergibt 1
    - `grep -c "impl From<&VacationBalanceTO> for service::vacation_balance::VacationBalance" rest-types/src/lib.rs` ergibt 1
    - `nix develop --command cargo check -p rest-types --features service-impl` Exit-Code 0
    - `nix develop --command cargo check -p rest-types --no-default-features` Exit-Code 0 (KEIN service-impl-Pull-In)
    - `nix develop --command cargo check --workspace` Exit-Code 0 (Plan 01 darf nichts brechen)
  </acceptance_criteria>

  <done>VacationBalanceTO ist im rest-types-Crate definiert, From-Impls korrekt feature-gated, beide cargo check-Varianten (mit und ohne service-impl) sind grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST | (außerhalb Plan-01-Scope; relevant für Plan 02 Permission-Gates) |
| service-trait Surface | Authentication<Context> wird in Trait propagiert — Plan 02 implementiert Gates; Plan 01 sorgt nur dafür, dass Signatur Authentication-Parameter trägt |
| Cross-Crate WASM-Boundary | rest-types darf NICHT versehentlich `service`-Crate in Frontend-Build ziehen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-01 | Elevation of Privilege | VacationBalanceService.get | mitigate (in Plan 02) | Trait-Signatur trägt `Authentication<Self::Context>` (Plan 01 sichert Parameter); Permission-Enforcement (HR ∨ self) erfolgt in Plan 02 (Service-Impl) |
| T-8-AUTH-02 | Elevation of Privilege | VacationBalanceService.get_team | mitigate (in Plan 02) | Trait-Doc deklariert "HR-only"; Plan 02 enforced via `permission_service.check_permission(HR_PRIVILEGE, ...)` |
| T-8-IDOR-01 | Information Disclosure | get(sales_person_id, ...) | mitigate (in Plan 02) | Trait-Signatur erlaubt Owner-Resolve via `verify_user_is_sales_person`; Plan 02 implementiert |
| T-8-XSS-01 | Tampering | n/a | n/a | Plan 01 erstellt nur Daten-Struct mit numerischen Feldern (`f32`, `i32`, `Uuid`, `u32`) — kein User-String-Field; XSS-Surface = 0 |
| T-8-INJECTION-01 | Injection | n/a | n/a | Plan 01 hat keinen i18n-, SQL- oder String-Interpolations-Path |
| T-8-WASM-CONTAINMENT | Information Disclosure | rest-types Cross-Crate Surface | mitigate | Alle From-Impls auf VacationBalanceTO MÜSSEN `#[cfg(feature = "service-impl")]` tragen — verhindert WASM-Build-Pull-In von Backend-Crates (STATE.md Constraint, Pitfall 1) |
</threat_model>

<verification>
**Layer 1 — Compile:**
- `nix develop --command cargo check -p service` grün
- `nix develop --command cargo check -p rest-types --features service-impl` grün
- `nix develop --command cargo check -p rest-types --no-default-features` grün (Crucial — verifiziert WASM-Compat-Pfad)
- `nix develop --command cargo check --workspace` grün (kein Side-Effect auf andere Crates)

**Layer 2 — Trait-Surface:**
- `cargo doc -p service --no-deps --document-private-items` zeigt VacationBalanceService Trait + VacationBalance Struct ohne Errors
- `MockVacationBalanceService` ist in `service::vacation_balance` automock-generiert (wird in Plan 02 Tests zum Mock-Setup verwendet)

**Layer 3 — Cross-Crate-Sanity:**
- `cargo build --target wasm32-unknown-unknown --manifest-path shifty-dioxus/Cargo.toml` (siehe Plan 05 für vollen WASM-Test) — Plan 01 darf den WASM-Pfad nicht brechen, weil shifty-dioxus rest-types ohne service-impl-Feature zieht.

**KEIN Test-Code in Plan 01** — Tests landen in Plan 02 (`service_impl/src/test/vacation_balance.rs`), weil dort der MockVacationBalanceService konsumiert wird.
</verification>

<success_criteria>
- service/src/vacation_balance.rs enthält VacationBalanceService Trait mit `get` und `get_team` plus VacationBalance Domain-Struct mit 7 Feldern
- service/src/lib.rs registriert das neue Modul
- rest-types/src/lib.rs definiert VacationBalanceTO mit ToSchema-Derive
- Beide From-Impls sind hinter `#[cfg(feature = "service-impl")]` gegated
- `cargo check -p service` und `cargo check -p rest-types --no-default-features` sind grün
- Plan 02 kann gegen die hier definierten Symbole bauen, ohne dass Trait-Form oder DTO-Form revisitiert werden müssen
</success_criteria>

<output>
After completion, create `.planning/phases/08-absence-crud-page-foundation/08-01-SUMMARY.md`.

Per `commit_docs: false` und jj-only-VCS: KEIN automatischer Commit. User entscheidet manuell mit `jj describe -m "feat(08): introduce VacationBalanceService trait + VacationBalanceTO"` ob commit erfolgt.
</output>
</content>
</invoke>