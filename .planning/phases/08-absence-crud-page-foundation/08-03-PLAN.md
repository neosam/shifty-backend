---
phase: 08-absence-crud-page-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - rest/snapshots
autonomous: false
requirements: [FUI-A-04]
requirements_addressed: [FUI-A-04]
tags: [shifty-backend, openapi-snapshot, insta]
must_haves:
  truths:
    - "OpenAPI-insta-Snapshot ist konsistent mit der neuen REST-Surface (`/vacation-balance/*` ist im Snapshot enthalten) — D-03 Backend-Surface gepinnt"
    - "Snapshot-Test wird in 3 aufeinanderfolgenden Runs deterministisch grün (kein Flake aus Hash-Map-Iteration)"
    - "User hat den Snapshot-Diff visuell verifiziert und akzeptiert (Checkpoint:human-verify)"
  artifacts:
    - path: rest/snapshots
      provides: "Aktualisierte OpenAPI-insta-Snapshot-Files mit /vacation-balance/* enthalten"
      contains: "vacation-balance"
  key_links:
    - from: rest/src/lib.rs
      to: rest/snapshots/*.snap
      via: "insta-Test, der OpenAPI-JSON gegen Snapshot diffed"
      pattern: "vacation-balance"
---

<objective>
Wave-3-Closure für den Backend-Track: Refresh des OpenAPI-insta-Snapshots, sodass die neue REST-Surface (`/vacation-balance/*`) gepinnt ist. v1.0 Phase 4 hat den Snapshot eingeführt; jede Backend-Surface-Änderung MUSS ihn refreshen (RESEARCH.md Open Question 5 + Pitfall-Reminder in PATTERNS.md).

Purpose: Verhindert dass spätere Phasen versehentliche Surface-Drifts einführen. Dient gleichzeitig als Sanity-Check, dass Plan 02 die Endpoints korrekt registriert hat.

Output:
- `rest/snapshots/*.snap` Update mit `/vacation-balance/{sales_person_id}/{year}` und `/vacation-balance/team/{year}` Path-Einträgen
- 3-Run-Determinism verified
- User-Acknowledge auf den Diff
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

## Project Constraints

- **VCS:** jj-only. Snapshot-Files committen mit `jj describe -m "test(08): refresh OpenAPI insta snapshot for /vacation-balance"`.
- **NixOS:** `nix develop` für `cargo`/`cargo-insta`.
- **insta-Tooling:** `cargo insta accept` ist der Standard-Flow für Snapshot-Refresh.
- **Determinism:** v1.0 Phase 4 hat verifiziert, dass der Snapshot 3-Run-stabil ist. Wenn der neue Snapshot zwischen Runs flackert (z. B. wegen HashMap-Iteration), MUSS die Ursache (sortierte Keys?) eskaliert werden — NICHT einfach den Snapshot mehrmals refreshen.
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-absence-crud-page-foundation/08-CONTEXT.md
@.planning/phases/08-absence-crud-page-foundation/08-RESEARCH.md
@.planning/phases/08-absence-crud-page-foundation/08-VALIDATION.md
@.planning/phases/08-absence-crud-page-foundation/08-02-PLAN.md
@CLAUDE.md
@CLAUDE.local.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Locate + run OpenAPI snapshot test, then accept the diff</name>

  <read_first>
    - rest/Cargo.toml (verifizieren dass `insta` als dev-dependency drin ist)
    - rest/src/lib.rs (existing OpenAPI-`paths(...)`-Block — findet die ApiDoc-Struktur)
    - .planning/milestones/v1.0-phases/04-* SUMMARYs ODER `rest/tests/`-Verzeichnis (für die existierende Snapshot-Test-Funktion; suche Test-Funktion via `grep -r "insta" rest/`)
    - rest/snapshots/ falls existiert ODER `rest/tests/snapshots/` (existing Snapshot-Files für Bestand)
  </read_first>

  <files>rest/snapshots/*.snap (oder rest/tests/snapshots/*.snap, abhängig vom Test-Layout)</files>

  <action>
    1. **Lokalisieren:** Im Repo-Root: `grep -rn "insta::assert" rest/ 2>/dev/null` und `find . -name '*.snap' -path '*/rest/*' 2>/dev/null` (in `nix develop`). Identifiziere den OpenAPI-Snapshot-Test (vermutlich in `rest/tests/openapi.rs` oder `rest/src/lib.rs` mit `#[cfg(test)]`-Block).

    2. **Run ohne Akzeptanz:** `nix develop --command cargo test -p rest --test openapi 2>&1 | tail -50` (oder welcher Test-Name auch immer den Snapshot-Diff antreibt). Erwartung: Test schlägt fehl mit insta-Pending-Snapshot-Diff für die neuen `/vacation-balance/*`-Pfade.

    3. **Diff sichten:** `nix develop --command cargo insta pending-snapshots --workspace 2>&1` listet die Pending-Diffs. Wenn mehrere existieren → eskalieren (sollten nur die neuen Plan-02-Endpoints sein).

    4. **Accept:** `nix develop --command cargo insta accept --workspace`. Damit werden alle Pending-Snapshots übernommen.

    5. **3-Run-Determinism-Check:** Drei Runs hintereinander:
       ```bash
       nix develop --command cargo test -p rest --test openapi
       nix develop --command cargo test -p rest --test openapi
       nix develop --command cargo test -p rest --test openapi
       ```
       Alle drei MÜSSEN grün sein. Wenn ein Run rot ist → flake-Ursache untersuchen (vermutlich nicht-deterministische Path-Reihenfolge in `paths(...)` oder Schema-Ordering).

    6. **Final-Verify:** `nix develop --command cargo test --workspace` Exit-Code 0 — Full-Suite-Sanity.

    **W-6: KEIN silent-pass.** Snapshot-Test wird über `cargo test -p rest --test openapi` (oder analoger Test-Name) lokalisiert UND grün gemacht. Falls KEIN Snapshot-Test in `rest/tests/` existiert: ESKALATION — Task NICHT als done markieren; Snapshot-Test-Stub als Wave-0-Item nachziehen (analog VALIDATION.md Wave-0-Items). Eskalation-Pfad: dokumentiere im Task-Output, dass der erwartete OpenAPI-Snapshot-Test fehlt, und füge ein Wave-0-Item zu VALIDATION.md hinzu, bevor Plan 03 als abgeschlossen gilt.
  </action>

  <verify>
    <automated>nix develop --command cargo test --workspace 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - OpenAPI-Snapshot-Test wurde lokalisiert (`grep -rn "insta::assert" rest/` ODER `find . -name '*.snap' -path '*/rest/*'` zeigt mindestens 1 Treffer). Falls 0 Treffer → ESKALATION (W-6, kein silent-pass) — VALIDATION.md Wave-0 ergänzen, Task NICHT als done markieren.
    - Snapshot-Test ist nach `cargo insta accept` grün: `nix develop --command cargo test -p rest --test openapi` Exit-Code 0 (oder analoger Test-Name, sobald lokalisiert)
    - `find . -name '*.snap' -newer .planning/phases/08-absence-crud-page-foundation/08-02-PLAN.md 2>/dev/null` listet mindestens 1 File (Snapshot wurde aktualisiert)
    - `grep -r "vacation-balance" rest/snapshots rest/tests/snapshots 2>/dev/null | wc -l` ergibt mindestens 2 (zwei Path-Entries: `{sales_person_id}/{year}` und `team/{year}`)
    - 3-Run-Determinism: Drei aufeinanderfolgende `cargo test -p rest --test openapi` (oder analoger Test-Name) sind grün
    - `nix develop --command cargo test --workspace` Exit-Code 0
  </acceptance_criteria>

  <done>OpenAPI-Snapshot enthält /vacation-balance-Surface deterministisch; Full-Suite ist grün.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-Sichtung des Snapshot-Diffs (was wurde an der OpenAPI-Surface geändert?)</name>

  <action>Human checkpoint — siehe &lt;how-to-verify&gt; für die exakten Schritte. Agent pausiert bis User "approved" oder Abweichung beschreibt.</action>

  <what-built>Plan 02 hat zwei neue REST-Endpoints + ein neues DTO eingeführt. Plan 03 Task 1 hat den OpenAPI-Insta-Snapshot aktualisiert.</what-built>

  <how-to-verify>
    1. Sichte den Snapshot-Diff:
       ```bash
       jj diff -r @ rest/snapshots/ rest/tests/snapshots/ 2>/dev/null
       # ODER (falls bereits committed in Task 1):
       jj diff -r @- rest/snapshots/ rest/tests/snapshots/ 2>/dev/null
       ```
       (Pfad an dein Layout anpassen.)
    2. Prüfe, ob nur Folgendes hinzugefügt wurde:
       - Path-Definition für `GET /vacation-balance/{sales_person_id}/{year}` mit Tags `["VacationBalance"]`
       - Path-Definition für `GET /vacation-balance/team/{year}` mit Tags `["VacationBalance"]`
       - Schema-Definition `VacationBalanceTO` mit den 7 Feldern (sales_person_id, year, entitled_days, carryover_days, used_days, planned_days, remaining_days)
       - Tag-Definition `VacationBalance`
    3. Confirm: keine unbeabsichtigten Änderungen an existierenden Pfaden (z. B. `/absence-period`, `/booking`, `/sales-person`).
    4. Wenn unerwartete Änderungen sichtbar sind → STOPP, eskaliere; sonst Approve.
  </how-to-verify>

  <resume-signal>Type "approved" wenn Snapshot-Diff genau die zwei neuen Pfade + DTO + Tag enthält, sonst beschreibe die Abweichung.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot ↔ Implementation | OpenAPI-Snapshot ist die Pinned-Public-API-Surface. Bei jedem Refresh muss menschlich verifiziert werden, dass keine zusätzlichen Endpoints unbeabsichtigt exposed werden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-AUTH-01 | n/a | n/a | n/a | Plan 03 ist Snapshot-Refresh, kein neuer Permission-Path. AUTH-Threats sind in Plan 02 abgedeckt. |
| T-8-AUTH-02 | n/a | n/a | n/a | siehe oben |
| T-8-IDOR-01 | n/a | n/a | n/a | siehe oben |
| T-8-XSS-01 | n/a | n/a | n/a | siehe oben |
| T-8-INJECTION-01 | n/a | n/a | n/a | siehe oben |
| T-8-SURFACE-DRIFT | Information Disclosure | OpenAPI-Snapshot | mitigate | Task 2 (Checkpoint) verlangt expliziten Human-Review des Diffs — verhindert dass unbeabsichtigt Endpoints gepinnt werden. |
</threat_model>

<verification>
- `cargo test --workspace` grün (inkl. Snapshot-Test)
- 3-Run-Determinism: drei aufeinanderfolgende Snapshot-Tests grün
- Human-Verify: User hat den Diff gesichtet und bestätigt, dass nur die 2 neuen Pfade + DTO + Tag hinzugefügt wurden
</verification>

<success_criteria>
- OpenAPI-Snapshot konsistent mit Plan-02-REST-Surface
- 3-Run-Determinism bestätigt
- User-Approval auf Snapshot-Diff erfolgt
- Wave 3 (Backend-Track) ist abgeschlossen — Wave 4 (Frontend) kann starten
</success_criteria>

<output>
After completion, create `.planning/phases/08-absence-crud-page-foundation/08-03-SUMMARY.md`.

User commited manuell mit `jj describe -m "test(08): refresh OpenAPI insta snapshot for /vacation-balance"`.
</output>
</content>
</invoke>