---
phase: 09-booking-flow-reverse-warnings-copy-week
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/component/warning_list.rs
  - shifty-dioxus/src/component/mod.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: true
requirements: [FUI-A-05]

must_haves:
  truths:
    - "WarningList renders BookingOnAbsenceDay as Person+Date+Category text (D-09, D-10, D-11)"
    - "WarningList renders BookingOnUnavailableDay as Person+KW/Year+Weekday text (D-10, D-11)"
    - "WarningList renders PaidEmployeeLimitExceeded as current/max text (D-10)"
    - "WarningList is extracted from absences.rs into component/ as the single source of truth, shared by shiftplan.rs and absences.rs (D-09)"
    - "Empty warnings list renders nothing (no dialog content)"
    - "WarningList with suppress_header: true renders no internal header row"
    - "All 7 new i18n keys exist in en/de/cs; German strings use Locale::De; button wording Trotzdem buchen/Abbrechen + singular/plural headers (D-12)"
    - "api::book_slot_with_conflict_check posts to /shiftplan-edit/booking and returns BookingCreateResultTO (D-01)"
    - "Old POST /booking (rest/src/booking.rs create_booking) stays unchanged — regression-locked via grep (D-02)"
    - "Full Phase-8-style test set: SSR snapshots per warning variant, empty-array no-dialog, per-locale reference-matcher tests, i18n parity extension (D-14)"
  artifacts:
    - path: "shifty-dioxus/src/component/warning_list.rs"
      provides: "Shared WarningsList newtype + WarningList component with 3 booking variants + person_name + suppress_header props + SSR tests"
      contains: "pub fn WarningList"
    - path: "shifty-dioxus/src/api.rs"
      provides: "book_slot_with_conflict_check returning BookingCreateResultTO"
      contains: "book_slot_with_conflict_check"
    - path: "shifty-dioxus/src/loader.rs"
      provides: "register_user_to_slot_with_conflict_check returning (Uuid, Vec<WarningTO>)"
      contains: "register_user_to_slot_with_conflict_check"
  key_links:
    - from: "shifty-dioxus/src/api.rs"
      to: "POST /shiftplan-edit/booking"
      via: "reqwest post + response.json::<BookingCreateResultTO>"
      pattern: "shiftplan-edit/booking"
    - from: "shifty-dioxus/src/component/mod.rs"
      to: "component/warning_list.rs"
      via: "pub mod + pub use"
      pattern: "pub use warning_list::"
---

<objective>
Build the shared, reusable foundation for the booking-flow reverse-warnings feature (FUI-A-05): the conflict-aware booking API/loader functions, the extracted-and-extended shared `WarningList` component (rendering the 3 booking-path warning variants), and all 7 new i18n keys across en/de/cs.

Purpose: Plan 02 wires these into the shiftplan editor dialog. Defining the API contract, the shared component, and the i18n keys first means Plan 02 consumes finished interfaces with no scavenger hunt.

Output: `component/warning_list.rs` (new), `component/mod.rs` (re-export), `api.rs` (+book_slot_with_conflict_check, -nothing yet), `loader.rs` (+register_user_to_slot_with_conflict_check), 4 i18n files (+7 keys, all parity/reference-matcher tests). This plan does NOT touch `shiftplan.rs` and does NOT remove copy-week code (that is Plan 02).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<vcs_jj_only>
This repo is jj-managed (co-located with git). Do NOT run `git commit`/`git add`. The user commits manually with jj. Make all code/file changes; leave committing to the user. GSD auto-commit is disabled.
</vcs_jj_only>

<context>
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-CONTEXT.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-RESEARCH.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-PATTERNS.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-UI-SPEC.md

<interfaces>
<!-- Verified directly from source 2026-06-12. Executor uses these — no exploration needed. -->

From rest-types/src/lib.rs (WarningTO variant field shapes — VERIFIED):
```rust
pub enum WarningTO {
    BookingOnAbsenceDay  { booking_id: Uuid, date: time::Date, absence_id: Uuid, category: AbsenceCategoryTO },
    BookingOnUnavailableDay { booking_id: Uuid, year: u32, week: u8, day_of_week: DayOfWeekTO },
    AbsenceOverlapsBooking { absence_id: Uuid, booking_id: Uuid, date: time::Date },
    AbsenceOverlapsManualUnavailable { absence_id: Uuid, unavailable_id: Uuid },
    PaidEmployeeLimitExceeded { slot_id: Uuid, booking_id: Uuid, year: u32, week: u8, current_paid_count: u8, max_paid_employees: u8 },
}
pub struct BookingCreateResultTO { pub booking: BookingTO, pub warnings: Vec<WarningTO> }
// AbsenceCategoryTO has variants: Vacation, SickLeave, UnpaidLeave (snake_case wire tags)
// DayOfWeekTO has variants: Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday
```

Existing i18n Keys to reuse (VERIFIED present in mod.rs + all locales):
- `Key::Monday`, `Key::Tuesday`, `Key::Wednesday`, `Key::Thursday`, `Key::Friday`, `Key::Saturday`, `Key::Sunday`
- `Key::AbsenceCategoryVacation`, `Key::AbsenceCategorySickLeave`, `Key::AbsenceCategoryUnpaidLeave`
- `Key::AbsenceWarningHeaderSingular`, `Key::AbsenceWarningHeaderPlural` (header pattern reference; NOT reused for the booking dialog — booking uses its own header keys)

Existing api::add_booking signature (VERIFIED api.rs:197) — copy structure, change URL + return:
```rust
pub async fn add_booking(config: Config, sales_person_id: Uuid, slot_id: Uuid, week: u8, year: u32) -> Result<(), reqwest::Error>
// posts BookingTO{ id: Uuid::nil(), sales_person_id, slot_id, calendar_week: week as i32, year, created:None, deleted:None, created_by:None, deleted_by:None, version: Uuid::nil() } to "{backend}/booking"
```
KEEP add_booking unchanged in this plan (Plan 02 decides removal). The api.rs `use rest_types::{...}` block (api.rs:3) ALREADY imports `BookingTO`, `DayOfWeekTO`, `WarningTO` is NOT present yet, `BookingCreateResultTO` is NOT present yet — add both.

Existing WarningsList newtype + WarningList component live INLINE in page/absences.rs:163–244 (VERIFIED). They are MOVED to component/warning_list.rs in this plan. Current props: `{ warnings: WarningsList, dense: bool }`. Current match arms render only `AbsenceOverlapsBooking` + `AbsenceOverlapsManualUnavailable`; the other 3 variants hit a `_ =>` fallback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 7 booking-warning i18n keys across en/de/cs with parity + reference-matcher tests</name>
  <read_first>
    - shifty-dioxus/src/i18n/mod.rs (Key enum ~line 435 area for AbsenceCategory*/AbsenceWarning* keys; parity test `i18n_absence_keys_present_in_all_locales` ~line 660; reference-matcher `i18n_absence_keys_match_german_reference` ~line 710) — ANALOG per 09-PATTERNS.md
    - shifty-dioxus/src/i18n/de.rs (AbsenceWarning* block ~line 846; ShiftplanTakeLastWeek ~line 34) — ANALOG
    - shifty-dioxus/src/i18n/en.rs (line ~32 ShiftplanTakeLastWeek; AbsenceWarning* block)
    - shifty-dioxus/src/i18n/cs.rs (line ~34 ShiftplanTakeLastWeek; AbsenceWarning* block)
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-UI-SPEC.md (Copywriting Contract — canonical exact strings)
  </read_first>
  <behavior>
    - Test `i18n_booking_warning_keys_present_in_all_locales`: for each Locale in [En, De, Cs], generate(locale) and assert each of the 7 new keys resolves to a non-empty string that is not "??".
    - Test `i18n_booking_warning_keys_match_german_reference`: generate(Locale::De), assert BookingWarningDialogConfirm == "Trotzdem buchen", BookingWarningDialogCancel == "Abbrechen", BookingWarningDialogHeaderSingular == "Hinweis · 1 Konflikt" (Pitfall-1 guard — catches Locale::En accidentally used in de.rs). Also assert the de.rs BookingWarningOnAbsenceDay string contains the `{person}` placeholder (guards against dropping the person name back to a hardcoded "Mitarbeiter").
  </behavior>
  <action>
    Do NOT remove `ShiftplanTakeLastWeek` in this plan — that is Plan 02 (dead-code cleanup). Only ADD the 7 new keys.

    In `i18n/mod.rs` Key enum, add 7 variants directly after `AbsenceWarningOverlapsManual` (or the last AbsenceWarning* key): `BookingWarningDialogHeaderSingular`, `BookingWarningDialogHeaderPlural`, `BookingWarningDialogConfirm`, `BookingWarningDialogCancel`, `BookingWarningOnAbsenceDay`, `BookingWarningOnUnavailableDay`, `BookingWarningPaidLimitExceeded`.

    In `i18n/de.rs` (use `Locale::De` on EVERY add_text — Pitfall 1) add (exact strings from UI-SPEC Copywriting Contract; per-item booking strings MUST carry the `{person}` placeholder, NOT a hardcoded "Mitarbeiter"):
    - BookingWarningDialogHeaderSingular → "Hinweis · 1 Konflikt"
    - BookingWarningDialogHeaderPlural → "Hinweis · {count} Konflikte"
    - BookingWarningDialogConfirm → "Trotzdem buchen"
    - BookingWarningDialogCancel → "Abbrechen"
    - BookingWarningOnAbsenceDay → "{person} ist am {date} als {category} abwesend."
    - BookingWarningOnUnavailableDay → "{person} ist in KW {week}/{year} ({day}) als nicht verfügbar markiert."
    - BookingWarningPaidLimitExceeded → "Bezahlt-Limit überschritten ({current}/{max})."

    In `i18n/en.rs` (use `Locale::En`):
    - HeaderSingular → "Notice · 1 Conflict"; HeaderPlural → "Notice · {count} Conflicts"; Confirm → "Book anyway"; Cancel → "Cancel"
    - OnAbsenceDay → "{person} is absent on {date} as {category}."
    - OnUnavailableDay → "{person} is marked as unavailable in week {week}/{year} ({day})."
    - PaidLimitExceeded → "Paid employee limit exceeded ({current}/{max})."

    In `i18n/cs.rs` (use `Locale::Cs`):
    - HeaderSingular → "Upozornění · 1 konflikt"; HeaderPlural → "Upozornění · {count} konflikty"; Confirm → "Zarezervovat stejně"; Cancel → "Zrušit"
    - OnAbsenceDay → "{person} je dne {date} nepřítomen jako {category}."
    - OnUnavailableDay → "{person} je v týdnu {week}/{year} ({day}) označen jako nedostupný."
    - PaidLimitExceeded → "Překročen limit placených zaměstnanců ({current}/{max})."

    In `i18n/mod.rs` `#[cfg(test)]` module add two tests (copy structure from `i18n_absence_keys_present_in_all_locales` and `i18n_absence_keys_match_german_reference`): `i18n_booking_warning_keys_present_in_all_locales` (per behavior) and `i18n_booking_warning_keys_match_german_reference` (per behavior — including the `{person}` placeholder assertion). Also extend the existing i18n parity loop/test group at `i18n/mod.rs:660+` if it enumerates keys explicitly so the new keys are covered.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test i18n_booking_warning</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BookingWarningDialogHeaderSingular\|BookingWarningDialogHeaderPlural\|BookingWarningDialogConfirm\|BookingWarningDialogCancel\|BookingWarningOnAbsenceDay\|BookingWarningOnUnavailableDay\|BookingWarningPaidLimitExceeded" shifty-dioxus/src/i18n/mod.rs` returns >= 14 (7 enum + at least 7 in tests)
    - `grep -c "Locale::De, Key::BookingWarning" shifty-dioxus/src/i18n/de.rs` returns 7
    - `grep -c "Locale::En, Key::BookingWarning" shifty-dioxus/src/i18n/en.rs` returns 7
    - `grep -c "Locale::Cs, Key::BookingWarning" shifty-dioxus/src/i18n/cs.rs` returns 7
    - `grep -c "Locale::En, Key::BookingWarning" shifty-dioxus/src/i18n/de.rs` returns 0 (Pitfall-1 guard)
    - The de.rs BookingWarningOnAbsenceDay string contains the `{person}` placeholder: `grep -A2 "Key::BookingWarningOnAbsenceDay" shifty-dioxus/src/i18n/de.rs | grep -c "{person}"` returns >= 1 (guards against shipping a hardcoded "Mitarbeiter" that drops the person name)
    - `grep -c "Mitarbeiter ist am {date}" shifty-dioxus/src/i18n/de.rs` returns 0 (no hardcoded-person regression)
    - `cd shifty-dioxus && cargo test i18n_booking_warning` exits 0 with both new tests passing (incl. the `{person}` reference-matcher assertion)
  </acceptance_criteria>
  <done>7 booking-warning keys exist in mod.rs enum and all 3 locale files with correct per-locale Locale arg; the de.rs booking-warning strings use the `{person}` placeholder; parity + reference-matcher tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared component/warning_list.rs (moved + extended with 3 booking variants + suppress_header) and re-export it</name>
  <read_first>
    - shifty-dioxus/src/page/absences.rs:163-244 (WarningsList newtype + WarningListProps + WarningList component — the EXACT source to MOVE) — ANALOG per 09-PATTERNS.md
    - shifty-dioxus/src/page/absences.rs:1989-2001 (render() + pin_de_locale() SSR fixture — replicate) — ANALOG
    - shifty-dioxus/src/page/absences.rs:112-122 (existing AbsenceCategory→Key style helper for reference; note it uses the STATE AbsenceCategory not AbsenceCategoryTO)
    - shifty-dioxus/src/component/mod.rs (pub mod / pub use style)
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-UI-SPEC.md (Component Contracts §2 — exact Tailwind classes, p-4 normalisation, person_name prop, new match-arm text templates)
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-PATTERNS.md (warning_list.rs section — full skeleton incl. suppress_header)
  </read_first>
  <behavior>
    - `warning_list_renders_booking_on_absence_day`: render a WarningList wrapping `[WarningTO::BookingOnAbsenceDay { booking_id: Uuid::nil(), date: date!(2026-12-24), absence_id: Uuid::nil(), category: AbsenceCategoryTO::Vacation }]` with `person_name: Some("Maria".into())` under pin_de_locale → output contains "Maria" and "2026-12-24" and "Urlaub".
    - `warning_list_renders_booking_on_unavailable_day`: render `[BookingOnUnavailableDay { booking_id: Uuid::nil(), year: 2026, week: 12, day_of_week: DayOfWeekTO::Monday }]` with person_name Some("Maria") under pin_de_locale → output contains "KW 12/2026" and the German Monday string and "Maria".
    - `warning_list_renders_paid_limit_exceeded`: render `[PaidEmployeeLimitExceeded { slot_id: Uuid::nil(), booking_id: Uuid::nil(), year: 2026, week: 12, current_paid_count: 2, max_paid_employees: 3 }]` → output contains "2" and "3" (e.g. "(2/3)").
    - `warning_list_empty_renders_nothing`: render WarningList with `WarningsList::empty()` → rendered string contains no warning-box markup (no "border-l-[3px]").
    - `warning_list_suppress_header_hides_internal_header`: render WarningList with one warning and `suppress_header: true` under pin_de_locale → output contains NO internal header element (the `text-micro text-warn font-semibold uppercase` header div is absent) while the warning item text is still present.
  </behavior>
  <action>
    Create `shifty-dioxus/src/component/warning_list.rs`. MOVE the `WarningsList` newtype (with `PartialEq` via `Rc::ptr_eq`, plus `empty()`/`len()`/`is_empty()`), `WarningListProps`, and `WarningList` component out of `absences.rs` into this file. Imports per 09-PATTERNS.md: `use std::rc::Rc; use dioxus::prelude::*; use rest_types::{WarningTO, AbsenceCategoryTO, DayOfWeekTO}; use crate::{base_types::ImStr, i18n::{generate, Key, Locale, I18N}};`.

    Extend `WarningListProps` with TWO new props:
    - `#[props(default = None)] pub person_name: Option<ImStr>`
    - `#[props(default = false)] pub suppress_header: bool` — when `true`, the component skips rendering the internal header row. The booking Dialog (Plan 02) passes `suppress_header: true` because `Dialog.title` already provides the "Hinweis · N Konflikt(e)" header, avoiding a redundant double-header. absences.rs (non-dialog) callers keep the default `false` so the internal header is still shown.

    Per UI-SPEC Spacing section you MAY drop the `dense` prop since both paths normalise to `p-4`; if you keep it, normalise both branches to `"p-4"`. Container class must be exactly: `border-l-[3px] border-warn bg-warn-soft rounded-md p-4 flex flex-col gap-2`. Header class: `text-micro text-warn font-semibold uppercase`. Item list: `list-disc pl-4 text-body text-ink`. Keep the `count == 0 → return rsx! {}` early-return.

    Header rendering: wrap the header `div` in `if !props.suppress_header { ... }` so the `text-micro text-warn font-semibold uppercase` header row is only emitted when the prop is false. The item `ul` always renders.

    KEEP the two existing absence-path arms (`AbsenceOverlapsBooking`, `AbsenceOverlapsManualUnavailable`) so absences.rs callers still work. REPLACE the `_ =>` fallback with three explicit arms (text from UI-SPEC; resolve `person` from `props.person_name.as_deref().unwrap_or("–")`):
    - `BookingOnAbsenceDay { date, category, .. }` → `i18n.t(Key::BookingWarningOnAbsenceDay).as_ref().replace("{person}", person).replace("{date}", &date.to_string()).replace("{category}", &i18n.t(category_key(category)).to_string())`
    - `BookingOnUnavailableDay { week, year, day_of_week, .. }` → replace `{person}`, `{week}`, `{year}`, and `{day}` (via `i18n.t(day_of_week_key(day_of_week))`). Do NOT reconstruct a `time::Date` (Pitfall 6).
    - `PaidEmployeeLimitExceeded { current_paid_count, max_paid_employees, .. }` → replace `{current}`/`{max}` (no person needed).

    Add two private helpers in this file:
    - `fn category_key(c: &AbsenceCategoryTO) -> Key` mapping Vacation→AbsenceCategoryVacation, SickLeave→AbsenceCategorySickLeave, UnpaidLeave→AbsenceCategoryUnpaidLeave.
    - `fn day_of_week_key(d: &DayOfWeekTO) -> Key` mapping Monday→Key::Monday … Sunday→Key::Sunday.

    Add a `#[cfg(test)]` module: replicate `render()` + `pin_de_locale()` from absences.rs verbatim, then the 5 SSR tests per behavior (incl. `warning_list_suppress_header_hides_internal_header`). Use small `#[component]` test wrappers that call `pin_de_locale()` in a `use_hook` before rendering `WarningList` (the established pattern).

    In `component/mod.rs` add `pub mod warning_list;` (alphabetically among `pub mod` lines) and `pub use warning_list::{WarningList, WarningsList};` among the re-exports.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --package shifty-dioxus warning_list</automated>
  </verify>
  <acceptance_criteria>
    - File `shifty-dioxus/src/component/warning_list.rs` exists and contains `pub fn WarningList` and `pub struct WarningsList`
    - `grep -c "suppress_header" shifty-dioxus/src/component/warning_list.rs` returns >= 3 (prop declaration + `if !props.suppress_header` guard + at least one test usage)
    - `grep -c "border-l-\[3px\] border-warn bg-warn-soft rounded-md p-4" shifty-dioxus/src/component/warning_list.rs` returns >= 1 (p-4 normalisation per UI-SPEC)
    - `grep -c "WarningTO::BookingOnAbsenceDay\|WarningTO::BookingOnUnavailableDay\|WarningTO::PaidEmployeeLimitExceeded" shifty-dioxus/src/component/warning_list.rs` returns 3
    - `grep -v '^#' shifty-dioxus/src/component/warning_list.rs | grep -c 'i18n.format_date\|Date::from_iso_week_date\|to_iso_week_date'` returns 0 (Pitfall 6: no Date reconstruction in BookingOnUnavailableDay)
    - `grep -c "pub use warning_list::{WarningList, WarningsList}" shifty-dioxus/src/component/mod.rs` returns 1
    - The `warning_list_suppress_header_hides_internal_header` test asserts the SSR output with `suppress_header: true` contains NO `text-micro text-warn font-semibold uppercase` header element while the warning item text is still present
    - `cd shifty-dioxus && cargo test --package shifty-dioxus warning_list` exits 0 with the 5 new SSR tests passing
  </acceptance_criteria>
  <done>Shared WarningList component lives in component/warning_list.rs with the 3 booking variants + person_name + suppress_header props, re-exported from component/mod.rs; absences.rs no longer defines it inline; 5 SSR tests pass (incl. the suppress-header guard).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add conflict-aware api + loader functions; update absences.rs to import the moved component</name>
  <read_first>
    - shifty-dioxus/src/api.rs:1-13 (use rest_types block — add BookingCreateResultTO, WarningTO), :197-225 (add_booking analog) — ANALOG per 09-PATTERNS.md
    - shifty-dioxus/src/loader.rs:286-296 (register_user_to_slot analog) — ANALOG
    - shifty-dioxus/src/page/absences.rs:19-26 + :163-244 (imports + the now-removed inline WarningList — add import of the moved component)
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-PATTERNS.md (api.rs + loader.rs sections — full function skeletons)
  </read_first>
  <behavior>
    - No new unit test required for this glue task (pure HTTP wiring + import fix). Coverage is the compile gate + the existing absences.rs test suite continuing to pass after the import change. (Acceptable per Nyquist: Task 1 and Task 2 in this plan already provide automated verifies; this is not 3 consecutive verify-less tasks.)
  </behavior>
  <action>
    In `api.rs`: add `BookingCreateResultTO` and `WarningTO` to the `use rest_types::{...}` block (Pitfall 4 — they are not imported yet). Add a new function `book_slot_with_conflict_check(config: Config, sales_person_id: Uuid, slot_id: Uuid, week: u8, year: u32) -> Result<BookingCreateResultTO, reqwest::Error>` that posts the same `BookingTO` body (id/version `Uuid::nil()`, `calendar_week: week as i32`) to `format!("{}/shiftplan-edit/booking", config.backend)`, calls `response.error_for_status_ref()?`, then `let result: BookingCreateResultTO = response.json().await?; Ok(result)`. KEEP `add_booking` unchanged (Plan 02 decides removal). Do NOT touch `copy_week` here (Plan 02).

    In `loader.rs`: add `register_user_to_slot_with_conflict_check(config: Config, slot_id: uuid::Uuid, user_id: uuid::Uuid, week: u8, year: u32) -> Result<(uuid::Uuid, Vec<rest_types::WarningTO>), ShiftyError>` that calls `api::book_slot_with_conflict_check(config, user_id, slot_id, week, year).await?` and returns `Ok((result.booking.id, result.warnings))`. KEEP `register_user_to_slot` unchanged. Do NOT touch `copy_from_previous_week` here (Plan 02).

    In `page/absences.rs`: since Task 2 removed the inline `WarningsList`/`WarningListProps`/`WarningList` definitions, add `use crate::component::warning_list::{WarningList, WarningsList};` (or via `crate::component::{WarningList, WarningsList}`) and remove the now-orphaned inline definitions if Task 2 left them. The absence-path callers pass no `person_name` (defaults to None) and no `suppress_header` (defaults to false → internal header still shown) and rely on the two unchanged absence arms — verify they still compile.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --package shifty-dioxus</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "book_slot_with_conflict_check" shifty-dioxus/src/api.rs` returns >= 1
    - `grep -c "shiftplan-edit/booking" shifty-dioxus/src/api.rs` returns 1
    - `grep -c "BookingCreateResultTO" shifty-dioxus/src/api.rs` returns >= 2 (import + return type/json)
    - `grep -c "register_user_to_slot_with_conflict_check" shifty-dioxus/src/loader.rs` returns 1
    - `grep -c "WarningsList\|WarningList" shifty-dioxus/src/page/absences.rs` shows usage via import, and `grep -c "pub struct WarningsList" shifty-dioxus/src/page/absences.rs` returns 0 (definition moved out)
    - `grep -c "POST.*\/booking\b\|format!(\"{}/booking\"" shifty-dioxus/src/api.rs` confirms `add_booking` still posts to old `/booking` (unchanged)
    - `cd shifty-dioxus && cargo test --package shifty-dioxus` exits 0 (whole frontend suite, incl. existing absences tests)
  </acceptance_criteria>
  <done>api::book_slot_with_conflict_check + loader::register_user_to_slot_with_conflict_check exist and compile; absences.rs imports the moved shared component; full frontend test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM client → backend REST | Booking POST + warnings come from the backend; client renders them |
| Warning payload → DOM render | `WarningTO` strings (dates, counts) interpolated into i18n templates and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `BookingCreateResultTO` deserialization in api.rs | mitigate | `booking_id` and warning fields are typed (`Uuid`, `u8`, `u32`, `time::Date`); serde deserialization rejects malformed payloads — no manual string parsing |
| T-09-02 | Information Disclosure | Warning text rendering (XSS) | accept | Dioxus RSX text nodes are escaped by the framework; warning strings are interpolated as text content, not raw HTML. No `dangerous_inner_html`. Backend-controlled enum data only — no free-form user input on this surface |
| T-09-03 | Spoofing | Person-name in warning | N/A | Person name is resolved client-side from the already-loaded sales-person resource (Plan 02); no auth decision depends on it |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test --package shifty-dioxus` green (all SSR + i18n tests, including new booking-warning tests).
- 7 new i18n keys present in en/de/cs; German uses `Locale::De` only (grep gate); de.rs booking-warning strings use `{person}` placeholder.
- `component/warning_list.rs` exists, re-exported, renders 3 booking variants, supports `suppress_header`; absences.rs imports it (no inline duplicate).
- `book_slot_with_conflict_check` posts to `/shiftplan-edit/booking`; `add_booking` (old `/booking`) untouched.
</verification>

<success_criteria>
- Foundation interfaces ready for Plan 02: `api::book_slot_with_conflict_check`, `loader::register_user_to_slot_with_conflict_check`, `component::{WarningList, WarningsList}` with `person_name` + `suppress_header` props, 7 i18n keys.
- All Wave-0 test stubs from 09-VALIDATION.md that live in `warning_list.rs` and `i18n/mod.rs` are created and green (SSR per-variant, empty→nothing, suppress-header, parity, reference-matcher).
- `cargo test --package shifty-dioxus` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-01-SUMMARY.md`.
</output>
