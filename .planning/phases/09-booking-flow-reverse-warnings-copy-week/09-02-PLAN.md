---
phase: 09-booking-flow-reverse-warnings-copy-week
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - shifty-dioxus/src/page/shiftplan.rs
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [FUI-A-05]

must_haves:
  truths:
    - "Booking from the shiftplan editor posts to /shiftplan-edit/booking; the single AddUserToSlot call-site is switched and no other booking call-site exists (D-01)"
    - "Old POST /booking (create_booking) stays unchanged — regression-locked via grep (D-02)"
    - "The new endpoint persists immediately (201) and returns warnings in the same body — no dry-run; empty warnings reload, non-empty open the dialog (D-03)"
    - "Non-empty warnings open a Dioxus confirm-dialog (not window.confirm) (D-03)"
    - "The booking dialog shows exactly ONE header (Dialog.title; WarningList internal header suppressed)"
    - "Abbrechen / ESC / X / backdrop all DELETE the optimistically-created booking (D-03)"
    - "Trotzdem buchen keeps the booking and reloads the shiftplan (D-03)"
    - "Rollback DELETE failure surfaces via error_handler AND calls update_shiftplan() — no silent-swallow on this path (D-04)"
    - "403 on booking POST is silently swallowed; 422 surfaces via error_handler (D-13)"
    - "Copy-Week is descoped (FUI-A-06 dropped); all Copy-Week frontend dead code removed; backend POST /booking + copy-week endpoint untouched (D-05, D-06, D-07)"
    - "ROADMAP (Phase-9 title + SC2) and REQUIREMENTS (FUI-A-06 dropped) doc-synced — user-authorized as part of the descope (D-08)"
  artifacts:
    - path: "shifty-dioxus/src/page/shiftplan.rs"
      provides: "RollbackBooking action + updated AddUserToSlot handler + BookingWarningDialog"
      contains: "RollbackBooking"
  key_links:
    - from: "shifty-dioxus/src/page/shiftplan.rs AddUserToSlot handler"
      to: "loader::register_user_to_slot_with_conflict_check"
      via: "coroutine match arm await"
      pattern: "register_user_to_slot_with_conflict_check"
    - from: "shiftplan.rs BookingWarningDialog on_close"
      to: "ShiftPlanAction::RollbackBooking"
      via: "cr.send on all close paths"
      pattern: "RollbackBooking"
    - from: "shiftplan.rs RollbackBooking handler"
      to: "api::remove_booking"
      via: "DELETE rollback"
      pattern: "remove_booking"
---

<objective>
Wire the Plan-01 foundation into the shiftplan editor: switch the single `AddUserToSlot` booking call-site to the conflict-aware loader, render returned warnings in a Dioxus confirm-dialog with optimistic-create + rollback semantics, remove all dead Copy-Week frontend code, and finalize ROADMAP/REQUIREMENTS doc-sync. Close the WASM + workspace regression gates.

Purpose: Delivers FUI-A-05 end-to-end (SC1) and the Copy-Week dead-code cleanup (SC2), while proving the old `POST /booking` call-sites are unchanged (SC3).

Output: `shiftplan.rs` updated (RollbackBooking action, dialog, copy-week removal); `api::copy_week` + `loader::copy_from_previous_week` deleted; `add_booking`/`register_user_to_slot` removed or deprecation-commented (Claude's discretion — see action); `Key::ShiftplanTakeLastWeek` removed from all 4 i18n files; source self-test guarding against copy-week reintroduction; ROADMAP + REQUIREMENTS doc updates; final regression gates.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<vcs_jj_only>
This repo is jj-managed (co-located with git). Do NOT run `git commit`/`git add`. The user commits manually with jj. Make all code/file changes; leave committing to the user. GSD auto-commit is disabled.
</vcs_jj_only>

<context>
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-CONTEXT.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-RESEARCH.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-PATTERNS.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-UI-SPEC.md
@.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-01-PLAN.md

<interfaces>
<!-- Produced by Plan 01 (Wave 1) — consume directly. -->
- `crate::component::{WarningList, WarningsList}` — WarningList props: `{ warnings: WarningsList, person_name: Option<ImStr>, suppress_header: bool (default false), [dense: bool if kept] }`. `WarningsList(pub Rc<[WarningTO]>)` with `empty()`, `len()`, `is_empty()`.
- When `suppress_header: true`, WarningList does NOT render its internal `text-micro text-warn font-semibold uppercase` header row — the caller's own header (the booking `Dialog.title`) is the sole header. The booking dialog MUST pass `suppress_header: true` to avoid a redundant double-header.
- `crate::api::book_slot_with_conflict_check(config, sales_person_id, slot_id, week, year) -> Result<BookingCreateResultTO, reqwest::Error>`
- `crate::loader::register_user_to_slot_with_conflict_check(config, slot_id, user_id, week, year) -> Result<(uuid::Uuid, Vec<rest_types::WarningTO>), ShiftyError>`
- i18n keys: `Key::BookingWarningDialogHeaderSingular`, `…Plural`, `…Confirm`, `…Cancel` (plus the 3 per-item keys handled inside WarningList).

<!-- Existing shiftplan.rs facts (VERIFIED 2026-06-12) -->
- `ShiftPlanAction` enum at shiftplan.rs:55; `CopyFromPreviousWeek` variant at :66; `AddUserToSlot` handler at :402-428; `RemoveUserFromSlot` handler at :430-447 (UUID-as-data pattern to follow for RollbackBooking); `CopyFromPreviousWeek` handler at :523-533.
- Dead unused var `let _take_last_week_str: ImStr = i18n.t(Key::ShiftplanTakeLastWeek).into();` at shiftplan.rs:131 — MUST be removed.
- Coroutine handle is `cr` (from `use_coroutine`); `update_shiftplan` is a closure in scope; `current_sales_person: Signal<Option<SalesPerson>>` (SalesPerson has `pub name: Rc<str>`) and `sales_persons_resource` available — use the booked person's name for `person_name`.
- `component/dialog.rs` exports `Dialog` + `DialogVariant` (already re-exported via `crate::component::{Dialog, DialogVariant}`); `Btn` + `BtnVariant` already imported in shiftplan.rs:8.
- `api::remove_booking(config, booking_id) -> Result<(), reqwest::Error>` (api.rs:227) — DELETE /booking/{id}; the rollback path.
- 403 silent-swallow pattern: `Err(crate::error::ShiftyError::Reqwest(ref e)) if e.status() == Some(reqwest::StatusCode::FORBIDDEN) => { ... }`.

<!-- Dead-code inventory (VERIFIED via grep) -->
- shiftplan.rs: enum variant :66, unused var :131, handler :523-533 (no RSX button references CopyFromPreviousWeek — only these 3 sites)
- api.rs: `copy_week` fn :237-251
- loader.rs: `copy_from_previous_week` fn :318-326
- i18n: `ShiftplanTakeLastWeek` in mod.rs:79, en.rs:32, de.rs:34, cs.rs:34
- BACKEND STAYS UNTOUCHED: rest/src/booking.rs (POST /booking), rest/src/shiftplan_edit.rs:173 (copy-week handler), rest-types CopyWeekResultTO
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch AddUserToSlot to conflict-aware flow; add RollbackBooking action + confirm dialog</name>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs:55-72 (ShiftPlanAction enum), :90-200 (signals/coroutine handle/i18n/dialog setup region), :400-447 (AddUserToSlot + RemoveUserFromSlot handlers), :690-720 (RSX body region where conditional dialogs/buttons live) — ANALOG per 09-PATTERNS.md
    - shifty-dioxus/src/component/dialog.rs (Dialog props: open, on_close, title, footer, variant)
    - shifty-dioxus/src/component/shiftplan_tab_bar.rs:201-243 (Dialog + footer Btn wiring analog) — ANALOG per 09-PATTERNS.md
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-UI-SPEC.md (Component Contracts §1 BookingWarningDialog + Interaction Contract state-machine + States table)
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-PATTERNS.md (shiftplan.rs section — exact action/handler/dialog skeletons incl. suppress_header: true)
  </read_first>
  <action>
    Add to `ShiftPlanAction` (after `RemoveUserFromSlotDay`, before the now-to-be-deleted `CopyFromPreviousWeek`): `RollbackBooking(Uuid)` — booking_id carried as data, NOT read from a signal in the handler (Pitfall 5 guard).

    Add two signals near the other `use_signal` declarations in the component body: `let mut pending_warnings: Signal<WarningsList> = use_signal(WarningsList::empty);` and `let mut pending_rollback_id: Signal<Option<Uuid>> = use_signal(|| None);`. Add `use crate::component::{Dialog, DialogVariant, WarningList, WarningsList};` (or extend existing `use crate::component::...` lines) and `use std::rc::Rc;` is already present (shiftplan.rs:1).

    Replace the `AddUserToSlot` handler body (currently calls `loader::register_user_to_slot`) with:
    ```rust
    match loader::register_user_to_slot_with_conflict_check(
        config.to_owned(), slot_id, sales_person_id, week, year,
    ).await {
        Ok((booking_id, warnings)) if !warnings.is_empty() => {
            pending_rollback_id.set(Some(booking_id));
            pending_warnings.set(WarningsList(Rc::from(warnings.as_slice())));
            // dialog opens reactively; do NOT call update_shiftplan() here
        }
        Ok(_) => { update_shiftplan(); }
        Err(crate::error::ShiftyError::Reqwest(ref e))
            if e.status() == Some(reqwest::StatusCode::FORBIDDEN) => {
            update_shiftplan(); // D-13: silent-swallow 403, still reload
        }
        Err(e) => { crate::error::error_handler(e); update_shiftplan(); } // D-13: 422 surfaced
    }
    ```

    Add a new `RollbackBooking(booking_id)` match arm (model on RemoveUserFromSlot):
    ```rust
    ShiftPlanAction::RollbackBooking(booking_id) => {
        if let Err(e) = api::remove_booking(config.to_owned(), booking_id).await {
            crate::error::error_handler(e); // D-04: surface error
        }
        pending_rollback_id.set(None);
        pending_warnings.set(WarningsList::empty());
        update_shiftplan(); // D-04: reload even on failure
    }
    ```

    In the RSX body (alongside other conditional dialogs), add the BookingWarningDialog gated on `!pending_warnings.read().is_empty()`:
    - Compute header: if `pending_warnings.read().len() == 1` use `Key::BookingWarningDialogHeaderSingular`, else `Key::BookingWarningDialogHeaderPlural` with `.replace("{count}", &len.to_string())`.
    - Snapshot `let rollback_id = pending_rollback_id.read().expect("rollback id set when warnings non-empty");` and `let warnings_snap = pending_warnings.read().clone();`.
    - person_name: `current_sales_person.read().as_ref().map(|sp| ImStr::from(sp.name.as_ref()))`.
    - Footer: Secondary `Btn` (label `Key::BookingWarningDialogCancel`) on_click → `cr.send(ShiftPlanAction::RollbackBooking(rollback_id))`; Primary `Btn` (label `Key::BookingWarningDialogConfirm`) on_click → `pending_rollback_id.set(None); pending_warnings.set(WarningsList::empty()); update_shiftplan();`.
    - `Dialog { open: true, on_close: move |_| { cr.send(ShiftPlanAction::RollbackBooking(rollback_id)); }, title: ImStr::from(header.as_str()), footer: Some(footer), variant: DialogVariant::Auto, WarningList { warnings: warnings_snap, person_name, suppress_header: true } }`.
    - CRITICAL (double-header): the `WarningList` call MUST pass `suppress_header: true`. The `Dialog.title` already renders the "Hinweis · N Konflikt(e)" header; without `suppress_header: true` the WarningList would render a SECOND identical header inside the dialog body (redundant double-header). The booking dialog therefore has exactly ONE header (the Dialog title bar).
    - CRITICAL (Pitfall 2): ALL close paths (X / ESC / backdrop) route through `on_close` → RollbackBooking. "Trotzdem buchen" clears pending BEFORE closing so no rollback fires.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --package shifty-dioxus</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RollbackBooking" shifty-dioxus/src/page/shiftplan.rs` returns >= 4 (enum variant + handler arm + cancel-button send + on_close send)
    - `grep -c "register_user_to_slot_with_conflict_check" shifty-dioxus/src/page/shiftplan.rs` returns 1
    - `grep -c "window.confirm\|web_sys::window().*confirm" shifty-dioxus/src/page/shiftplan.rs` returns 0 (no browser confirm)
    - `grep -c "pending_rollback_id\|pending_warnings" shifty-dioxus/src/page/shiftplan.rs` returns >= 6
    - `grep -c "remove_booking" shifty-dioxus/src/page/shiftplan.rs` returns >= 1 (rollback path)
    - The `WarningList { ... }` call inside the booking Dialog passes `suppress_header: true`: `grep -c "suppress_header: true" shifty-dioxus/src/page/shiftplan.rs` returns >= 1 (avoids the double-header — Dialog.title is the sole header)
    - Single-header guard via SSR: add (or extend) an SSR test that renders the booking dialog body for a single-warning case and asserts the `text-micro text-warn font-semibold uppercase` warning-header element appears EXACTLY ONCE in the output (the Dialog title bar is the only header; the WarningList internal header is suppressed). If the dialog is not directly SSR-renderable in isolation, render the `WarningList { warnings, person_name, suppress_header: true }` body fragment used by the dialog and assert ZERO occurrences of the `text-micro text-warn font-semibold uppercase` header class in the WarningList output.
    - `cd shifty-dioxus && cargo test --package shifty-dioxus` exits 0
  </acceptance_criteria>
  <done>AddUserToSlot uses the conflict-aware loader; RollbackBooking action + confirm dialog wired with all close paths routing to rollback; WarningList rendered with suppress_header: true so the dialog has exactly ONE header; frontend suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all Copy-Week dead frontend code + ShiftplanTakeLastWeek key; add reintroduction self-test</name>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs:66 (enum variant), :131 (unused _take_last_week_str var), :523-533 (handler arm)
    - shifty-dioxus/src/api.rs:237-251 (copy_week fn)
    - shifty-dioxus/src/loader.rs:318-326 (copy_from_previous_week fn)
    - shifty-dioxus/src/i18n/mod.rs:79 (Key::ShiftplanTakeLastWeek), en.rs:32, de.rs:34, cs.rs:34
    - .planning/phases/09-booking-flow-reverse-warnings-copy-week/09-RESEARCH.md (Dead Code Inventory table — exact symbols/lines)
  </read_first>
  <action>
    Delete every dead Copy-Week frontend symbol (D-06):
    - shiftplan.rs: remove the `CopyFromPreviousWeek` enum variant (:66), the unused `let _take_last_week_str: ImStr = i18n.t(Key::ShiftplanTakeLastWeek).into();` line (:131), and the entire `ShiftPlanAction::CopyFromPreviousWeek => { ... }` match arm (:523-533).
    - api.rs: delete `pub async fn copy_week(...)` (:237-251).
    - loader.rs: delete `pub async fn copy_from_previous_week(...)` (:318-326).
    - i18n/mod.rs: remove `ShiftplanTakeLastWeek,` from the Key enum (:79).
    - i18n/en.rs, de.rs, cs.rs: remove the respective `i18n.add_text(Locale::*, Key::ShiftplanTakeLastWeek, "...")` block.

    Do NOT touch backend `rest/src/booking.rs`, `rest/src/shiftplan_edit.rs:173` (copy_week_with_conflict_check), or `rest-types` `CopyWeekResultTO` (D-07).

    Add a source self-test guarding against reintroduction. Put it in `i18n/mod.rs` `#[cfg(test)]` (or `api.rs` test module) as `#[test] fn no_copy_week_in_frontend_source()`: read the frontend source files via `include_str!` for the symbols and assert absence. Use `include_str!` of each file and assert `!contains` for `"CopyFromPreviousWeek"`, `"copy_from_previous_week"`, `"fn copy_week"`, `"ShiftplanTakeLastWeek"`. Example:
    ```rust
    #[test]
    fn no_copy_week_in_frontend_source() {
        let shiftplan = include_str!("../page/shiftplan.rs");
        let api = include_str!("../api.rs");
        let loader = include_str!("../loader.rs");
        let modrs = include_str!("mod.rs");
        assert!(!shiftplan.contains("CopyFromPreviousWeek"), "CopyFromPreviousWeek must be removed");
        assert!(!loader.contains("copy_from_previous_week"), "loader copy_from_previous_week must be removed");
        assert!(!api.contains("fn copy_week"), "api copy_week must be removed");
        assert!(!modrs.contains("ShiftplanTakeLastWeek"), "ShiftplanTakeLastWeek key must be removed");
    }
    ```
    Adjust the relative `include_str!` paths to the actual location of the test module file.

    Claude's discretion on `add_booking`/`register_user_to_slot`: if now unused after Plan-02 wiring, either remove them OR keep with a `// kept: old POST /booking path, no current caller` comment. The OLD backend `POST /booking` MUST remain reachable conceptually but the frontend may drop its client fn — that is fine (SC3 is about the BACKEND handler being unchanged, not the FE wrapper).
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --package shifty-dioxus no_copy_week_in_frontend_source</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "CopyFromPreviousWeek" shifty-dioxus/src/` totals 0
    - `grep -rc "copy_from_previous_week" shifty-dioxus/src/` totals 0
    - `grep -rc "fn copy_week" shifty-dioxus/src/` totals 0
    - `grep -rc "ShiftplanTakeLastWeek" shifty-dioxus/src/` totals 0
    - Backend untouched: `grep -c "copy_week_with_conflict_check" rest/src/shiftplan_edit.rs` returns >= 1 AND `grep -c "CopyWeekResultTO" rest-types/src/lib.rs` returns >= 1
    - `cd shifty-dioxus && cargo test --package shifty-dioxus no_copy_week_in_frontend_source` exits 0
  </acceptance_criteria>
  <done>All Copy-Week frontend dead code and the ShiftplanTakeLastWeek key are gone; self-test guards reintroduction; backend copy-week + POST /booking handler untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Doc-sync ROADMAP + REQUIREMENTS; final WASM + workspace regression gate</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 9 bullet at line ~105; SC2 wording)
    - .planning/REQUIREMENTS.md (FUI-A-05 ~line 40, FUI-A-06 ~line 46, coverage table ~line 139)
    - shifty-dioxus/CLAUDE.md (WASM-Build-Gate command)
    - CLAUDE.local.md (NixOS nix-shell wrapper for WASM build)
  </read_first>
  <action>
    D-08 doc-sync (user-authorized): In `.planning/ROADMAP.md` Phase 9 bullet, confirm the title reflects "Booking-Flow Reverse-Warnings" with Copy-Week descoped and SC2 phrased as "dead Copy-Week frontend code removed" (already present per current ROADMAP — adjust only if stale; do not introduce FUI-A-06 as in-scope). In `.planning/REQUIREMENTS.md` confirm FUI-A-06 is marked `⊘ Dropped` and FUI-A-05 maps to Phase 9 — fix any drift. Mark the Phase 9 plan list (added by the planner) consistent. These are documentation edits only.

    Run the final regression gates:
    1. `cd shifty-dioxus && cargo test --package shifty-dioxus` — full frontend suite green.
    2. WASM build gate (from shifty-dioxus/): `cargo build --target wasm32-unknown-unknown`. On NixOS this may need the wrapper: `nix-shell -p openssl pkg-config lld --command "cargo build --target wasm32-unknown-unknown"`. Exit code must be 0 with no unused/unreachable-pattern warnings on removed copy-week symbols.
    3. Backend regression: from repo root `cargo check --workspace` (confirms backend untouched/compiling). Do NOT run destructive DB commands.

    This task includes the manual-only verification noted in 09-VALIDATION.md: the live confirm-dialog UX (book onto an absence/unavailable day against a running backend, verify dialog appears, test "Abbrechen" rolls back and "Trotzdem buchen" keeps). This is a checkpoint, NOT automatable — present it to the user after the automated gates pass.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --package shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd shifty-dioxus && cargo test --package shifty-dioxus` exits 0
    - `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` exits 0 (use nix-shell wrapper if toolchain not on PATH)
    - From repo root: `cargo check --workspace` exits 0
    - `grep -c "FUI-A-06" .planning/REQUIREMENTS.md` shows it marked Dropped (no in-scope re-introduction)
    - REQUIREMENTS coverage table maps FUI-A-05 → Phase 9
  </acceptance_criteria>
  <done>ROADMAP + REQUIREMENTS reflect the descope; frontend suite + WASM build + backend cargo check all green; live-dialog UAT presented to user.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Conflict-aware booking flow in the shiftplan editor with a Dioxus confirm-dialog (optimistic create + rollback). All automated gates (frontend tests, WASM build, backend cargo check) are green.</what-built>
  <how-to-verify>
    1. Start backend (port 3000) and frontend (`dx serve`, port 8080).
    2. In the shiftplan editor, book an employee onto a day where they have an absence (Vacation/Sick) OR are marked unavailable.
    3. Expected: a Dioxus dialog opens (NOT a browser confirm) with a SINGLE header "Hinweis · 1 Konflikt" (no duplicate header inside the dialog body) and a warning line naming the person + date/KW + reason.
    4. Click "Abbrechen" — the booking must disappear (DELETE rollback); shiftplan reloads.
    5. Repeat the booking, click "Trotzdem buchen" — the booking stays; shiftplan reloads.
    6. Repeat the booking, close via ESC and via backdrop click — each must roll back the booking (no phantom booking left).
    7. Book onto a normal (no-conflict) day — no dialog, booking just appears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., phantom booking after ESC, double header in dialog, English text in German UI).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM client → backend REST | Booking POST persists immediately (201); rollback DELETE reverses it |
| Dialog close paths → rollback dispatch | Every non-confirm close must reverse the optimistic booking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Phantom booking on dialog bypass | mitigate | All close paths (X / ESC / backdrop / Abbrechen) route through `Dialog.on_close` → `RollbackBooking(rollback_id)`; "Trotzdem buchen" clears pending BEFORE closing (Pitfall 2). booking_id passed as action data, not read from a signal mid-handler (Pitfall 5) |
| T-09-05 | Tampering | Phantom booking on rollback DELETE failure | mitigate | D-04: on DELETE error, call `error_handler(e)` AND `update_shiftplan()` — real state becomes visible, orphan detectable. No silent-swallow on rollback |
| T-09-06 | Elevation of Privilege | Booking without privilege | accept | 403 from backend silently swallowed (D-13), consistent with existing AddUserToSlot; all privilege enforcement is server-side. No new client-side gate |
| T-09-07 | Spoofing | Person-name in warning text | accept | Name resolved client-side from already-loaded sales-person resource for display only; no security decision depends on it |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test --package shifty-dioxus` green (incl. `no_copy_week_in_frontend_source`).
- WASM build `cargo build --target wasm32-unknown-unknown` exit 0; backend `cargo check --workspace` exit 0.
- grep gates: zero copy-week frontend symbols; backend copy-week + POST /booking handler present (SC3 regression-lock).
- shiftplan.rs uses `register_user_to_slot_with_conflict_check`, RollbackBooking on all dialog close paths, no `window.confirm`, WarningList rendered with `suppress_header: true` (single header in the dialog).
- ROADMAP/REQUIREMENTS reflect Copy-Week descope (FUI-A-06 dropped).
</verification>

<success_criteria>
- SC1: Editor booking posts to `/shiftplan-edit/booking`; warnings trigger Dioxus dialog (single header); Abbrechen → DELETE rollback, Trotzdem buchen → keep (verified by tests + UAT checkpoint).
- SC2: All Copy-Week frontend dead code removed (grep gates 0); backend endpoint untouched.
- SC3: Old `POST /booking` backend handler unchanged (grep verified).
- Live confirm-dialog UAT approved by user.
</success_criteria>

<output>
After completion, create `.planning/phases/09-booking-flow-reverse-warnings-copy-week/09-02-SUMMARY.md`.
</output>
