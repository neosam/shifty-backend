---
phase: 21-tabellen-lesbarkeit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/component/working_hours_mini_overview.rs
  - shifty-dioxus/src/page/absences.rs
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "UI-01: Die Stunden-Tabelle unter dem Schichtplan (WorkingHoursMiniOverview TableLayout) hat eine maximale Breite und Zebra-Striping (abwechselnde Zeilen-Hintergründe)."
    - "UI-01: Die bestehende is_selected/Hover-Hervorhebung gewinnt visuell gegen das Zebra (Selected-Zeile = bg-accent-soft, nicht zebra-überlagert)."
    - "UI-02: Die Mitarbeiter-Spalte der /absences-Tabelle ist deutlich schmaler als 1.5fr — alle drei grid-cols-Definitionen tragen denselben neuen Wert."
    - "D-21-03: Reine CSS/Tailwind-Änderung — keine Logik, keine neuen Props, keine i18n; SSR-Snapshot-Tests ändern sich nur in Klassen."
  artifacts:
    - path: "shifty-dioxus/src/component/working_hours_mini_overview.rs"
      provides: "TableLayout mit max-width-Container + Zebra-Row-Klassen"
      contains: "max-w-5xl"
    - path: "shifty-dioxus/src/page/absences.rs"
      provides: "Drei konsistente, schmalere grid-cols-Definitionen der Absence-Tabelle"
      contains: "grid-cols-[200px_170px_140px_90px_70px]"
  key_links:
    - from: "TableLayout container div"
      to: "Tailwind max-width utility"
      via: "static class string"
      pattern: "max-w-5xl"
    - from: "tbody tr row_class"
      to: "zebra background tokens"
      via: "3-way static match (selected > odd > even)"
      pattern: "bg-surface-2"
---

<objective>
Zwei rein visuelle Frontend-Polish-Änderungen für die Lesbarkeit breiter Tabellen
auf großen Bildschirmen:

- **UI-01:** `WorkingHoursMiniOverview` → `TableLayout` bekommt eine maximale Breite
  und Zebra-Striping (abwechselnde Zeilen-Hintergründe), wobei die bestehende
  `is_selected`-/Hover-Hervorhebung visuell Vorrang behält.
- **UI-02:** Die `/absences`-Tabelle (`AbsenceList`) bekommt eine deutlich schmalere
  Mitarbeiter-Spalte (weg von `1.5fr`), konsistent an allen drei grid-cols-Stellen.

Purpose: Auf großen Bildschirmen verrutscht man heute in der Zeile (kein Zebra,
keine max-width) und die Mitarbeiter-Spalte ist unnötig breit. Pure Tailwind-Politur,
kein Logik-/State-/API-Change.

Output: Geänderte Klassen-Strings in `working_hours_mini_overview.rs` und `absences.rs`;
angepasste SSR-Tests; `cargo test` + WASM-Build grün.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tabellen-lesbarkeit/21-CONTEXT.md

# Frontend-Regeln: Tailwind via npx, WASM-Build-Gate, Pitfall 5 (statische Klassen).
@shifty-dioxus/CLAUDE.md

<interfaces>
<!-- Verifizierte Code-Stellen — Executor braucht keine weitere Exploration. -->

UI-01 — shifty-dioxus/src/component/working_hours_mini_overview.rs
  - TableLayout container div (Z. 170–172):
      class: "bg-surface border border-border rounded-lg overflow-hidden select-none"
      style: "overflow-x: auto;"
      → table.w-full (Z. 173–174)
  - Row-Klasse pro tbody-Zeile (Z. 214–220):
      let is_selected = Some(sales_person_id) == props.selected_sales_person_id;
      let row_class = if is_selected {
          "border-t border-border bg-accent-soft cursor-pointer"
      } else {
          "border-t border-border cursor-pointer hover:bg-surface-alt"
      };
  - Die tbody-Schleife (Z. 201): `for working_hour in props.rows.iter()`.
  - Total-Zeile (Z. 275–291) behält ihre eigene Klasse (bg-surface-alt) — KEIN Zebra.
  - Verbotene Legacy-Klassen (Test Z. 544 `..._no_legacy_classes_in_source`):
    bg-gray-*, bg-white, text-gray-*, bg-blue/green/red-*, border-gray-*, border-black.
    → Zebra MUSS Design-Tokens nutzen (bg-surface / bg-surface-2), NICHT bg-gray-*.
  - SSR-Test-Pattern (Z. 362–366): render(app) → dioxus_ssr::render; assert!(html.contains(...)).
  - Bestehender Table-SSR-Test (Z. 572): erwartet genau 5 <tr> (3 Body + 1 Total + 1 Header).

UI-02 — shifty-dioxus/src/page/absences.rs (grid-cols an DREI Stellen, identisch):
  - Z. 1632 Header:        "hidden md:grid bg-surface-alt border-b border-border px-4 py-2 grid-cols-[1.5fr_170px_140px_90px_70px] gap-3.5 ..."
  - Z. 1732 HourlyMarkerRow: "w-full flex flex-col gap-2 md:grid md:grid-cols-[1.5fr_170px_140px_90px_70px] md:gap-3.5 px-4 py-3.5 ..."
  - Z. 1836 AbsenceListRow:  "w-full text-left flex flex-col gap-2 md:grid md:grid-cols-[1.5fr_170px_140px_90px_70px] md:gap-3.5 px-4 py-3.5 ..."
  - Kommentar Z. 1730 referenziert ebenfalls den grid-cols-String (mit aktualisieren, damit er nicht lügt).
  - Namens-Truncation (`truncate` in Spalte 1) bleibt unverändert erhalten.
  - SSR-Tests existieren in `mod tests` (ab Z. 2291), Render-Helper analog.

Tailwind (tailwind.config.js):
  - content deckt ./src/**/*.{rs,...} ab → statisch im Source vorkommende Utilities werden generiert.
  - max-w-5xl und grid-cols-[200px_...] sind statische Literale → werden generiert (kein safelist-Eintrag nötig).
  - bg-surface / bg-surface-2 / bg-surface-alt / bg-accent-soft sind als Theme-Farben definiert.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UI-01 — max-width + Zebra-Striping für TableLayout</name>
  <files>shifty-dioxus/src/component/working_hours_mini_overview.rs</files>
  <read_first>
    Lies Z. 161–296 (TableLayout) und Z. 357–600 (Test-Helper + Table-SSR-Tests),
    damit du den Render-Helper und den 5-`<tr>`-Test nicht brichst.
  </read_first>
  <behavior>
    - Test (neu): TableLayout-Render enthält die max-width-Klasse `max-w-5xl` am Container.
    - Test (neu): Bei >=2 Body-Zeilen, keine selektiert, erscheint mindestens eine
      zebra-Zeile mit `bg-surface-2` (gerade Parität) im gerenderten HTML.
    - Test (neu): Ist eine Zeile selektiert, trägt sie `bg-accent-soft` und NICHT
      `bg-surface-2` (Selected gewinnt über Zebra).
    - Bestehender Test `table_layout_renders_table_with_thead_tbody_and_total_row`
      (genau 5 `<tr>`) bleibt grün — Zebra ändert nur Klassen, nicht die Zeilenzahl.
    - Bestehender Test `working_hours_mini_overview_no_legacy_classes_in_source`
      bleibt grün — nur Design-Tokens verwenden.
  </behavior>
  <action>
    Per D-21-01 (UI-01):

    1. **max-width** am TableLayout-Container (Z. 170–172): ergänze die Klasse
       `max-w-5xl mx-auto` an den bestehenden Container-`class`-String, also:
       `"bg-surface border border-border rounded-lg overflow-hidden select-none max-w-5xl mx-auto"`.
       (Statisches Literal — Tailwind generiert `max-w-5xl`. `mx-auto` zentriert die
       Tabelle unter dem Schichtplan.) `style: "overflow-x: auto;"` bleibt.

    2. **Zebra-Striping** Pitfall-5-konform über statische Match-Arme: Iteriere die
       tbody-Schleife (Z. 201) mit `.enumerate()`:
       `for (idx, working_hour) in props.rows.iter().enumerate()`.
       Ersetze die `row_class`-Berechnung (Z. 216–220) durch eine 3-Wege-Logik mit
       AUSSCHLIESSLICH statischen Literal-Strings (kein format!()):

       ```rust
       let row_class = if is_selected {
           "border-t border-border bg-accent-soft cursor-pointer"
       } else if idx % 2 == 0 {
           "border-t border-border bg-surface-2 cursor-pointer hover:bg-surface-alt"
       } else {
           "border-t border-border bg-surface cursor-pointer hover:bg-surface-alt"
       };
       ```

       Damit gewinnt Selected (`bg-accent-soft`) über Zebra; Hover (`hover:bg-surface-alt`)
       überschreibt das Zebra beim Drüberfahren. Die Total-Zeile (Z. 275) bleibt unberührt.

    3. **Tests** (in `mod tests`, Pattern wie Z. 380 ff.): füge drei `#[test]`-Funktionen
       hinzu (siehe <behavior>), die mit `layout: WorkingHoursLayout::Table` und
       mehreren `make_row(...)` rendern und auf `max-w-5xl`, `bg-surface-2` bzw.
       `bg-accent-soft` (ohne `bg-surface-2` in der Selected-Zeile) prüfen. Nutze für
       den Selected-Test eine feste `Uuid` als `selected_sales_person_id` (Pattern wie
       Z. 457 `TEST_ID`).

    Keine neuen Props, keine i18n, keine Logikänderung an Werten (D-21-03).
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --lib working_hours_mini_overview</automated>
  </verify>
  <done>
    `grep -c 'max-w-5xl' shifty-dioxus/src/component/working_hours_mini_overview.rs`
    (außerhalb des Test-Moduls) >= 1; `bg-surface-2` als Zebra-Arm vorhanden; neue
    Zebra-/Selected-/max-width-Tests grün; alle bestehenden Tests grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI-02 — schmalere Mitarbeiter-Spalte (3 grid-cols konsistent)</name>
  <files>shifty-dioxus/src/page/absences.rs</files>
  <read_first>
    Lies Z. 1627–1660 (Header), Z. 1729–1735 (HourlyMarkerRow grid), Z. 1831–1840
    (AbsenceListRow grid) und die `mod tests` ab Z. 2291 (Render-Helper, vorhandene
    grid-/Marker-Tests), damit du keinen SSR-Test brichst.
  </read_first>
  <behavior>
    - Test (neu): Im gerenderten AbsenceList-Header-HTML erscheint exakt
      `grid-cols-[200px_170px_140px_90px_70px]` und NICHT mehr `1.5fr`.
    - Bestehende SSR-Tests (HourlyMarkerRow / AbsenceListRow / Header) bleiben grün —
      es ändert sich nur der grid-cols-String.
  </behavior>
  <action>
    Per D-21-02 (UI-02): Ersetze an ALLEN DREI Stellen den Spalten-Track
    `1.5fr` durch die feste, deutlich schmalere Breite `200px`, sodass das neue
    Track-Set überall identisch `200px 170px 140px 90px 70px` lautet:

    1. Z. 1632 Header:        `grid-cols-[1.5fr_170px_140px_90px_70px]`
       → `grid-cols-[200px_170px_140px_90px_70px]`
    2. Z. 1732 HourlyMarkerRow: `md:grid-cols-[1.5fr_170px_140px_90px_70px]`
       → `md:grid-cols-[200px_170px_140px_90px_70px]`
    3. Z. 1836 AbsenceListRow:  `md:grid-cols-[1.5fr_170px_140px_90px_70px]`
       → `md:grid-cols-[200px_170px_140px_90px_70px]`
    4. Kommentar Z. 1730 (referenziert `md:grid-cols-[1.5fr_...]`) ebenfalls auf
       `200px` aktualisieren, damit er nicht in die Irre führt.

    Alle drei MÜSSEN denselben Wert tragen (sonst verrutschen Header/Zeilen). Die
    Namens-Truncation (`truncate`) und sonstige Klassen bleiben unverändert. Keine
    Logik, keine Props, keine i18n (D-21-03).

    **Test** (in `mod tests`, Pattern wie vorhandene SSR-Tests): füge einen `#[test]`
    hinzu, der die AbsenceList (bzw. den Header) rendert und prüft:
    `html.contains("grid-cols-[200px_170px_140px_90px_70px]")` und
    `!html.contains("1.5fr")`. Falls ein Render der ganzen Liste zu aufwändig ist,
    nutze stattdessen einen Source-Sweep-Test analog zu
    `working_hours_mini_overview_no_legacy_classes_in_source`:
    `include_str!("absences.rs")` (Test-Modul abschneiden) und asserten, dass der
    Produktions-Source `1.5fr` NICHT mehr und `grid-cols-[200px_170px_140px_90px_70px]`
    mehrfach enthält.
  </action>
  <verify>
    <automated>cd shifty-dioxus && cargo test --lib absences</automated>
  </verify>
  <done>
    `grep -c 'grid-cols-\[200px_170px_140px_90px_70px\]' shifty-dioxus/src/page/absences.rs`
    >= 3 (Produktions-Code, ohne Test-Asserts mitzuzählen via Filterung); KEIN `1.5fr`
    mehr im Produktions-Source; neuer Test + alle bestehenden absences-Tests grün.
  </done>
</task>

</tasks>

<verification>
Phase-Gate (beide Tasks abgeschlossen):

1. `cd shifty-dioxus && cargo test` → alle Tests grün.
2. `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` → Exit 0 (WASM-Gate).
3. `grep -n 'max-w-5xl' shifty-dioxus/src/component/working_hours_mini_overview.rs`
   → max-width am TableLayout-Container vorhanden.
4. `grep -c 'grid-cols-\[200px_170px_140px_90px_70px\]' shifty-dioxus/src/page/absences.rs`
   → >= 3 (drei funktionale Stellen konsistent).
5. `grep -c '1.5fr' shifty-dioxus/src/page/absences.rs` im Produktions-Code → 0.
</verification>

<success_criteria>
- UI-01: TableLayout hat max-width (`max-w-5xl`) + Zebra-Striping über
  Design-Tokens; Selected-/Hover-Hervorhebung gewinnt visuell.
- UI-02: Alle drei `/absences`-grid-cols tragen identisch
  `200px 170px 140px 90px 70px` (deutlich schmaler als `1.5fr`), Truncation erhalten.
- D-21-03 eingehalten: keine Logik/Props/i18n; SSR-Tests ändern sich nur in Klassen.
- `cargo test` grün + WASM-Build Exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/21-tabellen-lesbarkeit/21-01-SUMMARY.md`.
</output>
