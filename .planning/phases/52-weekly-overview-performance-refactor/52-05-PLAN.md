---
phase: 52-weekly-overview-performance-refactor
plan: 05
type: tdd
wave: 5
depends_on: ["52-01", "52-04"]
files_modified:
  - service_impl/src/booking_information.rs
  - docs/features/F07-reporting-balance.md
  - docs/features/F07-reporting-balance_de.md
autonomous: true
requirements: [WOP-01, WOP-04, WOP-05]
must_haves:
  truths:
    - "D-52-01 — Bulk-Load-Scope: `special_days` (via bestehendes `get_by_year`), `shiftplan_reports` (via neu `extract_shiftplan_report_for_year`), `slots` (via bestehendes `get_slots`) — jeweils einmal für year UND year+1 (Spillover, D-52-04) VOR der Wochen-Schleife; In-Memory-Filter pro Woche im Consumer statt DAO-Roundtrip"
    - "D-52-04 — Spillover: `reporting_service.get_year(year)` UND `reporting_service.get_year(year+1)` vor der Schleife; Loop iteriert weiter `1..=(weeks_in_year+3)` mit Fallthrough auf Vec des Folgejahres"
    - "D-52-09 — MUST-preserve: Chain-C-Toggle-Read (`shortday_gate::read_active_from`) bleibt in `get_weekly_summary` bei Zeile ~309 (NICHT im Helper); CVC-06 Filter bei Zeilen 383-402 bleibt; Slot-Clipping-Logik bei Zeilen 409-439 bleibt (nutzt aber jetzt In-Memory-gefilterte `all_slots`)"
    - "D-52-15 — Frontend nicht angetastet; `WeeklySummaryTO` bit-identisch (nur Backend-Innenleben ändert sich)"
  <!-- planner-discipline-allow: get_slots_for_week_all_plans -->
  - "R1 — Slot-Filter-Semantik: In-Memory-Filter reproduziert EXAKT `SlotDao::get_slots_for_week_all_plans` (SQL bei `dao_impl_sqlite/src/slot.rs:151-168`): `slot.deleted IS NULL AND slot.valid_from <= sunday_of_week AND (slot.valid_to IS NULL OR slot.valid_to >= monday_of_week) AND (shiftplan.is_planning = 0 OR shiftplan.is_planning IS NULL)`"
    - "WOP-01 — `special_days.get_by_*` und `shiftplan_report.extract_shiftplan_report_for_*` Service-Calls sind pro Endpoint-Abruf auf JE 2 reduziert (year + year+1), NICHT ~55"
    - "WOP-04 — Latenz-Median `curl -w %{time_total}` über 5 Runs für `GET /booking-information/weekly-resource-report/2026` < 0.500 s auf Dev-DB"
    - "WOP-05 — Wave-1-Fixtures byte-identisch grün + gesamter Workspace-Test-Suite grün + Clippy `-D warnings` grün"
  artifacts:
    - "Umgebaute `get_weekly_summary`-Impl mit Bulk-Load-Präambel + In-Memory-Filter im Loop"
    - ".planning/phases/52-weekly-overview-performance-refactor/52-05-latency-post-refactor.txt"
    - "Ggf. `docs/features/F07-reporting-balance*.md` — Änderungshistorie-Notiz (nur wenn Doku Trait-Signaturen zitiert)"
  key_links:
    - "Bulk-Load-Präambel → ~55 Service-Calls fallen auf 6 (2×`get_year` + 2×`get_by_year` + 2×`extract_shiftplan_report_for_year` + 1×`get_slots`) = massive Latenz-Ersparnis"
    - "Byte-Identität → Wave-1-Fixtures aus Plan 01 sind das Regressions-Gate"
---

<objective>
Der zentrale Umbau von `BookingInformationServiceImpl::get_weekly_summary` (`service_impl/src/booking_information.rs:259-491`). Vor der Wochen-Schleife werden 7 Bulk-Loads (`get_year(year)`, `get_year(year+1)`, `get_by_year(year)`, `get_by_year(year+1)`, `extract_shiftplan_report_for_year(year)`, `extract_shiftplan_report_for_year(year+1)`, `get_slots`) einmalig ausgeführt. Im Loop werden die pro-Woche-Werte per In-Memory-Filter aus den vorgeladenen Kollektionen abgeleitet — die alten `reporting_service.get_week`, `special_day_service.get_by_week`, `shiftplan_report_service.extract_shiftplan_report_for_week`, `slot_service.get_slots_for_week_all_plans`-Calls im Loop entfallen. Ergebnis: byte-identisch zur Wave-0-Baseline (verifiziert durch Wave-1-Fixtures) und Latenz-Median < 500 ms.

Purpose: Zentraler Hotspot-Fix (~165 sequenzielle Service-Calls → 6). Live-Korrektheit bleibt, weil die drei Bulk-Loads jeden Request neu ausführen (kein Cache, kein Snapshot).

Output: Umgebaute Impl + Post-Refactor-Latenz-Baseline + optional Doku-Notiz in F07.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-weekly-overview-performance-refactor/52-CONTEXT.md
@.planning/phases/52-weekly-overview-performance-refactor/52-RESEARCH.md
@.planning/phases/52-weekly-overview-performance-refactor/52-01-SUMMARY.md
@.planning/phases/52-weekly-overview-performance-refactor/52-02-SUMMARY.md
@.planning/phases/52-weekly-overview-performance-refactor/52-03-SUMMARY.md
@.planning/phases/52-weekly-overview-performance-refactor/52-04-SUMMARY.md
@service_impl/src/booking_information.rs
@dao_impl_sqlite/src/slot.rs
@docs/features/F07-reporting-balance.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: `get_weekly_summary`-Umbau — Bulk-Load-Präambel + In-Memory-Loop</name>
  <files>service_impl/src/booking_information.rs</files>
  <read_first>
    - service_impl/src/booking_information.rs:259-491 (Vollständiger `get_weekly_summary`-Body, so wie er aktuell aussieht)
    - service_impl/src/booking_information.rs:291-303 (Bestehendes Load-once-Muster für `all_work_details` und `all_absences` — die 7 neuen Bulk-Loads werden direkt daneben eingefügt)
    - service_impl/src/booking_information.rs:309 (Toggle-Read `shortday_gate::read_active_from` — bleibt bei Zeile 309, wird NICHT in `reporting.get_year` verschoben)
    - service_impl/src/booking_information.rs:311-316 (Loop-Struktur `for week in 1..=(weeks_in_year + 3)` mit Spillover-Fallthrough — bleibt strukturell, aber die Daten kommen aus Vec-Index statt Service-Call)
    - service_impl/src/booking_information.rs:317-343 (Volunteer-Absence-Set — bleibt unangetastet, nutzt schon `all_absences` load-once)
    - service_impl/src/booking_information.rs:345-368 (Alte per-Woche Service-Calls für `week_report`, `special_days`, `shiftplan_reports` — WERDEN ERSETZT)
    - service_impl/src/booking_information.rs:383-402 (CVC-06 Filter — bleibt unangetastet, nutzt `all_work_details`)
    - service_impl/src/booking_information.rs:409-439 (Slot-Clipping-Logik + `slot_service.get_slots_for_week_all_plans`-Call — Service-Call WIRD ERSETZT, `shortday_gate::clip_slot_for_week`-Aufruf bleibt)
    - dao_impl_sqlite/src/slot.rs:151-168 (KRITISCH R1: DAO-Filter-Semantik für In-Memory-Reproduktion — `slot.deleted IS NULL AND slot.valid_from <= sunday AND (slot.valid_to IS NULL OR slot.valid_to >= monday) AND (shiftplan.is_planning = 0 OR shiftplan.is_planning IS NULL)`)
    - service/src/reporting.rs (`get_year`-Trait-Signatur aus Wave 4 Plan 04)
    - service/src/shiftplan_report.rs (`extract_shiftplan_report_for_year` aus Wave 3 Plan 03)
    - service/src/special_days.rs (`get_by_year` existiert bereits)
    - service/src/slot.rs:104-108 (`get_slots` existiert bereits, liefert alle Slots)
  </read_first>
  <behavior>
    Nach diesem Task hat `get_weekly_summary` diese Struktur:

    ```
    1. Permission-Checks (Zeilen 266-278) — UNVERÄNDERT.
    2. Load-once-Präambel (Zeilen 280-310 + NEUE Bulk-Loads):
       - weeks_in_year, volunteer_ids, all_work_details, all_absences — UNVERÄNDERT.
       - active_from = shortday_gate::read_active_from(...) — UNVERÄNDERT bei ~309.
       - NEU: year_reports = reporting_service.get_year(year, Authentication::Full, tx.clone().into()).await?;
       - NEU: next_year_reports = reporting_service.get_year(year + 1, Authentication::Full, tx.clone().into()).await?;
       - NEU: special_days_year = special_day_service.get_by_year(year, Authentication::Full).await?;
       - NEU: special_days_next_year = special_day_service.get_by_year(year + 1, Authentication::Full).await?;
       - NEU: shiftplan_reports_year = shiftplan_report_service.extract_shiftplan_report_for_year(year, Authentication::Full, tx.clone().into()).await?;
       - NEU: shiftplan_reports_next_year = shiftplan_report_service.extract_shiftplan_report_for_year(year + 1, Authentication::Full, tx.clone().into()).await?;
       - NEU: all_slots = slot_service.get_slots(Authentication::Full, tx.clone().into()).await?;
    3. Loop `for week in 1..=(weeks_in_year + 3)`:
       - Spillover-Adjustment (Zeilen 312-316): (target_year, target_week) — UNVERÄNDERT.
       - Volunteer-Absence-Set-Berechnung (Zeilen 317-343) — UNVERÄNDERT.
       - NEU: week_report — aus `year_reports` bzw. `next_year_reports` per Vec-Index gezogen.
         Wenn week > weeks_in_year: Index in next_year_reports = (week - weeks_in_year - 1) as usize (0-basiert für Woche 1 = Index 0).
         Sonst: Index in year_reports = (week - 1) as usize.
         Der zurückgegebene Vec-Eintrag ist `(u8, Arc<[ShortEmployeeReport]>)`; extrahiere `.1`.
         **R6 Off-by-one-Guard:** Schreibe die Index-Berechnung als reine Hilfsfunktion oder als klar kommentierten Inline-Block; Fixture 8 (Spillover) aus Wave 1 fängt Off-by-one ab.
       - NEU: special_days — In-Memory-Filter auf special_days_year / special_days_next_year:
         `special_days_source.iter().filter(|d| d.year == target_year && d.calendar_week == target_week).cloned().collect::<Arc<[_]>>()`.
         (Die Filter-Semantik entspricht dem alten `special_day_service.get_by_week(target_year, target_week, ...)`.)
       - NEU: shiftplan_reports — In-Memory-Filter auf shiftplan_reports_year / shiftplan_reports_next_year:
         `shiftplan_reports_source.iter().filter(|r| r.year == target_year && r.calendar_week == target_week).cloned().collect::<Arc<[_]>>()`.
       - CVC-06 Filter (Zeilen 353-402) — nutzt `week_report` und `shiftplan_reports` genau wie vorher. UNVERÄNDERT bis auf die Datenquelle.
       - Slot-Chain (Zeilen 409-439): `slot_service.get_slots_for_week_all_plans(year, week, ...)` WIRD ERSETZT durch In-Memory-Filter auf `all_slots`:
         ```
         let monday = time::Date::from_iso_week_date(target_year as i32, target_week, time::Weekday::Monday);
         let sunday = time::Date::from_iso_week_date(target_year as i32, target_week, time::Weekday::Sunday);
         let slots_for_week: Vec<Slot> = if let (Ok(mon), Ok(sun)) = (monday, sunday) {
             all_slots
                 .iter()
                 .filter(|slot| slot.deleted.is_none())  // R1: slot.deleted IS NULL
                 .filter(|slot| slot.valid_from <= sun)  // R1: valid_from <= sunday
                 .filter(|slot| slot.valid_to.map_or(true, |vt| vt >= mon))  // R1: valid_to IS NULL OR >= monday
                 // is_planning=0 vs NULL: aktuelles Slot-Model hat entweder shiftplan_id oder nicht;
                 // der DAO-JOIN LEFT JOIN shiftplan filtert auf shiftplan.is_planning = 0 OR NULL —
                 // slots ohne shiftplan_id (LEFT JOIN NULL) sind erlaubt. In den bestehenden Slot-Model
                 // ist das schon abgehandelt; falls das Slot-Struct `is_planning: bool` trägt: `.filter(|s| !s.is_planning)`.
                 // *** Executor MUSS `service::slot::Slot` inspizieren und die Filter-Bedingung passend nachbauen. ***
                 .cloned()
                 .collect()
         } else {
             Vec::new()
         };
         ```
         Danach folgt die alte Kette `.filter(Holiday-Drop) → .filter_map(shortday_gate::clip_slot_for_week)` UNVERÄNDERT.
       - Rest des Loop-Bodys (paid_hours, WorkingHoursPerSalesPerson.push, WeeklySummary.push) — UNVERÄNDERT.
    4. transaction_dao.commit(tx) — UNVERÄNDERT.
    ```

    **Kritisch (D-52-09):** Kein Verhaltens-Change gegenüber Wave-0-Baseline. Wave-1-Fixtures müssen alle 8 grün bleiben — inklusive Fixture 8 (Spillover Kombi). Falls eine Fixture divergiert, sofort auf Wave 2/3/4 Code prüfen und Semantik-Diff aufspüren.
  </behavior>
  <action>
    1. **Read `service/src/slot.rs`** um die konkrete Struktur von `Slot` zu prüfen — hat es `is_planning: bool`, `shiftplan_id: Option<Uuid>`, oder trägt das nur die DAO-Entity? Der In-Memory-Filter MUSS die JOIN-Semantik aus `dao_impl_sqlite/src/slot.rs:164` (`AND (shiftplan.is_planning = 0 OR shiftplan.is_planning IS NULL)`) im Service-Struct-Form nachbauen. Wenn `Slot` kein `is_planning`-Feld hat, muss `get_slots` bereits die Planning-Slots filtern (prüfen).

    2. **Einfügen** der 7 neuen Bulk-Loads direkt nach der Toggle-Read-Zeile (~Zeile 310). Reihenfolge: erst `year_reports` + `next_year_reports`, dann Special-Days year/year+1, dann Shiftplan-Reports year/year+1, dann `all_slots`. Alle mit `Authentication::Full` und `tx.clone().into()`.

    3. **Ersetzen im Loop** (Zeilen 345-368):
       - `week_report`-Zeilen 345-348 → Index-Zugriff auf `year_reports`/`next_year_reports`.
       - `special_days`-Zeilen 349-352 → In-Memory-Filter.
       - `shiftplan_reports`-Zeilen 360-368 → In-Memory-Filter.
       Die Variablen `week_report`, `special_days`, `shiftplan_reports` behalten ihre Namen und Typen — nur die Bezugsquelle wechselt. So bleiben alle weiteren Zeilen (383-402 CVC-06, 409-439 Slot-Chain) unverändert.

    4. **Ersetzen im Loop** (Zeile 409-411 Slot-DAO-Call):
       - `slot_service.get_slots_for_week_all_plans(...)` → In-Memory-Filter auf `all_slots` mit der DAO-Semantik aus Zeilen 164-168.
       - Die nachfolgende `.iter().filter(|slot| !special_days.iter().any(Holiday-Drop))`-Kette bleibt UNVERÄNDERT.
       - Die `.filter_map(shortday_gate::clip_slot_for_week(...))`-Kette bleibt UNVERÄNDERT.

    5. **Kein Removal** von: Toggle-Read (~309), CVC-06 Filter (~383-402), `absent_volunteer_ids`-Set (~317-343), `slot_hours`-Summe (~440-445), `paid_hours`/`WorkingHoursPerSalesPerson`-Loop (~446-466), `WeeklySummary::push`-Block (~471-487).

    6. **Wave-1-Fixture-Test laufen lassen NACH dem Umbau**:
       - `cargo test --package service_impl booking_information_weekly_summary_year_batch`
       - Alle 8 MÜSSEN grün bleiben.
       - Falls eine divergiert: Semantik-Diff aufspüren. Häufige Ursachen: falsche Vec-Index-Berechnung (R6), Slot-Filter-Semantik-Drift (R1), oder ein vergessener Filter beim In-Memory-Aufbau (z.B. `deleted IS NULL` beim SpecialDay).

    7. **Grep-Guards nach Umbau**:
       - `grep -c 'get_by_week' service_impl/src/booking_information.rs` sollte um 1 gesunken sein (der eine Call ist weg).
       - `grep -c 'extract_shiftplan_report_for_week' service_impl/src/booking_information.rs` sollte um 1 gesunken sein.
       - `grep -c 'get_slots_for_week_all_plans' service_impl/src/booking_information.rs` sollte um 1 gesunken sein.
       - `grep -c 'reporting_service.get_week' service_impl/src/booking_information.rs` sollte um 1 gesunken sein.
       Ausnahme: `get_summery_for_week` (Zeile 494) bleibt unangetastet — der ruft weiter `reporting.get_week` einmal auf, das ist sekundärer Konsument und nicht Scope dieser Phase.
  </action>
  <verify>
    <automated>cargo test --package service_impl booking_information && cargo test --package service_impl booking_information_chain_c && cargo test --package service_impl booking_information_weekly_summary_year_batch && cargo test --workspace && cargo clippy --workspace -- -D warnings 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `get_weekly_summary` hat 7 Bulk-Loads vor dem Loop.
    - Loop verwendet ausschließlich In-Memory-Filter statt Service-Calls für week_report, special_days, shiftplan_reports, slots.
    - `cargo test --package service_impl` grün — insbesondere:
      - Wave-1-Fixtures (`booking_information_weekly_summary_year_batch::fixture_*`) — alle 8 bit-exakt grün.
      - `booking_information_chain_c` — grün (Chain-C-Toggle-Read + Slot-Clipping unangetastet).
      - `booking_information` — grün.
    - `cargo test --workspace` grün.
    - `cargo clippy --workspace -- -D warnings` grün.
    - Grep-Guards oben erfüllt (vier `_for_week`/`get_week`-Aufrufe in `get_weekly_summary` weg).
    - Grep-Guard: `shortday_gate::read_active_from` weiterhin präsent in `booking_information.rs` (bleibt bei ~Zeile 309).
    - Trait-Signatur von `get_weekly_summary` unverändert (`git diff service/src/booking_information.rs` = leer, falls überhaupt eine Trait-Datei angefasst wurde — sollte NICHT sein).
  </done>
</task>

<task type="auto">
  <name>Task 2: Latenz-Messung nach Refactor + Doku-Check</name>
  <files>.planning/phases/52-weekly-overview-performance-refactor/52-05-latency-post-refactor.txt, docs/features/F07-reporting-balance.md, docs/features/F07-reporting-balance_de.md</files>
  <read_first>
    - .planning/phases/52-weekly-overview-performance-refactor/52-01-latency-baseline.txt (Wave-0-Baseline zum Vergleich)
    - .planning/phases/52-weekly-overview-performance-refactor/52-CONTEXT.md D-52-16 (Messmethode)
    - docs/features/F07-reporting-balance.md (Prüfen: zitiert die Doku Trait-Signaturen? Wenn ja: Änderungshistorie ergänzen. Wenn nein: keine Änderung nötig.)
    - docs/features/F07-reporting-balance_de.md (dito)
    - CLAUDE.md „Docs-Freshness-Gate"
  </read_first>
  <action>
    **A) Latenz-Messung nach Refactor:**
    1. Backend starten: `cd shifty-backend && nix develop --command cargo run` in Hintergrund. Wait bis `Server started on 3000`.
    2. Dieselbe Dev-DB wie in Wave 0 (Baseline lag in `52-01-latency-baseline.txt`).
    3. Messung identisch zu Wave 0:
       ```
       for i in 1 2 3 4 5; do curl -s -o /dev/null -w "%{time_total}\n" http://localhost:3000/booking-information/weekly-resource-report/2026; done | sort -n
       ```
    4. Ergebnis in `.planning/phases/52-weekly-overview-performance-refactor/52-05-latency-post-refactor.txt` schreiben:
       ```
       # Phase 52 Wave 4 — Latency Post-Refactor
       # Endpoint: GET /booking-information/weekly-resource-report/2026
       # Auth: mock_auth (Dev-Default)
       # Runs (sorted):
       # 1: 0.XXX s
       # 2: 0.XXX s
       # 3: 0.XXX s   ← median
       # 4: 0.XXX s
       # 5: 0.XXX s
       Median: X.XXX s
       Baseline median (Wave 0): Y.YYY s
       Speedup factor: Z.Z×
       Target (<0.500 s): PASS | FAIL
       Backend commit: <git rev-parse HEAD>
       Measured: <date -Iseconds>
       ```
    5. Backend stoppen.

    **B) Zielwert-Check (D-52-16):** Wenn Median > 0.500 s:
    - **Als Follow-Up dokumentieren, NICHT diese Phase blocken.** Der byte-identische Refactor ist die Kernaufgabe; wenn die Latenz nicht erreicht wird, kann eine Zusatz-Optimierung folgen (z.B. Load-once für `sales_person_service.get_all` — siehe RESEARCH R3/A2).
    - Warnung ins Textfile schreiben: „Zielwert nicht erreicht — Follow-Up-Kandidaten: (1) `sales_person.get` in `assemble_weeks` durch load-once ersetzen; (2) `booking(year, calendar_week)`-DB-Index (RESEARCH Q3); (3) `derive_hours_for_range`+`build_derived_holiday_map`-Batching."
    - Trotzdem WOP-04-Status im Text explizit vermerken (PASS / FAIL).

    **C) Docs-Freshness-Check (F07):**
    1. `grep -n 'get_year\|get_week\|extract_shiftplan_report' docs/features/F07-reporting-balance.md docs/features/F07-reporting-balance_de.md`. Wenn KEINE Trait-Signaturen zitiert werden: KEINE Änderung nötig, dokumentiere im SUMMARY „F07 zitiert keine Trait-Signaturen — keine Änderung."
    2. Wenn Trait-Signaturen zitiert werden: kurze Änderungshistorie-Sektion am Ende von F07.md und F07_de.md ergänzen:
       ```
       ## Änderungshistorie

       - **v2.5 Phase 52 (2026-07-05):** Additive Batch-Variante `ReportingService::get_year(year)`
         zusätzlich zu bestehendem `get_week(year, week)`. Balance-Formel, CVC-06 Cap-Semantik und
         `is_paid`-Filter unverändert. Byte-Identität zwischen Batch- und Einzel-Woche-Aufruf
         strukturell garantiert durch gemeinsamen internen `assemble_weeks`-Helper.
       ```
       Beide Sprachen synchron halten (Memory `feedback_docs_freshness`-Regel aus CLAUDE.md).
  </action>
  <verify>
    <automated>test -f .planning/phases/52-weekly-overview-performance-refactor/52-05-latency-post-refactor.txt && grep -q '^Median:' .planning/phases/52-weekly-overview-performance-refactor/52-05-latency-post-refactor.txt</automated>
  </verify>
  <done>
    - `52-05-latency-post-refactor.txt` existiert mit 5 Runs + Median + Baseline-Vergleich + PASS/FAIL-Status.
    - Wenn Median > 500 ms: Follow-Up-Kandidaten dokumentiert und im Phase-SUMMARY erwähnt (Phase nicht blockiert).
    - F07-Doku-Check durchgeführt: entweder keine Änderung nötig (dokumentiert) oder Änderungshistorie in beiden Sprachen aktualisiert.
    - Backend gestoppt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Kein neuer Trust-Boundary — der Umbau ist rein interner Aggregation-Refactor. Auth-Guards (`SHIFTPLANNER_PRIVILEGE`/`SALES_PRIVILEGE`) bleiben unverändert am Anfang von `get_weekly_summary`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-10 | Tampering | In-Memory-Slot-Filter divergiert von DAO-Semantik | high | mitigate | R1: Executor liest `dao_impl_sqlite/src/slot.rs:151-168` und reproduziert exakt (`deleted IS NULL`, `valid_from <= sunday`, `valid_to IS NULL OR valid_to >= monday`, Planning-Filter); Wave-1-Fixture 3 (ShortDay) verifiziert plus zusätzlicher Fixture-Cross-Check über valid_from/valid_to-Randfälle |
| T-52-11 | Tampering | Off-by-one bei Spillover-Index (Woche 53 → Index 0 im next_year Vec) | medium | mitigate | R6: Wave-1-Fixture 8 (Kombi) enthält explizit Spillover-Woche |
| T-52-12 | Elevation of Privilege | Chain-C-Toggle-Read wird versehentlich entfernt oder verschoben | high | mitigate | R8/D-52-09: Grep-Guard im `<done>`; Chain-C-Tests (`booking_information_chain_c.rs`) würden bei Verlust failen |
| T-52-13 | Denial of Service | Latenz-Ziel nicht erreicht → Follow-Up nötig | medium | accept | Als Follow-Up dokumentiert; Phase nicht blockiert wenn Byte-Identität erreicht ist |
</threat_model>

<verification>
- `cargo test --package service_impl booking_information_weekly_summary_year_batch` — alle 8 Fixtures bit-exakt grün.
- `cargo test --package service_impl booking_information_chain_c` grün — Chain-C-Toggle + Slot-Clipping unangetastet.
- `cargo test --workspace` grün.
- `cargo clippy --workspace -- -D warnings` grün.
- Grep-Guards:
  - `grep -c 'special_day_service.get_by_week' service_impl/src/booking_information.rs` = 0 in `get_weekly_summary`-Fn (Zeile 259-491)
  - `grep -c 'extract_shiftplan_report_for_week' service_impl/src/booking_information.rs` = weniger als vorher (`get_summery_for_week` bei ~Zeile 494 darf noch rufen)
  - `grep -c 'get_slots_for_week_all_plans' service_impl/src/booking_information.rs` = 0 in `get_weekly_summary`-Fn
  - `grep -c 'reporting_service.get_week' service_impl/src/booking_information.rs` = weniger als vorher
  - `grep -c 'shortday_gate::read_active_from' service_impl/src/booking_information.rs` >= 1 (bleibt bestehen)
- `52-05-latency-post-refactor.txt` existiert, Median dokumentiert, PASS/FAIL zum 500 ms-Ziel klar markiert.
</verification>

<success_criteria>
- Byte-Identität: alle 8 Wave-1-Fixtures grün nach Umbau.
- Bulk-Load-Muster durchgezogen: pro Endpoint-Request nur je 2 `get_year`/`get_by_year`/`extract_shiftplan_report_for_year` + 1 `get_slots` statt ~55×.
- Chain-C-Toggle-Read bleibt in `booking_information` (D-52-09 respektiert).
- Latenz-Post-Refactor gemessen und dokumentiert; wenn Ziel <500 ms verpasst wird, Follow-Ups dokumentiert.
- Docs-Freshness-Check auf F07 durchgeführt.
</success_criteria>

<output>
Create `.planning/phases/52-weekly-overview-performance-refactor/52-05-SUMMARY.md` when done. Include:
- Grep-Guard-Ergebnisse (alle 4 `_for_week`-Calls in `get_weekly_summary` weg, Toggle-Read bleibt, Chain-C-Tests grün).
- Wave-1-Fixture-Ergebnis (alle 8 bit-exakt grün).
- Latenz-Baseline (Wave 0) vs. Post-Refactor (Wave 4): Werte + Speedup-Faktor + PASS/FAIL zum <500 ms-Ziel.
- Docs-Freshness-Notiz: F07-Änderungshistorie ergänzt / nicht ergänzt (mit Begründung).
- Ggf. Follow-Up-Kandidaten wenn Latenz-Ziel nicht erreicht.
- Bestätigung: Frontend nicht angetastet (D-52-15 — grep `git diff shifty-dioxus/` = leer).
</output>

## Artifacts this phase produces

- Umgebaute `BookingInformationServiceImpl::get_weekly_summary`-Impl (byte-identisch, 7 Bulk-Loads statt ~165 Service-Calls)
- `.planning/phases/52-weekly-overview-performance-refactor/52-05-latency-post-refactor.txt`
- Ggf. `docs/features/F07-reporting-balance.md` + `_de.md` Änderungshistorie-Sektion
