---
phase: 33-special-days-ui-einstellungen
plan: 03
type: execute
wave: 2
depends_on: ["33-01", "33-02"]
files_modified:
  - shifty-dioxus/src/page/settings.rs
autonomous: true
requirements: [SPD-01, SPD-02, SPD-03, SPD-04]

must_haves:
  truths:
    - "D-33-02: Card-3 is gated by its own inner has_privilege(\"shiftplanner\") guard, NOT the page-level admin gate — a shiftplanner sees and uses it without a 403 mismatch (FE gate deckungsgleich to the backend create/delete gate, D-33-01)."
    - "D-33-04: a calendar date entered in the date picker is mapped to (iso_year, iso_week, weekday) via time::Date::parse + to_iso_week_date, then POSTed via create_special_day; the WASM datepicker caveat (D-25-06) is handled by binding through TextInput.on_change (oninput)."
    - "D-33-06: when type = ShortDay the time-of-day field is required and the Add button stays disabled until a valid time is entered; Holiday shows no time field."
    - "D-33-07: a live duplicate check against the loaded year list shows SettingsSpecialDaysDuplicateHint when a special day already exists for the chosen (year, calendar_week, day_of_week)."
    - "D-33-08 / SPD-02: existing special days render chronologically ascending (by calendar_week then day_of_week), each as 'DD.MM.YYYY (Weekday, KW NN, YYYY)' with a Holiday/ShortDay badge (ShortDay also shows its time), with an empty-state when the year has none."
    - "SPD-03: each list row has a Danger delete button calling delete_special_day(id) then reloading the year list with no confirmation dialog."
  artifacts:
    - "shifty-dioxus/src/page/settings.rs — Card-3 section (shiftplanner-gated) + pure helpers parse_date_to_iso_parts, special_day_iso_date, weekday_key, is_duplicate_special_day + #[cfg(test)] tests"
  key_links:
    - "Card-3 inner guard reads AUTH.read().auth_info...has_privilege(\"shiftplanner\"); the page-level admin gate is left unchanged (D-33-02)."
    - "create flow uses api::create_special_day; year list uses api::get_special_days_for_year; delete uses api::delete_special_day (all from Plan 02)."
    - "display context uses time::Weekday::from(DayOfWeekTO) and time::Date::from_iso_week_date — both conversions already exist in rest-types (From<DayOfWeekTO> for Weekday)."
---

<objective>
Add the Settings page Special-Days section (Card-3): a shiftplanner-gated card with a year picker,
a calendar-date create form (Holiday / ShortDay+time), a live duplicate hint, and a chronological
year list with type badges and delete. Implements SPD-01/02/03 on the Settings surface and the
D-33-02/04/06/07/08 decisions.

Purpose: Give shiftplanners a date-oriented management surface that fills the year list in one
request (the SPD-02 list) and reuses the existing Settings card visual language (Card-2 analog).

Output:
- shifty-dioxus/src/page/settings.rs — Card-3 component block + pure host-testable date helpers.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-special-days-ui-einstellungen/33-CONTEXT.md
@.planning/phases/33-special-days-ui-einstellungen/33-RESEARCH.md
@.planning/phases/33-special-days-ui-einstellungen/33-PATTERNS.md
@.planning/phases/33-special-days-ui-einstellungen/33-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure date helpers (parse / display / duplicate) + host tests</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <read_first>
    - shifty-dioxus/src/page/settings.rs (top-of-file imports; Card-2 date handling lines ~135-136 using time::Date::parse)
    - rest-types/src/lib.rs (SpecialDayTO fields; From<time::Weekday> for DayOfWeekTO line 280; From<DayOfWeekTO> for time::Weekday line 293 — both already exist, no manual match)
    - 33-RESEARCH.md § "Pattern 5" + § "Pattern 6" + § "Pitfall 1"
    - 33-UI-SPEC.md § "Date context string construction" + § "Date-to-KW mapping"
  </read_first>
  <behavior>
    - parse_date_to_iso_parts("2026-08-15") == Some((2026u32, 33u8, DayOfWeekTO::Saturday)).
    - parse_date_to_iso_parts("not-a-date") == None.
    - round-trip: special_day_iso_date(&SpecialDayTO built from the parsed parts of "2026-08-15") == time::Date for 2026-08-15.
    - weekday_key(DayOfWeekTO::Saturday) == Key::Saturday (and Monday→Key::Monday).
    - is_duplicate_special_day((2026,33,Saturday), &[entry with same triple]) == true; with a different week == false.
  </behavior>
  <action>
    Add four pure, dioxus-free free functions to settings.rs so they run under host `cargo test`:
    parse_date_to_iso_parts(date_str: &str) -> Option<(u32, u8, DayOfWeekTO)> — parse with
    format_description!("[year]-[month]-[day]"), call to_iso_week_date(), convert weekday via
    DayOfWeekTO::from(weekday); special_day_iso_date(entry: &SpecialDayTO) -> Option<time::Date>
    via time::Date::from_iso_week_date(entry.year as i32, entry.calendar_week, time::Weekday::from(entry.day_of_week));
    weekday_key(day: DayOfWeekTO) -> Key mapping the seven arms to Key::Monday..Key::Sunday;
    is_duplicate_special_day(parts: (u32,u8,DayOfWeekTO), list: &[SpecialDayTO]) -> bool comparing
    year + calendar_week + day_of_week. Add a `#[cfg(test)] mod tests` with the five behaviors above.
    These back the create mapping (D-33-04/SPD-01), the display context (SPD-02), and the duplicate
    check (D-33-07). Do NOT build the Card UI in this task.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo test --lib settings 2>&1 | tail -8) && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - The four helpers exist and are pure (no Signal / no rsx); tests pass under host cargo test.
    - parse + round-trip + weekday_key + duplicate behaviors all green.
  </acceptance_criteria>
  <done>Helper tests green; date mapping + duplicate logic locked before UI wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Card-3 shell + year picker + create form (shiftplanner-gated)</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <read_first>
    - shifty-dioxus/src/page/settings.rs (is_admin guard lines 28-38; Card-2 block lines 99-295: signal decls, use_resource, spawn save, inline feedback)
    - shifty-dioxus/src/component/form/inputs.rs (TextInput input_type/value/on_change — oninput, WASM-safe line 54; SelectInput)
    - shifty-dioxus/src/component/atoms/btn.rs (Btn variant + disabled prop)
    - 33-UI-SPEC.md § "Surface 2: Settings Page — Card-3" (Rows A-D) + § "Form validity rules" + § "Component Inventory"
    - 33-PATTERNS.md § "settings.rs — add Card-3" + § "Shared Patterns" (spawn + saving-guard, use_resource)
  </read_first>
  <action>
    Derive `is_shiftplanner` from AUTH (has_privilege("shiftplanner")) and render Card-3 only inside
    `if is_shiftplanner { ... }`, leaving the page-level admin gate untouched (D-33-02). Use the card
    shell + label-row classes from 33-UI-SPEC (identical to Card-2). Declare signals for year,
    date_str, selected type (Option<SpecialDayTypeTO>), time_str, save_result, saving. Load the year
    list with use_resource over api::get_special_days_for_year(config, year). Build Row B (year picker,
    w-24 number input), Row C (date TextInput type="date" via on_change; SelectInput Holiday/ShortDay;
    conditional time TextInput type="time" only when ShortDay per D-33-06; Btn Primary
    SettingsSpecialDaysAddBtn with disabled = !form_valid where form_valid = !date_str.is_empty() &&
    (type != ShortDay || !time_str.is_empty())). On submit: guard saving, build SpecialDayTO from
    parse_date_to_iso_parts (Task 1) + selected type + parsed time (None for Holiday), spawn
    api::create_special_day, on Ok set save_result + restart the resource, on Err set save_result(false);
    always clear saving. Row D: show SettingsSpecialDaysDuplicateHint (text-bad) when
    is_duplicate_special_day matches the current date against the loaded list (D-33-07); reuse
    Key::SettingsSaveError on create error and Key::SettingsSaved as the success flash. Bind the date
    input via TextInput.on_change (oninput) per the WASM caveat D-25-06 — do not use onchange.
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5) && cargo clippy --workspace -- -D warnings</automated>
    <human-check>Browser as shiftplanner: open Settings; Card-3 visible. Pick a date, type=ShortDay → time field appears and Add stays disabled until a time is set; type=Holiday → no time field. Pick a date that already has an entry → duplicate hint shows. Create a Holiday → it appears in the list and persists across reload (D-25-06 persist/display loop). As a non-shiftplanner the card is hidden.</human-check>
  </verify>
  <acceptance_criteria>
    - Card-3 renders only when is_shiftplanner; page admin gate unchanged.
    - ShortDay requires a time (Add disabled otherwise); Holiday has no time field.
    - Duplicate hint appears live for an existing (year, week, weekday); create POSTs and reloads the list.
    - WASM build green.
  </acceptance_criteria>
  <done>Create form + year picker functional and gated; WASM green; browser persist loop confirmed at verify-work.</done>
</task>

<task type="auto">
  <name>Task 3: Card-3 chronological list (badges, context string, delete) + empty state</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <read_first>
    - shifty-dioxus/src/page/settings.rs (Card-3 from Task 2; the loaded year-list resource)
    - shifty-dioxus/src/i18n/mod.rs (format_date trait method line ~35; Key::Monday..Sunday; new Key variants from Plan 02)
    - 33-UI-SPEC.md § "Card-3 structure" Row E + § "Copywriting Contract" + § "Delete feedback"
    - 33-PATTERNS.md § "Pattern 6: Date-Kontext-String" (via Task-1 helpers)
  </read_first>
  <action>
    Render Row E from the loaded year list. Empty-state (div.py-6.text-center +
    SettingsSpecialDaysEmptyBody with the {year}) when the list is empty. Otherwise iterate entries
    already ordered ascending by (calendar_week, day_of_week) from the backend; per row build the
    display string with i18n.format_date(special_day_iso_date(entry)) + weekday name via
    i18n.t(weekday_key(entry.day_of_week)) + SettingsSpecialDaysCalendarWeekAbbr + calendar_week + year,
    matching '15.08.2026 (Samstag, KW 33, 2026)' (D-33-08/SPD-02). Show a Holiday badge
    (bg-accent-soft text-accent) or ShortDay badge (bg-warn-soft text-warn); for ShortDay also show the
    time_of_day. Add a Btn Danger SettingsSpecialDaysDeleteBtn per row that, with a saving guard,
    spawns api::delete_special_day(entry.id) then restarts the resource — no confirmation dialog
    (SPD-03); on error show SettingsSpecialDaysDeleteError inline below the list. Use the badge / list
    / delete classes verbatim from 33-UI-SPEC § "Component Inventory".
  </action>
  <verify>
    <automated>(cd shifty-dioxus && cargo build --target wasm32-unknown-unknown 2>&1 | tail -5) && cargo clippy --workspace -- -D warnings</automated>
    <human-check>Browser as shiftplanner: the list shows each entry as 'DD.MM.YYYY (Weekday, KW NN, YYYY)' with the correct badge (and time for ShortDay), sorted ascending. Delete a row → it disappears immediately and stays gone after reload. Switch year → list reloads. Empty year shows the empty-state text.</human-check>
  </verify>
  <acceptance_criteria>
    - List renders the locale date + derived context + badge (+ ShortDay time), ascending.
    - Delete removes the entry with no dialog and reloads; delete error shows inline.
    - Empty-state shown for years with no entries; WASM build green.
  </acceptance_criteria>
  <done>Year list, badges, context string, delete, and empty-state complete; WASM green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user role → Card-3 UI | The card must only act for shiftplanners; a privilege mismatch would surface as a 403. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-06 | Elevation of Privilege (UX) | Card-3 visibility | medium | mitigate | Inner has_privilege("shiftplanner") guard matches the backend create/delete gate (D-33-01/02), so no shiftplanner-less user sees an actionable-then-403 card. |
| T-33-07 | Tampering (double submit) | create/delete actions | low | mitigate | saving-guard disables the action and the button while a spawn is in flight (33-UI-SPEC interaction contract). |
</threat_model>

<verification>
- `cd shifty-dioxus && cargo test --lib settings` green (helper tests).
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` green.
- `cargo clippy --workspace -- -D warnings` from backend root before commit.
- Manual (verify-work): datepicker persist/display loop + shiftplanner gating (D-25-06).
</verification>

<success_criteria>
- A shiftplanner can create (Holiday/ShortDay+time), see the chronological year list with badges + derived context, and delete — all from Settings, gated correctly with no 403 mismatch.
- All Phase-33 Settings decisions (D-33-02/04/06/07/08) are observable.
</success_criteria>

<output>
Create `.planning/phases/33-special-days-ui-einstellungen/33-03-SUMMARY.md` when done.
</output>
