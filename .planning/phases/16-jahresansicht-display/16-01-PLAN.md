---
phase: 16-jahresansicht-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service_impl/src/booking_information.rs
  - rest-types/src/lib.rs
autonomous: true
requirements: [CVC-07]
tags: [D-01, CVC-07]

must_haves:
  truths:
    - "D-01: overall_available_hours der Jahresansicht enthält committed_voluntary_hours (paid + committed + surplus)"
    - "CVC-07: WeeklySummaryTO trägt committed_voluntary_hours und mappt es aus WeeklySummary"
    - "D-01: nur die ERSTE get_weekly_summary-Variante (Z.273) wird geändert; die zweite get_summery_for_week (Z.386/547) bleibt unberührt"
  artifacts:
    - path: "service_impl/src/booking_information.rs"
      provides: "overall_available_hours = committed + volunteer + paid (erste Variante)"
      contains: "committed_voluntary_hours + volunteer_hours + paid_hours"
    - path: "rest-types/src/lib.rs"
      provides: "WeeklySummaryTO.committed_voluntary_hours + From-Mapping"
      contains: "committed_voluntary_hours"
  key_links:
    - from: "service_impl/src/booking_information.rs (WeeklySummary.committed_voluntary_hours)"
      to: "rest-types WeeklySummaryTO.committed_voluntary_hours"
      via: "impl From<&WeeklySummary> for WeeklySummaryTO"
      pattern: "committed_voluntary_hours: weekly_summary.committed_voluntary_hours"
---

<objective>
Backend-/Transport-Wave: Den in Phase 15 bereits berechneten `committed_voluntary_hours`-Term (Band 1, „zugesagt") in `overall_available_hours` der Jahresansicht einrechnen (D-01) und durch `WeeklySummaryTO` + `From<&WeeklySummary>` wire-fähig machen (CVC-07b).

Purpose: Damit die Diff-Spalte (verfügbar − benötigt), der Chart und alle Konsumenten konsistent dieselbe „verfügbar"-Zahl sehen, MUSS die Summe im Backend entstehen (D-01) — nicht erst im Frontend. Ohne das TO-Feld bricht zudem der Frontend-WASM-Compile (einzige `rest-types`-Crate).

Output: Geänderte Summenzeile (1 Token) + neues TO-Feld + Mapping-Arm + From-Roundtrip-Test.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located mit git). GSD-Auto-Commit ist deaktiviert (`commit_docs: false`). Der EXECUTOR committet NICHT selbst — der User committet manuell via jj. NIEMALS `git commit`/`git add`/`git checkout`. Falls ein Commit nötig erscheint: an den User zurückgeben.
</vcs_jj_only>
</execution_context>

<context>
@.planning/phases/16-jahresansicht-display/16-CONTEXT.md
@.planning/phases/16-jahresansicht-display/16-PATTERNS.md
@.planning/phases/16-jahresansicht-display/16-RESEARCH.md

<critical_facts>
- Es gibt ZWEI `WeeklySummary`-Producer in `service_impl/src/booking_information.rs`. NUR die ERSTE Variante `get_weekly_summary(year, …)` (ab Z.136, Summenzeile Z.273) speist die Jahresansicht (Achse B) → NUR DIESE ändern.
- Die ZWEITE Variante `get_summery_for_week(year, week, …)` (Z.297-562) ist Einzel-Wochen-only mit `committed_voluntary_hours: 0.0`-Placeholder (Z.547) und `overall_available_hours = volunteer + paid` (Z.386). DIESE BLEIBT UNVERÄNDERT — kein Touch (Research-Flag aufgelöst, RESEARCH § „Kritischer Research-Flag", Pitfall 2).
- KEIN Snapshot-Bump: `WeeklySummary` ist year-view-only, nicht persistiert. `CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt 7.
- KEIN OpenAPI-Task: `WeeklySummaryTO` hat kein `ToSchema`-Derive / `#[utoipa::path]` — nicht ergänzen.
- Der `WeeklySummary { … }`-Literal-Arm in der ersten Variante befüllt `committed_voluntary_hours` bereits korrekt (Phase 15) — NICHT anfassen, nur die Summenzeile.
</critical_facts>

<interfaces>
Service-Struct (bereits vorhanden, Phase 15) — `service/src/booking_information.rs`:
```rust
pub struct WeeklySummary {
    pub year: u32,
    pub week: u8,
    pub overall_available_hours: f32,
    pub paid_hours: f32,
    pub volunteer_hours: f32,            // Band 2 = per-person surplus (Phase 15)
    pub committed_voluntary_hours: f32,  // Band 1 = pledge (Phase 15)
    // ... weitere Felder ...
}
```

IST in `service_impl/src/booking_information.rs:270-273` (erste Variante):
```rust
// overall_available_hours stays volunteer_hours + paid_hours (Pitfall 2 — Phase 16 wires display).
// NOTE: volunteer_hours is now the per-person surplus (Band 2); the pledge (Band 1) lives
// in committed_voluntary_hours separately. Phase 16 will sum both bands for display.
let overall_available_hours = volunteer_hours + paid_hours;
```

IST in `rest-types/src/lib.rs:905-920` (Struct) + `:922-948` (From): `committed_voluntary_hours` fehlt in Struct UND Mapping-Arm. Analog: das bereits vorhandene `volunteer_hours`-Feld (Z.911) + Mapping-Arm (Z.930).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-01 — committed in overall_available_hours der Jahresansicht einrechnen</name>
  <files>service_impl/src/booking_information.rs</files>
  <read_first>
    - service_impl/src/booking_information.rs:136-295 (ERSTE Variante `get_weekly_summary` — der zu ändernde Pfad; Band-1/Band-2-Berechnung Z.207-226, Summenzeile Z.270-273, WeeklySummary-Literal Z.274-290)
    - service_impl/src/booking_information.rs:297-562 (ZWEITE Variante `get_summery_for_week` — NUR lesen, um zu bestätigen dass sie NICHT geändert wird; Z.386 + Z.547 bleiben)
    - .planning/phases/16-jahresansicht-display/16-PATTERNS.md § „service_impl/src/booking_information.rs" (IST/SOLL-Diff Z.59-77)
  </read_first>
  <behavior>
    - Test: In der ersten `get_weekly_summary`-Variante gilt nach D-01 für jede Woche `overall_available_hours == paid_hours + committed_voluntary_hours + volunteer_hours`.
    - Test (No-double-count, D-04): Mit `paid=10, committed=5, volunteer(surplus)=2` ist `overall_available_hours == 17.0` (committed wurde in Band 2 bereits per-Person abgezogen → keine Doppelzählung).
    - Test (Regression): Die zweite Variante `get_summery_for_week` liefert weiterhin `overall_available_hours = volunteer + paid` und `committed_voluntary_hours = 0.0` (unverändert).
  </behavior>
  <action>
    In `service_impl/src/booking_information.rs`, ERSTE Variante `get_weekly_summary`, die Summenzeile Z.273 ändern.

    IST (Z.273):
    ```rust
    let overall_available_hours = volunteer_hours + paid_hours;
    ```
    SOLL (D-01 — committed-Term ergänzen):
    ```rust
    let overall_available_hours = committed_voluntary_hours + volunteer_hours + paid_hours;
    ```
    Den darüberstehenden Phase-15-TODO-Kommentar (Z.270-272 „Phase 16 will sum both bands for display") durch einen kurzen Hinweis ersetzen, z.B.:
    ```rust
    // D-01 (Phase 16): available = paid + committed (Band 1, pledge) + volunteer (Band 2, surplus).
    // No double-count: Band 2 already subtracted committed per-person (Σ max(actual−committed,0)).
    ```
    Das `committed_voluntary_hours`-Binding existiert in diesem Scope bereits (Z.219-226, Phase 15) — KEINE neue Berechnung.

    NICHTS in der ZWEITEN Variante `get_summery_for_week` ändern: Z.386 (`= volunteer + paid`) und Z.547 (`committed_voluntary_hours: 0.0`) bleiben byte-identisch. Dieser Pfad bedient den Einzel-Wochen-Endpoint, nicht die Jahresansicht (RESEARCH Pitfall 2).

    Bestehende `service_impl`-Tests zu `get_weekly_summary` um einen Fall erweitern, der `overall_available_hours == paid + committed + volunteer` pinnt (CVC-07a) und die No-double-count-Invariante (`paid + committed + max(actual−committed,0)`) abdeckt. Test-Platzierung: bei den bestehenden `booking_information`-Tests im `service_impl`-Test-Modul.
  </action>
  <verify>
    <automated>cargo test -p service_impl booking_information</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "committed_voluntary_hours + volunteer_hours + paid_hours" service_impl/src/booking_information.rs` liefert genau EINEN Treffer (erste Variante, Z.~273).
    - `grep -vn '^[[:space:]]*//' service_impl/src/booking_information.rs | grep -c "let overall_available_hours = volunteer_hours + paid_hours;"` liefert `1` (die unveränderte zweite Variante, Z.386).
    - `grep -n "committed_voluntary_hours: 0.0" service_impl/src/booking_information.rs` liefert weiterhin genau EINEN Treffer (zweite Variante, Z.547 — unverändert).
    - `cargo test -p service_impl booking_information` grün; der neue Test, der `overall_available_hours == paid + committed + volunteer` pinnt, ist enthalten und grün.
  </acceptance_criteria>
  <done>Erste Variante summiert committed in overall_available_hours; zweite Variante unverändert; Test pinnt die Summe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WeeklySummaryTO trägt committed_voluntary_hours + From-Mapping + Roundtrip-Test</name>
  <files>rest-types/src/lib.rs</files>
  <read_first>
    - rest-types/src/lib.rs:904-948 (`WeeklySummaryTO`-Struct + `impl From<&WeeklySummary>`; Analog: `volunteer_hours`-Feld Z.911 + Mapping-Arm Z.930)
    - rest-types/src/lib.rs:2175-2231 (bestehendes `committed_voluntary`-Roundtrip-Test-Muster aus Phase 14 — Vorlage für den neuen From-Test)
    - .planning/phases/16-jahresansicht-display/16-PATTERNS.md § „rest-types/src/lib.rs — WeeklySummaryTO" (Z.85-122)
  </read_first>
  <behavior>
    - Test (From-Roundtrip): `WeeklySummaryTO::from(&ws).committed_voluntary_hours == ws.committed_voluntary_hours` für `ws.committed_voluntary_hours = 5.0`.
    - Test (Wire-Backward-Compat): JSON ohne `committed_voluntary_hours` deserialisiert zu `WeeklySummaryTO` mit `committed_voluntary_hours == 0.0` (via `#[serde(default)]`), kein Deserialisierungs-Fehler.
  </behavior>
  <action>
    In `rest-types/src/lib.rs`:

    (1) Struct-Feld nach Z.911 (`pub volunteer_hours: f32,`) hinzufügen — mit `#[serde(default)]` (Pitfall 7, Wire-Backward-Compat, Pattern wie `EmployeeWorkDetailsTO` Phase 14):
    ```rust
    pub volunteer_hours: f32,
    #[serde(default)]
    pub committed_voluntary_hours: f32,
    ```

    (2) Mapping-Arm im `#[cfg(feature = "service-impl")] impl From<&WeeklySummary> for WeeklySummaryTO` nach Z.930 (`volunteer_hours: weekly_summary.volunteer_hours,`) hinzufügen:
    ```rust
    volunteer_hours: weekly_summary.volunteer_hours,
    committed_voluntary_hours: weekly_summary.committed_voluntary_hours,
    ```

    KEIN `ToSchema`-Derive ergänzen (WeeklySummaryTO hat keine OpenAPI-Anbindung). KEIN Snapshot-Bump.

    (3) From-Roundtrip-Test im rest-types-Test-Modul ergänzen (analog dem bestehenden `committed_voluntary_fractional_survives_service_to_to_roundtrip`-Muster Z.2224): konstruiere eine `WeeklySummary` (hinter `#[cfg(feature = "service-impl")]` — wo auch der bestehende Test lebt) mit `committed_voluntary_hours = 5.0`, mappe zu `WeeklySummaryTO`, asserte `(to.committed_voluntary_hours - 5.0).abs() < f32::EPSILON`. Zusätzlich ein serde-default-Test: deserialisiere ein JSON-Objekt ohne `committed_voluntary_hours` und asserte das Feld == 0.0.
  </action>
  <verify>
    <automated>cargo test -p rest-types committed_voluntary && cargo build -p rest-types --features service-impl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "committed_voluntary_hours" rest-types/src/lib.rs` enthält Treffer im `WeeklySummaryTO`-Struct (mit vorausgehendem `#[serde(default)]`) UND im `From<&WeeklySummary>`-Mapping-Arm.
    - `grep -n "ToSchema" rest-types/src/lib.rs` zeigt KEINEN neuen ToSchema auf `WeeklySummaryTO` (Struct-Derive bleibt `#[derive(Clone, Debug, Serialize, Deserialize)]`).
    - `grep -n "CURRENT_SNAPSHOT_SCHEMA_VERSION" service_impl/src/billing_period_report.rs` zeigt weiterhin `= 7` (kein Bump).
    - `cargo test -p rest-types committed_voluntary` grün; From-Roundtrip-Test + serde-default-Test enthalten und grün.
  </acceptance_criteria>
  <done>WeeklySummaryTO trägt committed_voluntary_hours (mit serde-default), From mappt es, Roundtrip- und serde-default-Test grün.</done>
</task>

<task type="auto">
  <name>Task 3: Backend-Gate — Workspace-Test grün</name>
  <files>service_impl/src/booking_information.rs, rest-types/src/lib.rs</files>
  <read_first>
    - .planning/phases/16-jahresansicht-display/16-VALIDATION.md § „Sampling Rate" (Wave-Merge-Gate)
  </read_first>
  <action>
    Hartes Backend-Gate für diese Wave: `cargo test --workspace` am Backend-Root muss grün sein (keine Regression durch die TO-Erweiterung oder die Summenzeilen-Änderung). Falls ein Test rot ist, der wegen der neuen `overall_available_hours`-Summe abweicht (z.B. ein bestehender `get_weekly_summary`-Test, der die alte `volunteer + paid`-Summe pinnte), den erwarteten Wert auf `paid + committed + volunteer` korrigieren — NICHT die Produktionslogik zurückdrehen. Auf NixOS bei Bedarf `nix develop` für die Toolchain nutzen.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` exit 0, 0 failures.
  </acceptance_criteria>
  <done>Workspace-Suite grün; keine Regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST `GET /booking-information/weekly-resource-report/{year}` | bestehender, bereits permission-gegateter Read-Endpoint (`shiftplanner.or(sales)?`); kein neuer Input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | `WeeklySummaryTO.committed_voluntary_hours` (neues Read-Feld) | accept | Server-seitig aggregiert (Σ pro Woche), read-only, kein PII; gleicher Permission-Gate wie die bereits exponierten paid/volunteer-Aggregate. Kein neuer Leak-Pfad. |
| T-16-02 | Tampering | `overall_available_hours`-Berechnung | accept | Reine additive Server-Berechnung aus bereits gegateten Inputs; kein client-beeinflusster Wert; year-view-only, nicht persistiert (kein Snapshot-Integritäts-Risiko). |

Keine High-Severity-Threats: display-only Read-Erweiterung, kein neuer Auth-Surface, kein persistierter Schreibpfad, kein neues Secret. ASVS L1 V2/V3/V4/V5/V6 nicht anwendbar bzw. durch bestehende Gates abgedeckt.
</threat_model>

<verification>
- `cargo test -p service_impl booking_information` grün (D-01-Summe + No-double-count).
- `cargo test -p rest-types committed_voluntary` grün (From-Roundtrip + serde-default).
- `cargo test --workspace` grün (keine Regression).
- Zweite `get_summery_for_week`-Variante nachweislich unverändert (grep-Gates in Task 1).
</verification>

<success_criteria>
- `overall_available_hours = committed + volunteer + paid` in der ersten Variante (D-01); zweite Variante unberührt.
- `WeeklySummaryTO.committed_voluntary_hours` existiert mit serde-default + From-Mapping (CVC-07b).
- Kein Snapshot-Bump, kein ToSchema, kein OpenAPI-Change.
- Workspace-Tests grün.
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-jahresansicht-display/16-01-SUMMARY.md` erstellen.
</output>
