---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service/src/slot.rs
autonomous: true
requirements: [SHC-01]
must_haves:
  truths:
    - "D-51-01: `Slot::clip_to(cutoff: time::Time) -> Option<Slot>` exists on `service::slot::Slot`"
    - "D-04 case 1 (`slot.end <= cutoff`) returns `Some(unchanged)`"
    - "D-04 case 2 (`slot.end == cutoff`) returns `Some(unchanged)` (no special case)"
    - "D-04 case 3 (`slot.start >= cutoff`) returns `None`"
    - "D-04 case 4 (`slot.start < cutoff < slot.end`) returns `Some({..slot, to: cutoff})`"
  artifacts:
    - "`service/src/slot.rs` contains `impl Slot { pub fn clip_to(...) -> Option<Slot> }`"
    - "`service/src/slot.rs` contains `#[cfg(test)] mod tests` with four D-04 test cases"
  key_links:
    - "Method lives on the Slot struct (D-51-01) so all four BE-Aggregat-Ketten (P03/P04/P05/P06) can consume it directly without a shared-crate detour"
---

<objective>
Kanonische Slot-Clip-Funktion auf `service::slot::Slot` implementieren (D-51-01) und mit
vier D-04-Grenzfall-Tests absichern. Reine Fachlogik, keine Seiteneffekte, keine neue
Cargo-Dep.

Purpose: SHC-01 — eine einzige Wahrheit für Slot-Kürzung, konsumiert von allen vier
BE-Aggregat-Ketten in P03/P04/P05/P06 und (indirekt via P06) von Chain D.

Output: `service/src/slot.rs` mit `impl Slot { clip_to }` und `#[cfg(test)] mod tests` am
Dateiende (4 Cases). Kein anderer Code angefasst.
</objective>

<execution_context>
Auto-commit via git (jj-import) — kein manueller VCS-Schritt nötig.
Backend-Workspace: `/home/neosam/programming/rust/projects/shifty/shifty-backend`.
</execution_context>

<context>
- `service/src/slot.rs:12-25` — bestehende `pub struct Slot`. Feld-Typen: `from`, `to: time::Time`.
- `service/src/special_days.rs:41` — `SpecialDay.time_of_day: Option<time::Time>` (Konsum-Seite; nicht hier angepasst).
- CONTEXT.md D-51-01 (Ort), D-04 (Semantik-Tabelle), RESEARCH.md § "Slot Struct — signature for clip_to" (empfohlene Signatur, verbatim übernehmbar).
- Repo-Konvention: `#[cfg(test)] mod tests` am Dateiende mit `use super::*;` (kein separates `test/`-Modul für pure Fachobjekt-Fns).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `Slot::clip_to`</name>
  <files>service/src/slot.rs</files>
  <action>
Am Ende von `service/src/slot.rs` (nach `impl Ord for Slot { ... }` bei Zeile 72,
aber vor der `#[automock]`-Annotation von `SlotService` bei Zeile 74) einen neuen
`impl Slot`-Block einfügen mit genau einer Method `pub fn clip_to(&self, cutoff: time::Time) -> Option<Slot>`.

Semantik nach D-04 (aus CONTEXT.md / notes/shortday-slot-clipping-semantics.md):
- Wenn `self.from >= cutoff` → `None` (Slot komplett hinter Cutoff, D-04 Zeile 3).
- Sonst wenn `self.to <= cutoff` → `Some(self.clone())` (Slot endet spätestens am
  Cutoff, D-04 Zeile 1+2 zusammengefasst — Gleichheit ist explizit kein Sonderfall).
- Sonst → `Some(Slot { to: cutoff, ..self.clone() })` (Slot überlappt, wird auf
  Cutoff verkürzt, D-04 Zeile 4).

Doc-Kommentar (auf Deutsch, kurz — Executor darf leicht angleichen, aber Inhalt
gleich): erklärt die drei Ausgänge und referenziert "D-51-01 / D-04". Keine Panics,
keine `.unwrap()`, keine `unsafe`, keine `mut`-Args.

Implementierung folgt exakt der in `51-RESEARCH.md` unter "Empfohlene Signatur"
angegebenen Form (verbatim übernehmbar). Kein weiteres Refactoring an der Datei;
das existierende `impl From<...>`/`impl PartialOrd`/`impl Ord` unangetastet lassen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build -p service</automated>
  </verify>
  <done>
`service/src/slot.rs` enthält `impl Slot { pub fn clip_to(&self, cutoff: time::Time) -> Option<Slot> }` mit den drei Zweigen nach D-04. `cargo build -p service` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-04 unit tests</name>
  <files>service/src/slot.rs</files>
  <behavior>
    - Test 1 — `clip_to_leaves_slot_unchanged_when_slot_ends_before_cutoff`: Slot 08:00–12:00, Cutoff 14:30 → `Some(slot)` mit `from=08:00`, `to=12:00`.
    - Test 2 — `clip_to_leaves_slot_unchanged_when_slot_ends_exactly_at_cutoff`: Slot 13:00–14:30, Cutoff 14:30 → `Some(slot)` mit `to=14:30` (kein Sonderfall, D-04 Zeile 2).
    - Test 3 — `clip_to_returns_none_when_slot_starts_at_or_after_cutoff`: Slot 14:30–15:30 mit Cutoff 14:30 → `None`; Slot 15:00–16:00 mit Cutoff 14:30 → `None`.
    - Test 4 — `clip_to_shrinks_slot_when_slot_overlaps_cutoff`: Slot 14:00–15:00, Cutoff 14:30 → `Some(slot)` mit `from=14:00`, `to=14:30`; alle anderen Felder (`id`, `day_of_week`, `min_resources`, `max_paid_employees`, `valid_from`, `valid_to`, `deleted`, `version`, `shiftplan_id`) unverändert.
  </behavior>
  <action>
Am Dateiende von `service/src/slot.rs` (nach dem `pub trait SlotService`-Block bei
Zeile ~130) ein `#[cfg(test)] mod tests { use super::*; ... }` einfügen. Der Modul
enthält vier `#[test]`-Funktionen (Namen wie in `<behavior>` oben).

Fixture-Helper `fn make_slot(from: time::Time, to: time::Time) -> Slot`: konstruiert
einen Slot mit `id: Uuid::new_v4()`, `day_of_week: shifty_utils::DayOfWeek::Tuesday`,
`min_resources: 1`, `max_paid_employees: None`, `valid_from: time::Date::from_calendar_date(2026, time::Month::January, 1).unwrap()`,
`valid_to: None`, `deleted: None`, `version: Uuid::new_v4()`, `shiftplan_id: None`.

Time-Konstruktion: `time::Time::from_hms(H, M, 0).unwrap()`. Test 4 muss zusätzlich
prüfen, dass `id`, `day_of_week`, `min_resources`, `valid_from`, `version` zwischen
Input-Slot und Output-Slot identisch sind (`assert_eq!`), damit die
"nur-`to`-mutieren"-Regel per Test verankert ist.

Keine Snapshot-Tests, keine `insta`-Deps, keine `assert_matches!`-Makros (Repo
verwendet plain `assert!`/`assert_eq!`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service --lib slot::tests</automated>
  </verify>
  <done>
Vier D-04-Tests kompilieren und grün. `cargo test -p service --lib slot::tests` läuft mit "4 passed; 0 failed".
  </done>
</task>

<task type="auto">
  <name>Task 3: Workspace-Gates</name>
  <files>(gate only — no code change)</files>
  <action>
Nach den beiden vorherigen Tasks die Standard-Workspace-Gates fahren, um sicherzugehen,
dass nichts anderes bricht (der `impl Slot`-Block ist additiv, aber die
`use`-Sichtbarkeit könnte theoretisch Konsumenten irritieren).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test --workspace && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
`cargo test --workspace` grün (781+ Tests, kein Rückgang). `cargo clippy --workspace -- -D warnings` grün. Kein Snapshot-Bump, keine Migration, keine neue Cargo-Dep.
  </done>
</task>

</tasks>

<verification>
- `service/src/slot.rs` hat neu genau: einen `impl Slot { pub fn clip_to(...) -> Option<Slot> }`-Block plus ein `#[cfg(test)] mod tests`-Modul am Ende.
- Kein Konsument-Code (block.rs, shiftplan.rs, booking_information.rs, shiftplan_report.rs) angefasst — Konsum kommt in Wave 2.
- `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` beide grün.
</verification>

<success_criteria>
1. Verify-Command aus Task 2 zeigt "4 passed" für `slot::tests::*`.
2. `cargo test --workspace` weiterhin grün; keine Regression an bestehenden Tests.
3. `cargo clippy --workspace -- -D warnings` grün — insbesondere kein `dead_code`-Lint für `clip_to` (wird in P03/P04/P05/P06 konsumiert, aber solange nur die Tests-Konsumenten sind, muss `#[allow(dead_code)]` NICHT gesetzt werden — `pub`-Sichtbarkeit reicht dem Compiler).
4. Kein `CURRENT_SNAPSHOT_SCHEMA_VERSION`-Change (Snapshot bleibt 12).
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-01-SUMMARY.md` mit
Kurz-Zusammenfassung (Signature, Test-Zeilen-Nummern, D-04-Mapping-Tabelle).
</output>
