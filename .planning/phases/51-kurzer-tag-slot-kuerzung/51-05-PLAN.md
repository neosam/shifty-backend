---
phase: 51
plan: 05
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - service_impl/src/booking_information.rs
  - service_impl/src/test/booking_information.rs
autonomous: true
requirements: [SHC-02, SHC-05, SHC-06]
must_haves:
  truths:
    - "D-51-06 Chain C: `get_weekly_summary` (booking_information.rs:388-409) clippt Slots via `Slot::clip_to`, statt sie zu verwerfen (pre-existing Bug fix)"
    - "D-51-06 Chain C: `get_summery_for_week` (booking_information.rs:506-525) clippt Slots via `Slot::clip_to`, statt sie zu verwerfen"
    - "D-51-06 Chain C: `required_hours_by_day` (booking_information.rs:680-697) konsumiert bereits die pro-Tag-geclippte `slots`-Liste — kein zweiter Fix nötig"
    - "D-04 Zeile 4: `slot_hours = (slot.to - slot.from) * min_resources` liefert bei geclipptem 14:00–15:00→14:00–14:30 die halbe Zeit"
    - "D-51-03: Booking-Create bleibt unangefasst — kein `ShiftplanEditService`-Change; ein Test verifiziert, dass ein Booking auf Post-Cutoff-Slot NICHT abgelehnt wird"
    - "D-51-07: Stichtag-Gate pro Slot-Wochentag greift vor Clip"
    - "SHC-05: Existierende Bookings auf geclippten Slots werden nicht rewritten; nur `slot_hours`-Berechnung sieht die kürzere Dauer"
  artifacts:
    - "`service_impl/src/booking_information.rs:388-409` — Filter-Anti-Pattern durch Clip ersetzt"
    - "`service_impl/src/booking_information.rs:506-525` — Filter-Anti-Pattern durch Clip ersetzt"
    - "`service_impl/src/test/booking_information.rs` — neue Tests: `slot_hours` mit Clip (0,5h statt 1h), `slot_hours` ohne Gate (1h), Post-Cutoff-Slot fehlt in `required_hours_by_day`, Stichtag-Grenzfall, Booking-Create-nicht-abgelehnt"
  key_links:
    - "Chain C ist unabhängig von BlockService (Research §Q-01.4). Fix nur hier — hat keinen Effekt auf Chain A'"
    - "`required_hours_by_day` (:680-697) foldet über die bereits geclippte `slots`-Variable (aus Zeile 506-521) — automatisch korrekt"
---

<objective>
Chain C (BookingInformation — WeeklySummary + Booking-Conflicts) — pro-Slot-Clip an den
zwei bestehenden ShortDay-Filter-Sites einbauen. Auch hier ist das aktuelle Verhalten
ein pre-existing Bug (Slot komplett verwerfen statt clippen — verletzt D-04). Der Fix
implementiert das Feature UND fixt den Bug.

Purpose: SHC-02 (Ist-Stunden im weekly summary sehen geclippte Dauer), SHC-05
(existierende Bookings überleben), SHC-06 (Stichtag-Gate), D-51-03 (Booking-Create
bleibt unangefasst — durch Test verifiziert).

Output: `get_weekly_summary` und `get_summery_for_week` clippen. `required_hours_by_day`
fällt automatisch mit. Test-Suite mit `slot_hours`-Grenzfall + Booking-Create-Verify.
</objective>

<execution_context>
Auto-commit via git (jj-import).
`BookingInformationServiceImpl` ist Business-Logic-Tier; hat bereits `SpecialDayService`
als Basic-Dep (`service_impl/src/booking_information.rs:115`). Wir brauchen nur noch
`ToggleService` als weitere Basic-Dep. Bleibt zyklen-frei.
</execution_context>

<context>
- `service_impl/src/booking_information.rs:108-122` — `gen_service_impl!`-Block. `SpecialDayService` (Zeile 115) schon da; hier `ToggleService` ergänzen.
- `service_impl/src/booking_information.rs:388-409` (`get_weekly_summary`):
  - `:388-403` — Slot-Fetch + Filter-Anti-Pattern (`:394-401`, kompletter Slot verworfen wenn `slot.to > cutoff`).
  - `:404-409` — `slot_hours`-Berechnung: `(slot.to - slot.from).as_seconds_f32() / 3600.0 * slot.min_resources as f32`.
  - `:335` — `special_days` bereits gefetched (nutzt vorhandenen `special_day_service.get_by_week`).
- `service_impl/src/booking_information.rs:506-525` (`get_summery_for_week`):
  - `:494-497` — `special_days` bereits gefetched.
  - `:506-521` — analoges Filter-Anti-Pattern.
  - `:522-525` — analoge `slot_hours`-Berechnung.
- `service_impl/src/booking_information.rs:680-697` (`required_hours_by_day`):
  - Foldet über die geclippte `slots`-Variable aus dem Method-Scope. Ist Auto-Korrekt nach Fix.
- `service_impl/src/reporting.rs:164-180` — Toggle-Konsum-Muster.
- CONTEXT.md D-51-06 (Chain C), D-51-03 (Booking-Create), D-51-07 (Gate).
- Research §Risks 2: `:394-401` und `:512-519` sind die Bug-Sites.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chain C site 1 — `get_weekly_summary` clippt</name>
  <files>service_impl/src/booking_information.rs</files>
  <behavior>
    - Vor dem `slots`-Fetch (`:388-403`) den Toggle-Lookup einbauen (einmal pro Method-Call): `active_from: Option<time::Date>` via `shortday_gate::parse_active_from(...)`.
    - Die `slots`-Kette (`:388-403`) neu formen: aus dem `.filter(...).cloned().collect()` wird eine `.filter_map(|slot| { ... })`-Chain, die pro Slot:
      - Holiday-Filter: wenn ShortDay/Holiday am Wochentag → wie bisher (Holiday killt Slot ganz).
      - ShortDay-Gate + Clip: Cutoff für `slot.day_of_week` aus `special_days` extrahieren; wenn `active_from` und `should_clip(booking_date, active_from)` true → `slot.clip_to(cutoff)`; sonst Original clonen.
    - `slot_hours` (`:404-409`) unangetastet lassen — arithmetics greift automatisch die neuen Slot-Zeiten.
    - Booking-Date für das Gate: alle Slots einer Woche haben pro `day_of_week` ein eindeutiges Datum → `time::Date::from_iso_week_date(year as i32, week, slot.day_of_week.into())`.
  </behavior>
  <action>
Schritt 1 — Deps + Imports:
- `gen_service_impl!`-Block (`:108-122`) — neue Zeile für `ToggleService`:
  ```
  ToggleService: service::toggle::ToggleService<Context = Self::Context, Transaction = Self::Transaction> = toggle_service,
  ```
- `use service::toggle::ToggleService;` am Datei-Anfang.
- DI-Konstruktion `shifty_bin/src/main.rs` entsprechend erweitern.

Schritt 2 — `get_weekly_summary` (`:388-409`):
- Direkt vor `let slots: Arc<[Slot]> = ...` (Zeile 388) das Toggle-Prefetch einbauen:
  ```
  let toggle_value = match self.toggle_service.get_toggle_value(crate::shortday_gate::TOGGLE_NAME, context.clone(), None).await {
      Ok(v) => v, Err(ServiceError::Unauthorized) => None, Err(e) => return Err(e),
  };
  let active_from = crate::shortday_gate::parse_active_from(toggle_value.as_deref());
  ```
  (`context` ist im Scope; falls nicht, den Method-Parameter `context` durchreichen.)
- Die Kette `:388-403` ersetzen durch:
  ```
  let slots: Arc<[Slot]> = self
      .slot_service
      .get_slots_for_week_all_plans(year, week, Authentication::Full, tx.clone().into())
      .await?
      .iter()
      .filter_map(|slot| {
          // Holiday-Filter: kompletter Slot raus wenn Holiday auf diesem day_of_week.
          if special_days.iter().any(|day| day.day_of_week == slot.day_of_week && day.day_type == SpecialDayType::Holiday) {
              return None;
          }
          // ShortDay-Cutoff + Gate:
          let cutoff = special_days.iter().find_map(|day| {
              if day.day_of_week == slot.day_of_week && day.day_type == SpecialDayType::ShortDay { day.time_of_day } else { None }
          });
          let should = time::Date::from_iso_week_date(year as i32, week, slot.day_of_week.into())
              .ok()
              .map(|d| crate::shortday_gate::should_clip(d, active_from))
              .unwrap_or(false);
          match (cutoff, should) {
              (Some(c), true) => slot.clip_to(c),   // clip
              _ => Some(slot.clone()),               // ungated -> raw
          }
      })
      .collect();
  ```
- Der bestehende `special_days`-Fetch (wahrscheinlich weiter oben in `get_weekly_summary`) bleibt unverändert. Wenn er noch nicht existiert, ergänzen (Muster: siehe `get_summery_for_week` `:495-498`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl</automated>
  </verify>
  <done>
`get_weekly_summary` clippt; kein `slot.to > cutoff { continue }`-Filter mehr. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain C site 2 — `get_summery_for_week` clippt</name>
  <files>service_impl/src/booking_information.rs</files>
  <behavior>
    - Analog Task 1 für `get_summery_for_week` (`:506-525`).
    - `required_hours_by_day` (`:680-697`) foldet dann automatisch die pro-Tag-korrekten Werte (nutzt die geclippte `slots`-Variable aus dem Method-Scope) — kein separater Fix nötig.
  </behavior>
  <action>
- Vor `let slots: Arc<[Slot]> = ...` in `get_summery_for_week` (`:506`) das Toggle-Prefetch einbauen (dupliziert aus Task 1; Executor darf einen privaten Helper-Fn refactorn wenn's cleaner ist).
- Die Slots-Filter-Kette (`:506-521`) analog zu Task 1 auf `.filter_map(|slot| { … Holiday/ShortDay-Clip … })` umbauen.
- `required_hours_by_day` (`:680-697`) NICHT anfassen — konsumiert die neue `slots`-Variable und foldet korrekt.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl</automated>
  </verify>
  <done>
Zweite Site clippt. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — Chain C + Booking-Create-Verify</name>
  <files>service_impl/src/test/booking_information.rs</files>
  <behavior>
    - **Test A** `test_get_weekly_summary_clips_slot_hours_at_shortday`: Slot Mo 14:00–15:00, `min_resources=1`, ShortDay Mo Cutoff 14:30, Gate aktiv → `required_hours` in Weekly Summary enthält 0,5h für diesen Slot (statt 1h).
    - **Test B** `test_get_weekly_summary_ungated_no_clip`: gleicher Setup, aber `active_from = None` → `required_hours` enthält 1h (Legacy).
    - **Test C** `test_get_weekly_summary_drops_post_cutoff_slot`: Slot Mo 15:00–16:00, ShortDay Cutoff 14:30, Gate aktiv → Slot fehlt in `required_hours`.
    - **Test D** `test_get_summery_for_week_required_hours_by_day_respects_clip`: Slot Mo 14:00–15:00 + Slot Di 10:00–12:00, ShortDay Mo 14:30 (Cutoff), Gate aktiv → `monday_required_hours == 0.5`, `tuesday_required_hours == 2.0` (Di ungeclippt).
    - **Test E** `test_get_summery_for_week_stichtag_boundary`: `booking_date == active_from - 1` → ungeclippt; `booking_date == active_from` → geclippt. (SHC-06-Grenzfall.)
    - **Test F** `test_booking_create_on_post_cutoff_slot_not_rejected` (D-51-03): dieses Plan ändert `ShiftplanEditService` NICHT. Der Test hier ist eine Doku-Assertion, dass ein `POST /booking` für eine Booking auf einem Post-Cutoff-Slot durchgeht. Falls im Booking-Info-Test-File nicht der richtige Ort ist, den Test in `service_impl/src/test/booking.rs` oder `service_impl/src/test/shiftplan_edit.rs` platzieren — Executor entscheidet nach existierendem Test-Layout. Kern: Aufruf `booking_service.create(...)` mit Slot, dessen `from >= cutoff`, gibt `Ok(...)` zurück; kein neuer Error-Variant, kein 409.
  </behavior>
  <action>
- Neue Tests analog zu bestehenden `booking_information`-Tests. Mock-Deps ergänzen um `toggle_service: MockToggleService`; alle bestehenden Tests brauchen einen Default-Mock:
  `toggle_service.expect_get_toggle_value().returning(|_, _, _| Ok(None))`.
- Fixtures: `year=2026, week=31` (Mo=2026-07-27). Gate-aktiv: `active_from = Some(2026-07-01)`. Gate-aus: `Some(2099-01-01)` bzw. `None`.
- Test F: sofern der Test-File-Ort `test/booking_information.rs` nicht der richtige ist, in `test/booking.rs` oder `test/shiftplan_edit.rs` verschieben — hauptsache im `service_impl`-Test-Baum, sonst wird er nicht mitcompiliert.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib test::booking_information</automated>
  </verify>
  <done>
Sechs neue Tests grün. Test F belegt D-51-03 (keine Ablehnung von Post-Cutoff-Bookings).
  </done>
</task>

<task type="auto">
  <name>Task 4: Workspace gates</name>
  <files>(gate only)</files>
  <action>
Workspace-Test + Clippy. Sollten weitere Testfiles jetzt einen `toggle_service`-Mock
brauchen (weil `BookingInformationServiceImpl` eine neue Dep bekam), ergänze
`.returning(|_, _, _| Ok(None))` — Legacy-Off.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
Alle Workspace-Tests grün, Clippy grün. Snapshot-Version bleibt 12.
  </done>
</task>

</tasks>

<verification>
- Chain C beide Sites clippen; `required_hours_by_day` fällt automatisch mit.
- `slot.min_resources`-Multiplikation bleibt korrekt (min_resources unbeeinflusst vom Clip).
- Booking-Create bleibt view-layer-konform (D-51-03) — durch Test F belegt.
</verification>

<success_criteria>
1. `cargo test --workspace` grün.
2. `cargo clippy --workspace -- -D warnings` grün.
3. Kein neuer `ServiceError`-Variant, kein 409 für Post-Cutoff-Bookings.
4. Kein Filter-Anti-Pattern mehr in `booking_information.rs`.
5. Snapshot-Version bleibt 12.
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-05-SUMMARY.md` mit: Sites-Diff, neue Test-Namen, D-51-03-Verify-Test-Location.
</output>
