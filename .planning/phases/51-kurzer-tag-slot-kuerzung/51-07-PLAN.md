---
phase: 51
plan: 07
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - rest-types/src/lib.rs
  - shifty-dioxus/src/loader.rs
  - service_impl/src/test/pdf_shiftplan.rs
autonomous: true
requirements: [SHC-03, SHC-04]
must_haves:
  truths:
    - "D-51-09: `ShiftplanSlotTO` bekommt neues Feld `pub effective_to: time::Time` am Wrapper (nicht am `SlotTO`, weil `SlotTO` bidirektional ist — POST/PUT /slot in rest/src/slot.rs:100,124)"
    - "D-51-02: Fat Backend — FE bekommt fertigen `effective_to`-Wert im DTO; FE rechnet nichts (kein Clip-Aufruf im FE, kein Cutoff-Fetch im FE)"
    - "D-51-04: keine visuelle Zusatz-Markierung; FE-Loader liest `slot.slot.effective_to` statt `slot.slot.to` — dieselbe Rendering-Länge wie ein normaler kurzer Slot"
    - "SHC-03: WeekView rendert Slot 14:00–15:00 mit ShortDay-Cutoff 14:30 als 14:00–14:30-Zelle; Post-Cutoff-Slot fehlt (fällt bereits im BE weg)"
    - "SHC-04: PDF-Renderer konsumiert dasselbe `ShiftplanWeek`-Aggregat (Chain B) — verifiziert via Test in `test/pdf_shiftplan.rs`, KEINE Code-Änderung in `pdf_render.rs`"
    - "`SlotTO.to` bleibt **roh**. Beweisbar: SlotTO-Edit-Roundtrip (`GET /slot/{id}` → `PUT /slot/{id}`) berührt `effective_to` nie"
  artifacts:
    - "`rest-types/src/lib.rs:1069-1080` — `ShiftplanSlotTO` bekommt `pub effective_to: time::Time` + Doc-Kommentar analog `current_paid_count`"
    - "`rest-types/src/lib.rs:1111-1120` — `From<&service::shiftplan::ShiftplanSlot> for ShiftplanSlotTO` mappt `effective_to: slot.effective_to`"
    - "`shifty-dioxus/src/loader.rs:101` — `state::Slot.to` wird aus `slot.slot.effective_to` befüllt (statt `slot.slot.to`)"
    - "`shifty-dioxus/src/loader.rs:154` — analog für DayAggregate-Loader"
    - "`service_impl/src/test/pdf_shiftplan.rs` (oder `pdf_render.rs`) — neuer Test verifiziert: PDF-Render eines `ShiftplanWeek` mit gekürztem Slot rendert die geclippte Zeit (SHC-04)"
  key_links:
    - "Depends on P03: `ShiftplanSlot.effective_to` muss existieren, bevor der Mapper es lesen kann"
    - "P07 überlappt mit P03 auf `service_impl/src/shiftplan.rs`? NEIN — Struct-Def liegt in `service/src/shiftplan.rs` (P03), Mapper in `rest-types/src/lib.rs` (P07). Kein file_conflict."
    - "PDF-Renderer-Assertion belegt SHC-04 ohne Code-Change (Research §Q-01.3): `PdfShiftplanServiceImpl` (`service_impl/src/pdf_shiftplan.rs:149-152`) konsumiert das geclippte `ShiftplanWeek`-Aggregat aus Chain B"
---

<objective>
DTO + FE-Konsum + PDF-Verifikation. `ShiftplanSlotTO` bekommt `effective_to`, der
Mapper liest es aus dem service-Layer-Wrapper (P03 hat die Struct erweitert), der
FE-Loader nutzt `effective_to` statt `to`, und ein Test belegt, dass der PDF-Renderer
automatisch die geclippte Zeit rendert.

Purpose: SHC-03 (WeekView zeigt geclippt), SHC-04 (PDF konsistent), D-51-02 (Fat BE),
D-51-04 (keine Zusatzmarkierung), D-51-09 (Feld am Wrapper).

Output: DTO-Feld + Mapper + FE-Loader-Zeilen + PDF-Test. Kein `SlotTO`-Change (bleibt
bidirektional-safe).
</objective>

<execution_context>
Auto-commit via git (jj-import).
- Backend-Workspace: `/home/neosam/programming/rust/projects/shifty/shifty-backend`.
- Frontend-Workspace: `shifty-dioxus/` (separater Cargo-Workspace, own CLAUDE.md).
- WASM-Build-Gate: `cargo build --target wasm32-unknown-unknown` in `shifty-dioxus/`.
- Dioxus.toml `/shiftplan` Proxy existiert bereits (Zeile 86) — kein Proxy-Add nötig.
</execution_context>

<context>
- `rest-types/src/lib.rs:1069-1080` — `ShiftplanSlotTO` mit `current_paid_count` als Präzedenz.
- `rest-types/src/lib.rs:1111-1120` — `From<&service::shiftplan::ShiftplanSlot> for ShiftplanSlotTO` (Mapper).
- `rest-types/src/lib.rs:308-332` — `SlotTO` (bidirektional; NICHT anfassen).
- `service/src/shiftplan.rs:47-56` — `ShiftplanSlot` (nach P03: hat `effective_to`).
- `shifty-dioxus/src/loader.rs:97-105` — `map(|slot| Slot { ... to: slot.slot.to, ... })` im WeekView-Loader.
- `shifty-dioxus/src/loader.rs:150-158` — analog für DayAggregate-Loader.
- `shifty-dioxus/src/state/*` — Frontend-State-`Slot` (hat einfach `to: time::Time`). Anzeige-Logik in `week_view.rs:339-340, 417-418, 1042-1043` liest `slot.to_hour()` — nach Loader-Change ist das automatisch die effektive Zeit.
- `service_impl/src/pdf_render.rs:494-497` — `compute_slot_duration_hours(slot: &Slot)` liest `slot.from` und `slot.to`. **Achtung:** hier lebt die Duration im PDF! Nach P03 ist `slot` im PDF `ShiftplanSlot.slot` (roh) — d.h. der PDF-Renderer würde 60 min statt 30 min rechnen wenn nur `effective_to` am Wrapper hängt. **P07-Fix nötig:** `compute_slot_duration_hours(shiftplan_slot: &ShiftplanSlot)` (Wrapper statt Slot) oder Signatur beibehalten + Aufrufer passen `slot: Slot { to: effective_to, ..raw.clone() }` an. Der Executor entscheidet welcher Weg cleaner ist.
- `service_impl/src/pdf_render.rs:581-584` — `format!("{}:{}-{}:{}", slot.from.hour(), slot.from.minute(), slot.to.hour(), slot.to.minute())` — rendert die Slot-Beschriftung. Analog: nach P07 muss die Zelle mit `effective_to` beschriftet werden.
- CONTEXT.md D-51-09 (Wrapper-Feld), D-51-02 (Fat BE), D-51-04 (keine Zusatzmarkierung).
- Auto-Memory `feedback_dioxus_proxy_for_new_backend_endpoints` — `/shiftplan` Proxy existiert bereits (Zeile 86 der Dioxus.toml). Kein Add nötig.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `ShiftplanSlotTO` bekommt `effective_to`</name>
  <files>rest-types/src/lib.rs</files>
  <behavior>
    - Neues Feld `pub effective_to: time::Time` am `ShiftplanSlotTO` (Zeile 1069-1080), mit Doc-Kommentar (Muster `current_paid_count`).
    - `From<&service::shiftplan::ShiftplanSlot> for ShiftplanSlotTO` (Zeile 1111-1120) setzt `effective_to: slot.effective_to`.
    - `SlotTO` (Zeile 308-332) unangetastet.
    - Serialisiert als ISO-8601-Time (identisch zum `SlotTO.to`-Muster via `#[schema(value_type = String, format = "time")]`).
  </behavior>
  <action>
Schritt 1 — `ShiftplanSlotTO`-Struct (Zeile 1069-1080) erweitern:
```
#[derive(Debug, Serialize, Deserialize, ToSchema)]
pub struct ShiftplanSlotTO {
    pub slot: SlotTO,
    pub bookings: Vec<ShiftplanBookingTO>,
    /// Phase 5 (D-09): Wire-Mirror von …  (bestehend, unverändert)
    pub current_paid_count: u8,
    /// Phase 51 (D-51-09): View-Layer effektive End-Zeit für den Slot.
    /// Gleicht `slot.to` — außer der Tag ist ein ShortDay UND der
    /// D-51-07-Stichtag ist aktiv; dann `min(slot.to, cutoff)`.
    /// `slot.to` bleibt roh (bidirektional-DTO-Regel, siehe P07).
    /// FE rendert Slots immer mit `effective_to` als End-Zeit.
    #[schema(value_type = String, format = "time")]
    pub effective_to: time::Time,
}
```

Schritt 2 — Mapper (Zeile 1111-1120) erweitern:
```
#[cfg(feature = "service-impl")]
impl From<&service::shiftplan::ShiftplanSlot> for ShiftplanSlotTO {
    fn from(slot: &service::shiftplan::ShiftplanSlot) -> Self {
        Self {
            slot: (&slot.slot).into(),
            bookings: slot.bookings.iter().map(Into::into).collect(),
            current_paid_count: slot.current_paid_count,
            effective_to: slot.effective_to,
        }
    }
}
```

Schritt 3 — OpenAPI/utoipa: `ShiftplanSlotTO` ist bereits in `ApiDoc` (Backend-`utoipa`-Bootstrap in `rest/src/main.rs` bzw. `rest/src/lib.rs`). Kein `ApiDoc`-Change nötig; das Feld erscheint automatisch im Schema durch `ToSchema`-Derive.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p rest-types --all-features</automated>
  </verify>
  <done>
`ShiftplanSlotTO` hat `effective_to`; Mapper setzt es. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF-Renderer konsumiert `effective_to`</name>
  <files>service_impl/src/pdf_render.rs</files>
  <behavior>
    - `compute_slot_duration_hours` (`service_impl/src/pdf_render.rs:494-497`) muss die **effektive** Dauer rechnen, nicht die rohe. Zwei Wege:
      - Signatur-Change auf `fn compute_slot_duration_hours(slot: &ShiftplanSlot) -> f32` → nutzt `slot.effective_to - slot.slot.from`.
      - Oder: Aufrufer bauen einen "effektiven Slot"-Klon (`Slot { to: shiftplan_slot.effective_to, ..raw.clone() }`) und übergeben den. Weniger clean.
    - Zelle-Beschriftung (`service_impl/src/pdf_render.rs:581-584`, `format!("{}:{}-{}:{}", from_hour, from_min, to_hour, to_min)`): analog `effective_to.hour()/minute()` statt `slot.to.hour()/minute()`.
  </behavior>
  <action>
- Aufrufer-Audit: `grep -n "compute_slot_duration_hours\|slot\.to\.hour\(\)\|slot\.to\.minute\(\)" service_impl/src/pdf_render.rs` — alle Sites listen.
- Signatur-Change: `pub fn compute_slot_duration_hours(shiftplan_slot: &service::shiftplan::ShiftplanSlot) -> f32`. Berechnung: `(shiftplan_slot.effective_to - shiftplan_slot.slot.from).as_seconds_f32() / 3600.0`.
- Zelle-Beschriftung an `:581-584`: die entsprechenden `slot.to.hour()` / `slot.to.minute()`-Aufrufe durch `effective_to.hour()` / `effective_to.minute()` ersetzen. Konkret: der Aufrufer hat i. d. R. Zugriff auf `shiftplan_slot`, dessen `effective_to`-Feld ausgelesen wird.
- **Aufrufer-Ripple:** alle Sites, die bisher `compute_slot_duration_hours(&slot.slot)` aufrufen (oder ähnlich), an neue Signatur `&slot`-Wrapper anpassen. Compiler-Fehler-Trail folgen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl</automated>
  </verify>
  <done>
PDF-Renderer nutzt `effective_to` für Dauer und Beschriftung. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PDF-Renderer-Test — SHC-04</name>
  <files>service_impl/src/pdf_render.rs, service_impl/src/test/pdf_shiftplan.rs</files>
  <behavior>
    - **Test A** `test_pdf_renders_clipped_slot_duration`: Fixture — `ShiftplanWeek` mit einem Slot 14:00–15:00, `effective_to = 14:30`. `render_shiftplan_week_pdf(...)` liefert Bytes; wir prüfen, dass die berechnete Slot-Dauer 0,5h ist (via `compute_slot_duration_hours(&slot)` direkt aufrufen — pure fn).
    - **Test B** `test_pdf_renders_full_slot_duration_when_effective_to_equals_to`: `effective_to = slot.to` → Dauer 1h. Beweis dass Default-Path unverändert bleibt.
    - **Test C** (optional, nur wenn low-hanging): visueller Assert per Byte-Range im PDF, dass die Slot-Beschriftung "14:00-14:30" statt "14:00-15:00" enthält. **Nur wenn** der pdf-render-Test-Baum bereits Muster hat, den PDF-Bytestream nach Substrings zu prüfen. Sonst weglassen.
  </behavior>
  <action>
- Neuen Test im bestehenden `#[cfg(test)] mod tests`-Block von `service_impl/src/pdf_render.rs` (dort leben die anderen Renderer-Tests bereits ab Zeile 689+).
- Fixture-Slot mit `slot.to = 15:00, effective_to = 14:30`. Andere Slot-Felder aus der bestehenden Test-Fixture übernehmen.
- Test A direkt: `assert!((compute_slot_duration_hours(&shiftplan_slot) - 0.5).abs() < 1e-4)`.
- Test B: gleicher Fixture, `effective_to = 15:00` → `1.0`.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib pdf_render::tests</automated>
  </verify>
  <done>
Zwei Tests grün. SHC-04 belegt via automatischer Renderer-Assertion.
  </done>
</task>

<task type="auto">
  <name>Task 4: FE-Loader nutzt `effective_to`</name>
  <files>shifty-dioxus/src/loader.rs</files>
  <action>
- `shifty-dioxus/src/loader.rs:97-105` — im Slot-Map: `to: slot.slot.to,` (Zeile 101) durch `to: slot.effective_to,` ersetzen.
- `shifty-dioxus/src/loader.rs:150-158` — analog Zeile 154: `to: slot.slot.to,` → `to: slot.effective_to,`.
- **Wichtig:** die Variable ist `slot` (typ `ShiftplanSlotTO`), also `slot.effective_to` direkt am Wrapper — nicht `slot.slot.effective_to`.
- Kein Signal-/State-Change im FE nötig; `state::Slot` hat weiter `to: time::Time`.
- Analog `state::Slot.from = slot.slot.from` bleibt unverändert (`from` wird nicht geclippt).

Kein `Dioxus.toml`-Proxy-Change — `/shiftplan` ist bereits allowlisted (`shifty-dioxus/Dioxus.toml:86`). Kein FE-Component-Change (WeekView liest `state::Slot.to` weiter → automatisch geclippt).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <done>
Zwei Zeilen im `loader.rs` geändert; WASM-Build grün.
  </done>
</task>

<task type="auto">
  <name>Task 5: FE-Gates + Workspace-Gates</name>
  <files>(gate only)</files>
  <action>
Backend:
- `cargo test --workspace` (nach P03-Struct-Erweiterung + P07-DTO+PDF darf nichts brechen).
- `cargo clippy --workspace -- -D warnings`.

Frontend:
- `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` — WASM-Build grün.
- `cd shifty-dioxus && cargo clippy -- -D warnings` (siehe Auto-Memory `reference_dioxus_clippy_not_gated` — CI ignoriert das, aber wir wollen zumindest keine neuen Warnings einführen; wenn dennoch pre-existing Lints in shifty-dioxus vorkommen, den Executor entscheiden lassen, ob er `-A dead_code`-Suffix an eigenen Files hängt oder das Fix nur die eigenen Änderungen betrifft — Ziel: unsere neuen Zeilen sind clippy-clean, pre-existing bleibt).
- `cd shifty-dioxus && cargo test` — FE-Tests grün.

**Verifikation D-51-04 (keine visuelle Zusatzmarkierung):** grep in `shifty-dioxus/src/component/week_view.rs` nach neuen CSS-Klassen oder Icons — es sollten keine sein. Die Kürzung zeigt sich ausschließlich durch die kürzere Zelle (geclippte `to`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings &amp;&amp; cd shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown &amp;&amp; cargo test</automated>
  </verify>
  <done>
Alle Backend- + FE-Gates grün. WASM-Build grün. Snapshot-Version bleibt 12.
  </done>
</task>

</tasks>

<verification>
- DTO-Feld am Wrapper (D-51-09).
- FE-Loader konsumiert Wrapper-Feld — zwei Zeilen-Änderung (Zeile 101 + 154 in loader.rs).
- PDF-Renderer nutzt `effective_to` durch Signatur-Change an `compute_slot_duration_hours` (Wrapper statt Slot).
- `SlotTO.to` bleibt roh — SlotTO-Edit-Roundtrip (`GET/PUT /slot/{id}`) berührt `effective_to` nie.
- Keine visuelle Zusatz-Markierung (D-51-04) — kein WeekView-Component-Change.
</verification>

<success_criteria>
1. `cargo build -p rest-types --all-features` grün.
2. `cargo test --workspace` + `cargo clippy --workspace -- -D warnings` grün.
3. `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` grün.
4. `cd shifty-dioxus && cargo test` grün.
5. PDF-Renderer-Test A liefert 0,5h für einen `effective_to=14:30`-Slot.
6. `SlotTO`-Struct (`rest-types/src/lib.rs:308-332`) unangetastet — beweisbar via `git diff -- rest-types/src/lib.rs | grep -A2 'pub struct SlotTO'`.
7. Snapshot-Version bleibt 12.
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-07-SUMMARY.md` mit: DTO-Diff, Mapper-Diff, FE-Loader-Diff (2 Zeilen), PDF-Renderer-Signatur-Change, Test-Namen.
</output>