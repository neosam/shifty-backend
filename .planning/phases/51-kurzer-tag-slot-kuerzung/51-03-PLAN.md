---
phase: 51
plan: 03
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - service/src/shiftplan.rs
  - service_impl/src/shiftplan.rs
  - service_impl/src/test/shiftplan.rs
autonomous: true
requirements: [SHC-02, SHC-05, SHC-06]
must_haves:
  truths:
    - "D-51-06 Chain B: `build_shiftplan_day` setzt `ShiftplanSlot.effective_to` via `Slot::clip_to(cutoff)` (statt Slot zu verwerfen — pre-existing Bug in shiftplan.rs:62-66 gefixt)"
    - "D-51-09: `service::shiftplan::ShiftplanSlot` bekommt neues Feld `effective_to: time::Time`. `ShiftplanSlot.slot` bleibt roh — D-51-09 verlangt, dass persistenz-treue `Slot`-Werte nicht mutiert werden (weil `SlotTO` bidirektional ist, siehe P07)"
    - "D-51-07: Vor Clip prüft `build_shiftplan_day` das Stichtag-Gate via `shortday_gate::should_clip(booking_date, active_from)`; false → `effective_to = slot.to`"
    - "D-04 Zeile 4 erfüllt: Slot 14:00–15:00 mit Cutoff 14:30 landet mit `slot.to=15:00, effective_to=14:30` (Bug-Fix: früher fiel Slot komplett raus)"
    - "D-04 Zeile 3 erfüllt: Slot 15:00–16:00 mit Cutoff 14:30 wird aus `ShiftplanDay.slots` weggelassen (nicht im Vec)"
    - "SHC-05: Existierende Bookings auf gekürzten Slots werden unverändert in `ShiftplanSlot.bookings` transportiert (kein Rewrite)"
  artifacts:
    - "`service/src/shiftplan.rs` — `ShiftplanSlot` bekommt neues Feld `pub effective_to: time::Time` (D-51-09, Wrapper trägt Anzeige-Wert, `slot` bleibt roh)"
    - "`service_impl/src/shiftplan.rs` — Filter-Anti-Pattern (:62-66) durch `Slot::clip_to` + Gate ersetzt; `slot: slot.clone(), effective_to: clipped_or_original_to`"
    - "`service_impl/src/test/shiftplan.rs:251` — Test `test_get_shiftplan_week_with_special_days` auf D-04-Erwartung (Clipping via `effective_to`) angepasst"
    - "`service_impl/src/test/shiftplan.rs` — neuer Test `test_build_shiftplan_day_sets_effective_to_at_cutoff` (vor Stichtag `effective_to == slot.to`, am Stichtag `effective_to == cutoff`)"
  key_links:
    - "Chain B ist das gemeinsame View-Aggregat für WeekView + PDF (via `PdfShiftplanServiceImpl` in `pdf_shiftplan.rs:149-152`) — dieser Fix läuft implizit für PDF mit (SHC-04)"
    - "Konsumiert `Slot::clip_to` aus P01 (`service::slot::Slot`) und `shortday_gate::should_clip` aus P02"
---

<objective>
Chain B (Read-Aggregat WeekView + PDF) — bestehendes ShortDay-Filter-Anti-Pattern in
`build_shiftplan_day` (Datei `service_impl/src/shiftplan.rs:62-66`) durch die
kanonische Clip-Semantik ersetzen. Zusätzlich Stichtag-Gate aus D-51-07 vor den Clip
schalten. Pre-existing Bug wird dabei mit gefixt (Slots wurden bisher komplett
verworfen statt gekürzt zu werden — verletzte D-04).

Purpose: SHC-02 (WeekView zeigt geclippt), SHC-04 (PDF automatisch mit-korrekt),
SHC-05 (existierende Bookings überleben), SHC-06 (Stichtag-Gate).

Output: `build_shiftplan_day` konsumiert `Slot::clip_to` + `shortday_gate`. Test-Suite
aktualisiert: der bestehende `test_get_shiftplan_week_with_special_days` erwartet
jetzt Clipping statt Omission; ein neuer Test verifiziert das Stichtag-Gate.
</objective>

<execution_context>
Auto-commit via git (jj-import).
`build_shiftplan_day` ist `pub(crate)` und wird intern auch von `build_shiftplan_day_for_sales_person` (`:147-196`) via `build_shiftplan_day(...)`-Aufruf (`:160`) benutzt. Fix an der einen Stelle propagiert automatisch für Per-Sales-Person-Sicht.
</execution_context>

<context>
- `service_impl/src/shiftplan.rs:27-131` — `build_shiftplan_day` (der zu fixende Helper).
  - `:41-51` — ShortDay-Cutoff-Lookup (korrekt geschrieben, unangefasst lassen).
  - `:62-66` — Filter-Anti-Pattern (`if slot.to > cutoff { continue; }`). **Ersetzen**.
  - `:113-117` — `day_slots.push(ShiftplanSlot { slot: slot.clone(), ... })`. Hier den geclippten Slot pushen.
- `service_impl/src/shiftplan.rs:218-305, 307-399, 401-524, 526+` — vier Konsumenten der Helper-Fn. Übergeben `year, week, context`; wir brauchen diese Signaturen NICHT anpassen — der Helper selbst bekommt `year, week` als neue Args oder alternativ ein `active_from: Option<time::Date>` als vorgekautes Argument. **Empfehlung: `active_from` als Arg** — der Toggle-Lookup passiert einmal pro Endpoint (nicht sieben Mal pro Wochentag), und der Helper bleibt frei von `async`+`ToggleService`-Deps.
- `service_impl/src/reporting.rs:164-180` — Konsum-Muster für den Toggle (verbatim in die vier `get_shiftplan_*`-Methoden übernehmen).
- `service_impl/src/test/shiftplan.rs:250-312` — `test_get_shiftplan_week_with_special_days` (der Test, der die Bug-Semantik testet und angepasst werden muss).
- CONTEXT.md D-51-06 (Chain B), D-51-07 (Gate), D-04 (Cutoff-Regeln).
- Auto-Memory `feedback_decision_coverage_gate_truths` — jede D-51-Decision muss in `must_haves.truths` als Tag stehen, damit der Coverage-Gate greift.
- **Service-Tier-Konvention** (CLAUDE.md § Service-Tier-Konventionen): `ShiftplanViewServiceImpl` ist Business-Logic-Tier; darf `ToggleService` als Basic-Service konsumieren.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `build_shiftplan_day` — Filter zu Clip + Gate</name>
  <files>service_impl/src/shiftplan.rs</files>
  <behavior>
    - `service::shiftplan::ShiftplanSlot` bekommt ein neues Feld `pub effective_to: time::Time` (D-51-09). Default = `slot.to` wenn kein Cutoff greift.
    - Neue Signatur `build_shiftplan_day`: `year: u32, week: u8, active_from: Option<time::Date>` als letzte Args.
    - Innerhalb der Fn: nach dem existierenden `short_day_cutoff`-Lookup (`:42-51`) prüfen, ob das Gate für dieses Datum greift:
      - `let day_date = time::Date::from_iso_week_date(year as i32, week, day_of_week.into()).ok();`
      - `let effective_cutoff = day_date.and_then(|d| if shortday_gate::should_clip(d, active_from) { short_day_cutoff } else { None });`
    - Im Slot-Loop (`:56-118`): pro Slot per `Slot::clip_to(effective_cutoff)` prüfen:
      - Wenn `effective_cutoff = None` → Slot bleibt roh; `effective_to = slot.to`.
      - Wenn `effective_cutoff = Some(cutoff)` → `match slot.clip_to(cutoff)`:
        - `None` (Slot komplett hinter Cutoff, D-04 Zeile 3) → `continue` (Slot aus Vec weglassen).
        - `Some(clipped)` → `effective_to = clipped.to`.
    - `day_slots.push(ShiftplanSlot { slot: slot.clone(), bookings: slot_bookings, current_paid_count, effective_to })` in Zeile ~113:
      - `slot: slot.clone()` — **roh**, unangefasst. D-51-09 verlangt persistenz-treue Werte (weil `SlotTO` bidirektional ist, siehe P07).
      - `effective_to: <clipped.to oder slot.to>` — Wert für den View-Layer.
    - Semantik-Erhaltung: `is_holiday`-Check (`:37-39` + `:55`) unangetastet.
    - `build_shiftplan_day_for_sales_person` (`:147-196`) reicht die drei neuen Args (`year, week, active_from`) durch.
  </behavior>
  <action>
Schritt 0 — Struct-Erweiterung in `service/src/shiftplan.rs`:
- `pub struct ShiftplanSlot` (Zeile 47-56) bekommt ein neues Feld `pub effective_to: time::Time`. Doc-Kommentar direkt oberhalb:
  ```
  /// Phase 51 (D-51-09): view-layer effective end time. Equals `slot.to`
  /// unless the day is a ShortDay AND the D-51-07 stichtag gate is active,
  /// in which case it equals `min(slot.to, cutoff)`. `slot.to` bleibt roh
  /// (bidirektional-DTO-Regel, siehe P07).
  pub effective_to: time::Time,
  ```
- `#[derive(Debug, Clone)]` bleibt.

Schritt 1 — Signaturen in `service_impl/src/shiftplan.rs` erweitern:
- `pub(crate) fn build_shiftplan_day` (Zeile 27-35): neue Parameter `year: u32, week: u8, active_from: Option<time::Date>` an das Ende der Argumentliste anhängen.
- `pub(crate) fn build_shiftplan_day_for_sales_person` (Zeile 147-158): dieselben drei neuen Parameter anhängen und im inneren `build_shiftplan_day(...)`-Aufruf (Zeile 160-168) durchreichen.

Schritt 2 — `build_shiftplan_day`-Body:
- Nach dem existierenden `short_day_cutoff`-Block (Zeile 51) einfügen:
  ```
  let day_date = time::Date::from_iso_week_date(year as i32, week, day_of_week.into()).ok();
  let effective_cutoff = short_day_cutoff.filter(|_| {
      day_date.map(|d| crate::shortday_gate::should_clip(d, active_from)).unwrap_or(false)
  });
  ```
- Filter-Anti-Pattern in Zeilen 61-66 durch Clip ersetzen:
  ```
  let effective_to = if let Some(cutoff) = effective_cutoff {
      match slot.clip_to(cutoff) {
          None => continue,                    // Slot komplett hinter Cutoff (D-04 Zeile 3)
          Some(clipped) => clipped.to,         // Slot gekürzt (D-04 Zeile 4)
      }
  } else {
      slot.to                                  // Legacy / kein Gate → roh
  };
  ```
- Push-Zeile 113-117: `slot` bleibt `slot.clone()` (roh), plus neues Feld:
  ```
  day_slots.push(ShiftplanSlot {
      slot: slot.clone(),
      bookings: slot_bookings,
      current_paid_count,
      effective_to,
  });
  ```

Schritt 2b — Struct-Init-Ripple (Compile-Fehler-Guide):
Nach Struct-Erweiterung schlagen alle Test-Fixtures fehl, die `ShiftplanSlot { ... }` bauen — mindestens in `service_impl/src/pdf_render.rs` (Zeilen 873, 990, 995, 1037, 1081, 1139, 1229, 1234, 1239 laut Grep). Der Executor ergänzt in jedem dieser Init-Blocks `effective_to: slot.to,` (oder den passenden konkreten `time::Time`-Wert). Semantik: alle PDF-Test-Fixtures nutzen ungeclippte Zeiten → `effective_to == slot.to`. Kein weiterer Semantik-Change nötig; nur Feld ergänzen.

Schritt 3 — Vier Konsumenten (`get_shiftplan_week`, `get_shiftplan_day`, `get_shiftplan_week_for_sales_person`, `get_shiftplan_day_for_sales_person`) anpassen:
- Nach dem `special_days`-Fetch (jeweils direkt nach `special_day_service.get_by_week(...)`) den Toggle-Lookup einbauen — Muster verbatim aus `service_impl/src/reporting.rs:164-180`:
  ```
  let toggle_value = match self.toggle_service
      .get_toggle_value(crate::shortday_gate::TOGGLE_NAME, context.clone(), None).await
  {
      Ok(v) => v,
      Err(service::ServiceError::Unauthorized) => None,
      Err(e) => return Err(e),
  };
  let active_from = crate::shortday_gate::parse_active_from(toggle_value.as_deref());
  ```
- Alle Aufrufe von `build_shiftplan_day(...)` (`:287-296` in `get_shiftplan_week`; `:375-383` in `get_shiftplan_day`) bzw. `build_shiftplan_day_for_sales_person(...)` (`:502-514`, `:622-634`) um `year, week, active_from` erweitern.

Schritt 4 — Deps-Update:
- `ShiftplanViewServiceDeps` (`service_impl/src/shiftplan.rs:198-211`) benötigt `ToggleService` als neue Dep. Zeile im `gen_service_impl!`-Block einfügen (analog `reporting.rs:80`):
  ```
  ToggleService: service::toggle::ToggleService<Context = Self::Context, Transaction = Self::Transaction> = toggle_service,
  ```
- `use service::toggle::ToggleService;` am Datei-Anfang ergänzen.
- **DI-Konstruktion** in `shifty_bin/src/main.rs`: prüfen, ob `ShiftplanViewServiceImpl` bereits mit `toggle_service` konstruiert wird. Wenn nicht: den Parameter am Konstruktions-Call ergänzen. Beispiel-Suche: `ShiftplanViewServiceImpl::new(` — der Slot für `toggle_service` liegt in derselben `deps`-Struct-Position wie bei `reporting_service`. Wenn Server bereits einen `toggle_service` konstruiert hat (v1.7-HCFG-02), einfach als weiteren Parameter durchreichen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl -p shifty_bin</automated>
  </verify>
  <done>
`build_shiftplan_day` clippt statt zu verwerfen; alle vier Konsumenten reichen `active_from` durch. `cargo build` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update existing test + add gate boundary test</name>
  <files>service_impl/src/test/shiftplan.rs</files>
  <behavior>
    - **Bestehender Test** `test_get_shiftplan_week_with_special_days` (`:250-312`): assertions umstellen. Erwartete Semantik:
      - `slot.slot.to` bleibt roh (Test-Fixture-Wert unangefasst, `slot.slot.to == 15:00` für einen 12:00–15:00-Slot).
      - `slot.effective_to == 14:00` (Cutoff) für Tuesday-Slots die überlappen.
      - Slot komplett hinter Cutoff (`slot.from >= 14:00`) fehlt weiter im Vec (D-04 Zeile 3).
    - **Neuer Test** `test_build_shiftplan_day_effective_to_unclipped_before_stichtag`: `active_from = Some(2026-08-01)`, Test-Woche `2026-07-30` → `effective_to == slot.to`.
    - **Neuer Test** `test_build_shiftplan_day_effective_to_clipped_at_stichtag`: `active_from = Some(2026-08-01)`, Test-Woche mit Datum == Stichtag → `effective_to == cutoff`.
    - **Neuer Test** `test_build_shiftplan_day_none_active_from_no_clip`: `active_from = None` → `effective_to == slot.to` selbst wenn ShortDay existiert (Legacy).
    - **Neuer Test** `test_build_shiftplan_day_slot_field_stays_raw_when_clipped`: Bei aktivem Gate + Cutoff → `slot.slot.to == 15:00` (roh), `slot.effective_to == 14:00`. Beweist D-51-09.
    - **Neuer Test** `test_build_shiftplan_day_preserves_bookings_on_clipped_slot`: SHC-05 — Booking auf teil-geclipptem Slot landet unverändert in `ShiftplanSlot.bookings`.
  </behavior>
  <action>
Schritt 1 — Bestehenden Test anpassen (`:295-312`):
- Der Test setzt ShortDay Tuesday mit `time_of_day: Some(Time::from_hms(14, 0, 0).unwrap())`. Die alten Assertions `assert!(tuesday.slots.iter().all(|slot| slot.slot.to <= Time::from_hms(14, 0, 0).unwrap()))` bleiben semantisch okay, aber die **Bug-Semantik** (Slot 14:30–15:00 fehlt, weil `to > 14:00`) ist jetzt Clipping, nicht Omission. Wenn der Test-Fixture Slots hatte, die *vollständig hinter* dem Cutoff lagen, dann fallen die weiterhin raus (D-04 Zeile 3, korrekt). Wenn Slots *überlappten* (z. B. 13:00–15:00), müssen sie jetzt geclippt (13:00–14:00) im Ergebnis stehen. **Concrete change:** Fixture-Slots für Tuesday explizit anlegen (min. einer, der überlappt), Assertion `tuesday.slots.len() > 0` und `tuesday.slots[k].slot.to == Time::from_hms(14, 0, 0)` explizit prüfen.

Um Stichtag-Gate zu erlauben (Test soll clippen), den Aufruf `service.get_shiftplan_week(...)` mit gesetztem `active_from` fahren. Das bedeutet: `toggle_service` mocken. Der Test befindet sich in `service_impl/src/test/shiftplan.rs`; die `build_dependencies()`-Fixture (in dieser Datei, oberhalb der Tests) muss `MockToggleService` produzieren, der auf `.expect_get_toggle_value()` einen `Some("2024-01-01".to_string())` zurückgibt (Stichtag = weit vor 2024-Test-Fixture, damit Gate greift). **Alle bestehenden Tests, die `build_dependencies()` konsumieren, brauchen dann ebenfalls einen Mock-Toggle-Return** — für diese Tests reicht `.returning(|_, _, _| Ok(None))` (Legacy off, keine Kürzung erwartet).

Schritt 2 — Vier neue Tests anhängen (nach dem angepassten Test):
- Test-Struktur analog zu Bestehenden: `build_dependencies()` konfigurieren, `special_day_service.expect_get_by_week()` mit ShortDay-Fixture, `slot_service.expect_get_slots_for_week()` mit Test-Slots, `toggle_service.expect_get_toggle_value()` mit dem für den Test passenden Rückgabewert.
- **Achtung Datum-Fixture:** Die Test-Fixtures nutzen `2024, week 3` — das ergibt ISO-Datum Tuesday = 2024-01-16. Für Stichtag-Grenzfall-Tests entweder: (a) Fixtures auf ein Datum ändern, wo Vortag/Stichtag klar mappt, oder (b) `active_from` so setzen, dass es genau auf `2024-01-16` fällt bzw. einen Tag vorher. Empfehlung: alle vier neuen Tests mit `year=2026, week=31` fahren (Tuesday = 2026-07-28), Stichtag `2026-07-28` (inklusiv) bzw. `2026-07-29` (Vortag-Effekt) — sauber und lesbar.

Schritt 3 — Alle bestehenden Tests in `service_impl/src/test/shiftplan.rs`, die `get_shiftplan_week`/`get_shiftplan_day`/`_for_sales_person` aufrufen, brauchen einen `toggle_service.expect_get_toggle_value().returning(|_, _, _| Ok(None))`-Mock (Legacy-Default). Systematisch durchgehen und ergänzen.

Schritt 4 — `build_shiftplan_day`-Direktaufrufe in `service_impl/src/test/shiftplan.rs:314+` (der `--- Unit tests for build_shiftplan_day ---`-Block, ab Zeile 314): Signatur des Helpers hat sich um `year, week, active_from` erweitert → alle Direkt-Aufrufe brauchen die drei zusätzlichen Args. Für Tests, die nicht das Gate testen: `(2024, 3, None)` reicht (aktive Kürzung aus).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib test::shiftplan</automated>
  </verify>
  <done>
Der angepasste Test `test_get_shiftplan_week_with_special_days` grün (jetzt clippt statt zu verwerfen). Vier neue Tests grün. Kein bestehender Test in `test/shiftplan.rs` bricht.
  </done>
</task>

<task type="auto">
  <name>Task 3: Workspace gates</name>
  <files>(gate only)</files>
  <action>
Nach den beiden vorherigen Tasks:
- `cargo test --workspace` (Nicht nur `test/shiftplan.rs`, weil andere Konsumenten von `build_shiftplan_day` auch getroffen sein könnten — z. B. Test-Fixtures in `booking_information`-Tests, die WeekView aufrufen).
- `cargo clippy --workspace -- -D warnings`.
- **Kein `sqlx prepare` nötig** — keine neuen SQL-Queries.

Sollten andere Test-Files jetzt einen `toggle_service`-Mock brauchen (Kompilier-Fehler durch neue Dep im `ShiftplanViewServiceImpl`), ergänze sie mit `.returning(|_, _, _| Ok(None))` — Legacy-Off entspricht dem alten Verhalten aller nicht-P51-Tests.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
Alle Workspace-Tests grün, Clippy grün. Kein Snapshot-Bump. Keine neue Cargo-Dep.
  </done>
</task>

</tasks>

<verification>
- Chain B fixt Bug + implementiert Feature in einem Schritt (D-51-06 Research §Risks 2).
- Vier Endpoints (`GET /shiftplan/{id}/{year}/{week}`, `GET /shiftplan/day/{year}/{week}/{dow}`, plus die zwei sales-person-Varianten) liefern ab jetzt geclippte Slot-Fenster im Backend-Aggregat.
- PDF-Renderer (`service_impl/src/pdf_render.rs` via `PdfShiftplanServiceImpl::render`) konsumiert dasselbe Aggregat → SHC-04 fällt automatisch. **Wird in P07 per Assertion verifiziert**, nicht hier.
</verification>

<success_criteria>
1. `cargo test --workspace` grün mit dem angepassten `test_get_shiftplan_week_with_special_days` + vier neuen Gate/Bug-Fix-Tests.
2. `cargo clippy --workspace -- -D warnings` grün.
3. Snapshot-Version bleibt 12.
4. Bug-Fix-Diff sichtbar: keine `if slot.to > cutoff { continue; }`-Zeile mehr in `service_impl/src/shiftplan.rs`.
5. `service_impl/src/shiftplan.rs` hat neu eine `use crate::shortday_gate;` und zieht `ToggleService` via `gen_service_impl!`-Deps ein.
6. `service::shiftplan::ShiftplanSlot` hat neues Feld `pub effective_to: time::Time`. `ShiftplanSlot.slot` bleibt roh (D-51-09).
7. Alle Test-Fixtures die `ShiftplanSlot { ... }` initialisieren (in `service_impl/src/pdf_render.rs` u. a.) haben das Feld ergänzt.
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-03-SUMMARY.md` mit: Diff-Übersicht (welche `build_shiftplan_day`-Zeilen ersetzt), neue Test-Namen mit Zeilennummern, DI-Konstruktions-Änderung in `shifty_bin/src/main.rs` (falls nötig).
</output>
