---
phase: 51
plan: 06
type: execute
wave: 2
depends_on: [51-01, 51-02]
files_modified:
  - dao/src/shiftplan_report.rs
  - dao_impl_sqlite/src/shiftplan_report.rs
  - service_impl/src/shiftplan_report.rs
  - service_impl/src/test/shiftplan_report.rs
autonomous: true
requirements: [SHC-02, SHC-05, SHC-06]
must_haves:
  truths:
    - "D-51-08: Chain D wird Rust-Layer-clipping-refaktoriert. DAO liefert Roh-Zeilen pro Booking+Slot (statt SQL-SUM); `ShiftplanReportServiceImpl` aggregiert per `Slot::clip_to` + `shortday_gate` in Rust"
    - "D-51-06 Chain D: Balance/Ist-Stunden (`extract_shiftplan_report`, `extract_shiftplan_report_for_week`, `extract_quick_shiftplan_report`) respektieren Clipping"
    - "D-51-07: Stichtag-Gate pro Booking-Datum (year, week, day_of_week → concrete date) verhindert historisches Umbenennen von Ist-Stunden vor `active_from`"
    - "SHC-05: kein Booking-Rewrite; DAO liefert `booking_id` + `slot_id` + Zeiten unverändert, Rust-Layer clippt nur die Zeit-Berechnung"
    - "D-03: Snapshot-Immunität — persistierte `billing_period_sales_person`-Rows unangetastet; `CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt 12"
    - "Perf-Note (D-51-08): Row-Traffic steigt pro Booking, aber SQLite lokal — Balance-Historien mehrere Jahre bleiben verschmerzbar"
  artifacts:
    - "`dao/src/shiftplan_report.rs` — neuer Entity-Typ `ShiftplanReportRawRow` (booking_id, sales_person_id, day_of_week, year, calendar_week, time_from, time_to) für ungrouped raw-row queries"
    - "`dao/src/shiftplan_report.rs` — Trait-Methoden `extract_raw_shiftplan_report`, `extract_raw_shiftplan_report_for_week`, `extract_raw_quick_shiftplan_report` (Row-per-Booking, keine SQL-SUM)"
    - "`dao_impl_sqlite/src/shiftplan_report.rs` — Impl der drei neuen Trait-Methoden mit SQL, das je Booking eine Row liefert (kein SUM/GROUP BY)"
    - "`dao_impl_sqlite/src/shiftplan_report.rs` — alte drei SUM-Queries werden deprecated aber bleiben stehen (falls andere Konsumenten sie brauchen — Executor prüft), oder werden gelöscht wenn Service der einzige Konsument ist"
    - "`service_impl/src/shiftplan_report.rs` — die drei `extract_*`-Service-Methoden konsumieren neue DAO-Methoden, aggregieren + clippen + gaten in Rust"
    - "`service_impl/src/test/shiftplan_report.rs` — neue Tests (mit MockDao) für Clip + Gate + Bookings-Aggregation"
    - "`.sqlx/`-Dir aktualisiert (neue query!-Makros)"
  key_links:
    - "Chain D speist Balance-Views + Reporting (`service_impl/src/reporting.rs:281, 712, 901`). Nach Refactor sehen alle Balance-Konsumenten Ist-Stunden mit Clip + Gate"
    - "Snapshot-Reader (`billing_period_report.rs`) bleibt unangetastet — konsumiert persistierte Rows, nicht Live-Aggregat"
---

<objective>
Chain D (Balance / Ist-Stunden — `ShiftplanReportDao`) — Rust-Layer-Refactor (D-51-08).
Der DAO liefert ab jetzt pro Booking eine Row mit `booking_year, booking_calendar_week,
slot.day_of_week, slot.time_from, slot.time_to, sales_person_id` (kein SQL-`SUM`, kein
`GROUP BY`). Der Service-Layer (`ShiftplanReportServiceImpl`) aggregiert per
`Slot::clip_to` + `shortday_gate::should_clip` und returned das bestehende
`ShiftplanReportDay`/`ShiftplanQuickOverview`-Interface unverändert.

Purpose: SHC-02 (Balance-Konten sehen geclippte Ist-Stunden), SHC-05 (kein Rewrite),
SHC-06 (Stichtag-Gate), D-51-08 (kanonische Clip-Fn statt SQL-JOIN + MIN).

Output: Neue Trait-Methoden, neue SQL-Queries (raw rows), Service-Layer-Aggregation.
Alte Interface-Methoden (`extract_shiftplan_report`, `extract_quick_shiftplan_report`,
`extract_shiftplan_report_for_week`) bleiben in Trait+Service, aber intern via raw + Clip.
</objective>

<execution_context>
Auto-commit via git (jj-import).
**SQLx-prepare Pflicht** nach neuer `query!`/`query_as!`: `cargo sqlx prepare --workspace` + `.sqlx/` committen (siehe Auto-Memory `reference_sqlx_prepare_after_new_query`).
</execution_context>

<context>
- `dao/src/shiftplan_report.rs:9-16` — bestehende `ShiftplanReportEntity` (SUM-Result). Bleibt bestehen für Trait-Kompat.
- `dao/src/shiftplan_report.rs:24-28` — `ShiftplanQuickOverviewEntity`.
- `dao/src/shiftplan_report.rs:30-61` — Trait-Def.
- `dao_impl_sqlite/src/shiftplan_report.rs:62-101, 103-134, 136-167` — drei SQL-Queries mit `SUM(STRFTIME(time_to) - STRFTIME(time_from))` und `GROUP BY sales_person_id, year, calendar_week, day_of_week`. **Diese sollen ergänzt (nicht zwingend ersetzt) werden durch drei raw-row-Varianten.**
- `service_impl/src/shiftplan_report.rs:25-125` — drei Service-Methoden, die den DAO 1:1 durchreichen. Werden umgebaut.
- `service_impl/src/reporting.rs:281, 712, 901` — Konsumenten der Service-Methoden. Signatur bleibt, keine Aufrufer-Änderung.
- Research §Q-01.4 + §"Chain D": beschreibt Option B (Rust-Layer) — was wir hier bauen.
- Research §Q-01.4: `booking.year * 100 + calendar_week` als Range-Guard bleibt; Filterung nach Datum am Service-Layer greift zusätzlich (bereits vorhanden in `service_impl/src/shiftplan_report.rs:45-53`).

**Achtung SQL-Bug in bestehender Query (`:114` und `:147`):**
```
STRFTIME('%H', slot.time_to) + STRFTIME('%M', slot.time_to) / 60.0
                                                                  ^ Fehlender `- (STRFTIME('%H', slot.time_from) + STRFTIME('%M', slot.time_from) / 60.0)` in Zeile 114/147?
```
Nein — Zeile 114 rechnet:
`sum((STRFTIME('%H', slot.time_to) + STRFTIME('%M', slot.time_to) / 60.0) - (STRFTIME('%H', slot.time_from) + STRFTIME('%M', slot.time_from)))` — **fehlt `/60.0` beim Minute-Teil von `time_from`** (verifizieren!). Falls das ein Bug ist, in P06 mit fixen (siehe Zeile 114 und 147). Wenn die alten SQL-Queries entfernt werden (Rust-Layer übernimmt), verschwindet der Bug automatisch.
</context>

<tasks>

<task type="auto">
  <name>Task 1: DAO Trait — raw-row methods</name>
  <files>dao/src/shiftplan_report.rs</files>
  <action>
Neuen Entity-Typ hinzufügen:
```
#[derive(Clone, Debug, PartialEq)]
pub struct ShiftplanReportRawRow {
    pub sales_person_id: Uuid,
    pub booking_id: Uuid,
    pub year: u32,
    pub calendar_week: u8,
    pub day_of_week: DayOfWeek,
    pub time_from: time::Time,
    pub time_to: time::Time,
}
```

Drei neue Trait-Methoden im `pub trait ShiftplanReportDao` (nach den bestehenden drei ergänzen):
```
async fn extract_raw_shiftplan_report(
    &self,
    sales_person_id: Uuid,
    from_year: u32,
    from_week: u8,
    to_year: u32,
    to_week: u8,
    tx: Self::Transaction,
) -> Result<Arc<[ShiftplanReportRawRow]>, DaoError>;

async fn extract_raw_quick_shiftplan_report(
    &self,
    year: u32,
    until_week: u8,
    tx: Self::Transaction,
) -> Result<Arc<[ShiftplanReportRawRow]>, DaoError>;

async fn extract_raw_shiftplan_report_for_week(
    &self,
    year: u32,
    calendar_week: u8,
    tx: Self::Transaction,
) -> Result<Arc<[ShiftplanReportRawRow]>, DaoError>;
```

Der `#[automock]` an `pub trait ShiftplanReportDao` erzeugt automatisch `MockShiftplanReportDao` mit den neuen Methoden (kein manuelles `#[automock]`-Argument nötig, da der Trait bereits `#[automock(type Transaction = crate::MockTransaction;)]` hat).

**Die drei alten Trait-Methoden bleiben** — wir entscheiden nach Konsumenten-Audit (Task 5), ob sie später entfernt werden. Fürs erste co-existieren beide Signaturen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p dao</automated>
  </verify>
  <done>
Trait hat drei neue raw-Methoden. `MockShiftplanReportDao` implementiert sie. `cargo build -p dao` grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: DAO Impl — raw-row SQL</name>
  <files>dao_impl_sqlite/src/shiftplan_report.rs</files>
  <action>
Neuen DB-Struct-Typ:
```
pub struct ShiftplanReportRawRowDb {
    pub sales_person_id: Vec<u8>,
    pub booking_id: Vec<u8>,
    pub year: i64,
    pub calendar_week: i64,
    pub day_of_week: i64,
    pub time_from: time::Time,
    pub time_to: time::Time,
}
impl TryFrom<&ShiftplanReportRawRowDb> for ShiftplanReportRawRow {
    type Error = DaoError;
    fn try_from(entity: &ShiftplanReportRawRowDb) -> Result<Self, DaoError> {
        Ok(Self {
            sales_person_id: Uuid::from_slice(entity.sales_person_id.as_ref())?,
            booking_id: Uuid::from_slice(entity.booking_id.as_ref())?,
            year: entity.year as u32,
            calendar_week: entity.calendar_week as u8,
            day_of_week: DayOfWeek::from_number(entity.day_of_week as u8)
                .ok_or_else(|| DaoError::InvalidDayOfWeek(entity.day_of_week as u8))?,
            time_from: entity.time_from,
            time_to: entity.time_to,
        })
    }
}
```

Drei neue Impl-Methoden mit `query_as!`:
- `extract_raw_shiftplan_report` — Range-Query, WHERE `sales_person.id = ?` + Range + non-planning:
  ```
  SELECT sales_person.id as sales_person_id, booking.id as booking_id, booking.year, booking.calendar_week, slot.day_of_week, slot.time_from, slot.time_to
  FROM slot
  INNER JOIN booking ON (booking.slot_id = slot.id AND booking.deleted IS NULL)
  INNER JOIN sales_person ON booking.sales_person_id = sales_person.id
  LEFT JOIN shiftplan ON slot.shiftplan_id = shiftplan.id
  WHERE sales_person.id = ?
    AND booking.year * 100 + booking.calendar_week >= ? * 100 + ?
    AND booking.year * 100 + booking.calendar_week <= ? * 100 + ?
    AND (shiftplan.is_planning = 0 OR shiftplan.is_planning IS NULL)
  ```
  Kein `SUM`, kein `GROUP BY`. Rust aggregiert.

- `extract_raw_quick_shiftplan_report` — analog ohne day_of_week-Grouping (aber wir liefern day_of_week trotzdem, damit der Service-Layer clippen kann):
  ```
  SELECT sales_person.id as sales_person_id, booking.id as booking_id, booking.year, booking.calendar_week, slot.day_of_week, slot.time_from, slot.time_to
  FROM slot
  INNER JOIN booking ON (booking.slot_id = slot.id AND booking.deleted IS NULL)
  INNER JOIN sales_person ON booking.sales_person_id = sales_person.id
  LEFT JOIN shiftplan ON slot.shiftplan_id = shiftplan.id
  WHERE booking.year = ?
    AND booking.calendar_week <= ?
    AND (shiftplan.is_planning = 0 OR shiftplan.is_planning IS NULL)
  ```

- `extract_raw_shiftplan_report_for_week` — pro-Wochen-Version:
  ```
  SELECT sales_person.id as sales_person_id, booking.id as booking_id, booking.year, booking.calendar_week, slot.day_of_week, slot.time_from, slot.time_to
  FROM slot
  INNER JOIN booking ON (booking.slot_id = slot.id AND booking.deleted IS NULL)
  INNER JOIN sales_person ON booking.sales_person_id = sales_person.id
  LEFT JOIN shiftplan ON slot.shiftplan_id = shiftplan.id
  WHERE booking.year = ?
    AND booking.calendar_week = ?
    AND (shiftplan.is_planning = 0 OR shiftplan.is_planning IS NULL)
  ```

Beachte: `slot.time_from`/`slot.time_to` sind in SQLite als TEXT gespeichert. Der `time::Time`-Cast in sqlx läuft via `TIME`-Format (`HH:MM:SS`). Wenn Test-Fehler wegen Cast auftreten, den Cast-Hint mit `time_from: time::Time as "time_from: time::Time"` in der `query_as!`-Zeile ergänzen (Muster aus anderen SQL-Queries im Repo suchen — z. B. slot-DAO).

Nach neuer `query_as!` PFLICHT: `cargo sqlx prepare --workspace` + `.sqlx/` committen.

**Die drei alten Trait-Impls (SUM-Queries) bleiben stehen** — sie werden von den Service-Methoden nicht mehr aufgerufen (siehe Task 3), aber die Trait-Signatur verlangt sie. Task 5 klärt, ob wir sie loswerden können.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; nix-shell -p sqlx-cli --run "cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo sqlx prepare --workspace" &amp;&amp; cargo build -p dao_impl_sqlite</automated>
  </verify>
  <done>
Drei raw-Query-Impls kompilieren; `.sqlx/`-Dir enthält neue prepared queries; `cargo build -p dao_impl_sqlite` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service — Aggregation + Clip + Gate</name>
  <files>service_impl/src/shiftplan_report.rs</files>
  <behavior>
    - `extract_shiftplan_report_for_week(year, week, ctx, tx)` ruft `extract_raw_shiftplan_report_for_week`, iteriert Rohzeilen, konstruiert für jede einen ephemeren `Slot`-Wert (nur `from`+`to`+`day_of_week` gefüllt, rest default), fetched `special_days` + Toggle einmalig, wendet Gate + Clip an, summiert pro `(sales_person_id, day_of_week)` in Stunden, und emittiert das existierende `ShiftplanReportDay`-Interface.
    - `extract_shiftplan_report(sales_person_id, from_date, to_date, ctx, tx)` analog für Range; `special_days`+Toggle einmal pro Woche im Range vorziehen (Muster: `reporting.rs:186-198`).
    - `extract_quick_shiftplan_report(year, until_week, ctx, tx)` analog für Jahr-Bereich.
    - Gate: pro Row → `booking_date = time::Date::from_iso_week_date(row.year, row.calendar_week, row.day_of_week.into())`; wenn `should_clip(booking_date, active_from)` und Cutoff für `row.day_of_week` in dem Wochen-`special_days` existiert → `Slot::clip_to(cutoff)`; sonst raw.
    - Wenn Cutoff greift und `clip_to` = `None` → Row trägt 0h bei (Slot komplett hinter Cutoff, D-04 Zeile 3). Alternativ: die Row komplett weglassen (semantisch identisch — 0h × min_resources = 0h).
    - `min_resources` ist in Chain D KEIN Faktor — der Report zählt Stunden pro SalesPerson pro Booking. Also: `hours = (clipped.to - clipped.from).as_seconds_f32() / 3600.0`. Kein `* min_resources`.
  </behavior>
  <action>
- `ShiftplanReportServiceDeps` (Zeile 13-18) um `SpecialDayService` und `ToggleService` erweitern:
  ```
  SpecialDayService: service::special_days::SpecialDayService<Context = Self::Context> = special_day_service,
  ToggleService: service::toggle::ToggleService<Context = Self::Context, Transaction = Self::Transaction> = toggle_service,
  ```
- Imports ergänzen: `use service::special_days::{SpecialDayService, SpecialDayType};`, `use service::toggle::ToggleService;`, `use service::slot::Slot;`, `use shifty_utils::DayOfWeek;`.
- **`extract_shiftplan_report_for_week`** (Zeile 98-125) neu schreiben:
  1. Toggle+special_days-Prefetch.
  2. `raw_rows = self.shiftplan_report_dao.extract_raw_shiftplan_report_for_week(year, calendar_week, tx.clone()).await?`.
  3. `HashMap<(Uuid /*sales_person_id*/, DayOfWeek), f32 /*hours*/>` bauen.
  4. Pro row: ephemer-Slot (`Slot { from: row.time_from, to: row.time_to, day_of_week: row.day_of_week, ...defaults() }`, siehe unten), Gate+Cutoff-Lookup pro `row.day_of_week`, Clip anwenden, `hours = (clipped.to - clipped.from).as_seconds_f32() / 3600.0`, aufsummieren.
  5. Aus dem Map die `Vec<ShiftplanReportDay>` bauen.
- Helper-Fn `fn slot_from_row(row: &ShiftplanReportRawRow) -> Slot` als private Fn im Modul, die einen ephemeren Slot zusammenbaut. `id`, `min_resources`, etc. mit Dummies füllen — die werden im Clip-Kontext nicht gelesen (nur `from`, `to`, `day_of_week`).
- **`extract_shiftplan_report`** (Zeile 25-69) neu schreiben:
  1. Toggle-Lookup einmal (Range kann Jahres-übergreifend sein).
  2. Für jede Woche im Range separat `special_day_service.get_by_week(week.year, week.week, ctx)` (Muster `reporting.rs:186-198`). Sammel in `HashMap<ShiftyWeek, Arc<[SpecialDay]>>` — Cache innerhalb des Calls.
  3. `raw_rows = extract_raw_shiftplan_report(...).await?`.
  4. Existierende Datum-Filterung (Zeile 45-52) bleibt (`to_date().map(|d| d >= from_date && d <= to_date)`).
  5. Aggregation per `(sales_person_id, year, calendar_week, day_of_week)` in `HashMap`.
  6. Rest wie in `extract_shiftplan_report_for_week`.
- **`extract_quick_shiftplan_report`** (Zeile 71-96) neu schreiben:
  1. Analog. Jahres-Rollup: Aggregation-Key ist `(sales_person_id, year)` — `hours` sind Jahressumme.
  2. Achtung: Da wir pro-Woche `special_days` fetchen müssen (bis `until_week`), loop `for week in 1..=until_week`.

Keine Signatur-Änderung der drei `ShiftplanReportService`-Trait-Methoden. Alle bestehenden Konsumenten (`service_impl/src/reporting.rs`, `service_impl/src/booking_information.rs`) bleiben unangefasst.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p service_impl</automated>
  </verify>
  <done>
Drei Service-Methoden konsumieren raw-DAO + Clip + Gate. `cargo build -p service_impl` grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Tests — Chain D Aggregation + Clip + Gate + Snapshot-Immunity</name>
  <files>service_impl/src/test/shiftplan_report.rs</files>
  <behavior>
    - **Test A** `test_extract_for_week_clips_at_shortday`: raw-Row Mo 14:00–15:00, ShortDay Mo Cutoff 14:30, Gate aktiv → `hours == 0.5`.
    - **Test B** `test_extract_for_week_ungated_no_clip`: gleiche Row, `active_from = None` → `hours == 1.0`.
    - **Test C** `test_extract_for_week_post_cutoff_row_zero`: raw-Row Mo 15:00–16:00, Cutoff 14:30, Gate aktiv → Slot fällt raus, `hours == 0.0` (Row entweder weggelassen oder 0h — Test akzeptiert beide).
    - **Test D** `test_extract_for_week_stichtag_boundary`: booking_date == active_from - 1 → ungeclippt (1.0h); booking_date == active_from → geclippt (0.5h).
    - **Test E** `test_extract_for_week_aggregates_multiple_bookings`: zwei Bookings desselben SalesPerson an Mo 14:00–15:00 (getrennte Booking-Rows aus DAO) → summierte hours = 2×0.5 = 1.0h wenn geclippt. Beweist "kein Rewrite" (SHC-05) — DB liefert beide Bookings weiter.
    - **Test F** `test_extract_range_multi_week`: Range über 2 Wochen, jeweils eine ShortDay-Woche mit Gate aktiv + eine ohne (Gate aus per Wochen-Datum vor `active_from`) → summierter Ergebnis-Vektor enthält beide Wochen, aber nur die Gate-Woche ist geclippt.
    - **Test G** `test_snapshot_schema_version_unchanged`: `assert_eq!(CURRENT_SNAPSHOT_SCHEMA_VERSION, 12)`. Doku-Test, dass Chain-D-Refactor keinen Bump auslöst.
  </behavior>
  <action>
- Prüfe, ob `service_impl/src/test/shiftplan_report.rs` bereits existiert. Wenn ja, in der Datei anhängen; wenn nein, neu anlegen und in `service_impl/src/test/mod.rs` registrieren (`pub mod shiftplan_report;`).
- MockDao: `MockShiftplanReportDao::new()` mit `.expect_extract_raw_shiftplan_report_for_week()`, etc. Rückgaben von Hand konstruiert.
- Alle Tests: `MockSpecialDayService` + `MockToggleService` konfigurieren (analog zu P03/P04/P05).
- Fixture: `year = 2026, week = 31` (Mo = 2026-07-27). Gate-aktiv: `active_from = 2026-07-01` (weit vor Testwoche); Gate-Grenzfall: `active_from = 2026-07-27` (== Mo) und `active_from = 2026-07-28` (Mo davor = Vortag-Effekt).
- Test G importiert `use crate::billing_period_report::CURRENT_SNAPSHOT_SCHEMA_VERSION;` (Präzedenz: `test/billing_period_report.rs:1045`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl --lib test::shiftplan_report</automated>
  </verify>
  <done>
Sieben Tests grün. Aggregation korrekt über Booking-Rows.
  </done>
</task>

<task type="auto">
  <name>Task 5: Konsumenten-Audit + optional cleanup</name>
  <files>dao/src/shiftplan_report.rs, dao_impl_sqlite/src/shiftplan_report.rs</files>
  <action>
Grep-Audit: `grep -rn "\.extract_shiftplan_report\|\.extract_quick_shiftplan_report\|\.extract_shiftplan_report_for_week" /home/neosam/programming/rust/projects/shifty/shifty-backend/ --include="*.rs"` — welche Konsumenten die **SUM-Trait-Methoden** direkt vom DAO aufrufen.

Erwartete Konsumenten: nur `service_impl/src/shiftplan_report.rs` (Service-Layer). Der ruft die SUM-Methoden NICHT mehr — die neuen `extract_raw_*` reichen.

**Entscheidung Executor:**
- Wenn der Grep keine anderen Konsumenten der SUM-Methoden findet außer der eigenen Service-Impl → alte SUM-Methoden aus Trait + DaoImpl LÖSCHEN. Sauberer, ein Konsumenten-Pfad. Achtung: nach dem Löschen `cargo sqlx prepare --workspace` erneut fahren, um die alten `.sqlx/`-Files zu entfernen.
- Wenn der Grep externe Konsumenten findet (unwahrscheinlich, aber Sicherheits-Check) → SUM-Methoden LASSEN + `#[deprecated(note = "Phase 51 Chain D Rust-Layer-Refactor — use extract_raw_* + clip in service layer")]` an sie annotieren (aber der `#[deprecated]` triggert Clippy-Warnings wenn Konsumenten weiter aufrufen — dann besser lassen ohne Attribut).

Der Executor prüft und wählt.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
Grep-Audit dokumentiert. Alte SUM-Methoden entweder gelöscht oder mit Kommentar-Begründung erhalten. Workspace grün.
  </done>
</task>

<task type="auto">
  <name>Task 6: Workspace gates</name>
  <files>(gate only)</files>
  <action>
- `cargo sqlx prepare --workspace` (Pflicht nach Task 2 und ggf. Task 5 sowieso).
- Prüfe, dass `.sqlx/`-Dateien committed werden.
- `cargo test --workspace` + `cargo clippy --workspace -- -D warnings`.
- Verifiziere Snapshot-Konstant: `grep -q "pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12;" service_impl/src/billing_period_report.rs`.
- Verifiziere, dass persistierte `billing_period`-Snapshot-Rows unangetastet bleiben — der Snapshot-Reader in `billing_period_report.rs` konsumiert die persistierte Row-Menge, nicht die Live-Ist-Stunden-DAO. **Kein Test-Change dort erforderlich.**
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; nix-shell -p sqlx-cli --run "cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo sqlx prepare --workspace" &amp;&amp; cargo test --workspace &amp;&amp; cargo clippy --workspace -- -D warnings &amp;&amp; grep -q 'pub const CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 12;' service_impl/src/billing_period_report.rs</automated>
  </verify>
  <done>
Alle Gates grün. `.sqlx/` up-to-date. Snapshot-Version bleibt 12.
  </done>
</task>

</tasks>

<verification>
- Chain D refactored: DAO liefert raw rows, Service aggregiert. Kein SQL-JOIN mit special_day, kein `MIN()` in SQL.
- Existierende drei Service-Methoden-Signaturen unverändert — Aufrufer (`reporting.rs`, `booking_information.rs`) unangefasst.
- Snapshot-Reader (`billing_period_report.rs`) unangefasst — Snapshot-Immunität doppelt abgesichert (D-51-07 Gate + persistente Rows nie neu berechnet).
- Perf-Note: Row-Traffic pro Booking, aber SQLite lokal. Balance-Historien über Jahre bleiben nach Research §Q-01.4 verschmerzbar; ggf. spätere Materialisierung out-of-scope.
</verification>

<success_criteria>
1. `cargo sqlx prepare --workspace` läuft ohne Fehler; `.sqlx/`-Dir hat drei neue `query-*.json`.
2. `cargo test --workspace` grün — insbesondere `test::shiftplan_report::*` und `test::reporting_*` (die Chain D indirekt konsumieren).
3. `cargo clippy --workspace -- -D warnings` grün.
4. `CURRENT_SNAPSHOT_SCHEMA_VERSION = 12` unverändert.
5. `service_impl/src/reporting.rs` und `service_impl/src/booking_information.rs` unangetastet (Aufrufer-Kompat).
</success_criteria>

<output>
Erstelle `.planning/phases/51-kurzer-tag-slot-kuerzung/51-06-SUMMARY.md` mit: DAO-Trait-Delta, neue SQL-Queries (Kurz-Zitat), Service-Aggregations-Skizze, Aufrufer-Audit-Ergebnis, `.sqlx/`-Dateien.
</output>
