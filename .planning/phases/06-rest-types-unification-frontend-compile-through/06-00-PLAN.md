---
phase: 6
plan: 0
slug: backend-rest-types-prep
wave: 0
depends_on: []
files_modified:
  - shifty-backend/rest-types/src/lib.rs
  - shifty-backend/rest-types/Cargo.toml
  - shifty-backend/rest/src/user_invitation.rs
requirements: [RT-03]
must_haves: [M-01]
autonomous: true

must_haves:
  truths:
    - "Backend-`rest-types/src/lib.rs` exportiert `InvitationStatus`, `InvitationResponse`, `GenerateInvitationRequest` (Pre-Req für RT-03)."
    - "`ShiftplanTO` derive-Set enthält `PartialEq, Eq` (Frontend-Fork-Parity für Dioxus-Reaktivität)."
    - "Backend-Workspace `cargo check --workspace` und `cargo test --workspace --no-run` bleiben grün (Baseline 461 Tests)."
  artifacts:
    - path: "shifty-backend/rest-types/src/lib.rs"
      provides: "InvitationStatus enum + InvitationResponse struct + GenerateInvitationRequest struct, ShiftplanTO mit PartialEq/Eq derives"
      contains: "pub enum InvitationStatus"
    - path: "shifty-backend/rest/src/user_invitation.rs"
      provides: "Re-Export oder Service-Layer-Konversion gegen rest_types::InvitationStatus"
      contains: "rest_types::InvitationStatus"
  key_links:
    - from: "shifty-backend/rest/src/user_invitation.rs"
      to: "shifty-backend/rest-types/src/lib.rs"
      via: "use rest_types::{InvitationStatus, ...} statt lokaler Definitionen"
      pattern: "use rest_types::\\{[^}]*Invitation"
---

<objective>
Backend-seitige Vorbereitung für die rest-types-Konsolidierung: alle Types,
die der Frontend-Fork hat, das Backend-`rest-types` aber nicht, müssen vor
dem Cargo-Swap (Wave 1) im Backend-`rest-types` landen — sonst bricht der
Frontend-Build sofort mit `unresolved import`-Fehlern.

Purpose: Pre-Req für RT-03. Ohne diese Migration bricht Wave 1 unmittelbar.
Output: Backend-`rest-types` exportiert die Invitation-Familie + hat parity-derives auf `ShiftplanTO`.

**Task-Split (Revision iter 1):** Aufgespalten in zwei sequentielle auto-Tasks. Task 0 = derive-Parity + Hygiene (Änderungen A + B). Task 1 = Invitation-Familie-Migration + From-Impl-Umzug + Cargo-Toml-Sanity (Änderungen C + D + E). Beide bleiben autonomous; Task 1 startet erst, wenn Task 0 grün ist (gleiches `cargo check`-Gate).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-RESEARCH.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-UI-SPEC.md
@.planning/phases/06-rest-types-unification-frontend-compile-through/06-VALIDATION.md
@CLAUDE.md
@shifty-backend/CLAUDE.md
@shifty-backend/CLAUDE.local.md

<interfaces>
<!-- Vorhandene Backend-Definition (Quelle für Migration) -->
<!-- shifty-backend/rest/src/user_invitation.rs:23-71 -->

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, ToSchema)]
#[serde(rename_all = "lowercase")]
pub enum InvitationStatus {
    Valid,
    Expired,
    Redeemed,
    #[serde(rename = "sessionrevoked")]
    SessionRevoked,
}

#[derive(Debug, Serialize, Deserialize, ToSchema)]
pub struct GenerateInvitationRequest {
    pub username: String,
    pub expiration_hours: Option<i64>,
}

#[derive(Debug, Serialize, Deserialize, ToSchema)]
pub struct InvitationResponse {
    pub id: Uuid,
    pub username: String,
    pub token: Uuid,
    pub invitation_link: String,
    #[serde(with = "time::serde::rfc3339::option")]
    pub redeemed_at: Option<OffsetDateTime>,
    pub status: InvitationStatus,
}
```

<!-- Frontend-Fork-Variante (Vergleich): shifty-dioxus/rest-types/src/lib.rs:1387-1411 -->
<!-- Verwendet `#[serde(with = "optional_timestamp")]` und ein lokales optional_timestamp-Modul. -->
<!-- Frontend hat zusätzlich `Clone, ToSchema, PartialEq` auf InvitationResponse + InvitationStatus. -->

<!-- Backend `rest-types/Cargo.toml` time-Features (aktuell): -->
<!-- features = ["serde-human-readable"] -->
<!-- Für `#[serde(with = "time::serde::rfc3339::option")]` braucht es zusätzlich "formatting" + "parsing". -->

<!-- ShiftplanTO aktueller Backend-Stand (rest-types/src/lib.rs:13): -->
<!-- #[derive(Serialize, Deserialize, Clone, Debug, ToSchema)] -->
<!-- Soll: + PartialEq, Eq -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: ShiftplanTO PartialEq/Eq + shifty_utils-Import feature-gaten</name>
  <files>shifty-backend/rest-types/src/lib.rs</files>
  <read_first>
    - shifty-backend/rest-types/src/lib.rs (Zeilen 1-50 für Imports, Zeile 13 für ShiftplanTO derive-Block; gesamt ~2041 Zeilen)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md (Blocker 1 + 2, Zeilen 36-75)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-RESEARCH.md (§4 PartialEq-Diskrepanz; §6 Landmine 1 shifty_utils)
  </read_first>
  <action>
Zwei mechanische Änderungen in `shifty-backend/rest-types/src/lib.rs`. Beide isoliert testbar — nach dem Edit muss `cargo check --workspace` grün bleiben (Backend-Baseline darf nicht regredieren).

**Änderung A — `ShiftplanTO` Derives ergänzen (Zeile 13).**

VORHER:
```rust
#[derive(Serialize, Deserialize, Clone, Debug, ToSchema)]
pub struct ShiftplanTO {
```

NACHHER (Reihenfolge wie im Frontend-Fork: shifty-dioxus/rest-types/src/lib.rs:14):
```rust
#[derive(Serialize, Deserialize, Clone, Debug, PartialEq, Eq, ToSchema)]
pub struct ShiftplanTO {
```

**Änderung B — `shifty_utils`-Import feature-gaten (Zeile 8).**

VORHER:
```rust
use shifty_utils::{derive_from_reference, LazyLoad};
```

NACHHER:
```rust
#[cfg(feature = "service-impl")]
use shifty_utils::{derive_from_reference, LazyLoad};
```

Begründung: `derive_from_reference!` (Zeilen 1181, 1202) und `LazyLoad::new` (Zeile 745) werden ausschließlich in `#[cfg(feature = "service-impl")]`-gated Blöcken aufgerufen — verifiziert per Grep. Der Import ist im `default = []`-Build also tot. `shifty-utils` ist im Cargo.toml bereits als `optional = true` deklariert und wird NUR via `service-impl = ["dep:service", "dep:shifty-utils"]` aktiviert. Ohne Gate würde der Compiler den Import als Fehler werten, wenn `shifty-utils`-Feature aus ist — verifizieren!

**Konsistenz-Check (manuell, im Task-Body):**
1. `grep -nB1 '^use shifty_utils' shifty-backend/rest-types/src/lib.rs` zeigt `#[cfg(feature = "service-impl")]` direkt davor.
2. `grep -B1 '^pub struct ShiftplanTO' shifty-backend/rest-types/src/lib.rs` zeigt das neue Derive-Set mit `PartialEq, Eq`.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo check --workspace 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo check --workspace` Exit-Code 0
    - `grep -B1 '^pub struct ShiftplanTO' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | grep -c 'PartialEq.*Eq\|Eq.*PartialEq'` ist `1`
    - `grep -n '^use shifty_utils' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | wc -l` ist `0` (unconditional Import entfernt)
    - `grep -nB1 '^use shifty_utils' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | grep -c 'cfg(feature = "service-impl")'` ist `1` (gated Import vorhanden)
  </acceptance_criteria>
  <done>
    ShiftplanTO hat PartialEq+Eq; shifty_utils-Import ist feature-gated. Backend-Workspace kompiliert grün.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Invitation-Familie nach rest-types migrieren + From-Impl umziehen</name>
  <files>shifty-backend/rest-types/src/lib.rs, shifty-backend/rest-types/Cargo.toml, shifty-backend/rest/src/user_invitation.rs</files>
  <read_first>
    - shifty-backend/rest-types/src/lib.rs (vollständig falls noch nicht aus Task 0 im Kontext — Migrationsziel)
    - shifty-backend/rest-types/Cargo.toml (time-Features prüfen)
    - shifty-backend/rest/src/user_invitation.rs (Quelle der drei Typen, Zeilen 1-75)
    - .planning/phases/06-rest-types-unification-frontend-compile-through/06-PATTERNS.md (Blocker 3, Zeilen 79-126)
  </read_first>
  <action>
Drei mechanische Änderungen, sequentiell aufeinander aufbauend.

**Änderung C — Invitation-Familie hinzufügen.**

Am Ende von `rest-types/src/lib.rs` (nach der letzten existierenden Definition, vor `[lints]` falls vorhanden — sonst einfach ans Ende) folgenden Block einfügen:

```rust
// =====================
// User Invitation API (migrated from rest/src/user_invitation.rs in v1.2 Phase 6)
// =====================

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, ToSchema)]
#[serde(rename_all = "lowercase")]
pub enum InvitationStatus {
    /// Invitation is valid and can be used
    Valid,
    /// Invitation has expired and cannot be used
    Expired,
    /// Invitation has already been redeemed
    Redeemed,
    /// Invitation session has been revoked
    #[serde(rename = "sessionrevoked")]
    SessionRevoked,
}

#[derive(Debug, Clone, Serialize, Deserialize, ToSchema)]
pub struct GenerateInvitationRequest {
    /// Username of the user to invite
    pub username: String,
    /// Expiration time in hours (default: 168 hours = 7 days)
    pub expiration_hours: Option<i64>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, ToSchema)]
pub struct InvitationResponse {
    /// Unique ID of the invitation
    pub id: Uuid,
    /// Username of the invited user
    pub username: String,
    /// Invitation token (UUID)
    pub token: Uuid,
    /// Complete invitation link URL
    pub invitation_link: String,
    /// When the invitation was redeemed (null if not yet redeemed; RFC3339 string for WASM-compat)
    pub redeemed_at: Option<String>,
    /// Current status of the invitation
    pub status: InvitationStatus,
}
```

WICHTIGE DESIGN-ENTSCHEIDUNG (D-Phase6-01): `redeemed_at` ist hier `Option<String>` (RFC3339-Format) statt `Option<time::OffsetDateTime>` mit `#[serde(with = "time::serde::rfc3339::option")]`. Begründung: Backend-`rest-types/Cargo.toml` hat aktuell nur `time = { features = ["serde-human-readable"] }` — `rfc3339`-Serde-Hilfe braucht zusätzlich `formatting`+`parsing`+`std`. Das `Option<String>` ist wire-kompatibel mit dem Backend (Backend serialisiert RFC3339-String, Frontend deserialisiert ihn als String) und vermeidet eine Cargo-Feature-Erweiterung mit potenziellem WASM-Impact.

Der Frontend-Fork serialisiert `redeemed_at` über ein lokales `optional_timestamp`-Modul, das letztlich ebenfalls RFC3339-Strings produziert — wire-kompatibel.

`Clone` ist NEU gegenüber rest/src/user_invitation.rs — Frontend nutzt die Types in `Rc<[InvitationResponse]>` (siehe `shifty-dioxus/src/service/user_management.rs:53`), `Clone` ist erforderlich.

`PartialEq` auf `InvitationResponse` (nicht `Eq` wegen `Option<String>`) ist Frontend-Fork-Parity.

**Änderung D — `rest/src/user_invitation.rs` umstellen.**

Die lokale `InvitationStatus`-Definition (Zeilen 22-35), `GenerateInvitationRequest` (48-54), `InvitationResponse` (56-71) durch Re-Export ersetzen:

VORHER (Zeilen 22-71): drei Struct/Enum-Definitionen.

NACHHER:
```rust
// Re-exported from rest_types for the unified DTO surface (v1.2 Phase 6)
pub use rest_types::{GenerateInvitationRequest, InvitationResponse, InvitationStatus};
```

Die `From<ServiceInvitationStatus> for InvitationStatus`-Impl (Zeilen 37-46) bleibt ERHALTEN — sie konvertiert zwischen `service::user_invitation::InvitationStatus` und dem nun `rest_types`-stämmigen `InvitationStatus`. Wegen Rust's orphan rule muss diese Impl in `rest-types/src/lib.rs` umziehen, ABER nur unter `#[cfg(feature = "service-impl")]` (sie nutzt das `service`-Crate). Pattern analog zu den anderen `From<&service::...>`-Impls in rest-types/src/lib.rs.

In `rest-types/src/lib.rs`, nach der `InvitationResponse`-Definition, hinzufügen:
```rust
#[cfg(feature = "service-impl")]
impl From<service::user_invitation::InvitationStatus> for InvitationStatus {
    fn from(status: service::user_invitation::InvitationStatus) -> Self {
        match status {
            service::user_invitation::InvitationStatus::Valid => Self::Valid,
            service::user_invitation::InvitationStatus::Expired => Self::Expired,
            service::user_invitation::InvitationStatus::Redeemed => Self::Redeemed,
            service::user_invitation::InvitationStatus::SessionRevoked => Self::SessionRevoked,
        }
    }
}
```

In `rest/src/user_invitation.rs` die alte `From<ServiceInvitationStatus> for InvitationStatus`-Impl (Zeilen 37-46) entfernen — sie lebt jetzt in `rest-types`.

**Wenn die Backend-Code-Nutzung von `redeemed_at` mit `OffsetDateTime` arbeitet** (z.B. an einer Aufrufstelle in `rest/src/user_invitation.rs` weiter unten, die `redeemed_at: Some(now)` setzt): die Konstruktion auf `redeemed_at: Some(now.format(&time::format_description::well_known::Rfc3339).unwrap_or_default())` umstellen. Vor dem Schreiben in `rest/src/user_invitation.rs` per Grep prüfen: `grep -n 'redeemed_at' rest/src/user_invitation.rs`. Falls Konstruktion mit `OffsetDateTime` existiert, an Ort und Stelle umstellen. Falls Backend nur deserialisiert/durchreicht, keine Änderung.

**Änderung E — `Cargo.toml` für rest-types**: KEINE `time`-Feature-Erweiterung nötig (Design-Entscheidung D-Phase6-01 vermeidet das). Datei bleibt unverändert. Keine Änderung an `Cargo.toml` ist explizit Teil dieses Tasks — nur falls die `redeemed_at`-Aufrufstellen-Migration einen weiteren Feature-Bedarf zeigen sollte (unwahrscheinlich), Cargo.toml entsprechend bumpen.

**Konsistenz-Check vor Commit (manuell, im Task-Body):**
1. `grep -c "InvitationStatus" shifty-backend/rest-types/src/lib.rs` muss >= 5 sein (1× enum-Def, mind. 4× Match-Arms in From-Impl).
2. `grep -c "InvitationResponse" shifty-backend/rest-types/src/lib.rs` muss >= 1 sein.
3. `grep -c "GenerateInvitationRequest" shifty-backend/rest-types/src/lib.rs` muss >= 1 sein.
4. In `rest/src/user_invitation.rs`: grep -E '^pub enum InvitationStatus|^pub struct (Generate|Invitation)' MUSS exit 1 haben (keine lokale Definition mehr).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo check --workspace 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo check --workspace` Exit-Code 0
    - `cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test --workspace --no-run` Exit-Code 0
    - `grep -E '^pub enum InvitationStatus' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | wc -l` ist `1`
    - `grep -E '^pub struct InvitationResponse' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | wc -l` ist `1`
    - `grep -E '^pub struct GenerateInvitationRequest' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | wc -l` ist `1`
    - `grep -E '^pub enum InvitationStatus' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/user_invitation.rs | wc -l` ist `0` (lokale Definition entfernt)
    - `grep -E 'pub use rest_types::\{[^}]*Invitation' /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/user_invitation.rs | wc -l` ist `1`
  </acceptance_criteria>
  <done>
    Backend kompiliert grün; Backend-Tests linken (no-run grün); Invitation-Types leben in rest-types und werden von rest/src/user_invitation.rs re-exportiert; From-Impl ist mit `service-impl`-Feature in rest-types.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 6 ist Build-only-Refactor, keine neue API-Surface, keine veränderten Auth-Pfade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | I (Information Disclosure) | rest-types/src/lib.rs Re-Migration | accept | Keine neuen serialisierten Felder; `InvitationResponse.redeemed_at` Wechsel von `OffsetDateTime` zu `String` ist wire-identisch (RFC3339-String, was Backend bereits serialisiert hat — verifiziert via `time::serde::rfc3339::option`-Konvention). Keine zusätzliche Information leaked. |
| T-06-00-02 | T (Tampering) | OpenAPI-Schema-Drift | mitigate | `cargo check --workspace` muss grün bleiben — utoipa-`ToSchema` ist für alle drei migrierten Types erhalten. Der OpenAPI-Spec für `/user-invitation/*`-Endpoints wird strukturell IDENTISCH bleiben (gleiche Felder, gleicher Status-Enum). Kein neues `#[utoipa::path]` nötig. |
</threat_model>

<verification>
- Backend-Workspace-Compile: `cd shifty-backend && cargo check --workspace` exit 0 (nach Task 0 UND nach Task 1)
- Backend-Test-Link: `cd shifty-backend && cargo test --workspace --no-run` exit 0 (nach Task 1)
- Strukturchecks (siehe acceptance_criteria oben)
</verification>

<success_criteria>
- Wave 0 ist erst grün, wenn beide Tasks ihre acceptance_criteria erfüllen
- Backend-Baseline (461 Tests) bleibt theoretisch erreichbar (no-run reicht — Phase 7 prüft den Run)
</success_criteria>

<output>
After completion, create `.planning/phases/06-rest-types-unification-frontend-compile-through/06-00-SUMMARY.md`
</output>
</content>
</invoke>