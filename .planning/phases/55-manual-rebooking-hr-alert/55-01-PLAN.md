---
plan_id: 55-01
phase: 55
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [REB-MANUAL-01, REB-MANUAL-02, HR-ALERT-03, HR-ALERT-04]
autonomous: true
files_modified:
  - service/src/rebooking_reconciliation.rs
  - service_impl/src/rebooking_reconciliation.rs
  - service_impl/src/lib.rs
  - service/src/lib.rs
  - service/src/rebooking_batch.rs
  - service_impl/src/rebooking_batch.rs
  - service_impl/src/reporting.rs
  - dao/src/rebooking_batch.rs
  - dao_impl_sqlite/src/rebooking_batch.rs
  - service_impl/src/test/mod.rs
  - service_impl/src/test/rebooking_reconciliation.rs
must_haves:
  truths:
    - "D-55-01 alert_predicate pure fn implemented"
    - "D-55-02 list_pending_for_sales_person + find_pending_for_sales_person DAO enable HR-Alert flag"
    - "D-55-03 hydrate_pending_to_suggestion computes IST/DANN + voluntary_delta_before/after Backend-side"
    - "D-55-04 no undo state on rebooking batches"
    - "D-55-05 iso_year/iso_week are HR-chosen (not derived server-side)"
    - "D-55-06 direction (VolunteerWork<->ExtraWork) is caller-supplied"
    - "D-55-07 approve+reject both terminate the pending state"
    - "REB-MANUAL-01 rebook_manual writes 2 ExtraHours + 1 batch + 1 entry in one Tx"
    - "REB-MANUAL-02 both directions VolunteerWork->ExtraWork and ExtraWork->VolunteerWork supported"
    - "HR-ALERT-03 approve and reject are state-conditional (WHERE state='pending', affected-rows check)"
    - "HR-ALERT-04 pending suggestion claims the UNIQUE slot via existing partial index"
    - "VOL-ACCT-03 both ExtraHours rows are written with ExtraHoursSource::Rebooking AND source==Rebooking-Filter im ReportingService greift zentral"
  artifacts:
    - path: "service/src/rebooking_reconciliation.rs"
      provides: "RebookingReconciliationService trait + RebookingDirection enum + alert_predicate pure fn + hydrate_pending_to_suggestion method + list_pending_for_sales_person(Option<Uuid>) method"
    - path: "service_impl/src/rebooking_reconciliation.rs"
      provides: "RebookingReconciliationServiceImpl (BL) — rebook_manual, list_pending_for_sales_person(Option<Uuid>), suggest_for_week, approve_suggestion, reject_suggestion, hydrate_pending_to_suggestion"
    - path: "service_impl/src/reporting.rs"
      provides: "Zentraler source==Rebooking-Filter in Aggregations-Pipes (VOL-ACCT-03 Wave-1-Owner)"
    - path: "dao/src/rebooking_batch.rs"
      provides: "New DAO methods find_pending_for_sales_person + list_all_pending + update_state_conditional"
    - path: "dao_impl_sqlite/src/rebooking_batch.rs"
      provides: "SQLx impls for the three new DAO methods incl. .sqlx offline cache"
    - path: "service_impl/src/test/rebooking_reconciliation.rs"
      provides: "Predicate truth-table + rebook_manual happy-path + double-approve concurrency test + Rebooking-Filter im Reporting-Aggregat"
  prohibitions:
    - "MUST NOT bump CURRENT_SNAPSHOT_SCHEMA_VERSION (stays 12)"
    - "MUST NOT introduce an Undone state on RebookingBatchState (D-55-04)"
    - "MUST NOT write ExtraHours rows without ExtraHoursSource::Rebooking (VOL-ACCT-03)"
    - "MUST NOT bypass HR permission gate in any public service method"
  key_links:
    - from: "service_impl/src/rebooking_reconciliation.rs"
      to: "service_impl/src/extra_hours.rs"
      via: "ExtraHoursService::create called twice inside same transaction"
      pattern: "extra_hours_service.create"
    - from: "service_impl/src/rebooking_reconciliation.rs"
      to: "service_impl/src/rebooking_batch.rs"
      via: "RebookingBatchService::create writes batch + entry FK-linked to the two extra_hours rows"
      pattern: "rebooking_batch_service.create"
---

<objective>
Backend Business-Logic Foundation für F3+F5. Neuer BL-Service
`RebookingReconciliationService` orchestriert `ExtraHoursService` +
`RebookingBatchService` + Marker-Update in EINER Transaktion (REB-MANUAL-01).
Neue DAO-Queries `find_pending_for_sales_person` (für DTO-Feld
`has_pending_rebooking` in Plan 02) und `update_state_conditional`
(state-conditional UPDATE `WHERE state='pending'` für Approve/Reject-Race,
HR-ALERT-03). Pure fn `alert_predicate` mit Truth-Table (D-55-01,
`balance <= -0.5h`). Kein REST-Layer, kein rest-types — kommt in Plan 02.

Purpose: Datenmodell-Basis (Phase 54) bleibt intakt (`extra_hours.source`
Marker, UNIQUE-Slot); dieser Plan schließt die BL-Lücke zwischen Basic
Services und dem REST-Layer und beweist die atomare Doppel-Eintrag-Semantik +
den Race-Schutz.

Output: `service/src/rebooking_reconciliation.rs` (Trait +
`RebookingDirection` enum + `alert_predicate`), Impl in `service_impl/`,
2 neue DAO-Methoden auf `RebookingBatchDao` + `RebookingBatchService`,
Testmodul mit Truth-Table + Happy-Path + Concurrency-Test.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-manual-rebooking-hr-alert/55-CONTEXT.md

# Phase-54-Basis (bereits geshippt)
@service/src/rebooking_batch.rs
@service_impl/src/rebooking_batch.rs
@dao/src/rebooking_batch.rs
@dao_impl_sqlite/src/rebooking_batch.rs
@service/src/extra_hours.rs
@service_impl/src/extra_hours.rs

# Präzedenz für BL-Service-Tier-Aufbau (mehrere Aggregat-Deps, HR-Gate)
@service_impl/src/vacation_balance.rs
@service_impl/src/absence_conversion.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trait + Types + Pure-fn alert_predicate</name>
  <files>service/src/rebooking_reconciliation.rs, service/src/lib.rs, service_impl/src/test/rebooking_reconciliation.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service/src/rebooking_batch.rs — Trait-Muster, Doc-Kommentare, kind/state-Verwendung
    - service/src/voluntary_stats.rs — Präzedenz für BL-Service-Trait mit mehreren Aggregat-Deps und HR-only-Aggregat-Rückgabe
    - service/src/lib.rs — pub-mod-Registrierung, HR_PRIVILEGE Import
    - service/src/extra_hours.rs — ExtraHoursCategory + ExtraHoursSource Verwendung
    - dao/src/rebooking_batch.rs — RebookingBatchKind/State-Varianten
    - .planning/phases/55-manual-rebooking-hr-alert/55-CONTEXT.md — Decisions D-55-01, D-55-03, D-55-04, D-55-05, D-55-06, D-55-07
  </read_first>
  <action>
Erstelle `service/src/rebooking_reconciliation.rs` und registriere das
Modul in `service/src/lib.rs` (`pub mod rebooking_reconciliation;`, alphabetisch
zwischen `rebooking_batch` und `reporting` einfügen). Inhalt:

1. Enum `RebookingDirection` mit Varianten `VolunteerToExtra` und
   `ExtraToVolunteer` (D-55-06). Doc-Kommentar: „positive Menge; die
   Richtung entscheidet welche Kategorie die -N-Row und welche die +N-Row
   erhält".

2. Pure fn `pub fn alert_predicate(balance: f32, voluntary_ist: f32,
   cap_active: bool) -> bool` mit Body genau: `cap_active && balance <= -0.5
   && voluntary_ist > 0.0` (D-55-01, Float-Noise-Tolerance).

3. Pure fn `pub fn proposed_rebooking_hours(balance: f32, voluntary_ist:
   f32) -> f32` mit Body `balance.abs().min(voluntary_ist).max(0.0)`
   (D-55-03). Nicht negativ.

4. Struct `RebookingSuggestion` (Domain-Analog zum späteren
   `RebookingSuggestionTO`) mit Feldern:
   - `batch_id: Uuid`
   - `sales_person_id: Uuid`
   - `iso_year: u32`
   - `iso_week: u8`
   - `proposed_hours: f32`
   - `balance_before: f32`
   - `voluntary_ist_before: f32`
   - `voluntary_soll_before: f32`
   - `voluntary_delta_before: f32` (Backend-computed; = voluntary_ist_before - voluntary_soll_before, aber Backend-berechnet damit FE keine Arithmetik macht — D-55-03, Fat-Backend-Regel MEMORY `feedback_fat_backend_thin_client`)
   - `balance_after: f32`
   - `voluntary_ist_after: f32`
   - `voluntary_soll_after: f32`
   - `voluntary_delta_after: f32` (Backend-computed; = voluntary_ist_after - voluntary_soll_after)

5. Trait `RebookingReconciliationService` mit `#[automock(type Context=();
   type Transaction=dao::MockTransaction;)]` und `#[async_trait]`:
   - `type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;`
   - `type Transaction: dao::Transaction;`
   - `async fn rebook_manual(&self, sales_person_id: Uuid, iso_year: u32,
     iso_week: u8, direction: RebookingDirection, hours: f32, context:
     Authentication<Self::Context>, tx: Option<Self::Transaction>) ->
     Result<dao::rebooking_batch::RebookingBatchEntity, ServiceError>;`
     — Doc: HR-gated, kind=Manual, state=Approved, schreibt 2 ExtraHours +
     Batch + Entry in einer Tx.
   - `async fn suggest_for_week(&self, sales_person_id: Uuid, iso_year:
     u32, iso_week: u8, context: Authentication<Self::Context>, tx:
     Option<Self::Transaction>) -> Result<RebookingSuggestion, ServiceError>;`
     — Doc: HR-gated, kind=HrSuggestion, state=Pending, KEINE ExtraHours-
     Rows (`extra_hours_out_id`/`in_id` NULL bis Approve).
   - `async fn approve_suggestion(&self, batch_id: Uuid, context:
     Authentication<Self::Context>, tx: Option<Self::Transaction>) ->
     Result<dao::rebooking_batch::RebookingBatchEntity, ServiceError>;`
     — Doc: state-conditional Pending→Approved + schreibt die 2 Pair-
     ExtraHours-Rows + patcht `extra_hours_out_id`/`in_id` auf Entry.
   - `async fn reject_suggestion(&self, batch_id: Uuid, context:
     Authentication<Self::Context>, tx: Option<Self::Transaction>) ->
     Result<dao::rebooking_batch::RebookingBatchEntity, ServiceError>;`
     — Doc: state-conditional Pending→Rejected, KEIN ExtraHours-Schreiben.
     UNIQUE-Slot bleibt belegt.
   - `async fn list_pending_for_sales_person(&self, sales_person_id:
     Option<Uuid>, context: Authentication<Self::Context>, tx:
     Option<Self::Transaction>) -> Result<Arc<[RebookingSuggestion]>,
     ServiceError>;` — Doc: HR-gated Read. `Some(id)` = pro-Person;
     `None` = alle offenen Suggestions phase-weit (für
     `GET /rebooking-suggestions` in Plan 55-02). Rückgabe sind
     **hydrierte** `RebookingSuggestion`-Structs (nicht rohe Batch-Entities),
     baut IST/DANN + `voluntary_delta_before/after` über Aufruf von
     `hydrate_pending_to_suggestion` intern. Doc verweist auf D-55-02 +
     D-55-03.
   - `async fn hydrate_pending_to_suggestion(&self, batch:
     &dao::rebooking_batch::RebookingBatchEntity, context:
     Authentication<Self::Context>, tx: Option<Self::Transaction>) ->
     Result<RebookingSuggestion, ServiceError>;` — Doc: intern von
     `list_pending_for_sales_person` und `suggest_for_week` genutzt.
     Baut das IST/DANN-Bundle: `balance_before/after`,
     `voluntary_ist_before/after`, `voluntary_soll_before/after`,
     `voluntary_delta_before/after` (Backend-computed nach D-55-03,
     Fat-Backend-Regel), `proposed_hours`. HR-Gate + `use_transaction`.

Kein Impl-Body hier — nur Trait + pure fns + struct/enum. Der Trait folgt
strikt der Präzedenz aus `service/src/voluntary_stats.rs` (async_trait,
automock, `Authentication<Self::Context>`-Parameter zuletzt vor `tx`).

Erstelle `service_impl/src/test/rebooking_reconciliation.rs` als
Skelett-File mit `mod predicate_truth_table { … }` und teste `alert_predicate`
mit folgender Truth-Table (jeder Fall ein `#[test]`):

- `cap=false, balance=-10, vol=5 → false`
- `cap=true, balance=-0.49, vol=5 → false` (Grenzfall D-55-01)
- `cap=true, balance=-0.5, vol=5 → true` (Grenzfall D-55-01)
- `cap=true, balance=-0.51, vol=5 → true`
- `cap=true, balance=-5, vol=0 → false` (kein Ehrenamt)
- `cap=true, balance=0, vol=5 → false`
- `cap=true, balance=-5, vol=-0.1 → false` (Negativ-Guard)

Und `mod proposed_hours { … }` mit:
- `balance=-10, vol=5 → 5.0`
- `balance=-3, vol=10 → 3.0`
- `balance=0.4, vol=5 → 0.4` (abs; Alert-Predicate greift nicht — separat)
- `balance=0, vol=5 → 0.0`
- `balance=-5, vol=0 → 0.0`

Registriere das Testmodul in `service_impl/src/test/mod.rs` alphabetisch
zwischen `rebooking_batch` und `reporting_additive_merge`
(`pub mod rebooking_reconciliation;`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo test -p service_impl rebooking_reconciliation::predicate_truth_table -- --exact --nocapture &amp;&amp; cargo test -p service_impl rebooking_reconciliation::proposed_hours -- --exact --nocapture</automated>
  </verify>
  <done>
    - `service/src/rebooking_reconciliation.rs` existiert mit Trait + Enum
      `RebookingDirection` + Struct `RebookingSuggestion` + 2 pure fns.
    - `service/src/lib.rs` registriert `pub mod rebooking_reconciliation;`.
    - Trait hat 6 async Methoden (`rebook_manual`, `suggest_for_week`,
      `approve_suggestion`, `reject_suggestion`,
      `list_pending_for_sales_person(Option<Uuid>)`,
      `hydrate_pending_to_suggestion`).
    - Struct `RebookingSuggestion` hat die zwei Backend-computed Delta-Felder
      `voluntary_delta_before` + `voluntary_delta_after` (D-55-03).
    - Predicate-Truth-Table + Proposed-Hours-Tests grün.
    - Kein Impl-Body — Impl-File landet in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: DAO-Erweiterung (find_pending_for_sales_person + update_state_conditional)</name>
  <files>dao/src/rebooking_batch.rs, dao_impl_sqlite/src/rebooking_batch.rs, service/src/rebooking_batch.rs, service_impl/src/rebooking_batch.rs</files>
  <read_first>
    - dao/src/rebooking_batch.rs — bestehendes Trait, MockTransaction, RebookingBatchEntity/EntryEntity
    - dao_impl_sqlite/src/rebooking_batch.rs — bestehende sqlx-Queries als Vorlage; str<->enum-Roundtrip
    - service/src/rebooking_batch.rs — bestehende Service-Methoden als Muster für Doc/HR-Gate
    - service_impl/src/rebooking_batch.rs — gen_service_impl! + HR_PRIVILEGE-Pattern
    - service_impl/src/test/rebooking_batch.rs — bestehende Test-Helper (build_service, existing_batch_entity)
  </read_first>
  <action>
Ergänze in `dao/src/rebooking_batch.rs` das `RebookingBatchDao`-Trait um drei
Methoden (nach `list_entries_for_batch`, gleicher Signatur-Stil):

1. `async fn find_pending_for_sales_person(&self, sales_person_id: Uuid,
   tx: Self::Transaction) -> Result<Arc<[RebookingBatchEntity]>, DaoError>;`
   — Doc: Liefert alle aktiven (`deleted IS NULL`) Batches mit
   `state='pending'` für diesen SalesPerson; über alle iso_year/iso_week.
   Konsumiert von `HR-ALERT`-Predicate in Plan 02 (DTO-Feld
   `has_pending_rebooking`).

2. `async fn list_all_pending(&self, tx: Self::Transaction) ->
   Result<Arc<[RebookingBatchEntity]>, DaoError>;`
   — Doc: Liefert alle aktiven (`deleted IS NULL`) Batches mit
   `state='pending'` über alle Personen. Konsumiert von
   `list_pending_for_sales_person(None, ...)` in
   `RebookingReconciliationService` für den phase-weiten
   `GET /rebooking-suggestions`-Endpoint (Plan 55-02).

3. `async fn update_state_conditional(&self, batch_id: Uuid, expected_state:
   RebookingBatchState, new_state: RebookingBatchState, approved:
   Option<time::PrimitiveDateTime>, approved_by: Option<Arc<str>>, new_version:
   Uuid, process: &str, tx: Self::Transaction) -> Result<u64, DaoError>;`
   — Doc: state-conditional UPDATE (`WHERE state=? AND deleted IS NULL AND
   id=?`); Rückgabe = affected rows count (0 wenn niemand mehr Pending war).
   Approve/Reject-Race-Schutz (HR-ALERT-03, Pitfall 12).

Setze in `dao_impl_sqlite/src/rebooking_batch.rs` diese Methoden gegen die
existierende `rebooking_batch`-Tabelle um:

- Für `find_pending_for_sales_person`: `sqlx::query!(...)` (kein `query_as`
  — die bestehenden Selects mappen row-by-row auf `RebookingBatchEntity`).
  Query-Predicate: `WHERE sales_person_id = ? AND state = 'pending' AND
  deleted IS NULL ORDER BY iso_year, iso_week`.
- Für `update_state_conditional`: `sqlx::query!("UPDATE rebooking_batch SET
  state = ?, approved = ?, approved_by = ?, version = ?, update_process = ?,
  updated = ? WHERE id = ? AND state = ? AND deleted IS NULL")`. Verwende
  `RebookingBatchState`→`&str`-Konversion identisch zum bestehenden
  `create_batch_with_entries`. Rückgabe: `result.rows_affected()`.
  Neuer Zeitstempel via `time::PrimitiveDateTime::MIN`-Fallback ist NICHT
  akzeptabel — Aufrufer liefert `approved: Option<PrimitiveDateTime>` bereits.
  Für den `updated`-Spalten-Wert nutze `approved.unwrap_or_else(||
  time::OffsetDateTime::now_utc()...)` analog Bestand.

Ergänze das `RebookingBatchService`-Trait in `service/src/rebooking_batch.rs`
um drei durchreichende Methoden (HR-gated Wrapper):

- `async fn find_pending_for_sales_person(&self, sales_person_id: Uuid,
  context: Authentication<Self::Context>, tx: Option<Self::Transaction>)
  -> Result<Arc<[RebookingBatchEntity]>, ServiceError>;`
- `async fn list_all_pending(&self, context:
  Authentication<Self::Context>, tx: Option<Self::Transaction>) ->
  Result<Arc<[RebookingBatchEntity]>, ServiceError>;`
- `async fn update_state_conditional(&self, batch_id: Uuid, expected_state:
  RebookingBatchState, new_state: RebookingBatchState, approved:
  Option<time::PrimitiveDateTime>, approved_by: Option<Arc<str>>, context:
  Authentication<Self::Context>, tx: Option<Self::Transaction>) ->
  Result<u64, ServiceError>;`

Implementiere alle drei in `service_impl/src/rebooking_batch.rs` analog zu
`find_by_sales_person_year_week` (HR-Gate zuerst, `use_transaction` +
`commit`); `update_state_conditional` erzeugt `new_version` intern via
`uuid_service.new_uuid("rebooking-batch-service::update_state_conditional
version")` und gibt es an das DAO weiter. Falls `rows_affected == 0`,
gib **nicht** einen Fehler zurück — der Aufrufer (Reconciliation-Service)
entscheidet, wie er auf „already-transitioned" reagiert. Doc-Kommentar
verweist auf Plan 55-01, HR-ALERT-03, Pitfall 12.

Führe `nix develop -c cargo sqlx prepare --workspace` aus und committe die
neuen `.sqlx/`-Einträge zusammen mit den Query-Änderungen (MEMORY
`reference_sqlx_prepare_after_new_query`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build -p dao -p dao_impl_sqlite -p service -p service_impl &amp;&amp; SQLX_OFFLINE=true cargo test -p dao_impl_sqlite --no-run 2>&amp;1 | tail -20 &amp;&amp; nix develop -c cargo sqlx prepare --workspace --check</automated>
  </verify>
  <done>
    - `dao/src/rebooking_batch.rs` hat 3 neue Trait-Methoden
      (`find_pending_for_sales_person`, `list_all_pending`,
      `update_state_conditional`).
    - `dao_impl_sqlite/src/rebooking_batch.rs` implementiert alle drei gegen
      die bestehende `rebooking_batch`-Tabelle.
    - `.sqlx/` enthält die neuen Query-Fingerprints; `cargo sqlx prepare
      --workspace --check` läuft grün (CI-Parity, MEMORY
      `reference_sqlx_prepare_after_new_query`).
    - `service/src/rebooking_batch.rs` und `service_impl/src/rebooking_batch.rs`
      exponieren alle drei Methoden HR-gated.
    - Alle Cross-Konsumenten (Mocks in bestehenden Tests) kompilieren nach
      Trait-Erweiterung — falls automock durch neue Methoden bricht,
      `expect_*`-Stubs in bestehenden Tests entsprechend hinzufügen.
  </done>
</task>

<task type="auto">
  <name>Task 3: RebookingReconciliationServiceImpl (BL) + Reporting-Chain-Filter + Test-Suite</name>
  <files>service_impl/src/rebooking_reconciliation.rs, service_impl/src/lib.rs, service_impl/src/reporting.rs, service_impl/src/test/rebooking_reconciliation.rs</files>
  <read_first>
    - service/src/rebooking_reconciliation.rs (aus Task 1)
    - service_impl/src/rebooking_batch.rs — Muster für gen_service_impl! + Transaktions-Pre-Check
    - service_impl/src/vacation_balance.rs — BL-Service mit mehreren Aggregat-Deps
    - service_impl/src/extra_hours.rs — ExtraHoursService::create-Signatur und ExtraHours-Struct
    - service_impl/src/reporting.rs — nur relevante Methode: get_report_for_employee für balance/voluntary_ist-Snapshot
    - service_impl/src/test/rebooking_batch.rs — Test-Helper-Muster
  </read_first>
  <action>
Erstelle `service_impl/src/rebooking_reconciliation.rs` und registriere in
`service_impl/src/lib.rs` (`pub mod rebooking_reconciliation;`, alphabetisch
zwischen `rebooking_batch` und `reporting` einordnen).

Struct + gen_service_impl! wie folgt:

```
gen_service_impl! {
    struct RebookingReconciliationServiceImpl:
        RebookingReconciliationService = RebookingReconciliationServiceDeps {
        ExtraHoursService: ExtraHoursService<Context = Self::Context, Transaction = Self::Transaction> = extra_hours_service,
        RebookingBatchService: RebookingBatchService<Context = Self::Context, Transaction = Self::Transaction> = rebooking_batch_service,
        ReportingService: ReportingService<Context = Self::Context, Transaction = Self::Transaction> = reporting_service,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        ClockService: ClockService = clock_service,
        UuidService: UuidService = uuid_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

BL-Klassifizierung wird in Doc-Kommentar erklärt (MEMORY
`feedback_service_tier_convention` — mehrere Aggregate + Cross-Entity-
Invariante). Kein Zyklus: `RebookingBatchService` bleibt Basic;
`ReportingService` konsumiert diesen Service NICHT.

Trait-Impl:

1. `rebook_manual`:
   - HR-Gate (`check_permission(HR_PRIVILEGE, ...)`).
   - `use_transaction(tx)`.
   - Sanity-Check `hours > 0.0` — bei Verletzung `ServiceError::ValidationError`
     mit i18n-freundlicher Message.
   - Berechne `date_time` = Montag der ISO-Woche 00:00 (nutze
     `shifty_utils::ShiftyDate` + ISO-Wochen-Helper — Präzedenz
     `service_impl/src/reporting.rs`).
   - Baue `RebookingDirection` in zwei `ExtraHours`-Payloads um:
     - `out`: negatives `amount=-hours`, Kategorie = Quell-Kategorie
       (VolunteerWork bzw. ExtraWork), `source = ExtraHoursSource::Rebooking`,
       `description = "Rebooking-Pair (out) — batch-Manual"`.
     - `in`: positives `amount=+hours`, Kategorie = Ziel-Kategorie,
       `source = ExtraHoursSource::Rebooking`.
   - `let out_row = extra_hours_service.create(&out_payload, context,
     Some(tx.clone())).await?;`
     `let in_row = extra_hours_service.create(&in_payload, context,
     Some(tx.clone())).await?;`
   - Balance-Snapshot-Werte für Audit: lies via
     `reporting_service.get_report_for_employee(&sales_person_id, iso_year,
     iso_week, context, Some(tx.clone()))` — `balance_hours`,
     `volunteer_hours` als `balance_before` / `voluntary_actual`. Für
     `voluntary_committed` beim Manual-Pfad in Task 4/Plan 03 nachträglich
     via `working_hours_service` verfeinerbar — hier: `0.0` als Platzhalter
     und Doc-Kommentar „Manual-Pfad — Soll-Snapshot nicht erforderlich
     (Audit-nur)". Bei Feld `voluntary_soll_before` in `RebookingSuggestion`
     analog handeln.
   - Baue `RebookingBatchEntity { kind: Manual, state: Approved, iso_year,
     iso_week, sales_person_id, ... }` mit `id`/`version` = `Uuid::nil()`
     (RebookingBatchServiceImpl füllt sie).
   - Baue `RebookingBatchEntryEntity { hours, balance_before,
     voluntary_actual, voluntary_committed, extra_hours_out_id: Some(out_row.id),
     extra_hours_in_id: Some(in_row.id), ... }`.
   - `rebooking_batch_service.create(&batch, &[entry], context,
     Some(tx.clone())).await?` — bei `ServiceError::EntityAlreadyExists`
     durchreichen (UNIQUE-Kollision → 409 in Plan 02).
   - `transaction_dao.commit(tx).await?`.
   - Rückgabe: der von `rebooking_batch_service.create` zurückgegebene
     Entity.

2. `suggest_for_week`:
   - HR-Gate + `use_transaction`.
   - Reporting-Snapshot laden (balance, voluntary_ist).
   - `proposed_hours = proposed_rebooking_hours(balance, voluntary_ist)`
     (D-55-03).
   - `RebookingBatchEntity { kind: HrSuggestion, state: Pending, ... }`,
     `RebookingBatchEntryEntity { hours: proposed_hours, balance_before,
     voluntary_actual: voluntary_ist, voluntary_committed: voluntary_soll,
     extra_hours_out_id: None, extra_hours_in_id: None }`.
   - Kein ExtraHours-Schreiben in Pending — Claim-on-Suggest via
     UNIQUE-Index (HR-ALERT-04).
   - `rebooking_batch_service.create(...)`.
   - Baue und gib `RebookingSuggestion { batch_id, sales_person_id,
     iso_year, iso_week, proposed_hours, balance_before, voluntary_ist_before,
     voluntary_soll_before, balance_after: balance_before + proposed_hours,
     voluntary_ist_after: voluntary_ist_before - proposed_hours,
     voluntary_soll_after: voluntary_soll_before }` zurück (DANN-Werte
     Backend-berechnet, D-55-03).

3. `approve_suggestion`:
   - HR-Gate + `use_transaction`.
   - `let batch = rebooking_batch_service.find_by_id(batch_id, context,
     Some(tx.clone())).await?.ok_or(ServiceError::EntityNotFound(batch_id))?;`
   - Wenn `batch.state != Pending`: `Err(ServiceError::InvalidStateChange {
     from: batch.state, to: Approved })`. Neu-anlegen als
     `ServiceError`-Variante falls nicht vorhanden — sonst reuse
     `ServiceError::EntityConflict` oder ähnlich; siehe `service/src/lib.rs`.
     Falls kein passender Fehler existiert, füge `#[error("Rebooking batch
     already resolved")] BatchAlreadyResolved` zu `ServiceError` hinzu.
   - Hole Batch-Entries (`rebooking_batch_service` benötigt evtl. eine
     `list_entries_for_batch(batch_id, ...)`-Ergänzung; falls nicht in
     Task 2 mitgemacht → hier ergänzen). Ein Entry pro Batch (Design).
   - Schreibe die Pair-ExtraHours (Marker Rebooking) analog `rebook_manual`.
   - `let affected = rebooking_batch_service.update_state_conditional(
     batch_id, RebookingBatchState::Pending, RebookingBatchState::Approved,
     Some(now), Some(context.actor_display_name()), context, Some(tx.clone()))
     .await?;`
   - Wenn `affected == 0`: Rollback via Return + `ServiceError::BatchAlreadyResolved`
     (T-1 Double-Approve-Race).
   - Commit + Return frisch geladenes Entity.

4. `reject_suggestion`: analog zu approve, aber KEIN ExtraHours-Schreiben;
   `update_state_conditional(Pending → Rejected)`. UNIQUE-Slot bleibt
   belegt (D-55-07).

5. `list_pending_for_sales_person(sales_person_id: Option<Uuid>, ...)`:
   HR-Gate + `use_transaction`. Bei `Some(id)` → durchreichen an
   `rebooking_batch_service.find_pending_for_sales_person(id, ...)`.
   Bei `None` → alle offenen Pending-Batches phase-weit über eine
   Basic-DAO-Methode `list_all_pending()` (ergänze diese in Task 2 als
   Zusatz — DAO + Service-Wrapper analog `find_pending_for_sales_person`,
   Query `WHERE state='pending' AND deleted IS NULL ORDER BY iso_year,
   iso_week, sales_person_id`). Für jede Batch-Entity ruft der Service
   intern `hydrate_pending_to_suggestion(&batch, context, tx.clone())`
   und sammelt die Ergebnisse. `commit` + Return.

6. `hydrate_pending_to_suggestion(batch, context, tx)`:
   HR-Gate + `use_transaction`.
   - Reporting-Snapshot laden (`get_report_for_employee(batch.sales_person_id,
     batch.iso_year, batch.iso_week, ...)`) → `balance_before`,
     `voluntary_ist_before` (aus `volunteer_hours`).
   - `voluntary_soll_before` = via `working_hours_service` oder aus
     `employee_work_details.committed_voluntary_hours`; Präzedenz
     `service_impl/src/voluntary_stats.rs`.
   - `proposed_hours = proposed_rebooking_hours(balance_before,
     voluntary_ist_before)` (D-55-03).
   - `balance_after = balance_before + proposed_hours`.
   - `voluntary_ist_after = voluntary_ist_before - proposed_hours`.
   - `voluntary_soll_after = voluntary_soll_before` (Soll ändert sich nicht
     durch Umbuchung).
   - `voluntary_delta_before = voluntary_ist_before - voluntary_soll_before`.
   - `voluntary_delta_after = voluntary_ist_after - voluntary_soll_after`.
   - Baue und gib `RebookingSuggestion { batch_id: batch.id,
     sales_person_id: batch.sales_person_id, iso_year: batch.iso_year,
     iso_week: batch.iso_week, proposed_hours, balance_before,
     voluntary_ist_before, voluntary_soll_before, voluntary_delta_before,
     balance_after, voluntary_ist_after, voluntary_soll_after,
     voluntary_delta_after }` zurück.
   - Wird auch von `suggest_for_week` genutzt (nach dem Batch-`create`),
     um die Response-Struct zu bauen — kein Duplicate-Code.

**Reporting-Chain-Filter (VOL-ACCT-03, Wave-1-Owner):**

In `service_impl/src/reporting.rs` aktiviere den zentralen
`source == ExtraHoursSource::Rebooking`-Filter für Aggregation-Pipes:

- Suche mit `grep -n "find_by_sales_person\|find_by_iso_year\|extra_hours\|ExtraHoursSource" service_impl/src/reporting.rs` alle Aggregations-Pfade
  (`get_report_for_employee`, `get_reports_for_all_employees`, `get_week`,
  `get_report_for_employee_range`, `EmployeeReport.volunteer_hours`).
- Nach jedem `extra_hours`-Fetch die Vec/Arc auf
  `eh.source != ExtraHoursSource::Rebooking` filtern
  (`.iter().filter(|eh| eh.source != ExtraHoursSource::Rebooking).cloned().collect()`).
- Der Filter greift für `balance_hours`, `overall_hours`, `volunteer_hours`,
  `extra_work_hours`, `dynamic_hours` — Rebooking-Pair-Rows sind
  Null-Summe und dürfen die Aggregate nicht bewegen.
- Doc-Kommentar an jeder Filter-Stelle: „Phase 55 (VOL-ACCT-03,
  D-55-03): Rebooking-Rows sind Pair-Nullsummen; für Read-Aggregate
  MÜSSEN sie ausgeklammert werden (Pitfall 1 Doppel-Zählung;
  Property-Test 55-03 als CI-Guard)".

Testmodul erweitern um zusätzlichen Filter-Test:

- `mod reporting_filter`:
  - `reporting_aggregate_ignores_rebooking_marker_rows`:
    Setup mit Mock `ExtraHoursDao::find_by_*` → gibt drei Rows zurück:
    eine mit `source=Manual, category=VolunteerWork, amount=+2.0`,
    zwei mit `source=Rebooking` (Pair: -1.0 und +1.0).
    Assertion: `EmployeeReport.volunteer_hours` gleich dem Aggregat
    ohne die zwei Marker-Rows (also 2.0, nicht 2.0 ± Pair-Rest).

Testmodul erweitern (`service_impl/src/test/rebooking_reconciliation.rs`):

- `mod predicate_truth_table` + `mod proposed_hours` bereits aus Task 1.
- Neuer `mod service` mit:
  - `rebook_manual_writes_two_extra_hours_batch_and_entry_in_one_tx`:
    setzt `MockExtraHoursService`-Erwartungen auf 2x create mit
    Marker `Rebooking` (negativ + positiv, Kategorien passend zur
    Richtung); `MockRebookingBatchService::expect_create` erwartet
    Entity mit `kind=Manual`, `state=Approved`, korrekten iso_year/week;
    HR-Auth durchgereicht; verifiziert Rückgabe.
  - `rebook_manual_forbidden_for_non_hr`: `MockPermissionService`
    liefert `ServiceError::Forbidden` → Impl darf nicht durchgehen.
  - `rebook_manual_unique_collision_returns_entity_already_exists`:
    `MockRebookingBatchService::create` gibt
    `ServiceError::EntityAlreadyExists(other_id)` zurück; wird
    unverändert propagiert.
  - `rebook_manual_supports_both_directions`: 2 Parameterized-Tests
    (Direction::VolunteerToExtra + Direction::ExtraToVolunteer) —
    prüft Kategorien-Zuordnung.
  - `approve_suggestion_updates_state_writes_pair_rows`: Happy-Path;
    `MockRebookingBatchService::update_state_conditional` returns `1`.
  - `approve_suggestion_double_approve_race_yields_error`:
    `update_state_conditional` returns `0` → `Err(BatchAlreadyResolved)`.
  - `reject_suggestion_updates_state_without_writing_extra_hours`:
    `MockExtraHoursService::create` wird NIE gerufen; state → Rejected.
  - `reject_after_approve_yields_error`: `find_by_id` liefert Entity mit
    `state=Approved` → Impl gibt Fehler, ohne UPDATE zu versuchen.

Nutze für die Mocks die von `#[automock]` generierten
`MockExtraHoursService`, `MockRebookingBatchService`, `MockReportingService`,
`MockPermissionService`, `MockClockService`, `MockUuidService`,
`MockTransactionDao`. Muster: bestehende `service_impl/src/test/*` Files.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; cargo build --workspace &amp;&amp; cargo test -p service_impl rebooking_reconciliation -- --nocapture &amp;&amp; cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
    - `service_impl/src/rebooking_reconciliation.rs` existiert mit
      `RebookingReconciliationServiceImpl` (BL-Tier).
    - Modul in `service_impl/src/lib.rs` registriert.
    - Alle 6 Trait-Methoden implementiert; jede beginnt mit HR-Gate.
    - `rebook_manual` schreibt genau 2 ExtraHours (beide mit
      `ExtraHoursSource::Rebooking`) + 1 Batch + 1 Entry in einer Transaktion.
    - `approve/reject` nutzen state-conditional UPDATE via
      `RebookingBatchService::update_state_conditional`; `affected == 0` →
      `ServiceError::BatchAlreadyResolved`.
    - `suggest_for_week` schreibt Pending-Batch ohne ExtraHours (Claim-on-Suggest).
    - `hydrate_pending_to_suggestion` liefert vollständige IST/DANN-Bundles
      inkl. Backend-computed `voluntary_delta_before/after` (D-55-03,
      Fat-Backend).
    - `list_pending_for_sales_person(None, ...)` liefert alle offenen
      Suggestions phase-weit (für Plan 55-02 GET /rebooking-suggestions).
    - `service_impl/src/reporting.rs` filtert `source == Rebooking` in
      allen Aggregations-Pipes (VOL-ACCT-03 Wave-1-Owner) — Test
      `reporting_aggregate_ignores_rebooking_marker_rows` grün.
    - Alle Tests grün: Predicate-Truth-Table + Proposed-Hours + Service-Impl
      (mind. 8 Service-Tests) + Reporting-Filter.
    - `cargo clippy --workspace -- -D warnings` grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST/service → BL-Service | HR-Auth-Kontext durchgereicht; jeder public trait-Aufruf muss HR-gaten (Plan-02 verifies) |
| Zwei parallele HR-Aufrufe | Double-Approve-Race auf identischen Pending-Batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-01 | Tampering | approve/reject | high | mitigate | state-conditional UPDATE `WHERE state='pending'` + affected-rows-Check; `BatchAlreadyResolved`-Error bei 0 (Task 3) |
| T-55-02 | Elevation of Privilege | rebook_manual / suggest / approve / reject | high | mitigate | `PermissionService::check_permission(HR_PRIVILEGE, ...)` als erste await-Operation in jeder public Impl-Methode; Test `rebook_manual_forbidden_for_non_hr` |
| T-55-03 | Repudiation | rebook_manual | medium | mitigate | Batch + Entry inkl. `balance_before`/`voluntary_actual` Snapshot für Audit; `approved_by` = Actor-Displayname |
| T-55-04 | Integrity | ExtraHours-Marker-Vergessen | high | mitigate | Impl setzt für BEIDE Pair-Rows `ExtraHoursSource::Rebooking` (Task 3, Property-Test folgt in Plan 55-03) |
</threat_model>

<verification>
Wave-1-Gates:
- `cargo build --workspace` grün.
- `cargo test -p service_impl rebooking_reconciliation -- --nocapture` grün
  (predicate_truth_table + proposed_hours + service-Tests).
- `cargo test --workspace` grün (Regression: keine bestehenden Tests fallen
  durch neue Trait-Methoden im Basic-`RebookingBatchService` — Mock-Stubs
  in bestehenden Tests nachziehen).
- `cargo clippy --workspace -- -D warnings` grün.
- `.sqlx/` Cache aktualisiert (neuer sqlx-Query in Task 2) + committed.
</verification>

<success_criteria>
- Trait `RebookingReconciliationService` + Impl vollständig; 6 Methoden mit
  HR-Gate + Atomarität (inkl. `hydrate_pending_to_suggestion` und
  `list_pending_for_sales_person(Option<Uuid>)`).
- Alert-Predicate (D-55-01, `balance <= -0.5h`) als pure fn + Truth-Table
  bewiesen.
- `proposed_rebooking_hours` (D-55-03) als pure fn + Testmatrix.
- `RebookingSuggestion` hat `voluntary_delta_before/after` als
  Backend-computed Felder (D-55-03, Fat-Backend MEMORY).
- 3 neue DAO-Methoden (`find_pending_for_sales_person`, `list_all_pending`,
  `update_state_conditional`) auf `RebookingBatchDao` + Wrapper auf
  `RebookingBatchService`; sqlx offline cache committed.
- Impl schreibt für alle F3-ExtraHours `ExtraHoursSource::Rebooking`
  (VOL-ACCT-03-Vorbedingung).
- `service_impl/src/reporting.rs`-Aggregate klammern
  `source==Rebooking`-Rows aus — VOL-ACCT-03-Filter Wave-1-Owner.
- Concurrency-Test beweist Double-Approve-Race-Schutz (T-55-01).
</success_criteria>

<output>
Schreibe `.planning/phases/55-manual-rebooking-hr-alert/55-01-SUMMARY.md`
nach Wave-Ende.
</output>
