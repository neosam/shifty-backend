---
plan_id: 55-05
phase: 55
plan: 05
type: execute
wave: 4
depends_on: [55-04]
requirements: [HR-ALERT-01, HR-ALERT-02, REB-MANUAL-01, REB-MANUAL-03]
autonomous: true
files_modified:
  - shifty-dioxus/src/page/employees.rs
  - shifty-dioxus/src/page/employee_details.rs
  - shifty-dioxus/src/component/employees_list.rs
  - shifty-dioxus/src/component/employees_shell.rs
must_haves:
  truths:
    - "D-55-02 HR-Path in /employees renders RebookingAlertBanner for every ShortEmployeeReport with has_pending_rebooking=true"
    - "D-55-05 F3-Trigger als Header-Row-Button auf Employee-Details (unter TopBar, oberhalb Voluntary-Stats-Zeile) öffnet ManualRebookingModal mit default current KW"
    - "D-55-06 F3-Trigger is HR-only (visible only to users with hr privilege)"
    - "D-55-07 Approve/Reject in SuggestionModal triggers page refresh -> alert row disappears from list"
    - "HR-ALERT-01 alert visibility relies solely on backend field (no FE re-computation)"
  artifacts:
    - path: "shifty-dioxus/src/page/employees.rs"
      provides: "HR-Path renders RebookingAlertBanner block + wires SuggestionModal open/close"
    - path: "shifty-dioxus/src/page/employee_details.rs"
      provides: "F3-Trigger Header-Row-Button (HR-only, unter TopBar), opens ManualRebookingModal with sales_person + current KW"
  prohibitions:
    - "MUST NOT put the F3-Trigger button inside the voluntary_stats_row — Header-Row-Button ist die fixierte Wahl (WARNING #3)"
    - "MUST NOT re-evaluate the alert predicate in FE — trust ShortEmployeeReportTO.has_pending_rebooking"
    - "MUST NOT block Non-HR-users from viewing /employees (backend redacts fields; FE just skips banner if flag is false)"
  key_links:
    - from: "shifty-dioxus/src/page/employees.rs"
      to: "shifty-dioxus/src/component/rebooking_alert_banner.rs"
      via: "iterates ShortEmployeeReports; for each with has_pending_rebooking=true renders <RebookingAlertBanner>"
      pattern: "has_pending_rebooking"
    - from: "shifty-dioxus/src/page/employee_details.rs"
      to: "shifty-dioxus/src/component/manual_rebooking_modal.rs"
      via: "TopBar action opens ManualRebookingModal with current ISO year/week from clock or router state"
      pattern: "ManualRebookingModal"
---

<objective>
FE-Integration der drei Komponenten aus Plan 55-04 in die zwei Ziel-Seiten.

- `page/employees.rs`: Liste zeigt für jeden Employee mit
  `has_pending_rebooking=true` einen `RebookingAlertBanner`; Klick öffnet
  `RebookingSuggestionModal` mit dem Suggestion, das per Loader-GET
  gefiltert nach `pending_rebooking_id` geladen wurde.
- `page/employee_details.rs`: TopBar/Action-Menu bekommt einen F3-Trigger
  „Manuelle Umbuchung" (HR-only). Klick öffnet `ManualRebookingModal` mit
  aktueller KW als Default.

Purpose: Der Plan schließt den User-Flow — was aus Plan 55-04 als
Komponenten-Bibliothek existiert, wird jetzt tatsächlich sichtbar.

Output: 4 modifizierte Files; Live-App zeigt F3+F5 vollständig.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/55-manual-rebooking-hr-alert/55-CONTEXT.md
@.planning/phases/55-manual-rebooking-hr-alert/55-04-PLAN.md

# Präzedenzen
@shifty-dioxus/src/page/employees.rs
@shifty-dioxus/src/page/employee_details.rs
@shifty-dioxus/src/component/employees_list.rs
@shifty-dioxus/src/component/employees_shell.rs
@shifty-dioxus/src/component/top_bar.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: page/employees.rs — HR-Path Banner-Integration</name>
  <files>shifty-dioxus/src/page/employees.rs, shifty-dioxus/src/component/employees_list.rs, shifty-dioxus/src/component/employees_shell.rs</files>
  <read_first>
    - shifty-dioxus/src/page/employees.rs — bestehende Struktur (TopBar + EmployeesShell)
    - shifty-dioxus/src/component/employees_shell.rs — Layout (Master/Detail); wo werden Employees gelistet?
    - shifty-dioxus/src/component/employees_list.rs — Liste-Komponente + Datenquelle (ShortEmployeeReport?)
    - shifty-dioxus/src/service/employee.rs — Datenlade-Coroutine + State-Store
    - shifty-dioxus/src/component/rebooking_alert_banner.rs (aus Plan 55-04)
    - shifty-dioxus/src/component/rebooking_suggestion_modal.rs (aus Plan 55-04)
    - shifty-dioxus/src/loader.rs — `load_rebooking_suggestions_pending`
  </read_first>
  <action>
Untersuche zuerst mit `grep -n "ShortEmployeeReport\|employees_list\|EmployeesList\|EmployeesShell" shifty-dioxus/src/component/employees_list.rs shifty-dioxus/src/component/employees_shell.rs shifty-dioxus/src/page/employees.rs`, wo die Liste der Employees gerendert wird. Es gibt zwei sinnvolle Integrations-Punkte:

**Option 1 (bevorzugt):** In `EmployeesList` — pro Employee-Row wird der
Banner UNTER dem Namen gerendert, wenn `report.has_pending_rebooking`
true ist.

**Option 2:** In `EmployeesShell` als eigene Sektion oberhalb der Liste,
die alle pending-Reports zusammenfasst.

Wähle Option 1, weil die Ripple-Semantik aus 55-CONTEXT (D-55-02) besagt:
das Feld hängt an `ShortEmployeeReportTO`, also gehört das Rendering an
die Employee-Row.

Konkret:

1. In `EmployeesList` (oder wo auch immer `ShortEmployeeReport`-Elemente
   gerendert werden — grep verrät den Ort):
   - Ergänze `use crate::component::RebookingAlertBanner;`.
   - In der Loop über `reports.iter()`: unter dem Employee-Namen einen
     `if report.has_pending_rebooking { RebookingAlertBanner { … } }`-Block
     rendern.
   - `on_click`-Handler: setzt ein Parent-Signal `open_suggestion_batch_id:
     Signal<Option<Uuid>>` auf `Some(report.pending_rebooking_id.unwrap())`.
     Das Signal wird von `page/employees.rs` (oder `EmployeesShell`)
     verwaltet.

2. In `page/employees.rs`:
   - Ergänze `let mut open_suggestion_batch_id: Signal<Option<Uuid>> =
     use_signal(|| None);`.
   - Lade beim Öffnen die konkrete Suggestion via
     **Client-Side-Filter aus `load_rebooking_suggestions_pending`**
     (Loader existiert bereits in Plan 55-04). Nutze
     `use_resource(move || async move {
        let all = load_rebooking_suggestions_pending(config.clone()).await?;
        Ok(all.into_iter().find(|s| Some(s.batch_id) == *open_suggestion_batch_id.read()))
     })` als Dependency auf `open_suggestion_batch_id`.
     Der Backend-Endpoint liefert alle offenen Suggestions phase-weit
     (Plan 55-02, gestützt auf `list_pending_for_sales_person(None, ...)`
     aus Plan 55-01) — im FE reicht der `find`-Filter. **KEIN neuer
     Endpoint, KEIN Loader-Nachzug in Plan 55-04** (minimalste Änderung).
   - Bei `Some(suggestion)`: rendere `<RebookingSuggestionModal
     suggestion={suggestion} on_close=... on_after_action=... />` als
     Overlay. Bei `None` (Filter findet die batch_id nicht mehr — z.B. weil
     der Batch zwischenzeitlich approved/rejected wurde) → Modal-Close +
     Refresh (D-55-07 verifiziert automatisch).
   - `on_after_action` triggert `EmployeeAction::Refresh` — der bestehende
     Employee-Service-Coroutine wird die ShortEmployeeReport-Liste neu
     laden und der Banner verschwindet automatisch (Backend liefert
     `has_pending_rebooking=false` nach Approve/Reject; D-55-07).

3. Falls die Employees-Seite bislang keine ShortEmployeeReport-Daten mit
   der neuen `has_pending_rebooking`-Feld liest, prüfe: der zugrunde
   liegende Loader (`shifty-dioxus/src/loader.rs`) parst
   `ShortEmployeeReportTO` bereits — durch `#[serde(default)]` (Plan
   55-02) sind die neuen Felder wire-kompatibel; existierende Loader
   funktionieren ohne Änderung. Bringe nur den State-Mapper (falls in
   `state/employee.rs` ein `ShortEmployeeReport` existiert) auf den neuen
   Stand — ergänze die zwei Felder analog dem TO. Wenn der State bislang
   die TOs direkt konsumiert, ist keine Änderung nötig.

4. `no_billing_period_content_in_source`-Test in `page/employees.rs`
   nicht brechen — `RebookingAlertBanner`-Referenzen im Source-File sind
   ok (keiner der verbotenen Strings betroffen).

Wichtiger Guard: **kein FE-Recompute des Predicates** (MUST NOT laut
must_haves). Der Banner rendert genau dann, wenn Backend
`has_pending_rebooking=true` liefert — kein `if balance < 0 && …` im FE.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown 2>&amp;1 | tail -20 &amp;&amp; cargo test -p shifty-dioxus 2>&amp;1 | tail -10 &amp;&amp; grep -rn "has_pending_rebooking" src/ | head -5</automated>
  </verify>
  <done>
    - `page/employees.rs` (oder `EmployeesList`) rendert Banner Predicate-
      gated aus dem Backend-Feld.
    - Klick öffnet SuggestionModal.
    - `on_after_action` triggert Refresh der Liste; Banner verschwindet
      nach Approve/Reject (D-55-07 verifiziert via Backend-Round-Trip).
    - FE-Build + Tests grün.
  </done>
</task>

<task type="auto">
  <name>Task 2: page/employee_details.rs — F3-Trigger in TopBar/Action-Menu</name>
  <files>shifty-dioxus/src/page/employee_details.rs</files>
  <read_first>
    - shifty-dioxus/src/page/employee_details.rs — vollständig, insbesondere Header/Action-Bereich (grep TopBar-Verwendung)
    - shifty-dioxus/src/component/top_bar.rs — Action-Menu-Muster (Zeilen 1-60)
    - shifty-dioxus/src/component/manual_rebooking_modal.rs (aus Plan 55-04)
    - shifty-dioxus/src/service/auth.rs — `AUTH` + `has_privilege("hr")`-Muster
    - shifty-dioxus/src/service/employee.rs — Employee-Refresh-Coroutine
  </read_first>
  <action>
**Fixierte Wahl (WARNING #3):** Header-Row-Button in
`page/employee_details.rs` (unter TopBar, oberhalb Voluntary-Stats-Zeile).
Konsistent mit ExtraHoursModal-Präzedenz. KEINE TopBar-Menu-Integration
(vermeidet unnötigen Umbau in `component/top_bar.rs`).

Konkrete Schritte:

1. In `page/employee_details.rs`:
   - Ergänze in der `EmployeeDetailsAction`-Enum eine neue Variante
     `OpenManualRebooking` und `CloseManualRebooking` (Muster:
     `OpenExtraHours` / `CloseExtraHours`).
   - Ergänze Signal `let mut show_manual_rebooking_dialog =
     use_signal(|| false);`.
   - Erweitere die `while let Some(action) = rx.next().await`-match
     um die zwei neuen Varianten (setze das Signal).
   - Nach dem bestehenden Modal-Render-Bereich (`{show_extra_hours_dialog
     .read()} then rsx!{ ExtraHoursModal { … } }`): Renderer für
     `ManualRebookingModal` ergänzen — HR-gated via
     `AUTH.read().has_privilege("hr")`.
   - Für die aktuelle KW: nutze `chrono::Utc::now()` bzw. `time::OffsetDateTime`
     mit `time::util::iso_week`. Existiert im Codebase bereits ein
     `current_iso_year_week`-Helper (grep — insb. in `service/i18n.rs`
     bzw. `state/*`), nimm den. Default: aktuelle ISO-KW zur Zeit des
     Modal-Öffnens.
   - `on_success` → `EmployeeDetailsAction::CloseManualRebooking` +
     `EmployeeAction::Refresh` (Report + ShortReport neu laden — Balance
     wird jetzt Backend-berechnet mit Marker-Filter neutral bleiben, aber
     die BookingLog / Extra-Hours-Anzeige zeigt die neuen Rows).

2. Header-Row-Button (fixiert):
   - In `page/employee_details.rs`, unmittelbar unter der TopBar und
     oberhalb der Voluntary-Stats-Zeile (Präzedenz: dort sitzt der
     bestehende ExtraHoursModal-Trigger).
   - HR-only via `AUTH.read().has_privilege("hr")` — Non-HR sieht den
     Button gar nicht.
   - Button: `<Btn on_click=… variant=Secondary>
     {i18n.get_text(Key::RebookingModalTitleManual)}</Btn>` —
     dispatched `EmployeeDetailsAction::OpenManualRebooking`.
   - KEIN Eingriff in `component/top_bar.rs` — TopBar bleibt unverändert.

Dokumentiere die Fixierung im 55-05-SUMMARY (Rationale D-55-06 +
WARNING #3 aus Plan-Checker).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus &amp;&amp; cargo build --target wasm32-unknown-unknown 2>&amp;1 | tail -20 &amp;&amp; cargo test -p shifty-dioxus 2>&amp;1 | tail -10 &amp;&amp; cargo clippy -p shifty-dioxus -- -D warnings 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `page/employee_details.rs` hat F3-Trigger in TopBar bzw. Header-Row
      (HR-gated).
    - `EmployeeDetailsAction::Open/CloseManualRebooking`-Varianten +
      Signal + Renderer für `ManualRebookingModal`.
    - `on_success` triggert Employee-Refresh — neue ExtraHours-Rows
      sichtbar; Balance bleibt neutral (Property-Test 55-03 garantiert).
    - FE-Build + Tests + Clippy `-D warnings` grün.
  </done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-55-08 | Elevation of Privilege | F3-Trigger in TopBar sichtbar für Non-HR | medium | mitigate | FE-Gate via `AUTH.read().has_privilege("hr")`; Backend würde 403 zurückgeben — Defense-in-Depth. |
| T-55-09 | Integrity | Alert bleibt hängen nach Approve | high | mitigate | `on_after_action` triggert `EmployeeAction::Refresh`; Backend liefert `has_pending_rebooking=false` (D-55-07); Test: verify banner disappears after approval. |
</threat_model>

<verification>
- FE-Build + FE-Tests + FE-Clippy `-D warnings` grün.
- Manueller E2E-Smoke (nach `cargo run` Backend + `dx serve` Frontend als
  HR-User): `/employees` zeigt Banner bei einer Person mit cap+negativer
  Balance+voluntary>0; Klick öffnet Modal; Approve → Banner weg. Auf
  `/employees/{id}` sichtbar F3-Trigger → Modal öffnet → Buchung
  persistiert.
</verification>

<success_criteria>
- F5-Alert vollständig integriert (Banner + Modal + Approve/Reject-Flow).
- F3-Trigger in Employee-Details in TopBar/Header (HR-only).
- Refresh-Zyklus schließt sich; Banner verschwindet.
</success_criteria>

<output>
Schreibe `.planning/phases/55-manual-rebooking-hr-alert/55-05-SUMMARY.md`
nach Wave-Ende.
</output>
