---
phase: 08.3
plan: 02
type: execute
wave: 2
depends_on: [08.3-01]
files_modified:
  - dao_impl_sqlite/src/absence.rs
  - service_impl/src/absence.rs
  - service_impl/src/test/absence_no_drift.rs
autonomous: true
requirements: [FUI-A-10]
requirements_addressed: [FUI-A-10]
tags: [dao, service, crud, halftime, day-fraction, sqlx, threading, no-drift]

must_haves:
  truths:
    - "Alle 6 SELECT-Queries in `dao_impl_sqlite/src/absence.rs` selecten `day_fraction` und projizieren in `AbsencePeriodDb.day_fraction: String`."
    - "`TryFrom<&AbsencePeriodDb> for AbsencePeriodEntity` mapped `day_fraction` per `match &str { \"full\" => Full, \"half\" => Half, value => DaoError::EnumValueNotFound(value.into()) }` analog Kategorie-Pattern."
    - "INSERT-Statement in `create` schreibt `day_fraction` per neuen `day_fraction_to_str(&DayFractionEntity)`-Helper (`Full=\"full\"`, `Half=\"half\"`); Helper liegt am Modul-Ende neben `category_to_str`."
    - "`update` bleibt unverändert (Tombstone-Pattern — schreibt nur `deleted`/`update_version`/`update_process`; neue `day_fraction`-Werte landen via `create` der neuen Row, siehe PATTERNS.md Pattern 6)."
    - "`AbsenceServiceImpl::create` und `AbsenceServiceImpl::update` threaden `day_fraction` aus dem Request unverändert nach DAO (`new_entity` in update Z. 313-325 erweitert um `day_fraction: (&request.day_fraction).into()`; `create`-Pfad braucht keine Code-Änderung weil `to_owned`/`TryFrom`-Pfad das Feld automatisch kopiert)."
    - "Alle bestehenden Test-Fixtures (`period_to_entity` / `fixture_*_period` / Test-Builder in `service_impl/src/test/`) tragen `day_fraction: DayFractionEntity::Full` per Default oder leiten es per `..Default::default()` ab — alle bestehenden Tests bleiben grün (no-drift im Test-Layer)."
    - "Neuer Integration-Test `absence_period_default_day_fraction_is_full_on_legacy_rows` verifiziert: legacy-Insert OHNE day_fraction-Spalte (simuliert per Direct-SQL mit pre-migration-Schema) wird durch Migration zu Default `'full'` aufgefüllt; SELECT liefert `DayFractionEntity::Full`."
    - "Neuer Integration-Test `absence_period_create_and_select_with_half_persists_correctly` verifiziert: `create` mit `Half` → SELECT liefert `Half` zurück (Round-Trip)."
    - "`cargo build -p dao_impl_sqlite && cargo build -p service_impl` exit 0."
    - "`cargo test -p dao_impl_sqlite absence` exit 0 (alle existierenden + neuen DAO-Tests grün)."
    - "`cargo test -p service_impl absence` exit 0 (alle existierenden Absence-CRUD-Tests grün — no-drift im Service-Layer)."
  artifacts:
    - path: "dao_impl_sqlite/src/absence.rs"
      provides: "AbsencePeriodDb.day_fraction-Feld + TryFrom-Branch + day_fraction_to_str Helper + 6 SELECT-Query-Updates + INSERT-Update"
      min_lines: 25
    - path: "service_impl/src/absence.rs"
      provides: "update-Pfad new_entity um day_fraction erweitert; alle bestehenden Tests grün"
    - path: "service_impl/src/test/absence_no_drift.rs"
      provides: "2 neue Integration-Tests (legacy-default, round-trip-half) gegen In-Memory-SQLite"
      min_lines: 60
  key_links:
    - from: "dao_impl_sqlite::absence::AbsencePeriodDb"
      to: "dao_impl_sqlite::absence::AbsencePeriodEntity (TryFrom)"
      via: "day_fraction TEXT-String-Mapping zu Enum"
      pattern: "\"full\"\\s*=>\\s*DayFractionEntity::Full"
    - from: "service_impl::absence::AbsenceServiceImpl::update"
      to: "dao::absence::AbsencePeriodEntity"
      via: "request.day_fraction → new_entity.day_fraction"
      pattern: "day_fraction:\\s*\\(&request\\.day_fraction\\)\\.into\\(\\)"
---

<vcs_jj_only>
**VCS-Politik (jj-only) — verbindlich:** Dieses Repository ist `jj`-managed (co-located mit git). KEINE `git commit`-, `git add`-, `git reset`- oder `git checkout`-Mutations. Verwende `jj describe`, `jj commit -m "..."`, `jj new` für alle Mutationen. Convention für diese Phase: `feat(08.3-02): <subject>` für Source-Edits; `test(08.3-02): <subject>` für Test-Adds.
</vcs_jj_only>

<objective>
Ziehe das in Plan 01 angelegte `day_fraction`-Feld vollständig durch DAO-SQLite-Layer und Service-CRUD: alle 6 SELECT-Queries, der INSERT, alle TryFrom-Mappings, der Service-`update`-Pfad. Stelle die no-drift-Garantie für Bestandsdaten her, indem alle bestehenden Test-Fixtures auf `DayFraction::Full` defaulten, und füge zwei In-Memory-SQLite-Integration-Tests hinzu: legacy-Insert-Default-Test (no-drift) und Round-Trip-Test mit `Half`. Plan 04 (Reporting-Hot-Path) und Plan 05 (Cutover) hängen davon ab, dass `day_fraction` zu/von DB persistiert.

Purpose: DAO-Schicht ist die kritischste Threading-Stelle (sqlx-compile-time-check zwingt Symmetrie zwischen SELECT-Liste und `AbsencePeriodDb`-Struct). Wenn EINE SELECT-Liste vergessen wird, schlägt der Build fehl. Daher ist Vollständigkeit hier nicht-verhandelbar.

Output: 3 modifizierte Source-Files + 2 neue Integration-Tests; alle DAO + Absence-Service-Tests grün; no-drift im Test-Layer dokumentiert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-CONTEXT.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-RESEARCH.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-01-PLAN.md
@.planning/phases/08.3-halbtag-support-f-r-absences/08.3-VALIDATION.md

@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Live-Patterns aus den existierenden Files, damit der Executor nicht raten muss -->

From dao_impl_sqlite/src/absence.rs (existing — Z. 14-25):
```rust
struct AbsencePeriodDb {
    id: Vec<u8>,
    logical_id: Vec<u8>,
    sales_person_id: Vec<u8>,
    category: String,
    from_date: String,
    to_date: String,
    description: Option<String>,
    created: String,
    deleted: Option<String>,
    update_version: Vec<u8>,
    // NEU in Plan 02: day_fraction: String,
}
```

From dao_impl_sqlite/src/absence.rs (existing — Z. 60-66):
```rust
fn category_to_str(c: &AbsenceCategoryEntity) -> &'static str {
    match c {
        AbsenceCategoryEntity::Vacation => "Vacation",
        AbsenceCategoryEntity::SickLeave => "SickLeave",
        AbsenceCategoryEntity::UnpaidLeave => "UnpaidLeave",
    }
}
```

From dao_impl_sqlite/src/absence.rs (existing — Z. 35-39, Category-Match in TryFrom):
```rust
category: match row.category.as_str() {
    "Vacation" => AbsenceCategoryEntity::Vacation,
    "SickLeave" => AbsenceCategoryEntity::SickLeave,
    "UnpaidLeave" => AbsenceCategoryEntity::UnpaidLeave,
    value => return Err(DaoError::EnumValueNotFound(value.into())),
},
```

From dao_impl_sqlite/src/absence.rs (existing — Z. 257-273, INSERT-Statement):
```rust
query!(
    "INSERT INTO absence_period (id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_process, update_version) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
    id_vec,
    logical_id_vec,
    sp_vec,
    category_str,
    from_str,
    to_str,
    description,
    created,
    deleted,
    process,
    version_vec,
)
```

From service_impl/src/absence.rs (existing — Z. 313-325, update-Pfad new_entity):
```rust
let new_entity = absence::AbsencePeriodEntity {
    id: new_id,
    logical_id: active.logical_id,
    sales_person_id: active.sales_person_id,
    category: (&request.category).into(),
    from_date: request.from_date,
    to_date: request.to_date,
    description: request.description.clone(),
    created: now,
    deleted: None,
    version: new_version,
    // NEU in Plan 02: day_fraction: (&request.day_fraction).into(),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: dao_impl_sqlite/src/absence.rs durchziehen (AbsencePeriodDb + TryFrom + Helper + 6 SELECTs + INSERT)</name>
  <files>dao_impl_sqlite/src/absence.rs</files>
  <read_first>
    - dao_impl_sqlite/src/absence.rs (gesamter File — RESEARCH § 1.3 referenziert konkrete Zeilen: 14-25, 27-58, 60-66, 88-98, 107-117, 127-136, 143-152, 169-198, 217-235, 257-273, 277-300)
    - dao/src/absence.rs (nach Plan 01 — `DayFractionEntity` existiert; `AbsencePeriodEntity.day_fraction` ist neues Pflicht-Feld)
    - migrations/sqlite/20260517120000_add-day-fraction-to-absence-period.sql (aus Plan 01 — Spaltenname/Default-Wert)
    - .planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md (Z. 150-230 — kompletter DAO-SQLite-Pattern-Block mit 6 Sub-Patterns)
  </read_first>
  <action>
    **WICHTIG:** sqlx nutzt compile-time-Query-Checking. Die SELECT-Spalten in `query_as!`-Aufrufen werden mit den Feld-Namen+Typen in `AbsencePeriodDb` abgeglichen. Wenn EINE Stelle inkonsistent ist, schlägt der Build fehl. Stelle Symmetrie streng her:

    **Schritt A — `AbsencePeriodDb`-Struct erweitern** (Z. 14-25): Neues Feld am Ende:

    ```rust
        day_fraction: String,
    ```

    **Schritt B — `TryFrom<&AbsencePeriodDb> for AbsencePeriodEntity` erweitern** (Z. 27-57). Im Struct-Literal-Body, am Ende vor dem schließenden `}`, ergänze:

    ```rust
            day_fraction: match row.day_fraction.as_str() {
                "full" => DayFractionEntity::Full,
                "half" => DayFractionEntity::Half,
                value => return Err(DaoError::EnumValueNotFound(value.into())),
            },
    ```

    **Schritt C — `day_fraction_to_str`-Hilfsfunktion** direkt nach `category_to_str` (nach Z. 66):

    ```rust
    fn day_fraction_to_str(f: &DayFractionEntity) -> &'static str {
        match f {
            DayFractionEntity::Full => "full",
            DayFractionEntity::Half => "half",
        }
    }
    ```

    **Schritt D — alle 6 SELECT-Queries erweitern**. Pattern: in jeder SELECT-Liste `, day_fraction` direkt nach `update_version` (oder am Ende des SELECT-Tupels, wie es bestand ist) einfügen. Konkret:

    - Z. 88-98 `find_by_id`: `SELECT id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_version, day_fraction FROM absence_period WHERE id = ?`
    - Z. 107-117 `find_by_logical_id`: Spaltenliste analog
    - Z. 127-136 `find_by_sales_person`: Spaltenliste analog
    - Z. 143-152 `find_all`: Spaltenliste analog
    - Z. 169-198 `find_overlapping` (zwei Varianten — mit/ohne `exclude_logical_id`): beide Spaltenlisten analog
    - Z. 217-235 `find_overlapping_for_booking`: Spaltenliste analog

    **Hinweis:** Wenn eine SELECT-Liste die Spalten in einer Reihenfolge selectet, die NICHT die `AbsencePeriodDb`-Feldreihenfolge ist, MUSS die Reihenfolge weiter konsistent zu `AbsencePeriodDb` sein. `query_as!` projiziert positional. Wenn die existierenden SELECTs `update_version` direkt nach `deleted` haben (was die Live-Reihenfolge ist, siehe Z. 88: `... description, created, deleted, update_version`), dann liegt `day_fraction` AM ENDE der SELECT-Liste UND AM ENDE des Structs.

    **Schritt E — INSERT-Statement in `create` erweitern** (Z. 257-273):

    E.1 — Vor dem `query!`-Aufruf eine neue Binding-Variable ergänzen:

    ```rust
    let day_fraction_str = day_fraction_to_str(&entity.day_fraction);
    ```

    E.2 — Spaltenliste im INSERT um `, day_fraction` am Ende ergänzen + Placeholder + Wert:

    ```rust
    query!(
        "INSERT INTO absence_period (id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_process, update_version, day_fraction) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
        id_vec,
        logical_id_vec,
        sp_vec,
        category_str,
        from_str,
        to_str,
        description,
        created,
        deleted,
        process,
        version_vec,
        day_fraction_str,
    )
    ```

    **Schritt F — `update` BLEIBT UNVERÄNDERT** (Z. 277-300). PATTERNS.md Pattern 6: `update` ist das Tombstone-Pattern — es schreibt nur `deleted`, `update_version`, `update_process` auf die alte Row. Neue `day_fraction`-Werte landen via `create` der neuen Row (logical_id rotiert; das bestehende Versionierungs-Pattern aus Phase 1 wird unverändert wiederverwendet). Wenn du versucht bist, hier `day_fraction` zu schreiben — nicht tun. Es würde die Versionierungs-Semantik brechen.

    **Schritt G — Imports prüfen.** Wenn `DayFractionEntity` noch nicht importiert ist, ergänze:

    ```rust
    use dao::absence::DayFractionEntity;
    ```

    (oder im bestehenden `use dao::absence::{AbsenceCategoryEntity, AbsencePeriodEntity, ...}` als zusätzlichen Eintrag).

    **Sanity:** Wenn `sqlx` einen `error: column day_fraction not found` wirft, ist die Migration auf der dev-DB nicht angewendet. Lokal: `cargo sqlx migrate run` (nicht `reset`!) im `nix develop`-Env (siehe CLAUDE.local.md). Wenn `error: type mismatch: column day_fraction is String, but field is i64`, hat man `Option<String>` statt `String` gemacht — Migration deklariert `NOT NULL`, also `String` (nicht `Option<String>`).
  </action>
  <verify>
    <automated>
      grep -c "day_fraction: String" dao_impl_sqlite/src/absence.rs | grep -qx 1 \
      && grep -c '"full" => DayFractionEntity::Full' dao_impl_sqlite/src/absence.rs | grep -qx 1 \
      && grep -c '"half" => DayFractionEntity::Half' dao_impl_sqlite/src/absence.rs | grep -qx 1 \
      && grep -c "fn day_fraction_to_str" dao_impl_sqlite/src/absence.rs | grep -qx 1 \
      && grep -cE 'SELECT[^;]*day_fraction' dao_impl_sqlite/src/absence.rs | awk '{ if ($1 >= 6) exit 0; else exit 1 }' \
      && grep -c "day_fraction_str" dao_impl_sqlite/src/absence.rs | awk '{ if ($1 >= 2) exit 0; else exit 1 }' \
      && cargo sqlx prepare --check -p dao_impl_sqlite --workspace 2>&1 | tail -5 ; \
         cargo build -p dao_impl_sqlite
    </automated>
    Hinweis: `cargo sqlx prepare --check` ist nur dann hart-required wenn das Repo offline-sqlx nutzt; ansonsten reicht `cargo build`. Wenn cargo build grün ist (sqlx checked queries gegen die live DB), ist die Symmetrie verifiziert.
  </verify>
  <done>
    `AbsencePeriodDb.day_fraction: String` deklariert. `TryFrom`-Mapping mit `full`/`half`/Error-Fallback vorhanden. `day_fraction_to_str` als Helper neben `category_to_str`. Alle 6 SELECT-Queries selecten `day_fraction`. INSERT schreibt `day_fraction`. `update` unverändert. `cargo build -p dao_impl_sqlite` exit 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AbsenceServiceImpl::update durchziehen + bestehende Test-Fixtures auf Default-Full hieven</name>
  <files>
    service_impl/src/absence.rs,
    service_impl/src/test/absence_no_drift.rs
  </files>
  <read_first>
    - service_impl/src/absence.rs (insbesondere Z. 157-237 `create`-Pfad, Z. 239-348 `update`-Pfad, Z. 313-325 `new_entity`-Konstruktion)
    - service_impl/src/test/ (Verzeichnislisting — relevant: `absence_*.rs`-Files, `reporting_phase2_fixtures.rs` ggf. mit `period_to_entity` / `fixture_*_period`-Helpern)
    - service/src/absence.rs (nach Plan 01 — `AbsencePeriod.day_fraction: DayFraction` ist nun Pflicht-Feld; alle Konstruktions-Stellen sind Compile-Errors falls nicht angepasst)
    - dao/src/absence.rs (nach Plan 01 — `AbsencePeriodEntity.day_fraction: DayFractionEntity` ist Pflicht-Feld)
    - .planning/phases/08.3-halbtag-support-f-r-absences/08.3-PATTERNS.md (Z. 313-380 — Service-CRUD-Pattern)
    - dao_impl_sqlite/src/test/ ODER service_impl/src/test/ — schauen, wo die existing In-Memory-SQLite-Integration-Tests für Absence-Period leben (`grep -r "in_memory" dao_impl_sqlite/ service_impl/`)
  </read_first>
  <action>
    **Schritt A — `AbsenceServiceImpl::update` `new_entity`-Konstruktion erweitern** (Z. 313-325 in service_impl/src/absence.rs):

    Im `new_entity = absence::AbsencePeriodEntity { ... }`-Block am Ende vor `}` ergänzen:

    ```rust
            day_fraction: (&request.day_fraction).into(),
    ```

    **KRITISCH:** Hier MUSS `request.day_fraction` stehen — NICHT `active.day_fraction`. Sonst kann der User das Feld nie ändern. PATTERNS.md Z. 378 dokumentiert das explizit.

    **Schritt B — `create`-Pfad (Z. 157-237) prüfen.** Der `create`-Pfad ruft typisch `let mut entity = request.to_owned()` oder ähnlich und konvertiert via `AbsencePeriodEntity::try_from(&entity)` oder direktem `From`. Wenn die Konvertierung über die in Plan 01 erweiterten `From`/`TryFrom`-Pairs läuft, ist KEINE Änderung im `create`-Body nötig — das Feld wird automatisch propagiert. Lies den Pfad einmal genau durch und verifiziere; falls es ein Inline-Struct-Literal `AbsencePeriodEntity { ... }` ohne `From`-Konvertierung gibt, ergänze `day_fraction: (&request.day_fraction).into()` analog Schritt A.

    **Schritt C — Bestehende Test-Fixtures hieven.** Suche systematisch alle Konstruktions-Stellen von `AbsencePeriod` (Domain) und `AbsencePeriodEntity` (DAO) in den Test-Modulen:

    ```bash
    grep -rn "AbsencePeriod\s*{" service_impl/src/test/
    grep -rn "AbsencePeriodEntity\s*{" service_impl/src/test/
    grep -rn "AbsencePeriod\s*{" dao_impl_sqlite/src/
    grep -rn "AbsencePeriodEntity\s*{" dao_impl_sqlite/src/
    ```

    An jeder Konstruktions-Stelle ergänzen:
    - Für `AbsencePeriod`: `day_fraction: service::absence::DayFraction::Full,`
    - Für `AbsencePeriodEntity`: `day_fraction: dao::absence::DayFractionEntity::Full,`

    Mass-Edit-Strategie: Wenn 10+ Stellen betroffen, lieber einen kurzen `..Default::default()`-Fallback nutzen — aber NUR wenn die Struct `Default` derived; `AbsencePeriod` derived `Default` NICHT (wegen `Uuid`-Feldern ohne default), also expliziter Feld-Eintrag.

    **Helper-Fixtures wie `period_to_entity` / `fixture_vacation_period`** (RESEARCH § 10.1 + PATTERNS.md Z. 743): suche diese in `service_impl/src/test/` (Pfad `reporting_phase2_fixtures.rs` oder `absence_derive_hours_range.rs`-Modul). Setze den Default dort einmalig:

    ```rust
    // in period_to_entity / fixture_vacation_period etc.
    day_fraction: dao::absence::DayFractionEntity::Full,
    ```

    Damit alle existing Tests, die diese Helper benutzen, sofort grün sind.

    **Schritt D — Zwei neue Integration-Tests in `service_impl/src/test/absence_no_drift.rs`** (neue Datei, registriere im `service_impl/src/test/mod.rs` oder via `#[cfg(test)] mod ...`-Pfad):

    ```rust
    //! Phase 8.3 — No-Drift-Garantie + Halbtag-Round-Trip auf der DAO-Ebene
    //! (In-Memory-SQLite via dao_impl_sqlite). Validiert, dass die Migration
    //! `day_fraction TEXT NOT NULL DEFAULT 'full'` für Bestandsdaten das
    //! Default 'full' liefert UND dass eine Round-Trip-Persistierung mit
    //! `Half` korrekt wieder als `Half` gelesen wird.
    //!
    //! Pattern-Quelle: existing In-Memory-SQLite-Tests in
    //! dao_impl_sqlite/src/test/ (Setup-Helper sind dort definiert).

    use dao::absence::{AbsencePeriodEntity, DayFractionEntity};
    use uuid::Uuid;
    use time::macros::date;

    // Helper-Funktionen (Pool-Setup, Person-Fixture etc.) — wiederverwende
    // existing Helpers aus dao_impl_sqlite-Tests. Falls die Helpers privat
    // sind, ergänze `#[cfg(test)] pub(crate)`-Visibility ODER kopiere
    // minimale Setup-Funktion (8-12 Zeilen) lokal.

    #[tokio::test]
    async fn absence_period_default_day_fraction_is_full_on_legacy_rows() {
        // 1. Pool aufsetzen mit allen Migrations bis EXCL. Plan-01-Migration.
        //    (Alternative wenn 1 zu fummelig: alle Migrations laufen lassen,
        //    dann eine Row direkt per Raw-SQL einfügen, die das day_fraction-
        //    Feld OMITS — der Default 'full' wird vom Schema gefüllt.)
        // 2. INSERT INTO absence_period (...) VALUES (...) OHNE day_fraction-
        //    Spalte → DB füllt 'full' per DEFAULT.
        // 3. SELECT via AbsenceDaoImpl.find_by_id → Entity.day_fraction == Full.
        //
        // Variante (einfach): alle Migrations laufen, Direct-SQL-Insert OHNE
        // day_fraction-Spalte, DAO-Read.
        //
        // Setup-Code analog dao_impl_sqlite::test::absence::*::setup_pool().

        // ... Implementation
        // assert_eq!(read_entity.day_fraction, DayFractionEntity::Full);
    }

    #[tokio::test]
    async fn absence_period_create_and_select_with_half_persists_correctly() {
        // 1. Pool + Sales-Person-Fixture.
        // 2. AbsenceDaoImpl::create mit Entity { day_fraction: Half, ... }.
        // 3. AbsenceDaoImpl::find_by_id → Entity.day_fraction == Half.

        // ... Implementation
        // assert_eq!(read_entity.day_fraction, DayFractionEntity::Half);
    }
    ```

    Konkrete Setup-Helpers: schaue, ob in `dao_impl_sqlite/src/test/absence.rs` (oder `service_impl/src/integration_test/`) bereits ein In-Memory-Pool-Helper existiert. Wenn ja → re-use. Wenn nein → adapt das Pattern aus `dao_impl_sqlite/src/test/{anderer DAO}.rs` (z. B. `extra_hours.rs` oder ähnliches).

    **Falls der existing Test-Code Struct-Literal-Konstruktionen von `AbsencePeriod` oder `AbsencePeriodEntity` enthält, die wir hier nicht erfassen, ist der gesamte `cargo test -p service_impl absence` rot.** In dem Fall: `cargo build -p service_impl --tests` macht die fehlenden Stellen sichtbar; jede einzeln mit `day_fraction: DayFraction::Full` (Service-Layer) / `DayFractionEntity::Full` (DAO-Layer) reparieren.
  </action>
  <verify>
    <automated>
      grep -c "day_fraction: (&request.day_fraction).into()" service_impl/src/absence.rs | awk '{ if ($1 >= 1) exit 0; else exit 1 }' \
      && test -f service_impl/src/test/absence_no_drift.rs \
      && grep -c "absence_period_default_day_fraction_is_full_on_legacy_rows" service_impl/src/test/absence_no_drift.rs | grep -qx 1 \
      && grep -c "absence_period_create_and_select_with_half_persists_correctly" service_impl/src/test/absence_no_drift.rs | grep -qx 1 \
      && cargo build -p service_impl --tests \
      && cargo test -p service_impl absence \
      && cargo test -p dao_impl_sqlite absence
    </automated>
  </verify>
  <done>
    Service-`update`-Pfad propagiert `day_fraction` aus dem Request. Bestehende Test-Fixtures sind aktualisiert (default `Full`) — alle existierenden `cargo test -p service_impl absence` und `cargo test -p dao_impl_sqlite absence` grün. Zwei neue Integration-Tests (`absence_no_drift`-Modul) decken legacy-Default und Half-Roundtrip ab.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAO→SQLite (Entity→TEXT) | `day_fraction_to_str` writes; DB CHECK rejects unknown strings (defense-in-depth aus Plan 01) |
| SQLite→DAO (TEXT→Entity) | `TryFrom<&AbsencePeriodDb>` mit explizitem Match + `DaoError::EnumValueNotFound`-Fallback bei korruptem Wert |
| Service→DAO (Domain→Entity) | `From`-Pair (aus Plan 01); kein neuer Boundary in diesem Plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.3-06 | T (Tampering) | `TryFrom<&AbsencePeriodDb>` bei korruptem `day_fraction`-String in DB | mitigate | Match-Arm `value => return Err(DaoError::EnumValueNotFound(value.into()))` — analog Kategorie-Pattern. Bei DB-Korruption (sollte durch CHECK-Constraint ausgeschlossen sein) propagiert ein typisierter Fehler statt Panic. |
| T-08.3-07 | T (Tampering) | `update`-Pfad nutzt `request.day_fraction` (vom User) statt `active.day_fraction` | accept | DESIGN-Entscheidung: User MUSS das Feld ändern können (sonst kein Edit-Pfad nach Plan 06). Risiko ist gleich dem von `category`/`from_date`/`to_date` — Permission-Gate ist HR ∨ self auf der Service-Methode, untrusted Input akzeptiert in der erwarteten Form. |
| T-08.3-08 | I (Information Disclosure) | Bestehende Snapshot-Reports / Reporting-Read-Paths sehen nach Plan 02 noch keine Halbtag-Logik | accept | Plan 04 löst das. Bis dahin: `day_fraction = Half` in der DB führt zu unveränderten Reports (alte Logik liest das Feld nicht). Keine Reads zwischen Plan 02 und Plan 04 in Produktion. |
| T-08.3-09 | D (DoS) | Migrations-Run auf großem `absence_period` | accept | `ALTER TABLE ADD COLUMN ... DEFAULT 'full'` in SQLite ist O(1) für `NOT NULL DEFAULT`-Spalten (SQLite materialisiert die Default-Werte beim ersten READ, nicht beim Migration-Run). Risiko praktisch null für realistische DB-Größen. |
</threat_model>

<verification>
**Phase-Plan-Level-Checks nach Task 2:**

```bash
cargo build -p dao_impl_sqlite
cargo build -p service_impl --tests
cargo test -p dao_impl_sqlite absence
cargo test -p service_impl absence
cargo check --workspace
```

**Erwartetes Verhalten:** Nach Plan 02 sollte `cargo check --workspace` für DAO+Service-Crates grün sein. Cutover-Tests (Plan 05) und Frontend (Plan 06) sind noch broken. Reporting-Hot-Path (Plan 04) hängt nicht von Plan 02 ab (Plan 04 ändert nur die Multiplikation, kein neues Feld), läuft also parallel zu Plan 02 in Wave 3 — KORREKTUR: Plan 04 wartet auf Plan 01 (Domain-Enum), nicht auf Plan 02 (DAO-Threading). Da Plan 04 das `dominant.day_fraction`-Feld an einer Read-Position liest und das Feld erst durch Plan 02-DAO-Reads gefüllt wird, ist `wave: 3` und `depends_on: [08.3-01]` korrekt für Plan 04 — der Reporting-Test in Plan 04 benutzt Mockall, das liefert `Half` ohne DB. Wenn der Test "via DB" laufen würde, bräuchte Plan 04 → Plan 02. Wir haben Plan 04 Mockall-only gehalten.
</verification>

<success_criteria>
- [ ] `AbsencePeriodDb.day_fraction: String` deklariert (dao_impl_sqlite/src/absence.rs)
- [ ] `TryFrom<&AbsencePeriodDb> for AbsencePeriodEntity` mapped `day_fraction` mit `"full"`/`"half"`/Error-Fallback
- [ ] `day_fraction_to_str(&DayFractionEntity) -> &'static str` neben `category_to_str` deklariert
- [ ] Alle 6 SELECT-Queries selecten `day_fraction`-Spalte
- [ ] `create`-INSERT schreibt `day_fraction`-Spalte und -Wert
- [ ] `update` UNVERÄNDERT (Tombstone-Pattern; per PATTERNS.md Pattern 6)
- [ ] `AbsenceServiceImpl::update` propagiert `request.day_fraction` in `new_entity` (nicht `active.day_fraction`)
- [ ] Alle bestehenden Test-Fixtures (`period_to_entity`/`fixture_*_period`/inline `AbsencePeriod{...}`/`AbsencePeriodEntity{...}`-Konstruktionen) haben `day_fraction: Full`-Default
- [ ] 2 neue Integration-Tests in `service_impl/src/test/absence_no_drift.rs`: legacy-default + half-round-trip; beide grün
- [ ] `cargo build -p dao_impl_sqlite && cargo build -p service_impl --tests` exit 0
- [ ] `cargo test -p dao_impl_sqlite absence && cargo test -p service_impl absence` exit 0
</success_criteria>

<output>
Nach Plan-Abschluss `.planning/phases/08.3-halbtag-support-f-r-absences/08.3-02-SUMMARY.md` erstellen:
- Wie viele Fixture-Stellen hieven werden mussten (für Plan 05 + 06 als Indikator)
- Welche In-Memory-SQLite-Setup-Helper für die neuen Tests reused / kopiert wurden
- Allfällige sqlx-Stolperer (offline-prepare oder live-DB-Verbindung)
- VCS-Notizen: jj-commits unter `feat(08.3-02): <subject>` und `test(08.3-02): <subject>`
</output>
