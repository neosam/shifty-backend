---
phase: 54.5-voluntary-soll-absence-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 54.5-01
files_modified:
  - Cargo.toml
  - shifty_bin/Cargo.toml
  - service/Cargo.toml
  - service_impl/Cargo.toml
  - dao/Cargo.toml
  - dao_impl_sqlite/Cargo.toml
  - rest/Cargo.toml
  - rest-types/Cargo.toml
  - shifty-utils/Cargo.toml
  - shifty-dioxus/Cargo.toml
  - default.nix
  - flake.nix
  - shifty-dioxus/default.nix
  - shifty-dioxus/flake.nix
autonomous: false
requirements:
  - VOL-STAT-01
  - VOL-ACCT-01
user_setup: []

must_haves:
  truths:
    - "Alle Backend-Crates + shifty-dioxus-Crate + alle nix-Files sind von `2.6.1-dev` auf `2.6.1` gebumpt."
    - "Ein annotierter git-Tag `v2.6.1` existiert lokal und ist zum Remote gepusht."
    - "Der Tag-Message enthaelt strukturierte Release-Notes (EN) auf Basis der jj-Commit-Log-Subjects seit `v2.6.0`."
    - "`../shifty-nix` deployment pin ist auf `v2.6.1` gebumpt, verifiziert via `nix-build`, jj-committet, gepusht, und mit `v2.6.1` getaggt."
    - "Nach dem Release sind alle Cargo.toml wieder auf einem `-dev`-Base (Skript-Default `2.6.2-dev`) — konsistent zu Praezedenz-Commit `575cf2f` `Set version to 2.6.1-dev` nach dem v2.6.0-Release."
    - "GSD-Milestone v2.6 bleibt offen (STATE.md `current_phase: 54.5` wird nach Release auf `55` bzw. den naechsten planned-Phase-Slot bewegt; das ist explizit NICHT Teil dieses Plans)."
    - "Deployment bleibt manuell — `deploy-binaries.sh` / `build-and-deploy.sh` werden NICHT ausgefuehrt (per `release-version`-Skill-Regel)."
  artifacts:
    - "Git-Tag `v2.6.1` (annotiert, mit Release-Notes)"
    - "shifty-nix Tag `v2.6.1` + gebumpter `backend-version.txt`"
    - "Alle Cargo.toml + nix-Files auf `2.6.2-dev` (Skript-Default) nach dem Release-Commit"
  key_links:
    - "Plan 54.5-01 Wrap-Commit MUSS vor diesem Plan gruen und gepusht sein — der Release-Skill snapshotet den aktuellen main-HEAD als Tag-Payload"
    - "`.claude/skills/release-version/SKILL.md` ist die kanonische Ablauf-Referenz — dieser Plan invocet den Skill, dupliziert nicht dessen Schritte"
    - "`../shifty-nix` MUSS als Sibling-Directory existieren und schreibbar sein (der Skill fasst es via `cd ../shifty-nix && ./cli-release.sh $RELEASE` an)"
---

<objective>
Hotfix v2.6.1 releasen: den in Plan 54.5-01 gelieferten Voluntary-Soll-Absence-Aware-Fix
in ein reguläres SemVer-Release verpacken. Milestone v2.6 bleibt offen, weil die
naechsten geplanten Phasen (55, 56) noch ausstehen — das ist ein Patch-Release
auf der `2.6.x`-Linie, keine Milestone-Completion.

Purpose: Der Fix aus Plan 01 wird als tagged Release verfuegbar, damit die
NixOS-Deployment-Pipeline (`../shifty-nix` → deployment) darauf pinnen kann.
Trennung Fix-Commit ↔ Release-Commit ist bewusst: der User kann den Fix
inspizieren, bevor er das Release triggert.

Output: Git-Tag `v2.6.1` auf `main` mit annotierten Release-Notes, plus
gebumpter `shifty-nix`-Pin auf denselben Tag.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54.5-voluntary-soll-absence-fix/54.5-CONTEXT.md
@.planning/phases/54.5-voluntary-soll-absence-fix/54.5-01-PLAN.md
@.claude/skills/release-version/SKILL.md
@cli-update-version.sh
@update_versions.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Release-Guard — Plan 01 Wrap-Commit auf main pruefen</name>
  <files>.planning/phases/54.5-voluntary-soll-absence-fix/54.5-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/54.5-voluntary-soll-absence-fix/54.5-01-SUMMARY.md (aus Plan 01 — sichert dass der Fix ge-committet ist)
    - Recent git-Log via `jj log -r 'main..@' --no-graph -T 'description.first_line() ++ "\n"' | head -20`
  </read_first>
  <action>
    Sanity-Guard vor dem Release-Trigger:

    1. `test -f .planning/phases/54.5-voluntary-soll-absence-fix/54.5-01-SUMMARY.md` — Plan 01 muss abgeschlossen sein.

    2. `jj log --no-graph -r 'tags(exact:"v2.6.0")..@' -T 'description.first_line() ++ "\n"' | head -30` — es MUSS mindestens ein Commit mit `fix(54.5-01)` oder `fix(54.5)` in der Subject-Line seit `v2.6.0` existieren (der Fix-Wrap-Commit aus Plan 01).

    3. `git rev-parse v2.6.1 2>/dev/null && echo "ABORT: v2.6.1 already exists" && exit 1 || echo "ok: v2.6.1 free"` — der Ziel-Tag darf NICHT bereits existieren (per Skill-Regel Schritt 2 "if the candidate tag already exists STOP").

    4. `test -d ../shifty-nix && echo "ok: shifty-nix present" || echo "ABORT: sibling ../shifty-nix missing"` — der Deployment-Pin-Repo muss als Sibling existieren.

    5. `grep -m1 '^version = ' shifty_bin/Cargo.toml` MUSS `2.6.1-dev` liefern (Ausgangs-Zustand). Falls nicht — Base-Version ist bereits verschoben und der Skill wuerde das Falsche bumpen.

    Wenn alle 5 Checks gruen: weiter zu Task 2. Wenn einer failt: STOP und dem User den Grund melden, kein Auto-Fix.
  </action>
  <verify>
    <automated>test -f .planning/phases/54.5-voluntary-soll-absence-fix/54.5-01-SUMMARY.md &amp;&amp; ! git rev-parse v2.6.1 2>/dev/null &amp;&amp; test -d ../shifty-nix &amp;&amp; grep -q '^version = "2.6.1-dev"' shifty_bin/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Plan 01 SUMMARY existiert (Fix ist ge-committet).
    - `v2.6.1`-Tag existiert noch nicht (grep-verifiziert).
    - `../shifty-nix` ist als Sibling-Directory da.
    - `shifty_bin/Cargo.toml` steht auf `2.6.1-dev` (Skript-Ausgangs-Zustand korrekt).
  </acceptance_criteria>
  <done>
    Preconditions sind alle gruen; Release-Skill darf ausgefuehrt werden.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Version-Nummer und Release-Notes vom User bestaetigen lassen</name>
  <what-built>
    Nichts Automatisiertes in diesem Checkpoint — der `release-version`-Skill ist per Definition ein deliberately-human-triggered Ablauf (SKILL.md Zeile 68-76: "one-second check is the safety net"). Der Executor darf den Skill NICHT ohne explizite User-Bestaetigung anfahren, weil er einen unwiderruflichen Tag pusht + `../shifty-nix` anfasst + jj-Commits macht.
  </what-built>
  <how-to-verify>
    1. Der User startet den Skill selbst mit `/release-version` — oder er sagt dem Executor explizit "trigger release" bzw. bestaetigt den Version-Kandidaten.
    2. Der Skill leitet die Version aus `.planning/STATE.md` `milestone: v2.6` + Git-Tags ab und zeigt den Kandidaten `v2.6.1` an.
    3. Der User bestaetigt `v2.6.1` mit Enter — oder ueberschreibt (unwahrscheinlich, da Milestone v2.6 offen und v2.6.0 bereits released ist).
    4. Der Skill zeigt die generierten Release-Notes (aus `jj log -r 'tags(exact:"v2.6.0")..@'`) — erwartet: Bug-Fixes-Section mit dem Voluntary-Soll-Absence-Fix als Kernpunkt.
    5. Der User sieht die Release-Notes an und bestaetigt oder editiert.
  </how-to-verify>
  <resume-signal>User bestaetigt `v2.6.1` als Zielversion oder gibt eine explizite Ueberschreibung an (z.B. `2.6.1` unveraendert, oder Ausnahme-Fall wie `2.6.2` falls zwischen Plan 01 Merge und Release-Trigger ein weiterer Hotfix eingeschoben wurde).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: `release-version`-Skill ausfuehren (Backend-Bump + Tag + shifty-nix-Pin)</name>
  <files>Cargo.toml, shifty_bin/Cargo.toml, service/Cargo.toml, service_impl/Cargo.toml, dao/Cargo.toml, dao_impl_sqlite/Cargo.toml, rest/Cargo.toml, rest-types/Cargo.toml, shifty-utils/Cargo.toml, shifty-dioxus/Cargo.toml, default.nix, flake.nix, shifty-dioxus/default.nix, shifty-dioxus/flake.nix</files>
  <read_first>
    - .claude/skills/release-version/SKILL.md (gesamt — der Ablauf ist dort authoritativ dokumentiert)
    - cli-update-version.sh (Skript, das der Skill aufruft — Backend-Bump + build-gate + jj-commit + tag + push)
    - update_versions.sh (Sub-Skript, das der cli-update-version.sh nutzt — bumpt alle Cargo.toml + nix-Files)
    - ../shifty-nix/cli-release.sh (falls einsehbar — bumpt den Deployment-Pin)
  </read_first>
  <action>
    Der Executor invoked den `release-version`-Skill via Skill-Tool. Der Skill fuehrt intern die folgenden Schritte aus (aus SKILL.md; hier nur als Referenz — die Kanone bleibt der Skill selbst):

    Schritt 1-3 (Version-Derivation + Confirm): schon durch Task 1 + Checkpoint (Task 2) erledigt. Zielversion = `2.6.1`.

    Schritt 4 (Release-Notes generieren): Skill lauft `jj log -r 'tags(exact:"v2.6.0")..@' --no-graph -T 'description.first_line() ++ "\n"'` und kategorisiert die Commit-Subjects. Erwartete Notes (EN, denn Tag-Message ist public artifact):

        Bug Fixes:
        - voluntary_stats Soll now applies whole-week-out when the salesperson has an absence overlapping the ISO week (previously overstated Soll and understated the Ist/Soll delta; symmetric to the weekly display since v2.6.0)
        - contract_weeks_count_in_range excludes absence weeks from the F1 denominator (Ø ist pro Vertragswoche is now measured against available voluntary weeks)

        (Falls jj weitere Non-Chore-Commits seit v2.6.0 findet — z.B. STATE-Bookkeeping ausklammern per Skill-Regel "Skip pure planning/docs/chore churn".)

    Schritt 5 (Release-Skript): der Skill fuehrt `./cli-update-version.sh -m "<release notes>" "2.6.1"` aus der Backend-Root aus. Das Skript:
      - bumpt alle Cargo.toml + nix-Files von `2.6.1-dev` auf `2.6.1` (via `update_versions.sh 2.6.1`);
      - laeuft build-gate: `cargo build` (Backend) UND `cd shifty-dioxus && cargo build --target wasm32-unknown-unknown` (FE);
      - jj-committet den Bump, bewegt `main`, pusht;
      - erzeugt annotierten Tag `v2.6.1` mit Release-Notes, pusht Tags;
      - bumpt danach auf `2.6.2-dev` (Skript-Default), jj-committet nochmal, pusht.

    Schritt 6 (shifty-nix-Pin): der Skill fuehrt `cd ../shifty-nix && ./cli-release.sh 2.6.1` aus. Das rendert die Nix-Templates, verifiziert via `nix-build` dass der Backend-Tag von GitHub geholt werden kann, jj-committet, pusht `main` (NUR main — stray bookmarks nicht pushen), taggt `v2.6.1`, pusht Tags.

    Schritt 7 (Report): der Skill meldet `New release version: 2.6.1` und bestaetigt dass der shifty-nix-Pin gebumpt wurde. Deployment bleibt manuell — der Executor fuehrt NICHT `deploy-binaries.sh` / `build-and-deploy.sh` aus.

    Der Executor MUSS im backend nix dev shell laufen (`nix develop`) — das ist die einzige Umgebung, in der `cargo build --target wasm32-unknown-unknown` gruen ist (siehe SKILL.md Zeile 134-137 + MEMORY `reference_local_dev_commands`). Falls der Executor NICHT in nix develop laeuft: das Skript scheitert am WASM-Build und ruft dann den falschen Recovery-Path aus — daher: erst `nix develop` sicherstellen, sonst STOP.

    Fehler-Handling waehrend des Skript-Laufs:
    - Backend-Build failed → Fix im naechsten Commit vor Release-Trigger; keine partielle Bumps zurueckgesetzt (jj-Log dokumentiert).
    - WASM-Build failed → typisch: falsche Shell (nicht nix develop). Recovery: In `nix develop` neu starten.
    - `nix-build` (shifty-nix) 404s → Tag-Propagation zu GitHub braucht kurz; einmal wiederholen (SKILL.md Zeile 158-161).
    - jj-Push-Konflikt → main hat sich seit letztem Fetch bewegt; `jj git fetch` + Bookmark rebasen + retry.
  </action>
  <verify>
    <automated>git rev-parse v2.6.1 &amp;&amp; grep -m1 '^version = ' shifty_bin/Cargo.toml | grep -E '2\.6\.[2-9]-dev' &amp;&amp; test -f ../shifty-nix/backend-version.txt &amp;&amp; grep -q '2.6.1' ../shifty-nix/backend-version.txt</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse v2.6.1` liefert eine gueltige commit-sha (Tag existiert lokal).
    - `git ls-remote --tags origin v2.6.1` liefert eine Zeile (Tag ist zum Remote gepusht).
    - `shifty_bin/Cargo.toml` steht nach dem Release auf `2.6.2-dev` (Skript-Default-Post-Bump).
    - `../shifty-nix/backend-version.txt` enthaelt `2.6.1`.
    - `../shifty-nix` hat einen jj-Commit `2.6.1` und einen gepushten Tag `v2.6.1`.
    - `deploy-binaries.sh` / `build-and-deploy.sh` wurden NICHT ausgefuehrt (Deployment bleibt manuell — dokumentiert im SUMMARY).
  </acceptance_criteria>
  <done>
    v2.6.1 ist als annotierter Tag auf main gepusht; shifty-nix-Pin verifiziert und getaggt. Deployment liegt beim User.
  </done>
</task>

<task type="auto">
  <name>Task 4: Post-Release-Bookkeeping in STATE.md dokumentieren</name>
  <files>.planning/STATE.md, .planning/phases/54.5-voluntary-soll-absence-fix/54.5-02-SUMMARY.md</files>
  <read_first>
    - .planning/STATE.md (current_phase, milestone, progress-Tabelle)
    - .planning/ROADMAP.md Phasen 54.5-Sektion (ist die Phase im ROADMAP-Header korrekt reflektiert?)
    - MEMORY `feedback_phase_complete_roadmap_drift` — bei Dezimal-Phasen (8.x / 54.5) fixt gsd-sdk `phase.complete` weder ROADMAP-Header-Checkbox noch Progress-Tabelle; manueller Nachzug noetig.
    - MEMORY `feedback_state_record_session_corrupts` — SDK `state.record-session` zerschiesst STATE.md; NICHT verwenden.
  </read_first>
  <action>
    Manuelles Bookkeeping nach dem Release. STATE.md wird DIREKT via Edit-Tool aktualisiert — nicht via SDK-Kommando (Risiko-MEMORY).

    1. **STATE.md aktualisieren** — folgende Felder im YAML-Frontmatter:
       - `current_phase: "55"` (naechste geplante Phase, siehe ROADMAP.md v2.6-Sektion).
       - `current_phase_name: <naechster Phase-Name aus ROADMAP.md>` (aktuell "Manuelle Umbuchung + HR-Alert-Modal (F3 + F5)").
       - `status: planning` bzw. `discuss_phase` — je nach v2.6-Fortschritt.
       - `stopped_at: v2.6.1 released — Voluntary-Soll Absence-Aware-Fix ge-shippt.`
       - `last_updated: <aktuelles ISO-Datum>`.
       - `last_activity: <aktuelles Datum>`.
       - `last_activity_desc: v2.6.1 Hotfix ge-shippt — voluntary_stats Soll now whole-week-out on absence weeks; shifty-nix pinned to v2.6.1.`

    2. **Progress-Tabelle**: `completed_phases` um 1 hochziehen (54.5 zaehlt als eine complete Phase im v2.6-Zaehler); `completed_plans` um 2 hochziehen (54.5-01 + 54.5-02).

    3. **Shipped-Milestones-Sektion NICHT anfassen** — v2.6 ist noch offen. Der v2.6.1-Release bekommt keinen eigenen Milestone-Eintrag; er wird beim v2.6-Close als Zwischen-Release erwaehnt.

    4. **ROADMAP.md-Header**: falls die Phase-54.5-Checkbox in der Progress-Tabelle noch offen ist, per Edit auf `[x]` setzen (MEMORY `feedback_phase_complete_roadmap_drift` — Dezimal-Phasen erfordern manuellen Nachzug).

    5. **Kein Milestone-Close** — dieser Release ist Zwischen-Patch, nicht Milestone-Ship. `/gsd-complete-milestone` NICHT ausloesen.

    6. **Plan-SUMMARY schreiben** (`54.5-02-SUMMARY.md`) mit:
       - Zielversion: `2.6.1`.
       - Tag-Message (die tatsaechlich verwendeten Release-Notes).
       - Confirm-Commit-Hashes (jj-Commit-IDs) fuer Backend-Release + shifty-nix-Pin.
       - Bestaetigung: `../shifty-nix/backend-version.txt = 2.6.1`.
       - Bestaetigung: Deployment MANUELL, nicht ausgefuehrt.
       - Ausblick: Milestone v2.6 bleibt offen; naechste Phase = 55.
  </action>
  <verify>
    <automated>grep -q 'current_phase: "55"' .planning/STATE.md &amp;&amp; grep -q 'v2.6.1' .planning/STATE.md &amp;&amp; test -f .planning/phases/54.5-voluntary-soll-absence-fix/54.5-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` `current_phase` steht auf `"55"` (oder was in ROADMAP als naechster planned-Slot steht).
    - `.planning/STATE.md` erwaehnt `v2.6.1` als geshippte Zwischen-Release im `stopped_at` / `last_activity_desc`-Feld.
    - `54.5-02-SUMMARY.md` dokumentiert Tag-Message + shifty-nix-Pin-Bestaetigung.
    - Milestone v2.6 bleibt offen (kein `/gsd-complete-milestone`-Aufruf).
    - Deployment NICHT ausgefuehrt (dokumentiert im SUMMARY).
  </acceptance_criteria>
  <done>
    Bookkeeping ist sauber; das Projekt ist bereit fuer die naechste Phase (55).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local jj → GitHub Remote (git push) | Der Release pusht einen unwiderruflichen annotierten Tag `v2.6.1`. Falscher Commit-Snapshot → falsches Release. Mitigation: Preconditions-Check (Task 1) + explizites User-Confirm (Task 2). |
| Backend-Tag `v2.6.1` → shifty-nix `gen-backend.sh` | `gen-backend.sh` fetcht den Tarball von GitHub. Wenn der Tag falsch propagiert oder falscher SHA, wird der falsche Backend gepinnt. Skill-eigene Retry-Logik (SKILL.md Zeile 158-161) fangt Propagations-Verzoegerungen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54.5-02-01 | Repudiation | Git-Tag `v2.6.1` | high | mitigate | Annotierter Tag mit signierten Release-Notes (Skill Schritt 5). Tag-Message dokumentiert genau, was drin ist. Preconditions-Check (Task 1) verhindert doppelten Tag-Overwrite. |
| T-54.5-02-02 | Denial of Service | shifty-nix-Pin | low | accept | Falls `nix-build`-Verify in shifty-nix failt, ist der Deployment-Pin nicht gebumpt — v2.6.0 bleibt aktiv (kein Datenverlust, kein User-Impact). Manueller Retry moeglich. |
| T-54.5-02-03 | Elevation of Privilege | jj Push Konflikt | low | mitigate | Skill nutzt eigene Bookmark-Bewegung; falls Konflikt auftritt, jj meldet und Skill bricht ab (kein Force-Push). |
| T-54.5-02-SC | Tampering | npm/pip/cargo installs | n/a | n/a | Keine neuen Installs; nur Version-Bumps in bestehender Cargo.toml + nix. |
</threat_model>

<verification>
1. `git rev-parse v2.6.1` gibt eine Commit-SHA aus (Tag lokal).
2. `git ls-remote --tags origin v2.6.1` gibt eine Zeile aus (Tag remote).
3. `grep -m1 '^version' shifty_bin/Cargo.toml` zeigt `2.6.2-dev` (Post-Release-Bump).
4. `cat ../shifty-nix/backend-version.txt` zeigt `2.6.1`.
5. `git -C ../shifty-nix rev-parse v2.6.1` gibt eine Commit-SHA aus (shifty-nix-Tag).
6. `.planning/STATE.md` `current_phase: "55"` (oder passender naechster Slot).
7. Deployment NICHT ausgefuehrt — `deploy-binaries.sh` wurde nicht angefasst (Skill-Regel).
</verification>

<success_criteria>
- v2.6.1 als annotierter Tag auf main gepusht.
- shifty-nix-Pin auf v2.6.1 gebumpt + verifiziert + getaggt.
- STATE.md sauber auf Phase 55 gestellt.
- Milestone v2.6 bleibt offen.
- Deployment liegt beim User (manuell).
- Trennung Fix-Commit (Plan 01) ↔ Release-Commit (Plan 02) ist in der Git-History klar erkennbar.
</success_criteria>

<output>
Create `.planning/phases/54.5-voluntary-soll-absence-fix/54.5-02-SUMMARY.md` when done.

Dokumentiere im Summary:
- Finale Zielversion (`2.6.1`) und der User-Confirm-Kontext (Enter bestaetigt / andere Nummer geliefert).
- Die tatsaechlich verwendete Release-Notes (kopierbar 1:1 aus dem Tag-Message-Payload).
- jj-Commit-IDs fuer den Backend-Bump-Commit + den Post-Release-Dev-Bump-Commit.
- jj-Commit-ID fuer den shifty-nix-Pin-Commit.
- Bestaetigung dass `nix-build` in shifty-nix gruen war.
- Bestaetigung: Deployment NICHT ausgefuehrt (per Skill-Regel + user-owned).
- STATE.md-Diff (current_phase alt vs neu).
</output>
