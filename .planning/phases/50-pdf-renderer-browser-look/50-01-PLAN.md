---
phase: 50-pdf-renderer-browser-look
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - service_impl/Cargo.toml
  - service_impl/src/pdf_render.rs
autonomous: true
requirements: [PDF-01, PDF-02]

must_haves:
  truths:
    - "D-50-14: FIXED_RENDER_TIMESTAMP = time::macros::datetime!(2026-07-03 17:15 UTC) existiert als Test-Konstante in service_impl/src/pdf_render.rs `#[cfg(test)] mod test`"
    - "D-50-13: Der Test deterministic_bytes_for_same_input ist entfernt (Byte-Determinismus-Vertrag aufgehoben)"
    - "D-50-15: Der Test sales_persons_sorted_by_id ist entfernt (globale Sortier-Logik obsolet)"
    - "D-50-15: Der Test build_sales_person_row_lists_bookings_time_ranges ist entfernt (Row-Layout weg)"
    - "D-50-16: Sieben neue Renderer-Tests existieren als `#[ignore]`-Skelette: render_includes_timestamp_string, slot_boxes_sorted_by_start_time, names_within_slot_alphabetical, unpaid_marker_suffix, sunday_column_hidden_when_no_sunday_slots, sunday_column_shown_when_at_least_one_sunday_slot"
    - "D-50-07: `make_sales_person`-Fixture akzeptiert `is_paid: Option<bool>` als Parameter (für Freiwilligen-Suffix-Test)"
    - "Pitfall 1 / RESEARCH §Standard Stack: `service_impl/Cargo.toml` hat `time`-Feature `local-offset` aktiviert"
    - "`cargo build -p service_impl` grün nach Wave 1 (kompilierbar, auch mit ignorierten Tests)"
    - "`cargo clippy --workspace -- -D warnings` grün nach Wave 1"
  artifacts:
    - "service_impl/Cargo.toml (features-Zeile für time-crate erweitert)"
    - "service_impl/src/pdf_render.rs (mod test: neue Konstante + 7 ignorierte Skeleton-Tests + Fixture-Signatur-Änderung + 3 gestrichene Tests)"
  key_links:
    - "Wave 2 (50-02) macht diese ignorierten Tests grün — Ignore-Marker werden dort entfernt"
    - "Wave 3 (50-03) fügt separaten Service-Test `now_local_fallback_to_utc_on_indeterminate_offset` an dort neu angelegte Fixture-Location an"
  prohibitions:
    - "KEIN Löschen der Portierungs-Tests aus D-50-15 (empty_week_yields_valid_pdf_signature, header_contains_year_and_week, all_active_sales_persons_appear, build_page_header_produces_expected_text, build_day_column_headers_yields_seven_short_labels, normalize_pdf_id_removes_variable_id_array, find_all_subsequences_locates_multiple_occurrences)"
    - "KEINE neue Cargo-Dep (nur Feature-Aktivierung auf bestehendem `time`-Dep)"
    - "KEIN `todo!()` oder `unimplemented!()` in den ignorierten Test-Bodies — echte Assertion-Skelette schreiben (siehe RESEARCH §Code Examples E)"
    - "KEIN Ändern des Renderer-Bodys in Plan 01 — nur Test-Modul + Cargo-Feature"
---

<objective>
Wave-0-TDD-Vorbereitung für den kompletten Renderer-Rewrite in Wave 2.

Führt die drei minimal-invasiven Voraussetzungen für den TDD-Cycle ein:

1. Cargo-Feature `local-offset` auf dem `time`-Dep in `service_impl/Cargo.toml` (RESEARCH §Standard Stack, Pitfall 1). Ohne diese Feature-Aktivierung kompiliert `OffsetDateTime::now_local()` in Plan 03 nicht.
2. Fixed-Timestamp-Konstante `FIXED_RENDER_TIMESTAMP: OffsetDateTime = time::macros::datetime!(2026-07-03 17:15 UTC)` als Test-Konstante in `#[cfg(test)] mod test` von `service_impl/src/pdf_render.rs` (D-50-14).
3. Sieben neue Renderer-Unit-Tests aus D-50-16 als `#[ignore]`-Skelette (echte Assertion-Struktur, nicht `todo!()`). Ignore-Marker werden in Plan 02 entfernt, sobald der neue Renderer die Behavior erfüllt.
4. Fixture-Extension: `make_sales_person(name, is_paid)`-Signatur (D-50-07: `is_paid: Option<bool>`).
5. Streichen der drei obsoleten Tests: `deterministic_bytes_for_same_input`, `sales_persons_sorted_by_id`, `build_sales_person_row_lists_bookings_time_ranges` (D-50-13, D-50-15).

Purpose: Etabliert den RED-State für den TDD-Zyklus. Wave 2 macht die 7 ignorierten Tests grün, indem sie den alten Renderer komplett ersetzt. Die Portierungs-Tests aus D-50-15 (die bleiben) laufen weiterhin nach Wave 1 und Wave 2 grün, mit unverändertem `render_shiftplan_week_pdf`-Aufruf jetzt mit dem zusätzlichen Timestamp-Parameter erst nach Wave 2 (Signaturerweiterung).

Wichtig: Da die Renderer-Signatur in Wave 2 um den Timestamp-Parameter erweitert wird (D-50-11), müssen ALLE bestehenden Test-Call-Sites in `mod test` bereits jetzt in einer Weise strukturiert werden, dass sie den `FIXED_RENDER_TIMESTAMP` verwenden — konkret: die neuen `#[ignore]`-Tests rufen den Renderer bereits mit 5 Parametern auf (kompiliert erst nach Wave 2). Ergo müssen die neuen Test-Skelette in Plan 01 als `#[cfg(feature = "not-yet-implemented")]` oder analog gegen Compile geschützt sein — Alternative: da diese Tests `#[ignore]` sind, dürfen sie kompilieren nur wenn sie sich auf die noch-nicht-existierende Renderer-Signatur beziehen, was in Plan 01 die Kompilierung bricht.

Daher: in den `#[ignore]`-Skeletten wird der Renderer mit der aktuellen 4-Parameter-Signatur aufgerufen (kompatibel mit dem alten Renderer), UND ein `let _ = FIXED_RENDER_TIMESTAMP;`-Statement stellt sicher, dass die Konstante referenziert und der `dead_code`-Lint nicht triggert. Assertions werden gegen ein Fixed-Ergebnis geschrieben, das mit dem alten Renderer nicht grün ist (daher `#[ignore]`). In Plan 02 werden dann alle Test-Call-Sites (portierte + neue) auf 5-Parameter umgestellt.

Output: `service_impl/Cargo.toml` mit `local-offset`-Feature, `service_impl/src/pdf_render.rs` mit `#[cfg(test)] mod test` erweitert um Konstante, angepasste Fixture, 7 ignorierte Test-Skelette; drei obsolete Tests entfernt; `cargo build -p service_impl` + `cargo clippy --workspace -- -D warnings` grün.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/50-pdf-renderer-browser-look/50-CONTEXT.md
@.planning/phases/50-pdf-renderer-browser-look/50-RESEARCH.md
@.planning/phases/50-pdf-renderer-browser-look/50-VALIDATION.md

@service_impl/src/pdf_render.rs
@service_impl/Cargo.toml
</context>

<artifacts_this_phase_produces>
- Feature-Flag: `local-offset` in `service_impl/Cargo.toml` unter `[dependencies.time].features`
- Test-Konstante: `FIXED_RENDER_TIMESTAMP: time::OffsetDateTime = time::macros::datetime!(2026-07-03 17:15 UTC)` in `service_impl/src/pdf_render.rs` `#[cfg(test)] mod test`
- Fixture-Signatur: `make_sales_person(id_hex: u128, name: &str, is_paid: Option<bool>) -> SalesPerson`
- 7 neue `#[ignore]`-Test-Funktionen (RED-State):
  - `render_includes_timestamp_string`
  - `slot_boxes_sorted_by_start_time`
  - `names_within_slot_alphabetical`
  - `unpaid_marker_suffix`
  - `sunday_column_hidden_when_no_sunday_slots`
  - `sunday_column_shown_when_at_least_one_sunday_slot`
- Gestrichene Tests: `deterministic_bytes_for_same_input`, `sales_persons_sorted_by_id`, `build_sales_person_row_lists_bookings_time_ranges`
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Cargo-Feature aktivieren + `make_sales_person`-Fixture erweitern</name>
  <files>service_impl/Cargo.toml, service_impl/src/pdf_render.rs</files>
  <read_first>
    - service_impl/Cargo.toml (aktuelle `[dependencies.time]`-Sektion, Zeilen 47-49)
    - service_impl/src/pdf_render.rs Zeilen 259-269 (aktuelle `make_sales_person`-Fixture)
    - .planning/phases/50-pdf-renderer-browser-look/50-RESEARCH.md §"Standard Stack" (Feature-Aktivierung) + §"Common Pitfalls" Pitfall 1
    - .planning/phases/50-pdf-renderer-browser-look/50-CONTEXT.md §"D-50-07" + §"D-50-11" + §"D-50-14"
  </read_first>
  <behavior>
    - Test 1: `cargo build -p service_impl` kompiliert nach Feature-Aktivierung ohne Fehler.
    - Test 2: Alle bestehenden Aufrufer von `make_sales_person(id_hex, name)` in `mod test` sind auf `make_sales_person(id_hex, name, Some(true))` umgestellt (backward-kompatibles Default-Verhalten).
    - Test 3: `FIXED_RENDER_TIMESTAMP: time::OffsetDateTime` als `const` in `mod test` deklariert und referenzierbar (kompiliert nicht mit `dead_code`-Warning, weil in Wave-2-Tests genutzt).
  </behavior>
  <action>
    Schritt A — `service_impl/Cargo.toml`:
    Erweitere `[dependencies.time]` `features`-Liste um `"local-offset"`. Nach der Änderung MUSS die Zeile lauten: `features = ["std", "formatting", "macros", "local-offset"]`. Grund: `OffsetDateTime::now_local()` in Plan 03 (D-50-12, Aufrufer-Verantwortung) ist per `#[cfg(feature = "local-offset")]` geblockt (RESEARCH §"Common Pitfalls" Pitfall 1).

    Schritt B — `service_impl/src/pdf_render.rs` `#[cfg(test)] mod test`:
    1. Direkt unter `use uuid::Uuid;` in `mod test`, füge die Test-Konstante ein (D-50-14):
       Signature: `const FIXED_RENDER_TIMESTAMP: time::OffsetDateTime = time::macros::datetime!(2026-07-03 17:15 UTC);`
       Doc-Comment: `/// D-50-14: Fixed-Timestamp fixture for all renderer tests (2026-07-03 17:15 UTC).`
    2. Erweitere `make_sales_person`-Signatur von `fn make_sales_person(id_hex: u128, name: &str) -> SalesPerson` zu:
       `fn make_sales_person(id_hex: u128, name: &str, is_paid: Option<bool>) -> SalesPerson`
       Im Body: setze `is_paid` als übergebenen Parameter (statt hart `Some(true)`). Alle anderen Felder unverändert.
    3. Passe ALLE bestehenden `make_sales_person(...)`-Aufrufe in `mod test` an: dritter Argument `Some(true)` (backward-compat für die Portierungs-Tests). Konkret: in `all_active_sales_persons_appear` (Alice/Bob/Charlie), in `sales_persons_sorted_by_id` (falls noch da vor Schritt C), in `build_sales_person_row_lists_bookings_time_ranges` (falls noch da vor Schritt C).

    Schritt C — Nichts anderes ändern. Keine neuen Tests hier, keine gestrichenen Tests hier. Das ist Task 2/3.

    Wichtig: nach Task 1 muss `cargo build -p service_impl` UND `cargo test -p service_impl pdf_render` grün sein (alle bestehenden Tests kompilieren + laufen weiter — kein Behavior-Change am Renderer).
  </action>
  <acceptance_criteria>
    - `cargo build -p service_impl` exit code 0
    - `cargo test -p service_impl pdf_render --lib` läuft ohne Compile-Fehler (Tests grün wie zuvor)
    - `grep -E '^\s*features\s*=\s*\[.*local-offset' service_impl/Cargo.toml` findet mindestens 1 Match
    - `grep -c 'const FIXED_RENDER_TIMESTAMP' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn make_sales_person(id_hex: u128, name: &str, is_paid: Option<bool>)' service_impl/src/pdf_render.rs` = 1
    - `cargo clippy --workspace -- -D warnings` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build -p service_impl 2>&1 && cargo test -p service_impl pdf_render --lib 2>&1 | tail -20 && cargo clippy --workspace -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    Feature `local-offset` in `service_impl/Cargo.toml` aktiviert; `FIXED_RENDER_TIMESTAMP`-Konstante in `mod test` deklariert; `make_sales_person`-Signatur um `is_paid: Option<bool>`-Parameter erweitert; alle bestehenden Aufrufer angepasst; `cargo build -p service_impl` + `cargo test -p service_impl pdf_render` + `cargo clippy --workspace -- -D warnings` grün.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Drei obsolete Tests entfernen (D-50-13, D-50-15)</name>
  <files>service_impl/src/pdf_render.rs</files>
  <read_first>
    - service_impl/src/pdf_render.rs Zeilen 428-493 (`deterministic_bytes_for_same_input` + `sales_persons_sorted_by_id`)
    - service_impl/src/pdf_render.rs Zeilen 511-529 (`build_sales_person_row_lists_bookings_time_ranges`)
    - .planning/phases/50-pdf-renderer-browser-look/50-CONTEXT.md §"D-50-13" (Byte-Determinismus aufgehoben) + §"D-50-15" (Test-Portierung)
    - .planning/phases/50-pdf-renderer-browser-look/50-RESEARCH.md §"State of the Art" (Deprecated Tests) + §"Anti-Patterns to Avoid" (grep-Verifikation kein externer Konsument)
  </read_first>
  <behavior>
    - Test 1: `grep -rn "deterministic_bytes_for_same_input" service_impl/ rest/ shifty_bin/` findet keinen Treffer außer .planning-Docs.
    - Test 2: Die verbleibenden Portierungs-Tests aus D-50-15 (`empty_week_yields_valid_pdf_signature`, `header_contains_year_and_week`, `all_active_sales_persons_appear`, `build_page_header_produces_expected_text`, `build_day_column_headers_yields_seven_short_labels`, `normalize_pdf_id_removes_variable_id_array`, `find_all_subsequences_locates_multiple_occurrences`) sind weiterhin vorhanden und laufen grün.
    - Test 3: `cargo test -p service_impl pdf_render --lib` passt (weniger Tests als vorher, alle grün).
  </behavior>
  <action>
    Lösche folgende drei Test-Funktionen komplett (mit ihrem Doc-Kommentar-Block darüber) aus `service_impl/src/pdf_render.rs` `#[cfg(test)] mod test`:

    1. `deterministic_bytes_for_same_input` (Zeilen ~426-466) — Grund: Byte-Determinismus-Vertrag aus v2.2 aufgehoben (D-50-13), Timestamp bricht ihn ohnehin.
    2. `sales_persons_sorted_by_id` (Zeilen ~468-493) — Grund: Globale Sortier-Reihenfolge nach `sp.id` ist obsolet (D-50-06: Namen alphabetisch **innerhalb** einer Slot-Box). Wird in Wave 2 durch `names_within_slot_alphabetical` ersetzt.
    3. `build_sales_person_row_lists_bookings_time_ranges` (Zeilen ~511-529) — Grund: Das v2.2-Row-Layout (Namen-Spalte links + eine Zelle je Tag mit `HH:MM-HH:MM`) ist weg (D-50-15). Wird in Wave 2 durch Slot-Box-zentriertes Layout ersetzt.

    NICHT löschen:
    - `empty_week_yields_valid_pdf_signature`
    - `header_contains_year_and_week`
    - `all_active_sales_persons_appear` (muss in Wave 2 angepasst werden, aber Test-Body bleibt inhaltlich derselbe: prüft dass alle drei Namen hex-encoded im Textstream vorkommen)
    - `build_page_header_produces_expected_text`
    - `build_day_column_headers_yields_seven_short_labels`
    - `normalize_pdf_id_removes_variable_id_array`
    - `find_all_subsequences_locates_multiple_occurrences`

    Wichtig: NICHT die Test-Helper-Funktionen entfernen (`normalize_pdf_id`, `find_subsequence`, `find_all_subsequences`, `encode_ascii_to_pdf_hex`) — die werden in Wave 2 weiterhin gebraucht (D-50-15).

    Nach dem Löschen: `cargo test -p service_impl pdf_render --lib` MUSS grün laufen mit den verbleibenden Tests.
  </action>
  <acceptance_criteria>
    - `grep -c 'fn deterministic_bytes_for_same_input' service_impl/src/pdf_render.rs` = 0
    - `grep -c 'fn sales_persons_sorted_by_id' service_impl/src/pdf_render.rs` = 0
    - `grep -c 'fn build_sales_person_row_lists_bookings_time_ranges' service_impl/src/pdf_render.rs` = 0
    - `grep -c 'fn empty_week_yields_valid_pdf_signature' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn header_contains_year_and_week' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn all_active_sales_persons_appear' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn build_page_header_produces_expected_text' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn build_day_column_headers_yields_seven_short_labels' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn normalize_pdf_id_removes_variable_id_array' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn find_all_subsequences_locates_multiple_occurrences' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn normalize_pdf_id' service_impl/src/pdf_render.rs` = 2 (helper + test)
    - `cargo test -p service_impl pdf_render --lib` exit code 0
    - `cargo clippy --workspace -- -D warnings` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && ! grep -q 'fn deterministic_bytes_for_same_input\|fn sales_persons_sorted_by_id\|fn build_sales_person_row_lists_bookings_time_ranges' service_impl/src/pdf_render.rs && cargo test -p service_impl pdf_render --lib 2>&1 | tail -10 && cargo clippy --workspace -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    Drei obsolete Tests entfernt; sieben Portierungs-Tests aus D-50-15 verbleiben und laufen grün; Test-Helper (`normalize_pdf_id`, `find_subsequence`, `find_all_subsequences`, `encode_ascii_to_pdf_hex`) unverändert; `cargo test -p service_impl pdf_render --lib` + `cargo clippy --workspace -- -D warnings` grün.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Sechs neue ignorierte Renderer-Test-Skelette anlegen (D-50-16)</name>
  <files>service_impl/src/pdf_render.rs</files>
  <read_first>
    - service_impl/src/pdf_render.rs `mod test` (nach Task 2 sauber, aktuelle Fixtures + Helpers)
    - .planning/phases/50-pdf-renderer-browser-look/50-CONTEXT.md §"D-50-16" (Neue Renderer-Tests)
    - .planning/phases/50-pdf-renderer-browser-look/50-RESEARCH.md §"Code Examples" Beispiel E (Test-Fixture für Fixed-Timestamp) + Beispiel B (Namens-Sortierung mit Freiwilligen-Suffix) + Beispiel A (Slot-Box mit Zeit-Label + Namen)
    - .planning/phases/50-pdf-renderer-browser-look/50-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <behavior>
    - Test 1: Alle sechs neuen Test-Funktionen kompilieren.
    - Test 2: Alle sechs neuen Test-Funktionen haben `#[ignore = "Wave 2: erst nach Renderer-Rewrite grün — siehe 50-02-PLAN.md"]` als Attribute.
    - Test 3: Die sechs Tests rufen den bestehenden Renderer mit der ALTEN 4-Parameter-Signatur auf (`render_shiftplan_week_pdf(week, sales_persons, year, week)`) — sie kompilieren jetzt (Wave 1), scheitern beim Ausführen (RED, aber `#[ignore]` läuft nicht).
    - Test 4: Jeder Test enthält `let _ = FIXED_RENDER_TIMESTAMP;` (silencing `dead_code`-Lint auf der Konstante bis Wave 2 sie via 5-Parameter-Aufruf konsumiert).
    - Test 5: Jeder Test hat konkrete `assert!`/`assert_eq!` gegen ein Kriterium aus D-50-16 (nicht `todo!()`, nicht `unimplemented!()`).
  </behavior>
  <action>
    Füge folgende SECHS neue Test-Funktionen als `#[ignore]`-Skelette an das Ende von `#[cfg(test)] mod test` in `service_impl/src/pdf_render.rs` (nach den bestehenden Tests, vor der schließenden `}` des Moduls):

    Jeder Test bekommt das Attribut:
    `#[ignore = "Wave 2: erst nach Renderer-Rewrite grün — siehe 50-02-PLAN.md"]`
    gefolgt von `#[test]`.

    Jeder Test-Body ruft `render_shiftplan_week_pdf(&week, &sales_persons, year, week)` mit der aktuellen 4-Parameter-Signatur auf (Wave-1-kompatibel). Enthält zusätzlich `let _ = FIXED_RENDER_TIMESTAMP;` als Silencing-Statement für den `dead_code`-Lint auf der Konstante. Das echte Verwenden der Konstante als 5. Parameter erfolgt in Wave 2.

    1. **`render_includes_timestamp_string`** (D-50-16, PDF-02):
       Fixture: `empty_week(2026, 27)`, keine SalesPersons.
       Renderer-Call: 4-Parameter (Wave 1).
       Assertion: `let hex = encode_ascii_to_pdf_hex("Erstellt am 03.07.2026 17:15 Uhr"); assert!(find_subsequence(&bytes, hex.as_bytes()).is_some(), "timestamp string not found in PDF (hex: {})", hex);`
       Grund für `#[ignore]`: Der alte Renderer emittiert diesen Timestamp NICHT — daher scheitert der Assert. Wave 2 macht ihn grün.

    2. **`slot_boxes_sorted_by_start_time`** (D-50-16, D-50-02, PDF-01):
       Fixture: `empty_week(2026, 27)`, zwei Slots am Montag: einer 12:00-16:00, einer 08:00-11:00 (Reihenfolge im Vec: erst der spätere, dann der frühere — testet die Sortierung im Renderer).
       Renderer-Call: 4-Parameter (Wave 1).
       Assertion: `let idx_early = find_subsequence(&bytes, encode_ascii_to_pdf_hex("08:00").as_bytes()); let idx_late = find_subsequence(&bytes, encode_ascii_to_pdf_hex("12:00").as_bytes()); assert!(idx_early.is_some() && idx_late.is_some(), "both time labels must be present"); assert!(idx_early < idx_late, "08:00 must appear before 12:00 in the textstream (sort order D-50-02)");`
       Grund für `#[ignore]`: Der alte Renderer hat kein Zeitlabel-Konzept — scheitert an `is_some()`. Wave 2 emittiert Zeitlabels und sortiert.

    3. **`names_within_slot_alphabetical`** (D-50-16, D-50-06, PDF-01):
       Fixture: Ein Slot am Montag (08:00-12:00), drei Bookings mit Sales-Persons namens "Charlie", "Alice", "Bob" (Reihenfolge im Bookings-Vec: Charlie, Alice, Bob — testet alphabetische Sortierung im Renderer).
       Renderer-Call: 4-Parameter (Wave 1).
       Assertion: `let idx_alice = find_subsequence(&bytes, encode_ascii_to_pdf_hex("Alice").as_bytes()); let idx_bob = find_subsequence(&bytes, encode_ascii_to_pdf_hex("Bob").as_bytes()); let idx_charlie = find_subsequence(&bytes, encode_ascii_to_pdf_hex("Charlie").as_bytes()); assert!(idx_alice.is_some() && idx_bob.is_some() && idx_charlie.is_some(), "all names must appear"); assert!(idx_alice < idx_bob && idx_bob < idx_charlie, "names must be alphabetical case-insensitive within slot box (D-50-06)");`
       Grund für `#[ignore]`: Der alte Renderer sortiert Namen global nach `sp.id`, nicht alphabetisch innerhalb Slot. Wave 2 dreht das um.

    4. **`unpaid_marker_suffix`** (D-50-16, D-50-07, PDF-01):
       Fixture: Ein Slot am Montag mit einem einzigen Booking von einer Sales-Person, angelegt mit `make_sales_person(0x…0001, "Volunteer", Some(false))` (Freiwillige).
       Renderer-Call: 4-Parameter (Wave 1).
       Assertion: `let expected_hex = encode_ascii_to_pdf_hex("Volunteer (freiwillig)"); assert!(find_subsequence(&bytes, expected_hex.as_bytes()).is_some(), "unpaid marker suffix ' (freiwillig)' must appear after volunteer name (D-50-07)");`
       Grund für `#[ignore]`: Alter Renderer kennt keinen Suffix — scheitert. Wave 2 fügt Suffix hinzu.

    5. **`sunday_column_hidden_when_no_sunday_slots`** (D-50-16, D-50-08, PDF-01):
       Fixture: `empty_week(2026, 27)`, ein Slot am Samstag (08:00-12:00), kein Sonntag-Slot.
       Renderer-Call: 4-Parameter (Wave 1).
       Assertion: `let so_hex = encode_ascii_to_pdf_hex("So"); assert!(find_subsequence(&bytes, so_hex.as_bytes()).is_none(), "'So' column header must NOT appear when no Sunday slots (D-50-08)");`
       Grund für `#[ignore]`: Alter Renderer rendert immer 7 Spalten (inkl. "So"). Wave 2 macht Sonntag-Spalte dynamisch.

    6. **`sunday_column_shown_when_at_least_one_sunday_slot`** (D-50-16, D-50-08, PDF-01):
       Fixture: `empty_week(2026, 27)`, ein Slot am Sonntag (10:00-14:00).
       Renderer-Call: 4-Parameter (Wave 1).
       Assertion: `let so_hex = encode_ascii_to_pdf_hex("So"); assert!(find_subsequence(&bytes, so_hex.as_bytes()).is_some(), "'So' column header MUST appear when at least one Sunday slot exists (D-50-08)");`
       Grund für `#[ignore]`: Test ist grün mit altem Renderer (der immer "So" emittiert), aber der Assert-Kontext gehört zum D-50-08-Verhalten. Wave 2 verifiziert die dynamische Logik in Kombination mit Test 5.

    Hinweis zum siebten D-50-16-Test `now_local_fallback_to_utc_on_indeterminate_offset`: Der ist Service-Level (`pdf_shiftplan.rs`, D-50-12), NICHT Renderer-Level. Wird in Wave 3 (`50-03-PLAN.md`) angelegt.

    NICHT anfassen: bestehende Portierungs-Tests, Fixtures, Helper-Funktionen — die bleiben in aktueller Form für Wave 1 lauffähig.
  </action>
  <acceptance_criteria>
    - `grep -c '#\[ignore = "Wave 2: erst nach Renderer-Rewrite grün' service_impl/src/pdf_render.rs` = 6
    - `grep -c 'fn render_includes_timestamp_string' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn slot_boxes_sorted_by_start_time' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn names_within_slot_alphabetical' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn unpaid_marker_suffix' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn sunday_column_hidden_when_no_sunday_slots' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'fn sunday_column_shown_when_at_least_one_sunday_slot' service_impl/src/pdf_render.rs` = 1
    - `grep -c 'todo!()\|unimplemented!()' service_impl/src/pdf_render.rs` = 0
    - `cargo test -p service_impl pdf_render --lib` exit code 0 (bestehende Tests grün, ignorierte laufen nicht)
    - `cargo test -p service_impl pdf_render --lib -- --ignored --list 2>&1 | grep -c 'test'` >= 6 (die sechs neuen Tests werden als ignoriert erkannt)
    - `cargo clippy --workspace -- -D warnings` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl pdf_render --lib 2>&1 | tail -15 && echo '---' && cargo test -p service_impl pdf_render --lib -- --ignored --list 2>&1 | tail -20 && echo '---' && cargo clippy --workspace -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>
    Sechs neue `#[ignore]`-Test-Skelette in `mod test` mit konkreten Assertions gegen D-50-16-Behavior; alle bestehenden Portierungs-Tests grün; keine `todo!()`/`unimplemented!()` in Test-Bodies; Ignore-Marker referenzieren `50-02-PLAN.md`; `cargo test -p service_impl pdf_render --lib` grün, `cargo clippy --workspace -- -D warnings` grün.
  </done>
</task>

</tasks>

<verification>
Nach Wave 1 gelten die folgenden Gates:

1. **Cargo-Feature aktiv:** `grep -qE 'features.*local-offset' service_impl/Cargo.toml`
2. **Test-Konstante deklariert:** `grep -q 'const FIXED_RENDER_TIMESTAMP' service_impl/src/pdf_render.rs`
3. **Fixture erweitert:** `grep -q 'fn make_sales_person(id_hex: u128, name: &str, is_paid: Option<bool>)' service_impl/src/pdf_render.rs`
4. **Obsolete Tests gestrichen:** kein Match für `deterministic_bytes_for_same_input`, `sales_persons_sorted_by_id`, `build_sales_person_row_lists_bookings_time_ranges`
5. **Sechs neue ignorierte Tests existieren:** `render_includes_timestamp_string`, `slot_boxes_sorted_by_start_time`, `names_within_slot_alphabetical`, `unpaid_marker_suffix`, `sunday_column_hidden_when_no_sunday_slots`, `sunday_column_shown_when_at_least_one_sunday_slot`
6. **Portierungs-Tests intakt:** sieben Tests aus D-50-15 vorhanden
7. **Build grün:** `cargo build -p service_impl` exit code 0
8. **Tests grün:** `cargo test -p service_impl pdf_render --lib` exit code 0
9. **Clippy grün:** `cargo clippy --workspace -- -D warnings` exit code 0
</verification>

<success_criteria>
- Feature `local-offset` in `service_impl/Cargo.toml` aktiviert
- `FIXED_RENDER_TIMESTAMP`-Konstante in `mod test` deklariert
- `make_sales_person`-Fixture-Signatur erweitert um `is_paid`-Parameter
- Drei obsolete Tests entfernt (D-50-13, D-50-15)
- Sechs neue ignorierte Test-Skelette mit konkreten Assertions angelegt (D-50-16)
- `cargo build -p service_impl`, `cargo test -p service_impl pdf_render --lib`, `cargo clippy --workspace -- -D warnings` grün
- Wave-2-fähig: Test-Signaturen halten `FIXED_RENDER_TIMESTAMP` konsumbar für den 5-Parameter-Renderer
</success_criteria>

<output>
Create `.planning/phases/50-pdf-renderer-browser-look/50-01-SUMMARY.md` when done.
</output>
