---
phase: 8.5
slug: read-projektion-hr-inline-convert
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-09
---

# Phase 8.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in test harness (`cargo test`) + mockall + in-memory SQLite; WASM build gate via `cargo build --target wasm32-unknown-unknown` |
| **Config file** | none — workspace `Cargo.toml` |
| **Quick run command** | `cargo test -p service_impl` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~120 seconds (workspace) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p <touched-crate>` (e.g. `cargo test -p service_impl`, `cargo test -p rest`)
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** `cargo test --workspace` green AND `cargo build --target wasm32-unknown-unknown` (from `shifty-dioxus/`) green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 8.5-01-xx | 01 (Backlink-DAO) | 1 | FUI-A-08 | T-8.5-01 | Backlink read/write nur über überlebenden DAO; keine Cutover-Kopplung | integration (in-memory SQLite) | `cargo test -p dao_impl_sqlite migration_source` | ❌ W0 | ⬜ pending |
| 8.5-02-xx | 02 (AbsenceConversionService) | 1 | FUI-A-08 | T-8.5-02 | Convert nur für lebende Vacation/Sick/Unpaid-Row; soft-delete atomar in Tx | unit (mockall) + integration | `cargo test -p service_impl absence_conversion` | ❌ W0 | ⬜ pending |
| 8.5-03-xx | 03 (Convert-Endpoint) | 2 | FUI-A-08 | T-8.5-03 | `hr`-Privileg erzwungen; 403 ohne Privileg; Validierungsfehler pro Feld | integration + surface | `cargo test -p rest convert_to_absence` + `cargo test -p rest openapi_surface` | ❌ W0 | ⬜ pending |
| 8.5-04-xx | 04 (Read-Projektion Endpoint) | 2 | FUI-A-08 | T-8.5-04 | Projektion nur `hr ∨ self`; kein Range-Raten (Roh-Marker am `when`) | integration | `cargo test -p rest absence_projection` | ❌ W0 | ⬜ pending |
| 8.5-05-xx | 05 (Frontend Absence-Liste) | 3 | FUI-A-08 | — | Convert-Aktion HR-only gerendert; inline-Edit reuse permission | wasm build gate | `cargo build --target wasm32-unknown-unknown` | ✅ | ⬜ pending |
| 8.5-06-xx | 06 (Dialog-Umbau + i18n) | 3 | FUI-A-08 | — | Nicht-blockierender Hinweis; kein add_vacation-Call mehr | wasm build gate + locale-matcher | `cargo build --target wasm32-unknown-unknown` + `cargo test -p rest-types i18n` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task-IDs sind vorläufig; finale IDs setzt der Planner. Plan-Split/Wave-Topologie ist Planner-Detail.*

---

## Wave 0 Requirements

- [ ] `service_impl/src/test/absence_conversion.rs` — Convert-Tx Tests (lebend/soft-deleted, Kategorie-Gate, Overlap-Reject, DateOrderWrong)
- [ ] `dao_impl_sqlite` Tests für neuen `MigrationSourceDao` (Backlink read/write ohne `cutover_run_id`-Kopplung)
- [ ] `rest` integration + surface stubs für `convert-to-absence`-Endpoint + Read-Projektion (`openapi_surface.rs` erweitern)
- [ ] Per-locale reference-matcher Test für neue i18n-Keys (Guard gegen Locale::En-statt-De-Bug aus 08-04/08-05)

*Bestehende Infrastruktur (mockall + in-memory SQLite + openapi_surface + locale-matcher) deckt die Patterns; nur neue Test-Module/Cases sind anzulegen.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Inline-Darstellung „stundenbasiert"-Badge chronologisch korrekt gemischt | FUI-A-08 / SC1 | Visuelle Reihenfolge + Badge-Sichtbarkeit nicht grep-bar | Backend+Frontend starten, als HR `/absences` öffnen, stundenbasierten Eintrag neben native Range prüfen |
| Convert-Roundtrip: Marker verschwindet, native Range erscheint nach Reload | FUI-A-08 / SC3 | End-to-end Reload-Verhalten | Stundenbasierten Eintrag „In Zeitraum umwandeln", Range eingeben, Liste prüft Verschwinden des Markers + neue Range |
| Nicht-blockierender Dialog-Hinweis erscheint/verschwindet bei Kategorie-Wechsel | FUI-A-08 / SC4 | Reaktives UI-Verhalten | Working-Hours-Dialog: Vacation/Sick/Unpaid → Hinweis sichtbar; andere Kategorie → Hinweis weg |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
