---
phase: 08.5-read-projektion-hr-inline-convert
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - service/src/absence_conversion.rs
  - service/src/lib.rs
  - service_impl/src/absence_conversion.rs
  - service_impl/src/lib.rs
  - service_impl/src/test/absence_conversion.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [FUI-A-08]
must_haves:
  truths:
    - "HR kann eine lebende Vacation/SickLeave/UnpaidLeave extra_hours-Row in ein absence_period umwandeln"
    - "Convert legt absence_period an, schreibt Backlink, soft-deleted die extra_hours-Row — atomar in einer Tx"
    - "start > end liefert DateOrderWrong; Overlap liefert ValidationError(OverlappingPeriod)"
    - "Non-HR-Caller wird mit Forbidden abgelehnt; bereits soft-deletete/falsche Kategorie liefert EntityNotFound"
  artifacts:
    - path: "service/src/absence_conversion.rs"
      provides: "trait AbsenceConversionService"
      contains: "trait AbsenceConversionService"
    - path: "service_impl/src/absence_conversion.rs"
      provides: "AbsenceConversionServiceImpl (BL-Tier, lean 3-write Tx)"
      contains: "gen_service_impl!"
    - path: "service_impl/src/test/absence_conversion.rs"
      provides: "4 mockall unit tests + 1 in-memory integration test"
      contains: "fn convert_extra_hours"
  key_links:
    - from: "service_impl/src/absence_conversion.rs"
      to: "MigrationSourceDao"
      via: "upsert_migration_source nach absence_dao.create"
      pattern: "upsert_migration_source"
    - from: "service_impl/src/absence_conversion.rs"
      to: "extra_hours_service.soft_delete_bulk"
      via: "Authentication::Full nach hr-Gate im Service"
      pattern: "soft_delete_bulk"
---

<objective>
Baue den neuen `AbsenceConversionService` (Business-Logic-Tier) mit einer frischen, schlanken Single-Row-Convert-Tx — exakt die drei Kern-Writes aus `convert_quarantine_entry` (cutover.rs:551-582), OHNE Quarantäne-/Heuristik-/Drift-Ballast (D-01, D-02). Der Service besitzt: (1) Row by logical_id laden + validieren (lebend + Kategorie ∈ {Vacation, SickLeave, UnpaidLeave}), (2) Overlap-Check, (3) absence_dao.create, (4) Backlink-upsert über den neuen MigrationSourceDao, (5) extra_hours soft-delete. Privileg-Gate: `hr` (D-05).

Purpose: D-03 — dedizierte BL-Tier-Heimat, die nichts mit der Cutover-Maschinerie teilt; 8.6 löscht CutoverServiceImpl rein subtraktiv.
Output: Trait (service), Impl (service_impl), 4 mockall-Unit-Tests + 1 Integration-Test. Beide `lib.rs` + `test/mod.rs` registriert.

KEIN Snapshot-Bump (D-16): Convert ist ein per-row Daten-Umzug; Reporting summiert beide Quellen seit 8.4 additiv per-row (`deleted IS NULL`). `CURRENT_SNAPSHOT_SCHEMA_VERSION` wird NICHT erhöht.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-CONTEXT.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-RESEARCH.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-PATTERNS.md
@.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-01-SUMMARY.md

<interfaces>
<!-- Dependencies the executor consumes directly. -->

From dao/src/migration_source.rs (Plan 01):
```rust
pub struct MigrationSourceRow { pub extra_hours_id: Uuid, pub absence_period_id: Uuid, pub migrated_at: time::PrimitiveDateTime }
trait MigrationSourceDao { async fn upsert_migration_source(&self, row: &MigrationSourceRow, tx: Self::Transaction) -> Result<(), DaoError>; }
```

From service/src/extra_hours.rs (existing):
```rust
async fn soft_delete_bulk(&self, ids: Arc<[Uuid]>, update_process: &str, context: Authentication<Self::Context>, tx: Option<Self::Transaction>) -> Result<(), ServiceError>;
```
NOTE: soft_delete_bulk docstring sagt "caller MUST hold cutover_admin" — das ist die alte Phase-4-Prämisse. Der NEUE Service enforced sein eigenes `hr`-Gate (siehe unten) und ruft soft_delete_bulk intern mit `Authentication::Full` (Pitfall 5). Das hr-Gate ist der echte Guard.

ExtraHoursDao (load+validate single row by logical_id):
```rust
async fn find_by_logical_id(&self, logical_id: Uuid, tx: Self::Transaction) -> Result<Option<ExtraHoursEntity>, DaoError>;
```
ExtraHours/ExtraHoursEntity category enum variants: ExtraWork, Vacation, SickLeave, Holiday, Unavailable, UnpaidLeave, VolunteerWork, CustomExtraHours(..). Convert nur erlaubt für {Vacation, SickLeave, UnpaidLeave}.

From service_impl/src/cutover.rs:1470-1479 (helper to MOVE/COPY into new module):
```rust
fn extra_hours_category_to_absence(c: &ExtraHoursCategoryEntity) -> AbsenceCategoryEntity {
    match c {
        ExtraHoursCategoryEntity::Vacation => AbsenceCategoryEntity::Vacation,
        ExtraHoursCategoryEntity::SickLeave => AbsenceCategoryEntity::SickLeave,
        ExtraHoursCategoryEntity::UnpaidLeave => AbsenceCategoryEntity::UnpaidLeave,
        other => panic!("... non-legacy category: {:?}", other),
    }
}
```

Overlap + DateRange pattern (service_impl/src/absence.rs:189-207):
```rust
let new_range = DateRange::new(from_date, to_date).map_err(|_| ServiceError::DateOrderWrong(from_date, to_date))?;
let conflicts = self.absence_dao.find_overlapping(sp_id, (&target_category).into(), new_range, None, tx.clone()).await?;
if !conflicts.is_empty() { return Err(ServiceError::ValidationError(Arc::from([ValidationFailureItem::OverlappingPeriod(conflicts[0].logical_id)]))); }
```

HR_PRIVILEGE import: `use service::permission::{Authentication, HR_PRIVILEGE};`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AbsenceConversionService Trait (service-Crate)</name>
  <read_first>
    - service/src/cutover.rs:234-286 (analog: CutoverService trait-Shape, #[automock], type Context/Transaction — PATTERNS.md Z. 103-126)
    - service/src/absence.rs:60-95 (DayFraction enum + AbsencePeriod return-Typ)
    - service/src/lib.rs (Modul-Registrierung)
  </read_first>
  <action>
    Erstelle `service/src/absence_conversion.rs`. Imports analog `service/src/cutover.rs` (async_trait, automock, MockTransaction, Authentication, ServiceError, Uuid). Trait mit GENAU einer Methode (KEIN ManualRange-Wrapper — Caller liefert geparste Dates direkt, D-02):
    ```rust
    #[automock(type Context=(); type Transaction=dao::MockTransaction;)]
    #[async_trait]
    pub trait AbsenceConversionService {
        type Context: Clone + std::fmt::Debug + PartialEq + Eq + Send + Sync + 'static;
        type Transaction: dao::Transaction;

        /// Convert a single living extra_hours row (Vacation/SickLeave/UnpaidLeave)
        /// into an absence_period over the HR-supplied [from_date, to_date] range.
        /// hr-privileged. Atomic: create absence_period + write backlink + soft-delete extra_hours.
        async fn convert_extra_hours_to_absence(
            &self,
            extra_hours_id: Uuid,
            from_date: time::Date,
            to_date: time::Date,
            day_fraction: Option<crate::absence::DayFraction>,
            context: Authentication<Self::Context>,
            tx: Option<Self::Transaction>,
        ) -> Result<crate::absence::AbsencePeriod, ServiceError>;
    }
    ```
    Registriere `pub mod absence_conversion;` in `service/src/lib.rs`.
  </action>
  <verify>
    <automated>cargo check -p service 2>&1 | tail -5; grep -q "trait AbsenceConversionService" service/src/absence_conversion.rs && grep -q "pub mod absence_conversion" service/src/lib.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `service/src/absence_conversion.rs` enthält `pub trait AbsenceConversionService` mit `convert_extra_hours_to_absence`
    - Signatur trägt `from_date: time::Date`, `to_date: time::Date`, `day_fraction: Option<crate::absence::DayFraction>` (KEIN ManualRange)
    - `service/src/lib.rs` enthält `pub mod absence_conversion;`
    - `cargo check -p service` exit 0 (automock generiert `MockAbsenceConversionService`)
  </acceptance_criteria>
  <done>Trait im service-Crate, automock-Mock verfügbar, service compiliert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AbsenceConversionServiceImpl — lean 3-write Tx (service_impl-Crate)</name>
  <read_first>
    - service_impl/src/cutover.rs:527-582 (die DREI zu extrahierenden Writes — RESEARCH.md Code Examples Z. 505-555)
    - service_impl/src/cutover.rs:1470-1479 (extra_hours_category_to_absence helper)
    - service_impl/src/absence.rs:160-237 (Tx-open + hr-Gate + Overlap-Validate + commit — PATTERNS.md Z. 149-181)
    - service_impl/src/vacation_balance.rs:52-62 (gen_service_impl! BL-Tier-Pattern — PATTERNS.md Z. 132-147)
    - service/src/absence_conversion.rs (Trait aus Task 1)
    - dao/src/migration_source.rs (MigrationSourceDao aus Plan 01)
  </read_first>
  <behavior>
    - convert_extra_hours_to_absence mit hr-Privileg + lebender Vacation-Row + gültiger Range -> Ok(AbsencePeriod); absence_dao.create + migration_source_dao.upsert + extra_hours_service.soft_delete_bulk je 1x aufgerufen; commit aufgerufen
    - start > end -> Err(ServiceError::DateOrderWrong(start, end)); KEIN absence_dao.create
    - Overlap (find_overlapping liefert nicht-leer) -> Err(ServiceError::ValidationError([OverlappingPeriod(..)])); KEIN absence_dao.create, KEIN soft_delete
    - Non-HR (check_permission liefert Forbidden) -> Err(Forbidden); KEINE Writes
    - Row nicht gefunden / bereits soft-deleted / Kategorie ∉ {Vacation,SickLeave,UnpaidLeave} -> Err(EntityNotFound o.ä.); KEINE Writes
  </behavior>
  <action>
    Erstelle `service_impl/src/absence_conversion.rs`. `gen_service_impl!`-Block (BL-Tier, Deps exakt):
    ```rust
    gen_service_impl! {
        struct AbsenceConversionServiceImpl: service::absence_conversion::AbsenceConversionService = AbsenceConversionServiceDeps {
            ExtraHoursDao: dao::extra_hours::ExtraHoursDao<Transaction = Self::Transaction> = extra_hours_dao,
            AbsenceDao: dao::absence::AbsenceDao<Transaction = Self::Transaction> = absence_dao,
            MigrationSourceDao: dao::migration_source::MigrationSourceDao<Transaction = Self::Transaction> = migration_source_dao,
            ExtraHoursService: service::ExtraHoursService<Context = Self::Context, Transaction = Self::Transaction> = extra_hours_service,
            PermissionService: service::PermissionService<Context = Self::Context> = permission_service,
            TransactionDao: dao::TransactionDao<Transaction = Self::Transaction> = transaction_dao,
        }
    }
    ```
    Impl `convert_extra_hours_to_absence` in dieser Reihenfolge:
    1. **hr-Gate VOR Tx-Open** (D-05): `self.permission_service.check_permission(HR_PRIVILEGE, context.clone()).await?;`
    2. Tx öffnen: `let tx = self.transaction_dao.use_transaction(tx).await?;`
    3. `let now = time::OffsetDateTime::now_utc(); let migrated_at = time::PrimitiveDateTime::new(now.date(), now.time());`
    4. Row laden + validieren: `let entity = self.extra_hours_dao.find_by_logical_id(extra_hours_id, tx.clone()).await?.ok_or_else(|| ServiceError::EntityNotFoundGeneric(Arc::from("extra_hours not found or already converted")))?;` — `find_by_logical_id` filtert `deleted IS NULL`, also liefert eine bereits soft-deletete Row None (idempotent). Validiere Kategorie ∈ {Vacation, SickLeave, UnpaidLeave}; sonst `Err(ServiceError::EntityNotFoundGeneric(Arc::from("extra_hours category not convertible")))` (oder passendere ValidationError-Variante — siehe wie cutover.rs es macht). KEIN Jahr-Match-Check (D-14: 8.2 D-30 entfällt — NICHT replizieren).
    5. `target_category = extra_hours_category_to_absence(&entity.category)` — den Helper aus cutover.rs:1470-1479 in DIESES Modul kopieren (NICHT aus cutover.rs importieren, damit 8.6 cutover.rs sauber löschen kann; A3). Mache ihn `pub(crate)` falls Tests ihn brauchen.
    6. Overlap-Check (Pattern aus absence.rs:189-207, exclude_logical_id = None):
       ```rust
       let new_range = DateRange::new(from_date, to_date).map_err(|_| ServiceError::DateOrderWrong(from_date, to_date))?;
       let conflicts = self.absence_dao.find_overlapping(entity.sales_person_id, target_category.clone(), new_range, None, tx.clone()).await?;
       if !conflicts.is_empty() { return Err(ServiceError::ValidationError(Arc::from([ValidationFailureItem::OverlappingPeriod(conflicts[0].logical_id)]))); }
       ```
    7. **Write 1 — absence_dao.create** (cutover.rs:530-553): baue `AbsencePeriodEntity` mit `id == logical_id == Uuid::new_v4()`, `sales_person_id: entity.sales_person_id`, `category: target_category`, `from_date`, `to_date`, `description: Arc::from("")`, `created: migrated_at`, `deleted: None`, `version: Uuid::new_v4()`, `day_fraction: day_fraction.as_ref().map(dao::absence::DayFractionEntity::from).unwrap_or(dao::absence::DayFractionEntity::Full)`. `self.absence_dao.create(&entity, "absence_conversion::convert", tx.clone()).await?;`
    8. **Write 2 — Backlink** (über MigrationSourceDao, KEIN cutover_run_id): `self.migration_source_dao.upsert_migration_source(&dao::migration_source::MigrationSourceRow { extra_hours_id, absence_period_id, migrated_at }, tx.clone()).await?;`
    9. **Write 3 — soft-delete** (cutover.rs:575-582, identisch; Pitfall 5: intern Authentication::Full nach dem hr-Gate): `self.extra_hours_service.soft_delete_bulk(Arc::from([extra_hours_id]), "absence_conversion::convert", Authentication::Full, Some(tx.clone())).await?;`
    10. `self.transaction_dao.commit(tx).await?;` und `Ok((&created_entity).into())` (AbsencePeriodEntity -> service::absence::AbsencePeriod).

    Registriere `pub mod absence_conversion;` in `service_impl/src/lib.rs`. NICHT `CURRENT_SNAPSHOT_SCHEMA_VERSION` anfassen (D-16).
  </action>
  <verify>
    <automated>cargo check -p service_impl 2>&1 | tail -8; grep -q "gen_service_impl" service_impl/src/absence_conversion.rs && grep -q "upsert_migration_source" service_impl/src/absence_conversion.rs && grep -q "soft_delete_bulk" service_impl/src/absence_conversion.rs && ! grep -q "cutover_run_id\|synthetic_run_id\|find_legacy_extra_hours\|detect_weekly_lump_sum\|absence_range_source_active" service_impl/src/absence_conversion.rs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `service_impl/src/absence_conversion.rs` enthält `gen_service_impl!` mit Deps: ExtraHoursDao, AbsenceDao, MigrationSourceDao, ExtraHoursService, PermissionService, TransactionDao
    - hr-Gate (`check_permission(HR_PRIVILEGE, ...)`) steht VOR `use_transaction`
    - Enthält die drei Writes: `absence_dao.create`, `migration_source_dao.upsert_migration_source`, `extra_hours_service.soft_delete_bulk(... Authentication::Full ...)`
    - Enthält KEINE Cutover-Artefakte (grep auf `cutover_run_id`, `synthetic_run_id`, `find_legacy_extra_hours`, `detect_weekly_lump_sum`, `absence_range_source_active` liefert 0 Treffer)
    - `extra_hours_category_to_absence` ist im Modul kopiert (NICHT aus cutover importiert)
    - `service_impl/src/lib.rs` enthält `pub mod absence_conversion;`
    - `CURRENT_SNAPSHOT_SCHEMA_VERSION` unverändert (`git diff` zeigt keine Änderung an billing_period_report.rs)
    - `cargo check -p service_impl` exit 0
  </acceptance_criteria>
  <done>Schlanke 3-write Convert-Tx im BL-Tier, hr-gated, ohne Cutover-Ballast, kein Snapshot-Bump.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — 4 mockall Unit + 1 in-memory Integration</name>
  <read_first>
    - service_impl/src/test/vacation_balance.rs:97-162 (Deps-Struct + build_dependencies + build_service Pattern — PATTERNS.md Z. 187-227)
    - service_impl/src/test/reporting_additive_merge.rs (Integration-Test-Setup gegen in-memory SQLite + run migrations)
    - service_impl/src/test/mod.rs (Modul-Registrierung)
    - service_impl/src/absence_conversion.rs (Impl aus Task 2)
  </read_first>
  <behavior>
    - convert_extra_hours_happy_path: hr ok + lebende Vacation-Row + gültige Range -> Ok; verifiziert via mockall `.times(1)` auf absence_dao.create, migration_source_dao.upsert_migration_source, extra_hours_service.soft_delete_bulk
    - convert_extra_hours_date_order_wrong: start > end -> Err(DateOrderWrong); absence_dao.create `.times(0)`
    - convert_extra_hours_overlap_rejected: find_overlapping liefert nicht-leer -> Err(ValidationError mit OverlappingPeriod); soft_delete_bulk `.times(0)`
    - convert_requires_hr_privilege: check_permission liefert Err(Forbidden) -> Err(Forbidden); use_transaction `.times(0)` ODER alle Writes `.times(0)`
    - Integration (in-memory SQLite): seed extra_hours Vacation-Row -> convert -> absence_period existiert + extra_hours ist soft-deleted (deleted IS NOT NULL) + migration_source-Backlink existiert
  </behavior>
  <action>
    Erstelle `service_impl/src/test/absence_conversion.rs`. Mockall-Variante: `AbsenceConversionDependencies`-Struct mit je einem Mock-Feld pro gen_service_impl!-Dep (`MockExtraHoursDao`, `MockAbsenceDao`, `MockMigrationSourceDao`, `MockExtraHoursService`, `MockPermissionService`, `MockTransactionDao`); `impl AbsenceConversionServiceDeps` + `build_service`; `build_dependencies()` mit Default-Expectations (`transaction_dao.expect_use_transaction().returning(|_| Ok(MockTransaction))`, `.expect_commit().returning(|_| Ok(()))`). Schreibe die 4 Unit-Tests gemäß <behavior> mit präzisen `.expect_*().times(n)`-Assertions (für happy-path `.times(1)` auf die drei Writes; für Fehlerpfade `.times(0)`).

    Integration-Test: nutze das in-memory-SQLite-Setup aus `reporting_additive_merge.rs` (Pool + `sqlx::migrate!` o.ä.). Konstruiere die echten DAO-Impls + den echten ExtraHoursService (oder via vorhandenem Test-Harness, falls eines existiert). Seed eine Vacation-extra_hours-Row, rufe `convert_extra_hours_to_absence`, asserte: (a) absence_period mit category Vacation + from/to existiert, (b) die extra_hours-Row ist soft-deleted (`find_by_logical_id` liefert None), (c) `migration_source_dao.find_by_extra_hours_id(id)` liefert Some mit korrektem absence_period_id. Test-Name: `convert_extra_hours_happy_path_integration`. Falls ein vollständiges Integration-Harness zu teuer ist, ist die mockall-Variante der happy-path Pflicht und der Integration-Test als `#[tokio::test]` mit minimalem direkten DAO-Setup zu schreiben — NICHT `#[ignore]`.

    Registriere `mod absence_conversion;` in `service_impl/src/test/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p service_impl absence_conversion 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `service_impl/src/test/absence_conversion.rs` enthält ≥ 4 mockall-Unit-Tests + 1 Integration-Test (kein `#[ignore]`)
    - `service_impl/src/test/mod.rs` enthält `mod absence_conversion;`
    - `cargo test -p service_impl absence_conversion` exit 0, alle Tests grün
    - Happy-path-Test asserted `.times(1)` auf absence_dao.create + upsert_migration_source + soft_delete_bulk
    - hr-Test asserted, dass ohne Privileg keine Writes passieren
  </acceptance_criteria>
  <done>4 Unit + 1 Integration grün; Convert-Verhalten + Fehlerpfade + Authz abgedeckt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST -> Service | Convert-Aufruf trägt Authentication<Context>; Service ist die Authz-Heimat |
| Service -> DAO | Service koordiniert atomare Tx über 3 DAO/Service-Writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.5-02a | Elevation of Privilege | convert_extra_hours_to_absence | mitigate | hr-Gate `check_permission(HR_PRIVILEGE, ...)` VOR Tx-Open; Unit-Test `convert_requires_hr_privilege` asserted Forbidden + 0 Writes |
| T-8.5-02b | Tampering | Double-convert | mitigate | `find_by_logical_id` filtert `deleted IS NULL` -> bereits konvertierte Row liefert None -> EntityNotFound (idempotent) |
| T-8.5-02c | Tampering | Falsche Kategorie | mitigate | Kategorie-Validierung ∈ {Vacation,SickLeave,UnpaidLeave} vor jedem Write; ExtraWork/Holiday/etc. abgelehnt |
| T-8.5-02d | Tampering | Inkonsistenter absence-State durch Overlap | mitigate | find_overlapping(exclude_logical_id=None) vor create; ValidationError(OverlappingPeriod) bei Konflikt |
| T-8.5-02e | Information Disclosure | IDOR — fremde extra_hours_id | mitigate | hr-Gate begrenzt auf HR-Rolle (D-05: Convert ist HR-Aktion, nicht self); REST-Plan 03 reicht echtes Context durch |
</threat_model>

<verification>
- `cargo test -p service_impl absence_conversion` grün (4 Unit + 1 Integration)
- `cargo check --workspace` exit 0 (rust-analyzer-Stale-Diagnostics ignorieren)
- grep: keine Cutover-Artefakte im neuen Service-Impl
- `git diff service_impl/src/billing_period_report.rs` leer (kein Snapshot-Bump, D-16)
</verification>

<success_criteria>
- AbsenceConversionService (Trait + BL-Tier-Impl) liefert atomaren, hr-gated, validierten Convert
- Drei Writes exakt aus cutover.rs extrahiert, ohne Quarantäne/Heuristik/Drift
- Alle 5 Tests grün
- Kein Snapshot-Schema-Bump (D-16)
</success_criteria>

<output>
After completion, create `.planning/phases/08.5-read-projektion-hr-inline-convert/08.5-02-SUMMARY.md`
</output>
