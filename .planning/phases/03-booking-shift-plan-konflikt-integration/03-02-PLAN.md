---
phase: 03-booking-shift-plan-konflikt-integration
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - service/src/warning.rs
  - service/src/lib.rs
  - service/src/shiftplan.rs
  - dao/src/absence.rs
  - dao_impl_sqlite/src/absence.rs
  - .sqlx/
autonomous: true
nyquist_compliant: false
requirements:
  - BOOK-01
  - BOOK-02
  - PLAN-01
must_haves:
  truths:
    - "Ein neues Modul `service/src/warning.rs` exportiert `pub enum Warning` mit den 4 Varianten aus D-Phase3-14 — KEINE 5. Variante (D-Phase3-17 deferred)."
    - "`service/src/lib.rs` re-exportiert `pub mod warning;` und `pub use warning::Warning;`."
    - "`dao::absence::AbsenceDao` hat eine neue Trait-Methode `find_overlapping_for_booking(sales_person_id, range, tx) -> Result<Arc<[AbsencePeriodEntity]>, DaoError>` (kategorie-frei, single SQL-Query, D-Phase3-05)."
    - "`dao_impl_sqlite::absence::AbsenceDaoImpl::find_overlapping_for_booking` ist implementiert mit `WHERE sales_person_id = ? AND from_date <= ? AND to_date >= ? AND deleted IS NULL` (Pflicht für Pitfall-1 / SC4)."
    - "`.sqlx/`-Cache ist via `cargo sqlx prepare --workspace -- --tests` aktualisiert; `cargo build --workspace` GRÜN."
    - "`service/src/shiftplan.rs` enthält `pub enum UnavailabilityMarker` mit den 3 Varianten aus D-Phase3-10; `ShiftplanDay.unavailable: Option<UnavailabilityMarker>` ist additiv ergänzt; bestehender `build_shiftplan_day` setzt `unavailable: None` als Default (Build-Bruch erzwingt Update an allen Construction-Sites)."
  artifacts:
    - path: "service/src/warning.rs"
      provides: "Warning-Enum mit 4 Varianten (BookingOnAbsenceDay, BookingOnUnavailableDay, AbsenceOverlapsBooking, AbsenceOverlapsManualUnavailable)"
      contains: "pub enum Warning"
    - path: "dao/src/absence.rs"
      provides: "AbsenceDao::find_overlapping_for_booking Trait-Method"
      contains: "find_overlapping_for_booking"
    - path: "dao_impl_sqlite/src/absence.rs"
      provides: "SQLx-Impl von find_overlapping_for_booking mit Soft-Delete-Filter"
      contains: "WHERE sales_person_id = ?"
    - path: "service/src/shiftplan.rs"
      provides: "UnavailabilityMarker-Enum + ShiftplanDay.unavailable-Field"
      contains: "pub enum UnavailabilityMarker"
  key_links:
    - from: "service/src/lib.rs"
      to: "service/src/warning.rs"
      via: "pub mod warning"
      pattern: "pub mod warning"
    - from: "service/src/warning.rs"
      to: "service/src/absence.rs (AbsenceCategory)"
      via: "use crate::absence::AbsenceCategory"
      pattern: "AbsenceCategory"
    - from: "dao_impl_sqlite/src/absence.rs"
      to: "absence_period table"
      via: "SQL composite index `(sales_person_id, from_date) WHERE deleted IS NULL`"
      pattern: "deleted IS NULL"
---

<objective>
Wave-1-Domain-Surface: Lege das `Warning`-Domain-Enum (Phase-3-Erfolgs-Pfad-Modell) im neuen Modul `service/src/warning.rs` an. Erweitere die DAO-Schicht um die kategorie-freie Range-Lookup-Methode `find_overlapping_for_booking` (Trait + SQLx-Impl). Erweitere `ShiftplanViewService`-Trait-Datei um `UnavailabilityMarker`-Enum und das additive `unavailable: Option<UnavailabilityMarker>`-Feld auf `ShiftplanDay`. KEINE Service-Trait-Sig-Brüche an `AbsenceService` / `ShiftplanEditService` — die kommen mit ihren Impls in Wave 2 (Plan 03 + Plan 04).

Purpose: Reine "Domain-Surface" — keine Business-Logik, keine Cross-Service-Wiring. Wave 2+ baut darauf auf.

Output: 4 Files patched / 1 NEW; .sqlx-Cache aktualisiert.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md
@.planning/phases/01-absence-domain-foundation/01-CONTEXT.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Domain-Enum-Layout aus service/src/absence.rs:27-32 -->

```rust
// service/src/absence.rs:27-32 — Pattern für Domain-Enum
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum AbsenceCategory {
    Vacation,
    SickLeave,
    UnpaidLeave,
}
```

<!-- Existing AbsenceDao-Trait Layout aus dao/src/absence.rs:43-103 -->

```rust
#[automock(type Transaction = crate::MockTransaction;)]
#[async_trait]
pub trait AbsenceDao {
    type Transaction: crate::Transaction;

    async fn find_overlapping(
        &self,
        sales_person_id: Uuid,
        category: AbsenceCategoryEntity,
        range: DateRange,
        exclude_logical_id: Option<Uuid>,
        tx: Self::Transaction,
    ) -> Result<Arc<[AbsencePeriodEntity]>, crate::DaoError>;
    // ...
}
```

<!-- Existing find_overlapping SQLx-Pattern aus dao_impl_sqlite/src/absence.rs:155-204 -->

Vorhandene Allen-Algebra-Range-Match-Form (None-Branch ohne exclude_logical_id):
```sql
SELECT id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_version
FROM absence_period
WHERE sales_person_id = ?
  AND category = ?
  AND from_date <= ?
  AND to_date >= ?
  AND deleted IS NULL
```

<!-- ShiftplanDay-heutige Form aus service/src/shiftplan.rs:14-18 -->

```rust
#[derive(Debug, Clone)]
pub struct ShiftplanDay {
    pub day_of_week: DayOfWeek,
    pub slots: Vec<ShiftplanSlot>,
}
```

<!-- service/src/lib.rs Modul-Liste — Z. 7-42 (alphabetisch) -->

`pub mod sales_person_unavailable;` Z. 28
`pub mod scheduler;`                Z. 29
`pub mod session;`                  Z. 30
`pub mod shiftplan;`                Z. 31
...
`pub mod week_message;`             Z. 42

→ `pub mod warning;` GEHÖRT alphabetisch zwischen `pub mod uuid_service;` (Z. 41) und `pub mod week_message;` (Z. 42).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Warning-Modul anlegen + in `service/src/lib.rs` re-exportieren</name>
  <read_first>
    - service/src/lib.rs (Modul-Reihe Z. 7-42)
    - service/src/absence.rs (Z. 27-32 — AbsenceCategory-Enum-Layout)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-14 — die 4 Warning-Varianten verbatim)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 62-124 — Modul-Header + Enum-Definition)
    - shifty-utils/src/lib.rs (verifiziere Existenz von `pub use date_range::DayOfWeek;` oder analog)
  </read_first>
  <files>service/src/warning.rs, service/src/lib.rs</files>
  <action>
    1. **Erstelle NEUE Datei** `service/src/warning.rs` mit folgendem **vollständigen** Inhalt (verbatim aus 03-CONTEXT.md `<specifics>` + 03-PATTERNS.md):

    ```rust
    //! Domain-Warnings für Phase-3 (Booking ⇄ Absence ⇄ ManualUnavailable Cross-Source-Konflikte).
    //!
    //! `Warning` ist Erfolgs-Pfad — sie wird in den Wrapper-Result-Structs
    //! `BookingCreateResult` / `CopyWeekResult` (in `service::shiftplan_edit`) und
    //! `AbsencePeriodCreateResult` (in `service::absence`) propagiert. KEIN
    //! `ServiceError`-Pfad. KEIN ValidationFailureItem (das wäre 422; Warnings
    //! sind 200/201 mit Liste).
    //!
    //! Granularität (D-Phase3-15): eine Warning pro betroffenem Booking-Tag.

    use shifty_utils::DayOfWeek;
    use time::Date;
    use uuid::Uuid;

    use crate::absence::AbsenceCategory;

    /// Cross-Source-Konflikt-Warning. Vier Varianten, jede trägt nur die für die
    /// jeweilige Quelle relevanten Felder. Frontend rendert eine Liste.
    ///
    /// Stable per D-Phase3-14 — die 5. Variante `ManualUnavailableOnAbsenceDay`
    /// ist deferred (D-Phase3-17, Folgephase).
    #[derive(Clone, Debug, PartialEq, Eq)]
    pub enum Warning {
        /// Beim Anlegen eines Bookings auf einem Tag, der durch eine
        /// AbsencePeriod abgedeckt ist (Reverse-Warning, BOOK-02).
        BookingOnAbsenceDay {
            booking_id: Uuid,
            date: Date,
            absence_id: Uuid,
            category: AbsenceCategory,
        },
        /// Beim Anlegen eines Bookings auf einem Tag, der durch
        /// `sales_person_unavailable` abgedeckt ist (Reverse-Warning, BOOK-02).
        BookingOnUnavailableDay {
            booking_id: Uuid,
            year: u32,
            week: u8,
            day_of_week: DayOfWeek,
        },
        /// Beim Anlegen einer AbsencePeriod, die ein bestehendes Booking
        /// überlappt (Forward-Warning, BOOK-01).
        AbsenceOverlapsBooking {
            absence_id: Uuid,
            booking_id: Uuid,
            date: Date,
        },
        /// Beim Anlegen einer AbsencePeriod, die einen bestehenden manuellen
        /// `sales_person_unavailable`-Eintrag überdeckt (Forward-Warning,
        /// BOOK-01, D-Phase3-16: KEIN Auto-Cleanup).
        AbsenceOverlapsManualUnavailable {
            absence_id: Uuid,
            unavailable_id: Uuid,
        },
    }
    ```

    2. **Patch** `service/src/lib.rs`: Suche die alphabetische Modul-Reihe (Z. 7-42). Füge nach `pub mod uuid_service;` (Z. 41) und VOR `pub mod week_message;` (Z. 42) eine neue Zeile ein:
    ```rust
    pub mod warning;
    ```
    Verifiziere alphabetische Reihenfolge: `uuid_service` < `warning` < `week_message`. Korrekt.

    3. KEINE Re-Export-Anpassung in `lib.rs` (kein `pub use warning::Warning;`) — Konsumenten machen `use service::warning::Warning;` analog `use service::absence::AbsencePeriod;`.

    4. **Verifiziere**: `shifty_utils::DayOfWeek` ist verfügbar. Falls nicht (sollte sein per Phase 1), passe Import an.
  </action>
  <verify>
    <automated>cargo build -p service 2>&1 | tail -10</automated>
    <automated>grep -c "pub enum Warning" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/warning.rs</automated>
    <automated>grep -c "pub mod warning;" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/lib.rs</automated>
    <automated>grep -E "BookingOnAbsenceDay|BookingOnUnavailableDay|AbsenceOverlapsBooking|AbsenceOverlapsManualUnavailable" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/warning.rs | grep -v '^//' | wc -l</automated>
  </verify>
  <done>
    - cargo build -p service GRÜN
    - service/src/warning.rs existiert mit `pub enum Warning` (Verify-2 == 1)
    - service/src/lib.rs enthält `pub mod warning;` (Verify-3 == 1)
    - Alle 4 Warning-Varianten existieren im Source (Verify-4 ≥ 4 — Var-Definitions, KEINE Comments)
  </done>
</task>

<task type="auto">
  <name>Task 2: AbsenceDao-Trait + SQLx-Impl `find_overlapping_for_booking` (kategorie-frei) + sqlx-prepare</name>
  <read_first>
    - dao/src/absence.rs (Z. 43-103 — AbsenceDao-Trait + automock)
    - dao_impl_sqlite/src/absence.rs (Z. 155-204 — find_overlapping-Impl als Vorlage)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-05)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 350-428 — Trait+Impl-Templates)
    - shifty-utils/src/date_range.rs (DateRange.from()/to()-API)
    - .planning/phases/01-absence-domain-foundation/01-CONTEXT.md (D-04 — composite index `(sales_person_id, from_date) WHERE deleted IS NULL`)
  </read_first>
  <files>dao/src/absence.rs, dao_impl_sqlite/src/absence.rs, .sqlx/</files>
  <action>
    1. **Patch** `dao/src/absence.rs`: Füge eine neue Trait-Methode nach `find_overlapping` (NACH Z. 85, VOR `create` Z. 87) ein. Exakte Signatur:

    ```rust
        /// Findet aktive Absence-Periods derselben `sales_person_id`, die `range`
        /// inklusiv überlappen — **kategorie-frei** (alle 3 AbsenceCategory-Werte
        /// werden zurückgegeben). Verwendet vom
        /// `AbsenceService::find_overlapping_for_booking`-Pfad und vom
        /// `ShiftplanEditService::book_slot_with_conflict_check`-Pfad
        /// (Phase 3, D-Phase3-05).
        ///
        /// Nutzt den bestehenden Composite-Index
        /// `idx_absence_period_sales_person_from(sales_person_id, from_date)
        ///  WHERE deleted IS NULL` (Phase-1-D-04). Single-Roundtrip auch bei
        /// copy_week-Loops; Performance-skalierbar.
        async fn find_overlapping_for_booking(
            &self,
            sales_person_id: Uuid,
            range: DateRange,
            tx: Self::Transaction,
        ) -> Result<Arc<[AbsencePeriodEntity]>, crate::DaoError>;
    ```

    `#[automock]` (Z. 43) deckt die neue Methode automatisch — `MockAbsenceDao::expect_find_overlapping_for_booking()` wird verfügbar.

    2. **Patch** `dao_impl_sqlite/src/absence.rs`: Füge die Impl-Methode in den `impl AbsenceDao for AbsenceDaoImpl`-Block (oder analoger Block — exakte Position: NACH der bestehenden `find_overlapping`-Impl ungefähr Z. 204, VOR `create`-Impl). Exakter Body:

    ```rust
    async fn find_overlapping_for_booking(
        &self,
        sales_person_id: Uuid,
        range: DateRange,
        tx: Self::Transaction,
    ) -> Result<Arc<[AbsencePeriodEntity]>, DaoError> {
        let sp_vec = sales_person_id.as_bytes().to_vec();
        // ISO-8601 YYYY-MM-DD; lex-sort == date-sort.
        let from_str = range.from().format(&time::format_description::well_known::Iso8601::DATE)?;
        let to_str = range.to().format(&time::format_description::well_known::Iso8601::DATE)?;

        Ok(query_as!(
            AbsencePeriodDb,
            "SELECT id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_version \
             FROM absence_period \
             WHERE sales_person_id = ? \
               AND from_date <= ? \
               AND to_date >= ? \
               AND deleted IS NULL \
             ORDER BY from_date",
            sp_vec,
            to_str,   // range.to → from_date <= range.to
            from_str, // range.from → to_date >= range.from
        )
        .fetch_all(tx.tx.lock().await.as_mut())
        .await
        .map_db_error()?
        .iter()
        .map(AbsencePeriodEntity::try_from)
        .collect::<Result<Arc<[_]>, _>>()?)
    }
    ```

    Verifiziere die Imports — `query_as!`, `AbsencePeriodDb`, `map_db_error`, `Iso8601` sind alle bereits in der Datei verwendet (siehe bestehende `find_overlapping`). Bei Bedarf passe Import-Pfad für `Iso8601::DATE` exakt an die bestehende `find_overlapping`-Impl an (gleiche Form).

    3. **sqlx-prepare** ausführen, um `.sqlx/`-Cache zu aktualisieren:
    ```bash
    nix-shell -p sqlx-cli --run "DATABASE_URL=sqlite://./localdb.sqlite3 cargo sqlx prepare --workspace -- --tests"
    ```
    Falls die Lokaldb fehlt oder abweicht: `cd dao_impl_sqlite && nix-shell -p sqlx-cli --run "sqlx db reset --source ../migrations/sqlite -y && DATABASE_URL=sqlite://../localdb.sqlite3 cargo sqlx prepare -- --tests"`. Plan-Phase-Hinweis: Die genaue Form hängt vom lokalen Setup ab — Executor passt an, falls cargo build über fehlenden sqlx-cache klagt.

    4. **Verifiziere** mit `cargo build --workspace`. Bei `error: failed to find sqlx data` oder ähnlichem → sqlx-prepare wiederholen.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -15</automated>
    <automated>grep -n "find_overlapping_for_booking" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao/src/absence.rs | wc -l</automated>
    <automated>grep -n "find_overlapping_for_booking" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao_impl_sqlite/src/absence.rs | wc -l</automated>
    <automated>grep -c "deleted IS NULL" /home/neosam/programming/rust/projects/shifty/shifty-backend/dao_impl_sqlite/src/absence.rs</automated>
    <automated>cargo test -p dao_impl_sqlite 2>&1 | tail -10</automated>
  </verify>
  <done>
    - cargo build --workspace GRÜN (sqlx-Cache OK)
    - dao/src/absence.rs hat `find_overlapping_for_booking` (Verify ≥ 1)
    - dao_impl_sqlite/src/absence.rs hat `find_overlapping_for_booking` (Verify ≥ 1)
    - `WHERE ... deleted IS NULL` ist mehrfach präsent (Pflicht für Pitfall-1; bestehende Queries hatten es schon — neuer SQL-Block muss es enthalten)
    - cargo test -p dao_impl_sqlite GRÜN (Phase-1-DAO-Tests bleiben grün; neuer Test wird in Plan 06 hinzugefügt)
  </done>
</task>

<task type="auto">
  <name>Task 3: `UnavailabilityMarker`-Enum + `ShiftplanDay.unavailable`-Field + `build_shiftplan_day` Default-Setzen</name>
  <read_first>
    - service/src/shiftplan.rs (vollständig)
    - service_impl/src/shiftplan.rs (Z. 24-108 — `build_shiftplan_day`-Helper, exakte ShiftplanDay-Construction-Site)
    - service/src/absence.rs (Z. 27-32 — AbsenceCategory-Layout)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-10, D-Phase3-13)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 273-348 — UnavailabilityMarker + Field-Add-Patches)
  </read_first>
  <files>service/src/shiftplan.rs, service_impl/src/shiftplan.rs</files>
  <action>
    1. **Patch** `service/src/shiftplan.rs`: Füge VOR der bestehenden `pub struct ShiftplanDay`-Definition (Z. 14) ein:

    ```rust
    /// Per-Tag-Marker für die per-sales-person-Sicht (D-Phase3-10). Bei
    /// Doppel-Quelle (AbsencePeriod UND ManualUnavailable am selben Tag) wird
    /// `Both` gesetzt — die `absence_id`/`category` der AbsencePeriod werden
    /// mitgeführt, weil sie semantisch reicher als der bloße ManualUnavailable-
    /// Eintrag sind.
    ///
    /// Granularität: pro Tag genau eine Variante. Slots bleiben sichtbar —
    /// Frontend rendert die Markierung als Per-Tag-Badge zusätzlich zu den
    /// Slots (D-Phase3-10).
    #[derive(Debug, Clone, PartialEq, Eq)]
    pub enum UnavailabilityMarker {
        AbsencePeriod {
            absence_id: uuid::Uuid,
            category: crate::absence::AbsenceCategory,
        },
        ManualUnavailable,
        Both {
            absence_id: uuid::Uuid,
            category: crate::absence::AbsenceCategory,
        },
    }
    ```

    Dann **patch** den bestehenden `ShiftplanDay`-Struct (aktuell Z. 14-18) auf:

    ```rust
    #[derive(Debug, Clone)]
    pub struct ShiftplanDay {
        pub day_of_week: DayOfWeek,
        pub slots: Vec<ShiftplanSlot>,
        /// Phase-3-Marker, gesetzt nur durch die per-sales-person-Sicht
        /// (`get_shiftplan_*_for_sales_person`). Globale `get_shiftplan_*` lassen
        /// das Feld immer `None`.
        pub unavailable: Option<UnavailabilityMarker>,
    }
    ```

    KEINE Trait-Methoden in DIESEM Plan ergänzen — die kommen mit Plan 04 (Wave 3).

    2. **Patch** `service_impl/src/shiftplan.rs`: Suche die `ShiftplanDay { ... }`-Construction-Site im `build_shiftplan_day`-Helper (Z. ~104-107). Füge `unavailable: None,` als zusätzliches Feld hinzu, exakt:

    ```rust
    Ok(ShiftplanDay {
        day_of_week,
        slots: shiftplan_slots,
        unavailable: None, // Phase-3 additiv — globale Sicht setzt nie etwas
    })
    ```

    Such-Hinweis: in `service_impl/src/shiftplan.rs` mit `grep -n "ShiftplanDay {" service_impl/src/shiftplan.rs` die Zeile finden. Es darf weitere Construction-Sites geben (z.B. Tests) — JEDE muss `unavailable: None` ergänzen, sonst Build-Bruch (gewünscht).

    3. **Verifiziere** alle Construction-Sites mit:
    ```bash
    grep -n "ShiftplanDay {" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/shiftplan.rs /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/shiftplan.rs
    ```
    Jede produktive Construction MUSS `unavailable:` enthalten.

    4. KEINE Tests-Anpassungen jenseits der Compile-Fehler — falls bestehende Tests `ShiftplanDay { day_of_week: ..., slots: ... }` konstruieren, MÜSSEN sie um `unavailable: None,` ergänzt werden. Plan-Phase erlaubt Test-Patches ZUR Wahrung der Build-Grünheit.

    5. KEINE neuen Trait-Methoden auf `ShiftplanViewService` — sie kommen mit Plan 04. KEIN `find_overlapping_for_booking` auf `AbsenceService`-Trait — kommt mit Plan 03. Wave 1 ist NUR Domain-Surface (Field/Enum/Domain-Modell).
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -10</automated>
    <automated>grep -c "pub enum UnavailabilityMarker" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/shiftplan.rs</automated>
    <automated>grep -c "pub unavailable: Option<UnavailabilityMarker>" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/shiftplan.rs</automated>
    <automated>grep -nE "ShiftplanDay\s*\{" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/shiftplan.rs | wc -l</automated>
    <automated>cargo test --workspace 2>&1 | tail -15</automated>
  </verify>
  <done>
    - cargo build --workspace GRÜN
    - service/src/shiftplan.rs enthält `pub enum UnavailabilityMarker` (Verify-2 == 1)
    - ShiftplanDay hat das `unavailable`-Feld (Verify-3 == 1)
    - cargo test --workspace bleibt GRÜN (Phase-1+2-Tests laufen weiter; Phase-3-Stubs sind ignored)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DAO read-query | `find_overlapping_for_booking` nimmt `sales_person_id` + Range; SQL-Injection wird durch parametrisierte Query (`query_as!`) ausgeschlossen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-W1-01 | Information Disclosure | `find_overlapping_for_booking` Soft-Delete-Filter fehlt | mitigate | `WHERE deleted IS NULL`-Klausel ist im SQL-Template — Pitfall-1-Test (Plan 06) verifiziert end-to-end. |
| T-3-W1-02 | Tampering | Vergessenes `unavailable: None` in Construction-Sites | mitigate | Field-Add ist Compiler-erzwungen — ohne Patch bricht der Build. Verify-Step grep-zählt Construction-Sites. |
| T-3-W1-03 | Information Disclosure | DAO-Methode liefert kategorie-frei alle Absences (auch Sick); Privacy-konform? | accept | DAO-Layer hat keine Permission-Logik (per Repo-Konvention); Service-Layer (`AbsenceService::find_overlapping_for_booking`, Plan 03) gated mit HR ∨ self. |
</threat_model>

<verification>
Wave-1-Domain-Surface-Verifikation:
- `cargo build --workspace` GRÜN (sqlx-Cache OK; Field-Add bricht nirgends)
- `cargo test --workspace` GRÜN (Phase-1+2-Tests + DAO-Smoke-Tests laufen; Phase-3-Stubs ignored)
- Domain-Surface-Files vorhanden: `service/src/warning.rs` (NEW), `service/src/lib.rs` (PATCH), `dao/src/absence.rs` (PATCH), `dao_impl_sqlite/src/absence.rs` (PATCH), `service/src/shiftplan.rs` (PATCH), `service_impl/src/shiftplan.rs` (PATCH `build_shiftplan_day`)
- BookingService-Files UNANGETASTET: `jj diff service/src/booking.rs service_impl/src/booking.rs` zeigt 0 changes (D-Phase3-18 Regression-Lock — wird in Plan 06 final verifiziert).
</verification>

<success_criteria>
- [ ] `service/src/warning.rs` existiert mit `pub enum Warning` (4 Varianten verbatim aus D-Phase3-14)
- [ ] `service/src/lib.rs` hat `pub mod warning;` alphabetisch korrekt
- [ ] `dao/src/absence.rs` hat `find_overlapping_for_booking` Trait-Methode (kategorie-frei, kein `exclude_logical_id`)
- [ ] `dao_impl_sqlite/src/absence.rs` hat passenden Impl mit `WHERE deleted IS NULL`-Filter
- [ ] `.sqlx/`-Cache ist aktualisiert (cargo build --workspace findet alle prepared queries)
- [ ] `service/src/shiftplan.rs` hat `pub enum UnavailabilityMarker` (3 Varianten) + `ShiftplanDay.unavailable: Option<UnavailabilityMarker>`
- [ ] Alle `ShiftplanDay { ... }`-Construction-Sites enthalten `unavailable: None,`
- [ ] `cargo build --workspace` GRÜN
- [ ] `cargo test --workspace` GRÜN
- [ ] `service/src/booking.rs` unverändert (verify mit `jj diff service/src/booking.rs` — leer)
- [ ] `service_impl/src/booking.rs` unverändert (verify mit `jj diff service_impl/src/booking.rs` — leer)
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-shift-plan-konflikt-integration/03-02-SUMMARY.md` summarizing:
- New: `service/src/warning.rs`
- Patched: `service/src/lib.rs`, `dao/src/absence.rs`, `dao_impl_sqlite/src/absence.rs`, `service/src/shiftplan.rs`, `service_impl/src/shiftplan.rs`, `.sqlx/`
- Confirm BookingService files untouched (D-Phase3-18 Regression-Lock vorbereitet)
- Confirm cargo build + test green
- Note for Plan 03 (Wave 2): Warning enum + AbsenceDao::find_overlapping_for_booking sind verfügbar; AbsenceService kann jetzt Forward-Warnings produzieren.
</output>
