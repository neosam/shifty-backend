---
phase: 03-booking-shift-plan-konflikt-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 02
files_modified:
  - service/src/absence.rs
  - service_impl/src/absence.rs
  - rest/src/absence.rs
  - shifty_bin/src/main.rs
autonomous: true
nyquist_compliant: false
requirements:
  - BOOK-01
  - BOOK-02
must_haves:
  truths:
    - "`AbsenceService::create` und `AbsenceService::update` geben `AbsencePeriodCreateResult` (Wrapper mit `absence: AbsencePeriod` + `warnings: Arc<[Warning]>`) zurück — Sig-Bruch verbatim aus D-Phase3-01 + D-Phase3-04."
    - "`AbsenceService::find_overlapping_for_booking(sales_person_id, range, ctx, tx) -> Result<Arc<[AbsencePeriod]>, ServiceError>` ist als Trait-Methode + Impl präsent; Permission HR ∨ verify_user_is_sales_person (D-Phase3-12). Diese DAO-/Service-Surface enabled BOOK-01 (Forward-Warning) UND BOOK-02 (Reverse-Warning, in Plan 04 von ShiftplanEditService konsumiert)."
    - "`AbsenceServiceImpl` (gen_service_impl!-Block) hat zwei neue Deps: `BookingService` (Basic, für Booking-Loop) und `SalesPersonUnavailableService` (Basic, für ManualUnavailable-Loop) — Direction Business-Logic→Basic, kein Cycle."
    - "`AbsenceServiceImpl::create`/`update` produzieren `Warning::AbsenceOverlapsBooking` pro Booking-Tag in der NEUEN Range (D-Phase3-04: alle Tage, kein Diff) und `Warning::AbsenceOverlapsManualUnavailable` pro überlappendem manuellen Eintrag (D-Phase3-16: KEIN Auto-Cleanup)."
    - "`shifty_bin/src/main.rs` konstruiert `AbsenceServiceImpl` mit den neuen `booking_service.clone()` + `sales_person_unavailable_service.clone()`-Pässen — Konstruktionsreihenfolge bleibt Tier-konform (Basic vor Business-Logic)."
    - "REST-Handler `create_absence_period` + `update_absence_period` rufen den neuen Wrapper-Result-Pfad. Body-Serialisierung erfolgt mit `AbsencePeriodTO` (Plan 05 fügt `AbsencePeriodCreateResultTO` hinzu); für Plan 03 ist der Body temporär `result.absence` (warnings dropped) — markiert mit `// TODO Plan-05: Wrapper-DTO`. Alternativ baut Plan 03 schon ein internes `result.absence`-Mapping ohne Warnings; Plan 05 stellt dann auf den Wrapper um."
    - "Bestehende AbsenceService-Tests (Phase 1+2 in `service_impl/src/test/absence.rs`) bleiben grün, ggf. mit minimalem Patch (`.unwrap().absence` statt `.unwrap()` an Call-Sites — Compiler-erzwungen)."
    - "BookingService-Files (`service/src/booking.rs`, `service_impl/src/booking.rs`, `rest/src/booking.rs`, `service_impl/src/test/booking.rs`) bleiben UNVERÄNDERT (D-Phase3-18 Regression-Lock)."
  artifacts:
    - path: "service/src/absence.rs"
      provides: "AbsencePeriodCreateResult struct + Sig-Brüche create/update + find_overlapping_for_booking Trait-Method (BOOK-01 + BOOK-02 DAO-Surface)"
      contains: "AbsencePeriodCreateResult"
    - path: "service_impl/src/absence.rs"
      provides: "Forward-Warning-Loop + neue Deps + Impl find_overlapping_for_booking"
      contains: "AbsenceOverlapsBooking"
    - path: "shifty_bin/src/main.rs"
      provides: "DI-Wiring für AbsenceServiceImpl mit neuen Basic-Service-Deps"
      contains: "booking_service: booking_service.clone()"
  key_links:
    - from: "service_impl/src/absence.rs"
      to: "service/src/booking.rs (BookingService::get_for_week)"
      via: "self.booking_service.get_for_week"
      pattern: "self\\.booking_service\\.get_for_week"
    - from: "service_impl/src/absence.rs"
      to: "service/src/sales_person_unavailable.rs (get_all_for_sales_person)"
      via: "self.sales_person_unavailable_service.get_all_for_sales_person"
      pattern: "sales_person_unavailable_service\\.get_all_for_sales_person"
    - from: "service_impl/src/absence.rs"
      to: "dao::absence::AbsenceDao::find_overlapping_for_booking"
      via: "self.absence_dao.find_overlapping_for_booking"
      pattern: "absence_dao\\.find_overlapping_for_booking"
---

<objective>
Wave-2-AbsenceService-Erweiterung: Bring den `AbsenceService` auf Phase-3-Form. Sig-Brüche `create`/`update` zu `AbsencePeriodCreateResult`. Neue Trait-Methode `find_overlapping_for_booking` (Service-Surface für BOOK-01 Forward-Warning UND BOOK-02 Reverse-Warning, letzteres konsumiert von ShiftplanEditService in Plan 04). Forward-Warning-Loop in der Impl: pro Booking im Range eine `AbsenceOverlapsBooking`-Warning, pro überlappendem ManualUnavailable eine `AbsenceOverlapsManualUnavailable`-Warning. Erweitere DI um `BookingService` + `SalesPersonUnavailableService`. Verdrahte in `main.rs`. Migriere REST-Handler `create_absence_period` / `update_absence_period` minimal (Wrapper-DTO kommt mit Plan 05 — Plan 03 unwrappen vorerst die `absence`).

Purpose: Forward-Warning-Pfad (BOOK-01) in einem Plan vollständig liefern + DAO/Service-Surface für BOOK-02 ausspielen. AbsenceService → BookingService + SalesPersonUnavailableService Direction (Business-Logic ↑ konsumiert Basic ↓; kein Cycle, da BookingService nichts von AbsenceService weiß per D-Phase3-18).

Output: 4 Files patched.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-VALIDATION.md
@.planning/phases/03-booking-shift-plan-konflikt-integration/03-02-SUMMARY.md
@CLAUDE.md
@CLAUDE.local.md

<interfaces>
<!-- Heutige AbsenceService-Trait Sig — service/src/absence.rs:124-188 -->

```rust
#[automock(type Context=(); type Transaction=dao::MockTransaction;)]
#[async_trait]
pub trait AbsenceService {
    type Context: Clone + Debug + PartialEq + Eq + Send + Sync + 'static;
    type Transaction: dao::Transaction;

    async fn find_all(...) -> Result<Arc<[AbsencePeriod]>, ServiceError>;
    async fn find_by_sales_person(...) -> Result<Arc<[AbsencePeriod]>, ServiceError>;
    async fn find_by_id(...) -> Result<AbsencePeriod, ServiceError>;
    async fn create(...) -> Result<AbsencePeriod, ServiceError>;          // <-- SIG-BRUCH
    async fn update(...) -> Result<AbsencePeriod, ServiceError>;          // <-- SIG-BRUCH
    async fn delete(...) -> Result<(), ServiceError>;
    async fn derive_hours_for_range(...) -> Result<BTreeMap<Date, ResolvedAbsence>, ServiceError>;
}
```

<!-- Heutige gen_service_impl-Form — service_impl/src/absence.rs:39-50 -->

```rust
gen_service_impl! {
    struct AbsenceServiceImpl: AbsenceService = AbsenceServiceDeps {
        AbsenceDao: AbsenceDao<Transaction = Self::Transaction> = absence_dao,
        PermissionService: PermissionService<Context = Self::Context> = permission_service,
        SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,
        ClockService: ClockService = clock_service,
        UuidService: UuidService = uuid_service,
        SpecialDayService: SpecialDayService<Context = Self::Context> = special_day_service,
        EmployeeWorkDetailsService: EmployeeWorkDetailsService<Context = Self::Context, Transaction = Self::Transaction> = employee_work_details_service,
        TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
    }
}
```

<!-- Heutiger create-Body — service_impl/src/absence.rs:137-202 (Anker, NICHT verbatim re-implementieren) -->

Phase-1-Logik bleibt erhalten:
1. Permission HR ∨ self
2. Range-Validation (DateRange::new)
3. Self-Overlap-Check (find_overlapping mit Kategorie + None für create / Some(logical_id) für update)
4. UUID-/Clock-Setup
5. DAO::create (oder update für Tombstone+Insert)
6. commit

Phase-3-Erweiterung: VOR commit ergänze Forward-Warning-Loop, dann gib `AbsencePeriodCreateResult` zurück.

<!-- BookingService::get_for_week-Signatur — service/src/booking.rs:73 (verifiziere mit grep) -->

Existing API:
```rust
async fn get_for_week(
    &self,
    week: u8,
    year: u32,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<Arc<[Booking]>, ServiceError>;
```

<!-- SalesPersonUnavailableService::get_all_for_sales_person-Signatur — service/src/sales_person_unavailable.rs:61-66 -->

```rust
async fn get_all_for_sales_person(
    &self,
    sales_person_id: Uuid,
    context: Authentication<Self::Context>,
    tx: Option<Self::Transaction>,
) -> Result<Arc<[SalesPersonUnavailable]>, ServiceError>;
```

<!-- SlotService::get_slot-Signatur (für Booking → Date-Konversion) -->

Bereits in `service_impl/src/absence.rs` ggf. nicht im DepSet — Phase 3 darf KEIN SlotService-Dep auf AbsenceService legen, weil das eine zusätzliche Tier-Mid-Layer-Dep wäre. Stattdessen: Plan 03 nutzt die `Booking.calendar_week + booking.year` — aber für die Date-Auflösung pro Booking-Tag braucht es day_of_week. **Lösung:** Plan-03-Executor MUSS prüfen, ob `Booking` selbst `slot_id` trägt oder ob `BookingService::get_for_week` mit Slot-Resolution kommt. Falls Slot-Resolution nötig, ergänzt Plan 03 ALTERNATIV einen `SlotService`-Dep auf AbsenceService (Basic-Tier — erlaubt). 

**Vorgabe:** Wenn `BookingService::get_for_week` schon die `Booking`-Struct mit Slot-Felder anreicherter Form liefert (z.B. via JOIN im DAO), nutze das. Sonst füge `SlotService` als Dep ergänzend zum DepSet hinzu (analog zur Pattern-Vorlage in 03-PATTERNS.md Z. 491-498).

<!-- Existing Slot-Fixture-Pattern — service_impl/src/test/shiftplan.rs:32-45 -->

```rust
pub fn default_slot() -> Slot {
    Slot {
        id: default_slot_id(),
        day_of_week: DayOfWeek::Monday,
        from: Time::from_hms(9, 0, 0).unwrap(),
        to: Time::from_hms(17, 0, 0).unwrap(),
        min_resources: 1,
        valid_from: Date::from_calendar_date(2024, Month::January, 1).unwrap(),
        valid_to: None,
        deleted: None,
        version: default_slot_version(),
        shiftplan_id: None,
    }
}
```

Plan 03 verwendet diese Form als Vorlage für eine lokale `default_slot_monday()`-Helper-Funktion in `test/absence.rs`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AbsenceService-Trait erweitern — `AbsencePeriodCreateResult` + Sig-Brüche + `find_overlapping_for_booking`</name>
  <read_first>
    - service/src/absence.rs (vollständig — Z. 1-260 lesen)
    - service/src/warning.rs (NEU aus Plan 02)
    - service/src/booking.rs (Z. 60-100 — `get_for_week`-Signatur verifizieren)
    - service/src/sales_person_unavailable.rs (Z. 61-66 — `get_all_for_sales_person`-Signatur)
    - service/src/sales_person.rs (Z. 124-129 — `verify_user_is_sales_person`-Signatur)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (D-Phase3-01, D-Phase3-04, D-Phase3-12)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 209-271 — Wrapper-Struct + Sig-Brüche + neue Trait-Method)
    - shifty-backend/CLAUDE.md (§ "Service-Tier-Konventionen" — AbsenceService ist Business-Logic; darf BookingService konsumieren)
  </read_first>
  <files>service/src/absence.rs</files>
  <behavior>
    - Test 1: `MockAbsenceService::expect_find_overlapping_for_booking()` ist verfügbar (mockall via #[automock]).
    - Test 2: `AbsencePeriodCreateResult { absence, warnings }` ist konstruierbar mit Arc::from(Vec::<Warning>::new()) → empty warnings (Smoke).
    - Test 3: `AbsenceService::create`-Trait-Sig erzwingt Wrapper-Return — alle Konsumenten müssen patchen.
  </behavior>
  <action>
    1. **Patch** `service/src/absence.rs`:

    a) Füge `pub struct AbsencePeriodCreateResult` VOR dem `pub trait AbsenceService` (vor Z. 124) ein:

    ```rust
    /// Output von `AbsenceService::create` und `AbsenceService::update`.
    /// Enthält die persistierte AbsencePeriod + alle Forward-Warnings für die
    /// NEUE Range (D-Phase3-04: kein Diff-Modus, alle Bookings + ManualUnavailables
    /// in der neuen Range produzieren Warnings).
    #[derive(Debug, Clone)]
    pub struct AbsencePeriodCreateResult {
        pub absence: AbsencePeriod,
        pub warnings: std::sync::Arc<[crate::warning::Warning]>,
    }
    ```

    b) Patch `create` (Z. 154-159) — neue Sig:
    ```rust
    async fn create(
        &self,
        entity: &AbsencePeriod,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<AbsencePeriodCreateResult, ServiceError>;
    ```

    c) Patch `update` (Z. 161-166) — symmetrisch:
    ```rust
    async fn update(
        &self,
        entity: &AbsencePeriod,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<AbsencePeriodCreateResult, ServiceError>;
    ```

    d) Füge NACH `update` (vor `delete`, Z. ~166) eine neue Trait-Methode ein:
    ```rust
    /// Findet alle aktiven AbsencePeriods derselben `sales_person_id`, die
    /// `range` überlappen — kategorie-frei (alle 3 AbsenceCategory-Werte).
    /// Für `ShiftplanEditService::book_slot_with_conflict_check`-Reverse-
    /// Warning + interner Konsumenten. Permission HR ∨
    /// `verify_user_is_sales_person(sales_person_id)` (D-Phase3-12).
    async fn find_overlapping_for_booking(
        &self,
        sales_person_id: Uuid,
        range: shifty_utils::DateRange,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<Arc<[AbsencePeriod]>, ServiceError>;
    ```

    Verifiziere `Arc` import — `use std::sync::Arc;` muss am Datei-Anfang stehen (verifiziere mit grep).

    2. KEINE Impl-Anpassungen in DIESEM Task — Task 2 macht das. Die Datei wird NICHT kompilieren bis Task 2 abgeschlossen ist. Das ist OK — `cargo build -p service` kann partiell scheitern, der workspace-Build wird in Task 2/3/4 fertig.

    3. KEINE anderen Files anfassen.
  </action>
  <verify>
    <automated>cargo build -p service 2>&1 | tail -20</automated>
    <automated>grep -c "pub struct AbsencePeriodCreateResult" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/absence.rs</automated>
    <automated>grep -c "find_overlapping_for_booking" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/absence.rs</automated>
    <automated>grep -E "Result<AbsencePeriodCreateResult" /home/neosam/programming/rust/projects/shifty/shifty-backend/service/src/absence.rs | wc -l</automated>
  </verify>
  <done>
    - `service/src/absence.rs` hat AbsencePeriodCreateResult-Struct (Verify-2 == 1)
    - hat find_overlapping_for_booking-Trait-Method (Verify-3 ≥ 1)
    - hat 2x `Result<AbsencePeriodCreateResult` (Verify-4 == 2 — create + update)
    - cargo build -p service KANN scheitern (Impl noch nicht angepasst — wird in Task 2 fixed)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AbsenceServiceImpl — gen_service_impl + Forward-Warning-Loop in create/update + find_overlapping_for_booking-Impl + Test-Anpassungen</name>
  <read_first>
    - service_impl/src/absence.rs (Z. 1-300 — vollständig lesen)
    - service_impl/src/test/absence.rs (Z. 121-300 — bestehende Test-Struktur, Mock-Setup-Idiom)
    - service_impl/src/test/shiftplan.rs (Z. 20-45 — `default_slot()`-Fixture-Pattern als Vorlage)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 432-589 — gen_service_impl + Forward-Warning-Code)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-CONTEXT.md (Skizze in `<specifics>`, Z. 461-541 PATTERNS)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-RESEARCH.md (Operation 2 — Forward-Warning-Loop)
    - shifty-utils/src/date_range.rs (DateRange::iter_days, .contains, .from(), .to())
    - service/src/booking.rs (Booking-Struct: id, sales_person_id, slot_id, year, calendar_week)
    - service/src/sales_person_unavailable.rs (SalesPersonUnavailable-Struct: id, sales_person_id, year, calendar_week, day_of_week)
    - shifty-backend/CLAUDE.md (§ "Service-Tier-Konventionen" + § "Transaction Management")
  </read_first>
  <files>service_impl/src/absence.rs, service_impl/src/test/absence.rs</files>
  <action>
    1. **Patch** `service_impl/src/absence.rs` (TDD reichen für Forward-Warning-Logik aus — Tests werden Compile-Errors entdecken):

    a) `gen_service_impl!`-Block (Z. 39-50) erweitern um zwei neue Dep-Felder. Falls SlotService-Dep nötig (für `Booking → Slot.day_of_week`-Auflösung pro Booking, weil `Booking` selbst kein day_of_week trägt — verifiziere mit grep "pub struct Booking" service/src/booking.rs), als drittes Feld ergänzen:

    ```rust
    gen_service_impl! {
        struct AbsenceServiceImpl: AbsenceService = AbsenceServiceDeps {
            AbsenceDao: AbsenceDao<Transaction = Self::Transaction> = absence_dao,
            PermissionService: PermissionService<Context = Self::Context> = permission_service,
            SalesPersonService: SalesPersonService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_service,
            ClockService: ClockService = clock_service,
            UuidService: UuidService = uuid_service,
            SpecialDayService: SpecialDayService<Context = Self::Context> = special_day_service,
            EmployeeWorkDetailsService: EmployeeWorkDetailsService<Context = Self::Context, Transaction = Self::Transaction> = employee_work_details_service,
            TransactionDao: TransactionDao<Transaction = Self::Transaction> = transaction_dao,
            // NEU für Phase 3 (D-Phase3-08):
            BookingService: service::booking::BookingService<Context = Self::Context, Transaction = Self::Transaction> = booking_service,
            SalesPersonUnavailableService: service::sales_person_unavailable::SalesPersonUnavailableService<Context = Self::Context, Transaction = Self::Transaction> = sales_person_unavailable_service,
            SlotService: service::slot::SlotService<Transaction = Self::Transaction> = slot_service,
        }
    }
    ```

    Hinweis: `SlotService::get_slot`-Signatur verifizieren mit grep — gleiche Form wie heute in `shiftplan_edit.rs` benutzt.

    b) `create` patchen (Z. 137-202) — Return-Type wird `Result<AbsencePeriodCreateResult, ServiceError>`. Nach der bestehenden DAO-`create`-Zeile (Z. ~197), VOR dem `commit`, einfügen:

    ```rust
    // Phase 3 — Forward-Warning-Loop (D-Phase3-04: alle Tage in NEUER Range)
    let mut warnings: Vec<service::warning::Warning> = Vec::new();
    let new_range = shifty_utils::DateRange::new(entity.from_date, entity.to_date)
        .map_err(|_| ServiceError::DateOrderWrong(entity.from_date, entity.to_date))?;

    // 1) Bookings im Range — Loop über betroffene Kalenderwochen (C-Phase3-02 Default)
    let mut weeks_seen: std::collections::BTreeSet<(u32, u8)> = std::collections::BTreeSet::new();
    for day in new_range.iter_days() {
        let (iso_year, iso_week, _weekday) = day.to_iso_week_date();
        if !weeks_seen.insert((iso_year as u32, iso_week as u8)) {
            continue;
        }
        let bookings = self
            .booking_service
            .get_for_week(iso_week as u8, iso_year as u32, Authentication::Full, tx.clone().into())
            .await?;
        for b in bookings.iter() {
            if b.sales_person_id != entity.sales_person_id {
                continue;
            }
            let slot = self
                .slot_service
                .get_slot(&b.slot_id, Authentication::Full, tx.clone().into())
                .await?;
            let booking_date = time::Date::from_iso_week_date(
                b.year as i32,
                b.calendar_week as u8,
                slot.day_of_week.into(),
            )?;
            if !new_range.contains(booking_date) {
                continue;
            }
            warnings.push(service::warning::Warning::AbsenceOverlapsBooking {
                absence_id: entity.id,
                booking_id: b.id,
                date: booking_date,
            });
        }
    }

    // 2) ManualUnavailables im Range — clientside-Filter
    let manual_all = self
        .sales_person_unavailable_service
        .get_all_for_sales_person(
            entity.sales_person_id,
            Authentication::Full,
            tx.clone().into(),
        )
        .await?;
    for mu in manual_all.iter() {
        if mu.deleted.is_some() {
            continue;
        }
        let mu_date = match time::Date::from_iso_week_date(
            mu.year as i32,
            mu.calendar_week as u8,
            mu.day_of_week.into(),
        ) {
            Ok(d) => d,
            Err(_) => continue,
        };
        if !new_range.contains(mu_date) {
            continue;
        }
        warnings.push(service::warning::Warning::AbsenceOverlapsManualUnavailable {
            absence_id: entity.id,
            unavailable_id: mu.id,
        });
    }
    ```

    Dann am Methoden-Ende `commit` + Return:
    ```rust
    self.transaction_dao.commit(tx).await?;
    Ok(AbsencePeriodCreateResult {
        absence: entity,  // bzw. die persistierte Form, je nach existing Body-Variable
        warnings: std::sync::Arc::from(warnings),
    })
    ```

    Verifiziere: existing create-Body produziert `entity` als finale `AbsencePeriod`-Variable (kann `entity` heißen oder `created_entity` o.ä. — exakte Variable an die existing Logik anpassen).

    c) `update` patchen (Z. ~204-260) — symmetrisch zu `create`. Forward-Warning-Loop läuft NACH der Tombstone+Insert-Logik (also nach DAO-update + DAO-create), VOR `commit`. Die `entity.id` aus `create` durch `active.logical_id` ersetzen (oder die neue `update_id` — die verbleibende ID nach Tombstone+Insert). Anatomie identisch zur `create`-Erweiterung.

    Plan-Phase erlaubt Refactor in privaten Helper:
    ```rust
    async fn compute_forward_warnings(
        &self,
        absence_id: Uuid,
        sales_person_id: Uuid,
        new_range: DateRange,
        tx: <Self as service::absence::AbsenceService>::Transaction,
    ) -> Result<Vec<service::warning::Warning>, ServiceError> {
        // ... gleiche Logik wie oben, parametrisiert auf absence_id ...
    }
    ```
    Wenn extrahiert, MUSS `create` und `update` denselben Helper rufen — DRY.

    d) NEUE Methode `find_overlapping_for_booking` ergänzen (am Ende der `impl ... for AbsenceServiceImpl<Deps>`-Block):

    ```rust
    async fn find_overlapping_for_booking(
        &self,
        sales_person_id: Uuid,
        range: shifty_utils::DateRange,
        context: Authentication<Self::Context>,
        tx: Option<Self::Transaction>,
    ) -> Result<Arc<[AbsencePeriod]>, ServiceError> {
        let tx = self.transaction_dao.use_transaction(tx).await?;
        // Permission HR ∨ self
        let (hr, sp) = join!(
            self.permission_service.check_permission(HR_PRIVILEGE, context.clone()),
            self.sales_person_service.verify_user_is_sales_person(
                sales_person_id, context, tx.clone().into()
            ),
        );
        hr.or(sp)?;

        let entities = self
            .absence_dao
            .find_overlapping_for_booking(sales_person_id, range, tx.clone())
            .await?;
        let result: Arc<[AbsencePeriod]> = entities.iter().map(AbsencePeriod::from).collect();
        self.transaction_dao.commit(tx).await?;
        Ok(result)
    }
    ```

    Verifiziere: `AbsencePeriod::from(&AbsencePeriodEntity)` ist in Phase 1 definiert (`service/src/absence.rs` oder via `From`-Impl) — verifiziere mit grep.

    e) Imports am Datei-Anfang ergänzen, falls fehlt: `use service::warning;` (für `service::warning::Warning`-Pfad — Hinweis: file ist `service_impl`-crate, also direkt `service::warning::Warning`).

    2. **Patch** `service_impl/src/test/absence.rs`:

    a) `AbsenceDependencies`-Struct (Z. 121-194) erweitern um die zwei neuen Mock-Felder + ggf. `MockSlotService`:
    ```rust
    pub(crate) struct AbsenceDependencies {
        // ... bestehende 8 Felder ...
        // NEU für Phase 3:
        pub booking_service: MockBookingService,
        pub sales_person_unavailable_service: MockSalesPersonUnavailableService,
        pub slot_service: MockSlotService,
    }
    ```

    b) `build_dependencies` erweitern um Default-Returns. **Wichtig:** Default für `slot_service.get_slot` MUSS einen echten Slot-Wert zurückgeben (kein `unimplemented!()`), sonst panicken Bestand-Tests, die SlotService nie überschreiben (z.B. reine Self-Overlap-Tests, die niemals Booking-Iteration triggern, aber auch jeder Override-Test, der vergisst, slot_service zu setzen). Lokale Helper-Funktion `default_slot_monday()` einführen (Felder analog zu `service_impl/src/test/shiftplan.rs:32-45`):

    ```rust
    /// Default-Slot-Fixture für AbsenceService-Tests. Tests die einen spezifischen
    /// Slot brauchen (z.B. um day_of_week zu variieren), überschreiben
    /// `slot_service.expect_get_slot()` lokal — sonst greift dieser Default,
    /// damit Bestand-Tests OHNE Override (z.B. reine Self-Overlap-Tests) nicht panicken.
    fn default_slot_monday() -> service::slot::Slot {
        service::slot::Slot {
            id: uuid::uuid!("7A7FF57A-782B-4C2E-A68B-4E2D81D79380"),
            day_of_week: shifty_utils::DayOfWeek::Monday,
            from: time::Time::from_hms(9, 0, 0).unwrap(),
            to: time::Time::from_hms(17, 0, 0).unwrap(),
            min_resources: 1,
            valid_from: time::Date::from_calendar_date(2024, time::Month::January, 1).unwrap(),
            valid_to: None,
            deleted: None,
            version: uuid::Uuid::new_v4(),
            shiftplan_id: None,
        }
    }
    ```

    Default-Returns:
    ```rust
    let mut booking_service = MockBookingService::new();
    booking_service.expect_get_for_week().returning(|_, _, _, _| Ok(std::sync::Arc::from([])));
    let mut sales_person_unavailable_service = MockSalesPersonUnavailableService::new();
    sales_person_unavailable_service
        .expect_get_all_for_sales_person()
        .returning(|_, _, _| Ok(std::sync::Arc::from([])));
    let mut slot_service = MockSlotService::new();
    slot_service
        .expect_get_slot()
        .returning(|_, _, _| Ok(default_slot_monday()));  // Default-Slot statt unimplemented!()
    ```

    Verifikation: existing `Slot`-Felder gegen `service/src/slot.rs:13` prüfen — wenn ein Feld renamed/ergänzt wurde, lokales `default_slot_monday()` entsprechend anpassen.

    c) `build_service` (analog Z. 146-157) ergänzen um `into()`-Pässe für die drei neuen Felder.

    d) Bestehende Tests, die `service.create(...).await.unwrap()` auf einen `AbsencePeriod` direkt erwarten, MÜSSEN angepasst werden zu `service.create(...).await.unwrap().absence` (Compiler-erzwungen). Plan-Phase erlaubt mass-replace via Editor-Suche; Executor verifiziert mit `cargo test -p service_impl test::absence`.

    3. **Verifiziere**: `cargo test -p service_impl test::absence` GRÜN. Bestehende Phase-1+2-AbsenceService-Tests müssen weiter passieren (mit minimalem Patch).

    4. KEINE Anpassungen an `service_impl/src/test/absence_derive_hours_range.rs` jenseits von `.unwrap().absence`-Anpassungen falls `create`/`update` dort gerufen werden.
  </action>
  <verify>
    <automated>cargo build -p service_impl 2>&1 | tail -20</automated>
    <automated>cargo test -p service_impl test::absence 2>&1 | tail -15</automated>
    <automated>grep -c "AbsenceOverlapsBooking" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/absence.rs</automated>
    <automated>grep -c "AbsenceOverlapsManualUnavailable" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/absence.rs</automated>
    <automated>grep -c "find_overlapping_for_booking" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/absence.rs</automated>
    <automated>grep -c "booking_service: MockBookingService" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/absence.rs</automated>
    <automated>grep -c "fn default_slot_monday" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/absence.rs</automated>
    <automated>grep -c "unimplemented!" /home/neosam/programming/rust/projects/shifty/shifty-backend/service_impl/src/test/absence.rs</automated>
  </verify>
  <done>
    - cargo build -p service_impl GRÜN
    - cargo test -p service_impl test::absence GRÜN (bestehende Phase-1+2 AbsenceService-Tests + neue ggf. mit Default-Returns)
    - service_impl/src/absence.rs enthält Forward-Warning-Loop (Verify ≥ 1 für jede Warning-Variante)
    - service_impl/src/absence.rs enthält find_overlapping_for_booking-Impl (Verify ≥ 1)
    - test/absence.rs hat MockBookingService-Feld (Verify == 1)
    - test/absence.rs hat `default_slot_monday()`-Fixture-Helper (Verify-7 ≥ 1)
    - test/absence.rs hat KEINE `unimplemented!()`-Marker im Mock-Setup (Verify-8 == 0; jede `unimplemented!()`-Spur in build_dependencies wäre eine Test-Falle)
  </done>
</task>

<task type="auto">
  <name>Task 3: REST-Handler `create_absence_period` + `update_absence_period` minimal anpassen + DI-Wiring in main.rs</name>
  <read_first>
    - rest/src/absence.rs (heutige Form vollständig)
    - shifty_bin/src/main.rs (Z. 678-746 — Konstruktion von booking_service, sales_person_unavailable_service, slot_service, absence_service)
    - .planning/phases/03-booking-shift-plan-konflikt-integration/03-PATTERNS.md (Z. 1086-1138 — REST-Handler-Diff; Z. 829-892 — main.rs-Diff)
    - shifty-backend/CLAUDE.md (§ "Service-Tier-Konventionen" — Konstruktionsreihenfolge: Basic vor Business-Logic)
  </read_first>
  <files>rest/src/absence.rs, shifty_bin/src/main.rs</files>
  <action>
    1. **Patch** `rest/src/absence.rs`:

    a) `create_absence_period` (Z. ~43-74) — `result` ist jetzt `AbsencePeriodCreateResult`. **Plan 03 minimaler Diff** (Wrapper-DTO kommt mit Plan 05 — Plan 03 unwrappt vorerst und droppt Warnings, markiert mit TODO):

    ```rust
    let result = svc.create(&(&body).into(), context.into(), None).await?;
    // TODO Plan-05: AbsencePeriodCreateResultTO statt AbsencePeriodTO im Body
    // Phase-3-Plan-03 dropt Warnings im Body — Frontend ist auf der alten Form,
    // bis Plan 05 den Wrapper-DTO ausspielt.
    let to = AbsencePeriodTO::from(&result.absence);
    ```

    Status-Code bleibt 201, Body-Form bleibt `AbsencePeriodTO` (kein OpenAPI-Bruch in Plan 03).

    b) `update_absence_period` (Z. ~152-174) — symmetrisch:
    ```rust
    let result = svc.update(&(&body).into(), context.into(), None).await?;
    let to = AbsencePeriodTO::from(&result.absence);
    ```

    c) KEINE ApiDoc-Änderung in DIESEM Plan — Plan 05 macht das.

    2. **Patch** `shifty_bin/src/main.rs`:

    a) Suche die `let absence_service = Arc::new(service_impl::absence::AbsenceServiceImpl { ... })`-Konstruktion (Z. ~737). Ergänze die zwei (oder drei, falls SlotService) neuen Felder am Ende des Struct-Literals:

    ```rust
    let absence_service = Arc::new(service_impl::absence::AbsenceServiceImpl {
        absence_dao,
        permission_service: permission_service.clone(),
        sales_person_service: sales_person_service.clone(),
        clock_service: clock_service.clone(),
        uuid_service: uuid_service.clone(),
        special_day_service: special_day_service.clone(),
        employee_work_details_service: working_hours_service.clone(),
        transaction_dao: transaction_dao.clone(),
        // NEU für Phase 3:
        booking_service: booking_service.clone(),
        sales_person_unavailable_service: sales_person_unavailable_service.clone(),
        slot_service: slot_service.clone(),
    });
    ```

    b) **Verifiziere Konstruktionsreihenfolge**: 
    ```bash
    grep -n "let booking_service\|let sales_person_unavailable_service\|let slot_service\|let absence_service" /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty_bin/src/main.rs
    ```
    Erwartete Reihenfolge:
    - sales_person_unavailable_service (Z. ~678) — Basic
    - booking_service (Z. ~699) — Basic
    - slot_service (irgendwo VOR absence_service — verifiziere)
    - absence_service (Z. ~737) — Business-Logic (erst NACH allen Basic-Deps)

    Falls `slot_service` NACH `absence_service` konstruiert wird, **MUSS** Plan 03 die Konstruktion vorziehen (move slot_service-block VOR absence_service). KEINE OnceLock-Tricks erlaubt (per § "Service-Tier-Konventionen").

    3. KEINE Änderungen in `shiftplan_edit_service`-Konstruktion oder `shiftplan_view_service`-Konstruktion — die kommen mit Plan 04.

    4. **Verifiziere** mit `cargo build --workspace`. Bei Compile-Fehler in shiftplan_edit_service oder shiftplan_view_service (weil sie evtl. AbsenceService verwenden ohne neue Form) → solange diese Files Plan-04-territorium sind, ist es OK; aber alle Phase-1+2-Pfade MÜSSEN kompilieren.

    Hinweis: Falls `shiftplan_edit_service` oder andere existing Services `AbsenceService::create` rufen (verify mit grep), müssen sie minimal angepasst werden zu `.create(...).await?.absence` — das ist ein Compiler-Bruch durch den Sig-Bruch, gewünscht.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -20</automated>
    <automated>cargo test --workspace 2>&1 | tail -15</automated>
    <automated>grep -c "result.absence" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest/src/absence.rs</automated>
    <automated>grep -c "booking_service: booking_service.clone()" /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty_bin/src/main.rs</automated>
    <automated>jj diff service/src/booking.rs service_impl/src/booking.rs 2>&1 | wc -l</automated>
    <automated>jj diff rest/src/booking.rs service_impl/src/test/booking.rs 2>&1 | wc -l</automated>
  </verify>
  <done>
    - cargo build --workspace GRÜN
    - cargo test --workspace GRÜN (Phase-1+2-Tests + neue Phase-3-Forward-Warnings — letztere noch ohne dedizierte Tests, die kommen Plan 06)
    - REST-Handler `create_absence_period`/`update_absence_period` rufen den Wrapper-Pfad und unwrappen `.absence` (Verify-3 ≥ 2)
    - main.rs verdrahtet `absence_service` mit `booking_service.clone()` (Verify-4 ≥ 1, gemeinsam mit shiftplan_edit/shiftplan_view die schon clones nutzen — exakte Zahl ist hier ≥ 4)
    - **D-Phase3-18 Regression-Lock vorbereitet:** `jj diff service/src/booking.rs service_impl/src/booking.rs` produziert 0 Zeilen Output (Verify-5 == 0)
    - `jj diff rest/src/booking.rs service_impl/src/test/booking.rs` produziert 0 Zeilen Output (Verify-6 == 0)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → AbsenceService Public-API | `create`/`update`/`find_overlapping_for_booking`-Calls; Permission HR ∨ self gated im Service-Layer (D-Phase3-12). |
| AbsenceService → BookingService (intern) | `Authentication::Full`-Bypass; outer Permission ist oben verifiziert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-AbsServ-Read | Information Disclosure | `find_overlapping_for_booking` exposed Absence-IDs einer fremden sales_person_id | mitigate | Permission HR ∨ `verify_user_is_sales_person(sales_person_id)` (D-Phase3-12) — gleiche Regel wie Phase-1 D-09. _forbidden-Test in Plan 06. |
| T-3-Forward-Loop | Denial of Service | 60-Tage-Absence triggert 60×N Booking-Reads (Performance) | accept | C-Phase3-02 (deferred); Default = N-Calls. Plan 06 verifiziert Performance — wenn Test-Druck, optimieren. |
| T-3-Cycle-Risk | Tampering | Versehentlich BookingService-Dep auf Domain-Service | mitigate | D-Phase3-18 Regression-Lock: `jj diff service_impl/src/booking.rs` MUSS leer bleiben — verify in Task 3. |
| T-3-Soft-Del-Drop | Information Disclosure | ManualUnavailable-Loop bypass `deleted IS NULL` | mitigate | Forward-Warning-Loop hat `if mu.deleted.is_some() { continue; }`-Guard explizit. Pitfall-1-Test (Plan 06) full-stack-verifiziert. |
</threat_model>

<verification>
Wave-2-AbsenceService-Verifikation:
- `cargo build --workspace` GRÜN
- `cargo test --workspace` GRÜN
- AbsenceService-Trait + Impl + REST + DI ist Phase-3-konform (Wrapper + neue Deps + neue Methoden)
- BookingService UNVERÄNDERT (D-Phase3-18 Regression-Lock — `jj diff` empty)
- Phase-1+2 AbsenceService-Tests passen mit minimalen `.unwrap().absence`-Patches
</verification>

<success_criteria>
- [ ] `service/src/absence.rs` hat `AbsencePeriodCreateResult` + Sig-Brüche `create`/`update` + `find_overlapping_for_booking`-Trait-Method (Service-Surface für BOOK-01 + BOOK-02)
- [ ] `service_impl/src/absence.rs` hat erweiterten gen_service_impl + Forward-Warning-Loop in `create` und `update` + Impl `find_overlapping_for_booking` + Permission HR ∨ self
- [ ] Forward-Warning-Loop produziert `Warning::AbsenceOverlapsBooking` und `Warning::AbsenceOverlapsManualUnavailable` korrekt
- [ ] `rest/src/absence.rs` ruft den Wrapper-Pfad und unwrappt `.absence` für Body (Plan 05 ergänzt full Wrapper-DTO)
- [ ] `shifty_bin/src/main.rs` verdrahtet AbsenceServiceImpl mit den neuen Basic-Deps; Konstruktionsreihenfolge bleibt Tier-konform
- [ ] `service_impl/src/test/absence.rs` hat erweiterte AbsenceDependencies inkl. lokale `default_slot_monday()`-Fixture; Default-Mock von `slot_service.get_slot` returniert echten Slot (kein `unimplemented!()`); bestehende Tests passieren mit minimalen `.unwrap().absence`-Anpassungen
- [ ] **D-Phase3-18 Regression-Lock**: `jj diff service/src/booking.rs service_impl/src/booking.rs rest/src/booking.rs service_impl/src/test/booking.rs` produziert 0 Zeilen
- [ ] `cargo build --workspace` + `cargo test --workspace` GRÜN
</success_criteria>

<output>
After completion, create `.planning/phases/03-booking-shift-plan-konflikt-integration/03-03-SUMMARY.md` summarizing:
- Patched: service/src/absence.rs (Wrapper + Sig-Brüche + Trait-Method — BOOK-01 + BOOK-02 Service-Surface)
- Patched: service_impl/src/absence.rs (gen_service_impl + Forward-Warning-Loop + find_overlapping_for_booking-Impl)
- Patched: rest/src/absence.rs (Wrapper-Result-Pfad mit `.absence`-Unwrap; Plan-05-TODO)
- Patched: shifty_bin/src/main.rs (DI für AbsenceService mit neuen Basic-Deps)
- Patched: service_impl/src/test/absence.rs (AbsenceDependencies + Default-Mocks + lokales `default_slot_monday()`-Fixture)
- Confirmation: BookingService 4 Files unangetastet (D-Phase3-18 Regression-Lock)
- Note for Plan 04: AbsenceService::find_overlapping_for_booking ist verfügbar; ShiftplanEditService kann Reverse-Warnings produzieren (BOOK-02).
</output>
</output>
