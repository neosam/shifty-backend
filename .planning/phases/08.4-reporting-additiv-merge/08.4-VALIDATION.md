---
phase: 8.4
slug: reporting-additiv-merge
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-09
---

# Phase 8.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: 08.4-RESEARCH.md § Validierungsarchitektur (HIGH confidence, live-code verified).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `tokio::test` + `mockall` (unit) + in-memory SQLite (integration) |
| **Config file** | none — workspace `Cargo.toml`, tests under `service_impl/src/test/` + `shifty_bin/src/integration_test/` |
| **Quick run command** | `cargo test -p service_impl` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~60–180 seconds (workspace) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p service_impl`
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~180 seconds

---

## Per-Task Verification Map

> Task IDs are assigned by the planner. The rows below map the phase's known
> observable behaviors (from RESEARCH § Validierungsarchitektur) to test types.
> The planner MUST attach an `<automated>` verify command to each task covering these.

| Behavior | Wave | Requirement / SC | Threat Ref | Secure Behavior | Test Type | Automated Command | File | Status |
|----------|------|------------------|------------|-----------------|-----------|-------------------|------|--------|
| Additive Summe: nur `extra_hours` → vacation_hours = ExtraHours-Summe + 0 | 1 | SC-1 | — | N/A | unit (mockall) | `cargo test -p service_impl reporting_additive` | `reporting_additive_merge.rs` | ⬜ pending |
| Additive Summe: nur `absence_period` → vacation_hours = derive-Summe + 0 | 1 | SC-1 | — | N/A | unit (mockall) | `cargo test -p service_impl reporting_additive` | `reporting_additive_merge.rs` | ⬜ pending |
| Additive Summe: beide Quellen, distinkte Tage → Summe beider (Kern-SC-1) | 1 | SC-1 | — | N/A | unit (mockall) | `cargo test -p service_impl reporting_additive` | `reporting_additive_merge.rs` | ⬜ pending |
| Konvertierte (soft-deleted) Rows zählen nicht doppelt | 1 | SC-1 | — | per-row Quellen-Invariante hält strukturell (`deleted IS NULL`-Load) | unit (mockall) | `cargo test -p service_impl reporting_additive` | `reporting_additive_merge.rs` | ⬜ pending |
| Vacation/Sick/Unpaid-`extra_hours` wieder anlegbar (Gate entfernt) | 1 | SC-2 | — | kein `ExtraHoursCategoryDeprecated` aus `create()` mehr | unit (mockall) | `cargo test -p service_impl extra_hours` | `extra_hours.rs` | ⬜ pending |
| Snapshot-Schema-Version = 5 | 1 | SC-3 | — | N/A | unit | `cargo test -p service_impl billing_period_snapshot_locking` | `billing_period_snapshot_locking.rs` | ⬜ pending |
| Snapshot-Regression sauber (alte Snapshots als „older schema") | 1 | SC-4 | — | N/A | unit/integration | `cargo test --workspace` | billing_period snapshot tests | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 here is **destructive test cleanup** (existing tests assert the now-removed flag/gate model and break immediately). These MUST land before or together with the code change:

- [ ] `reporting_flag_off_bit_identity.rs` — delete or fully replace (asserts `derive_hours_for_range.times(0)`; breaks instantly after additive merge).
- [ ] `reporting_flag_on_integration.rs` — rename + rewrite onto the additive model (or remove if subsumed by new `reporting_additive_merge.rs`).
- [ ] `reporting_additive_merge.rs` — NEW test file for the three additive scenarios + no-double-count.
- [ ] `billing_period_snapshot_locking.rs` — `assert_eq!(4, …)` → `assert_eq!(5, …)`.
- [ ] `extra_hours.rs` Test `create_vacation_returns_403_error_variant_when_flag_on` — delete; add/keep a "create Ok" assertion for the un-gated path.
- [ ] `cutover.rs` integration test `test_403_body_format_for_deprecated_category` (~Z. 660–789) — delete or rewrite to "create Ok".

*Existing in-memory-SQLite + mockall infrastructure covers all phase behaviors — no new framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| — | — | All phase behaviors have automated verification. | — |

*All phase behaviors have automated verification (unit + integration).*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (broken legacy tests replaced)
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
