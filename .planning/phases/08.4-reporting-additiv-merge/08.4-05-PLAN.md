---
phase: 08.4-reporting-additiv-merge
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - service_impl/src/reporting.rs
  - service_impl/src/test/reporting_additive_merge.rs
  - service_impl/src/test/reporting_phase2_fixtures.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Fuer DYNAMISCHE Vertraege (is_dynamic=true) bewegt eine reine absence_period-Buchung balance_hours IDENTISCH (~0, innerhalb 0.01) zum aequivalenten extra_hours-Eintrag — QUELLEN-UNABHAENGIG — in get_report_for_employee_range, get_reports_for_all_employees UND get_week."
    - "Die absence_period-derived Balance-Reduktion ist pro Woche auf 0.0 gegated, wenn die Woche keine Vertragsarbeitszeit hat (expected_hours <= 0.0 / planned_hours <= 0.0) — symmetrisch zur extra_hours-absense_hours-Behandlung (Z.263 / Z.976) und zur Referenz hours_per_week (Z.988-996)."
    - "Die DISPLAY-Felder (vacation_hours / sick_leave_hours / unpaid_leave_hours) bleiben in allen drei Methoden UNGEGATET additiv — die derived Stunden erscheinen weiterhin in den Display-Spalten unabhaengig vom Vertragstyp (Truth 3 & 4 bleiben gruen: test_all_employees_additive_merge=12h, test_get_week_additive_merge=11h)."
    - "CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 6 — dieser Fix vervollstaendigt die in Plan 04 eingefuehrte v6-Computation korrekt (kein neuer persistierter Ruleset), daher KEIN weiterer Bump 6->7. Der Pin-Test snapshot_schema_version_pinned bleibt auf 6 gruen."
  artifacts:
    - path: "service_impl/src/reporting.rs"
      provides: "Per-Woche-gegatete absence-derived Balance-Reduktion in get_reports_for_all_employees + get_week; Display-Merge unveraendert additiv"
      contains: "absence_derived_balance"
    - path: "service_impl/src/test/reporting_phase2_fixtures.rs"
      provides: "is_dynamic=true Fixture (8h-Vertrag-Spiegel mit dynamischem Flag)"
      contains: "fixture_work_details_dynamic_mon_fri"
    - path: "service_impl/src/test/reporting_additive_merge.rs"
      provides: "Balance-Parity-Tests fuer dynamische Vertraege ueber alle drei Methoden (balance ~0 quellen-unabhaengig)"
      contains: "dynamic"
  key_links:
    - from: "reporting.rs get_reports_for_all_employees (per-Woche-Fold)"
      to: "absence_derived_balance_total -> expected_hours / dynamic_hours"
      via: "pro-Woche-Beitrag nur wenn expected_hours > 0.0 (gleiche Bedingung wie absense_hours Z.263)"
      pattern: "absence_derived_balance"
    - from: "reporting.rs get_week (Balance-Reduktion)"
      to: "absence_derived_total -> expected_hours"
      via: "clamp auf 0.0 wenn planned_hours <= 0.0 (dynamic-Guard, Spiegel von abense_hours)"
      pattern: "planned_hours <= 0.0"
    - from: "reporting_additive_merge.rs dynamic Parity-Tests"
      to: "ShortEmployeeReport.balance_hours == 0 (beide Quellen)"
      via: "fixture_work_details_dynamic_mon_fri + zwei Laeufe (extra_hours vs derived)"
      pattern: "fixture_work_details_dynamic_mon_fri"
---

<objective>
Gap (VERIFICATION Truth 9 / REVIEW CR-01 + WR-01): Balance-Parity (M-02/M-03 — eine reine
absence_period-Buchung bewegt die Balance identisch zum aequivalenten extra_hours-Eintrag,
QUELLEN-UNABHAENGIG) ist fuer `is_dynamic=false` verifiziert (Truth 8 gruen), aber fuer
`is_dynamic=true` in ZWEI der drei Report-Methoden gebrochen. Die dritte Methode
(get_report_for_employee_range via hours_per_week) ist bereits korrekt und ist die Referenz.

**Defekt A — get_reports_for_all_employees (reporting.rs:403-406):** `absence_derived_total` wird
als Jahreslumpen (eine derive_hours_for_range-Aufruf ueber [first_day_in_year .. until_week-Sonntag],
Z.378-404) berechnet und Z.405-406 BEDINGUNGSLOS von expected_hours/dynamic_hours abgezogen. Der
per-Wochen-Fold (Z.248-273) setzt fuer dynamische Wochen (`expected_hours <= 0.0`-Zweig) aber
`absense_hours: 0.0` und `planned_hours = overall_hours`. Resultat fuer einen dynamischen
Mitarbeiter mit absence_period-Urlaub: `expected_hours = overall_hours - absence_derived_total`,
also `balance = absence_derived_total > 0`. Der aequivalente extra_hours-Urlaub liefert `balance = 0`
(sein absense_hours wird im selben Zweig auf 0.0 gezwungen). Asymmetrie -> M-02-Verstoss.

**Defekt B — get_week (reporting.rs:816-824):** `expected_hours = planned_hours - abense_hours -
absence_derived_total` (Z.818). Fuer dynamische Wochen liefert `weight_for_week` `planned_hours = 0.0`
(Z.898). Also `expected_hours = -absence_derived_total` (negativ) und `balance_hours = overall_hours
- expected_hours` wird um den vollen derived-Betrag aufgeblasen.

**Referenz (KORREKT) — hours_per_week (reporting.rs:976-997):** `absence_hours` ist gegated
`if working_hours_for_week <= 0.0 { 0.0 } else { sum }`, und der derived-Beitrag
`derived_absence_hours` nutzt DENSELBEN Guard (Z.988-996). Genau diese per-Woche-Gating-Disziplin
ist auf Defekt A und B zu uebertragen.

**KRITISCHE Unterscheidung Display vs. Balance:** Die DISPLAY-Felder (vacation_hours /
sick_leave_hours / unpaid_leave_hours, in ShortEmployeeReport) sind BEREITS korrekt + verifiziert
additiv (Truth 3 & 4 gruen). Sie muessen UNGEGATET/additiv bleiben — die derived Stunden zeigen
sich immer in den Display-Spalten, unabhaengig vom Vertragstyp. NUR die BALANCE-Reduktion (der von
expected_hours / dynamic_hours abgezogene Anteil) wird per-Woche auf 0.0 gegated bei dynamischen
Wochen. Der Display-Merge darf beim Balance-Fix NICHT brechen.

Purpose: Balance-Neutralitaet bei Abwesenheit gilt QUELLEN-UNABHAENGIG fuer JEDEN Vertragstyp
(M-02/M-03). Output: per-Woche-gegatete Balance-Reduktion in beiden defekten Methoden; ein
is_dynamic=true Fixture; Balance-Parity-Tests fuer alle drei Methoden, die pre-Fix RED und post-Fix
GREEN sind; dokumentierter Snapshot-Versions-Entscheid (kein Bump 6->7).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located git). GSD-Auto-Commit ist AUS (`commit_docs: false`).
NIEMALS `git commit` / `git add` / `git`-Fallbacks ausfuehren. Der User committet manuell mit jj.
Dieser Plan enthaelt KEINEN Commit-Task. Falls committet wird: jj-nativ.
</vcs_jj_only>

<context>
@.planning/phases/08.4-reporting-additiv-merge/08.4-CONTEXT.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-VERIFICATION.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-REVIEW.md
@.planning/phases/08.4-reporting-additiv-merge/08.4-04-SUMMARY.md

<snapshot_versioning_decision>
ENTSCHEIDUNG (mit Code-Evidenz, NICHT angenommen): KEIN weiterer Bump 6->7.

`CURRENT_SNAPSHOT_SCHEMA_VERSION` wurde in Plan 08.4-04 von 5 auf 6 gebumpt (billing_period_report.rs:62)
— exakt fuer die Aenderung, die balance_hours/expected_hours um absence_period-derived Stunden
reduziert. v6 ist in DIESER NOCH-NICHT-RELEASTEN Phase eingefuehrt worden. Dieser Fix korrigiert den
DYNAMISCHEN Zweig genau jener v6-Computation — er fuehrt KEINEN neuen persistierten Ruleset ein,
sondern bringt die bereits unter v6 deklarierte "absence-derived reduziert Balance symmetrisch"-Regel
fuer dynamische Vertraege in den korrekten (gegateten) Zustand. Per shifty-backend/CLAUDE.md
§ "Billing Period Snapshot Schema Versioning" ist ein Bump erforderlich, wenn die Computation eines
EXISTIERENDEN, BEREITS PERSISTIERTEN value_type sich gegenueber einem bereits geschriebenen Snapshot
aendert. Unter v6 existiert noch KEIN released/persistierter Snapshot mit der defekten dynamischen
Balance (v6 ist neu in dieser Phase). Es gibt also keinen v6-Snapshot "alter Regeln", gegen den ein
Validator driften koennte — der Fix vervollstaendigt v6, statt es zu veraendern.

-> VERDICT: snapshot_schema_version bleibt auf 6. Der Pin-Test `snapshot_schema_version_pinned` bleibt
   auf 6 gruen (Task 3 assertiert das explizit). KEIN Bump 6->7. Der Entscheid + diese Begruendung
   sind im SUMMARY zu dokumentieren.
</snapshot_versioning_decision>

<interfaces>
Referenz-Guard (EXAKT spiegeln — kopiere die bewaehrte Form, erfinde keine neue), reporting.rs:976-996:
```rust
let absence_hours = if working_hours_for_week <= 0.0 {
    0.0f32
} else {
    filtered_extra_hours_list
        .iter()
        .filter(|eh| eh.category.as_report_type() == ReportType::AbsenceHours)
        .map(|eh| eh.amount)
        .sum::<f32>()
};
// ...
let derived_absence_hours = if working_hours_for_week <= 0.0 {
    0.0f32
} else {
    derived_absence
        .iter()
        .filter(|(d, _)| ShiftyDate::from(**d).as_shifty_week() == week)
        .map(|(_, r)| r.hours)
        .sum::<f32>()
};
let absence_hours = absence_hours + derived_absence_hours;
```

Defekt-A-Kontext (get_reports_for_all_employees):
- Per-Woche-Fold (Z.202-367): pro Woche wird `(expected_hours, dynamic_hours)` berechnet (Z.220-234).
  Im dynamischen Zweig `expected_hours <= 0.0` (Z.248) wird `absense_hours: 0.0` gesetzt (Z.263).
  Im else-Zweig (Z.274) wird `absense_hours` aus extra_hours summiert (Z.285-289).
- Die derived-Map wird NACH dem Fold als Jahreslumpen geholt (Z.378-389) und Z.405-406 ungegated
  abgezogen. `WeeklyHours` (Struct-Def Z.186-200) hat KEIN Feld fuer die per-Woche-gegatete derived-
  Balance — das muss hinzugefuegt werden.
- `ShiftyWeek` / `ShiftyDate::from(time::Date).as_shifty_week()` ist im File etabliert (Z.993). Das
  derive-Ergebnis ist `BTreeMap<time::Date, service::absence::ResolvedAbsence>` (Schluessel = Tag).

Defekt-B-Kontext (get_week): `planned_hours` aus `weight_for_week` (Z.784-788), dynamisch = 0.0
(Z.898). `abense_hours` (Z.709-716) aus extra_hours Availability::Unavailable. Reduktion Z.818.

ResolvedAbsence: `{ category: AbsenceCategory, hours: f32 }` (service/src/absence.rs). Die drei
Kategorien Vacation/SickLeave/UnpaidLeave sind alle AbsenceHours -> alle reduzieren Balance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defekt A — per-Woche-gegatete absence-Balance-Reduktion in get_reports_for_all_employees</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:120-200 (get_reports_for_all_employees Kopf; derived-Map noch nicht geholt; WeeklyHours-Struct-Def Z.186-200 — hier wird ein Feld ergaenzt)
    - service_impl/src/reporting.rs:202-367 (per-Woche-Fold; dynamischer Zweig Z.248-273 mit absense_hours: 0.0; else-Zweig Z.274-346; fold-Reducer Z.348-367)
    - service_impl/src/reporting.rs:378-422 (derived-Jahreslumpen Z.378-389; absence_derived_*-Akkumulatoren Z.390-399; ungegate Reduktion Z.403-406; Display-Merge Z.415-419)
    - service_impl/src/reporting.rs:976-997 (REFERENZ-Guard hours_per_week: absence_hours + derived_absence_hours mit `if working_hours_for_week <= 0.0 { 0.0 } else { ... }` — exakt diese Form spiegeln)
  </read_first>
  <behavior>
    - Dynamischer Vertrag (planned_hours/expected_hours <= 0.0 in der Urlaubswoche) + absence_period-Vacation 8h: balance_hours == 0 (innerhalb 0.01), weil die derived Balance-Reduktion fuer diese Woche auf 0.0 gegated ist.
    - Derselbe Urlaub als extra_hours: balance_hours == 0 (war schon so) — Parity haelt.
    - Nicht-dynamischer Vertrag (expected_hours > 0.0): derived Vacation 8h reduziert expected_hours weiterhin um 8h (unveraendert zu Truth 8) — die Gating-Bedingung greift NICHT.
    - DISPLAY: vacation_hours bleibt 12h im bestehenden test_all_employees_additive_merge (4h extra + 8h derived) — der ungegate Display-Jahreslumpen bleibt erhalten.
  </behavior>
  <action>
Ersetze in `get_reports_for_all_employees` die ungegate Jahreslumpen-Balance-Reduktion durch eine
per-Woche-gegatete Reduktion, OHNE den Display-Merge zu veraendern.

**Schritt A — derived-Map VOR den per-Woche-Fold ziehen.** Verschiebe den derive_hours_for_range-Aufruf
(aktuell Z.378-389) so, dass das `derived`-Ergebnis bereits VOR `let weekly_hours = (0..=...).map(...)`
(Z.202) verfuegbar ist. Die Range-Argumente bleiben EXAKT gleich
(`ShiftyDate::first_day_in_year(year).to_date()` .. `ShiftyWeek::new(year, until_week).as_date(DayOfWeek::Sunday).to_date()`).
Begruendung: der Fold braucht die per-Woche-Sicht auf `derived`, um pro Woche gegated beizutragen.

**Schritt B — WeeklyHours um ein gegatetes Balance-Feld erweitern.** Fuege der `WeeklyHours`-Struct
(Z.186-200) ein Feld hinzu:
```rust
absence_derived_balance_hours: f32,
```
(Default 0.0 via `#[derive(Default)]` — passt.)

**Schritt C — im per-Woche-Closure pro Woche den gegateten derived-Beitrag berechnen.** Im Closure
(Z.203-347) ist `year`/`week` die aufgeloeste ISO-Woche und `expected_hours` (Z.220) bekannt. Berechne
VOR dem `if expected_hours <= 0.0`-Branch (oder in beiden Zweigen identisch) den per-Woche-derived-
Balance-Beitrag, GEGATET mit DERSELBEN Bedingung wie absense_hours (`expected_hours <= 0.0` -> 0.0):
```rust
let absence_derived_balance_hours = if expected_hours <= 0.0 {
    0.0f32
} else {
    derived
        .iter()
        .filter(|(d, _)| {
            let w = ShiftyDate::from(**d).as_shifty_week();
            w.year == year && w.week == week
        })
        .map(|(_, r)| r.hours)
        .sum::<f32>()
};
```
(Spiegelt exakt den Referenz-Guard hours_per_week Z.988-996. `derived` ist hier `BTreeMap<time::Date,
ResolvedAbsence>`; pruefe die exakte ShiftyWeek-Feldzugriffe `.year`/`.week` anhand der File-Nutzung
Z.205-218 bzw. ShiftyWeek-Konstruktion — falls die Felder anders heissen, nutze die im File etablierte
Form `ShiftyWeek::new(year, week)`-Vergleich analog Z.382.)

Setze das Feld in BEIDEN WeeklyHours-Konstruktoren:
- dynamischer Zweig (Z.260-273): `absence_derived_balance_hours: 0.0,` (immer 0 — Woche hat keine
  Vertragsarbeitszeit; identisch zu absense_hours: 0.0).
- else-Zweig (Z.332-345): `absence_derived_balance_hours,` (der oben berechnete gegate Wert).

**Schritt D — im fold-Reducer (Z.348-367) das neue Feld akkumulieren:**
```rust
acc.absence_derived_balance_hours += week.absence_derived_balance_hours;
```

**Schritt E — die Balance-Reduktion umstellen.** Ersetze die ungegate Reduktion (Z.403-406):
```rust
let absence_derived_total =
    absence_derived_vacation_hours + absence_derived_sick_leave_hours + absence_derived_unpaid_leave_hours;
let expected_hours = weekly_hours.planned_hours - weekly_hours.absense_hours - absence_derived_total;
let dynamic_hours = weekly_hours.dynamic_hours - weekly_hours.absense_hours - absence_derived_total;
```
durch die per-Woche-gegate Summe:
```rust
// Gap (Phase 8.4 / CR-01): per-Woche-gegatete Balance-Reduktion. Display bleibt ungegate (Jahres-
// lumpen absence_derived_vacation/sick/unpaid), aber die expected/balance-Reduktion zaehlt nur die
// derived Stunden der Wochen MIT Vertragsarbeitszeit — symmetrisch zu absense_hours (Z.263) und zur
// Referenz hours_per_week (Z.988). Verhindert dynamic-contract Balance-Asymmetrie (M-02).
let expected_hours = weekly_hours.planned_hours - weekly_hours.absense_hours - weekly_hours.absence_derived_balance_hours;
let dynamic_hours = weekly_hours.dynamic_hours - weekly_hours.absense_hours - weekly_hours.absence_derived_balance_hours;
```
WICHTIG: Die `absence_derived_vacation_hours` / `absence_derived_sick_leave_hours` /
`absence_derived_unpaid_leave_hours`-Akkumulatoren (Z.390-399) und ihre Verwendung im Display-Merge
(Z.415-419: `vacation_hours: weekly_hours.vacation_hours + absence_derived_vacation_hours` usw.)
bleiben UNVERAENDERT — sie sind der ungegate Display-Jahreslumpen und MUESSEN additiv bleiben. Loesche
NUR die `absence_derived_total`-Bindung (Z.403-404), falls sie nach der Umstellung ungenutzt ist.
`overall_hours` (Z.407) und `balance_hours = overall_hours - expected_hours + previous_year_carryover`
(Z.408) bleiben unveraendert; balance folgt automatisch aus dem korrigierten expected_hours.

Keine neuen Imports noetig (ShiftyDate/ShiftyWeek bereits im File).
  </action>
  <acceptance_criteria>
    - `grep -c "absence_derived_balance_hours" service_impl/src/reporting.rs` >= 5 (Feld-Def + 2 Konstruktoren + fold-Reducer + 2 Reduktions-Verwendungen).
    - `grep -n "weekly_hours.absense_hours - weekly_hours.absence_derived_balance_hours" service_impl/src/reporting.rs` findet BEIDE Reduktionen (expected + dynamic).
    - `grep -c "if expected_hours <= 0.0" service_impl/src/reporting.rs` >= 2 (bestehender Zweig Z.248 + neuer Guard).
    - Display-Merge intakt: `grep -n "weekly_hours.vacation_hours + absence_derived_vacation_hours" service_impl/src/reporting.rs` findet weiterhin den ungegate Display-Merge.
    - `nix develop -c cargo build -p service_impl` exit 0.
    - `nix develop -c cargo test -p service_impl test_all_employees_additive_merge` exit 0 (Display 12h bleibt — Truth 3 nicht gebrochen).
  </acceptance_criteria>
  <verify>
    <automated>nix develop -c cargo test -p service_impl test_all_employees_additive_merge</automated>
  </verify>
  <done>get_reports_for_all_employees reduziert expected_hours/dynamic_hours/balance_hours um eine per-Woche-gegatete derived-Summe (0.0 bei dynamischen Wochen), der Display-Merge bleibt ungegate additiv; test_all_employees_additive_merge bleibt gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Defekt B — dynamic-Guard auf der Balance-Reduktion in get_week</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:700-784 (get_week pro-Mitarbeiter; abense_hours Z.709-716; planned_hours/dynamic_hours aus weight_for_week Z.784-788)
    - service_impl/src/reporting.rs:794-842 (derived-Block Z.794-813; absence_derived_total Z.816-817; ungegate Reduktion Z.818; apply_weekly_cap Z.819-820; dynamic_hours Z.822; balance Z.824; Display-Merge Z.835-839)
    - service_impl/src/reporting.rs:849-905 (weight_for_week — dynamisch liefert planned_hours 0.0, Z.898)
    - service_impl/src/reporting.rs:976-996 (REFERENZ-Guard — `if working_hours_for_week <= 0.0 { 0.0 } else { ... }`)
  </read_first>
  <behavior>
    - Dynamische Woche (planned_hours == 0.0) + absence_period-Vacation 8h: expected_hours == 0.0 (nicht -8.0), balance_hours == overall_hours (nicht aufgeblasen um 8h). Bei overall_hours == 0 -> balance == 0.
    - Derselbe Urlaub als extra_hours: balance == 0 (war schon so, da abense_hours bei dynamic in get_week ebenfalls nicht reduziert werden sollte) — Parity haelt.
    - Nicht-dynamische Woche (planned_hours > 0.0): derived Vacation 8h reduziert expected_hours weiterhin um 8h.
    - DISPLAY: sick_leave_hours bleibt 11h im bestehenden test_get_week_additive_merge (3h extra + 8h derived).
  </behavior>
  <action>
Gate die derived-Balance-Reduktion in get_week mit DERSELBEN dynamic-Bedingung wie die Referenz
hours_per_week (Z.988), ohne den Display-Merge (Z.835-839) zu veraendern.

Ersetze die ungegate Reduktion (reporting.rs:816-818):
```rust
let absence_derived_total =
    absence_derived_vacation_hours + absence_derived_sick_leave_hours + absence_derived_unpaid_leave_hours;
let expected_hours = planned_hours - abense_hours - absence_derived_total;
```
durch eine auf `planned_hours <= 0.0` gegatete Variante (Spiegel von abense_hours-/hours_per_week-Guard):
```rust
// Gap (Phase 8.4 / WR-01): dynamic-Guard auf der derived-Balance-Reduktion. Bei dynamischen Wochen
// liefert weight_for_week planned_hours = 0.0 (Z.898) — die derived Stunden duerfen expected_hours
// dann NICHT reduzieren (sonst negatives expected, aufgeblasene balance). Symmetrisch zu abense_hours
// und zur Referenz hours_per_week (Z.988). Display-Merge (Z.835-839) bleibt ungegate additiv.
let absence_derived_balance_total = if planned_hours <= 0.0 {
    0.0f32
} else {
    absence_derived_vacation_hours + absence_derived_sick_leave_hours + absence_derived_unpaid_leave_hours
};
let expected_hours = planned_hours - abense_hours - absence_derived_balance_total;
```
Passe die `dynamic_hours`-Reduktion (Z.822) entsprechend an:
```rust
let dynamic_hours = dynamic_hours - abense_hours - absence_derived_balance_total;
```
`apply_weekly_cap` (Z.819-820), `overall_hours` (Z.823) und `balance_hours = overall_hours -
expected_hours` (Z.824) bleiben unveraendert und konsumieren das korrigierte expected_hours
automatisch.

WICHTIG: Der Display-Merge (Z.835-839: `vacation_hours: vacation_hours + absence_derived_vacation_hours`
usw.) und die einzelnen `absence_derived_vacation_hours`/`_sick_leave_hours`/`_unpaid_leave_hours`-
Akkumulatoren (Z.804-812) bleiben UNVERAENDERT — die derived Stunden erscheinen weiterhin in den
Display-Spalten unabhaengig vom Vertragstyp. Es wird NUR der von expected/dynamic abgezogene Betrag
gegated.

Keine neuen Imports noetig.
  </action>
  <acceptance_criteria>
    - `grep -c "absence_derived_balance_total" service_impl/src/reporting.rs` >= 3 (Bindung + expected + dynamic).
    - `grep -n "if planned_hours <= 0.0" service_impl/src/reporting.rs` findet den neuen Guard.
    - `grep -n "planned_hours - abense_hours - absence_derived_balance_total" service_impl/src/reporting.rs` findet die gegate expected-Reduktion.
    - Display-Merge intakt: `grep -n "vacation_hours + absence_derived_vacation_hours" service_impl/src/reporting.rs` findet weiterhin den ungegate get_week-Display-Merge.
    - `nix develop -c cargo build -p service_impl` exit 0.
    - `nix develop -c cargo test -p service_impl test_get_week_additive_merge` exit 0 (Display 11h bleibt — Truth 4 nicht gebrochen).
  </acceptance_criteria>
  <verify>
    <automated>nix develop -c cargo test -p service_impl test_get_week_additive_merge</automated>
  </verify>
  <done>get_week clampt die derived-Balance-Reduktion auf 0.0 bei dynamischen Wochen (planned_hours <= 0.0); expected_hours wird nie negativ; der Display-Merge bleibt ungegate additiv; test_get_week_additive_merge bleibt gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: is_dynamic=true Fixture + dynamische Balance-Parity-Tests (3 Methoden) + Snapshot-Pin-Verifikation + Full-Suite-Gate</name>
  <files>service_impl/src/test/reporting_phase2_fixtures.rs, service_impl/src/test/reporting_additive_merge.rs</files>
  <read_first>
    - service_impl/src/test/reporting_phase2_fixtures.rs:44-72 (fixture_work_details_8h_mon_fri — exakte Vorlage fuer die dynamische Variante; nur is_dynamic flippen)
    - service_impl/src/test/reporting_additive_merge.rs:610-715 (build_parity_service + test_balance_parity_absence_period_vs_extra_hours — exakte Struktur fuer die dynamischen Range-Parity-Tests spiegeln)
    - service_impl/src/test/reporting_additive_merge.rs:361-491 (test_all_employees_additive_merge + test_get_week_additive_merge — Mock-Setup-Vorlage fuer die all_employees- und get_week-Parity-Tests)
    - service_impl/src/reporting.rs:849-905 (weight_for_week — is_dynamic=true liefert planned_hours 0.0; bestaetigt dass die Fixture die dynamic-Wochen aktiviert)
    - service_impl/src/test/billing_period_snapshot_locking.rs:25-30 (Pin-Test bleibt 6 — KEIN Bump 6->7)
  </read_first>
  <behavior>
    - test_balance_parity_dynamic_employee_range (get_report_for_employee_range): zwei Laeufe (extra_hours 8h vs derived 8h, 2024-06-03); (expected_extra - expected_absence).abs() < 0.01 UND (balance_extra - balance_absence).abs() < 0.01; ZUSAETZLICH dynamic-spezifisch: balance_extra.abs() < 0.01 UND balance_absence.abs() < 0.01 (beide ~0). Dieser ist bereits korrekt (Referenz) und bleibt gruen pre+post-Fix.
    - test_balance_parity_dynamic_all_employees (get_reports_for_all_employees): gleiche zwei Laeufe; pre-Fix RED (balance_absence == 8, nicht 0), post-Fix GREEN.
    - test_balance_parity_dynamic_get_week (get_week): gleiche zwei Laeufe; pre-Fix RED (balance aufgeblasen), post-Fix GREEN.
    - snapshot_schema_version_pinned: bleibt 6 (kein Bump).
  </behavior>
  <action>
**Schritt A — dynamisches Fixture (reporting_phase2_fixtures.rs).** Fuege nach
`fixture_work_details_8h_mon_fri` (Z.72) eine dynamische Variante hinzu — identische Struktur, nur
`is_dynamic: true`:
```rust
/// 8h/Tag Mo-Fr, gueltig KW22-25/2024, aber DYNAMISCHER Vertrag (is_dynamic=true).
/// weight_for_week liefert dann planned_hours = 0.0 fuer alle Wochen -> Balance soll quellen-
/// unabhaengig neutral bleiben (M-02/M-03 dynamic). Spiegel von fixture_work_details_8h_mon_fri.
#[allow(dead_code)]
pub fn fixture_work_details_dynamic_mon_fri() -> EmployeeWorkDetails {
    EmployeeWorkDetails {
        is_dynamic: true,
        ..fixture_work_details_8h_mon_fri()
    }
}
```
(Falls `..base()`-Spread mit der Fixture nicht kompiliert, kopiere die Felder vollstaendig und setze
`is_dynamic: true` + eine eigene `id` z.B. `Uuid::from_u128(0x...0011)`.)

**Schritt B — drei dynamische Parity-Tests (reporting_additive_merge.rs).** Importiere
`fixture_work_details_dynamic_mon_fri` aus `crate::test::reporting_phase2_fixtures` (zur bestehenden
use-Liste Z.40-42 hinzufuegen).

(1) `test_balance_parity_dynamic_employee_range` — spiegelt
`test_balance_parity_absence_period_vs_extra_hours` (Z.676), aber `build_parity_service` muss das
DYNAMISCHE Fixture nutzen. Entweder erweitere `build_parity_service` um einen
`work_details: EmployeeWorkDetails`-Parameter (und passe die 2 bestehenden Aufrufe Z.681/693 auf
`fixture_work_details_8h_mon_fri()` an), ODER schreibe einen lokalen `build_parity_service_dynamic`
mit `fixture_work_details_dynamic_mon_fri()` in der `expect_find_by_sales_person_id`-Closure. Lauf 1:
extra_hours Vacation 8h am 2024-06-03, derived leer. Lauf 2: extra_hours leer, derived {2024-06-03:
Vacation 8h}. Assertions:
```rust
assert!((report_extra.expected_hours - report_absence.expected_hours).abs() < 0.01, "...");
assert!((report_extra.balance_hours - report_absence.balance_hours).abs() < 0.01, "...");
assert!(report_extra.balance_hours.abs() < 0.01, "dynamic: extra_hours-Vacation darf balance nicht bewegen, got {}", report_extra.balance_hours);
assert!(report_absence.balance_hours.abs() < 0.01, "dynamic: absence_period-Vacation darf balance nicht bewegen, got {}", report_absence.balance_hours);
```

(2) `test_balance_parity_dynamic_all_employees` — Mock-Setup gespiegelt von
`test_all_employees_additive_merge` (Z.362), aber `expect_all().returning(|_,_| Ok(Arc::from(vec![
fixture_work_details_dynamic_mon_fri()])))`. Fuehre ZWEI get_reports_for_all_employees(2024, 23)-Laeufe
aus (eigene Mock-Instanzen): Lauf A mit extra_hours=[Vacation 8h am 2024-06-03] + derived leer;
Lauf B mit extra_hours=[] + derived {2024-06-03: Vacation 8h}. Greife `reports[0]` ab. Assertions:
```rust
assert!((report_a.expected_hours - report_b.expected_hours).abs() < 0.01, "expected quellen-unabhaengig");
assert!((report_a.balance_hours - report_b.balance_hours).abs() < 0.01, "balance quellen-unabhaengig");
assert!(report_a.balance_hours.abs() < 0.01, "dynamic all_employees extra: balance ~0, got {}", report_a.balance_hours);
assert!(report_b.balance_hours.abs() < 0.01, "dynamic all_employees absence: balance ~0, got {}", report_b.balance_hours);
```

(3) `test_balance_parity_dynamic_get_week` — Mock-Setup gespiegelt von `test_get_week_additive_merge`
(Z.435), aber `expect_all_for_week().returning(|_,_,_,_| Ok(Arc::from(vec![
fixture_work_details_dynamic_mon_fri()])))`. ZWEI get_week(2024, 23)-Laeufe: Lauf A extra_hours
[Vacation 8h am 2024-06-03] + derived leer; Lauf B extra_hours [] + derived {2024-06-03: Vacation 8h}.
Gleiche vier Assertions wie (2) (quellen-unabhaengig + beide balance ~0).

Diese drei Tests MUESSEN gegen den UNGEFIXTEN Code (vor Task 1/2) bei (2) und (3) FAILEN
(balance_b ~8 statt 0) und nach Task 1/2 PASSEN — sie sind der Regressions-Beweis. (1) ist bereits
gruen (Referenz). Dokumentiere im SUMMARY, dass (2)+(3) den pre-Fix-RED -> post-Fix-GREEN-Uebergang
belegen.

**Schritt C — Snapshot-Versions-Entscheid verifizieren.** KEINE Aenderung an
billing_period_report.rs noetig. Verifiziere nur, dass der Pin auf 6 bleibt (siehe
snapshot_versioning_decision oben: dieser Fix vervollstaendigt v6, kein Bump 6->7). Dokumentiere den
Verdict + Begruendung im SUMMARY.

**Schritt D — Full-Suite-Gate.** Fuehre die volle Suite aus. KEINE destruktiven DB-Operationen
(kein `sqlx database reset`/drop) — die Suite laeuft gegen in-memory SQLite. Behebe etwaige
Regressionen aus Task 1/2.
  </action>
  <acceptance_criteria>
    - `grep -c "fixture_work_details_dynamic_mon_fri" service_impl/src/test/reporting_phase2_fixtures.rs` >= 1.
    - `grep -c "is_dynamic: true" service_impl/src/test/reporting_phase2_fixtures.rs` == 1.
    - `grep -c "test_balance_parity_dynamic" service_impl/src/test/reporting_additive_merge.rs` == 3 (drei dynamische Parity-Tests).
    - `grep -c "fixture_work_details_dynamic_mon_fri" service_impl/src/test/reporting_additive_merge.rs` >= 3 (in allen drei Tests genutzt).
    - `nix develop -c cargo test -p service_impl test_balance_parity_dynamic` exit 0 (alle drei dynamischen Tests gruen NACH dem Fix).
    - `nix develop -c cargo test -p service_impl reporting_additive` exit 0 (alle additiven + Parity-Tests, jetzt 12: 9 alt + 3 neu dynamisch).
    - `nix develop -c cargo test -p service_impl snapshot_schema_version_pinned` exit 0 (Pin bleibt 6, KEIN Bump).
    - `grep -c "CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 6" service_impl/src/billing_period_report.rs` == 1 (unveraendert 6).
    - `nix develop -c cargo test --workspace` exit 0 (0 compile-errors, 0 failures; service_impl-Count steigt um die 3 neuen Tests gegenueber Baseline 428).
    - `grep -c derive_hours_for_range service_impl/src/reporting.rs` == 3 (Invariante aus Plan 03 weiterhin gehalten — 3 Aufrufe).
  </acceptance_criteria>
  <verify>
    <automated>nix develop -c cargo test --workspace</automated>
  </verify>
  <done>is_dynamic=true Fixture existiert; drei dynamische Balance-Parity-Tests (range/all_employees/get_week) beweisen balance ~0 quellen-unabhaengig und sind post-Fix gruen (all_employees + get_week pre-Fix RED); Snapshot bleibt auf 6 (Pin gruen); cargo test --workspace gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST-Caller → ReportingService | HR-gepruefte Reporting-Reads (get_reports_for_all_employees / get_week) |
| ReportingService → AbsenceService.derive_hours_for_range | interner Call mit Authentication-Kontext (unveraendert) |
| ReportingService.get_report_for_employee_range → BillingPeriodReport-Snapshot | persistierte balance/expected-Werte (write-once, schema v6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0844-05-01 | Tampering | Snapshot value_types Balance/ExpectedHours (dynamic-contract) | mitigate | Der Fix korrigiert die dynamische v6-Computation, OHNE einen neuen persistierten Ruleset einzufuehren. Da unter v6 noch kein released Snapshot mit der defekten dynamischen Balance existiert (v6 ist neu in dieser Phase), ist KEIN weiterer Bump 6->7 erforderlich (siehe snapshot_versioning_decision). Pin-Test snapshot_schema_version_pinned erzwingt 6 (Task 3) und verhindert ein versehentliches stilles Aendern der Version. |
| T-0844-05-02 | Information Disclosure | Display-Felder vs. Balance-Felder | accept | Die Gating-Aenderung betrifft NUR den Balance-Reduktions-Pfad; die Display-Felder (vacation/sick/unpaid_leave_hours) bleiben ungegate additiv. Kein neuer Datenfluss, keine zusaetzliche Exposition. Bestehende RBAC-Checks (HR_PRIVILEGE) unveraendert. |
| T-0844-05-03 | Elevation of Privilege | per-Woche-Gating-Logik | accept | Die Gating-Bedingung (`expected_hours <= 0.0` / `planned_hours <= 0.0`) ist reine numerische Vertragslogik ohne Auth-Bezug; die derived-Map wurde bereits mit dem geprueften `context` erzeugt. Kein neuer Auth-Pfad. |

Kein `high`-Threat. Der einzige korrektheits-/sicherheitsrelevante Pflicht-Punkt (Snapshot-Versionierung)
ist durch den dokumentierten "kein Bump 6->7"-Entscheid + Pin-Verifikation (Task 3) abgedeckt.
</threat_model>

<verification>
- `nix develop -c cargo test --workspace` exit 0 (0 compile-errors, 0 failures).
- `nix develop -c cargo test -p service_impl test_balance_parity_dynamic` exit 0 (drei dynamische Parity-Tests gruen).
- `nix develop -c cargo test -p service_impl reporting_additive` exit 0 (12 Tests: 9 alt + 3 neu).
- `nix develop -c cargo test -p service_impl snapshot_schema_version_pinned` exit 0 (Pin bleibt 6).
- Display-Merge intakt: test_all_employees_additive_merge (12h) + test_get_week_additive_merge (11h) bleiben gruen.
- `grep -c derive_hours_for_range service_impl/src/reporting.rs` == 3 (Plan-03-Invariante gehalten).
</verification>

<success_criteria>
- Fuer is_dynamic=true bewegt eine reine absence_period-Buchung balance_hours IDENTISCH (~0, < 0.01) zum extra_hours-Aequivalent — in ALLEN drei Methoden (Truth 9 erfuellt, M-02/M-03 quellen-unabhaengig).
- Die derived-Balance-Reduktion ist per-Woche gegated (0.0 bei dynamischen Wochen) in get_reports_for_all_employees + get_week; get_report_for_employee_range war bereits korrekt.
- Die Display-Felder bleiben ungegate additiv (Truth 3 & 4 weiterhin gruen).
- Snapshot-Version bleibt 6 (dokumentierter Entscheid: kein Bump 6->7); Pin-Test gruen.
- Drei dynamische Parity-Tests existieren; all_employees + get_week sind pre-Fix RED, post-Fix GREEN.
- cargo test --workspace gruen.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08.4-reporting-additiv-merge/08.4-05-SUMMARY.md` erstellen.
Dokumentiere darin: (a) die per-Woche-Gating-Form in beiden Methoden (Defekt A: WeeklyHours-Feld +
fold; Defekt B: planned_hours <= 0.0 clamp) und dass sie den Referenz-Guard hours_per_week spiegelt;
(b) den Snapshot-Versions-Entscheid "kein Bump 6->7" mit Begruendung (v6 in dieser Phase eingefuehrt,
kein released Snapshot mit defekter dynamischer Balance, Fix vervollstaendigt v6); (c) dass die drei
dynamischen Parity-Tests den pre-Fix-RED -> post-Fix-GREEN-Uebergang fuer all_employees + get_week
belegen und get_report_for_employee_range bereits korrekt war; (d) dass die Display-Felder bewusst
ungegate additiv blieben.
</output>
