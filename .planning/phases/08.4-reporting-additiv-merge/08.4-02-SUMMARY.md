---
phase: 08.4-reporting-additiv-merge
plan: 02
subsystem: api
tags: [rust, reporting, extra_hours, absence-period, snapshot-versioning, feature-flag, additive-merge]

# Dependency graph
requires:
  - phase: 08.4-reporting-additiv-merge (Plan 01)
    provides: Test-first RED-Suite (4 additive Tests, Snapshot-Pin 5, create_vacation_always_succeeds, bereinigte cutover.rs)
  - phase: 08.2-cutover
    provides: soft_delete_bulk (per-row deleted IS NULL Invariante gegen Doppelzählung)
  - phase: 08.3
    provides: derive_hours_for_range Halbtag-Awareness + Snapshot-Schema-Version 4
provides:
  - Additiver Reporting-Merge ohne Flag-Branch (reporting.rs summiert lebende extra_hours + derive_hours_for_range)
  - FeatureFlagService-Dep aus ReportingServiceImpl entfernt (0 verbleibende Uses)
  - extra_hours-Schreibsperre (Vacation/SickLeave/UnpaidLeave) entfernt — Koexistenz-Modell M-01 aktiv
  - CURRENT_SNAPSHOT_SCHEMA_VERSION = 5 (Input-Menge der Aggregations-Computation geändert)
affects: [08.5-read-projection, 08.6-flag-removal]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additiver Quellen-Merge: extra_hours.filter(cat).sum() + absence_derived_*_hours statt Flag-exklusiver Branch"
    - "Snapshot-Bump + Input-Computation-Änderung im selben jj-Commit (CLAUDE.md § Snapshot Versioning)"

key-files:
  created:
    - .planning/phases/08.4-reporting-additiv-merge/08.4-02-SUMMARY.md
  modified:
    - service_impl/src/reporting.rs
    - service_impl/src/billing_period_report.rs
    - service_impl/src/extra_hours.rs
    - shifty_bin/src/main.rs
    - service_impl/src/test/reporting_additive_merge.rs
    - service_impl/src/test/mod.rs
  deleted:
    - service_impl/src/test/reporting_flag_on_integration.rs

key-decisions:
  - "FeatureFlagService-Dep aus ReportingServiceImpl ENTFERNT: grep ergab 0 echte is_enabled-Verwendungen nach Löschen des use_absence_range_source-Blocks → Dep + Import in reporting.rs, Type-Alias + DI-Konstruktion in main.rs, sowie Test-Mock-Bundle in reporting_additive_merge.rs entfernt."
  - "reporting_flag_on_integration.rs gelöscht statt umgebaut: Der Flag=on-Test (999h-ExtraHours werden gefiltert, vacation=16) testet exakt das abgeschaffte Flag-Modell und bricht unter dem additiven Merge. Seine Szenarien sind vollständig durch reporting_additive_merge.rs (4 Szenarien) subsumiert."

patterns-established:
  - "Additiver Merge: keine Überlappungs-Erkennung (Modell A); Doppelzählung strukturell durch soft-delete (deleted IS NULL im DAO) verhindert"
  - "FeatureFlagService-Dep-Entfernung folgt dem grep-Zweig aus dem Plan (0 Uses → Dep + alle DI/Test-Bundles bereinigen)"

requirements-completed: []

# Metrics
duration: ~22min
completed: 2026-06-09
---

# Phase 8.4 Plan 02: Reporting-Additiv-Merge + Deprecation-Rückbau Summary

**Reporting summiert Vacation/SickLeave/UnpaidLeave jetzt dauerhaft additiv aus lebenden `extra_hours` (deleted IS NULL) PLUS `derive_hours_for_range` — der globale `absence_range_source_active`-Flag-Switch ist aus Reporting- und extra_hours-create-Pfad entfernt, FeatureFlagService-Dep aus ReportingServiceImpl ausgebaut, Snapshot-Schema auf v5 gebumpt; volle Workspace-Suite grün.**

## Performance

- **Duration:** ~22 min
- **Started:** 2026-06-09T09:22Z
- **Completed:** 2026-06-09
- **Tasks:** 2
- **Files modified:** 7 (1 erstellt, 6 modifiziert, 1 gelöscht)

## Accomplishments

### Task 1 — Additiver Reporting-Merge + Snapshot-Bump 4→5 (EIN jj-Commit, `6eeed303`)
- `reporting.rs`: `use_absence_range_source`-Flag-Read-Block, der ExtraHours-Filter-Block und der conditional `derive_hours_for_range`-Block entfernt; ersetzt durch unbedingten `derive_hours_for_range`-Aufruf mit direkten `absence_derived_*_hours`-Akkumulatoren (D-01/D-02).
- EmployeeReport-Konstruktion: `vacation_hours`/`sick_leave_hours`/`unpaid_leave_hours` auf additive Form `extra_hours.filter(cat).map(eh.amount).sum() + absence_derived_*` gebracht. Betragsfeld `eh.amount` (gegen Live-Code verifiziert — nicht `eh.hours`).
- `FeatureFlagService`-Dep aus `ReportingServiceImpl` entfernt (Import Z. 12, gen_service_impl!-Dep Z. 71), DI in `main.rs` angepasst (Type-Alias + Konstruktion), Test-Mock-Bundle in `reporting_additive_merge.rs` bereinigt.
- `billing_period_report.rs`: `CURRENT_SNAPSHOT_SCHEMA_VERSION` 4 → 5 mit v5-History-Kommentar (Input-Menge der Computation geändert: beide Quellen statt quellen-exklusiv).
- Reporting-Änderung + Snapshot-Bump in **einem** jj-Commit (CLAUDE.md § Snapshot Versioning).

### Task 2 — Schreibsperre-Rückbau in extra_hours.rs (`2400ae8b`)
- Das `ExtraHoursCategoryDeprecated`/`absence_range_source_active`-Gate im `create()`-Pfad vollständig entfernt (D-03). Vacation/SickLeave/UnpaidLeave sind im Koexistenz-Modell (M-01) wieder ohne Einschränkung anlegbar.
- Der normale RBAC-Permission-Gate (`check_permission(HR_PRIVILEGE)` / `verify_user_is_sales_person`) bleibt als separate, weiterhin aktive Prüfung erhalten (per grep 12 Treffer, T-08.4-02).
- `FeatureFlagService`-Dep in `ExtraHoursServiceImpl` bewusst BEHALTEN (andere Pfade nutzen ihn — Entfernung erst in 8.6).

## FeatureFlagService-Dep-Zweig (Plan Task 1 Schritt 5)

`grep -n "feature_flag_service" service_impl/src/reporting.rs` nach Löschen des `use_absence_range_source`-Blocks ergab **0 echte Verwendungen** (nur noch Dep-Deklaration + Import). Daher: **Zweig „Dep entfernen" gewählt.** Bereinigt wurden:
- `reporting.rs`: Import `feature_flag::FeatureFlagService` + gen_service_impl!-Dep-Zeile.
- `shifty_bin/src/main.rs`: `type FeatureFlagService = ...` aus `ReportingServiceDependencies` + `feature_flag_service: ...` aus der `ReportingServiceImpl`-Konstruktion.
- `service_impl/src/test/reporting_additive_merge.rs`: `feature_flag_service`-Feld, `MockFeatureFlagService`-Import, TestDeps-Assoc-Type, `new()`/`build()`-Einträge und die `is_enabled().times(0)`-Zeile entfernt.

`cargo build --workspace` ist ohne unused-import-Warning für `FeatureFlagService` (in reporting.rs) durchgelaufen.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `reporting_flag_on_integration.rs` testete das abgeschaffte Flag-Modell und brach den Phase-Gate**
- **Found during:** Task 1 (vor Build).
- **Issue:** Plan 01 hatte diese Datei laut PATTERNS.md „auf additives Modell umbauen" sollen, aber nicht angefasst. Sie war weiterhin in `mod.rs` registriert und assertierte das alte Flag=on-Verhalten (999h-ExtraHours werden gefiltert → `vacation_hours == 16`). Unter dem additiven Merge würden diese 999h jetzt ADDIERT — der Test schlägt fehl und blockiert `cargo test --workspace` (Phase-Gate).
- **Fix:** Datei gelöscht und aus `service_impl/src/test/mod.rs` deregistriert. Die additiven Szenarien sind vollständig durch die in Plan 01 angelegte `reporting_additive_merge.rs` (4 Szenarien) abgedeckt — kein Test-Coverage-Verlust.
- **Files modified:** `service_impl/src/test/reporting_flag_on_integration.rs` (gelöscht), `service_impl/src/test/mod.rs`.
- **Commit:** `6eeed303` (Teil von Task 1).

**2. [Rule 3 - Blocking] Ungenutzter `ExtraHoursCategory`-Import nach Gate-Rückbau**
- **Found during:** Task 2 (Build).
- **Issue:** Nach Entfernen des `matches!(extra_hours.category, Vacation | SickLeave | UnpaidLeave)`-Gates war `ExtraHoursCategory` in `extra_hours.rs` ungenutzt → `-D unused-imports` brach den Build.
- **Fix:** `ExtraHoursCategory` aus dem `extra_hours::{...}`-Import entfernt (`ExtraHours, ExtraHoursService` bleiben).
- **Files modified:** `service_impl/src/extra_hours.rs`.
- **Commit:** `2400ae8b` (Teil von Task 2).

**3. [Rule 3 - Blocking] grep-Acceptance-Criterion `ExtraHoursCategoryDeprecated == 0` durch Kommentar verletzt**
- **Found during:** Task 2 (Verifikation).
- **Issue:** Mein erklärender Rückbau-Kommentar enthielt den Literal-Namen `ExtraHoursCategoryDeprecated` → `grep -c` lieferte 1 statt 0 (gleiche Falle wie Plan 01 dokumentiert).
- **Fix:** Kommentar umformuliert auf „das frühere Deprecation/Flag-Gate" → Kriterium erfüllt (0).
- **Files modified:** `service_impl/src/extra_hours.rs`.
- **Commit:** `2400ae8b`.

## Build-/Test-Status

- `cargo build --workspace`: **grün, keine Warnings** (kein unused FeatureFlagService-Import).
- `cargo test -p service_impl reporting_additive`: **4 passed, 0 failed**.
- `cargo test -p service_impl billing_period_snapshot_locking`: **2 passed, 0 failed** (Snapshot-Pin auf 5 grün).
- **Phase-Gate** `cargo test --workspace`: **vollständig grün** — alle Crates `test result: ok`, 0 Failures (u.a. service_impl 423 passed, shifty_bin-Integration 79 passed inkl. cutover.rs).
- **Smoke** `cargo run` (40s Timeout): Migrationen liefen, Scheduler gestartet, `Running server at 127.0.0.1:3000` — **kein Panic**.

## Acceptance Criteria (grep-verifiziert)

- `CURRENT_SNAPSHOT_SCHEMA_VERSION: u32 = 5` in billing_period_report.rs: **1** ✓
- `use_absence_range_source` in reporting.rs: **0** ✓
- `absence_derived_vacation_hours` in reporting.rs: **3** (≥2) ✓
- non-comment `absence_range_source_active` in reporting.rs: **0** ✓
- `feature_flag` in reporting.rs: **0** ✓
- `ExtraHoursCategoryDeprecated` in extra_hours.rs: **0** ✓
- `absence_range_source_active` in extra_hours.rs: **0** ✓
- Permission-Marker (`check_permission`/`verify_user_is_sales_person`) in extra_hours.rs: **12** (≥1) ✓

## Threat Surface

Keine neue Threat-Surface über den Plan-`<threat_model>` hinaus. Der RBAC-Permission-Check im extra_hours-create-Pfad ist nachweislich erhalten (T-08.4-02 accept); der additive Merge addiert ausschließlich innerhalb sales_person_id-gescopter Quellen (T-08.4-03 accept); der Snapshot-Bump ist die Tampering-Mitigation (T-08.4-04 mitigate, Locking-Test pinnt 5).

## Known Stubs

Keine. Der `ServiceError::ExtraHoursCategoryDeprecated`-Variant + der `rest/src/lib.rs`-Handler-Arm bleiben als toter Code bis Phase 8.6 stehen (Assumption A2 — bewusst, kein Stub im Sinne von nicht-verdrahteten Daten).

## Task Commits

Jeder Task atomar per jj committet (GSD-Auto-Commit deaktiviert, `commit_docs: false`):

1. **Task 1: Additiver Reporting-Merge + Snapshot-Bump 4→5** — `6eeed303` (feat) — EIN Commit für Reporting + Snapshot (Snapshot-Versionierungs-Regel).
2. **Task 2: extra_hours-Schreibsperre-Rückbau** — `2400ae8b` (feat).

_Hinweis: STATE.md und ROADMAP.md wurden NICHT angefasst (orchestrator-owned). Diese SUMMARY-Datei wird vom User mit jj committet._

## Self-Check: PASSED

- `service_impl/src/reporting.rs` (additiver Merge) — FOUND
- `service_impl/src/billing_period_report.rs` (CURRENT_SNAPSHOT_SCHEMA_VERSION = 5) — FOUND
- `service_impl/src/extra_hours.rs` (Gate entfernt) — FOUND
- `service_impl/src/test/reporting_flag_on_integration.rs` — correctly DELETED
- `.planning/phases/08.4-reporting-additiv-merge/08.4-02-SUMMARY.md` — FOUND
- Commit `6eeed303` (Task 1) — FOUND
- Commit `2400ae8b` (Task 2) — FOUND

---
*Phase: 08.4-reporting-additiv-merge*
*Completed: 2026-06-09*
