---
phase: 08.4-reporting-additiv-merge
plan: 01
subsystem: testing
tags: [rust, mockall, reporting, extra_hours, snapshot-versioning, feature-flag, tdd]

# Dependency graph
requires:
  - phase: 08.2-cutover
    provides: soft_delete_bulk (per-row deleted IS NULL invariant against double counting)
  - phase: 08.3
    provides: derive_hours_for_range half-day awareness + snapshot schema version 4
provides:
  - Bereinigte Test-Suite ohne Flag/Gate-Asserts (kein derive_hours_for_range().times(0)-Trap, kein ExtraHoursCategoryDeprecated-Assert mehr)
  - Neue Test-Datei reporting_additive_merge.rs mit 4 additiven Szenarien (SC-1, test-first, RED bis Plan 02)
  - Snapshot-Locking-Test pinnt Version 5 (SC-3, test-first, RED bis Plan 02 die Const bumpt)
affects: [08.4-02-reporting-production-merge, 08.6-flag-removal]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Test-first Wave-0: brechende Tests zuerst entfernen/ersetzen, dann Produktionscode (Plan 02) — kein broken-compile-Fenster"
    - "Additive-Merge-Mockall-Pattern: is_enabled().times(0) + unconditional derive_hours_for_range-Mock"

key-files:
  created:
    - service_impl/src/test/reporting_additive_merge.rs
  modified:
    - service_impl/src/test/mod.rs
    - service_impl/src/test/extra_hours.rs
    - service_impl/src/test/billing_period_snapshot_locking.rs
    - shifty_bin/src/integration_test/cutover.rs
  deleted:
    - service_impl/src/test/reporting_flag_off_bit_identity.rs

key-decisions:
  - "setup_common_mocks-Helper eingeführt, um die identische Boilerplate (permission/sales_person/work_details/shiftplan_report/carryover/transaction + is_enabled().times(0)) über alle 4 Szenarien DRY zu halten"
  - "RED-Zustand bewusst belassen: 4 additive Tests + snapshot-pin + create_vacation_always_succeeds scheitern zur Laufzeit, weil Plan 02 den Produktionscode noch nicht geändert hat (test-first)"

patterns-established:
  - "Wave-0-Test-Bereinigung: destruktives Entfernen von Flag/Gate-Asserts vor der Code-Änderung verhindert ein broken-compile-Fenster über Plan 01+02"
  - "run_report-async-Helper kapselt den get_report_for_employee_range-Call mit fixierter Range/Auth"

requirements-completed: []

# Metrics
duration: ~18min
completed: 2026-06-09
---

# Phase 8.4 Plan 01: Reporting-Additiv-Merge Test-Bereinigung + Scaffold Summary

**Wave-0-Test-Bereinigung: Flag/Gate-Asserts entfernt, neue additive Reporting-Tests (4 Szenarien) + Snapshot-Version-Pin auf 5 angelegt — test-first, RED bis Plan 02, Workspace kompiliert ohne broken-compile-Fenster.**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-06-09T09:14:32Z
- **Completed:** 2026-06-09
- **Tasks:** 2
- **Files modified:** 5 (1 erstellt, 3 modifiziert, 1 gelöscht)

## Accomplishments
- `reporting_flag_off_bit_identity.rs` gelöscht (enthielt `derive_hours_for_range().times(0)`-Trap, der sofort nach Plan 02 bricht) und aus `mod.rs` deregistriert.
- `extra_hours.rs`: Gate-Test `create_vacation_returns_403_error_variant_when_flag_on` gelöscht; `create_vacation_succeeds_when_flag_off` → `create_vacation_always_succeeds` (Flag-Setup durch `is_enabled().times(0)` ersetzt); `create_extra_work_succeeds_when_flag_on` → `create_extra_work_does_not_trigger_flag_check`.
- `cutover.rs`: AFTER-Vacation-Block asserted jetzt `Ok` statt `ExtraHoursCategoryDeprecated` (M-01 Koexistenz); `test_403_body_format_for_deprecated_category` vollständig entfernt (inkl. Modul-Doc-Liste).
- `billing_period_snapshot_locking.rs`: Version-Pin von 4 auf 5 (Phase-8.4-Bump-Begründung), Header aktualisiert.
- Neue `reporting_additive_merge.rs` mit 4 additiven Szenarien (nur extra_hours / nur absence_period / beide distinkte Tage (SC-1-Kern) / konvertierte Rows nicht doppelt) + in `mod.rs` registriert.

## Task Commits

Jeder Task atomar per jj committet:

1. **Task 1: Flag/Gate-Tests entfernen + mod.rs bereinigen** — `98bc6e10` (test)
2. **Task 2: Snapshot-Pin 5 + additive Test-Datei + Registrierung** — `37242798` (test)

_Hinweis: GSD-Auto-Commit ist deaktiviert (`commit_docs: false`); diese SUMMARY-Änderung wird vom User mit jj committet._

## Files Created/Modified
- `service_impl/src/test/reporting_additive_merge.rs` (neu) — 4 additive Reporting-Szenarien mit ReportingMocks/TestDeps-Pattern, `setup_common_mocks`-Helper, `run_report`-async-Helper.
- `service_impl/src/test/mod.rs` — `reporting_flag_off_bit_identity` deregistriert, `reporting_additive_merge` registriert.
- `service_impl/src/test/extra_hours.rs` — Gate-Test gelöscht, zwei Tests umbenannt/vereinfacht (`times(0)` auf is_enabled).
- `service_impl/src/test/billing_period_snapshot_locking.rs` — Pin auf 5, Header-Begründung Phase 8.4.
- `shifty_bin/src/integration_test/cutover.rs` — AFTER-Vacation auf `Ok`, 403-deprecated-Test entfernt.
- `service_impl/src/test/reporting_flag_off_bit_identity.rs` — gelöscht.

## Decisions Made
- `setup_common_mocks`-Helper eingeführt für DRY-Boilerplate über alle 4 Szenarien (über den reinen Copy-Pattern-Vorschlag aus PATTERNS.md hinaus — funktional identisch, nur entdupliziert).
- `run_report` als async-Helper statt `block_on`, da innerhalb `#[tokio::test]` direkt awaitbar (Korrektur eines initialen `futures::executor::block_on`-Ansatzes vor dem ersten Compile).

## Deviations from Plan

None - plan executed exactly as written. Die RED-Tests sind kein Fehler, sondern der explizit gewollte test-first-Zustand (siehe Plan `<verification>` und environment_notes).

## RED-Tests (erwartet, bis Plan 02 landet)

Diese Tests scheitern zur **Laufzeit** (nicht beim Kompilieren) — der Produktionscode aus Plan 02 fehlt noch:

| Test | Datei | RED-Grund |
|------|-------|-----------|
| `test_additive_only_extra_hours` | reporting_additive_merge.rs | Prod liest noch `is_enabled` → `times(0)`-Panic |
| `test_additive_only_absence_period` | reporting_additive_merge.rs | dito |
| `test_additive_both_sources_distinct_days` | reporting_additive_merge.rs | dito |
| `test_additive_converted_rows_not_double_counted` | reporting_additive_merge.rs | dito |
| `test_snapshot_schema_version_pinned` | billing_period_snapshot_locking.rs | Prod-Const ist noch 4, Test pinnt 5 |
| `create_vacation_always_succeeds` | extra_hours.rs | Gate liest noch `is_enabled` für Vacation → `times(0)`-Panic |

Plan 02 entfernt den Flag-Read im Reporting- und im extra_hours-create-Pfad und bumpt die Const auf 5 → alle obigen werden GREEN.

## Issues Encountered
- Acceptance-Criterion `grep -c test_403_body_format_for_deprecated_category == 0` schlug zunächst fehl, weil mein Lösch-Kommentar und die Modul-Doc-Liste den Funktionsnamen weiterhin enthielten. Behoben durch Umformulierung beider Kommentare (kein Funktionsname mehr) → Kriterium erfüllt.

## Build-/Test-Status
- `cargo test -p service_impl --no-run`: **kompiliert sauber**.
- `cargo test --workspace --no-run`: **kompiliert sauber** (inkl. shifty_bin-Integration-Tests mit cutover.rs).
- Kein broken-compile-Fenster — der RED-Zustand ist rein laufzeitseitig (Assertion-/Mock-Panics), wie test-first beabsichtigt.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 02 kann jetzt den Produktionscode ändern (Flag-Filter in `reporting.rs` entfernen, additiver Merge, Gate-Rückbau in `extra_hours.rs`, `CURRENT_SNAPSHOT_SCHEMA_VERSION` → 5) — die Tests sind bereits auf das Zielverhalten ausgerichtet und werden dadurch GREEN.
- Plan 02 führt `cargo test --workspace` als Phase-Gate aus.

## Self-Check: PASSED

- `service_impl/src/test/reporting_additive_merge.rs` — FOUND
- `.planning/phases/08.4-reporting-additiv-merge/08.4-01-SUMMARY.md` — FOUND
- `service_impl/src/test/reporting_flag_off_bit_identity.rs` — correctly DELETED
- Commit `98bc6e10` (Task 1) — FOUND
- Commit `37242798` (Task 2) — FOUND

---
*Phase: 08.4-reporting-additiv-merge*
*Completed: 2026-06-09*
