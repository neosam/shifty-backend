---
phase: 25-feiertags-auto-anrechnung-stichtag-konfiguration
plan: 03
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - shifty-dioxus/src/api.rs
  - shifty-dioxus/src/loader.rs
  - shifty-dioxus/src/page/settings.rs
  - shifty-dioxus/src/i18n/mod.rs
  - shifty-dioxus/src/i18n/en.rs
  - shifty-dioxus/src/i18n/de.rs
  - shifty-dioxus/src/i18n/cs.rs
autonomous: false
requirements: [HCFG-02]
must_haves:
  truths:
    - "An admin can set, change and clear the holiday activation date on the existing admin-gated /settings page; after reload the saved date is shown again (D-25-06, HCFG-02)"
    - "The Save button is enabled whenever the date input is non-empty (NOT gated on change-from-loaded) because programmatic <input type=date> changes do not reliably fire Dioxus signals (D-25-06, HCFG-02)"
    - "Clearing the date disables the automation inline with no confirmation dialog (D-25-06)"
    - "All five new labels exist in en, de and cs and the i18n completeness test passes (D-25-06, HCFG-02)"
  artifacts:
    - path: "shifty-dioxus/src/api.rs"
      provides: "get_toggle_value / set_toggle_value / clear_toggle_value REST clients"
      contains: "/value"
    - path: "shifty-dioxus/src/loader.rs"
      provides: "get_holiday_cutoff_date / set_holiday_cutoff_date"
      contains: "holiday_auto_credit"
    - path: "shifty-dioxus/src/page/settings.rs"
      provides: "Card 2 — holiday auto-credit date input + Save/Clear + feedback"
      contains: "SettingsHolidayAutoCredit"
    - path: "shifty-dioxus/src/i18n/mod.rs"
      provides: "5 new Key variants"
      contains: "SettingsHolidayAutoCreditLabel"
  key_links:
    - from: "shifty-dioxus/src/page/settings.rs"
      to: "shifty-dioxus/src/loader.rs"
      via: "Card 2 load/save call get_holiday_cutoff_date / set_holiday_cutoff_date"
      pattern: "holiday_cutoff_date"
    - from: "shifty-dioxus/src/loader.rs"
      to: "shifty-dioxus/src/api.rs"
      via: "loaders call the toggle value REST clients for key holiday_auto_credit"
      pattern: "toggle_value"
---

<objective>
Add a single date-input field ("Feiertags-Automatik aktiv ab") to the existing admin-gated
settings page so an admin can set / change / clear the holiday-automatic activation date, wired
to the toggle value REST endpoints created in 25-01. Add the three frontend API clients, the two
loader helpers, and the five i18n keys in en/de/cs. This is the HCFG-02 frontend half and the
only user-visible surface in Phase 25 (all reporting changes are invisible derive-on-read).

Purpose: HCFG-02 (frontend half). Implements D-25-06 (settings UI analog to Phase 24, i18n
de/en/cs, inline feedback, no confirmation dialog, WASM date-signal caveat).

Output: Card 2 on /settings, api.rs + loader.rs clients, 5 i18n keys across three locales.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-CONTEXT.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-UI-SPEC.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-PATTERNS.md
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-RESEARCH.md

# 25-01 produced the REST endpoints this plan calls
@.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-01-SUMMARY.md

# Frontend conventions
@shifty-dioxus/CLAUDE.md
</context>

<artifacts_produced>
- api.rs: `get_toggle_value(config, name) -> Option<String>` (204 → None), `set_toggle_value(config, name, value)`, `clear_toggle_value(config, name)`
- loader.rs: `get_holiday_cutoff_date(config)`, `set_holiday_cutoff_date(config, Option<&str>)` (key `holiday_auto_credit`)
- i18n Keys: SettingsHolidayAutoCreditLabel, ...Description, ...Save, ...Clear, ...UnsetHint (+ en/de/cs strings, verbatim from 25-UI-SPEC.md)
- settings.rs Card 2: date input + Save + Clear + inline feedback + unset hint, admin-gated
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Frontend API clients, loaders, and i18n keys (en/de/cs)</name>
  <files>shifty-dioxus/src/api.rs, shifty-dioxus/src/loader.rs, shifty-dioxus/src/i18n/mod.rs, shifty-dioxus/src/i18n/en.rs, shifty-dioxus/src/i18n/de.rs, shifty-dioxus/src/i18n/cs.rs</files>
  <read_first>
    - 25-PATTERNS.md sections "shifty-dioxus/src/api.rs", "shifty-dioxus/src/loader.rs", "shifty-dioxus/src/i18n/{mod,en,de,cs}.rs" (exact functions + verbatim strings)
    - 25-UI-SPEC.md "Copywriting Contract" (the exact en/de/cs strings for all 5 keys) and the status-code contract (GET 204 = None)
    - shifty-dioxus/src/api.rs lines 1574-1595 (set_toggle / get_toggle_enabled analog)
    - shifty-dioxus/src/loader.rs lines 1008-1024 (set_toggle / get_toggle_enabled analog)
    - shifty-dioxus/src/i18n/mod.rs existing Settings Key block + the i18n completeness test (~line 1295)
  </read_first>
  <action>
    In api.rs (after the existing toggle clients ~line 1595) add three functions mirroring the
    existing toggle clients' URL + error pattern: get_toggle_value(config, name) -> Result<
    Option&lt;String&gt;, reqwest::Error&gt; that returns Ok(None) when the response status is 204
    and otherwise parses the JSON Option&lt;String&gt;; set_toggle_value(config, name, value: &str)
    doing a PUT with json(value); clear_toggle_value(config, name) doing a DELETE. Match the actual
    status-code contract finalized in 25-01 (see 25-01-SUMMARY.md) — align the None branch to it.

    In loader.rs (after the existing toggle loaders ~line 1024) add get_holiday_cutoff_date(config)
    delegating to api::get_toggle_value(config, "holiday_auto_credit"), and set_holiday_cutoff_date(
    config, value: Option&lt;&str&gt;) calling set_toggle_value for Some and clear_toggle_value for
    None. Use the exact toggle key string "holiday_auto_credit" (must match 25-01 + 25-02).

    In i18n/mod.rs add the five Key variants after the existing Settings keys:
    SettingsHolidayAutoCreditLabel, SettingsHolidayAutoCreditDescription, SettingsHolidayAutoCreditSave,
    SettingsHolidayAutoCreditClear, SettingsHolidayAutoCreditUnsetHint. In en.rs, de.rs and cs.rs add
    add_text entries for all five with the strings exactly as given in the 25-UI-SPEC.md Copywriting
    Contract table (do not paraphrase). All three locales must be populated or the completeness test fails.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo test i18n</automated>
  </verify>
  <acceptance_criteria>
    api.rs exposes get/set/clear_toggle_value; loader.rs exposes get/set_holiday_cutoff_date keyed to holiday_auto_credit;
    5 new i18n keys present with verbatim en/de/cs strings; the i18n completeness test passes.
  </acceptance_criteria>
  <done>API + loader + i18n compile and the i18n completeness test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Card 2 — holiday auto-credit date input</name>
  <files>shifty-dioxus/src/page/settings.rs</files>
  <read_first>
    - 25-UI-SPEC.md "Layout Contract" (Card 2 structure), "State Machine", "Interaction Contract" (save/clear/load flows), "Accessibility Contract"
    - 25-PATTERNS.md section "shifty-dioxus/src/page/settings.rs" (admin guard, resource+signal, on-save/on-clear handlers, RSX layout)
    - shifty-dioxus/src/page/settings.rs lines 1-148 (Phase 24 Card 1 pattern: admin guard, use_resource, use_effect, inline feedback Some(true)/Some(false)/None at 131-143)
    - MEMORY caveat (reference_dioxus_browser_test_date_inputs): programmatic date input does not trigger Dioxus signals
  </read_first>
  <action>
    Add a second card below the existing Phase-24 card, reusing the admin guard verbatim (if not
    admin → not-authorized rsx). Add signals: date_str (String/ImStr, ISO yyyy-mm-dd or ""),
    date_str_loaded_empty (bool), a save_result (Option&lt;bool&gt;) and a saving (bool). Load via
    use_resource(get_holiday_cutoff_date) + use_effect: Some(Some(date)) → set date_str + loaded_empty
    false; Some(None) → date_str "" + loaded_empty true.

    Build the RSX per the 25-UI-SPEC Layout Contract: div.bg-surface...mt-4 containing Row A
    (label span Key::SettingsHolidayAutoCreditLabel + description span ...Description), Row B
    (max-w-[200px] date input bound to date_str), Row C (Save button → on_save, Clear button → on_clear
    disabled when saving or date_str empty, plus inline feedback reusing Key::SettingsSaved /
    Key::SettingsSaveError per the Some(true)/Some(false)/None match), and Row D (unset hint
    Key::SettingsHolidayAutoCreditUnsetHint shown only when date_str_loaded_empty).

    on_save: if date_str empty return; validate it parses as an ISO date (set save_result Some(false)
    on parse failure and return — client-side input validation, defense in depth); set saving true;
    spawn set_holiday_cutoff_date(Some(&date_str)); on Ok set save_result Some(true) + loaded_empty
    false; on Err Some(false); clear saving. on_clear: spawn set_holiday_cutoff_date(None); on Ok set
    date_str "", loaded_empty true, save_result Some(true); on Err Some(false). NO confirmation dialog
    (inline only, per memory feedback_warnings_inline_not_dialog). CRITICAL WASM caveat: enable Save
    whenever date_str is non-empty — do NOT gate Save on "value changed from loaded state".
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend/shifty-dioxus && cargo build --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    /settings shows Card 2 (admin only) with the date input, Save/Clear buttons, inline feedback and unset hint;
    Save is enabled on any non-empty date; Clear disables the automation without a dialog. WASM build succeeds.
  </acceptance_criteria>
  <done>Card 2 renders and wires load/save/clear to the loaders; WASM build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — settings date field set/change/persist + locale + admin gate</name>
  <action>
    No automation here — Tasks 1-2 built and WASM-compiled the Card 2 date field. This task pauses for
    a human to confirm the set / change / persist-after-reload behaviour, locale switching, and the
    non-admin guard in a real browser, because programmatic &lt;input type=date&gt; changes cannot be
    exercised reliably in automated WASM tests (documented caveat).
  </action>
  <what-built>
    Admin-gated settings Card 2 with a date input that persists the holiday "active-from" date via the
    new toggle value endpoints. The backend reporting effect is invisible (derive-on-read); only the
    set/change/persist-after-reload behaviour of the date field needs human eyes, because programmatic
    <input type=date> changes cannot be exercised reliably in automated WASM tests (documented caveat).
  </what-built>
  <how-to-verify>
    1. Start backend (port 3000) and frontend (`dx serve`, port 8080) — see CLAUDE.md Full Stack section.
    2. As an admin, open http://localhost:8080/settings — Card 2 "Feiertags-Automatik aktiv ab" is visible.
    3. Pick a date, click "Datum speichern" → inline "Saved." appears.
    4. Reload the page → the saved date is still shown (persisted).
    5. Click "Löschen (deaktivieren)" → field clears, unset hint "Nicht gesetzt — Automatik inaktiv." shows.
    6. Switch locale (en/cs) → labels are translated.
    7. As a non-admin, open /settings → the card is not editable / not-authorized guard holds.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered/persisted incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → REST /toggle/{name}/value | Admin client submits a date; server enforces auth + ISO validation (25-01) |
| settings page → admin gate | Non-admins must not edit the cutoff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07 | Elevation of Privilege | settings Card 2 | high | mitigate | Reuse the existing admin guard (is_admin) — non-admins get the not-authorized view; server-side toggle_admin gate (25-01) is authoritative |
| T-25-08 | Tampering | date input → PUT | low | mitigate | Client-side ISO parse before PUT (defense in depth); server re-validates and rejects non-ISO (25-01 T-25-01) |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this phase (RESEARCH audit: N/A) |
</threat_model>

<verification>
- `cargo test i18n` (shifty-dioxus) passes — completeness across en/de/cs.
- `cargo build --target wasm32-unknown-unknown` (shifty-dioxus) succeeds.
- Human checkpoint: set/change/persist-after-reload + locale switch + non-admin guard (HCFG-02 manual half).
</verification>

<success_criteria>
- Admin can set/change/clear the activation date on /settings and it persists across reload (HCFG-02).
- Save enabled on non-empty date (WASM caveat honored); no confirmation dialog on clear.
- All five labels translated in en/de/cs; non-admins cannot edit.
</success_criteria>

<output>
Create `.planning/phases/25-feiertags-auto-anrechnung-stichtag-konfiguration/25-03-SUMMARY.md` when done.
</output>
