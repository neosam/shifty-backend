---
phase: 01-absence-domain-foundation
plan: 01
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - dao/src/absence.rs
  - dao/src/lib.rs
  - dao_impl_sqlite/src/absence.rs
  - dao_impl_sqlite/src/lib.rs
autonomous: true
requirements: [ABS-01, ABS-02]
must_haves:
  truths:
    - "D-01: DAO-Trait + Impl in `dao/src/absence.rs` und `dao_impl_sqlite/src/absence.rs` (Modul-Pfade per CONTEXT.md D-01)."
    - "`AbsenceDao`-Trait existiert mit 7 Methoden inkl. `find_overlapping(.., exclude_logical_id, ..)`."
    - "`AbsencePeriodEntity` modelliert alle 11 DB-Spalten (id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, version, audit-process)."
    - "D-02, D-03: `AbsenceCategoryEntity` hat genau 3 Varianten (Vacation, SickLeave, UnpaidLeave) — eigene Enum, kein Reuse von `ExtraHoursCategory`, Compiler garantiert Kategorie-Validität."
    - "Alle DAO-Read-Queries enthalten `WHERE deleted IS NULL` (Soft-Delete-Konvention)."
    - "`find_overlapping` filtert Inclusive-Allen (`from_date <= ?` UND `to_date >= ?`); im `Some(exclude)`-Branch zusätzlich `logical_id != ?`."
    - "`MockAbsenceDao` ist via `#[automock]` automatisch verfügbar (Voraussetzung für Plan 02-Service-Tests)."
  artifacts:
    - path: "dao/src/absence.rs"
      provides: "AbsenceDao-Trait, AbsencePeriodEntity, AbsenceCategoryEntity, MockAbsenceDao"
      exports: ["AbsenceDao", "AbsencePeriodEntity", "AbsenceCategoryEntity", "MockAbsenceDao"]
    - path: "dao_impl_sqlite/src/absence.rs"
      provides: "AbsenceDaoImpl mit 7 SQLx-Queries"
      contains: "AbsenceDaoImpl"
    - path: "dao/src/lib.rs"
      provides: "Trait-Re-Export"
      contains: "pub mod absence"
    - path: "dao_impl_sqlite/src/lib.rs"
      provides: "Impl-Re-Export"
      contains: "pub mod absence"
  key_links:
    - from: "dao_impl_sqlite/src/absence.rs"
      to: "dao::absence::AbsenceDao"
      via: "impl AbsenceDao for AbsenceDaoImpl"
      pattern: "impl AbsenceDao for AbsenceDaoImpl"
    - from: "dao_impl_sqlite/src/absence.rs::find_overlapping"
      to: "absence_period (DB-Tabelle)"
      via: "sqlx::query_as! mit WHERE sales_person_id = ? AND category = ? AND from_date <= ? AND to_date >= ? AND deleted IS NULL"
      pattern: "from_date <= \\?"
    - from: "dao/src/absence.rs"
      to: "shifty_utils::DateRange"
      via: "use shifty_utils::DateRange als find_overlapping-Argument"
      pattern: "shifty_utils::DateRange"
---

<objective>
Wave 1 — Plan A: DAO-Layer für `absence_period`. Trait + Entity + Enum-Spiegel in `dao/`, SQLx-Implementation in `dao_impl_sqlite/`. `find_overlapping` mit Two-Branch-Pattern (Pitfall 9). Read-Queries durchgängig `WHERE deleted IS NULL`. `MockAbsenceDao` wird via `#[automock]` für die Service-Tests in Plan 02 gebraucht.

Purpose: Service-Layer (Plan 02) hängt direkt von `MockAbsenceDao` und der konkreten Entity-Surface ab. Integration-Tests (Plan 04) verifizieren das DAO durch echtes In-Memory-SQLite.

Output: Trait-Datei, Impl-Datei, zwei `lib.rs`-Patches; SQLx-Offline-Cache nach Implementation regeneriert.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-absence-domain-foundation/01-CONTEXT.md
@.planning/phases/01-absence-domain-foundation/01-RESEARCH.md
@.planning/phases/01-absence-domain-foundation/01-PATTERNS.md
@.planning/phases/01-absence-domain-foundation/01-VALIDATION.md
@.planning/phases/01-absence-domain-foundation/01-00-SUMMARY.md
@dao/src/extra_hours.rs
@dao/src/lib.rs
@dao_impl_sqlite/src/extra_hours.rs
@dao_impl_sqlite/src/lib.rs

<interfaces>
<!-- Verifizierte bestehende Konturen — direkt benutzen, keine Erkundung nötig. -->

`dao/src/extra_hours.rs:1-86` — Trait-Vorlage für `AbsenceDao`:
```rust
use std::sync::Arc;
use async_trait::async_trait;
use mockall::automock;
use uuid::Uuid;

#[automock(type Transaction = crate::MockTransaction;)]
#[async_trait]
pub trait ExtraHoursDao {
    type Transaction: crate::Transaction;

    async fn find_by_id(&self, id: Uuid, tx: Self::Transaction) -> Result<Option<ExtraHoursEntity>, crate::DaoError>;
    async fn find_by_logical_id(&self, logical_id: Uuid, tx: Self::Transaction) -> Result<Option<ExtraHoursEntity>, crate::DaoError>;
    // ...
    async fn create(&self, entity: &ExtraHoursEntity, process: &str, tx: Self::Transaction) -> Result<(), crate::DaoError>;
    async fn update(&self, entity: &ExtraHoursEntity, process: &str, tx: Self::Transaction) -> Result<(), crate::DaoError>;
}
```

`dao_impl_sqlite/src/extra_hours.rs:70-78` — DAO-Impl-Struct + Konstruktor:
```rust
pub struct ExtraHoursDaoImpl {
    pub _pool: Arc<sqlx::SqlitePool>,
}
impl ExtraHoursDaoImpl {
    pub fn new(pool: Arc<sqlx::SqlitePool>) -> Self {
        Self { _pool: pool }
    }
}
```

`dao_impl_sqlite/src/extra_hours.rs:218-241` — Update-Idiom (Soft-Delete-only):
```rust
async fn update(&self, entity: &ExtraHoursEntity, process: &str, tx: Self::Transaction) -> Result<(), crate::DaoError> {
    let id_vec = entity.id.as_bytes().to_vec();
    let version_vec = entity.version.as_bytes().to_vec();
    let delete = entity.deleted.map(|dt| dt.format(&Iso8601::DATE_TIME)).transpose()?;
    query!(
        "UPDATE extra_hours SET deleted = ?, update_version = ?, update_process = ? WHERE id = ?",
        delete, version_vec, process, id_vec,
    ).execute(tx.tx.lock().await.as_mut()).await.map_db_error()?;
    Ok(())
}
```

`dao/src/lib.rs:14` — `pub mod extra_hours;` (Pattern für Re-Export).
`dao_impl_sqlite/src/lib.rs:15` — `pub mod extra_hours;` (Pattern).
</interfaces>

<additivity>
- KEINE Datei in `dao/src/extra_hours.rs`, `dao_impl_sqlite/src/extra_hours.rs`, `service_impl/...` darf editiert werden.
- KEINE Änderung am `MockTransaction`-Trait oder `crate::Transaction`-Surface.
</additivity>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: `dao/src/absence.rs` — Trait, Entity, Enum-Spiegel, automock</name>
  <files>dao/src/absence.rs, dao/src/lib.rs</files>
  <read_first>
    - dao/src/extra_hours.rs (Vorlage komplett, Zeilen 1–86)
    - dao/src/lib.rs (Re-Export-Pattern Zeile 14)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§6 + Code-Beispiel Zeilen 818–902)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer C/AbsenceDao, Zeilen 137–215)
    - .planning/phases/01-absence-domain-foundation/01-CONTEXT.md (D-01..D-05, D-15)
  </read_first>
  <behavior>
    - `MockAbsenceDao` (automock-generiert) ist von Tests in Plan 02 importierbar.
    - `AbsenceCategoryEntity` matched mit Compiler-Garantie auf genau 3 Varianten — `ExtraWork`/`Custom(_)` lassen sich nicht in eine `AbsencePeriodEntity` setzen.
    - `find_overlapping`-Signatur erlaubt `exclude_logical_id: Option<Uuid>` (None bei Create, Some bei Update — D-15).
    - Trait kompiliert ohne Impl (reines Interface, automock-bound).
  </behavior>
  <action>
    1. **Neue Datei `dao/src/absence.rs`** mit folgendem Inhalt (Vorlage: `dao/src/extra_hours.rs`):

       ```rust
       use std::sync::Arc;

       use async_trait::async_trait;
       use mockall::automock;
       use shifty_utils::DateRange;
       use time::Date;
       use uuid::Uuid;

       #[derive(Clone, Debug, PartialEq, Eq)]
       pub enum AbsenceCategoryEntity {
           Vacation,
           SickLeave,
           UnpaidLeave,
       }

       #[derive(Clone, Debug, PartialEq, Eq)]
       pub struct AbsencePeriodEntity {
           pub id: Uuid,
           pub logical_id: Uuid,
           pub sales_person_id: Uuid,
           pub category: AbsenceCategoryEntity,
           pub from_date: Date,
           pub to_date: Date,
           pub description: Arc<str>,
           pub created: time::PrimitiveDateTime,
           pub deleted: Option<time::PrimitiveDateTime>,
           pub version: Uuid,
       }

       #[automock(type Transaction = crate::MockTransaction;)]
       #[async_trait]
       pub trait AbsenceDao {
           type Transaction: crate::Transaction;

           async fn find_by_id(
               &self,
               id: Uuid,
               tx: Self::Transaction,
           ) -> Result<Option<AbsencePeriodEntity>, crate::DaoError>;

           async fn find_by_logical_id(
               &self,
               logical_id: Uuid,
               tx: Self::Transaction,
           ) -> Result<Option<AbsencePeriodEntity>, crate::DaoError>;

           async fn find_by_sales_person(
               &self,
               sales_person_id: Uuid,
               tx: Self::Transaction,
           ) -> Result<Arc<[AbsencePeriodEntity]>, crate::DaoError>;

           async fn find_all(
               &self,
               tx: Self::Transaction,
           ) -> Result<Arc<[AbsencePeriodEntity]>, crate::DaoError>;

           /// Find active rows for a sales person and category that overlap `range`.
           /// `exclude_logical_id` is used during update so the row being edited
           /// does not collide with itself (D-15).
           async fn find_overlapping(
               &self,
               sales_person_id: Uuid,
               category: AbsenceCategoryEntity,
               range: DateRange,
               exclude_logical_id: Option<Uuid>,
               tx: Self::Transaction,
           ) -> Result<Arc<[AbsencePeriodEntity]>, crate::DaoError>;

           async fn create(
               &self,
               entity: &AbsencePeriodEntity,
               process: &str,
               tx: Self::Transaction,
           ) -> Result<(), crate::DaoError>;

           /// Soft-delete or audit-update: writes `deleted`, `update_version`, `update_process` BY `id`.
           /// Reine Mutation des Body NICHT unterstützt — Service rotiert via `update(tombstone) + create(neu)`.
           async fn update(
               &self,
               entity: &AbsencePeriodEntity,
               process: &str,
               tx: Self::Transaction,
           ) -> Result<(), crate::DaoError>;
       }
       ```

       **Bewusst weggelassen** (per RESEARCH.md §6 Notiz): kein `delete`-Trait-Method (ExtraHours hat es als `unimplemented!()`; sauberer = nicht im Trait).

    2. **Patch `dao/src/lib.rs`** — vorhandene Zeile 14 ist `pub mod extra_hours;`. Neue Zeile direkt darunter (alphabetisch wäre `absence` davor — erlaubt; einfacher: direkt nach `extra_hours`):
       ```rust
       pub mod absence;
       pub mod extra_hours;
       ```
       (oder konsistent vor `extra_hours;` einsortieren — alphabetische Reihenfolge ist Repo-Konvention.)

    Anti-Patterns explizit verboten:
    - KEIN `Custom(Uuid)` oder anderer Variant in `AbsenceCategoryEntity` (D-02, D-03 — saubere Domain-Trennung).
    - KEIN `delete(&self, id: Uuid, …)` Trait-Method (RESEARCH.md §6 Notiz).
    - KEIN Reuse von `dao::extra_hours::ExtraHoursCategoryEntity` (D-03).
    - KEIN `exclude_logical_id: Uuid` (muss `Option<Uuid>`, sonst kein Create-Pfad ohne Exclude).
  </action>
  <verify>
    <automated>cargo build -p dao</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dao/src/absence.rs` exit 0.
    - `grep -c "pub trait AbsenceDao" dao/src/absence.rs` = 1.
    - `grep -c "pub struct AbsencePeriodEntity" dao/src/absence.rs` = 1.
    - `grep -c "pub enum AbsenceCategoryEntity" dao/src/absence.rs` = 1.
    - `grep -E "Vacation|SickLeave|UnpaidLeave" dao/src/absence.rs | wc -l` ≥ 3.
    - `grep -c "Custom\\|ExtraWork\\|Holiday\\|Unavailable\\|VolunteerWork" dao/src/absence.rs` = 0 (D-02/03).
    - `grep -c "#\\[automock(type Transaction = crate::MockTransaction;)\\]" dao/src/absence.rs` = 1.
    - `grep -E "fn (find_by_id|find_by_logical_id|find_by_sales_person|find_all|find_overlapping|create|update)" dao/src/absence.rs | wc -l` ≥ 7.
    - `grep -c "fn delete" dao/src/absence.rs` = 0 (RESEARCH §6 Notiz).
    - `grep -c "exclude_logical_id: Option<Uuid>" dao/src/absence.rs` = 1 (D-15).
    - `grep -c "shifty_utils::DateRange" dao/src/absence.rs` ≥ 1.
    - `grep -c "pub mod absence" dao/src/lib.rs` = 1.
    - `cargo build -p dao` exit 0.
    - `cargo doc -p dao --no-deps 2>&1 | grep -c "warning: missing documentation\\|error" | xargs -I{} test {} -le 50` (Doc-Build sauber, kein neuer Build-Fehler).
  </acceptance_criteria>
  <done>Trait + Entity + Enum + automock kompiliert; Mock wird ab Plan 02 nutzbar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: `dao_impl_sqlite/src/absence.rs` — SQLx-Implementation aller 7 Methoden</name>
  <files>dao_impl_sqlite/src/absence.rs, dao_impl_sqlite/src/lib.rs</files>
  <read_first>
    - dao_impl_sqlite/src/extra_hours.rs (komplette Vorlage, Zeilen 1–251)
    - dao_impl_sqlite/src/lib.rs (Re-Export Zeile 15)
    - .planning/phases/01-absence-domain-foundation/01-RESEARCH.md (§7 find_overlapping Two-Branch Zeilen 1127–1203, §3 Schema)
    - .planning/phases/01-absence-domain-foundation/01-PATTERNS.md (Layer C dao_impl, Zeilen 218–367)
    - .planning/phases/01-absence-domain-foundation/01-00-SUMMARY.md (Migration ist live)
  </read_first>
  <behavior>
    - Alle 7 DAO-Methoden compilieren mit `query_as!`/`query!` gegen die neue Tabelle (Pitfall 5: Migration MUSS bereits gelaufen sein).
    - `find_overlapping` mit `Some(exclude)` filtert die Self-Row zuverlässig aus (D-15).
    - Single-Day-Range `[2026-05-15, 2026-05-15]` matcht gegen sich selbst (Inclusive Allen, Pitfall 1).
    - `find_by_id`/`find_by_logical_id`/`find_by_sales_person`/`find_all`/`find_overlapping` enthalten alle `AND deleted IS NULL` (Pitfall 3, CC-05).
    - `update` schreibt `deleted`, `update_version`, `update_process` per `id` — reine Soft-Delete-Mutation, kein Body-Update.
    - Unbekannter `category`-String → `DaoError::EnumValueNotFound`.
  </behavior>
  <action>
    Erstelle `dao_impl_sqlite/src/absence.rs` mit dem Folgenden Aufbau (Vorlage: `dao_impl_sqlite/src/extra_hours.rs`):

    1. **Imports** (analog `extra_hours.rs:1-13`):
       ```rust
       use std::sync::Arc;

       use crate::{ResultDbErrorExt, TransactionImpl};
       use async_trait::async_trait;
       use dao::{
           absence::{AbsenceCategoryEntity, AbsenceDao, AbsencePeriodEntity},
           DaoError,
       };
       use shifty_utils::DateRange;
       use sqlx::{query, query_as};
       use time::{format_description::well_known::Iso8601, Date, PrimitiveDateTime};
       use uuid::Uuid;
       ```

    2. **DB-Row-Struct** (analog `ExtraHoursDb`):
       ```rust
       struct AbsencePeriodDb {
           id: Vec<u8>,
           logical_id: Vec<u8>,
           sales_person_id: Vec<u8>,
           category: String,
           from_date: String,
           to_date: String,
           description: Option<String>,
           created: String,
           deleted: Option<String>,
           update_version: Vec<u8>,
       }
       ```
       Felder-Reihenfolge MUSS mit der `SELECT id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_version FROM absence_period`-Reihenfolge in jedem `query_as!` übereinstimmen (sqlx-Pflicht).

    3. **TryFrom<&AbsencePeriodDb> für AbsencePeriodEntity** (analog `extra_hours.rs:27-68`):
       - `id`/`logical_id`/`sales_person_id`/`update_version` via `Uuid::from_slice(&bytes)?`.
       - `category` Match: `"Vacation" => Vacation`, `"SickLeave" => SickLeave`, `"UnpaidLeave" => UnpaidLeave`, `value => return Err(DaoError::EnumValueNotFound(value.into()))`.
       - `from_date` und `to_date` via `Date::parse(&row.from_date, &Iso8601::DATE)?` (gleiches Idiom wie `extra_hours.rs:55-58` für `date_time`, aber mit `Iso8601::DATE` für day-only).
       - `description` via `row.description.clone().unwrap_or_default().as_str().into()` (→ `Arc<str>`).
       - `created` via `PrimitiveDateTime::parse(&row.created, &Iso8601::DATE_TIME)?`.
       - `deleted` via `row.deleted.as_ref().map(|s| PrimitiveDateTime::parse(s, &Iso8601::DATE_TIME)).transpose()?`.

    4. **Helper-Function** (private, im selben Modul):
       ```rust
       fn category_to_str(c: &AbsenceCategoryEntity) -> &'static str {
           match c {
               AbsenceCategoryEntity::Vacation => "Vacation",
               AbsenceCategoryEntity::SickLeave => "SickLeave",
               AbsenceCategoryEntity::UnpaidLeave => "UnpaidLeave",
           }
       }
       ```

    5. **Struct + Konstruktor** (analog `extra_hours.rs:70-78`):
       ```rust
       pub struct AbsenceDaoImpl {
           pub _pool: Arc<sqlx::SqlitePool>,
       }

       impl AbsenceDaoImpl {
           pub fn new(pool: Arc<sqlx::SqlitePool>) -> Self {
               Self { _pool: pool }
           }
       }
       ```

    6. **`#[async_trait] impl AbsenceDao for AbsenceDaoImpl`** mit folgenden 7 Methoden — JEDE Read-Query enthält `AND deleted IS NULL` (CC-05, Pitfall 3):

       - `find_by_id(id, tx)`:
         ```sql
         SELECT id, logical_id, sales_person_id, category, from_date, to_date,
                description, created, deleted, update_version
           FROM absence_period
          WHERE id = ?
            AND deleted IS NULL
         ```
         Bind: `id.as_bytes().to_vec()`. `.fetch_optional(...)` → `.map(AbsencePeriodEntity::try_from).transpose()`.

       - `find_by_logical_id(logical_id, tx)`:
         ```sql
         SELECT ... FROM absence_period WHERE logical_id = ? AND deleted IS NULL
         ```
         Analog `find_by_id` mit `logical_id.as_bytes().to_vec()`.

       - `find_by_sales_person(sales_person_id, tx)`:
         ```sql
         SELECT ... FROM absence_period
          WHERE sales_person_id = ? AND deleted IS NULL
          ORDER BY from_date
         ```
         `.fetch_all(...)` → `rows.iter().map(AbsencePeriodEntity::try_from).collect::<Result<Arc<[_]>, _>>()`.

       - `find_all(tx)`:
         ```sql
         SELECT ... FROM absence_period WHERE deleted IS NULL
          ORDER BY sales_person_id, from_date
         ```
         Analog `find_by_sales_person` ohne sp-Filter.

       - `find_overlapping(sales_person_id, category, range, exclude_logical_id, tx)` — **Two-Branch-Pattern** (Pitfall 9, RESEARCH §7.1):
         ```rust
         let sp_vec = sales_person_id.as_bytes().to_vec();
         let category_str = category_to_str(&category);
         let from_str = range.from().format(&Iso8601::DATE)?;
         let to_str = range.to().format(&Iso8601::DATE)?;

         let rows = match exclude_logical_id {
             Some(exclude) => {
                 let exclude_vec = exclude.as_bytes().to_vec();
                 query_as!(
                     AbsencePeriodDb,
                     "SELECT id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_version FROM absence_period WHERE sales_person_id = ? AND category = ? AND from_date <= ? AND to_date >= ? AND logical_id != ? AND deleted IS NULL",
                     sp_vec, category_str, to_str, from_str, exclude_vec,
                 ).fetch_all(tx.tx.lock().await.as_mut()).await.map_db_error()?
             }
             None => {
                 query_as!(
                     AbsencePeriodDb,
                     "SELECT id, logical_id, sales_person_id, category, from_date, to_date, description, created, deleted, update_version FROM absence_period WHERE sales_person_id = ? AND category = ? AND from_date <= ? AND to_date >= ? AND deleted IS NULL",
                     sp_vec, category_str, to_str, from_str,
                 ).fetch_all(tx.tx.lock().await.as_mut()).await.map_db_error()?
             }
         };

         rows.iter().map(AbsencePeriodEntity::try_from).collect::<Result<Arc<[_]>, _>>()
         ```
         **Allen-Inclusive-Bind-Reihenfolge ist KRITISCH:** `from_date <= to_str` (existing.from ≤ probe.to) und `to_date >= from_str` (existing.to ≥ probe.from). Pitfall 1: niemals `<` oder `>` benutzen.

       - `create(entity, process, tx)`:
         ```sql
         INSERT INTO absence_period
            (id, logical_id, sales_person_id, category, from_date, to_date,
             description, created, deleted, update_process, update_version)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
         ```
         Binds: `id_vec, logical_id_vec, sp_vec, category_to_str(&entity.category), entity.from_date.format(&Iso8601::DATE)?, entity.to_date.format(&Iso8601::DATE)?, entity.description.as_ref(), entity.created.format(&Iso8601::DATE_TIME)?, entity.deleted.map(|d| d.format(&Iso8601::DATE_TIME)).transpose()?, process, entity.version.as_bytes().to_vec()`.

       - `update(entity, process, tx)` (analog `extra_hours.rs:218-241`):
         ```sql
         UPDATE absence_period
            SET deleted = ?, update_version = ?, update_process = ?
          WHERE id = ?
         ```
         Binds: `delete_str, version_vec, process, id_vec`.

    7. **Patch `dao_impl_sqlite/src/lib.rs`** — neben `pub mod extra_hours;` (Zeile 15) füge `pub mod absence;` ein (alphabetisch davor):
       ```rust
       pub mod absence;
       ...
       pub mod extra_hours;
       ```

    8. **SQLx-Offline-Cache regenerieren** nach Implementation (Pitfall 5):
       ```
       nix-shell -p sqlx-cli --run "cargo sqlx prepare --workspace"
       ```

    Anti-Patterns explizit verboten:
    - KEIN `WHERE deleted IS NULL` weglassen in irgendeinem `SELECT` (Pitfall 3).
    - KEIN Single-Query mit Sentinel-UUID statt Two-Branch (Pitfall 9).
    - KEIN `from_date < ?` (half-open) — Pitfall 1, Single-Day-Range bricht.
    - KEIN `delete`-Method-Impl (Trait hat keinen). DAO-Soft-Delete läuft über `update(entity { deleted: Some(now) })`.
    - KEIN ALTER an `dao_impl_sqlite/src/extra_hours.rs` (Pitfall 7).
  </action>
  <verify>
    <automated>cargo build -p dao_impl_sqlite</automated>
  </verify>
  <acceptance_criteria>
    - `test -f dao_impl_sqlite/src/absence.rs` exit 0.
    - `grep -c "pub struct AbsenceDaoImpl" dao_impl_sqlite/src/absence.rs` = 1.
    - `grep -c "impl AbsenceDao for AbsenceDaoImpl" dao_impl_sqlite/src/absence.rs` = 1.
    - `grep -c "pub fn new(pool: Arc<sqlx::SqlitePool>)" dao_impl_sqlite/src/absence.rs` = 1.
    - `grep -v '^[[:space:]]*--' dao_impl_sqlite/src/absence.rs | grep -v '^[[:space:]]*//' | grep -E "FROM absence_period" | grep -v "AND deleted IS NULL" | wc -l` = 0 (jede SELECT-Query hat den Soft-Delete-Filter; CC-05).
    - `grep -E "from_date *<= *\\?" dao_impl_sqlite/src/absence.rs | wc -l` ≥ 1 (Inclusive Allen).
    - `grep -E "to_date *>= *\\?" dao_impl_sqlite/src/absence.rs | wc -l` ≥ 1 (Inclusive Allen).
    - `grep -E "from_date *< *\\?|to_date *> *\\?" dao_impl_sqlite/src/absence.rs | wc -l` = 0 (KEINE half-open Bounds, Pitfall 1).
    - `grep -c "logical_id != \\?" dao_impl_sqlite/src/absence.rs` = 1 (D-15 Two-Branch-Variante).
    - `grep -c "fn delete" dao_impl_sqlite/src/absence.rs` = 0.
    - `grep -c "category_to_str" dao_impl_sqlite/src/absence.rs` ≥ 1.
    - `grep -c "EnumValueNotFound" dao_impl_sqlite/src/absence.rs` ≥ 1.
    - `grep -c "pub mod absence" dao_impl_sqlite/src/lib.rs` = 1.
    - `cargo build -p dao_impl_sqlite` exit 0.
    - `cargo build --workspace` exit 0.
    - `git diff dao_impl_sqlite/src/extra_hours.rs` ist leer (Pitfall 7).
    - Nach `cargo sqlx prepare --workspace`: neue `.sqlx/query-*.json`-Files für die `absence_period`-Queries vorhanden (`grep -l "absence_period" .sqlx/*.json | wc -l` ≥ 7 — 7 Queries: 4 reads + create + update + 2 find_overlapping = 8, mindestens 7).
  </acceptance_criteria>
  <done>SQLx-Impl kompiliert, Offline-Cache regeneriert, alle Read-Queries soft-delete-gefiltert, Two-Branch find_overlapping mit Inclusive-Allen.</done>
</task>

<task type="auto">
  <name>Task 1.3: Wave-1-DAO-Smoke-Gate (`cargo build --workspace` + scope test)</name>
  <files>(no file modifications — verification gate)</files>
  <read_first>
    - .planning/phases/01-absence-domain-foundation/01-VALIDATION.md (Sampling Rate)
  </read_first>
  <action>
    Run:
    ```
    cargo build --workspace
    cargo test --workspace --no-run
    cargo test -p dao
    cargo test -p dao_impl_sqlite
    cargo test -p shifty-utils
    ```
    Expected: alles grün; keine neuen Failures gegenüber Wave-0-Stand.

    Falls `cargo test -p dao_impl_sqlite` neue Failures wirft, die nicht durch Phase-1-Tasks erklärbar sind (z.B. weil eine bestehende sqlx-prepare-Kopie veraltet ist), dann **erneut** `cargo sqlx prepare --workspace` laufen lassen.
  </action>
  <verify>
    <automated>cargo build --workspace &amp;&amp; cargo test -p dao -p dao_impl_sqlite -p shifty-utils</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exit 0.
    - `cargo test --workspace --no-run 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0` (Tests compilieren).
    - `cargo test -p dao -p dao_impl_sqlite -p shifty-utils 2>&1 | grep -c "FAILED"` = 0.
    - `git diff service_impl/src/billing_period_report.rs service_impl/src/extra_hours.rs service_impl/src/booking.rs` ist leer (Additivität).
  </acceptance_criteria>
  <done>Wave 1 DAO-Layer abgeschlossen; Wave 2 (Service) freigegeben.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → DAO | Service übergibt Domain-Werte (sales_person_id, category, range); DAO bindet sie als parameterized SQLx-Queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `category` (TEXT-Spalte, untrusted Input) | mitigate | sqlx parameterized binding via `query_as!`; `try_from`-Mapping wirft `DaoError::EnumValueNotFound` für unbekannte Strings. |
| T-01-01-02 | Information Disclosure | Soft-deleted Tombstones | mitigate | Jeder Read-SQL enthält `AND deleted IS NULL` (CC-05); grep-gate in acceptance_criteria erzwingt das (Pitfall 3). |
| T-01-01-03 | Tampering | `find_overlapping` Self-Collision beim Update | mitigate | Two-Branch-Pattern mit `logical_id != ?`-Filter (D-15, Pitfall 9). |
| T-01-01-04 | DoS | unbounded `find_all`-Query | accept | Phase 1 hat keinen Pagination-Mechanismus; Read-Sicht beschränkt durch HR-only-Permission im Service-Layer (Plan 02). v2 kann Pagination nachziehen. |
</threat_model>

<verification>
- `cargo build -p dao` und `cargo build -p dao_impl_sqlite` als Compile-Gates.
- `cargo sqlx prepare --workspace` regeneriert Offline-Cache; neue `absence_period`-Queries sichtbar als `.sqlx/query-*.json`.
- Pattern-Grep-Gates auf inclusive Allen + Soft-Delete-Filter + Two-Branch-Form als acceptance_criteria.
- Additivität: kein Diff in ExtraHours-DAO-Files (Pitfall 7).
</verification>

<success_criteria>
- `dao::absence::AbsenceDao`, `AbsencePeriodEntity`, `AbsenceCategoryEntity`, `MockAbsenceDao` sind exportierbar.
- `dao_impl_sqlite::absence::AbsenceDaoImpl` implementiert alle 7 Trait-Methoden.
- Alle Read-SQL haben `WHERE deleted IS NULL`.
- `find_overlapping` benutzt Inclusive-Allen-Bounds.
- `cargo build --workspace` ist grün; SQLx-Cache regeneriert.
</success_criteria>

<output>
After completion, create `.planning/phases/01-absence-domain-foundation/01-01-SUMMARY.md` mit:
- DAO-Trait-Surface (Methoden-Liste mit Signaturen).
- Entity-Felder-Liste (`AbsencePeriodEntity`).
- SQLx-Query-Set (Liste der 8 SQL-Strings, eine Zeile pro Methode).
- Confirmed: `cargo build --workspace` grün; `.sqlx/`-Cache enthält ≥ 7 absence_period-Queries.
- Discrete: keine `delete`-Methode im Trait (D-07; service rotiert via update+create).
</output>
