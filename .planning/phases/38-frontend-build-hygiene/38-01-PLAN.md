---
phase: 38-frontend-build-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shifty-dioxus/src/page/shiftplan.rs
  - "shifty-dioxus/src/** (additional files touched by `cargo fix` — unused imports/vars/mut, determined at execution)"
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "The live warning baseline is re-captured from the CURRENT tree (post phases 36/37) before any change — not taken from the stale 2026-07-01 CONTEXT list (HYG-01)"
    - "`cargo build` (shifty-dioxus) no longer emits any unused-import / unused-variable / unnecessary-`mut` warnings after `cargo fix` (HYG-01, D-02)"
    - "The two deprecated `time::format_description::parse` call sites in `src/page/shiftplan.rs` are migrated to `parse_borrowed` with identical parsing behavior (HYG-01, D-05)"
    - "Scope stays strictly on the rustc `cargo build` warnings; the ~198 dioxus clippy lints are untouched and dioxus remains out of the CI clippy gate (D-08, D-09)"
  artifacts:
    - "shifty-dioxus/src/page/shiftplan.rs (deprecated parse sites migrated, still compiles)"
    - ".planning/phases/38-frontend-build-hygiene/38-01-SUMMARY.md (records the re-captured baseline + the categorized warning counts)"
  key_links:
    - "cargo fix output → auto-fixable warning bucket reduced to 0 (HYG-01)"
    - "parse → parse_borrowed rename → deprecated warning bucket reduced to 0 (HYG-01, D-05)"
---

<objective>
Clear the mechanically-fixable slice of the shifty-dioxus rustc warnings: re-baseline
the CURRENT `cargo build` warning set, auto-remove unused imports / unused variables /
unnecessary `mut` via `cargo fix` (D-02), and migrate the two deprecated
`time::format_description::parse` call sites to `parse_borrowed` (D-05). This leaves only
dead-code warnings for plan 38-02 to remediate manually.

Purpose: Knock out the low-risk, high-volume portion of HYG-01 first so the manual
dead-code pass in 38-02 starts from a clean, dead-code-only warning list.

Output: A shifty-dioxus tree whose `cargo build` warnings are now exclusively dead-code
warnings; a 38-01-SUMMARY.md recording the re-captured baseline and the categorized counts.

Scope guard (D-08, D-09): ONLY the rustc `cargo build` warnings are in scope. Do NOT
touch the ~198 pre-existing dioxus clippy lints and do NOT add dioxus to any clippy gate.

Artifacts this phase produces: This is a removal/rename phase — it mostly deletes or
rewrites code rather than adding files. The only genuinely new artifact is the SUMMARY.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-frontend-build-hygiene/38-CONTEXT.md
@.planning/ROADMAP.md
@shifty-dioxus/CLAUDE.md
</context>

<build_conventions>
- All dioxus builds/tests run from `shifty-dioxus/` through the project dev shell. Per
  CONTEXT + shifty-dioxus/CLAUDE.md the convention is `nix develop ../ --command <cmd>`
  (plain `nix-shell` is broken — shell.nix is broken). Adjust only if the dev shell name
  differs on this machine; the point is a working rust toolchain, not the exact invocation.
- The warning-count check is HYG-01's core signal: `cargo build 2>&1 | grep -c 'warning:'`.
- Do NOT run clippy in the dioxus shell (E0514). dioxus clippy is out of scope this phase.
</build_conventions>

<tasks>

<task type="execute">
  <name>Task 1: Re-baseline the CURRENT cargo build warning set</name>
  <files>shifty-dioxus/ (read-only build; no source edits in this task)</files>
  <read_first>
    - .planning/phases/38-frontend-build-hygiene/38-CONTEXT.md (the 2026-07-01 warning inventory — treat as STALE reference, not ground truth)
    - shifty-dioxus/CLAUDE.md (build conventions)
  </read_first>
  <action>
    From `shifty-dioxus/`, produce a fresh warning list from the current tree and capture
    it to a scratch file (e.g. under the session scratchpad, NOT under .planning/):
    `nix develop ../ --command cargo build 2>&1 | tee <scratch>/38-warnings-baseline.txt`.

    CRITICAL — the CONTEXT "50 warnings" list was captured BEFORE phases 36 and 37 landed
    new frontend code (SelectInput `value` prop / settings.rs; dialog.rs BackdropPress /
    absence_convert_modal.rs / contract_modal.rs help spans / new i18n keys). The current
    set may differ: some previously-dead symbols may now be USED, and there may be new
    warnings. Act on THIS live list, never on the stale CONTEXT list.

    Categorize every current warning into three buckets and record the counts:
    (a) auto-fixable (unused imports, unused variables, unnecessary `mut`) — for `cargo fix`;
    (b) deprecated `time::format_description::parse` (expected: the 2 sites in
        src/page/shiftplan.rs at ~line 122 and ~line 1291) — for the parse_borrowed rename;
    (c) dead-code (never-constructed variants, unused fns/methods/enums/structs/consts,
        never-read fields) — deferred to plan 38-02.

    Do NOT modify any source in this task. Output is the categorized inventory only; carry
    the numbers into the SUMMARY so 38-02 and the checker can see the delta from CONTEXT.
  </action>
  <verify>
    <automated>cd shifty-dioxus && nix develop ../ --command cargo build 2>&1 | grep -c 'warning:' ; true</automated>
  </verify>
  <acceptance_criteria>
    - A fresh `cargo build` warning list from the current tree is captured to a scratch file.
    - Every current warning is assigned to one of the three buckets (auto-fixable / deprecated / dead-code) with counts recorded for the SUMMARY.
    - No source files were modified in this task.
  </acceptance_criteria>
  <done>A fresh, categorized warning inventory from the current tree is captured and recorded for the SUMMARY; no source edits made.</done>
</task>

<task type="execute">
  <name>Task 2: cargo fix the auto-fixable warnings + migrate deprecated parse → parse_borrowed</name>
  <files>shifty-dioxus/src/page/shiftplan.rs, shifty-dioxus/src/** (files touched by cargo fix)</files>
  <read_first>
    - shifty-dioxus/src/page/shiftplan.rs (the two deprecated call sites, ~line 122 and ~line 1291)
    - The scratch baseline file from Task 1 (the auto-fixable + deprecated buckets)
  </read_first>
  <action>
    Step 1 — auto-fixable (D-02): from `shifty-dioxus/`, run
    `nix develop ../ --command cargo fix --bin shifty-dioxus -p shifty-dioxus --allow-dirty --allow-staged`.
    This removes the unused imports / unused variables (e.g. on_close, error_store,
    date_iso_format_clone1/2) / unnecessary `mut`. The exact count may differ from the
    stale "14" — trust the live result.

    Step 2 — deprecated parse (D-05): in src/page/shiftplan.rs, rename the two
    `time::format_description::parse("...")` calls to `time::format_description::parse_borrowed(...)`.
    Note `parse_borrowed` is generic over a const format-description version, so the call is
    `parse_borrowed::<2>("[day].[month]")` and `parse_borrowed::<2>("[hour]:[minute]")`.
    Both format strings are string literals ('static), so the returned borrowed items are
    `'static` and remain a drop-in for the existing `.unwrap()` + downstream usage — no
    behavior change. If (and only if) the migration turns out to be awkward at these sites
    (unexpected lifetime/borrow friction), fall back to `#[allow(deprecated)]` per D-05 and
    record why in the SUMMARY.

    Step 3 — rebuild and confirm the auto-fixable and deprecated buckets are now empty; any
    remaining warnings must be dead-code only (handed to plan 38-02). Do NOT start deleting
    dead code in this plan.
  </action>
  <verify>
    <automated>cd shifty-dioxus && nix develop ../ --command cargo build 2>&1 | grep -E 'unused_imports|unused_variables|unused_mut|deprecated|format_description::parse' | grep -c 'warning' </automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` still compiles (exit 0) after cargo fix + the parse_borrowed rename.
    - The auto-fixable warning bucket (unused import/variable, unnecessary mut) is 0.
    - The deprecated `time::format_description::parse` warning bucket is 0 (either migrated to parse_borrowed, or — only if awkward — a justified `#[allow(deprecated)]` per D-05 recorded in SUMMARY).
    - No dead-code symbols were deleted in this plan (that is 38-02's job).
  </acceptance_criteria>
  <done>`cargo build` compiles; auto-fixable and deprecated warning buckets are 0 (parse migrated to parse_borrowed); only dead-code warnings remain for 38-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure compiler-warning cleanup: no new inputs, endpoints, deps, or data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Tampering | parse_borrowed rename in src/page/shiftplan.rs | low | mitigate | Format strings are 'static literals; rebuild confirms identical parse behavior. No runtime input change. |
| T-38-SC | Tampering | package installs | n/a | accept | No new dependencies installed this phase (D: no new deps) — supply-chain gate not applicable. |
</threat_model>

<verification>
- `cargo build` (shifty-dioxus, via dev shell) compiles with exit 0.
- The auto-fixable and deprecated warning buckets are both 0; only dead-code warnings remain.
- No dioxus clippy work performed (D-08/D-09 scope guard honored).
</verification>

<success_criteria>
- Re-captured live warning baseline recorded (delta from CONTEXT noted).
- Unused-import/variable/`mut` warnings eliminated via `cargo fix` (D-02).
- Both deprecated `parse` sites migrated to `parse_borrowed` (D-05), build green.
- Remaining warnings are exclusively dead-code, ready for plan 38-02.
</success_criteria>

<output>
Create `.planning/phases/38-frontend-build-hygiene/38-01-SUMMARY.md` when done —
include the re-captured baseline counts (auto-fixable / deprecated / dead-code) and the
delta from the stale CONTEXT list.
</output>
