---
phase: 17-contract-editor-unpaid-volunteer-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - service_impl/src/reporting.rs
  - service_impl/src/billing_period_report.rs
  - service_impl/src/test/reporting_additive_merge.rs
autonomous: true
requirements: [CVC-10]
tags: [D-06, D-GATING-STYLE]

must_haves:
  truths:
    - "D-06: get_week (reporting.rs) gibt KEINE unbezahlte Person (sales_person.is_paid == false) im ShortEmployeeReport-Result zurück — gegatet auf sales_person.is_paid, nicht auf Record-Präsenz"
    - "D-GATING-STYLE: build_new_billing_period (billing_period_report.rs) überspringt unbezahlte Personen (is_paid == false) im Billing-Loop — Personen-Set-Konsistenz mit Year-Summary"
    - "Eine unbezahlte Person (is_paid=false, expected_hours=0, committed_voluntary=5) leakt NICHT in paid_hours / Billing / WorkingHoursPerSalesPerson"
    - "Personen-Set-Konsistenz: year-summary (get_week), all-employees-report (get_reports_for_all_employees) und Billing (build_new_billing_period) enthalten dieselbe paid-Menge"
  artifacts:
    - path: "service_impl/src/reporting.rs"
      provides: "is_paid-Gate im get_week-Result-Loop"
      contains: "is_paid"
    - path: "service_impl/src/billing_period_report.rs"
      provides: "is_paid-Gate im build_new_billing_period-Loop"
      contains: "is_paid"
    - path: "service_impl/src/test/reporting_additive_merge.rs"
      provides: "get_week-Seiteneffekt-Integrationstest (CVC-10)"
      contains: "get_week"
  key_links:
    - from: "service_impl/src/reporting.rs::get_week"
      to: "SalesPerson.is_paid"
      via: "is_paid-Lookup vor result.push"
      pattern: "is_paid"
    - from: "service_impl/src/billing_period_report.rs::build_new_billing_period"
      to: "SalesPerson.is_paid"
      via: "is_paid-Filter/continue im sales_persons-Loop"
      pattern: "is_paid"
---

<objective>
Gate jede work-details-iterierende paid-only-Site explizit auf `sales_person.is_paid` (nicht auf Record-Präsenz), damit rein unbezahlte Freiwillige (die ab Plan 04 einen `EmployeeWorkDetails`-Record mit `is_paid = false`, `expected_hours = 0` halten) NICHT in `paid_hours` / Billing / Year-Summary lecken. Das schützt die Personen-Set-Konsistenz über die drei paid-Reporting-Pfade (year-summary / all-employees-report / Billing).

Purpose: Die historische „has work-details ⇒ paid employee"-Annahme bricht in v1.4 (D-04 / D-UNPAID-RECORD). `is_paid` lebt auf `SalesPerson`, NICHT auf `EmployeeWorkDetails`. Zwei at-risk-Sites iterieren heute OHNE paid-Filter: `reporting::get_week` (nutzt `all_for_week`, Haupt-Leak-Risiko) und `billing_period_report::build_new_billing_period` (nutzt `get_all`). Diese werden hier gegatet.

Output: is_paid-Gates in `reporting.rs::get_week` + `billing_period_report.rs::build_new_billing_period`, plus ein `get_week`-Seiteneffekt-Integrationstest, der das No-Leak-Verhalten + Personen-Set-Konsistenz pinnt (CVC-10 Success Criterion 3).

Scope-Grenzen (per RESEARCH 17-RESEARCH.md At-Risk-Sites):
- `get_reports_for_all_employees` (reporting.rs Z.164) ist BEREITS auf `is_paid` gegatet — NICHT anfassen, nur als Referenz-Pattern nutzen.
- `vacation_balance.rs::get_team` nutzt `get_all_paid` (Z.146) — bereits korrekt, NICHT anfassen.
- KEIN `CURRENT_SNAPSHOT_SCHEMA_VERSION`-Bump (D-05 / Achse-B-only, bleibt 7).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-CONTEXT.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-RESEARCH.md
@.planning/phases/17-contract-editor-unpaid-volunteer-path/17-PATTERNS.md

<vcs_jj_only>
Dieses Repo ist jj-managed (co-located mit git). Führe NIEMALS `git commit`/`git add` aus.
GSD-Auto-Commit ist deaktiviert — der User committed manuell via jj. Lass alle Änderungen
uncommitted im Working-Tree zurück; dokumentiere im SUMMARY, was committed werden muss.
</vcs_jj_only>

<build_commands>
Auf NixOS via `nix develop` (NICHT `nix-shell`, shell.nix kaputt):
- Backend-Tests dieses Plans: `nix develop --command cargo test -p service_impl`
- Workspace-Check: `nix develop --command cargo check --workspace`
NIEMALS `sqlx database reset` (destruktiv). Diese Plan-Änderungen brauchen KEINE Migration.
</build_commands>

<interfaces>
<!-- get_week result-push site (reporting.rs ~Z.881-898): SalesPerson wird HEUTE erst
     inline im push gefetcht. Für das Gate: SalesPerson VOR dem push fetchen, is_paid prüfen,
     bei false `continue`. -->

reporting.rs::get_week, aktueller push (Z.881-898):
```rust
result.push(ShortEmployeeReport {
    sales_person: Arc::new(
        self.sales_person_service
            .get(sales_person_id, Authentication::Full, tx.clone())
            .await?,
    ),
    balance_hours,
    // ... weitere Felder ...
});
```

reporting.rs Referenz-Gate-Pattern (get_reports_for_all_employees, BEREITS korrekt, Z.164):
```rust
.filter(|employee| employee.is_paid.unwrap_or(false))
```

SalesPerson.is_paid ist `Option<bool>` (rest-types / service-Struct). Lese-Idiom: `sales_person.is_paid.unwrap_or(false)`.

billing_period_report.rs::build_new_billing_period sales_persons-Loop (Z.316-333):
```rust
let sales_persons = self
    .sales_person_service
    .get_all(context.clone(), tx.clone().into())
    .await?;
let mut sales_person_reports = Vec::new();
for sales_person in sales_persons.iter() {
    let sales_person_report = self
        .build_billing_period_report_for_sales_person(...)
        .await?;
    sales_person_reports.push(sales_person_report);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: is_paid-Gate in reporting.rs::get_week + Integrationstest (CVC-10 Haupt-Leak-Site)</name>
  <files>service_impl/src/reporting.rs, service_impl/src/test/reporting_additive_merge.rs</files>
  <read_first>
    - service_impl/src/reporting.rs (gesamtes get_week ~Z.709-902 lesen; insbesondere all_for_week-Iteration Z.717-723 und result.push-Site Z.881-898)
    - service_impl/src/reporting.rs Z.139-204 (get_reports_for_all_employees — das BEREITS-korrekte is_paid-Gate-Pattern Z.164 als Vorlage)
    - service_impl/src/test/reporting_additive_merge.rs (gesamte Datei — bestehende ReportingService-Mockall-Struktur als Test-Vorlage für get_week/get_weekly_summary-Setup)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-RESEARCH.md § "At-Risk-Sites für is_paid-Gating" (Site 1 + Site 3)
  </read_first>
  <behavior>
    - Test get_week_skips_unpaid_person: Eine Person mit is_paid=false, expected_hours=0, committed_voluntary=5 und aktivem Vertrag in der Test-Woche erscheint NICHT im get_week-Result (ShortEmployeeReport-Vec); eine paid-Person mit Vertrag erscheint weiterhin.
    - Test get_week_unpaid_no_paid_hours_leak: Über get_weekly_summary (ruft get_week) trägt die unbezahlte Person 0.0 zu paid_hours bei UND erscheint nicht in WorkingHoursPerSalesPerson (Personen-Set-Leak).
  </behavior>
  <action>
    Füge in `reporting.rs::get_week` ein explizites `is_paid`-Gate ein, das die `SalesPerson` VOR dem `result.push(...)` fetcht und bei `is_paid == false` per `continue` überspringt.

    Konkret: Der bestehende `result.push(ShortEmployeeReport { sales_person: Arc::new(self.sales_person_service.get(...).await?), ... })` (Z.881-898) fetcht die SalesPerson heute inline im Push. Restrukturiere so:
    1. Direkt vor dem `result.push` die SalesPerson einmalig fetchen:
       ```rust
       let sales_person = self
           .sales_person_service
           .get(sales_person_id, Authentication::Full, tx.clone())
           .await?;
       // D-06 / CVC-10: gate auf is_paid (nicht auf Record-Präsenz). Unbezahlte Freiwillige
       // (is_paid=false, expected_hours=0) halten ab Phase 17 einen EmployeeWorkDetails-Record,
       // dürfen aber NICHT in paid_hours / WorkingHoursPerSalesPerson / Year-Summary lecken.
       if !sales_person.is_paid.unwrap_or(false) {
           continue;
       }
       ```
    2. Im `result.push` das vorgefetchte `sales_person` verwenden: `sales_person: Arc::new(sales_person),` statt des inline-Fetches.

    WICHTIG: Das `continue` springt zur nächsten Iteration der `for (sales_person_id, working_hours) in working_hours`-Schleife (Z.742). Stelle sicher, dass kein bereits-allokierter Wert vor dem `continue` benötigt wird (alle berechneten Hours-Werte werden nur im `push` konsumiert — verworfen ist korrekt).

    Füge dann zwei Tests in `service_impl/src/test/reporting_additive_merge.rs` hinzu (mockall-Integration über die dort bereits aufgebaute ReportingService-Mock-Struktur; nutze einen `approx`-Helper für f32-Vergleiche, falls vorhanden — sonst lokal `(a-b).abs() < 0.001`):
    - `get_week_skips_unpaid_person`: Setup mit zwei Personen (paid + unpaid mit committed_voluntary=5, expected_hours=0). Assert: get_week-Result enthält genau die paid-Person, NICHT die unpaid.
    - `get_week_unpaid_no_paid_hours_leak`: get_weekly_summary über das Jahr; assert paid_hours für die Woche == nur paid-Person-Beitrag (kein unpaid-Leak) UND working_hours_per_sales_person enthält die unpaid-Person-Id nicht.

    Beide Tests gehören in `service_impl/src/test/reporting_additive_merge.rs`, da dort der get_week/get_weekly_summary-Mock-Pfad bereits aufgebaut ist (RESEARCH § Sources nennt diese Datei als Mockall-Struktur-Vorlage). Dieser Plan fasst `service_impl/src/test/booking_information.rs` NICHT an — die Snapshot-Version-Regression + D-05-Band-Tests dort gehören zu Plan 02 (Wave-1-Parallelität: keine File-Überlappung).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop --command cargo test -p service_impl get_week</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_paid" service_impl/src/reporting.rs` zeigt einen NEUEN Treffer im get_week-Bereich (Z.709-902), zusätzlich zum bestehenden bei get_reports_for_all_employees
    - `grep -c "continue" service_impl/src/reporting.rs` zeigt mindestens 1 Treffer im get_week-Loop (war vorher ggf. 0 in diesem Loop)
    - `grep -n "get_week_skips_unpaid_person\|get_week_unpaid_no_paid_hours_leak" service_impl/src/test/reporting_additive_merge.rs` zeigt beide neuen Tests
    - `nix develop --command cargo test -p service_impl get_week` läuft grün (neue + bestehende get_week-Tests passieren)
    - `nix develop --command cargo test -p service_impl` läuft grün (keine Regression in der bestehenden Suite)
  </acceptance_criteria>
  <done>get_week überspringt unbezahlte Personen explizit via is_paid-Gate; zwei Integrationstests beweisen No-Leak in paid_hours + WorkingHoursPerSalesPerson.</done>
</task>

<task type="auto">
  <name>Task 2: is_paid-Gate in billing_period_report::build_new_billing_period (Personen-Set-Konsistenz)</name>
  <files>service_impl/src/billing_period_report.rs</files>
  <read_first>
    - service_impl/src/billing_period_report.rs Z.299-348 (build_new_billing_period — get_all-Loop Z.316-333, KEIN paid-Filter)
    - service_impl/src/billing_period_report.rs Z.75 (CURRENT_SNAPSHOT_SCHEMA_VERSION = 7 — bestätige, dass dieser Plan ihn NICHT ändert)
    - .planning/phases/17-contract-editor-unpaid-volunteer-path/17-RESEARCH.md § "At-Risk-Sites" Site 4 + Assumptions Log A1 (numerisch harmlos, aber Personen-Set-Konsistenz)
    - shifty-backend/CLAUDE.md § "Billing Period Snapshot Schema Versioning" (warum KEIN Bump nötig ist: Gate ändert keinen value_type, nur das Personen-Set)
  </read_first>
  <action>
    Füge im `build_new_billing_period`-`sales_persons`-Loop (Z.322-333) ein explizites `is_paid`-Skip ein, damit unbezahlte Personen nicht als leere `BillingPeriodSalesPerson`-Einträge im Snapshot landen (Personen-Set-Konsistenz mit get_week / get_reports_for_all_employees).

    Konkret, als erste Zeile im `for sales_person in sales_persons.iter()`-Body:
    ```rust
    // D-GATING-STYLE / CVC-10: unbezahlte Personen (is_paid=false) werden ab Phase 17
    // EmployeeWorkDetails-Records halten (rein freiwillige Helfer). Sie dürfen NICHT als
    // BillingPeriodSalesPerson-Einträge im Snapshot erscheinen — Personen-Set-Konsistenz
    // mit get_week (year-summary) + get_reports_for_all_employees (all-employees-report).
    // KEIN value_type-Change → KEIN CURRENT_SNAPSHOT_SCHEMA_VERSION-Bump (bleibt 7).
    if !sales_person.is_paid.unwrap_or(false) {
        continue;
    }
    ```

    WICHTIG — KEIN Snapshot-Bump: Diese Änderung berührt keinen persistierten `BillingPeriodValueType` und keine value_type-Berechnung; sie filtert nur das Personen-Set vor. `CURRENT_SNAPSHOT_SCHEMA_VERSION` bleibt 7. NICHT ändern.

    Falls `is_paid.unwrap_or(false)` auf dem hier verfügbaren `sales_person`-Typ nicht direkt zugreifbar ist (z.B. weil `sales_persons` ein anderer Typ als `SalesPerson` ist): verifiziere den Feldzugriff per Read der get_all-Signatur und passe das Idiom an den tatsächlichen Typ an (das Feld ist `is_paid: Option<bool>` auf der SalesPerson-Service-Struct).

    Dieser Plan fasst `service_impl/src/test/booking_information.rs` NICHT an (gehört Plan 02 — Wave-1-Parallelität). Die Bestätigung, dass das Billing-Personen-Set-Gate KEINEN Bump auslöst, wird hier ausschließlich über den unveränderten `CURRENT_SNAPSHOT_SCHEMA_VERSION = 7`-Wert in billing_period_report.rs sichergestellt; der bestehende `snapshot_schema_version_unchanged_at_7`-Regressionstest (in Plan 02s Datei) bleibt grün, ohne dass dieser Plan ihn editiert.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && nix develop --command cargo test -p service_impl billing_period</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_paid" service_impl/src/billing_period_report.rs` zeigt einen NEUEN Treffer im build_new_billing_period-Bereich (Z.299-348)
    - `grep -n "CURRENT_SNAPSHOT_SCHEMA_VERSION" service_impl/src/billing_period_report.rs` zeigt unverändert `= 7` (kein Bump)
    - `nix develop --command cargo test -p service_impl snapshot_schema_version_unchanged_at_7` läuft grün (Version weiterhin 7)
    - `nix develop --command cargo test -p service_impl billing_period` läuft grün
    - `nix develop --command cargo test -p service_impl` läuft grün (keine Regression)
  </acceptance_criteria>
  <done>build_new_billing_period überspringt unbezahlte Personen; Snapshot-Version bleibt 7 (per Regressionstest bestätigt).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reporting-Service → Year-Summary/Billing-Output | Eine unbezahlte Freiwillige mit EmployeeWorkDetails-Record (is_paid=false, expected_hours=0) ist eine neue Datenklasse, die bisher nicht existierte; sie passiert die work-details-iterierenden Service-Schleifen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege (Daten-Integrität) | reporting.rs::get_week (all_for_week, ungated) | mitigate | Explizites `is_paid`-Gate (`if !sales_person.is_paid.unwrap_or(false) { continue; }`) vor result.push; Integrationstest get_week_unpaid_no_paid_hours_leak pinnt das No-Leak-Verhalten in paid_hours + WorkingHoursPerSalesPerson |
| T-17-02 | Information Disclosure (Personen-Set-Drift) | billing_period_report::build_new_billing_period (get_all, ungated) | mitigate | `is_paid`-Skip im Billing-Loop garantiert Personen-Set-Konsistenz mit Year-Summary; numerisch bereits 0, aber konzeptuelle Korrektheit + Debuggability |
| T-17-03 | Tampering (Snapshot-Drift durch versehentlichen Bump) | CURRENT_SNAPSHOT_SCHEMA_VERSION | accept | Gate berührt keinen value_type; snapshot_schema_version_unchanged_at_7-Regressionstest verhindert versehentlichen Bump (Version bleibt 7) |
</threat_model>

<verification>
- `nix develop --command cargo test -p service_impl` grün (inkl. neue get_week-Tests + bestehende Suite)
- `nix develop --command cargo check --workspace` grün
- `grep` bestätigt is_paid-Gates an beiden Sites; CURRENT_SNAPSHOT_SCHEMA_VERSION unverändert 7
- get_reports_for_all_employees + vacation_balance.get_team UNVERÄNDERT (bereits korrekt gegatet)
</verification>

<success_criteria>
- get_week (reporting.rs) gibt keine unbezahlte Person im ShortEmployeeReport-Result zurück (gegatet auf sales_person.is_paid)
- build_new_billing_period überspringt unbezahlte Personen (Personen-Set-Konsistenz)
- Integrationstest beweist: unbezahlte Person (is_paid=false, expected_hours=0, committed_voluntary=5) leakt nicht in paid_hours / WorkingHoursPerSalesPerson
- CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 7 (Regressionstest grün)
- Keine Regression in der bestehenden service_impl-Suite
</success_criteria>

<output>
After completion, create `.planning/phases/17-contract-editor-unpaid-volunteer-path/17-01-SUMMARY.md`.
Dokumentiere: gewählte Test-Datei für get_week-Tests, exakte Gate-Insertionspunkte (Zeilennummern), Bestätigung KEIN Snapshot-Bump, jj-Commit-Hinweis (User committed manuell).
</output>
