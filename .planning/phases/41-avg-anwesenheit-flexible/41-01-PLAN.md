---
phase: 41-avg-anwesenheit-flexible
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - service/src/reporting.rs
  - service_impl/src/test/reporting_avg_attendance.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [AVG-01]
must_haves:
  truths:
    - "D-AVG-01: pure fn returns Ø geleistete Stunden pro Anwesenheitstag = Σ Arbeitsstunden / Anzahl Anwesenheitstage (tagebasiert, NICHT die Wochen-Zahl A-22-1)"
    - "D-AVG-02: ein Anwesenheitstag ist ein Kalendertag mit ≥1 Eintrag der Kategorie Shiftplan|ExtraWork|VolunteerWork und hours > 0; Datum wird dedupliziert"
    - "D-AVG-03: Vacation, SickLeave, Holiday, UnpaidLeave, Unavailable (und Custom) sind KEINE Anwesenheitstage → per Konstruktion aus dem Nenner"
    - "D-AVG-06: bei < 2 Anwesenheitstagen liefert average_hours_per_attendance_day None (nicht aussagekräftig)"
    - "D-AVG-08: A-22-1 (average_worked_hours_per_week) bleibt unverändert; kein Snapshot-Bump — CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 12"
  artifacts:
    - "service/src/reporting.rs :: pub struct EmployeeAttendanceStatistics { average_hours_per_attendance_day: Option<f32>, attendance_days: u32, total_worked_hours: f32 }"
    - "service/src/reporting.rs :: pub fn average_hours_per_attendance_day(days: &[WorkingHoursDay]) -> EmployeeAttendanceStatistics"
    - "service_impl/src/test/reporting_avg_attendance.rs (7 pure-fn Nyquist-Fälle)"
  key_links:
    - "pure fn liest WorkingHoursDay.date (time::Date, kein Wrapper) + .hours + .category — dedupliziert Datum via BTreeSet<time::Date>"
---

<objective>
Neue, von A-22-1 GETRENNTE pure Aggregat-Funktion `average_hours_per_attendance_day` plus
Ergebnis-Struct `EmployeeAttendanceStatistics` in `service/src/reporting.rs` — TDD (RED→GREEN).

Purpose: Die tagebasierte AVG-01-Kennzahl ("wie lang arbeitet ein flexibler MA an den Tagen,
an denen er da ist") als reine, mock-freie Funktion, damit die Nenner-Regel (Absence per
Konstruktion draußen) und die <2-Tage-Schwelle deterministisch getestet werden.
Output: Struct + pure fn + Testmodul `reporting_avg_attendance.rs` mit allen Nyquist-Fällen.
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-avg-anwesenheit-flexible/41-CONTEXT.md
@.planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md
@.planning/phases/41-avg-anwesenheit-flexible/41-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 1) delivers the first two artifacts of the phase:
- `EmployeeAttendanceStatistics` result struct + `average_hours_per_attendance_day` pure fn.
Later waves add: service trait method + impl (41-02), `EmployeeAttendanceStatisticsTO` +
`GET /report/{id}/attendance-statistics` handler/route/ApiDoc (41-03), FE store field +
loader + rendering + i18n keys (41-04).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Nyquist-Testmodul für die pure Anwesenheits-Funktion</name>
  <files>service_impl/src/test/reporting_avg_attendance.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/reporting_avg_weekly.rs (Struktur-Vorlage: data-driven pure-fn Tests, keine Mocks, `week(..)`-Builder)
    - service/src/reporting.rs:14 (enum ExtraHoursReportCategory-Varianten) und :44-49 (WorkingHoursDay { date: time::Date, hours: f32, category })
    - .planning/phases/41-avg-anwesenheit-flexible/41-VALIDATION.md (Requirements → Test Map, Zeilen 41-47)
  </read_first>
  <behavior>
    - user_example: 12 verschiedene Anwesenheitstage, Summe 54h → Some(4.5)
    - absence_day_not_counted: ein Tag Vacation=8h ohne Arbeit → attendance_days zählt ihn NICHT
    - mixed_day_counts_work_only: selber Tag Shiftplan=4h + Vacation=4h → 1 Anwesenheitstag, Zähler nur 4h
    - custom_category_not_attendance: Custom-Eintrag mit hours>0 → kein Anwesenheitstag
    - empty_slice_returns_none: leeres Slice → attendance_days=0, average_hours_per_attendance_day=None
    - one_day_returns_none: genau 1 Anwesenheitstag → None (D-AVG-06 Schwelle)
    - two_days_returns_some: 2 Anwesenheitstage → Some(f32) (Schwelle exakt erfüllt)
  </behavior>
  <action>
    Lege `service_impl/src/test/reporting_avg_attendance.rs` an, analog zur Struktur von
    `reporting_avg_weekly.rs`, aber operierend auf einem `Vec<WorkingHoursDay>` statt
    `GroupedReportHours`. Baue einen kleinen Helper `day(date, hours, category)` der ein
    `service::reporting::WorkingHoursDay` mit `date: time::Date` (via `time::macros::date!`),
    `hours` und der übergebenen `ExtraHoursReportCategory` konstruiert (übrige Pflichtfelder
    mit benignen Defaults — Felder aus reporting.rs:44-49 verifizieren). Importiere
    `service::reporting::{average_hours_per_attendance_day, EmployeeAttendanceStatistics,
    WorkingHoursDay, ExtraHoursReportCategory}`.
    Schreibe die 7 `#[test]`-Fälle exakt nach der behavior-Liste (Funktionsnamen wie oben,
    da die VALIDATION-Test-Map genau diese Namen greift). Für die Deduplizierungs-Fälle
    (mixed_day) denselben `date!` zweimal mit unterschiedlichen Kategorien verwenden.
    Registriere `pub mod reporting_avg_attendance;` in `service_impl/src/test/mod.rs`
    (neben `pub mod reporting_avg_weekly;`).
    RED-Zustand: Struct und Funktion existieren in Task 2 noch nicht → Modul kompiliert nicht;
    das IST der erwartete RED. Committe die Tests als RED-Commit.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl reporting_avg_attendance 2>&1 | grep -qiE "cannot find|unresolved import|error\[E0" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - Datei `reporting_avg_attendance.rs` existiert mit 7 benannten Tests exakt nach behavior-Liste
    - `pub mod reporting_avg_attendance;` in test/mod.rs registriert
    - Build/Test schlägt fehl, weil `average_hours_per_attendance_day`/`EmployeeAttendanceStatistics` fehlen (RED bestätigt)
  </acceptance_criteria>
  <done>7 benannte Tests existieren und schlagen im RED-Zustand fehl (Funktion/Struct fehlen); mod registriert.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): pure fn average_hours_per_attendance_day + Struct implementieren</name>
  <files>service/src/reporting.rs</files>
  <read_first>
    - service/src/reporting.rs:207-244 (A-22-1 average_worked_hours_per_week — als Stil-Vorlage, NICHT ändern und NICHT wiederverwenden)
    - service/src/reporting.rs:14 (ExtraHoursReportCategory) und :44-49 (WorkingHoursDay Felder)
    - .planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md §"Pattern 1" (verifizierte Referenz-Implementierung; date ist time::Date direkt, kein .to_date())
  </read_first>
  <behavior>
    Erfüllt exakt die 7 Testfälle aus Task 1 (GREEN). Nenner = DISTINCT Datum der
    Arbeits-Einträge; Zähler = Summe der Arbeits-Stunden; <2 Tage → None.
  </behavior>
  <action>
    Füge in `service/src/reporting.rs` UNTERHALB von `average_worked_hours_per_week` (A-22-1
    bleibt Byte-für-Byte unverändert) hinzu:
    (1) `#[derive(Clone, Debug, PartialEq)] pub struct EmployeeAttendanceStatistics` mit den
    Feldern `average_hours_per_attendance_day: Option<f32>`, `attendance_days: u32`,
    `total_worked_hours: f32` (Doc-Kommentare mit D-AVG-Referenzen).
    (2) `pub fn average_hours_per_attendance_day(days: &[WorkingHoursDay]) -> EmployeeAttendanceStatistics`:
    filtere Einträge auf `hours > 0.0 && matches!(category, Shiftplan | ExtraWork | VolunteerWork)`
    (D-AVG-02); sammle deren `date` in ein `BTreeSet<time::Date>` → `attendance_days = set.len() as u32`
    (D-AVG-03: alle Absence- und Custom-Kategorien fallen per Filter heraus, kein Doppelzählen
    durch Dedup); `total_worked_hours` = Summe der gefilterten `hours`;
    `average = if attendance_days >= 2 { Some(total / attendance_days as f32) } else { None }`
    (D-AVG-06). Nutze `time::Date` direkt (WorkingHoursDay.date ist kein Wrapper).
    A-22-1 (`average_worked_hours_per_week`) darf weder aufgerufen noch modifiziert werden —
    getrennte Funktion, getrennter Input-Typ (`&[WorkingHoursDay]`).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl reporting_avg_attendance && cargo test -p service_impl reporting_avg_weekly && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Alle 7 `reporting_avg_attendance`-Tests grün (GREEN)
    - A-22-1-Regression `reporting_avg_weekly` weiterhin grün (Funktion unverändert)
    - `cargo clippy --workspace -- -D warnings` sauber
    - `grep -n "average_worked_hours_per_week" service/src/reporting.rs` zeigt A-22-1 unverändert vorhanden
    - `grep "CURRENT_SNAPSHOT_SCHEMA_VERSION.*12" service_impl/src/billing_period_report.rs` matcht (kein Snapshot-Bump, D-AVG-08)
  </acceptance_criteria>
  <done>Alle 7 Tests grün, A-22-1 unverändert, Snapshot-Version 12, Clippy sauber.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (keine) | Reine In-Memory-Funktion über bereits geladene Report-Daten; kein I/O, keine Auth, kein untrusted Input an dieser Stelle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | Nenner-Fehler: Absence/Custom-Tag fälschlich gezählt | medium | mitigate | Kategorie-`matches!`-Filter + Dedup via BTreeSet; Unit-Tests absence_day_not_counted / custom_category_not_attendance / mixed_day_counts_work_only |
| T-41-02 | Tampering | versehentliche Änderung an A-22-1 → Snapshot-Drift | high | mitigate | Getrennte Funktion/Input-Typ; A-22-1-Regressionstest grün + grep, dass CURRENT_SNAPSHOT_SCHEMA_VERSION = 12 bleibt |
| T-41-SC | Tampering | Supply-Chain (neue Pakete) | low | accept | Keine neuen Dependencies (RESEARCH Package Legitimacy Audit: entfällt) |
</threat_model>

<verification>
- `cargo test -p service_impl reporting_avg_attendance` (7 Fälle grün)
- `cargo test -p service_impl reporting_avg_weekly` (A-22-1 unverändert)
- `cargo clippy --workspace -- -D warnings`
- Grep: A-22-1 vorhanden/unverändert; Snapshot-Version = 12
</verification>

<success_criteria>
`average_hours_per_attendance_day` + `EmployeeAttendanceStatistics` existieren, alle 7
Nyquist-Fälle grün, A-22-1 unangetastet, kein Snapshot-Bump, Clippy sauber.
</success_criteria>

<output>
Create `.planning/phases/41-avg-anwesenheit-flexible/41-01-SUMMARY.md` when done
</output>
