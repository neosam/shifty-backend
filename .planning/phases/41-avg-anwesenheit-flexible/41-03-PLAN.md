---
phase: 41-avg-anwesenheit-flexible
plan: 03
type: execute
wave: 3
depends_on: ["41-01", "41-02"]
files_modified:
  - rest-types/src/lib.rs
  - rest/src/report.rs
autonomous: true
requirements: [AVG-02]
must_haves:
  truths:
    - "D-AVG-05: neuer HR-gated Endpoint GET /report/{id}/attendance-statistics; Nicht-HR → 403; nicht-flexible MA → 200 mit null-Body (kein Wert), server-seitig gefiltert"
    - "D-AVG-04: Endpoint ist range-aware (year/until_week Query-Params) → nutzt den angezeigten Report-Zeitraum, KEINE Erweiterung des year-to-date /weekly-statistics (kein Breaking Change)"
    - "D-AVG-08: reines Read-Aggregat serialisiert via EmployeeAttendanceStatisticsTO — keine Persistenz, kein Snapshot-Bump (Version bleibt 12)"
  artifacts:
    - "rest-types/src/lib.rs :: pub struct EmployeeAttendanceStatisticsTO (ToSchema) + impl From<&EmployeeAttendanceStatistics>"
    - "rest/src/report.rs :: get_attendance_statistics Handler (#[utoipa::path]) + Route /{id}/attendance-statistics + ReportApiDoc paths+components(schemas) Eintrag"
  key_links:
    - "Handler ruft reporting_service().get_employee_attendance_statistics(...) und serialisiert Option → JSON null bei None (nicht-flexibler MA)"
---

<objective>
REST-Schicht: Transport-Object `EmployeeAttendanceStatisticsTO` in rest-types + neuer
HR-gated, range-aware Endpoint `GET /report/{id}/attendance-statistics?year=Y&until_week=W`
inkl. `#[utoipa::path]`, Route und ApiDoc-Eintrag.

Purpose: Die AVG-01-Kennzahl über HTTP verfügbar machen (AVG-02), als EIGENER Endpoint statt
Erweiterung von /weekly-statistics (das ist year-to-date; Erweiterung wäre Breaking Change).
Output: TO + From-Impl, Handler + Route + ApiDoc (Swagger-sichtbar).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-avg-anwesenheit-flexible/41-CONTEXT.md
@.planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md
@.planning/phases/41-avg-anwesenheit-flexible/41-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 3) adds the transport + HTTP surface: `EmployeeAttendanceStatisticsTO`
(rest-types, single source of truth consumed by FE) and the `get_attendance_statistics`
handler/route/ApiDoc. Consumes 41-01 struct (From impl) + 41-02 trait method. FE consumes
the TO in 41-04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: EmployeeAttendanceStatisticsTO + From-Impl in rest-types</name>
  <files>rest-types/src/lib.rs</files>
  <read_first>
    - rest-types/src/lib.rs:596-617 (EmployeeWeeklyStatisticsTO — ToSchema + From-Vorlage inkl. `#[cfg(feature = "service-impl")]`)
    - .planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md §"Pattern 5"
  </read_first>
  <action>
    Füge nach `EmployeeWeeklyStatisticsTO` hinzu:
    `#[derive(Clone, Debug, PartialEq, Serialize, Deserialize, ToSchema)] pub struct
    EmployeeAttendanceStatisticsTO` mit Feldern `average_hours_per_attendance_day: Option<f32>`,
    `attendance_days: u32`, `total_worked_hours: f32` (Doc-Kommentare mit D-AVG-Referenzen).
    Dazu `#[cfg(feature = "service-impl")] impl From<&service::reporting::EmployeeAttendanceStatistics>
    for EmployeeAttendanceStatisticsTO` (1:1 Feld-Mapping) — exakt dem Feature-Gate-Muster von
    EmployeeWeeklyStatisticsTO folgend.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build -p rest-types --all-features 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `EmployeeAttendanceStatisticsTO` mit `ToSchema` und den 3 Feldern existiert
    - `From<&EmployeeAttendanceStatistics>` unter `#[cfg(feature = "service-impl")]` kompiliert
    - `cargo build -p rest-types --all-features` grün
  </acceptance_criteria>
  <done>`EmployeeAttendanceStatisticsTO` (ToSchema) + From-Impl kompilieren mit --all-features.</done>
</task>

<task type="auto">
  <name>Task 2: HR-gated Endpoint get_attendance_statistics + Route + ApiDoc</name>
  <files>rest/src/report.rs</files>
  <read_first>
    - rest/src/report.rs:26 (ReportRequest year/until_week), :155-204 (get_weekly_statistics Handler + generate_route + ReportApiDoc — komplette Kopiervorlage)
    - .planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md §"Pattern 4" (verifizierter Handler, Route-Eintrag, ApiDoc-Eintrag)
  </read_first>
  <action>
    (1) Handler `get_attendance_statistics<RestState: RestStateDef>` analog zu
    `get_weekly_statistics`, aber mit `Query<ReportRequest>` (year + until_week) und Aufruf
    `reporting_service().get_employee_attendance_statistics(&sales_person_id, query.year,
    query.until_week, context.into(), None)`. Ergebnis ist `Option<EmployeeAttendanceStatistics>`:
    serialisiere `maybe_stats.as_ref().map(EmployeeAttendanceStatisticsTO::from)` via
    `serde_json::to_string` → bei `None` (nicht-flexibler MA) wird der Body `null`, Status 200.
    Wrappe alles in `error_handler(async { ... }.await)` (bestehender Pattern; 403 kommt aus
    dem Forbidden des Service-Gates).
    `#[utoipa::path(get, path = "/{id}/attendance-statistics", tags=["Report"], params(id, year,
    until_week), responses(200 = EmployeeAttendanceStatisticsTO, 403, 500))` + `#[instrument(skip(rest_state))]`.
    (2) Route in `generate_route`: `.route("/{id}/attendance-statistics",
    get(get_attendance_statistics::<RestState>))`.
    (3) `ReportApiDoc`: `get_attendance_statistics` zu `paths(...)` und
    `EmployeeAttendanceStatisticsTO` zu `components(schemas(...))` hinzufügen (PFLICHT — sonst
    fehlt der Endpoint in Swagger UI).
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build --workspace 2>&1 | tail -5 && grep -q "get_attendance_statistics" rest/src/report.rs && grep -q "EmployeeAttendanceStatisticsTO" rest/src/report.rs && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Handler `get_attendance_statistics` mit `#[utoipa::path]` + Route `/{id}/attendance-statistics` vorhanden
    - `ReportApiDoc` listet `get_attendance_statistics` in paths() UND `EmployeeAttendanceStatisticsTO` in components(schemas())
    - `cargo build --workspace` grün, `cargo clippy --workspace -- -D warnings` sauber
    - Nicht-flexibler MA → Body `null` (Service-None); Nicht-HR → 403 via error_handler
  </acceptance_criteria>
  <done>Endpoint + Route + ApiDoc registriert, Workspace baut, Clippy sauber, None→null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST (Axum) | HTTP-Request mit sales_person_id (Path) + year/until_week (Query); Auth-Context via Extension; HR-Daten in der Response |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-06 | Information Disclosure | get_attendance_statistics gibt HR-Kennzahl an Nicht-HR | high | mitigate | Auth im Service-Gate (41-02); Handler ruft Service, error_handler mappt Forbidden → 403; keine eigene Bypass-Logik im Handler |
| T-41-07 | Information Disclosure | IDOR: fremde MA-Statistik über id-Param | medium | mitigate | HR_PRIVILEGE erlaubt HR den Blick auf alle MA per Design; kein separater Owner-Check nötig; Nicht-HR bleibt 403 |
| T-41-08 | Tampering | Query-Manipulation (year/until_week out of range) | low | mitigate | Rust-Typen u32/u8 + Service-seitiger Clamp `until_week.min(weeks_in_year(year))` (41-02) |
| T-41-SC | Tampering | Supply-Chain (neue Pakete) | low | accept | Keine neuen Dependencies |
</threat_model>

<verification>
- `cargo build --workspace` + `cargo clippy --workspace -- -D warnings`
- Grep: Handler + TO im ApiDoc registriert (paths + components(schemas))
- Manuell/Smoke (optional): `curl -s "/report/{id}/attendance-statistics?year=2026&until_week=26"` als HR → 200; als Nicht-HR → 403; statischer MA → Body `null`
</verification>

<success_criteria>
TO + HR-gated range-aware Endpoint vorhanden, in Swagger/ApiDoc registriert, Workspace baut,
Clippy sauber; None-Fall (nicht-flexibel) serialisiert zu `null`.
</success_criteria>

<output>
Create `.planning/phases/41-avg-anwesenheit-flexible/41-03-SUMMARY.md` when done
</output>
