---
phase: 41-avg-anwesenheit-flexible
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - service/src/reporting.rs
  - service_impl/src/reporting.rs
  - service_impl/src/test/reporting_attendance_gate.rs
  - service_impl/src/test/mod.rs
autonomous: true
requirements: [AVG-01, AVG-02]
must_haves:
  truths:
    - "D-AVG-04: der Service aggregiert über den angezeigten Report-Zeitraum (year/until_week via get_report_for_employee), kein separater Datums-Picker"
    - "D-AVG-05: HR_PRIVILEGE-Check ist die ERSTE Operation (kein Datenabruf vor Auth); nicht-flexible MA (kein is_dynamic) → Ok(None), server-seitig gefiltert"
    - "D-AVG-08: reines Read-Aggregat über get_report_for_employee — kein neuer BillingPeriodValueType, keine Persistenz, CURRENT_SNAPSHOT_SCHEMA_VERSION bleibt 12"
  artifacts:
    - "service/src/reporting.rs :: async fn get_employee_attendance_statistics(...) -> Result<Option<EmployeeAttendanceStatistics>, ServiceError> im trait ReportingService"
    - "service_impl/src/reporting.rs :: impl get_employee_attendance_statistics (HR-gate → is_dynamic-Filter → report-fetch → pure fn)"
    - "service_impl/src/test/reporting_attendance_gate.rs (Mock-Tests HR-Gate + is_dynamic-None)"
  key_links:
    - "Impl ruft in Reihenfolge: permission_service.check_permission(HR_PRIVILEGE) → employee_work_details_service.find_by_sales_person_id → get_report_for_employee → average_hours_per_attendance_day über flatten(by_week[*].days)"
---

<objective>
Business-Logic-Tier: neue Trait-Methode `get_employee_attendance_statistics` im
`ReportingService` (service) + Implementierung (service_impl), die HR-gated und
server-seitig auf `is_dynamic` gefiltert die AVG-01-Kennzahl über den Report-Zeitraum liefert.

Purpose: Zugriffskontrolle (HR only) und Scope-Filter (nur flexible MA) gehören server-seitig
(D-AVG-05) — die pure fn aus 41-01 wird hier über den bereits existierenden Report-Pfad
gefüttert, ohne neue DB-Query und ohne A-22-1 zu berühren.
Output: Trait-Methode, Impl, Mock-Tests für HR-Gate (403) und is_dynamic-Filter (None).
</objective>

<execution_context>
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/workflows/execute-plan.md
@/home/neosam/programming/rust/projects/shifty/shifty-backend/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-avg-anwesenheit-flexible/41-CONTEXT.md
@.planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md
@.planning/phases/41-avg-anwesenheit-flexible/41-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 2) adds the service-layer artifacts: the `ReportingService` trait method
and its implementation (HR gate + is_dynamic filter + report aggregation) plus mock tests.
Consumes 41-01's `average_hours_per_attendance_day` / `EmployeeAttendanceStatistics`.
Produced by later waves: REST TO/endpoint (41-03), FE (41-04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Trait-Methode get_employee_attendance_statistics deklarieren</name>
  <files>service/src/reporting.rs</files>
  <read_first>
    - service/src/reporting.rs:289 (get_employee_weekly_statistics — Signatur-Vorlage) und :260 (get_report_for_employee)
    - .planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md §"Pattern 2"
  </read_first>
  <action>
    Ergänze im `trait ReportingService` die async-Methode
    `get_employee_attendance_statistics(&self, sales_person_id: &Uuid, year: u32, until_week: u8,
    context: Authentication<Self::Context>, tx: Option<Self::Transaction>)
    -> Result<Option<EmployeeAttendanceStatistics>, ServiceError>`.
    Rückgabe `Option`: `None` = nicht-flexibler MA (D-AVG-05). Doc-Kommentar mit Verweis auf
    D-AVG-04 (Zeitraum) und D-AVG-05 (HR-gated, is_dynamic). Importiere/qualifiziere
    `EmployeeAttendanceStatistics` aus demselben Modul. A-22-1-Methoden bleiben unverändert.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build -p service 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Trait `ReportingService` enthält `get_employee_attendance_statistics` mit der exakten Signatur
    - `cargo build -p service` schlägt fehl NUR wegen fehlender Impl (erwartet bis Task 2), Trait selbst kompiliert
  </acceptance_criteria>
  <done>Trait-Methode `get_employee_attendance_statistics` mit exakter Signatur deklariert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Impl (HR-gate → is_dynamic-Filter → report → pure fn)</name>
  <files>service_impl/src/reporting.rs</files>
  <read_first>
    - service_impl/src/reporting.rs:246-300 (impl-Block-Start + get_employee_weekly_statistics-Vorlage mit HR-Gate) und :61-70 (ReportingServiceDeps: employee_work_details_service vorhanden)
    - service/src/employee_work_details.rs:245 (find_by_sales_person_id) und :240 (all — Fallback)
    - .planning/phases/41-avg-anwesenheit-flexible/41-RESEARCH.md §"Pattern 3"
  </read_first>
  <behavior>
    - Nicht-HR-Context → Err(ServiceError::Forbidden) VOR jedem Datenabruf
    - MA ohne is_dynamic==true in work_details → Ok(None)
    - flexibler MA → Ok(Some(stats)) mit der pure-fn-Aggregation über alle by_week[*].days
  </behavior>
  <action>
    Implementiere `get_employee_attendance_statistics` im `impl ReportingService for
    ReportingServiceImpl`-Block. Reihenfolge strikt (D-AVG-05): (1) `self.permission_service
    .check_permission(HR_PRIVILEGE, context.clone()).await?` als ERSTE Operation.
    (2) `is_dynamic`-Filter: work_details über `employee_work_details_service
    .find_by_sales_person_id(sales_person_id, Authentication::Full, tx.clone())` laden (falls
    diese Methode die erwartete Signatur nicht bietet, `all()` + `filter(|w| w.sales_person_id
    == *sales_person_id)` wie im bestehenden get_reports_for_all_employees-Pfad); wenn
    `!work_details.iter().any(|w| w.is_dynamic)` → `return Ok(None)`.
    (3) `let until_week = until_week.min(time::util::weeks_in_year(year as i32))` (Input-Clamp).
    (4) `get_report_for_employee(sales_person_id, year, until_week, context, tx)` (D-AVG-04).
    (5) alle Tage flatten: `report.by_week.iter().flat_map(|w| w.days.iter().cloned()).collect()`.
    (6) `service::reporting::average_hours_per_attendance_day(&all_days)` → `Ok(Some(stats))`.
    KEINE neue DAO-Query, KEIN Schreibpfad, `billing_period_report.rs` nicht anfassen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo build --workspace 2>&1 | tail -5 && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` grün, `ReportingServiceImpl` implementiert die Methode vollständig
    - HR-Gate ist die erste await-Operation (per Code-Review + Task-3-Test bestätigt)
    - `cargo clippy --workspace -- -D warnings` sauber
  </acceptance_criteria>
  <done>Impl vollständig: HR-Gate zuerst, is_dynamic-Filter → Ok(None), Aggregation über Report-Zeitraum; Workspace baut, Clippy sauber.</done>
</task>

<task type="auto">
  <name>Task 3: Mock-Tests HR-Gate + is_dynamic-Filter</name>
  <files>service_impl/src/test/reporting_attendance_gate.rs, service_impl/src/test/mod.rs</files>
  <read_first>
    - service_impl/src/test/reporting_holiday_auto_credit.rs:1-70 (ReportingMocks/TestDeps-Builder mit MockPermissionService + MockEmployeeWorkDetailsService — exakte Mock-Setup-Vorlage)
    - service_impl/src/test/reporting_additive_merge.rs (Basis-Struktur desselben Deps-Builders)
  </read_first>
  <behavior>
    - attendance_statistics_requires_hr: check_permission liefert Err(Forbidden) → Methode gibt Forbidden zurück, es wird KEIN report/work_details geladen
    - attendance_statistics_returns_none_for_static: HR ok, work_details ohne is_dynamic → Ok(None), get_report_for_employee wird nicht aufgerufen
  </behavior>
  <action>
    Lege `service_impl/src/test/reporting_attendance_gate.rs` an; übernimm den
    `ReportingMocks`/`TestDeps`-Builder 1:1 aus `reporting_holiday_auto_credit.rs`.
    Test 1 `attendance_statistics_requires_hr`: `permission_service.expect_check_permission()`
    gibt `Err(ServiceError::Forbidden)` zurück; setze `employee_work_details_service` und
    Report-Mocks mit `.expect_*().times(0)` (Beweis: kein Datenabruf vor Auth); assert das
    Ergebnis ist `Err(ServiceError::Forbidden)`.
    Test 2 `attendance_statistics_returns_none_for_static`: `check_permission` → `Ok(())`;
    `employee_work_details_service` liefert work_details mit `is_dynamic == false`;
    Report-/Aggregat-Mocks `.times(0)`; assert Ergebnis `Ok(None)`.
    Registriere `pub mod reporting_attendance_gate;` in `service_impl/src/test/mod.rs`.
    Falls Task 2 `all()` statt `find_by_sales_person_id` nutzt, Mock entsprechend setzen.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend && cargo test -p service_impl reporting_attendance_gate && cargo clippy --workspace -- -D warnings && grep -q "CURRENT_SNAPSHOT_SCHEMA_VERSION.*12" service_impl/src/billing_period_report.rs</automated>
  </verify>
  <acceptance_criteria>
    - Beide Mock-Tests grün
    - HR-Fehlerfall beweist via `.times(0)` dass work_details/report NICHT geladen werden (Auth zuerst)
    - is_dynamic==false-Fall liefert Ok(None) ohne Report-Abruf
    - `cargo clippy --workspace -- -D warnings` sauber
  </acceptance_criteria>
  <done>Beide Mock-Tests grün; HR-Gate-first und is_dynamic-Filter server-seitig bewiesen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (REST/Extension-Context) → ReportingService | Authentication<Context> + sales_person_id kreuzen hier die Auth-Grenze; HR-Daten dürfen erst nach Privilege-Check fließen |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-03 | Information Disclosure | Unautorisierter Zugriff auf HR-Kennzahl | high | mitigate | `check_permission(HR_PRIVILEGE, ctx)` ist ERSTE await-Operation; Test attendance_statistics_requires_hr beweist kein Datenabruf vor Auth (.times(0)) |
| T-41-04 | Information Disclosure | is_dynamic-Filter FE-seitig umgehbar | high | mitigate | Filter server-seitig: nicht-flexible MA → Ok(None), Test attendance_statistics_returns_none_for_static |
| T-41-05 | Tampering | Input year=0 / until_week=99 | low | mitigate | `until_week.min(weeks_in_year(year))`-Clamp (bestehender Pattern aus weekly-statistics) |
| T-41-SC | Tampering | Supply-Chain (neue Pakete) | low | accept | Keine neuen Dependencies |
</threat_model>

<verification>
- `cargo test -p service_impl reporting_attendance_gate` (HR-Gate + is_dynamic)
- `cargo test -p service_impl reporting_avg_weekly` (A-22-1 unverändert)
- `cargo build --workspace` + `cargo clippy --workspace -- -D warnings`
- Grep: `CURRENT_SNAPSHOT_SCHEMA_VERSION.*12` matcht (kein Bump)
</verification>

<success_criteria>
Trait-Methode + Impl vorhanden; HR-Gate first + is_dynamic-Filter server-seitig verifiziert;
Read-Aggregat ohne Persistenz/Snapshot-Bump; Workspace baut, Clippy sauber.
</success_criteria>

<output>
Create `.planning/phases/41-avg-anwesenheit-flexible/41-02-SUMMARY.md` when done
</output>
