---
phase: 05-slot-paid-capacity-warning
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - rest-types/src/lib.rs
autonomous: true
requirements: [D-08, D-09, D-10]
must_haves:
  truths:
    - "SlotTO carries max_paid_employees: Option<u8> with serde(default) for backward-compat"
    - "WarningTO has 5th variant PaidEmployeeLimitExceeded mirroring service::warning::Warning"
    - "From<&service::warning::Warning> for WarningTO covers the new variant exhaustively"
    - "ShiftplanSlotTO carries current_paid_count: u8 mirroring the service-tier struct"
    - "Existing 4 WarningTO variants and existing SlotTO/ShiftplanSlotTO fields are byte-preserved"
  artifacts:
    - path: "rest-types/src/lib.rs"
      provides: "SlotTO + WarningTO + ShiftplanSlotTO extended for Phase 5; From impls cover all new fields/variants"
      contains: "PaidEmployeeLimitExceeded"
  key_links:
    - from: "rest-types::WarningTO::PaidEmployeeLimitExceeded"
      to: "service::warning::Warning::PaidEmployeeLimitExceeded"
      via: "From<&Warning> for WarningTO match arm"
      pattern: "Warning::PaidEmployeeLimitExceeded"
    - from: "rest-types::SlotTO.max_paid_employees"
      to: "service::slot::Slot.max_paid_employees"
      via: "From<&Slot> for SlotTO + From<&SlotTO> for Slot pass-through"
      pattern: "max_paid_employees"
    - from: "rest-types::ShiftplanSlotTO.current_paid_count"
      to: "service::shiftplan::ShiftplanSlot.current_paid_count"
      via: "From<&ShiftplanSlot> for ShiftplanSlotTO pass-through"
      pattern: "current_paid_count"
---

<objective>
Mirror the service-tier additions on the REST DTO surface in `rest-types/src/lib.rs`:
- Add `max_paid_employees: Option<u8>` to `SlotTO` (with `#[serde(default)]` for backward-compat).
- Add the 5th variant `PaidEmployeeLimitExceeded` to `WarningTO` (utoipa `ToSchema`-compatible).
- Add `current_paid_count: u8` to `ShiftplanSlotTO`.
- Update all relevant `From` impls.

Purpose: Closes the wire-surface half of D-08, D-09, D-10. After this plan, REST consumers see the new field on every Slot read/write, the new warning variant in `BookingCreateResultTO.warnings`, and the new count on the shiftplan-week-view. No new endpoints — existing handlers in `rest/src/slot.rs` accept the extended `SlotTO` body without rewrite.

Output: One file (`rest-types/src/lib.rs`) extended with the three additions + their `From` impl bridges.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-slot-paid-capacity-warning/05-CONTEXT.md
@.planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md
@shifty-backend/CLAUDE.md
@rest-types/src/lib.rs
@service/src/slot.rs
@service/src/warning.rs
@service/src/shiftplan.rs

<interfaces>
Existing rest-types surface (per Pattern-Map). Plan 05 only adds; nothing renamed/removed.

`SlotTO` (Pattern-Map lines 305-358):
- Struct with fields: id, day_of_week, from, to, min_resources, valid_from, valid_to, deleted, version, shiftplan_id
- INSERT `max_paid_employees: Option<u8>` after `min_resources` (with `#[serde(default)]`)
- `From<&Slot> for SlotTO` and `From<&SlotTO> for Slot` at lines 326-358

`WarningTO` (Pattern-Map lines 1655-1735):
- 4 variants: BookingOnAbsenceDay, BookingOnUnavailableDay, AbsenceOverlapsBooking, AbsenceOverlapsManualUnavailable
- INSERT `PaidEmployeeLimitExceeded { ... }` as 5th variant (Phase 5)
- `impl From<&service::warning::Warning> for WarningTO` at lines 1692-1735 — needs 5th match arm
- Serde tag/content: `#[serde(tag = "kind", content = "data", rename_all = "snake_case")]` — the new variant becomes wire-tag `paid_employee_limit_exceeded`

`ShiftplanSlotTO` (Pattern-Map line 968 area):
- Struct: slot, bookings
- INSERT `current_paid_count: u8` (Phase 5)
- `impl From<&service::shiftplan::ShiftplanSlot> for ShiftplanSlotTO` at line ~1003-1010

Notes:
- `BlockTO` (lines 1438-1448) embeds `Vec<SlotTO>` — new field cascades automatically. No structural change.
- `BookingCreateResultTO { booking, warnings }` already at line ~1783 with `warnings: Vec<WarningTO>` — new variant cascades automatically once `From<&Warning> for WarningTO` is exhaustive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend SlotTO with max_paid_employees + both From impls</name>
  <read_first>
    - rest-types/src/lib.rs (Pattern-Map lines 305-358)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md section "rest-types/src/lib.rs" subsection "a) Extend SlotTO"
    - service/src/slot.rs (verify the field name and type after Plan 03)
  </read_first>
  <files>rest-types/src/lib.rs</files>
  <action>
    Three edits inside `rest-types/src/lib.rs` for `SlotTO`:

    Edit A. `SlotTO` struct (lines ~305-324): insert a new field immediately after `pub min_resources: u8,`:
    ```rust
    #[serde(default)]
    pub max_paid_employees: Option<u8>,
    ```
    The `#[serde(default)]` is MANDATORY per Pattern-Map Pitfall #8 — existing API consumers that omit the field must continue to deserialize successfully (`Option::default() == None`, matching D-15 "no limit").

    Edit B. `impl From<&Slot> for SlotTO` (lines ~326-340): add the line `max_paid_employees: slot.max_paid_employees,` immediately after the `min_resources:` line. Direct pass-through.

    Edit C. `impl From<&SlotTO> for Slot` (lines ~341-358): add the line `max_paid_employees: to.max_paid_employees,` (match the local variable name used by the surrounding `min_resources:` line — Pattern-Map indicates `to` or `slot`, depending on the file's local convention; match what's already there).

    Do not run any git/jj commands. The user controls all commits manually.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; grep -B 1 "pub max_paid_employees: Option" rest-types/src/lib.rs | grep -q "serde(default)" &amp;&amp; cargo build -p rest-types 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub max_paid_employees: Option" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs` returns 1.
    - The line preceding the field declaration contains `#[serde(default)]`: `grep -B 1 "pub max_paid_employees: Option" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | grep -c "serde(default)"` returns 1.
    - Both `From` impls map the field: `grep -c "max_paid_employees:" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs` returns ≥ 3 (struct field + 2 From-impl assignments — more if cascade through other DTOs that embed SlotTO inline).
    - `cargo build -p rest-types` exits 0.
  </acceptance_criteria>
  <done>
    `SlotTO` carries `max_paid_employees: Option<u8>` (serde-default-protected); both `From` impls bridge the field; `rest-types` compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add WarningTO::PaidEmployeeLimitExceeded variant + From arm</name>
  <read_first>
    - rest-types/src/lib.rs (Pattern-Map lines 1655-1735 — `WarningTO` enum + `From<&Warning>` impl)
    - service/src/warning.rs (after Plan 02 — verify exact field order on the new `Warning::PaidEmployeeLimitExceeded` variant)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md section "rest-types/src/lib.rs" subsection "b) Extend WarningTO"
  </read_first>
  <files>rest-types/src/lib.rs</files>
  <action>
    Two edits in `rest-types/src/lib.rs` for `WarningTO`:

    Edit A. Add the 5th variant inside the `WarningTO` enum (after the existing 4 variants — preserve their order). Use exactly these fields in this exact order (mirrors the service-tier `Warning::PaidEmployeeLimitExceeded` from Plan 02):
    ```rust
    PaidEmployeeLimitExceeded {
        slot_id: Uuid,
        booking_id: Uuid,
        year: u32,
        week: u8,
        current_paid_count: u8,
        max_paid_employees: u8,
    },
    ```
    The serde-tag wire form is auto-derived from the enum-level `#[serde(rename_all = "snake_case")]` attribute → JSON tag becomes `"paid_employee_limit_exceeded"`. utoipa picks up the variant via the existing `ToSchema` derive on the enum — no per-variant annotation needed.

    Edit B. Extend `impl From<&service::warning::Warning> for WarningTO` (lines ~1692-1735) — add a 5th match arm. Use destructuring with reference fields and dereference inline (matches the existing 4 arms' style):
    ```rust
    service::warning::Warning::PaidEmployeeLimitExceeded {
        slot_id, booking_id, year, week, current_paid_count, max_paid_employees,
    } => Self::PaidEmployeeLimitExceeded {
        slot_id: *slot_id,
        booking_id: *booking_id,
        year: *year,
        week: *week,
        current_paid_count: *current_paid_count,
        max_paid_employees: *max_paid_employees,
    },
    ```

    The compiler will enforce match exhaustiveness — if the existing impl uses a `_ =>` wildcard arm, REMOVE the wildcard and add the explicit arm above. Per Pattern-Map: existing arms are explicit (no wildcard), so the new arm is purely additive.

    Do not run any git/jj commands. The user controls all commits manually.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; grep -c "PaidEmployeeLimitExceeded" rest-types/src/lib.rs &amp;&amp; cargo build -p rest-types 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PaidEmployeeLimitExceeded" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs` returns ≥ 3 (enum variant declaration + From-arm pattern + From-arm constructor).
    - The variant declares all 6 expected fields: `grep -A 8 "PaidEmployeeLimitExceeded {" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs | grep -cE "slot_id|booking_id|year|week|current_paid_count|max_paid_employees"` returns ≥ 12 (6 fields × 2 occurrences: enum-decl + From-arm pattern; if From-arm pattern uses different layout, ≥ 6 from the enum decl alone).
    - `cargo build -p rest-types` exits 0 (proves the `From<&Warning>` impl is exhaustive — if it weren't, rustc emits E0004).
    - `cargo build` (workspace) exits 0.
  </acceptance_criteria>
  <done>
    `WarningTO` has 5 variants; `From<&Warning>` is exhaustive; new variant serializes with snake_case tag `paid_employee_limit_exceeded`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend ShiftplanSlotTO with current_paid_count + From impl</name>
  <read_first>
    - rest-types/src/lib.rs (Pattern-Map lines 967-971 + 1003-1010 — struct + From impl)
    - service/src/shiftplan.rs (after Plan 04 — verify `ShiftplanSlot.current_paid_count: u8`)
    - .planning/phases/05-slot-paid-capacity-warning/05-PATTERNS.md section "rest-types/src/lib.rs" subsection "c) Extend ShiftplanSlotTO"
  </read_first>
  <files>rest-types/src/lib.rs</files>
  <action>
    Two edits in `rest-types/src/lib.rs` for `ShiftplanSlotTO`:

    Edit A. `ShiftplanSlotTO` struct (line ~968 area): add `pub current_paid_count: u8,` immediately after `pub bookings: Vec<ShiftplanBookingTO>,`.

    Edit B. `impl From<&service::shiftplan::ShiftplanSlot> for ShiftplanSlotTO` (lines ~1003-1010): add `current_paid_count: slot.current_paid_count,` to the constructor inside `Self { ... }`.

    Plan 04 already declared the service-tier field; this task only mirrors it on the wire. No `#[serde(default)]` needed because the field is non-Optional (D-09: always populated) — old clients that don't know about the field will silently ignore it on read.

    Do not run any git/jj commands. The user controls all commits manually.
  </action>
  <verify>
    <automated>cd /home/neosam/programming/rust/projects/shifty/shifty-backend &amp;&amp; grep -q "pub current_paid_count: u8" rest-types/src/lib.rs &amp;&amp; cargo build 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pub current_paid_count: u8" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs` returns 1.
    - `grep -c "current_paid_count:" /home/neosam/programming/rust/projects/shifty/shifty-backend/rest-types/src/lib.rs` returns ≥ 2 (struct field + From-impl assignment).
    - `cargo build -p rest-types` exits 0.
    - `cargo build` (workspace) exits 0.
  </acceptance_criteria>
  <done>
    `ShiftplanSlotTO.current_paid_count: u8` declared; `From` impl bridges the value; workspace compiles.
  </done>
</task>

</tasks>

<verification>
- `cargo build` (workspace) exits 0 — exhaustiveness of `From<&Warning>` enforced by rustc
- All three additions present: SlotTO field, WarningTO variant + From arm, ShiftplanSlotTO field + From impl
- BlockTO and BookingCreateResultTO automatically inherit the new fields/variant via embedding (no manual edit there)
- No OpenAPI snapshot to update (per Pattern-Map "No Analog Found" + CONTEXT.md "Strikt nicht in Scope": `rest/tests/openapi_snapshot.rs` was deleted in commit fdb70b5)
</verification>

<success_criteria>
- SlotTO carries `max_paid_employees: Option<u8>` with `#[serde(default)]`
- WarningTO has 5th variant matching service-tier Warning
- ShiftplanSlotTO carries `current_paid_count: u8`
- All `From` impls bridge cleanly; workspace compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-slot-paid-capacity-warning/05-05-SUMMARY.md` documenting:
- Exact line numbers for the 3 inserted struct fields and the 5th `WarningTO` variant
- Whether the existing `From<&Warning>` impl had a wildcard arm (and if so, that it was removed)
- Confirmation that `BlockTO` and `BookingCreateResultTO` were NOT touched (cascade through embedding)
- Wire-form sanity: paste the JSON shape `{"kind":"paid_employee_limit_exceeded","data":{...}}` for the record
</output>
